{
  "name": "claude-compliance-guard",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:clean": "rm -rf dist && tsc",
    "test": "node scripts/run-tests.mjs",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
