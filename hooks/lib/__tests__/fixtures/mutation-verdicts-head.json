{
  "capturedAt": "38c5123",
  "root": "/project",
  "protectedPaths": [
    "agents/**",
    "rules/**",
    "hooks/config.json",
    "hooks/hooks.json",
    "settings.json",
    "bin/monitor",
    "skills/**",
    ".claude-plugin/plugin.json",
    "fusion-workbench/.guard-state/**"
  ],
  "rows": [
    {
      "id": "none/nojoin/cd/if/rm/protected-relative",
      "command": "if cd rules; then rm x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/rm/protected-absolute",
      "command": "if cd build; then rm /project/rules/x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `then rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/rm/unprotected-relative",
      "command": "if cd build; then rm out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/if/rm/unprotected-absolute",
      "command": "if cd build; then rm /project/build/out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/if/rm-rf/protected-relative",
      "command": "if cd rules; then rm -rf x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/rm-rf/protected-absolute",
      "command": "if cd build; then rm -rf /project/rules/x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `then rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/rm-rf/unprotected-relative",
      "command": "if cd build; then rm -rf out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/if/rm-rf/unprotected-absolute",
      "command": "if cd build; then rm -rf /project/build/out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/if/mv/protected-relative",
      "command": "if cd rules; then mv x.md x.md.moved; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/mv/protected-absolute",
      "command": "if cd build; then mv /project/rules/x.md /project/rules/x.md.moved; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `then mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/mv/unprotected-relative",
      "command": "if cd build; then mv out.js out.js.moved; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/if/mv/unprotected-absolute",
      "command": "if cd build; then mv /project/build/out.js /project/build/out.js.moved; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/if/sed-i/protected-relative",
      "command": "if cd rules; then sed -i.bak -e 's/seed/changed/' x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/sed-i/protected-absolute",
      "command": "if cd build; then sed -i.bak -e 's/seed/changed/' /project/rules/x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `then sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/sed-i/unprotected-relative",
      "command": "if cd build; then sed -i.bak -e 's/seed/changed/' out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/if/sed-i/unprotected-absolute",
      "command": "if cd build; then sed -i.bak -e 's/seed/changed/' /project/build/out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/if/cp/protected-relative",
      "command": "if cd rules; then cp /project/notes.txt x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/cp/protected-absolute",
      "command": "if cd build; then cp /project/notes.txt /project/rules/x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `then cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/cp/unprotected-relative",
      "command": "if cd build; then cp /project/notes.txt out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/if/cp/unprotected-absolute",
      "command": "if cd build; then cp /project/notes.txt /project/build/out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/if/redirect/protected-relative",
      "command": "if cd rules; then echo overwritten > x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/redirect/protected-absolute",
      "command": "if cd build; then echo overwritten > /project/rules/x.md; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `then echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/redirect/unprotected-relative",
      "command": "if cd build; then echo overwritten > out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/if/redirect/unprotected-absolute",
      "command": "if cd build; then echo overwritten > /project/build/out.js; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/if/tee/protected-relative",
      "command": "if cd rules; then tee x.md < /dev/null; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/tee/protected-absolute",
      "command": "if cd build; then tee /project/rules/x.md < /dev/null; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `then tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/if/tee/unprotected-relative",
      "command": "if cd build; then tee out.js < /dev/null; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/if/tee/unprotected-absolute",
      "command": "if cd build; then tee /project/build/out.js < /dev/null; fi",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `then tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "then tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "none/nojoin/cd/while/rm/protected-relative",
      "command": "while cd rules; do rm x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/rm/protected-absolute",
      "command": "while cd build; do rm /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/rm/unprotected-relative",
      "command": "while cd build; do rm out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/while/rm/unprotected-absolute",
      "command": "while cd build; do rm /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/while/rm-rf/protected-relative",
      "command": "while cd rules; do rm -rf x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/rm-rf/protected-absolute",
      "command": "while cd build; do rm -rf /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/rm-rf/unprotected-relative",
      "command": "while cd build; do rm -rf out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/while/rm-rf/unprotected-absolute",
      "command": "while cd build; do rm -rf /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/while/mv/protected-relative",
      "command": "while cd rules; do mv x.md x.md.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/mv/protected-absolute",
      "command": "while cd build; do mv /project/rules/x.md /project/rules/x.md.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/mv/unprotected-relative",
      "command": "while cd build; do mv out.js out.js.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/while/mv/unprotected-absolute",
      "command": "while cd build; do mv /project/build/out.js /project/build/out.js.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/while/sed-i/protected-relative",
      "command": "while cd rules; do sed -i.bak -e 's/seed/changed/' x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/sed-i/protected-absolute",
      "command": "while cd build; do sed -i.bak -e 's/seed/changed/' /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/sed-i/unprotected-relative",
      "command": "while cd build; do sed -i.bak -e 's/seed/changed/' out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/while/sed-i/unprotected-absolute",
      "command": "while cd build; do sed -i.bak -e 's/seed/changed/' /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/while/cp/protected-relative",
      "command": "while cd rules; do cp /project/notes.txt x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/cp/protected-absolute",
      "command": "while cd build; do cp /project/notes.txt /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/cp/unprotected-relative",
      "command": "while cd build; do cp /project/notes.txt out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/while/cp/unprotected-absolute",
      "command": "while cd build; do cp /project/notes.txt /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/while/redirect/protected-relative",
      "command": "while cd rules; do echo overwritten > x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/redirect/protected-absolute",
      "command": "while cd build; do echo overwritten > /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/redirect/unprotected-relative",
      "command": "while cd build; do echo overwritten > out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/while/redirect/unprotected-absolute",
      "command": "while cd build; do echo overwritten > /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/while/tee/protected-relative",
      "command": "while cd rules; do tee x.md < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/tee/protected-absolute",
      "command": "while cd build; do tee /project/rules/x.md < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/while/tee/unprotected-relative",
      "command": "while cd build; do tee out.js < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/while/tee/unprotected-absolute",
      "command": "while cd build; do tee /project/build/out.js < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "none/nojoin/cd/until/rm/protected-relative",
      "command": "until cd rules; do rm x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/rm/protected-absolute",
      "command": "until cd build; do rm /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/rm/unprotected-relative",
      "command": "until cd build; do rm out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/until/rm/unprotected-absolute",
      "command": "until cd build; do rm /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/until/rm-rf/protected-relative",
      "command": "until cd rules; do rm -rf x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/rm-rf/protected-absolute",
      "command": "until cd build; do rm -rf /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/rm-rf/unprotected-relative",
      "command": "until cd build; do rm -rf out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/until/rm-rf/unprotected-absolute",
      "command": "until cd build; do rm -rf /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/until/mv/protected-relative",
      "command": "until cd rules; do mv x.md x.md.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/mv/protected-absolute",
      "command": "until cd build; do mv /project/rules/x.md /project/rules/x.md.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/mv/unprotected-relative",
      "command": "until cd build; do mv out.js out.js.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/until/mv/unprotected-absolute",
      "command": "until cd build; do mv /project/build/out.js /project/build/out.js.moved; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/until/sed-i/protected-relative",
      "command": "until cd rules; do sed -i.bak -e 's/seed/changed/' x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/sed-i/protected-absolute",
      "command": "until cd build; do sed -i.bak -e 's/seed/changed/' /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/sed-i/unprotected-relative",
      "command": "until cd build; do sed -i.bak -e 's/seed/changed/' out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/until/sed-i/unprotected-absolute",
      "command": "until cd build; do sed -i.bak -e 's/seed/changed/' /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/until/cp/protected-relative",
      "command": "until cd rules; do cp /project/notes.txt x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/cp/protected-absolute",
      "command": "until cd build; do cp /project/notes.txt /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/cp/unprotected-relative",
      "command": "until cd build; do cp /project/notes.txt out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/until/cp/unprotected-absolute",
      "command": "until cd build; do cp /project/notes.txt /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/until/redirect/protected-relative",
      "command": "until cd rules; do echo overwritten > x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/redirect/protected-absolute",
      "command": "until cd build; do echo overwritten > /project/rules/x.md; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/redirect/unprotected-relative",
      "command": "until cd build; do echo overwritten > out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/until/redirect/unprotected-absolute",
      "command": "until cd build; do echo overwritten > /project/build/out.js; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/until/tee/protected-relative",
      "command": "until cd rules; do tee x.md < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/tee/protected-absolute",
      "command": "until cd build; do tee /project/rules/x.md < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `do tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/until/tee/unprotected-relative",
      "command": "until cd build; do tee out.js < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/until/tee/unprotected-absolute",
      "command": "until cd build; do tee /project/build/out.js < /dev/null; done",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `do tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "do tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm/protected-relative",
      "command": "{ cd rules; } && rm x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm/protected-absolute",
      "command": "{ cd build; } && rm /project/rules/x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm/unprotected-relative",
      "command": "{ cd build; } && rm out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm/unprotected-absolute",
      "command": "{ cd build; } && rm /project/build/out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm-rf/protected-relative",
      "command": "{ cd rules; } && rm -rf x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm-rf/protected-absolute",
      "command": "{ cd build; } && rm -rf /project/rules/x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm-rf/unprotected-relative",
      "command": "{ cd build; } && rm -rf out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/brace/rm-rf/unprotected-absolute",
      "command": "{ cd build; } && rm -rf /project/build/out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/brace/mv/protected-relative",
      "command": "{ cd rules; } && mv x.md x.md.moved",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/mv/protected-absolute",
      "command": "{ cd build; } && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/mv/unprotected-relative",
      "command": "{ cd build; } && mv out.js out.js.moved",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/brace/mv/unprotected-absolute",
      "command": "{ cd build; } && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/brace/sed-i/protected-relative",
      "command": "{ cd rules; } && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/sed-i/protected-absolute",
      "command": "{ cd build; } && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/sed-i/unprotected-relative",
      "command": "{ cd build; } && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/brace/sed-i/unprotected-absolute",
      "command": "{ cd build; } && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/brace/cp/protected-relative",
      "command": "{ cd rules; } && cp /project/notes.txt x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/cp/protected-absolute",
      "command": "{ cd build; } && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/cp/unprotected-relative",
      "command": "{ cd build; } && cp /project/notes.txt out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/brace/cp/unprotected-absolute",
      "command": "{ cd build; } && cp /project/notes.txt /project/build/out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/brace/redirect/protected-relative",
      "command": "{ cd rules; } && echo overwritten > x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/redirect/protected-absolute",
      "command": "{ cd build; } && echo overwritten > /project/rules/x.md",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/redirect/unprotected-relative",
      "command": "{ cd build; } && echo overwritten > out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/brace/redirect/unprotected-absolute",
      "command": "{ cd build; } && echo overwritten > /project/build/out.js",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/brace/tee/protected-relative",
      "command": "{ cd rules; } && tee x.md < /dev/null",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/tee/protected-absolute",
      "command": "{ cd build; } && tee /project/rules/x.md < /dev/null",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/brace/tee/unprotected-relative",
      "command": "{ cd build; } && tee out.js < /dev/null",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/brace/tee/unprotected-absolute",
      "command": "{ cd build; } && tee /project/build/out.js < /dev/null",
      "families": [
        "compound"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm/protected-relative",
      "command": "cd rules && echo hi | rm x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm/protected-absolute",
      "command": "cd build && echo hi | rm /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm/unprotected-relative",
      "command": "cd build && echo hi | rm out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm/unprotected-absolute",
      "command": "cd build && echo hi | rm /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm-rf/protected-relative",
      "command": "cd rules && echo hi | rm -rf x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm-rf/protected-absolute",
      "command": "cd build && echo hi | rm -rf /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm-rf/unprotected-relative",
      "command": "cd build && echo hi | rm -rf out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/rm-rf/unprotected-absolute",
      "command": "cd build && echo hi | rm -rf /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/mv/protected-relative",
      "command": "cd rules && echo hi | mv x.md x.md.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/mv/protected-absolute",
      "command": "cd build && echo hi | mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/mv/unprotected-relative",
      "command": "cd build && echo hi | mv out.js out.js.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/mv/unprotected-absolute",
      "command": "cd build && echo hi | mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/sed-i/protected-relative",
      "command": "cd rules && echo hi | sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/sed-i/protected-absolute",
      "command": "cd build && echo hi | sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/sed-i/unprotected-relative",
      "command": "cd build && echo hi | sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/sed-i/unprotected-absolute",
      "command": "cd build && echo hi | sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/cp/protected-relative",
      "command": "cd rules && echo hi | cp /project/notes.txt x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/cp/protected-absolute",
      "command": "cd build && echo hi | cp /project/notes.txt /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/cp/unprotected-relative",
      "command": "cd build && echo hi | cp /project/notes.txt out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/cp/unprotected-absolute",
      "command": "cd build && echo hi | cp /project/notes.txt /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/redirect/protected-relative",
      "command": "cd rules && echo hi | cat > x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cat > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cat > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/redirect/protected-absolute",
      "command": "cd build && echo hi | cat > /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cat > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cat > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/redirect/unprotected-relative",
      "command": "cd build && echo hi | cat > out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cat > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cat > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/redirect/unprotected-absolute",
      "command": "cd build && echo hi | cat > /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/tee/protected-relative",
      "command": "cd rules && echo hi | tee x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/tee/protected-absolute",
      "command": "cd build && echo hi | tee /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/tee/unprotected-relative",
      "command": "cd build && echo hi | tee out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipeline/tee/unprotected-absolute",
      "command": "cd build && echo hi | tee /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm/protected-relative",
      "command": "cd rules | cat && rm x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm/protected-absolute",
      "command": "cd build | cat && rm /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm/unprotected-relative",
      "command": "cd build | cat && rm out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm/unprotected-absolute",
      "command": "cd build | cat && rm /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm-rf/protected-relative",
      "command": "cd rules | cat && rm -rf x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm-rf/protected-absolute",
      "command": "cd build | cat && rm -rf /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm-rf/unprotected-relative",
      "command": "cd build | cat && rm -rf out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/rm-rf/unprotected-absolute",
      "command": "cd build | cat && rm -rf /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/mv/protected-relative",
      "command": "cd rules | cat && mv x.md x.md.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/mv/protected-absolute",
      "command": "cd build | cat && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/mv/unprotected-relative",
      "command": "cd build | cat && mv out.js out.js.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/mv/unprotected-absolute",
      "command": "cd build | cat && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/sed-i/protected-relative",
      "command": "cd rules | cat && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/sed-i/protected-absolute",
      "command": "cd build | cat && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/sed-i/unprotected-relative",
      "command": "cd build | cat && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/sed-i/unprotected-absolute",
      "command": "cd build | cat && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/cp/protected-relative",
      "command": "cd rules | cat && cp /project/notes.txt x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/cp/protected-absolute",
      "command": "cd build | cat && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/cp/unprotected-relative",
      "command": "cd build | cat && cp /project/notes.txt out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/cp/unprotected-absolute",
      "command": "cd build | cat && cp /project/notes.txt /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/redirect/protected-relative",
      "command": "cd rules | cat && echo overwritten > x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/redirect/protected-absolute",
      "command": "cd build | cat && echo overwritten > /project/rules/x.md",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/redirect/unprotected-relative",
      "command": "cd build | cat && echo overwritten > out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/redirect/unprotected-absolute",
      "command": "cd build | cat && echo overwritten > /project/build/out.js",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/tee/protected-relative",
      "command": "cd rules | cat && tee x.md < /dev/null",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/tee/protected-absolute",
      "command": "cd build | cat && tee /project/rules/x.md < /dev/null",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/tee/unprotected-relative",
      "command": "cd build | cat && tee out.js < /dev/null",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "none/nojoin/cd/pipe-head/tee/unprotected-absolute",
      "command": "cd build | cat && tee /project/build/out.js < /dev/null",
      "families": [
        "pipeline"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "true/or/cd/bare/rm/protected-relative",
      "command": "true || cd rules && rm x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/or/cd/bare/rm/protected-absolute",
      "command": "true || cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/or/cd/bare/rm/unprotected-relative",
      "command": "true || cd build && rm out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/or/cd/bare/rm/unprotected-absolute",
      "command": "true || cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/or/cd/bare/rm-rf/protected-relative",
      "command": "true || cd rules && rm -rf x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/or/cd/bare/rm-rf/protected-absolute",
      "command": "true || cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/or/cd/bare/rm-rf/unprotected-relative",
      "command": "true || cd build && rm -rf out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/or/cd/bare/rm-rf/unprotected-absolute",
      "command": "true || cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/or/cd/bare/mv/protected-relative",
      "command": "true || cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/or/cd/bare/mv/protected-absolute",
      "command": "true || cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/or/cd/bare/mv/unprotected-relative",
      "command": "true || cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/or/cd/bare/mv/unprotected-absolute",
      "command": "true || cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/or/cd/bare/sed-i/protected-relative",
      "command": "true || cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/or/cd/bare/sed-i/protected-absolute",
      "command": "true || cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/or/cd/bare/sed-i/unprotected-relative",
      "command": "true || cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/or/cd/bare/sed-i/unprotected-absolute",
      "command": "true || cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/or/cd/bare/cp/protected-relative",
      "command": "true || cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/or/cd/bare/cp/protected-absolute",
      "command": "true || cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/or/cd/bare/cp/unprotected-relative",
      "command": "true || cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/or/cd/bare/cp/unprotected-absolute",
      "command": "true || cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/or/cd/bare/redirect/protected-relative",
      "command": "true || cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/or/cd/bare/redirect/protected-absolute",
      "command": "true || cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/or/cd/bare/redirect/unprotected-relative",
      "command": "true || cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/or/cd/bare/redirect/unprotected-absolute",
      "command": "true || cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/or/cd/bare/tee/protected-relative",
      "command": "true || cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/or/cd/bare/tee/protected-absolute",
      "command": "true || cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/or/cd/bare/tee/unprotected-relative",
      "command": "true || cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/or/cd/bare/tee/unprotected-absolute",
      "command": "true || cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "true/pipe/cd/bare/rm/protected-relative",
      "command": "true | cd rules && rm x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/rm/protected-absolute",
      "command": "true | cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/rm/unprotected-relative",
      "command": "true | cd build && rm out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/pipe/cd/bare/rm/unprotected-absolute",
      "command": "true | cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/pipe/cd/bare/rm-rf/protected-relative",
      "command": "true | cd rules && rm -rf x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/rm-rf/protected-absolute",
      "command": "true | cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/rm-rf/unprotected-relative",
      "command": "true | cd build && rm -rf out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/pipe/cd/bare/rm-rf/unprotected-absolute",
      "command": "true | cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/pipe/cd/bare/mv/protected-relative",
      "command": "true | cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/mv/protected-absolute",
      "command": "true | cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/mv/unprotected-relative",
      "command": "true | cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/pipe/cd/bare/mv/unprotected-absolute",
      "command": "true | cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/pipe/cd/bare/sed-i/protected-relative",
      "command": "true | cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/sed-i/protected-absolute",
      "command": "true | cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/sed-i/unprotected-relative",
      "command": "true | cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/pipe/cd/bare/sed-i/unprotected-absolute",
      "command": "true | cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/pipe/cd/bare/cp/protected-relative",
      "command": "true | cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/cp/protected-absolute",
      "command": "true | cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/cp/unprotected-relative",
      "command": "true | cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/pipe/cd/bare/cp/unprotected-absolute",
      "command": "true | cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/pipe/cd/bare/redirect/protected-relative",
      "command": "true | cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/redirect/protected-absolute",
      "command": "true | cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/redirect/unprotected-relative",
      "command": "true | cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/pipe/cd/bare/redirect/unprotected-absolute",
      "command": "true | cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "true/pipe/cd/bare/tee/protected-relative",
      "command": "true | cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/tee/protected-absolute",
      "command": "true | cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "true/pipe/cd/bare/tee/unprotected-relative",
      "command": "true | cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "true/pipe/cd/bare/tee/unprotected-absolute",
      "command": "true | cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "false/or/cd/bare/rm/protected-relative",
      "command": "false || cd rules && rm x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/or/cd/bare/rm/protected-absolute",
      "command": "false || cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/or/cd/bare/rm/unprotected-relative",
      "command": "false || cd build && rm out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/or/cd/bare/rm/unprotected-absolute",
      "command": "false || cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/or/cd/bare/rm-rf/protected-relative",
      "command": "false || cd rules && rm -rf x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/or/cd/bare/rm-rf/protected-absolute",
      "command": "false || cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/or/cd/bare/rm-rf/unprotected-relative",
      "command": "false || cd build && rm -rf out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/or/cd/bare/rm-rf/unprotected-absolute",
      "command": "false || cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/or/cd/bare/mv/protected-relative",
      "command": "false || cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/or/cd/bare/mv/protected-absolute",
      "command": "false || cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/or/cd/bare/mv/unprotected-relative",
      "command": "false || cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/or/cd/bare/mv/unprotected-absolute",
      "command": "false || cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/or/cd/bare/sed-i/protected-relative",
      "command": "false || cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/or/cd/bare/sed-i/protected-absolute",
      "command": "false || cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/or/cd/bare/sed-i/unprotected-relative",
      "command": "false || cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/or/cd/bare/sed-i/unprotected-absolute",
      "command": "false || cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/or/cd/bare/cp/protected-relative",
      "command": "false || cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/or/cd/bare/cp/protected-absolute",
      "command": "false || cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/or/cd/bare/cp/unprotected-relative",
      "command": "false || cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/or/cd/bare/cp/unprotected-absolute",
      "command": "false || cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/or/cd/bare/redirect/protected-relative",
      "command": "false || cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/or/cd/bare/redirect/protected-absolute",
      "command": "false || cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/or/cd/bare/redirect/unprotected-relative",
      "command": "false || cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/or/cd/bare/redirect/unprotected-absolute",
      "command": "false || cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/or/cd/bare/tee/protected-relative",
      "command": "false || cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/or/cd/bare/tee/protected-absolute",
      "command": "false || cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/or/cd/bare/tee/unprotected-relative",
      "command": "false || cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/or/cd/bare/tee/unprotected-absolute",
      "command": "false || cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "false/pipe/cd/bare/rm/protected-relative",
      "command": "false | cd rules && rm x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/rm/protected-absolute",
      "command": "false | cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/rm/unprotected-relative",
      "command": "false | cd build && rm out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/pipe/cd/bare/rm/unprotected-absolute",
      "command": "false | cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/pipe/cd/bare/rm-rf/protected-relative",
      "command": "false | cd rules && rm -rf x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/rm-rf/protected-absolute",
      "command": "false | cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/rm-rf/unprotected-relative",
      "command": "false | cd build && rm -rf out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/pipe/cd/bare/rm-rf/unprotected-absolute",
      "command": "false | cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/pipe/cd/bare/mv/protected-relative",
      "command": "false | cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/mv/protected-absolute",
      "command": "false | cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/mv/unprotected-relative",
      "command": "false | cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/pipe/cd/bare/mv/unprotected-absolute",
      "command": "false | cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/pipe/cd/bare/sed-i/protected-relative",
      "command": "false | cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/sed-i/protected-absolute",
      "command": "false | cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/sed-i/unprotected-relative",
      "command": "false | cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/pipe/cd/bare/sed-i/unprotected-absolute",
      "command": "false | cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/pipe/cd/bare/cp/protected-relative",
      "command": "false | cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/cp/protected-absolute",
      "command": "false | cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/cp/unprotected-relative",
      "command": "false | cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/pipe/cd/bare/cp/unprotected-absolute",
      "command": "false | cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/pipe/cd/bare/redirect/protected-relative",
      "command": "false | cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/redirect/protected-absolute",
      "command": "false | cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/redirect/unprotected-relative",
      "command": "false | cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/pipe/cd/bare/redirect/unprotected-absolute",
      "command": "false | cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "false/pipe/cd/bare/tee/protected-relative",
      "command": "false | cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/tee/protected-absolute",
      "command": "false | cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "false/pipe/cd/bare/tee/unprotected-relative",
      "command": "false | cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "false/pipe/cd/bare/tee/unprotected-absolute",
      "command": "false | cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "test-d/or/cd/bare/rm/protected-relative",
      "command": "[ -d nope ] || cd rules && rm x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/rm/protected-absolute",
      "command": "[ -d nope ] || cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/rm/unprotected-relative",
      "command": "[ -d nope ] || cd build && rm out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/or/cd/bare/rm/unprotected-absolute",
      "command": "[ -d nope ] || cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/or/cd/bare/rm-rf/protected-relative",
      "command": "[ -d nope ] || cd rules && rm -rf x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/rm-rf/protected-absolute",
      "command": "[ -d nope ] || cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/rm-rf/unprotected-relative",
      "command": "[ -d nope ] || cd build && rm -rf out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/or/cd/bare/rm-rf/unprotected-absolute",
      "command": "[ -d nope ] || cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/or/cd/bare/mv/protected-relative",
      "command": "[ -d nope ] || cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/mv/protected-absolute",
      "command": "[ -d nope ] || cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/mv/unprotected-relative",
      "command": "[ -d nope ] || cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/or/cd/bare/mv/unprotected-absolute",
      "command": "[ -d nope ] || cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/or/cd/bare/sed-i/protected-relative",
      "command": "[ -d nope ] || cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/sed-i/protected-absolute",
      "command": "[ -d nope ] || cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/sed-i/unprotected-relative",
      "command": "[ -d nope ] || cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/or/cd/bare/sed-i/unprotected-absolute",
      "command": "[ -d nope ] || cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/or/cd/bare/cp/protected-relative",
      "command": "[ -d nope ] || cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/cp/protected-absolute",
      "command": "[ -d nope ] || cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/cp/unprotected-relative",
      "command": "[ -d nope ] || cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/or/cd/bare/cp/unprotected-absolute",
      "command": "[ -d nope ] || cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/or/cd/bare/redirect/protected-relative",
      "command": "[ -d nope ] || cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/redirect/protected-absolute",
      "command": "[ -d nope ] || cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/redirect/unprotected-relative",
      "command": "[ -d nope ] || cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/or/cd/bare/redirect/unprotected-absolute",
      "command": "[ -d nope ] || cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/or/cd/bare/tee/protected-relative",
      "command": "[ -d nope ] || cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/tee/protected-absolute",
      "command": "[ -d nope ] || cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/or/cd/bare/tee/unprotected-relative",
      "command": "[ -d nope ] || cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/or/cd/bare/tee/unprotected-absolute",
      "command": "[ -d nope ] || cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm/protected-relative",
      "command": "[ -d nope ] | cd rules && rm x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm/protected-absolute",
      "command": "[ -d nope ] | cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm/unprotected-relative",
      "command": "[ -d nope ] | cd build && rm out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm/unprotected-absolute",
      "command": "[ -d nope ] | cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm-rf/protected-relative",
      "command": "[ -d nope ] | cd rules && rm -rf x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm-rf/protected-absolute",
      "command": "[ -d nope ] | cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm-rf/unprotected-relative",
      "command": "[ -d nope ] | cd build && rm -rf out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/rm-rf/unprotected-absolute",
      "command": "[ -d nope ] | cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/pipe/cd/bare/mv/protected-relative",
      "command": "[ -d nope ] | cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/mv/protected-absolute",
      "command": "[ -d nope ] | cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/mv/unprotected-relative",
      "command": "[ -d nope ] | cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/mv/unprotected-absolute",
      "command": "[ -d nope ] | cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/pipe/cd/bare/sed-i/protected-relative",
      "command": "[ -d nope ] | cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/sed-i/protected-absolute",
      "command": "[ -d nope ] | cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/sed-i/unprotected-relative",
      "command": "[ -d nope ] | cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/sed-i/unprotected-absolute",
      "command": "[ -d nope ] | cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/pipe/cd/bare/cp/protected-relative",
      "command": "[ -d nope ] | cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/cp/protected-absolute",
      "command": "[ -d nope ] | cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/cp/unprotected-relative",
      "command": "[ -d nope ] | cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/cp/unprotected-absolute",
      "command": "[ -d nope ] | cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/pipe/cd/bare/redirect/protected-relative",
      "command": "[ -d nope ] | cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/redirect/protected-absolute",
      "command": "[ -d nope ] | cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/redirect/unprotected-relative",
      "command": "[ -d nope ] | cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/redirect/unprotected-absolute",
      "command": "[ -d nope ] | cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "test-d/pipe/cd/bare/tee/protected-relative",
      "command": "[ -d nope ] | cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/tee/protected-absolute",
      "command": "[ -d nope ] | cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/tee/unprotected-relative",
      "command": "[ -d nope ] | cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "test-d/pipe/cd/bare/tee/unprotected-absolute",
      "command": "[ -d nope ] | cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "echo/or/cd/bare/rm/protected-relative",
      "command": "echo hi || cd rules && rm x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/rm/protected-absolute",
      "command": "echo hi || cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/rm/unprotected-relative",
      "command": "echo hi || cd build && rm out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/or/cd/bare/rm/unprotected-absolute",
      "command": "echo hi || cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/or/cd/bare/rm-rf/protected-relative",
      "command": "echo hi || cd rules && rm -rf x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/rm-rf/protected-absolute",
      "command": "echo hi || cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/rm-rf/unprotected-relative",
      "command": "echo hi || cd build && rm -rf out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/or/cd/bare/rm-rf/unprotected-absolute",
      "command": "echo hi || cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/or/cd/bare/mv/protected-relative",
      "command": "echo hi || cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/mv/protected-absolute",
      "command": "echo hi || cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/mv/unprotected-relative",
      "command": "echo hi || cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/or/cd/bare/mv/unprotected-absolute",
      "command": "echo hi || cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/or/cd/bare/sed-i/protected-relative",
      "command": "echo hi || cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/sed-i/protected-absolute",
      "command": "echo hi || cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/sed-i/unprotected-relative",
      "command": "echo hi || cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/or/cd/bare/sed-i/unprotected-absolute",
      "command": "echo hi || cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/or/cd/bare/cp/protected-relative",
      "command": "echo hi || cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/cp/protected-absolute",
      "command": "echo hi || cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/cp/unprotected-relative",
      "command": "echo hi || cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/or/cd/bare/cp/unprotected-absolute",
      "command": "echo hi || cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/or/cd/bare/redirect/protected-relative",
      "command": "echo hi || cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/redirect/protected-absolute",
      "command": "echo hi || cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/redirect/unprotected-relative",
      "command": "echo hi || cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/or/cd/bare/redirect/unprotected-absolute",
      "command": "echo hi || cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/or/cd/bare/tee/protected-relative",
      "command": "echo hi || cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/tee/protected-absolute",
      "command": "echo hi || cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/or/cd/bare/tee/unprotected-relative",
      "command": "echo hi || cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/or/cd/bare/tee/unprotected-absolute",
      "command": "echo hi || cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm/protected-relative",
      "command": "echo hi | cd rules && rm x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm/protected-absolute",
      "command": "echo hi | cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm/unprotected-relative",
      "command": "echo hi | cd build && rm out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm/unprotected-absolute",
      "command": "echo hi | cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm-rf/protected-relative",
      "command": "echo hi | cd rules && rm -rf x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm-rf/protected-absolute",
      "command": "echo hi | cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm-rf/unprotected-relative",
      "command": "echo hi | cd build && rm -rf out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/pipe/cd/bare/rm-rf/unprotected-absolute",
      "command": "echo hi | cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/pipe/cd/bare/mv/protected-relative",
      "command": "echo hi | cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/mv/protected-absolute",
      "command": "echo hi | cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/mv/unprotected-relative",
      "command": "echo hi | cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/pipe/cd/bare/mv/unprotected-absolute",
      "command": "echo hi | cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/pipe/cd/bare/sed-i/protected-relative",
      "command": "echo hi | cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/sed-i/protected-absolute",
      "command": "echo hi | cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/sed-i/unprotected-relative",
      "command": "echo hi | cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/pipe/cd/bare/sed-i/unprotected-absolute",
      "command": "echo hi | cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/pipe/cd/bare/cp/protected-relative",
      "command": "echo hi | cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/cp/protected-absolute",
      "command": "echo hi | cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/cp/unprotected-relative",
      "command": "echo hi | cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/pipe/cd/bare/cp/unprotected-absolute",
      "command": "echo hi | cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/pipe/cd/bare/redirect/protected-relative",
      "command": "echo hi | cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/redirect/protected-absolute",
      "command": "echo hi | cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/redirect/unprotected-relative",
      "command": "echo hi | cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/pipe/cd/bare/redirect/unprotected-absolute",
      "command": "echo hi | cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "echo/pipe/cd/bare/tee/protected-relative",
      "command": "echo hi | cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/tee/protected-absolute",
      "command": "echo hi | cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "echo/pipe/cd/bare/tee/unprotected-relative",
      "command": "echo hi | cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "echo/pipe/cd/bare/tee/unprotected-absolute",
      "command": "echo hi | cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "ls/or/cd/bare/rm/protected-relative",
      "command": "ls || cd rules && rm x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/rm/protected-absolute",
      "command": "ls || cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/rm/unprotected-relative",
      "command": "ls || cd build && rm out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/or/cd/bare/rm/unprotected-absolute",
      "command": "ls || cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/or/cd/bare/rm-rf/protected-relative",
      "command": "ls || cd rules && rm -rf x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/rm-rf/protected-absolute",
      "command": "ls || cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/rm-rf/unprotected-relative",
      "command": "ls || cd build && rm -rf out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/or/cd/bare/rm-rf/unprotected-absolute",
      "command": "ls || cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/or/cd/bare/mv/protected-relative",
      "command": "ls || cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/mv/protected-absolute",
      "command": "ls || cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/mv/unprotected-relative",
      "command": "ls || cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/or/cd/bare/mv/unprotected-absolute",
      "command": "ls || cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/or/cd/bare/sed-i/protected-relative",
      "command": "ls || cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/sed-i/protected-absolute",
      "command": "ls || cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/sed-i/unprotected-relative",
      "command": "ls || cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/or/cd/bare/sed-i/unprotected-absolute",
      "command": "ls || cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/or/cd/bare/cp/protected-relative",
      "command": "ls || cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/cp/protected-absolute",
      "command": "ls || cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/cp/unprotected-relative",
      "command": "ls || cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/or/cd/bare/cp/unprotected-absolute",
      "command": "ls || cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/or/cd/bare/redirect/protected-relative",
      "command": "ls || cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/redirect/protected-absolute",
      "command": "ls || cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/redirect/unprotected-relative",
      "command": "ls || cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/or/cd/bare/redirect/unprotected-absolute",
      "command": "ls || cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/or/cd/bare/tee/protected-relative",
      "command": "ls || cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/tee/protected-absolute",
      "command": "ls || cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/or/cd/bare/tee/unprotected-relative",
      "command": "ls || cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/or/cd/bare/tee/unprotected-absolute",
      "command": "ls || cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "or-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm/protected-relative",
      "command": "ls | cd rules && rm x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm/protected-absolute",
      "command": "ls | cd build && rm /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm/unprotected-relative",
      "command": "ls | cd build && rm out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm/unprotected-absolute",
      "command": "ls | cd build && rm /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm-rf/protected-relative",
      "command": "ls | cd rules && rm -rf x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm-rf/protected-absolute",
      "command": "ls | cd build && rm -rf /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `rm -rf /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm-rf/unprotected-relative",
      "command": "ls | cd build && rm -rf out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `rm -rf out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "rm -rf out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/pipe/cd/bare/rm-rf/unprotected-absolute",
      "command": "ls | cd build && rm -rf /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/pipe/cd/bare/mv/protected-relative",
      "command": "ls | cd rules && mv x.md x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv x.md x.md.moved` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv x.md x.md.moved",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/mv/protected-absolute",
      "command": "ls | cd build && mv /project/rules/x.md /project/rules/x.md.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `mv /project/rules/x.md /project/rules/x.md.moved` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv /project/rules/x.md /project/rules/x.md.moved",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/mv/unprotected-relative",
      "command": "ls | cd build && mv out.js out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `mv out.js out.js.moved` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "mv out.js out.js.moved",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/pipe/cd/bare/mv/unprotected-absolute",
      "command": "ls | cd build && mv /project/build/out.js /project/build/out.js.moved",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/pipe/cd/bare/sed-i/protected-relative",
      "command": "ls | cd rules && sed -i.bak -e 's/seed/changed/' x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/sed-i/protected-absolute",
      "command": "ls | cd build && sed -i.bak -e 's/seed/changed/' /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `sed -i.bak -e s/seed/changed/ /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/sed-i/unprotected-relative",
      "command": "ls | cd build && sed -i.bak -e 's/seed/changed/' out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `sed -i.bak -e s/seed/changed/ out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "sed -i.bak -e s/seed/changed/ out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/pipe/cd/bare/sed-i/unprotected-absolute",
      "command": "ls | cd build && sed -i.bak -e 's/seed/changed/' /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/pipe/cd/bare/cp/protected-relative",
      "command": "ls | cd rules && cp /project/notes.txt x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/cp/protected-absolute",
      "command": "ls | cd build && cp /project/notes.txt /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `cp /project/notes.txt /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/cp/unprotected-relative",
      "command": "ls | cd build && cp /project/notes.txt out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `cp /project/notes.txt out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "cp /project/notes.txt out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/pipe/cd/bare/cp/unprotected-absolute",
      "command": "ls | cd build && cp /project/notes.txt /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/pipe/cd/bare/redirect/protected-relative",
      "command": "ls | cd rules && echo overwritten > x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > x.md` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > x.md",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/redirect/protected-absolute",
      "command": "ls | cd build && echo overwritten > /project/rules/x.md",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `echo overwritten > /project/rules/x.md` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > /project/rules/x.md",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/redirect/unprotected-relative",
      "command": "ls | cd build && echo overwritten > out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `echo overwritten > out.js` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "echo overwritten > out.js",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/pipe/cd/bare/redirect/unprotected-absolute",
      "command": "ls | cd build && echo overwritten > /project/build/out.js",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": false,
        "mutates": true
      }
    },
    {
      "id": "ls/pipe/cd/bare/tee/protected-relative",
      "command": "ls | cd rules && tee x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee x.md < /dev/null` writes `x.md` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee x.md < /dev/null",
        "targetPath": "x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/tee/protected-absolute",
      "command": "ls | cd build && tee /project/rules/x.md < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command writes a protected path. The segment `tee /project/rules/x.md < /dev/null` writes `rules/x.md`, which is under compliance guard protection. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/rules/x.md < /dev/null",
        "targetPath": "rules/x.md"
      }
    },
    {
      "id": "ls/pipe/cd/bare/tee/unprotected-relative",
      "command": "ls | cd build && tee out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee out.js < /dev/null` writes `out.js` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee out.js < /dev/null",
        "targetPath": "out.js"
      }
    },
    {
      "id": "ls/pipe/cd/bare/tee/unprotected-absolute",
      "command": "ls | cd build && tee /project/build/out.js < /dev/null",
      "families": [
        "flat",
        "pipe-joiner"
      ],
      "verdict": {
        "deny": true,
        "mutates": true,
        "reason": "fusion policy: this Bash command mutates a relative path after a `cd` the shell does not guarantee succeeded. Only `&&` guarantees it — after `;`, `||`, `|`, `&` or a newline bash runs the rest of the command from wherever it never left, so the segment `tee /project/build/out.js < /dev/null` writes `<` at one of two places and the guard cannot tell which (fail-closed). Join the `cd` to what follows it with `&&` — which is also what makes the command correct in the shell — or name the target as an absolute path. Do not rephrase the command — the guard segments compound commands and inspects subshells — and do not re-route through Edit or Write, which are guarded on the same list. STOP and ask the user.",
        "offendingSegment": "tee /project/build/out.js < /dev/null",
        "targetPath": "<"
      }
    }
  ]
}
