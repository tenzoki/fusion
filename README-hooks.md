# Compliance Guard — Claude Code Hook System

The Compliance Guard is fusion's hook layer around Claude Code's tool calls. **It decides nothing.** It sees every write-tool call, every `Bash` call and every sub-agent dispatch, allows all of them, and exists for three products: a trace of what the write surface did, a diagnostic when a project's configuration file is broken or names something fusion has retired, and — since v10.8.0 — the machine-written rows of the orchestrator's event log (`task_start`/`task_done` per dispatch, the session-marker heartbeat; `bin/fusion-commit-lock with` writes the `commit` row on the same footing). The name is historical and kept, because the event vocabulary, the state directory and the monitor's panel all carry it.

## Concept

When Claude Code executes a Write, Edit, MultiEdit, or NotebookEdit tool call, the hook runs *before* the write happens — and lets it through. Two things come out of that call:

1. **The write trace** — one `guard_allow` row per write-tool call in `fusion-workbench/.guard-state/events.jsonl`, naming the tool and the file. That log is what the monitor's panel renders, and it is the only record of what the write surface did.
2. **The configuration diagnostic** — one `guard_advisory` per problem the configuration loader hands back, on every guarded call, `Bash` included, for as long as the project's file is unreadable or names a retired file or key.

`Bash` calls reach the hook for the diagnostic alone. Nothing about a shell command is inspected, and an innocuous `Bash` call in a correctly configured project writes no guard state at all — no event row, nothing under `.guard-state/`.

A sub-agent dispatch (`Task`/`Agent`) reaches both hooks for one purpose: the machine-written `task_start`/`task_done` pair in `fusion-workbench/orchestrator-events.jsonl`, written only while the hook payload carries a session identifier, and carrying `person`/`checkout`/`session_id` resolved by the hook itself. A dispatch is not a "guarded call": it sees no configuration diagnostic, and on the ordinary path it writes nothing under `.guard-state/`. **One condition breaks that.** When the payload carries no session identifier the row cannot be attributed, so instead of dropping it in silence the hook appends one `guard_advisory` to `.guard-state/events.jsonl` naming the condition and what became of the row — `ABSENT_SESSION_ID_ADVISORY` in `hooks/lib/orchestrator-events.ts` is its stable prefix. That is the one route by which a dispatch reaches the guard log at all, and it is where an unexplained advisory row on a dispatch comes from. `hooks/lib/orchestrator-events.ts` carries the schema, the gate and the measured compliance gap that moved these rows off the prompt.

**Nothing here blocks a tool call, and nothing can.** Everything that once did is gone, each on its own measurement, and each is written up below rather than deleted, because a reader arriving from an older tree or from an `events.jsonl` full of `guard_block` rows needs somewhere to land: the [protected-path half](#the-protected-path-half-and-why-it-was-removed) (2026-08-12), the [decision-governed deny and the halt](#the-decision-governed-deny-the-escalation-counter-and-the-halt-were-removed-on-2026-08-16) (2026-08-16), and two policies before those that read the *text* of a shell command — one predicting which files it would write, one predicting whether it would move HEAD — gone with the shell lexer they shared.

**No part of the guard self-disables here any more, because no part of it decides anything anywhere.** One stand-down used to: the write-tool branch of `hooks/guard.ts` recognised this repository by a `.claude-plugin/plugin.json` matching the plugin's own name and allowed every write, so a fusion developer could edit the plugin's own source. It was built because the guard used to protect `agents/**`, `rules/**` and the plugin manifest, which is correct for a project *using* fusion and wrong when developing fusion itself, where every edit to those files would have been blocked and written back. **That protection was removed on 260812**, along with the whole protected-path half of the guard, and the stand-down outlived it by four days: it went on 260816 with the guard's last verdict, so the hook now behaves identically in this tree and in a consuming project (`hooks/guard.ts`, the header's account of what it used to check). A second stand-down covered the churn heatmap in `hooks/tracker.ts` and went with the heatmap on 260815.

**The rule the two established outlives both of them, and it is what the next stand-down is measured against:** a stand-down is evaluated in the coordinate space the mechanism keys its state by. The write-tool check read cwd (`isFusionPluginCwd()`, deleted from `hooks/lib/self-detect.ts` on 260816), because `guard.ts` computed its verdict in cwd's space; churn asked about the workbench root it walked up to (`isFusionPluginRoot(dir)`), because its keys were anchored there. A root-anchored mechanism with a cwd-anchored stand-down misses every session started one directory down, and the reverse is the reverse hole. Both halves of that were measured on the protected-path measurement — whose stand-down had to move up to the root in v6.0.1 when its measurement root did, or a session started in `fusion-workbench/`, the ordinary case here, would have begun reverting the developer's own files. Two mechanisms and their stand-downs are gone. `hooks/lib/self-detect.ts` now keeps `isFusionPluginRoot(dir)` alone and with no caller, so the root-anchored form stays available to whoever needs it next, and `bin/fusion-plugin-cwd` is the only surviving implementation of the cwd-anchored form.

A **git branch-switch policy** used to run here too, and it was the one thing the stand-down never covered. It was deleted outright on 260809: it decided from a shell command's *text* whether the command would move HEAD, which is the same undecidable question the write-path classifier answered until v6.0.0, and in one afternoon it cost five patches, a sixth open entrance, and 24 consecutive false blocks against the agents' own verification commands. Nothing about a `Bash` command is read by the guard any more, in this repository or any other, and since 260812 nothing a `Bash` command *does* is measured either.

- Compliance guard, **observation-only since 260816** (TypeScript hooks, compiled to `hooks/dist/`) — it decides nothing. The PreToolUse hook receives the four write tools, Bash and the sub-agent dispatch tool, allows every one of them, and exists for three products: one `guard_allow` row per write-tool call in `.guard-state/events.jsonl`, which is what the monitor's panel renders, one `guard_advisory` per problem the configuration loader hands back, and (v10.8.0) the machine-written `task_start`/`task_done` pair per dispatch in `orchestrator-events.jsonl` — `hooks/lib/orchestrator-events.ts`, written only while an orchestrator session is in flight. Every path through it writes `{}`; the name is historical and is kept because the event vocabulary, the state directory and the monitor panel all carry it (`hooks/guard.ts`, header). What it used to do went in three steps. It blocked the four write tools against a `protectedPaths` list and measured those paths around every guarded call so a change reaching them any other way was written back; that half was removed on 260812. It counted per-file churn in a heatmap and emitted warning events for the monitor at configured thresholds; that half warned, never blocked, and was removed on 260815. It blocked writes into the decision-governed categories at `high` sensitivity and halted after three consecutive blocks; that check, the halt, the consecutive-block counter, the escalation module and the halt-clearing script were removed on 260816

- **No guard rule is emitted any more.** The always-on `protected-path-discipline.md` (under `rules/` until 260812) was the agent-facing half of the protected-path measurement and went with it, as its sibling rule for the branch policy had gone on 260809. It was 10 541 bytes on every one of the sixteen agents' every dispatch. **Measured on 260812, the day it went:** the always-on floor stood at 98 443 bytes per dispatch before the removal, 91 090 of shipped rule text plus this project's 7 353-byte chat voice profile, and the step took 10 420 off that rather than the full 10 541, because it also had to correct a present-tense claim in `rules/critical-stance.md` that the removal made false, at +121 bytes. **The floor as it stands today is deliberately not stated here.** It moves with every rule edit, so any number written into this line is stale before it is committed: by 260813 the two that stood here had each drifted 5 796 bytes low. Measure it when you need it, with `wc -c` over the always-on set — the **unindented** `emit_if_exists` lines in `bin/fusion-rules`, plus the project's chat voice profile. The indented ones are conditional emissions and are not part of the floor; the same `wc -c` over those files gives what they add, which has been about a third again. In this repository `bin/fusion-rules coder | xargs wc -c` prints the same total in one command, because `coder` draws no conditional rule. **Consequence worth knowing, and since 260816 it has shrunk to nothing:** no rule tells a sub-agent what a halt is or how it is cleared, and there is no longer anything to tell. No halt is reachable, the counter that raised one and the script that cleared one are both gone, and a legacy flag a consuming project still carries blocks nothing — the two migration rows at the end of this document say what to do about it. **The always-on set is an explicit `emit_if_exists` list in `bin/fusion-rules`** (not a pattern match), so a new always-on rule needs a line there or it will not load.

### The decision-governed deny, the escalation counter and the halt were removed on 2026-08-16

A write into a path matched by `guard.categoryPaths` at `high` sensitivity was denied, and the deny counted toward a threshold (default 3) past which the guard entered **halt mode** and blocked every write until a human ran a clearing script.

Two facts ended it, and they compose. Once the protected-path half went on 2026-08-12, this deny was the only thing left that could raise a halt — and it shipped switched off: fusion's own layer declared `categoryPaths: {}` and `decisions: []`, so a governed path existed only where a project wrote one. Neither reachable consuming project had written one, and all 50 recorded `guard_block` rows in the larger one's 37 186 events read "Protected path". The halt was reachable only through a check nobody had armed.

The whole apparatus went together: the deny, the consecutive-block counter, the halt, `lib/escalation.ts`, the `clear-halt.ts` script that was the only thing that cleared a halt, and the six settings behind them — the four that armed the deny (`guard.categoryPaths`, `guard.categorySensitivity`, `guard.defaultSensitivity` and the `decisions` list) plus `escalation.blocksBeforeHalt` and the `guard.enabled` switch that stood the whole thing down. A halt flag left in a consuming project's `escalation.json` blocks nothing at this version, and there is no script to clear it with — `/fusion:setup` offers to delete the file instead.

### Churn detection was removed on 2026-08-15

A PostToolUse tracker used to record every file mutation in a heatmap under
`.guard-state/churn.json` and emit `churn_warning` / `churn_critical` events at configured
per-session thresholds. It never blocked anything: it was a signal the monitor's warnings
panel rendered and the orchestrator read at Setup through a `bin/` ranking helper. The
whole of it is gone — the module, that helper, the two event types, the two configuration
leaves, and the plugin-repo stand-down that existed only to keep a fusion developer's own
edits out of the count.

The PostToolUse hook is observation-only in full, and measures rather than counts: review
coverage, staging drift and citation form, three measurements on narrow triggers. A fourth,
session-state drift, went on 2026-08-15 with the hand-maintained counters that were its
subject. It
carried one thing that was never observation-only — the protected-path measurement, which
put a changed protected path back and raised the halt from a hook that cannot block. That
went on 2026-08-12, and the halt's one remaining source went on 2026-08-16, so no hook
raises anything now.

## Architecture

```
Claude Code
  |
  +-- SessionStart
  |     +-- exports FUSION_PLUGIN_ROOT to $CLAUDE_ENV_FILE
  |     +-- emits the "Fusion loaded" systemMessage banner (static printf)
  |     +-- session-start.ts
  |     |     |   Warns when the workbench root sits ABOVE the working
  |     |     |   directory instead of at it. Silent otherwise.
  |     |     \-- lib/workbench-root.ts   (the same upward walk every hook uses)
  |     +-- session-id.ts
  |     |     Prints "fusion: session_id=<uuid>" on plain stdout, the
  |     |     channel that reaches the model -- where the banner above
  |     |     uses systemMessage, which reaches only the user. Silent
  |     |     when the payload carries no session_id. Also exports
  |     |     FUSION_SESSION_ID to $CLAUDE_ENV_FILE (v10.8.0).
  |     \-- exports FUSION_PERSON / FUSION_CHECKOUT to $CLAUDE_ENV_FILE
  |           |   (one bin/fusion-identity run per session, v10.8.0)
  |           \-- identity-notice.js
  |                 Puts that run's mint announcement in front of the
  |                 user as a systemMessage. Silent unless this run
  |                 minted .checkout-id.
  |
  +-- PreToolUse (Write/Edit/MultiEdit/NotebookEdit/Bash/Task/Agent)
  |     \-- guard.ts
  |           |   Decides NOTHING. Every call is allowed. Bash is
  |           |   inspected for nothing and, in a correctly configured
  |           |   project, writes no guard state at all. A sub-agent
  |           |   dispatch (Task/Agent) writes the machine-written
  |           |   task_start row and nothing else.
  |           +-- <project-root>/fusion.json (citations.extraPaths; read
  |           |     for the diagnostic, not for a verdict)
  |           +-- fusion-workbench/.guard-state/events.jsonl (the write
  |           |     trace and the configuration advisories)
  |           \-- fusion-workbench/orchestrator-events.jsonl (task_start,
  |                 via lib/orchestrator-events.ts, orchestrator sessions only)
  |
  +-- PostToolUse (Write/Edit/MultiEdit/NotebookEdit/Bash/Task/Agent)
        \-- tracker.ts
              |   Measures. Observation only: it cannot block, and it no
              |   longer raises or counts anything either. A dispatch
              |   additionally writes the task_done row, and every call
              |   refreshes the session-marker heartbeat (rate-limited).
              +-- fusion-workbench/.guard-state/{review-coverage,
              |     staging-drift}.json (one throttle record per measurement)
              +-- fusion-workbench/.guard-state/events.jsonl (audit log)
              \-- fusion-workbench/orchestrator-events.jsonl (task_done;
                    a backgrounded dispatch parks .guard-state/dispatch-map.json
                    instead, resolved at the real stop by:)

  +-- SubagentStop
        \-- subagent-stop.ts
              \-- fusion-workbench/orchestrator-events.jsonl (the backgrounded
                    dispatch's task_done, paired via the parked mapping)
```

## Getting Started

### 1. Verify hooks are wired

The plugin wires hooks automatically via `hooks.json`. The hooks use pre-compiled JavaScript (`hooks/dist/`) invoked with plain `node` — no `npx tsx` needed, no `node_modules` at runtime.

The effective hook configuration:

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "[ -n \"${CLAUDE_PLUGIN_ROOT}\" ] && echo \"export FUSION_PLUGIN_ROOT=${CLAUDE_PLUGIN_ROOT}\" >> \"$CLAUDE_ENV_FILE\" || true" },
          { "type": "command", "command": "printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"systemMessage\":\"Fusion loaded. Orchestrator sessions: run /fusion:setup before any other work.\"}}'" },
          { "type": "command", "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/dist/session-start.js" },
          { "type": "command", "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/dist/session-id.js" },
          { "type": "command", "command": "<the FUSION_PERSON/FUSION_CHECKOUT export — one bin/fusion-identity run, appended to $CLAUDE_ENV_FILE; hooks.json carries the exact command>" }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit|Bash|Task|Agent",
        "hooks": [
          { "type": "command", "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/dist/guard.js" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit|Bash|Task|Agent",
        "hooks": [
          { "type": "command", "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/dist/tracker.js" }
        ]
      }
    ]
  }
}
```

### 2. Review the configuration

The settings are `citations.extraPaths`, the non-Markdown paths a project declares as carrying record citations, and since 260921 `citations.exhibits`, the records it declares as exhibits; the guard is not among them, and neither is anything else. `orchestrator.maxTurns` and `orchestrator.dispatchMinutes` stood beside it until 2026-09-10 and are retired. A project declares them in `fusion.json` at its own root, which `/fusion:setup` seeds — see [Per-project configuration](#per-project-configuration-fusionjson). The guard has no settings at all, because it decides nothing; the six that configured it went with the checks they tuned, along with the plugin-level configuration file that carried their defaults.

Reviewing the configuration is therefore two questions: which of your non-Markdown files cite records, and whether your project root still carries the retired `fusion-guard.json`. The second answers itself — the guard tells you on every guarded tool call until you delete it.

### 3. Start a Claude Code session

The hooks activate automatically on session start. No manual startup needed.

### Start your session at the project root

Start Claude Code in the directory that holds `fusion-workbench/`. Starting one or more
levels below it leaves fusion working, but not everywhere — and the first screen of the
session now says so:

```
fusion: restart this session at the project root.

  project root:      /home/you/acme
  working directory: /home/you/acme/fusion-workbench

This session started below the project root. Some of fusion's checks
resolve against the working directory instead of the root, so from here
they inspect the wrong directory and let through what they would
otherwise stop. The workbench itself is found by walking up, so your
files and settings are still read from the right place.
```

The split behind it is small: `lib/workbench-root.ts` walks up from the working directory
looking for `fusion-workbench/.fusion-setup`, so the root it finds is either the working
directory itself or an ancestor of it. Found at the working directory, or not found at all
(not a fusion project), the hook stays silent. Found above it, you get the message.

**What the warning does and does not do.** It changes no behaviour and blocks nothing. It
exists because something fusion resolves against the working directory rather than the
project root inspects, from a subdirectory, a directory that is not the project's. **One
such resolution is left**, and it is why the warning still fires: the work-tree preference
of `bin/fusion-rules`, `bin/fusion-paths` and `bin/fusion-source-root`, each of which asks
`bin/fusion-plugin-cwd` whether the working directory is fusion's own source repository,
with no upward walk. Started one directory down inside that repository, all three read the
installed plugin copy instead of the work tree being edited — which is a session spent
reading rules and prompts days stale against the sources in front of you, silently.

Two sharper instances stood here and are gone: the protected-path deny, matched against the
working directory until 2026-08-12, and the pair the guard carried until 2026-08-16 — the
project-relative spelling the decision-governed check matched in, and the stand-down that
asked cwd whether it was fusion's own repository. `hooks/session-start.ts` carries the full
account. Teaching the surviving helpers to walk up separately would be three copies of a
walk with three chances to disagree; one message at the moment the working directory is
still cheap to change makes the assumption they share audible instead.

The tracker's three measurements were never among them: each is anchored at the workbench
root rather than at cwd, so each reports the same thing whatever directory the session
started in. The rule that anchoring follows — evaluate a mechanism where it keys its state
— is stated in `lib/self-detect.ts`. It was established on the protected-path measurement
and carried by the churn heatmap, and it outlived both of them and the stand-down it was
last written for.

## Files

| File | Purpose | Committed? |
|------|---------|------------|
| `session-start.ts` | SessionStart hook — warns when the session started below the project root. See [Start your session at the project root](#start-your-session-at-the-project-root) | Yes |
| `session-id.ts` | SessionStart hook — prints `fusion: session_id=<uuid>` on plain stdout, which is the channel that reaches the model; `systemMessage` reaches the user and never it. Both halves were measured against Claude Code 2.1.245 and read from the transcript rather than from the model's testimony about its own context (`260825-2214-can-a-hook-obtain-the-session-identifier.md`). It is a fourth SessionStart command rather than a line inside `session-start.ts` because one process writes one stdout and the two need opposite channels: an envelope that carries the warning routes it away from the model and leaves the model's copy empty. A payload with no usable identifier produces no output at all — anything written here becomes model context, so absence is silence rather than a line saying there is nothing to say. Since v10.8.0 it also appends `export FUSION_SESSION_ID=<uuid>` to `$CLAUDE_ENV_FILE` (after the stdout line, charset-gated, fail-open), so the model's shell commands can put the identifier on the event rows they still write | Yes |
| `identity-notice.ts` | SessionStart hook — carries `bin/fusion-identity`'s mint announcement to the user. It is not a sixth SessionStart command: the identity-export clause pipes its own captured output here, because the process that mints `.checkout-id` is that clause and no later hook can tell a just-minted identifier from an old one. Silent unless this run minted. Fixes the fault where the announcement existed on a channel nothing reads: measured against Claude Code 2.1.261, a SessionStart command's stderr at exit 0 reaches neither the user nor the model (the transcript keeps it in the `hook_success` attachment, the renderer returns null for that type, and the context mapping takes only a non-empty `content`), while a `systemMessage` becomes a `hook_system_message` attachment the user sees (`260905-0933_*_the-mint-announcement-is-unreachable-on-every-path-that-actually-mints.md`) | Yes |
| `subagent-stop.ts` | SubagentStop hook (v10.8.1) — the backgrounded dispatch's real completion. A backgrounded dispatch returns at launch, so PostToolUse's `task_done` would record "accepted" and not "finished"; this hook resolves the `agentId → tool_use_id` mapping the tracker parked at launch and emits the row when the sub-agent actually stops. Its payload was measured before it was trusted (`260827-0740-subagentstop-payload-measurement.md`): it carries `agent_id` and `session_id` and no tool-use id, and a sync dispatch's SubagentStop fires before any mapping entry exists — the ordering, not a heuristic, prevents duplicate rows | Yes |
| `guard.ts` | PreToolUse hook — allows every call and decides nothing. It writes the `guard_allow` trace row for a write-tool call, the `guard_advisory` rows for whatever the configuration loader could not resolve, and (v10.8.0) the machine-written `task_start` row for a sub-agent dispatch, and that is the whole of it. Its own header carries the account of each check that used to live here and the measurement that removed it | Yes |
| `tracker.ts` | PostToolUse hook — runs the staging, review-coverage and citation-form measurements and hands the model whatever they have to say. Observation only: a PostToolUse hook cannot block, and since 2026-08-12 it raises nothing either. Each measurement fires on a narrow trigger, so since 2026-08-15 an ordinary write at an unremarkable path reaches no measurement here at all — the session-state drift check that held the every-tool-call slot went with the hand-maintained counters it measured. Since v10.8.0 it also writes the machine-written `task_done` row for a sub-agent dispatch and refreshes the session-marker heartbeat (rate-limited on the marker's own mtime); neither is a measurement — they record and say nothing | Yes |
| `review-coverage.ts` | Prints the review-coverage tiling for `agents/orchestrator.md` `## Review coverage` and `## Closing a work item`. Run through `bin/fusion-review-coverage`. Finding an uncovered range is `verdict=uncovered` on stdout, never a non-zero exit and never a release gate — whether a release may go out over an unreviewed range is an unfiled decision this program does not pre-empt | Yes |
| `events-query.ts` | Prints the identity-scoped readings of the orchestrator event log — `presence` and `dispatches`, the second being how long each dispatch of a measured agent ran and how many ran longer than the threshold this program itself defines, since no project configures one. A third, `turns`, stood beside them until 2026-09-10 and went with the Turn it counted, along with the session state file it scoped itself by. Run through `bin/fusion-events`, which hands it the identity; `lib/events-query.ts` is the computation, and this file is what opens the log and parses the checkout roster the wrapper hands over in `FUSION_EVENTS_ROSTER` into the two maps presence takes. A figure that could not be taken is absent from stdout, named on stderr and reported by the exit code, never rendered as a zero | Yes |
| `plan-size.ts` | Prints the live plans against the plan-size ceiling from `260909-1615_*_spec-cut-fusion-to-a-working-minimum.md` `### C5`. Run through `bin/fusion-plan-size`, by a person, and by nothing else: no hook calls it, no pipeline step invokes it and no test gates on it. A plan over the ceiling is `verdict=over` on stdout and exit 0 — the report-only shape was the user's ruling at `260909-1700_*_does-the-plan-size-ceiling-fail-hard-or-only-report.md` (option 1), because no plan has a measured reader and a hard bound would be the first in that cut enforced without one. `verdict=empty` is a real answer and reaches exit 0 like the other two | Yes |
| `citation-check.ts` | Prints the citation check over a consuming project: every `.md` under the workbench, the frozen stores read exactly like the live tree since `32fe0d49`, plus `CLAUDE.md`, `rules/`, `.claude/rules/` and `docs/`, plus every file the project declared in `citations.extraPaths` — judged by `lib/citation-scan.ts`. Run through `bin/fusion-citation-check`; `/fusion:cleanup` Step 8 prints its `verdict=` line. A violation is a row on stdout, never a non-zero exit, and nothing is rewritten. Since 2026-09-01 only a row in a file somebody still edits moves `verdict=` — `isLiveRecord()` from `lib/citation-corpus.ts` inside the workbench, everything outside it in scope — while every row is still printed and carries an `edited` / `not-edited` column, and `edited-files`, `edited-violations` and `unedited-violations` put the scope in the `KEY=value` block. | Yes |
| `citation-sweep.ts` | Rewrites store-prefixed citations to the storeless form, and `--repair` undoes the bare-stamp expansion an earlier version applied. Run through `bin/fusion-citation-sweep`, by a person and never by a pipeline or skill. Its corpus is the workbench's Markdown plus each `<path>` argument plus the project's declared `citations.extraPaths`. `--dry-run` (the default) prints the census; a writing mode stands behind three guards named in its header: (a) a workbench tracked in a git work tree, no uncommitted change naming a file the run will read — the corpus question rather than a clean tree, since the tracked event log makes a clean tree unreachable inside a session — and every extra `<path>` inside that work tree and tracked by it (exit 4); (b) the census first and `--yes` before any write (exit 5); and (c) no bare-stamp resolution at all. Replaced the `.mjs` script under `hooks/scripts/` on 2026-08-29 so the install tarball runs it without `node_modules`. | Yes |
| `order.ts` | Prints the order the work-item store imposes on itself: the `KEY=value` block, then one row per item in that order, then the cycle and unresolved rows. Run through `bin/fusion-work-order`, by a person and by nothing else — no hook calls it, no pipeline step invokes it and no test gates on it. A cycle is `verdict=cyclic` on stdout and exit 0: **no exit code carries the verdict**, the stdout-verdict rule its four reporting siblings carry, because a cycle is a finding about the store and not a failure of this program. Where `no-depends-on-field=` or `unresolved-edges=` is above zero a mandatory `note=` line names the count and says that `ready=` is optimistic by it; an item-form record whose head yields no readable `**Status:**` is counted in `unreadable-head=` and named on an `unreadable=` row rather than dropped | Yes |
| `edge-answers.ts` | Prints what prior curator runs already asked the user about a work-item edge: the `KEY=value` block, then one row per proposed edge carrying the outcome value, the dependent, the target and the field. Run through `bin/fusion-edge-answers`, by `agents/curator.md` `### The suppression read` and by nothing else — no hook calls it, no pipeline step invokes it and no test gates on it. An empty corpus is `verdict=empty` on stdout and exit 0: **no exit code carries the verdict**, the stdout-verdict rule its five reporting siblings carry, because "no edge was ever proposed here" is a finding about the project and not a failure of this program. Where `unreadable=` is above zero a mandatory `note=` line says so, and says to treat those pairs as unanswered | Yes |
| `lib/paths.ts` | Case folding for a path comparison — one function, `foldCase`, and one caller, `tracker.ts`, which folds both sides of a containment test so a case-insensitive file system does not decide whether the review-coverage measurement fires. It carried the glob-to-regex compiler and the lexical normalisation under it until 2026-08-16; both existed to answer whether a path falls inside a configured SET of paths, and the guard has no such set left | Yes |
| `lib/config.ts` | JSON config loader. The two live settings are `citations.extraPaths` and `citations.exhibits`, two layers (the project's `fusion.json`, then `DEFAULTS`), and a diagnostics list that is now its main product: an unreadable file, a wrongly typed value, a retired file at the project root and a retired top-level key inside the one that is read are each named rather than dropped in silence | Yes |
| `lib/events.ts` | Append-only event logger | Yes |
| `lib/orchestrator-events.ts` | The machine-written rows of `fusion-workbench/orchestrator-events.jsonl` (v10.8.0): `task_start`/`task_done` per sub-agent dispatch, written only while the hook payload carries a session identifier, and the session-marker heartbeat, written while the marker exists. Identity is env-first (`FUSION_PERSON`/`FUSION_CHECKOUT` from the SessionStart export), else one run of `bin/fusion-identity` resolved relative to this file — never a second implementation of the criterion; an unresolved half is an absent key. Timestamps follow the log's standing convention (UTC, second resolution, no `Z`). Its header carries the measured compliance gap that moved these rows off the prompt (87 % batch-written timestamps, identity on 2.6 % of lines) and the stated residual of the project-scoped gate | Yes |
| `lib/fail-open.ts` | The ordering rule both hooks run on: the verdict goes to stdout first and unguarded, every record of it after, each in its own `try`, so a failed report cannot withdraw the answer it reports. `failOpen` is the error tail of each entry point; `answer` and `bestEffort` carry the same rule to every site inside `main` where an escalation save, an event append or the churn heatmap once stood ahead of the verdict. The heatmap went on 2026-08-15 and the escalation save on 2026-08-16; the rule is what the surviving sites keep, and it is why a report that throws can no longer cost a hook its stdout. The reports append under `.guard-state/`, the likeliest thing to have failed, which is how reporting first turned a deny into `{}` and swallowed the halt sentence  Also home of `exitZeroOnStdoutEpipe()` (v10.9.0): the four reporting CLIs exit 0 silently when their reader closed stdout first — a reader that stopped listening is not the program's fault, and every other stream error still throws | Yes |
| `lib/guard-state-file.ts` | The read-coerce-write seam under `fusion-workbench/.guard-state/`: resolves one state file, reads it, hands whatever it holds to the caller's coercion, and writes it back atomically. The coercion is a parameter, so absence, unparseable text and a valid JSON value of the wrong shape are one answer and no state module has anywhere to put an `as` cast — the defect that let a `{}` state file throw on the next field access and discard the halt message the same tool call had already produced. The three measurement throttle stores use it and are now the only callers: `protected-snapshot.ts`, the one module that never fitted the seam, went with the protected-path half, `churn.ts` with the heatmap, `state-drift.ts` with the session counters, and `escalation.ts` with the halt on 2026-08-16, taking its own merge with it. Nothing had to come out of this module when any of them went, which is what the seam being a parameter buys | Yes |
| `lib/git.ts` | The one `git` the measurements run: `execFileSync` in a named root, stderr discarded, every failure — not a repository, an unresolvable ref, a non-zero exit, the timeout — collapsed to `null`, because no caller distinguishes them and each turns "git would not say" into a report that claims nothing rather than into a fault. It existed verbatim in `review-coverage.ts` and `staging-drift.ts`, and inline in the removed `state-drift.ts`, until decision `260811-1146_*_does-the-measurement-family-get-a-shared-chassis-before-the-fourth-module.md`. The default timeout is a ref read's; `staging-drift.ts` passes a larger one at its `git status` call, which is the only call in the family that walks a working tree | Yes |
| `lib/staging-drift.ts` | The staging-drift measurement (issue 260811-0114_*_the-queue-rebuild-and-its-history-file-never-entered-a-commit-and-survive-only-in-the-working-tree.md): reads `git status --porcelain` over the workbench and classifies every entry — `record` (an authored artifact no commit carries), `commit-message` (a commit-message-shaped **name** that no artifact store owns, which is where the improvised `.commit-msg-tmp` lands; the store scoping is what the class is, not a detail of it, because without it the name test also claimed every authored record whose topic slug says "commit message" and the model was told to delete it, issue 260811-1141_*_any-workbench-file-whose-name-contains-commit-message-is-classified-as-a-commit-message-and-the-model-is-told-to-delete-it.md), `in-flight` (live state and the machine-written surfaces, never a fault), `unclassified` (a user's own note, named with the statement that nothing is claimed about it). Two callers: `staging-drift.ts` behind `bin/fusion-staging-drift`, and `tracker.ts` on the one trigger of **HEAD having moved** since the previous tool call — read with `git rev-parse`, never inferred from the command's text. The commit is the first moment an unstaged record is a fault; while a task is still in flight it is the normal state. It reports and never stages, because a mechanism that staged would be a second author of a staging list whose whole value is that a human wrote every path in it. The name test alone is exported as `hasCommitMessageName`, because a second caller asks a different question of the same string — `commit-message-path.test.ts` asks whether a shipped prompt *prescribes* a message file inside the workbench, and a prescription pointing into a store is exactly what the location scoping forgives (issue 260811-1410_*_the-commit-message-path-gate-narrowed-with-the-classifier-it-reuses-and-no-longer-catches-a-prescription-inside-a-store.md). One pattern, two scopings, neither transcribed | Yes |
| `lib/review-coverage.ts` | The review-coverage measurement (issue 260810-1205_*_seven-of-sixteen-commits-in-the-session-range-never-reached-a-review-pass-and-nothing-measures-the-gap.md): tiles the review files' own mandated `**Reviewed-range:**` fields against `git rev-list <session-start>..HEAD` and names, commit by commit, what no reviewer opened — plus the `**Not-opened:**` list the last pass declared, which is the next dispatch's scope. Two callers: `review-coverage.ts` behind `bin/fusion-review-coverage`, and `tracker.ts` on the one trigger of a review file landing. Deliberately NOT on an every-tool-call path — the slot `lib/state-drift.ts` held until it was removed on 2026-08-15, and which nothing occupies now: an uncovered range mid-session is the normal state, and a check that fires on its commonest path is one its reader learns to ignore. It reports and never gates | Yes |
| `lib/plan-size.ts` | The plan-size measurement: live (`_o_`/`_p_`) plans in every planning store, shaper specs excluded and counted separately, weighed against a ceiling. One caller, `plan-size.ts` behind `bin/fusion-plan-size`; no hook and no gate reads it. The ceiling in `DEFAULT_CEILING` is a **chosen** policy value and not a threshold read off a measurement of readers — the module header says so, says what it was chosen against (a ceiling above the whole corpus prints one verdict forever), and `--ceiling <bytes>` re-reads the same corpus at any other number, which is what keeps a wrong choice worth a line of stdout. A file that cannot be read is skipped rather than counted as zero, since a zero would report a plan as comfortably under a ceiling nobody measured it against. The corpus definition is a deliberate second copy of the one in `lib/__tests__/plan-stopping-section-lint.test.ts`, that one being test-scoped and this one shipping; the header owns the residual rather than leaving it to be found | Yes |
| `lib/events-query.ts` | The identity-scoped readings of `fusion-workbench/orchestrator-events.jsonl`, behind `bin/fusion-events`: `presence`, who else has written to the log inside a window; and `measureDispatchDurations`, which pairs each measured agent's `task_start` with its `task_done` and reports how long that dispatch ran against the threshold the reading was handed — a property of the reading, since the dispatch bound was retired on 2026-09-10 and no dispatch carries a stopping time — with `counted`, `unattributable` and `unpaired` partitioning every pair the cutoff and the agent filter admitted. The file carries `merge=union` (class R2 in `rules/workbench-tracking.md`), so after a pull it holds two checkouts' lines with no ordering between the blocks, and sorting it moves a reader from vague to wrong rather than repairing it (issue `260823-1302_*_the-monitor-attributes-a-merged-event-log-to-one-session-and-reports-another-checkouts-state.md`). Membership is therefore read off each line's own `checkout` field, and an **absent** identifier reads as the reading checkout's own — which is what carries the whole pre-C4 log and what makes an unresolved reader behave exactly as it did before. Presence keeps the lines this checkout did not write and drops ours; `countTurns`, which did the reverse, went on 2026-09-10 with the Turn it counted. It opens no file, runs no subprocess and asks nothing about git: the log text, the reading identity, the current time and (since v10.21) the checkout registry's identity map are arguments, so every case in its classification table is a fixture string. That map is what makes two git identities one person has registered count as one person: `measurePresence` classifies on `canon(g) = identityMap[g] ?? g`, and an empty map is the identity function, so a project with no registry runs the same code and gets the figures it got before the registry existed. `parseTs` appends the `Z` designator the emit convention omits, which is the standing trap in `CLAUDE.md`'s symptom table. `other_people=0` is a real figure; a figure that could not be taken is reported through an exit code by the entry point and never as a smaller number | Yes |
| `lib/workbench-root.ts` | Walks up from cwd to find `fusion-workbench/.fusion-setup` (single source of truth for workbench presence in TS) | Yes |
| `lib/self-detect.ts` | Detects the fusion plugin's own repo. **Nothing under `hooks/` calls it**, and that is the decided state: both stand-downs it served are gone — the churn heatmap's on 2026-08-15, the guard's write-tool branch on 2026-08-16 with the verdict it was standing down. The cwd-anchored entry point went with the second; `isFusionPluginRoot(dir)` is kept for the rule the module carries, that a mechanism is stood down in the coordinate space it keys its state by, which is expensive to re-derive and invisible when got wrong. The shell side, `bin/fusion-plugin-cwd`, is a live helper with three consumers and is no longer paired with this file | Yes |
| `lib/domain-cascade.ts` | Parses the domain cascade out of `agents/orchestrator.md` Setup Step 5 and **executes** it. No hook calls it — the gates do, so what they measure is the verdict a real project reaches rather than the layout of the prompt's branch lines. There is deliberately no second copy of the cascade in TypeScript: the interpreter runs the prompt's own block. That keeps *this file* from drifting and nothing else, which is where the claim used to overreach — a second copy shipped in `skills/cleanup/SKILL.md` in the pre-fix order while both gates read the orchestrator prompt alone (issue 260810-1918_*_the-cleanup-skill-carries-a-second-domain-cascade-in-the-pre-fix-order-and-no-gate-reads-it.md). The module now also *finds* a statement of the cascade, fenced or prose, and `domain-cascade.test.ts` runs that over the file set, allowing exactly one. How far that reaches is not described here and not described in the module either — it is the `REACH` object at the foot of the module, and [the section below](#how-far-the-domain-cascade-reach-gate-reaches) is rendered from it | Yes |
| `lib/citation-corpus.ts` | Which workbench records are LIVE — the files somebody still edits. One predicate, `isLiveRecord()`, read by two consumers with different stakes: `lib/__tests__/workbench-citation-lint.test.ts` uses it as its CORPUS, where an admitted file carrying a dangling citation reddens the suite, and `citation-check.ts` uses it as its VERDICT SCOPE, where a row is printed either way and only `verdict=` narrows. It lived inside that test file until 2026-09-01 and moved here whole, with its reasoning, when decision `260830-2225_*_should-an-archived-violation-move-the-checkers-verdict-line.md` scoped the reporter's verdict; nothing about what the gate asserts or reads changed with the move. Its header carries the wide reading of a live decision, the two markers a live plan carries, the frozen stores and their root anchoring, the hole at terminal state, and the one judgement the reporter needed and the gate never did — that the marker-less kinds (history, analyses, reviews, consult, memos, investigations) are not edited, because a history entry records what was true then. One hook reads one export: `lib/citation-form.ts` takes `FROZEN_PREFIXES` for its file test, so the write-time report and the two citation consumers agree about which stores are frozen rather than each spelling the list. `isLiveRecord()` itself is deliberately NOT the write-time trigger — a history file and an analysis are outside it, and two of the three instances that filed the write-time defect were exactly those | Yes |
| `lib/citation-scan.ts` | The workbench-record citation grammar and its resolver, moved out of the test tree on 2026-08-29 so it compiles into `hooks/dist/` (decision `260828-0904_*_does-fusion-ship-a-citation-checker-to-consuming-projects.md`). `createScanner(workbenchRoot)` binds the grammar to one workbench; the test helper at `hooks/lib/__tests__/helpers/citation-scan.ts` is now a shim that binds fusion's own roots and adds no grammar. Its callers are the two citation gates, `citation-check.ts`, `citation-sweep.ts` and, since 2026-09-06, `lib/citation-form.ts` inside the PostToolUse hook — one grammar, five readers, and a second implementation is the defect this project has removed more than once | Yes |
| `lib/citation-form.ts` | The citation-form measurement (issue 260906-0115_*_three-agents-in-one-session-wrote-a-citation-the-always-on-rule-forbids-and-only-a-later-gate-caught-it.md): when a `.md` record lands under the workbench, it hands the file to `lib/citation-scan.ts` and reports the citations spelled in a form the project retired, at the moment of writing rather than at the next gate run. Three narrowings carry it, and each is a place it could have become noise. **The file**: `.md` under the workbench, frozen stores out through `lib/citation-corpus.ts`'s own `FROZEN_PREFIXES`; the marker-less kinds are IN, because two of the three instances that filed the defect were a history file and an analysis, which the release gate's corpus excludes. **The lines**: only what this tool call wrote, whole-file for a `Write` and the located `new_string` for an `Edit`/`MultiEdit`, so an edit is never handed a violation somebody else left in the same file — while the WHOLE file is still scanned, because a fragment read alone carries no fence and a quoted exhibit would be judged as a pointer. **The verdict**: `store-prefixed` and `stale-marker`, and never `dangling` — a failed lookup is what a dead pointer, a probe fixture quoted in prose, an unqualified foreign record and a record about to be written all produce, and 260830-2235_*_the-fabricated-name-exemption-keys-on-the-literal-foo-so-every-realistic-probe-fixture-is-read-as-a-real-citation.md states that no decidable keying property has been proposed for telling them apart. A hit the grammar marked as somebody's exhibit is silent here even where the gate reports it, which follows the sweep rather than the gate because a write-time report asks the writer to respell. One caller, `tracker.ts`; no CLI and no `bin/` wrapper, because `bin/fusion-citation-check` already answers the same question over the whole corpus | Yes |
| `lib/dispatch-bytes.ts` | What one dispatch loads, in bytes, measured at the dispatch and written onto its `task_start` row: the agent prompt, the rules `bin/fusion-rules` emits to that agent, `CLAUDE.md`, their total, and the delta against the project's own armed baseline. The rule count **runs the helper** rather than reproducing its emission list, memoised in `.guard-state/rule-sizes.json` on the agent, the plugin root and the newest mtime under the rule directories, so a second dispatch of the same agent spawns nothing. A helper that is absent or exits non-zero makes `bytes_rules` and `bytes_total` absent keys and earns one advisory — never a zero, which would read as a dispatch that loaded no rules. It also reads the `**Work-item:**` line off the dispatch prompt and never scans the backlog store. The module's own header is the authoritative documentation | Yes |
| `lib/work-graph.ts` | The order the work-item store imposes on itself, computed per run and stored nowhere: it reads each live item's `**Depends-on:**` field, resolves the entries against the store, and returns readiness, depth, what each item blocks, the entries that name nothing and the cycles. The graph spans live items only, so a `done` or `dropped` item is outside it and so is a terminal Circle record, which keeps its state in a filename marker and carries no such field to read; the figures therefore measure unfinished work and say nothing about what has closed. Its own header is the authoritative documentation | Yes |
| `lib/edge-answers.ts` | What prior curator runs already asked the user about a work-item edge: it walks every curator run file under the workbench — nothing excluded, not the evidence anchor, not `$SCAN_ANALYSES`, not `archive/`, because a run file records a refusal whatever its age — reads each ledger entry carrying an `**Edge:**` line, and collapses them to one row per dependent, target and field, the later run file's answer winning. One caller, `edge-answers.ts` behind `bin/fusion-edge-answers`; no hook and no gate reads it. **The consequence group is deliberately not a column:** the suppression key reads the outcome value alone once `not-offered` names the entry the gate never put, and a key assembled from two lines is the drift that ruling removed. The outcome parse is a heuristic over free-form prose and the header states it exactly, including that only a row line counts and that it fails toward `none` — an unrecognised shape costs a re-proposal, never a silent suppression | Yes |
| `lib/stores.ts` | The artifact stores, named once: `RECORD_STORES` is the layout tree's twelve under `shared/`, `LEGACY_STORES` the pre-container backlog store the tree still holds, `RETIRED_REVIEW_FOLDERS` the three pre-v4 review folders. The staging classifier, the citation grammar and the path-literal lint each compose their set from these and say which they take, where until 2026-09-22 three hand-kept lists drifted by one element each way; `path-literal-lint.test.ts` parses the tree in `rules/fusion-workbench-conventions.md` on every run and requires `RECORD_STORES` to equal it | Yes |
| `package.json` | Dev dependencies (tsx, typescript, vitest) | Yes |

- **`hooks/`** — TypeScript source + compiled `dist/`. SessionStart, PreToolUse, PostToolUse and SubagentStop hooks; `hooks/hooks.json` is the registration and the list to read. Uses `process.cwd()` not `__dirname` for project-local state. SessionStart runs five independent commands: the `FUSION_PLUGIN_ROOT` export, the static "Fusion loaded" banner, `session-start.ts`, `hooks/session-id.ts` and (v10.8.0) the identity export — one `bin/fusion-identity` run whose `PERSON`/`CHECKOUT` land as `FUSION_PERSON`/`FUSION_CHECKOUT` in `$CLAUDE_ENV_FILE`; `session-id.ts` additionally exports `FUSION_SESSION_ID` there. The third warns, as a `systemMessage`, when a workbench root is found *above* the working directory rather than at it. One cwd-anchored resolution is left for that warning to be about, the work-tree preference of the three `bin/` helpers listed under `bin/fusion-plugin-cwd`; the two sharper ones it used to name went with the protected-path deny on 260812 and with the guard's last verdict on 260816. The warning fixes none of it, and makes the assumption audible at the one moment the working directory is still cheap to change. The fourth puts the Claude Code session identifier in front of the model on plain stdout, the channel measured to reach it, and it is its own command for a sharper reason than the other three are theirs: one process writes one stdout, and a recognised JSON envelope routes `systemMessage` to the user while leaving the model's copy empty, so the warning and the identifier cannot come out of one process. The PreToolUse guard inspects `Bash` for **nothing** and allows it immediately, writing no guard state at all **when the project's `fusion.json` is valid**; a sub-agent dispatch is allowed the same way, writing only its machine-written event row and never guard state. A broken file, a retired top-level key inside it, or a leftover `fusion-guard.json` at the project root each emit one `guard_advisory` per guarded call, Bash included, because the diagnostic loop sits above the Bash return (`hooks/guard.ts`, and the comment there states the departure). The last policy that read a command's text — the git branch/worktree guard, with the shell lexer and command-word resolver under it — was deleted on 260809; the last thing that watched what a command *did* — the protected-path measurement, a fingerprint of the whole protected list taken in `guard.ts` before the call and compared again in `tracker.ts` after it, with anything that moved written back and the guard halted — was deleted on 260812 with the rest of that half. **No check remains**: the halt and the decision-governed categories both went on 260816. Documented for users in `README-hooks.md`.
- **`fusion.json` + `templates/fusion.json`** — The per-project configuration, renamed from `fusion-guard.json` on 260816 when the last guard setting left it. `/fusion:setup` seeds the template into a consuming project's root, git-tracked so every change to it shows in a diff; the root copy here equals the template outside the top-level keys a project is meant to set for itself, and that is exactly what `config.test.ts` pins: it cuts the keys named in its `PROJECT_SET_KEYS` out of both sides and holds every remaining byte identical. This repository sets one of the two keys that constant names: its declaration of the non-Markdown paths that carry record citations (`citations.extraPaths`, added 260831); the loader's other live leaf, `citations.exhibits` (260921), it leaves at `[]`. The other, the `orchestrator` container, holds nothing but two retirements since 260910 — `orchestrator.maxTurns`, which went with the Turn loop, and `orchestrator.dispatchMinutes`, which went with the dispatch bound — and this repository declares neither. The template declares none of them and carries only the documentation note for each. The merge is per **leaf** key across **two** layers — this file, then the built-in `DEFAULTS` in `hooks/lib/config.ts` — so a declared value wins outright and an omitted or invalid one falls straight through to what fusion ships. The plugin layer that used to sit between them went with the guard settings it carried. Retirement now has two scopes rather than one leaf: `RETIRED_PROJECT_FILES` names a leftover `fusion-guard.json` at the project root and `RETIRED_TOP_LEVEL_KEYS` names `guard`, `decisions`, `escalation` and, since 260824, `churn` inside the new file; either earns one advisory per guarded call until the file or the key is deleted, and the file advisory says there is nothing to copy across, because every setting that file could carry is retired too. Nothing in the guard defends this file from an agent; the git diff is the only bound left, which is what the template's `_gitTracked` note says.

- **SessionStart hook output** — `systemMessage` JSON for user-visible banners; plain stdout is `additionalContext` for the model only.

### How far the domain-cascade reach gate reaches

One decision — which domain a workbench is — is stated in one place, `agents/orchestrator.md` Setup Step 5. A gate in `domain-cascade.test.ts` reads the plugin's own prompts, skill bodies and rule files and fails if a second one states it too.

**The paragraph below is generated.** It is rendered from the `REACH` object in `hooks/lib/domain-cascade.ts` by `describeReach()`, and the test suite compares this file against that output byte-for-byte. Editing it by hand fails the suite. That is the point: the claim about this gate has twice been broader than the gate itself — once asserting a second definition was impossible while one was shipping, once naming three holes when there were four — and a sentence generated from the thing that measures cannot say more than the measurement does. Each line below carries probes the suite runs, so a hole that gets closed and a claim that gets stale both turn the suite red.

Regenerate after changing `REACH`:

```bash
cd hooks && npm run build && cd .. \
  && node -e "import('./hooks/dist/lib/domain-cascade.js').then(m => console.log(m.describeReach()))"
```

<!-- BEGIN generated: domain-cascade reach -->
**Scanned:** `agents/*.md`, `skills/*/SKILL.md`, `rules/*.md` — every agent prompt, every skill body, every rule file. Exactly one of them may state the cascade.

**Caught.** Each line is asserted against probes in `domain-cascade.test.ts`:

- A domain name in backticks, double quotes, single quotes or asterisk bold. Four spellings, because nothing in this project requires one of them and the removed work-queue prompt wrote the names bare.
- A paraphrase naming the counts in prose — code files, source files, data files, and the decisions, issues, analyses and commits the retired branches read — rather than by variable name.
- A paraphrase written with the cascade's own variable names.
- A stale copy restating the retired four-outcome cascade. Its two surviving outcomes still fire, and the four counts only the retired branches ever read are still recognised as inputs, so the likeliest second copy from here on is caught rather than walked past.
- A stale copy written only in the two retired outcomes, `strategic` and `knowledge`. The retired names are recognised as domain names for the same reason the retired counts are recognised as inputs: a paragraph written before 2026-08-15 and never re-read is the copy most likely to be met from here on.
- One sentence hard-wrapped across two lines. A line and its continuation are scanned joined, which is the shape this repository's own 78-column prose produces by default.

**Not caught.** Each line is asserted to still be a miss, so closing one of these turns the suite red until this list is corrected:

- A domain name written as a plain word, with no markup around it. This is the plainest second copy anyone would write and it is NOT caught. Matching bare words was measured over the scanned set and rejected on cost, because both surviving domain names are ordinary English words in these files and `code files` is both a domain name and an input phrase. The figure below fell from 11 to 8 at the v11 roster cut, which removed three of the scanned prompts; the cost is a property of the corpus, so it is re-measured whenever the corpus moves and never carried forward. Measured cost of matching bare words, across the scanned set and outside the definition site: 8 lines of honest prose selected on single lines, 8 with the continuation window. The suite re-measures both numbers.
- A paraphrase spread across the rows of a table, or across three or more wrapped lines. A table row and a list item each open a block and are never joined to the line above them, and the window is two lines wide.
- A paraphrase naming no input. It names no evidence, so it restates less than the cascade decides.
- A paraphrase naming its inputs in words the prose list does not carry. The list is a fixed set of spellings, not a synonym set.

**Not scanned**, with what running the gate over it yields today:

- `docs/*.md` — clean. Left out on a measured cost that has since expired. A line in `docs/philosophy.md` said what each of four domains PRIORITISED, in a line shape-identical to a paraphrase, and scanning `docs/` meant either that false positive or an exemption list. With two domains the line names no count and the directory now measures clean, so the reason for the exclusion is gone and only the exclusion is left. That is an uncovered directory, not a justified one.
- `CLAUDE.md` — clean. A consumer by the same contract that puts `rules/` in the file set, and it is not scanned. That reason stands; the measurement moved. It measured clean until the retired domain names joined the detector on 2026-08-24, then fired on one line — its agent-roster line, which names the two retired values beside the two live ones and the words the input list carries. That line left this file for `README-agents.md` on 2026-09-16, so the file measures clean again and the widening's one measured cost is the entry below rather than this one. Re-measured, not re-baselined: the line is unchanged and is still outside the scanned set.
- `README-agents.md` — fires. Where the agent-roster line went, and the entry above is the only reason this one exists: the one measured cost of the 2026-08-24 widening is a property of that line, not of the file holding it, so it is declared wherever the line is rather than dropped when the line moves. Not scanned, and not a consumer — it is the reference documentation for the prompts that are.
- `README-hooks.md` — clean. Documentation about the gate, including this block. Not scanned, and it would be wrong to scan the file whose job is to quote the claim.
<!-- END generated: domain-cascade reach -->

### The bin/ helper roster

Every executable under `bin/`, and what each is for. The roster stood in
`CLAUDE.md`'s Layout table until 2026-09-12 and was moved here unchanged: 22 rows
and 29 660 bytes, a third of a file charged to all eleven dispatch paths at zero
head-room, while this file sits on none of them (decision `260911-2237_*_where-does-the-bin-helper-roster-belong-when-a-third-of-claude-md-is-pointers-charged-eleven-times.md`,
option 1). Twelve of the rows say in their own text that the helper's own header
is the authoritative documentation; that remains true, and this table is the
index rather than the documentation.

`hooks/lib/__tests__/derivable-enumerations-lint.test.ts` section 7 holds the
enumeration closed against this table in both directions: every file under `bin/`
has a row here, and no row names a file that does not exist.

| Path | Purpose |
|---|---|
| `bin/monitor` | HTML dashboard binary, served by the orchestrator from `fusion-workbench/monitor` (copied at Setup) |
| `bin/fusion-rules` | Per-agent rule discovery helper (used in every agent's Setup). Takes an optional second arg `fusion-rules <agent> [<topic>]` and reads an optional consuming-project manifest `./rules/context-manifest.yaml` to emit topic- and agent-scoped units (a `path` file or a `skill:<name>` pointer). When the manifest is absent, output is **byte-identical** to before (`HYG-NO-REGRESS`); a malformed manifest fails loudly (exit 3). Topic is derived from the work item this checkout has claimed (its slug's keywords, or an explicit `Topic:`/`Tags:` line in the item) unless the CLI arg overrides it; the claim is compared by equality on the eight hex characters, so an item another checkout holds resolves nothing. Convention authored in `rules/context-manifest.md` + `rules/context-lean-claude-md.md`. Added v5.x (Circle B). |
| `bin/fusion-paths` | Per-consumer path resolver (used in every agent's and skill's Setup, alongside `bin/fusion-rules`). Emits `KEY=value` write/scan targets (`OUT_*`, `SCAN_*`, `WORKBENCH`) so no prompt hard-codes a store path. The set of keys a consumer gets is *derived* from grepping that consumer's own prompt — never declared. `<name>` is an agent OR a skill (one flat namespace). Signature `fusion-paths <name> [<item-dir>]`: the optional second argument names the work item in scope, as a directory name that must already exist in the container store, and it is how a dispatcher sends an agent into an item this checkout does not hold; one that names no directory is exit **1**, a caller's mistake and not a workbench fault. With none, the scope is this checkout's own claim, read through `bin/fusion-claimed-package` — the single implementation of that criterion, called rather than re-derived. Every `OUT_*` then lands in the container of the item in scope, or in the shared store when there is none; every `SCAN_*` names both, container first, collapsing to the shared one alone. Exit shape mirrors `fusion-rules` (0/1/2) plus its own 3 and 4. **Exit 3 is scope undeterminable and nothing else** — two claimed items, or this checkout's identifier unreadable inside a git work tree — and never a fall back to the shared store, which is exit 0 and a different answer. It is not the old orphaned-`.active-circle` code wearing a new meaning; that one went with the pointer. Exit 4 stays a fusion bug (a prompt names a key the resolver cannot value), and `fusion-rules`'s own 3 is a malformed manifest and says nothing about paths. |
| `bin/fusion-workbench-root` | Walks up from `pwd` to find a directory containing `fusion-workbench/.fusion-setup`. Prints the absolute path or exits 1. The single source of truth for "is this project fusion-set-up, and where". Hooks call the TS equivalent (`hooks/lib/workbench-root.ts`); agents call this script. |
| `bin/fusion-stores` | The bash copy of the store names v12 renamed, and of their v11 names for the transition window: six `KEY=value` lines, no arguments. `bin/fusion-paths` and the claim scan read it rather than naming a store; `fusion-stores.test.ts` holds it equal to `hooks/lib/stores.ts`. **Its own header is the authoritative documentation.** |
| `bin/fusion-plugin-cwd` | Answers "is cwd the fusion plugin's own source repo?" — exit 0/1, prints nothing. **The only cwd-anchored implementation of the criterion.** It was the shell half of `hooks/lib/self-detect.ts` `isFusionPluginCwd()` until that function was deleted on 260816 with the guard's stand-down; what survives on the TypeScript side is the root-anchored `isFusionPluginRoot(dir)`, which is the other coordinate space rather than a duplicate. The criterion is still kept consistent across the two, and the helper's own header carries the pairing and its residual imprecision. Consumed by `bin/fusion-rules` (work-tree rules preference), `bin/fusion-paths` (work-tree prompt preference) and `bin/fusion-source-root` (the root a skill body's citations of shipped files open against) — see the Rules-loading convention below. Checks `./.claude-plugin/plugin.json` **at cwd, with no upward walk** — deliberately congruent with the root-anchored form's criterion, so from a subdirectory of this repo the helpers behave as in a consuming project. That used to happen *silently*; since the SessionStart warning (`hooks/session-start.ts`) a session started below the root says so on its first screen, naming both directories. The behaviour is unchanged — only its visibility. Added v5.x per decision `260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen.md` (option c, textschicht Circle). |
| `bin/fusion-source-root` | The root a citation of a plugin-shipped **file** resolves against, read at the first step of every skill body that opens one, instead of each body restating the branch. Which bodies those are is a list that moves: `grep -rl fusion-source-root skills/` names them, and `skills/archive/SKILL.md` joined the original four on 260819 without this row noticing. Prints `$PWD` when `bin/fusion-plugin-cwd` says cwd is this repo, `$FUSION_PLUGIN_ROOT` otherwise; exits 2 printing nothing when neither is available, which is the `UNRESOLVED` the callers report. It replaced four executable copies of that criterion across two skill bodies — a correction to one of them once reached two and left two standing (issue `260811-0109_*_the-source-root-rooting-reached-two-skills-and-two-more-still-cite-the-install-copy.md`, decision `260810-2145_*_should-a-repeated-skill-body-snippet-become-a-bin-helper-now-that-one-fact-lives-in-four-executable-copies.md` option 1). It roots **reads of shipped text**, plus exactly one copied-asset comparison: a `bin/` executable is still run from `$FUSION_PLUGIN_ROOT`, and so is an asset a skill copies (`templates/`, `stilwerk/`, `bin/monitor`, `.claude-plugin/plugin.json`), Setup's first copy of a stylometric profile included. The exception, added 260821 with `/fusion:setup` Step 0e, is the *comparison and refresh* of the four profiles already copied into a workbench: that reads the shipped file through this helper, because a check whose whole purpose is to notice a shipped file that moved cannot read the install copy, where `install.sh`'s GitHub tarball means it never moves. It was decided for that comparison alone (`260820-2324_*_is-the-work-tree-the-refresh-source-when-setup-runs-in-the-plugins-own-repository.md`, option 1); whether the work-tree preference reaches helper resolution is part (c) of decision `260810-1544_*_should-prompt-called-bin-helpers-get-one-guarded-call-convention-and-does-the-work-tree-preference-extend-to-them.md` and is deliberately unanswered. Every call site guards with `[ -x ]`, per that record's part (b): the installed copy need not carry a helper added between releases, and a bare call is exit 127 at a skill's own first step. |
| `bin/fusion-session-mark` | Tracks the active orchestrator session via `fusion-workbench/.session-marker` (file mtime is the heartbeat). Subcommands: `check` / `write` / `heartbeat` / `clear`. Used by `/fusion:setup` Step 0c to warn the user when a second orchestrator is starting against the same project. Advisory only — fusion has no concurrency lock; the warning lets the user notice the collision and decide. |
| `bin/fusion-commit-lock` | The POSIX mutex around `git add` and `git commit`, acquired by whoever is about to commit. Atomic `mkdir fusion-workbench/.commit-lock/`, workbench-anchored, so distinct projects hold independent locks while sessions on one project share one. Subcommands `acquire <tag>` / `release` / `with <tag> -- <cmd...>` / `check`, of which `with` is the canonical form. **The protocol is authored in `rules/commit-lock.md`** — who acquires, the two stale-lock paths at 60 seconds, the noclobber holder write and the failure modes are spelled there, and this row deliberately does not restate them. `bin/fusion-rules` emits that rule to `orchestrator` only; the script's own header carries the same contract for a reader who reaches the file first. That file now also carries what is none of those things and belongs to no mutex — the two measured defects behind the orchestrator's commit procedure, the truncated commit message and the `git add -u` staging loss — which `agents/orchestrator.md` `### Step 4 — commit` cites there rather than restating, because the prompt is charged to every dispatch and the rule file is not. |
| `bin/fusion-cadence-anchor` | The per-checkout high-water marks the cleanup pipeline reads so its passes run over the delta instead of the whole workbench (v10.8.1, Phase 2 of `refactor/260827-0335-bookkeeping-cost-repair-plan.md`). One flat `KEY=value` file at `fusion-workbench/.cadence-anchors`, class L in `rules/workbench-tracking.md` — a pulled mark would claim coverage a checkout never performed. **Its own header is the authoritative documentation**: the subcommands (`get`/`set`/`changed-files`/`changed-since`), the key roster, why only a proven `changed=no` may skip a pass, and the exit table. Two consumers: `/fusion:cleanup` Step 3 skips the reconciler dispatch on a proven `changed=no`, and the reconciler's Step 1 inventory reads `changed-files` to open only live-marker records plus what actually moved. Both verdicts run over the tracking corpus, not the class-L live state — surfaces that change on every session would make `no` unreachable. Every call site guards with `[ -x ]` per decision `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md`. |
| `bin/fusion-checkout-name` | The checkout registry's only writer and its principal reader: the one program that turns eight hex characters into a name a human recognises. Four subcommands — `resolve <8hex>`, `roster`, `suggest [<8hex>]`, `register [--alias A] [--person P]` — over one file per checkout at `fusion-workbench/shared/checkouts/<8hex>.md`, class R1, created on the first `register` and never otherwise. It resolves the workbench through `bin/fusion-workbench-root` and this checkout's hex through `bin/fusion-identity`, and nothing it calls calls it back. **Its own header is the authoritative documentation**, and this row summarises it rather than restating it: the usage block, the five-code exit table, the entry grammar with a worked example, and the three reasons the design turns on. Two things a reader should carry away from here. **The alias is an attribute and the hex stays the key** — in the filename, in every event line, in every claim field and in every comparison; promoting the alias would make every record already written read as a stranger's from the switch point backward (`260904-1058_*_is-the-checkout-alias-the-identifier-or-an-attribute-of-the-minted-one.md`). **A resolution failure renders the hex, never a substituted name**: `resolve` on an unregistered hex exits 3 printing nothing, so a consumer can tell a read from a guess and every display site falls back to what it rendered before the store existed. No field carries a hostname, an operating-system account name or a folder path, by decision (`260904-1058_*_does-a-registry-entry-carry-hostname-account-name-and-folder-path.md`, option 1). A collision on an alias is reported and never enforced, for the reason the claim field already carries: a checkout that has not pulled cannot see an entry that has not been pushed. Every call site guards with `[ -x ]` per decision `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md`. |
| `bin/fusion-prose-metric` | Counts a file's em-dashes **in its own prose voice** and reports the rate against the ceiling of one per 1000 prose words. Bash and awk; no node, no compiled half; `fusion-prose-metric.test.ts` drives the real script (added after this row first claimed there was no test file). **Its own header is the authoritative documentation**, and this row summarises it rather than restating it: the usage block, the exit-code table, and the four regions that are not prose. Those regions are a fenced code block, an inline code span, a block-quote line, and, in a YAML profile, the subtree of an `examples:` or `anti_examples:` key; word counts exclude the same four, so a file is never credited with the words its exhibits are made of. It exists because the measurement it replaces, `grep -o` over a whole file, counted a file's exhibits of the em-dash fault as instances of it: `rules/user-facing-output.md` read 6 and failed its own ceiling while carrying **one** em-dash of its own. Only `—` U+2014 is counted, never `–`. The fence rule is deliberately not shared with `fencedContentLines()` in `hooks/lib/__tests__/helpers/citation-scan.ts`, which is test-scoped, line-level, and covers one of the four; that duplication is a known residual the header records. **It reports and never gates:** no exit code carries a prose verdict and nothing wires it into `npm test`, which is the same stdout-verdict rule `bin/fusion-staging-drift` and `bin/fusion-review-coverage` carry. Whether the ceiling is read per file or across the always-on corpus is answered per file, by the orchestrator during an unattended run and not by the user; the record was deferred on 2026-08-29 to a later Circle that meets the question again (`260820-2314_*_is-the-em-dash-ceiling-read-per-file-or-across-the-always-on-corpus.md`). |
| `bin/fusion-claimed-package` | Which work package this checkout has claimed — one implementation, called by the two Setup helpers that ask. Prints `PACKAGE=` and `CONTAINER=` under whichever container root the record stands in; it was named fusion-claimed-item, printing `ITEM=`, until v12. **Its own header is the authoritative documentation.** Two claimed items, under one root or across both, is refused rather than resolved, and unknown scope is never the shared store. |
| `bin/fusion-identity` | The one place a run obtains **who** wrote a record and **which checkout** holds it. Prints `PERSON=<name> <<email>>` from `git config`, read and never written, and `CHECKOUT=<8 hex>` read from `fusion-workbench/.checkout-id`, minted there on first read by a noclobber write so two concurrent agents mint one identifier. **Its own header is the authoritative documentation**, and this row summarises it rather than restating it: the usage block, the six-code exit table and why the identifier is minted rather than derived. Two things a reader should carry away from here. **Exit 1 is the only code that means stop** — inside a git work tree with `user.name` or `user.email` unset, or with no `git` that can be run at all, the caller halts, files nothing and substitutes nothing; the condition is evaluated in this one place and no agent prompt restates it. **Not a git work tree is exit 4 and never a halt**: no identity is owed there, the record carries the agent alone and the person field is absent rather than empty, which is what keeps single-user non-git operation working (decision `260824-0613_*_does-a-filing-agent-halt-in-a-tree-that-is-not-a-git-work-tree-at-all.md`, option 2). The remaining codes split the two halves so a caller branches on a number rather than on a missing line: 3 the checkout half unresolved, 4 the person half not owed, 5 both, 2 usage. `.checkout-id` is class L in `rules/workbench-tracking.md` and never travels. |
| `bin/fusion-events` | Reads `fusion-workbench/orchestrator-events.jsonl` by the identity on each line rather than by a line's position in it. Its subcommands are `presence` and `dispatches`. `presence [--days N]` names the other people and the further checkouts of your own that have started a session inside the window, one `party=` line each (TAB-separated, because the person value is git's `Name <email>` form), with the fifth field naming what the party is on: the `work_item` of its latest `task_start` in the window (the guard writes it on every dispatch that names a `**Work-item:**`), else the Circle read off a pre-cut `session_start`'s `history_file` (no session row has carried that field since 2026-09-10), else `none on record`, a statement where `unknown` was not (260921); `dispatches [--minutes N] [--since YYYY-MM-DD]` pairs each bound agent's `task_start` with its `task_done` and prints one `dispatch=` line per pair, carrying how long that dispatch ran, against a threshold the reading itself sets, under the counts `counted`, `unattributable` and `unpaired`, which partition every pair the cutoff and the agent filter admitted so that a figure not taken never reads as a zero. A third subcommand, `turns`, stood beside these two until 2026-09-10 and went with the Turn it counted. **Its own header is the authoritative documentation**, and this row summarises it rather than restating it: the usage block, the output shape, the three-class split and the exit table. Three things a reader should carry away from here. **The file carries `merge=union`** (class R2 in `rules/workbench-tracking.md`), so after a pull it holds two checkouts' lines with no ordering between the blocks; sorting it moves a reader from vague to wrong rather than repairing it (issue `260823-1302_*_the-monitor-attributes-a-merged-event-log-to-one-session-and-reports-another-checkouts-state.md`), which is why membership is read off the line's own `checkout` field. **An absent identifier reads as this checkout's own**, so the pre-C4 log is fully counted and a reader that cannot resolve its own identifier behaves exactly as it did before — the degradation is stated rather than hidden. **Identity is passed in, never looked up**: the wrapper reads the `FUSION_PERSON`/`FUSION_CHECKOUT` pair the SessionStart hook exported (that helper's own output, carried forward — v10.8.0) or, with the pair unset, runs its sibling `bin/fusion-identity`, and hands `PERSON`, `CHECKOUT` and the exit code to the node program in the environment, which keeps the criterion evaluated in one place and keeps `hooks/lib/events-query.ts` a pure function of the log text, the reading identity and the current time. A figure that could not be taken is absent from stdout and named on stderr, and the exit code says which one it was: `other_people=0` and `counted=0` are real answers, and nothing here renders a failed read as a zero. Thin bash wrapper over `hooks/dist/events-query.js`, resolved relative to itself so an install copy and a work tree each run their own build; the call site guards with `[ -x ]` per decision `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md`. |
| `bin/fusion-count-sources` | Counts a project's source files and structured-data files for the workbench-domain heuristic in `agents/orchestrator.md` Setup Step 5 (`code_files`, `data_files`). Prints `code_files=`, `data_files=` and `counted_by=` in the same `KEY=value` shape `bin/fusion-paths` uses. **Its own header is the authoritative documentation**, and this row summarises it rather than restating it: one counting mechanism and no fallback (`git ls-files`, so whatever `.gitignore` excludes never appears), the exit-code table, and why an absent count is `unavailable` and never `0` (a zero inverts the very ratio the heuristic reads). Decision `260809-1731_*_how-should-the-domain-heuristic-count-a-projects-source-files.md` chose the mechanism; the call site guards with `[ -x ]` per decision `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md`, because the installed copy need not carry a helper added between releases. |
| `bin/fusion-session-domain` | The one place a skill body reads the session's domain. Prints `domain=<code|data>` and `source=<event-log|default>` in the `KEY=value` shape (a third `source`, `helper-missing`, is what a call site prints on its `[ -x ]` miss, and the header defines it), reading the `domain` on the newest hook-written `session_start` row for this checkout in `fusion-workbench/orchestrator-events.jsonl`, and falling back to `code` with the fallback named on its own line and on stderr; exit 3 with nothing on stdout when no workbench is above the working directory, because a defaulted domain there would be an answer about a project that never ran setup. **Its own header is the authoritative documentation.** Replaced the two-line read three skill bodies each carried, which had diverged on the first copy; the one call site left is `skills/reconcile`, guarding with `[ -x ]`; of the other two, one went at v11 with the portfolio command and one on 260910 with the Circle it captured — read the set with `grep -rl fusion-session-domain skills/` rather than from this sentence. **One source and no second one**: a read of a session state file stood behind the log until 2026-09-10 and went when nothing wrote that file, because a fallback onto a file no writer maintains answers with whatever the last writer left. Decision `260810-2145_*_should-a-repeated-skill-body-snippet-become-a-bin-helper-now-that-one-fact-lives-in-four-executable-copies.md`, the reserved half. |
| `bin/fusion-staging-drift` | Names what the workbench is holding that no commit carries (issue `260811-0114_*_the-queue-rebuild-and-its-history-file-never-entered-a-commit-and-survive-only-in-the-working-tree.md`). Every entry under `fusion-workbench/` is printed, in four classes — `record` and `commit-message` are the fault classes and the only ones reaching `verdict=`, `in-flight` is the machine-written set and never a fault, `unclassified` is named with the statement that nothing is claimed about it. Thin bash wrapper over `hooks/dist/staging-drift.js`, resolved relative to itself. Authored in `hooks/lib/staging-drift.ts` and described in `README-hooks.md`'s `hooks/lib` table; usage and exit codes in the script's own header, including the same stdout-verdict rule its one remaining sibling carries. There were three of these thin wrappers until 2026-08-15, when `bin/fusion-state-drift` went with the counters it measured; `bin/fusion-review-coverage` is the other survivor. It reports and never stages: a mechanism that staged would be a second author of a staging list whose whole value is that a human wrote every path in it. |
| `bin/fusion-review-coverage` | Tiles the review files' declared `**Reviewed-range:**` fields against the session's commit range and names, commit by commit, what no reviewer opened (issue `260810-1205_*_seven-of-sixteen-commits-in-the-session-range-never-reached-a-review-pass-and-nothing-measures-the-gap.md`). Read by `agents/orchestrator.md` `## Review coverage` and `## Closing a work item`; `--since` defaults to the `git_head_at_start` on the newest hook-written `session_start` row for this checkout, which is machine-written and cannot go stale — an anchor rather than a tally, which is why it outlived the session counters and then the state file that had been its fallback. Thin bash wrapper over `hooks/dist/review-coverage.js`, resolved relative to itself. Authored in `hooks/lib/review-coverage.ts` and described in `README-hooks.md`'s `hooks/lib` table; usage and exit codes in the script's own header, with the same stdout-verdict rule. It reports and is **not** a release gate — whether a release may go out over an uncovered range is an unfiled decision this program does not pre-empt. |
| `bin/fusion-plan-size` | Measures the live (`_o_`/`_p_`) plans in every planning store against a plan-size ceiling and names, largest first, the ones over it; shaper specs are excluded and counted on their own key, because a spec is not a plan and the ceiling is not about it. Thin bash wrapper over `hooks/dist/plan-size.js`, resolved relative to itself so an install copy and a work tree each run their own build. **Its own header is the authoritative documentation**, and this row summarises it rather than restating it: the usage block, the `KEY=value` output, the per-plan rows and the exit table. Two things a reader should carry away. **It reports and gates nothing** — no exit code carries the verdict, no test and no command runs it, which is the same stdout-verdict rule `bin/fusion-staging-drift`, `bin/fusion-review-coverage` and `bin/fusion-citation-check` carry; ruled that way because no plan has a measured reader, so a hard bound would be the first in that cut enforced without one (`260909-1700_*_does-the-plan-size-ceiling-fail-hard-or-only-report.md`, option 1). And **the ceiling is a chosen value, not a measured threshold**: the library header says what it was chosen against, and `--ceiling <bytes>` re-reads the same corpus at any other number. Every call site guards with `[ -x ]` per decision `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md`. |
| `bin/fusion-claude-md-weight` | Where a project's `CLAUDE.md` carries its weight: it divides the file by one heading level, picked once, and prints bytes and lines per section, largest first, marked against a stated **reporting threshold**. **Its own header is the authoritative documentation**, including the threshold's value and what it was chosen against. Two things a reader should carry away. It **renders no verdict about topic**: whether a section is bound to one is a property of the work a reader is doing and not of the text, so no input this program has decides it, and the judgement stays the reader's, applied with the criterion in `rules/context-lean-claude-md.md` (`260916-1126_*_may-the-drift-check-report-weight-when-it-cannot-decide-topic.md`, option 1 — and the same record is why no keyword list or delegation heuristic may be added here). And it **reports**: it writes nothing, says so on every output shape, exits 0 on every path including a root with no `CLAUDE.md`, prints one line when no section is over the threshold, and names `/fusion:curate` as what acts on a finding. Self-contained POSIX shell with no compiled half, unlike `bin/fusion-plan-size` above. |
| `bin/fusion-work-order` | The order the work-item store imposes on itself, computed per run and stored nowhere. **Its own header is the authoritative documentation.** No exit code carries the verdict, and `ready=` is optimistic by the counts a mandatory `note=` line prints. |
| `bin/fusion-edge-answers` | What prior curator runs already asked the user about a work-item edge: one row per proposed edge, carrying the outcome value, the dependent, the target and the field. `agents/curator.md` `### The suppression read` calls it instead of reading the corpus itself — 971 958 bytes over 14 run files on this tree at 2026-09-18, of which exactly one carries an edge entry. **Its own header is the authoritative documentation**: the corpus it reads with nothing excluded, the outcome parse and how it fails, and the exit table. Two things a reader should carry away. **The consequence group is deliberately not a column**, because the suppression key reads the outcome value alone once `not-offered` names the entry the gate never put (`260918-0712_*_how-does-the-curators-edge-survey-know-not-to-re-propose-an-edge-the-user-declined.md`). And it reports and gates nothing: no exit code carries the verdict, the stdout-verdict rule every other reporting helper here carries, and a mandatory `note=` line states any entry it could not resolve. |
| `bin/fusion-citation-check` | Reports, over a consuming project, every record citation the storeless grammar cannot resolve: every `.md` under the workbench, the frozen stores (`archive/`, `stashes/`, `.migration-v2-backup/`) read exactly like the live tree since `32fe0d49` deleted the exclusion, plus the project's `CLAUDE.md`, `rules/*.md`, `.claude/rules/*.md` and `docs/**/*.md`, plus every file the project declared in `citations.extraPaths` (260831), which is how a non-Markdown file enters a corpus at all — outside Markdown there is no fence and no blockquote, so nothing in the text tells a pointer from an exhibit and the project declares instead. `bin/fusion-citation-sweep` resolves the same leaf through the same function, so the two hand-run helpers share one corpus; the blocking gate `hooks/lib/__tests__/workbench-citation-lint.test.ts` deliberately does not read the declaration, because it has no approvable baseline and runs in everyone's `npm test`. Thin bash wrapper over `hooks/dist/citation-check.js`, resolved relative to itself; the grammar is `hooks/lib/citation-scan.ts`, the one fusion's own gates run (decision `260828-0904_*_does-fusion-ship-a-citation-checker-to-consuming-projects.md`). **Its own header is the authoritative documentation**: the `KEY=value` block, the violation rows, `--undecidable`, the exit table. Two things a reader should carry away. It reports and never gates: `verdict=` is a line of stdout and no exit code carries it, the same rule `bin/fusion-review-coverage` and `bin/fusion-staging-drift` carry, and it never rewrites, since the rewriter is `bin/fusion-citation-sweep`, run by hand. And it carries the standing one-release-behind cost of every `bin/` helper: absent from an installed copy until `fusion --update`, so `/fusion:cleanup` prints `helper-missing` through its `[ -x ]` guard until then (decision `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md`). |
| `bin/fusion-citation-sweep` | Rewrites a consuming project's store-prefixed record citations to the storeless form, and repairs (`--repair`) what the v10.20.0 sweep broke. Thin bash wrapper over `hooks/dist/citation-sweep.js`, resolved relative to itself; the same grammar as the checker, so no second detector exists. It was a `.mjs` script under `hooks/scripts/`, one only a checkout with `node_modules` could run, until decision `260829-1623_*_does-fusion-ship-the-citation-sweep-or-only-the-checker-and-under-which-guards.md` (option 2) put it on `bin/` and compiled it. **Its own header is the authoritative documentation**: the modes, the exit table, the rewrite table. Three things a reader should carry away. `--dry-run` is the default and prints the census; `--write` stands behind three guards, each evaluated before a byte moves: the workbench is tracked, no pending change touches a file the run will read, and every extra path argument is inside the work tree and tracked by it (else exit 4, one line naming which condition failed), the census is printed and nothing is written without `--yes` (else exit 5), and there is no bare-stamp resolution and no option to enable one, since that rule produced every corrupted token fusion's own first run left behind. **No fusion pipeline or skill runs it**; `/fusion:cleanup` prints the checker's verdict and stops. And `citation-sweep.test.ts` is a release gate: a swept scratch tree and this repository's own committed workbench must each dry-run to `rewrites=0`, which is the idempotency fusion's first run lacked. |
| `bin/fusion-forum` | Answers what arrived from the remote that this checkout has not read yet, out of a fetched ref and never out of the working tree. Three subcommands: `new <store-rel-path>` fetches, takes the set difference of two `git ls-tree` listings (the one at the mark and the one at the fetched upstream), drops this checkout's own entries and prints the delta as `KEY=value`; `show <commit> <git-path>` prints one entry's blob at the commit `new` pinned; `seen <commit>` advances `last_forum_read_commit` through `bin/fusion-cadence-anchor`, which exists so that key name is spelled in one place and never in a skill body. The store path is passed in and never known here: `bin/fusion-paths` stays its single resolution point, which is why `hooks/lib/__tests__/path-literal-lint.test.ts` needs no `DEFINITION_SITES` entry for this file. **Its own header is the authoritative documentation**, and this row summarises it rather than restating it: the usage block, the exit table, the state vocabulary, and the reasons the design turns on (a set difference rather than a diff, no timeout, no first-run branch). Three things a reader should carry away. **`new=0` is a real answer and never an error** — it reaches exit 0 like any other count, and a caller that reads it as a failure is wrong about this program. **`note=` is one line kind with three distinct uses**, not one condition: the checkout identifier could not be read, so nothing was filtered out and this checkout's own entries may be in the list; the mark does not resolve to a commit here, so the whole store reads as new; the branch tracks a local ref, so no fetch was owed. Each names a degradation that changed the answer rather than hiding it. And **`show` fails with its own exit 1**, git's stderr passed through, instead of leaking git's 128, which is what keeps the exit table total and lets a caller branch on a number this program defines. Every call site guards with `[ -x ]` per decision `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md`. |

## Usage

### Tuning the guard: there is nothing left to tune

**Nothing blocks, so nothing has to be turned down.** This section used to be a table of
four rows — off entirely, advisory-only, looser, and how to clear a stuck halt — assembled
from `guard.enabled`, the per-category sensitivities and `escalation.blocksBeforeHalt`. All
six guard settings are gone with the checks they tuned, and a project that still declares
them in a leftover `fusion-guard.json` is declaring into a file fusion does not read.

What the hook still produces, and how to turn it down: nothing turns it down. The write
trace is one `guard_allow` row per write-tool call, and the configuration advisories stop
when the configuration is fixed, which is the whole point of them repeating.

**Why there is no `enabled: false` any more.** The switch existed so a project could stand
down a mechanism that was deciding against it. Nothing decides, so the only thing a switch
could turn off is the event log the monitor renders — and a project that wants that has
`.guard-state/` in its own tree to delete.

**How a path is used.** It is not matched against anything. `extractFilePath` in `guard.ts`
reads `file_path` (or `notebook_path`) out of the tool input, writes it into the trace row
verbatim, and nothing else reads it. An absolute path stays absolute: normalising it to the
working directory served the pattern matching that is gone, and a trace is more useful with
the path the tool was actually handed.

**The matching that used to happen, kept because an open question outlived it.** Paths were
matched on their *text*, no symlink resolved, against the globs in `guard.categoryPaths`, in
the coordinate space of the process's working directory. The match was **case-sensitive**.
The protected list before it folded case unconditionally on every platform, because a glob
compiles to a case-sensitive regex and on a case-insensitive filesystem — APFS in its
default configuration, so every stock macOS install — the whole list was bypassable by
shifting one letter. Whether `findRelevantDecisions` should have folded too was an open
question (`260804-1632_*_should-findrelevantdecisions-fold-case-…`),
and it is moot rather than answered: both sides of it are deleted.

### What a halt was, and what a project still carrying one should do

A halt blocked every `Write`, `Edit`, `MultiEdit` and `NotebookEdit` call until a human ran
a clearing script, and it printed this:

```
[HALTED] All write operations blocked. The guard has been halted after
repeated violations. The halt is recorded per project and the clearing script
finds it by walking up from its working directory, so the `cd` is part of the
command: cd <project-root> && node <plugin-root>/hooks/dist/clear-halt.js
```

**None of that exists at this version.** The halt, the counter that raised it and the script
named in the message were removed on 2026-08-16. A `haltActive: true` left in a project's
`fusion-workbench/.guard-state/escalation.json` is an inert flag that nothing reads: it
blocks nothing, and there is no command to clear it with. `/fusion:setup` notices the file
and offers to delete it, which removes a flag rather than unblocking anything.

It never reached the shell in its last year either. The halt once blocked every `Bash`
command a mutation classifier recognised as a write, which is the undecidable question in
miniature — "does this command write a file at all?" — and it went with the classifier.

In an existing event log the halt rows are distinguishable by their detail: `Halt active —
write tool call blocked`, `Halt raised by this block — <cause>`, `Halt raised by the
protected-path measurement (<n> path(s) changed)` and `Halt active — mutating Bash command
blocked`. **Nothing writes any of them.** The `guard_block`, `guard_halt` and `halt_cleared`
event types left the vocabulary on 2026-08-16 with the mechanisms behind them; `bin/monitor`
still styles the rows, deliberately, because a log written before the removal is still read
after it.

### Per-project configuration: `fusion.json`

A consuming project may ship `fusion.json` at its project root — `/fusion:setup` seeds it from `templates/fusion.json`, declaring nothing but its documentation notes — and the hooks read it on every guarded tool call. **What it configures is the `citations` container and nothing else:** `citations.extraPaths`, the project's own list of citation-bearing non-Markdown paths, and `citations.exhibits`, the records it declares as exhibits, both of which the two citation helpers resolve when they are run. No hook reads either. Two leaves stood beside it until 2026-09-10 and both went that day — `orchestrator.maxTurns` with the Phase-2 Turn loop, `orchestrator.dispatchMinutes` with the dispatch bound. A project still declaring either gets one advisory naming that leaf, and the `orchestrator` container is now walked for its two retirements alone. Everything else the file used to carry configured the guard, which decides nothing.

**Two layers, merged per leaf key.** The project's file, then fusion's built-in `DEFAULTS`. There were three until 2026-08-16; the middle one was the plugin's own configuration file, which existed so guard settings had a plugin-level default a project could narrow, and it went with them. A layer carrying nothing is a claim rather than a capability.

A key the project declares is taken exactly as written; a key it omits — even inside an object it did declare — falls straight through to the default. `{"citations":{"extraPaths":["src/**/*.go"]}}` sets that list and touches nothing else. A value with the wrong type is dropped, named in a `guard_advisory`, and then inherits exactly as an omitted key does; a file that is not valid JSON is dropped and reported the same way, never ignored in silence. A **missing** file is silent, and must be: it is the ordinary state of a project that has configured nothing.

The leaf walk was kept as the shape through the release in which the orchestrator's budget was the only leaf it had to walk, rather than collapsed into a single fallback, so the next setting to land here would inherit the rule instead of re-deriving it. `citations.extraPaths` is that setting, and it did; `citations.exhibits` is the second, and inherited the same rule.

#### `citations.extraPaths` — the citation-bearing paths a project declares

`{"citations":{"extraPaths":["src/**/*.go","internal/*.py"]}}` names the **non-Markdown** files that `bin/fusion-citation-check` and `bin/fusion-citation-sweep` read in addition to the Markdown corpus they already read. Each entry is a git pathspec evaluated under `:(glob)` — `*` does not cross a `/`, `**` does — resolved against the files git tracks, so an untracked or ignored file is never named by one. A pattern that is absolute or carries a `..` segment is refused from the string alone, before git is asked; a pattern matching nothing is named as matching nothing. Both go to stderr, and the checker prints `declared-patterns=` and `declared-files=` on stdout, the second reading `unavailable` rather than `0` where git will not answer at all.

It is a **declaration** because the question underneath it cannot be decided from a file's text. Outside Markdown there is no code fence and no block quote, and those two are the whole of fusion's pointer-versus-exhibit distinction: a record name inside a docstring and one inside a comment that points at a real record are the same input to any reader of the bytes. So a project writes down which of its files cite records, and fusion detects nothing. Declare nothing and you get `[]`, which is exactly the corpus the two helpers read before the leaf existed — no extra files, no advisory, no moved figure.

The value must be an array of strings with none of them empty; anything else is dropped **whole**, named in a `guard_advisory`, and inherits as an omission does, so one bad entry never leaves half a declaration in force. The loader answers only "is this the right type"; whether a pattern names any file is answered once, at the resolution site. Both hand-run helpers read the leaf and the blocking gate `hooks/lib/__tests__/workbench-citation-lint.test.ts` deliberately does not — it has no approvable baseline and runs in everyone's `npm test`, so a corpus set by an editable configuration leaf would redden the suite of somebody who edited nothing.

fusion declares `bin/*`, `hooks/*.ts` and `hooks/lib/*.ts` for itself, and deliberately not `hooks/lib/__tests__/*.ts`: those files are fixtures whose record names are exhibits, which is exactly the judgement a declaration exists to record.

#### `citations.exhibits` — the records a project declares as exhibits

`{"citations":{"exhibits":["<stamp>_*_<slug>.md"]}}` is the one **subtraction** a project can make, per record: each entry is a storeless record basename with the marker wildcarded and ending in `.md`, and a corpus file whose own basename matches one is an exhibit — every token in it is reported `exempt` with the reason `declared-exhibit`, the shape-decided `store-prefixed` verdict included, which is what a fence alone does not silence. The sweep never rewrites it, because it rewrites nothing that carries a reason. Until 260921 a project could widen the corpus and never narrow it, so a record whose subject is a path — a shell transcript, a reproduced checker message, a listing of where files were found — was reported as a violation the tool itself forbade anyone to repair. An entry that does not end in `.md` drops the whole array with an advisory naming the index: a citation may be a prefix, a silencing declaration may not, since `2609` would declare a month of records at once. The checker prints `declared-exhibits=` beside its verdict so the declaration is visible, and the residual is accepted rather than hidden: a genuine violation declared an exhibit is silenced, and nothing mechanical tells the two apart, the same residual the `foreign:` qualifier carries. The blocking gate does not read this leaf, for the reason it does not read `extraPaths`; the write-time hook does not either, but already skips every hit carrying a reason, so only an unfenced token in a declared exhibit still draws its sentence.

The file is **git-tracked on purpose**. It decides which of the project's files fusion's citation helpers read, so every change to it has to appear in a diff and pass the same review as the rest of the project. Until 2026-08-12 the diff was the *second* line of defence: the guard added this file to the protected list as soon as it existed and wrote back any change an agent made to it. That defence went with the protected-path half, so an agent can edit it like any other file and the git history is the only place a change to it shows.

**Retirement is announced, not dropped in silence**, and at two scopes. A setting a project once wrote and can no longer write is neither a validation failure nor an unknown key: it still looks like a setting to whoever wrote it, and going through in the silence every unrecognised key gets is the one thing that must not happen to it. So it is named in a `guard_advisory` on every guarded tool call — Bash included — until it comes out of the project's tree.

- **A retired file.** `fusion-guard.json` at the project root, replaced by `fusion.json` on 2026-08-16. Not one byte of it is read. **Copy nothing across before deleting it**: every setting it could carry is retired too, so the migration is the deletion. That was not so when this diagnostic was written — a Turn budget left behind was silently not applied, and warning about it was the whole of the v10 migration. The budget went on 2026-09-10 and the diagnostic stays, because a project that never migrated still has the file. This is the channel that does not depend on Setup, because it runs on every guarded call where a Setup step runs once per session and only for a project that runs Setup at all. It is not the only place a session hears it — the orchestrator repeats every diagnostic the loader returns in its Setup-complete summary, at its own Setup Step 2, so a project that does run Setup hears it there as well.
- **A retired top-level key** inside the file that *is* read: `guard`, `decisions` and `escalation`, which is what a project sees if it copies its old file across rather than starting from the template.

The leaf-scoped table that named `guard.protectedPaths` on its own has no members left and is gone: that key now sits inside a retired container, so the container's diagnostic names it. It comes back the day a leaf inside a *live* container is retired.

Two things the validator deliberately does **not** do. It does not reject unknown keys — the seeded template is mostly underscore-prefixed documentation notes, and rejecting them would make the shipped template a broken file. And it does not diagnose `null`, which has always meant "nothing configured" here.

### The protected-path half, and why it was removed

This section described a live mechanism until 2026-08-12. It is kept, shorter, because the mechanism's absence is what a reader of an older tree, an older copy of this file, or an existing `events.jsonl` will come here looking for.

**What it was, in two generations.** Until 2026-08-07 it was a classifier: `lib/bash-mutation-guard.ts` read a shell command's text and decided which files that command was about to write, so a write aimed at `guard.protectedPaths` could be denied before it ran. That question is undecidable — a path can be assembled at run time, arrive on stdin, or pass through `eval`, an alias, a shell function or a variable no classifier sees. 12 923 lines were built against it, carrying 21 documented residuals, and four days in a real consuming project produced 17 blocks and 0 real hits (`260807-0825_*_should-the-guard-predict-shell-writes-or-enforce-them.md`, option 3). What replaced it asked a decidable question instead — *has a protected path changed?* — by fingerprinting every path on the list in `guard.ts` before the tool call, fingerprinting them again in `tracker.ts` after it, writing back whatever moved and raising the halt outright. The route to the file no longer mattered, which is what the prediction had never managed.

**Why the whole half went anyway.** Measuring the result was the right answer to the wrong question. Across roughly 450 records in this project and its largest consumer there was no instance of the failure the protection existed to prevent, and the mechanism stood down in fusion's own tree — the only tree whose patterns name what they say they name — from the first public release. What it did cost was measurable: 53 records in one consuming project that exist only because agents may not write files that project owns, across 143 days, against zero halts. Six modules, a shell reporter, eleven test files and one always-on agent rule went with it.

**What went with it, by name.** Four modules under `hooks/lib` — `lib/protected-snapshot.ts`, `lib/rules-write-exemption.ts`, `lib/fs-locator.ts`, `lib/reverted-copy.ts` — plus the standalone effective-list reporter and its `bin/` wrapper; CHECK 2 in `guard.ts` and job 1 in `tracker.ts`; the `guard.protectedPaths` configuration leaf, the self-protection floor over `fusion-guard.json`, and the `FUSION_ALLOW_RULES_WRITE` session flag that existed only to soften the deny. Nothing replaced any of it: a protected path is not listed, not watched, not restored and not reported, by any hook.

**What survives it.** One thing, and it is not a mechanism. The rule the two stand-downs established — evaluate a stand-down in the coordinate space the mechanism keys its state by — outlived the measurement it was measured on, the churn heatmap that carried it next, and the write-tool stand-down it was last written for. It is kept in `lib/self-detect.ts` against the next mechanism that needs one.

The migration this section used to describe is over. A halt raised by the old mechanism blocked at CHECK 1 and cleared through `clear-halt.js` until 2026-08-16; both are gone, and a leftover flag is now an inert value `/fusion:setup` offers to delete. A project whose root still carries `fusion-guard.json` at all — `guard.protectedPaths` inside it or not — gets the retired-*file* advisory described under [Per-project configuration](#per-project-configuration-fusionjson), which is a louder statement than the retired-key advisory it replaces.

### Viewing the event log

```bash
jq -R 'fromjson? // empty' fusion-workbench/.guard-state/events.jsonl
```

Or tail it in a second terminal during a session:

```bash
tail -f fusion-workbench/.guard-state/events.jsonl | jq -R 'fromjson? // empty'
```

Per line, not `jq .` over the stream: this log is written by one function, `emitEvent` in `hooks/lib/events.ts`, in a single append per row, so a torn line takes a crash mid-write, but the orchestrator's log has shell writers and the two are read with one habit. The convention and its reason are stated once, in the event-log row of `README-agents.md` `### Orchestrator observability`.

### Running tests

```bash
cd hooks && npm install && npm test
```

`npm test` compiles first and then runs vitest, as it always did. What changed is where the compile goes: into a private staging directory of its own, named to the run in `FUSION_TEST_DIST`, from which the shipped `hooks/dist/` is refreshed file by file. `npx vitest run` on its own still works and skips the compile.

**The suite is safe to run concurrently with itself in one checkout**, which is not a courtesy — the orchestrator dispatches executors in parallel batches and each of them runs this command to decide whether its own change lands. Three things make that true, and each is documented at its own source:

- `hooks/scripts/build.mjs` — the build never deletes `hooks/dist/`. It replaces each file with `rename(2)`, so a path under `dist/` is never momentarily absent and never half-written, and it prunes an output only once its source is gone. It used to be `rm -rf dist && tsc`, which left the whole tree missing for one to two seconds per run and failed three suites in three different shapes.
- `hooks/lib/__tests__/helpers/guard-harness.ts` `TEST_DIST` — the cases that spawn or copy a compiled artifact read the run's private build, not the shared one.
- `hooks/vitest.config.mjs` — one run uses half the machine's cores rather than all of them. Measured: at one worker per core, three concurrent runs failed 5 of 9 times on fork/exec starvation; at half, 9 of 9 were green, and a single run costs nothing measurable.

### Three gates that can fail the suite over text nobody compiled

`npm test` blocks on three cases that are not about the code under them. Each is documented in full at its own source; the table exists so a reader meets them before a red run rather than during one.

| Gate | Goes red when | What the failure names |
|---|---|---|
| `hooks/lib/__tests__/committed-dist.test.ts` | the committed `hooks/dist/` is not the compilation of the committed source, or the installed TypeScript is not the exact version `hooks/package.json` pins | which of the two it is. A wrong compiler is reported as a toolchain mismatch and explicitly not as an artifact defect, so the remedy is the install and not a rebuild; a stale artifact names the differing files and the `npm run build` that repairs them. |
| `hooks/lib/__tests__/workbench-citation-lint.test.ts` | a live workbench record carries a citation that resolves to nothing. The corpus is the Circle records, `portfolio.md`, the open issues, the live decisions and the live plans — a live plan being one carrying `_o_` or `_p_`, the two states in which an executor is dispatched against the document, and `portfolio.md` counting where the checkout has one, being class L of `rules/workbench-tracking.md` and so regenerated per checkout rather than carried by git. | the record, the line, the token, and the two remedies its message carries: correct the path, or restate the token as a statement rather than a pointer. |
| `hooks/lib/__tests__/plan-stopping-section-lint.test.ts` | a live plan carries no `## Where this work stops`, an empty one, or nothing but the shipped angle-bracket placeholder | the plan, which of the three failures it is, and what to write in the section. It judges presence only, never whether a stopping condition is the right one. |

**The middle one is the one that surprises**, and the surprise was accepted rather than mitigated. Its corpus is recomputed from the tree on every run and it carries no approvable baseline, so there is nothing an author can edit to make it green except the citation itself. The cost is that an archive sweep, or a newly filed record carrying a bad citation, turns the suite red for somebody who touched no citation and no code. That is the gate working. The governing record is `260819-1645_*_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md`.

The four growth bounds in the next section are blocking in the same way, and are documented there.

### Growth bounds on the shipped text

Three surfaces of this plugin have a **failing** bound on how much they may grow, and the suite goes red when one of them spends its head-room. This is a bound on the *rate of addition*, not on size: a shrink never trips it, and each surface's head-room was derived from that surface's own replayed `git` history rather than picked. A fourth bound, the per-dispatch-path total, works differently and is **not** documented here. It is authored where it is enforced: `hooks/lib/__tests__/rules-emission-golden.test.ts`, the `THE DISPATCH-PATH BOUND` banner comment and the `DISPATCH_HEAD_ROOM` constant under it, with the eleven rows and their arming provenance in `hooks/lib/__tests__/fixtures/dispatch-path.baseline`, which carries its own header stating what the arming did and did not absolve. Those two are the whole account; this section deliberately neither restates them nor lists that bound in the table below, because it measures a different quantity — the LEVEL one dispatch reads, not the RATE at which a surface grows. One thing about it *is* here and nowhere else, because the ruling that authorised it put it here: the log of the 2026-09-22 re-arming, in `#### The dispatch-path re-arming of 2026-09-22` at the end of this section. A log is not the authoring; the event itself is defined in that fixture's header.

| Surface | Unit | Head-room | Bounded in |
|---|---|---|---|
| `agents/*.md` | bytes | 18 000 | `hooks/lib/__tests__/surface-growth-bound.test.ts` |
| `skills/*/SKILL.md` | bytes | 39 260 | `hooks/lib/__tests__/surface-growth-bound.test.ts` |
| the hook test suite | lines | 3 030 | `hooks/lib/__tests__/surface-growth-bound.test.ts` |

**A fourth row stood at the head of that table until 2026-09-11: the always-on rule set every agent loads, bounded at 12 000 bytes of head-room in `hooks/lib/__tests__/rules-emission-golden.test.ts` since its arming on 2026-08-14. It was retired, and this is why that was safe.** The per-dispatch-path bound counts every always-on byte inside the rule component of all eleven paths, at zero head-room, so one byte added to a file every agent loads is charged eleven times there and once here — and whichever bound has less margin binds first. Measured at `9ff123c5`: the always-on set stood at 74 873 bytes against a budget of 77 498, so 2 625 bytes of margin, while the tightest path, `reviewer`, stood at 188 842 against a baseline of 189 012, so 170 bytes. 170 is less than 2 625, so the per-path bound already refuses every always-on addition the retired bound would have refused, and 2 455 bytes earlier. Nothing was opened; a second, weaker assertion over bytes that are bounded harder elsewhere was removed. The defect that asked for it carries the other half of the argument: the retired bound could see neither `CLAUDE.md` nor a conditionally emitted rule file, and over the thirteen days to 2026-09-09 it stayed green while the coder path rose 21.9 percent through exactly those two. What stays in that file is the never-failing report on each role's *own* rule text.

**The last two figures are not the derived ones.** `skills/` was derived at 20 000 and the hook tests at 2 500. The hook tests were raised five times, once on 2026-09-11, three times on 2026-09-16 and once on 2026-09-18; `skills/` four times, twice on 2026-09-11, once on 2026-09-16 and once on 2026-09-17, each under its own ruling and all of them read back on one date. What each raise bought, what is left of it, and what restoring the derived figure costs are in `#### The head-room raises, and the reduction read on 2026-10-10` at the end of this section.

**The budgets are independent.** One instrument computes all of them — `hooks/lib/__tests__/helpers/growth-bound.ts`, which takes the baseline map and the head-room as arguments so no surface can see another's numbers — but there is no shared pool. Growth in `agents/` cannot be paid for by shrinkage in `skills/`, and a single surface crossing its own bound fails the suite alone. Role-specific rule text only **reports** and never fails: it is bought by the agents that need it, not by every dispatch.

When a bound fires, the failure names the files that grew and by how much. **Cut where the growth is.** Regenerating a golden records the movement and never clears a bound — the golden says what the files measure, the baseline says what they are allowed to measure *from*. A baseline moves at exactly three moments, each of which is written down: after a cleanup, at a one-time arming, or at a merge of two lines of development that were each inside the bound at their own head — the third because the bound measures addition per line and a merge adds two measured lines' growth to one baseline at once. That third event moves the baseline to the merged figure and its entry names the merge commit and both parent figures; a merge whose parents were not each inside is a cut like any other. **A piece of work that only cuts is not one of the three**, however much it removed: the floor stays where it is so that the growth above it stays readable. Raising a surface's *head-room* is a fourth named event, and it moves no baseline at all. The rule is authored once, in `hooks/lib/__tests__/helpers/growth-bound.ts` `## Re-baselining`, and is not restated here; what is written down below is the log of when each event was used, the measurement the cut-only refusal rests on, and the records that govern all four.

Each bound keeps a golden fixture so that every movement lands in a diff somebody reads. Regenerating one is deliberate and cannot be left switched on — the run rewrites the fixture and then fails on purpose:

```bash
cd hooks && UPDATE_RULES_GOLDEN=1   npx vitest run lib/__tests__/rules-emission-golden.test.ts
cd hooks && UPDATE_SURFACE_GOLDEN=1 npx vitest run lib/__tests__/surface-growth-bound.test.ts
```

**What no bound covers.** The hook-test surface counts every `.ts` file in the suite's own tree at any depth (`hooks/lib/__tests__/**.ts`) — a recursive walk rather than a list of directories, so a test file in a new subdirectory is measured on the day it arrives; the three `.mjs` files under `hooks/` — the build script, the test runner and the vitest configuration — are hook scripts rather than tests and fall outside every surface. Nothing bounds them, and nothing bounds `hooks/*.ts`, `hooks/lib/*.ts`, `bin/`, `docs/` or the READMEs either. That is a statement of coverage, not a justification: those surfaces were not measured, and arming a bound on a corpus nobody measured is the one thing the instrument's own rule forbids.

- **Shipped text carries failing growth bounds (260815).** Editing `agents/*.md`, `skills/*/SKILL.md`, the hook tests or a rule file can turn `npm test` red — not for what the text says, but for how much it grew. **Which surfaces are bounded, at what head-room, at what each is measured from, and what no bound covers is this section, and a pointer at it cites `README-hooks.md` `### Growth bounds on the shipped text`.** Do not copy a figure out of it into the pointer: every one of them has moved. **The budgets are independent:** shrinking `skills/` buys `agents/` nothing, by construction. A shrink never trips a bound — they measure the rate of addition, which is what the project's own history says the binding constraint is. **One further bound works differently and reaches `CLAUDE.md`**: `hooks/lib/__tests__/fixtures/dispatch-path.baseline` charges that file to all eleven dispatch paths at **zero head-room**, so N bytes added to it puts every path N over at once and must be paid for inside the same paths. **The way out of a red bound is a cut, never an edit to a baseline or a head-room constant**; both move only at the events named in `hooks/lib/__tests__/helpers/growth-bound.ts` `## Re-baselining`.

#### When each event was used, and what governs it

**Three armings.** A bound that used to report starts blocking on a corpus already past the head-room it would enforce, so the baseline is set at the arming rather than at the old figure. It has happened three times: on 2026-08-14 for the always-on rule core (`hooks/lib/__tests__/rules-emission-golden.test.ts`, retired 2026-09-11 — see above), on 2026-08-15 for the three surfaces in `hooks/lib/__tests__/surface-growth-bound.test.ts`, and on 2026-09-09 for the per-dispatch-path total, back in the first of those files. The third is the one deliberately taken *before* the cut it bounds rather than at it, because an arming taken at a cut absolves the cut. What each arming's re-baseline let through is read in the record the armed file points at: `260922-0939-rules-emission-golden-cut-log-and-retired-core-bound-argument-2026-08-05-to-2026-09-11.md` for the first, `260921-1855-surface-growth-bound-arming-and-re-baseline-log-2026-08-15-to-2026-09-05.md` for the second, and the third stays as text at the foot of `hooks/lib/__tests__/rules-emission-golden.test.ts`.

**One cleanup and one merge.** The 2026-08-17 cleanup re-baseline and the 2026-09-05 merge re-baseline are both logged in full, with their per-surface figures and what each absolved, in `260921-1855-surface-growth-bound-arming-and-re-baseline-log-2026-08-15-to-2026-09-05.md`, which the header of `hooks/lib/__tests__/surface-growth-bound.test.ts` points at. The cleanup entry is the shape worth knowing: the floor it copied in had *fallen*, and the surface came out of it with less room than the shrink lying under the old baseline had given it. A cut does not bank its own savings as head-room.

**The cut-only refusal, and the measurement it rests on.** Whether a piece of work that only cuts may re-baseline the surfaces it cut was open from 2026-08-22 and was ruled by the user on 2026-09-11: it never does (`260822-1154_*_does-a-cut-only-circle-re-baseline-the-surfaces-it-cuts.md`, option 1). The reason is a measurement rather than a caution. Re-baselining is not one move with one sign, and at `e6703d51` the four surfaces then bounded would have gone four different ways (the first row's bound has since been retired, and the figures are kept as the measurement the ruling rests on):

| Surface | Head-room now | After a re-baseline onto the post-cut sizes |
|---|---|---|
| the always-on rule set | 2 625 bytes | 12 000 |
| `skills/*/SKILL.md` | 0 bytes | 20 866 |
| the hook test suite | 0 lines | 2 595 |
| `agents/*.md` | 61 378 bytes | 18 000 |

Three surfaces would have gained margin nobody earned. The fourth, `agents/`, had cut far below its own floor, and re-baselining would have taken that shrink away. A rule keyed on whether you cut gives the wrong answer on the surface that cut most, which is what makes the literal reading of event 1 indefensible rather than merely risky.

**The request refused in August came back in September as head-room.** On 2026-08-22 a piece of work that needed room to do its work asked for a baseline move and did not get it; the room was cut first (`260822-1102_*_what-happens-when-a-planned-circles-required-work-exceeds-the-remaining-head-room.md`, option 1). On 2026-09-10 the same need was granted as a head-room raise instead (`260910-2256_*_may-the-growth-bounds-be-raised-for-the-duration-of-the-container-restoration.md`, option 2), and the raises it authorised are logged in the next section. The two requests are different: one asks to be absolved of growth somebody is about to choose to make, the other asks for room while every byte of that growth stays charged above an unmoved floor. That is why the head-room raise preserves what the cut-only refusal preserves, and why it is not a fourth way to move a baseline.

**Nothing detects a raised head-room**, any more than a raised baseline. Both are constants in a test file, and the only thing standing between either and a silent move is that a person reads the diff. The eight raises taken so far are visible because each wrote itself down, not because anything checked. The question is open as `260815-1942_*_nothing-detects-a-raised-growth-baseline-and-the-only-bound-on-one-is-a-comment.md`, and the fourth event makes it larger rather than smaller.

**Governing records.** The arming form: `260814-0738_*_how-is-the-always-on-growth-bound-armed-when-the-corpus-is-already-over-budget.md`. The merge form: `260905-1810_*_does-a-growth-bound-re-baseline-after-a-merge-of-two-lines-that-were-each-inside-it.md`, with the measurement that raised it in `260905-1755_*_the-merge-puts-two-surface-growth-budgets-over-while-neither-line-was-over-on-its-own.md`. The cut-only refusal and the head-room raise: the two records named above.

**Why the instrument sits under `hooks/lib/__tests__/helpers/` and does not move up.** Two gates make a top-level `hooks/lib/` module wrong, either one sufficient. `hooks/lib/__tests__/derivable-enumerations-lint.test.ts` holds this file's `hooks/lib` table in exact set equality with the modules in `hooks/lib`, so a module there needs a documented row in the same commit, and a test helper is not a hook module. And `hooks/tsconfig.json` excludes `lib/__tests__`, so nothing under it compiles into `hooks/dist/` and the build's orphan prune never sees it.

#### The head-room raises, and the reduction read on 2026-10-10

Nine raises across two of the head-room figures above have been taken — three on 2026-09-11 (`SKILL_HEAD_ROOM` twice, `TEST_LINE_HEAD_ROOM` once), four on 2026-09-16 (`SKILL_HEAD_ROOM` once, `TEST_LINE_HEAD_ROOM` three times), one on 2026-09-17 (`SKILL_HEAD_ROOM`) and one on 2026-09-18 (`TEST_LINE_HEAD_ROOM`, +190) — and this is the one place that says so. **The ninth is the exception to that, and deliberately.** Its justification is authored on the constant itself in `hooks/lib/__tests__/surface-growth-bound.test.ts` — who authorised it, when, and for what — because the user who authorised it asked for it there; this entry is the index to it and not a second copy. One search for a cut has been taken instead of a raise, on 2026-09-17, and its entry sits at the end beside the eight. **No baseline moved at any of them.** A baseline records what each file weighed when its surface was last settled; head-room records how much the surface may grow from there. These raises move the second and leave the first exactly where it stood, so every byte and every line added under them still counts as growth above an unmoved floor. That is what makes the reduction a reading rather than a promise: the surplus is the sum of the raises, and putting the derived figures back is the whole of it.

| Constant | Derived | Raise still standing | Restoring the derived figure means cutting |
|---|---|---|---|
| `SKILL_HEAD_ROOM` | 20 000 bytes | +19 260 | `skills/*/SKILL.md` to at most 208 768 bytes (floor 188 768 + 20 000), which is 3 829 below where the surface stood at `9d5b1e80`, the commit that landed the third raise |
| `TEST_LINE_HEAD_ROOM` | 2 500 lines | +530 | `hooks/lib/__tests__/**.ts` to at most 21 728 lines (floor 19 228 + 2 500) |

The `skills/` figure is the sum of four raises, +866, +1 045, +3 000 and +14 349, each under its own ruling; the column records the whole distance from the derived figure, because that is what the reduction reads back. Its restore target fell with the floor on 2026-09-16 — the `log-activity/SKILL.md` entry was dropped from `SKILL_BASELINE` with the file, taking the floor 202 397 -> 188 768 — which is the baseline map moving for the reason it always may, that an entry nothing is measured against inflates the floor. The raise did not move it.

The first raise on the hook tests was +154 lines on the day and 59 of it has since been given back, which the paragraph on this section's own move explains; the second, entered below, was +98 and none of it is left, the third, entered after it, was +128 and none of that is left either, the fourth was +19 and none of that is left either, and the fifth was +190, spent to the line on the day it was taken. 154 - 59 + 98 + 128 + 19 + 190 = 530, which is the standing figure in the table's third column. The two floors are the sums of `SKILL_BASELINE` and `TEST_LINE_BASELINE` in `hooks/lib/__tests__/surface-growth-bound.test.ts`, so a restore target moves only when a baseline moves. What the two surfaces measure *today* is in `hooks/lib/__tests__/fixtures/surface-growth.golden`, blocks `[skills bytes]` and `[hook-tests lines]`, and is not copied here: at the second `skills/` raise on 2026-09-11 they stood at 224 308 bytes and 21 745 lines, the first exactly at budget again and the second 78 lines inside its own, so the next byte of a skill body turns the suite red.

**Nothing else was raised.** The always-on rule core and `agents/*.md` each absorbed their spend inside head-room that already existed, and so did the per-dispatch-path bound in `hooks/lib/__tests__/rules-emission-golden.test.ts`, the failing bound this section's table does not list and the one that stands at zero head-room by construction. So the reading has these two figures to restore and no others.

**Why a raise at all**, when the standing answer to a red bound is a cut. The user ruled it on 2026-09-10: `260910-2256_*_may-the-growth-bounds-be-raised-for-the-duration-of-the-container-restoration.md`, option 2, the budgets raised as far as the work needs and a reduction attempted afterwards. The work is the eleven-step restoration planned in `260910-2145_*_restore-the-per-work-item-container.md`. A cut was looked for first and was not available in full: what the hook suite holds is reasoned prose with a claim per paragraph, and funding one step's cases by deleting another step's reasoning is the trade the 2026-08-05 decision already refused. On `skills/` a cut *was* taken and is not the whole of it. Step S6's first draft measured 223 277 bytes and was tightened to 223 000 out of its own new prose, and 263 bytes went back on when the two rewritten walks were run against the live store and refuted a claim both prompts had made, that a legacy marked record carries no `**Status:**` at all.

**What each raise bought.** The 866 bytes went to step S6: the two depth-1 walks over the work-item store becoming two-level walks that reach a container's record in either of the two forms the store now holds, the whole-container archive move, and the repair of a paragraph that told the archive body every `SCAN_*` names exactly one directory, which the restored resolution contract had made false. The 154 lines went in two parts, 118 for step S9's citation corpus and the grammar learning the second container-record form, and 36 for writing the `skills/` raise down, which costs hook-test lines on a surface that had none left. Each of the three moves names its own before-and-after figure in the commit that made it.

**The second `skills/` raise is ruled by a different ruling, and it says which.** `260910-2256_*_may-the-growth-bounds-be-raised-for-the-duration-of-the-container-restoration.md` scoped its raises to the container restoration, and that work is finished, so it does not reach this one. The user ruled the second raise in the session of 2026-09-11, shown the measurement and the two alternatives it stood against: a cut of 1 045 bytes elsewhere in `skills/`, or reverting the repair that had spent them. No record was filed for that ruling, so this entry is the whole of its account.

**What the second raise bought**: the migration-detection repair filed as `260911-1126_*_neither-setup-nor-migrate-detects-a-live-circle-record-so-the-v11-conversion-never-runs.md`, which blocked the release because neither `/fusion:setup` nor `/fusion:migrate` recognised a live Circle record, so the v11 conversion never ran in a workbench that still held one. It cost +820 bytes in the body of `/fusion:setup` and +225 in the body of `/fusion:migrate`, which is the 1 045 exactly and no more: the raise was spent to the byte, and the surface stands at zero margin after it, so the next raise has to ask again and write itself down. It joins the 2026-10-10 reading above rather than opening a second date: one reduction date for this surface, with the surplus summed.

**The third `skills/` raise, 2026-09-16: `SKILL_HEAD_ROOM` 21 911 -> 24 911, +3 000 bytes.** No baseline moved with it. The user ruled it in the session of 2026-09-16, shown the measurement and the two alternatives it stood against: cutting live substance out of a skill body, or abandoning the merge. No record was filed for that ruling, so this entry is the whole of its account.

**What it bought**, and why the pressure existed. The work is the merge of the retired `log-activity` body into `/fusion:cadence` — written as a bare name because the command no longer resolves and the phantom-skill gate reads every `/fusion:` token — planned in `260915-2309_*_merge-log-activity-into-cadence.md` under the ruling in `260915-2309_*_does-the-activity-log-keep-its-own-command-or-become-the-first-half-of-cadence.md`: one command that writes the activity log and then digests it. The merged body measures 21 901 bytes where the two it replaces measured 19 727 and 13 273, so the merge is a cut of 11 099 on the surface — and it was still 2 211 over the bound, because the deletion drops the deleted file's baseline entry as well as its bytes and `log-activity/SKILL.md` sat 356 below its own baseline. A cut was looked for first and was taken: a dead-text pass over the merged body banked 890 bytes and established that the remaining 2 211 do not exist as an honest cut. The pressure is older than this merge and is what the raise is really answering — four skill bodies (`check`, `news`, `reconcile`, `post`) carry no baseline entry at all, **53 436 bytes running in full as of `c066bfd3`** against head-room that was derived before any of them existed. That figure was written here as 52 842, which is exact at `9d5b1e80` and 594 stale by 2026-09-17 (`check` +611, `news` -17); it is corrected rather than dated because the sentence states it in the present tense, which is the error `260916-0734_*_the-head-room-raise-log-states-a-surface-total-and-two-margins-that-no-committed-tree-holds.md` was filed for. The four are five as of the entry below.

**What is left of it.** At `9d5b1e80`, the commit that landed the raise, the surface measured 212 597 bytes against a budget of 213 679 (floor 188 768 + 24 911), so 1 082 bytes of margin — the first raise on this surface not spent to the byte. Every step of that plan landed in that one commit, so 212 597 is the whole of it and not a figure taken mid-landing. What the surface measures *today* is in `hooks/lib/__tests__/fixtures/surface-growth.golden`, block `[skills bytes]`, and is deliberately not copied here — an earlier draft of this paragraph stated a mid-landing total in the present tense and was 293 bytes out on every figure derived from it (`260916-0734_*_the-head-room-raise-log-states-a-surface-total-and-two-margins-that-no-committed-tree-holds.md`).

**The second `TEST_LINE_HEAD_ROOM` raise, 2026-09-16: 2 595 -> 2 693, +98 lines.** No baseline moved with it. The user ruled it in the session of 2026-09-16, shown the shortfall measured against a test that had already been written and run green, and the two alternatives it stood against: hunting a 98-line cut elsewhere in the suite first, or landing `bin/fusion-claude-md-weight` with no test at all. No record was filed for that ruling, so this entry is the whole of its account.

**What it bought**: `hooks/lib/__tests__/claude-md-weight.test.ts`, the only test that reads `bin/fusion-claude-md-weight`, pinning the contract clauses that helper's own header states — a stated zero where the root has no `CLAUDE.md`, exit 0 on every path including misuse, one line and nothing else for a clean file, `/fusion:curate` named in the report's last line, and no verdict about topic. The middle one and the last are **inverted** assertions, which is why the file opens with a header saying so: a later reader who "fixes" either into a non-zero exit or a topic judgement has reversed `260916-1126_*_may-the-drift-check-report-weight-when-it-cannot-decide-topic.md` rather than tightened a test. The work is step C4c of `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md`, whose own step text sequenced this gate and told the executor to stop at it rather than reach for the constant.

**A cut was looked for first and was not available.** The file is 131 lines against 33 of margin. The shortfall was measured rather than estimated — the test was written in full and run green before anything was asked for — and it does not close by compression: stripped of every comment and every blank line the file is still 81 lines — 131 less 23 blank, less 24 opening `//`, less the 3 single-line doc comments a count by `//` and blanks alone misses — so deleting all of its reasoning, which is the trade the 2026-08-05 decision already refuses and which would take out the very header that stops the inverted assertions being undone, still leaves it 48 over. The plan had projected nine lines and that projection was stale when it was read: the redundant skill-roster gate retired in `da1c62ed` freed 24 lines rather than the 31 estimated, and 33 was the margin that left.

**What is left of it: nothing.** The raise was spent to the line, as the 2026-09-11 one was. At the landing — the new test file and a line-neutral edit to the constant, which is the whole of it and not a figure read mid-way — the surface measured 21 921 lines against a budget of 21 921 (floor 19 228 + 2 693), so zero margin, and the next line added anywhere in the suite turns it red. What the surface measures today is in `hooks/lib/__tests__/fixtures/surface-growth.golden`, block `[hook-tests lines]`, and is deliberately not copied here, for the reason the paragraph above this one records.

**The third `TEST_LINE_HEAD_ROOM` raise, 2026-09-16: 2 693 -> 2 821, +128 lines.** No baseline moved with it, and none of the others did either: not one `TEST_LINE_BASELINE` entry, not `AGENT_BASELINE` or `SKILL_BASELINE` beside it, not `RULE_BASELINE` in the rules-emission golden test, and not `hooks/lib/__tests__/fixtures/dispatch-path.baseline`. **It is the third raise of this constant, all three inside one day, and the second on this date.** It takes the standing raise above the derived 2 500 from +193 to +321, which is what the table above now reads; a log that records each raise as if it were the first is how a bound stops meaning anything, so the running total is written here rather than left to be summed. The day is also why every figure below is stamped: the margin on this surface read 9 lines at `c9d4013d`, 33 at `da1c62ed`, 0 at `d3ff530c` and 0 still at `611658de`, all four re-measured off `git` rather than quoted. The user ruled the raise in the session of 2026-09-16, shown the measurement below and the two cheaper shapes it stood against: funding only the two cheap gates at +51 and leaving the dear one unwritten, or cutting the dear one to 57 lines for +108. He funded all three. No record was filed for that ruling, so this entry is the whole of its account.

**What it bought**: three gates, one per record, written in full and run green on the working tree at `611658de` before anything was asked for, and each case proved by mutating what it asserts against rather than by being read.

- **+77 lines in `hooks/lib/__tests__/claude-md-weight.test.ts`**, for `260916-1314_*_the-weight-helpers-test-pins-the-banner-and-the-exit-code-while-the-measurement-goes-unasserted.md`. The nine cases that file already held assert exit statuses, banner regexes and line counts, and not one of them reads a figure `bin/fusion-claude-md-weight` computed; the record ran six mutations of the helper — reversed sort, under-threshold rows dropped, `heading-level=` deleted, the trailing-newline correction broken, among them — and all nine held for every one. The four new cases read the figures: every section is a row, largest first, marked against the threshold; the head figures sum to the file at the level the rows were cut at; a file with no trailing newline is one byte lighter in the row that ends it; and a `--threshold` that is not a number is a misuse under which nothing is weighed, which the existing exit-code case could not carry because every path of that helper exits 0 and the assertion held under its own inversion.
- **+23 lines in `hooks/lib/__tests__/derivable-enumerations-lint.test.ts`**, for `260916-1315_*_the-retired-gates-redundancy-argument-covers-one-direction-and-claude-md-still-claims-the-other.md`. `da1c62ed` retired a two-direction gate over `CLAUDE.md` as redundant. The open-set half genuinely was, carried by "no shipped doc cites a phantom skill"; the closed half — every skill directory is named — was not, the surviving set-equality check being over a different file. The sentence claiming a gate holds that direction then moved to `README-agents.md` in `72911b86` with no gate following it, so what is restored is anchored to **the sentence that makes the claim** rather than to the file that carried it, and the next move of the passage cannot drop it silently again.
- **+28 lines in the same file**, for `260916-1323_*_the-two-selector-enumerations-are-not-held-by-a-gate-and-the-hook-test-surface-cannot-pay-for-one.md` — a record that exists only because this bound refused the gate when the surface stood at 0 lines, and whose own text names that refusal as the reason the cheaper fix landed instead. The gate holds `SEL` in `skills/setup/SKILL.md` equal as a set to the selector table in `skills/check/SKILL.md`. The two are one roster stated twice; they drifted in a single commit (`c9d4013d`) with a green suite, leaving the eleventh check invisible to Setup until a reviewer read both files.

**A cut was looked for first, and compression was measured rather than assumed.** Stripped of every comment and every blank line the addition is still 63 lines — `claude-md-weight.test.ts` goes 81 -> 115 under that stripping and `derivable-enumerations-lint.test.ts` 284 -> 313 — so deleting all of the reasoning, including the headers recording which mutation proved which case, leaves the surface 63 over and buys nothing but an unreadable suite. That is the trade the 2026-08-05 decision already refuses, and here it does not even close.

**What is left of it: nothing.** The raise was spent to the line, as both raises before it were. At this landing the surface measures 22 049 lines against a budget of 22 049 (floor 19 228 + 2 821), so zero margin again, and the next line added anywhere in the suite turns it red. 22 049 is the whole landing and not a figure read mid-way: nothing else in it adds a line to `hooks/lib/__tests__/**.ts`, the constant's edit being one line for one line, this entry sitting in a README that no surface bounds, the regenerated golden not being TypeScript, and a pin re-approval appending to a line that already exists. What the surface measures *today* is in that golden, block `[hook-tests lines]`, and is deliberately not copied here, for the reason the second raise's own entry records.

**The fourth `TEST_LINE_HEAD_ROOM` raise, 2026-09-16: 2 821 -> 2 840, +19 lines.** No baseline moved with it, and no other head-room did. **It is the fourth raise of this constant, all four inside one day for this surface, and the third on this date.** It takes the standing raise above the derived 2 500 from +321 to +340, which is what the table above now reads. The user ruled it in the session of 2026-09-16, shown the measurement below and the two alternatives it stood against: landing the fix with its new interface unasserted, or hunting a 19-line cut elsewhere in the suite first. No record was filed for that ruling, so this entry is the whole of its account.

**What it bought**: one case in `hooks/lib/__tests__/fusion-checkout-name.test.ts`, the acceptance test written out by `260916-2105_*_the-news-skill-reads-an-entry-from-a-fetched-ref-and-names-its-writer-from-the-working-tree.md`. `/fusion:news` read a message body out of a fetched ref and then named its writer out of the working tree, so a checkout that registered in a commit the reader had fetched and not pulled was rendered as one that never registered — false at the moment it was printed. The fix is `resolve --at <commit>` in `bin/fusion-checkout-name`, an unbounded surface, and it cost `skills/news/SKILL.md` **-17 bytes**, so the test is the only part of the work that asked for anything. The case registers, commits, deletes the entry from the working tree, and asserts that the working tree answers exit 3 while the commit answers with the alias; it then pins the split the fix turns on, that exit 3 is an answer *about* the registry at that commit and the new exit 6 is the absence of one, so a failure to look can never again be rendered as "never registered".

**A cut was looked for first and was not available.** The test was written in full and run green before anything was asked for, and it was tightened before it was measured: a `git()` helper was factored out of the fixture and the two config calls that repeated it, and three wrapped statements were closed up, which took the addition from 22 lines to 19. What remains is four `git` calls, three assertions and the two comments naming which false rendering each one refuses. The files furthest above their baseline on this surface — `citation-form.test.ts` +352, `dispatch-bytes.test.ts` +223, `fusion-forum.test.ts` +220 — are other subjects' tests, and thinning one of them to fund this is the trade the 2026-08-05 decision refuses.

**What is left of it: nothing.** The raise was spent to the line, as all three before it were. At this landing the surface measures 22 068 lines against a budget of 22 068 (floor 19 228 + 2 840), so zero margin again. 22 068 is the whole landing and not a figure read mid-way: the constant's edit is one line for one line, this entry sits in a README that no surface bounds, the regenerated golden is not TypeScript, and the pin re-approval in `reference-resolution-lint.test.ts` changes two digits on a line that already exists.

**The fourth `SKILL_HEAD_ROOM` raise, 2026-09-17: 24 911 -> 39 260, +14 349 bytes.** No baseline moved with it, and no other head-room did: not one `SKILL_BASELINE` entry, not `AGENT_BASELINE` or `TEST_LINE_BASELINE` beside it, not `RULE_BASELINE` in `hooks/lib/__tests__/rules-emission-golden.test.ts`, and not `hooks/lib/__tests__/fixtures/dispatch-path.baseline`. **It is the fourth raise of this constant and the eighth overall.** It takes the standing raise above the derived 20 000 from +4 911 to +19 260, which is what the table above now reads; the restore-target column does not move with it, that column being the untouched floor plus the derived figure. The user ruled it at 15:16 on 2026-09-17, shown the measurement below and the stop condition his own plan carries — that a body which will not fit inside the raise goes back to him rather than being absorbed by a second raise nobody ruled. No record was filed for that ruling, so this entry is the whole of its account.

**What it bought**: `skills/discuss/SKILL.md`, the body of `/fusion:discuss`, at 14 350 bytes. It runs a bounded discussion between the agent the user is talking to and `consultant`, one round at a time in the chat, against a claim register it writes from the first round onward and closes with a recommendation it does not act on. The raise is one byte smaller than the body because the surface stood at 213 678 against a budget of 213 679 before it — the whole of the margin the third raise left, spent here.

**A cut was looked for first and was not available, and the impossibility is measured rather than asserted.** The search is `260917-1508-the-cut-search-on-both-bounded-surfaces.md`, run over all fourteen bodies as a separate dispatch so it could not be folded into the step that feels the pressure. **Dead subject matter across the surface: 0 bytes** — every path, helper, agent, rule and store named in the six bodies most likely to carry residue resolves against the tree, and the eleven rows of `check`'s selector table each have a live backing. **Cross-body duplication: at most 1 299 bytes**, of which 877 is executable shell a body cannot run from inside another body and 282 is one-line citations of `rules/fusion-workbench-conventions.md`, so the defensible residue is near zero — against 14 349, short by at least 13 050. And the addition does not compress into the gap either: stripped of every blank line it is 14 273 bytes, and stripped to its frontmatter, its nineteen headings and its fenced shell — every sentence of reasoning deleted, including the entry-block shape, the stopping rule and the note recording which assertion is inverted — it is still 2 557, which leaves the surface 2 556 over and buys nothing but an unreadable body. That is the trade the 2026-08-05 decision already refuses, and here it does not even close. Two full searches over this surface have already been run and **taken**: the ten-row ledger of 2026-09-08 (`260908-1346-the-cut-ledger-for-the-skills-surface-and-what-the-post-body-owes.md`, -9 564 bytes) and the dead-text pass entered above (+890). The nine bodies carrying a baseline entry measure 160 242 today against a floor of 188 768, so **they already stand 28 526 bytes below their own floor**; `setup` has given back 27 812 and `cleanup` 16 906.

**What is left of it: nothing.** The raise was spent to the byte, as the 2026-09-11 raises were. At this landing the surface measures 228 028 bytes against a budget of 228 028 (floor 188 768 + 39 260), so zero margin, and the next byte of any skill body turns the suite red. 228 028 is the whole landing and not a figure read mid-way: this entry sits in a README that no surface bounds, the constant's edit is one line for one line in a file on the other surface, the cut entered below is in a test file, and the regenerated golden is not a skill body.

**The standing cause is older than this raise and is not this work's to fix.** Five bodies now carry no `SKILL_BASELINE` entry at all — `check`, `discuss`, `news`, `post` and `reconcile`, 67 786 bytes running in full — against head-room derived before any of them existed. That is a floor problem presenting as a head-room problem, and a fifth raise will not be the last one while it stands. Moving a baseline is governed by the three events in `hooks/lib/__tests__/helpers/growth-bound.ts` `## Re-baselining` and none of them fires here, so it needs its own ruling; it is carried as an open question in the search above and belongs with `260909-1634_*_how-should-the-skill-surface-be-cut-once-the-agent-and-ceremony-cut-has-landed.md`.

**A cut taken in place of a raise, 2026-09-17: `TEST_LINE_HEAD_ROOM` unmoved at 2 840, +0 lines.** It is entered here beside the eight raises because a search that found something is as much a part of this account as one that did not, and because the next reader of this log should be able to see that the cut branch is real rather than rhetorical. No baseline moved and no head-room moved, so the standing raise above the derived 2 500 stays at +340 and the table above reads exactly as it did. Nobody ruled it, because nothing was asked for: the plan's step B12 takes the cut branch whenever the search returns one, and the user's 15:16 ruling covers the `skills/` raise above and this surface not at all.

**What was over.** Step B10 added one case to `hooks/lib/__tests__/fusion-paths.test.ts` — the three properties of the new `OUT_DISCUSSION` key, its third assertion inverted and commented as such — and the surface went 23 lines over, which is what a surface the fourth raise left at exactly zero margin does the next time anything is added to it.

**What the search found: 45 lines, net.** Four negative pins inside the same file assert that no consumer receives `CIRCLE`, `OUT_CIRCLE`, `SCAN_CIRCLES`, `PORTFOLIO`, `OUT_INVESTIGATION`, `SCAN_INVESTIGATIONS`, `SCAN_CONSULT` or `SCAN_MEMOS`. All four are subsumed twice over: `bin/fusion-paths` derives a consumer's key set by grepping its prompt, so the six prefixed keys exit 4 against the `ORDER` check and fail the parameterised set-equality case before it compares anything, while `CIRCLE` and `PORTFOLIO` cannot enter the key set at all, carrying no `OUT_`/`SCAN_` prefix for the derivation regex to admit. The investigation pin's own stated rationale — that it catches "re-adding an arm without a prompt to name it" — is false for the same reason, and the `SCAN_CONSULT` pin concedes the redundancy in its own comment. 46 lines out, one line back in, so `fusion-paths.test.ts` goes 786 -> 741. **One pin in the same block was kept by hand and the reason is worth keeping:** `it("emits no history key to any agent")` is not redundant, because `OUT_HISTORY` and `SCAN_HISTORY` both have resolver arms and `ORDER` entries, so a prompt naming either resolves silently and the set-equality case passes.

**One line of the four pins was not a negative pin, and taking the cut as first written would have lost a real guard.** `expect(parse(run(project, "consultant").stdout).OUT_CONSULT).toBe("shared/consult")` sat inside the `SCAN_CONSULT` block and is the only assertion anywhere in the suite on the value `OUT_CONSULT` resolves to — a value that matters more than most, `bin/fusion-paths` returning a bare `shared/consult` with no scope prefix, so it is the one output key that does not follow the work-item container rule. This was proved rather than argued: with the four pins removed as first written, mutating that value to `shared/consultation` left all 69 cases of the file passing. The assertion moved into `it("routes writers to their own output kind")`, at a cost of the one line above, where the same mutation fails.

**What is left of it: 22 lines, and it is the first margin above zero on this surface in four raises.** At this landing the surface measures 22 046 lines against a budget of 22 068 (floor 19 228 + 2 840). The floor does not move: `fusion-paths.test.ts` keeps its baseline entry and the file still exists, so every line added under the four raises still counts as growth above an unmoved floor, and the cut banks no savings as head-room — which is the cut-only refusal above, applied to a cut too small to have asked for anything.

**The ruling's residual stands and is not argued away here:** the instrument stops measuring during exactly the work it exists to measure, and this project has one instance on record of that shape lasting thirteen days. What bounds it is that every raise names itself with the figure before and after, and that the reduction is read on **2026-10-10**, the date step C6's deferred measurement already carries, so one reading answers both.

**One reduction has already been taken, and it is what put this section here.** The raise log stood in the header comment of `hooks/lib/__tests__/surface-growth-bound.test.ts` until 2026-09-11, where it cost the hook-test surface the lines it was written in and had already drifted on its own headline figure, stating the `skills/` overage as 866 twice and as 603 once. Moving it into this README costs no bounded surface anything, because the READMEs are on no bound, and it leaves one account of the figures instead of two. `TEST_LINE_HEAD_ROOM` came back down with it, 2 654 to 2 595, spent to the line as the raise was: the reduction gives back budget, not working room, and the surface is at zero margin either way. That is a reduction and not a raise, and the table above already reads net of it.

#### The dispatch-path re-arming of 2026-09-22

**All eleven dispatch-path rows were re-armed at the measurement, under an event scoped to that bound and to no other.** The event reads: *after a cut of a component shared by every row, a zero-sum bound follows the measurement down.* It is authored in the header of `hooks/lib/__tests__/fixtures/dispatch-path.baseline`, where this bound is authored; this entry is its log and not a second definition. The user ruled it on 2026-09-22 at 1220, option 1 of `260922-0922_*_are-the-dispatch-path-rows-re-armed-at-the-post-cut-totals-and-under-which-event.md`, which also required that the eleven before-and-after figures be written here and that the dispatch-path case be green at the commit that lands them.

**Why it was owed.** The rows were armed on 2026-09-09 charging `CLAUDE.md` to every path at 93 432 bytes. The 2026-09-16 cut of that file took it to 8 021, so every row carried the difference as slack on a bound whose own header says head-room is zero: 43 536 bytes on the tightest of them and 160 066 on the widest. A zero-sum bound standing that far above the measurement refuses nothing, which is not the instrument its header describes.

**It does not reach the three rate surfaces in the table above**, and `hooks/lib/__tests__/helpers/growth-bound.ts` `## Re-baselining: the three events at which a baseline moves` stands unedited, heading and body. `260822-1154_*_does-a-cut-only-circle-re-baseline-the-surfaces-it-cuts.md` rules the cut-only refusal for those surfaces and is not superseded. What admits the event here is this bound's own definition, zero-sum, which no rate surface has. No head-room constant moved: `DISPATCH_HEAD_ROOM` is still `0`.

| Path | prompt | rules emitted | `CLAUDE.md` | total |
|---|---|---|---|---|
| analyst | 21 038 → 21 032 | 84 319 → 94 942 | 93 432 → 8 021 | 198 789 → 123 995 |
| coder | 9 649 → 12 165 | 85 175 → 86 650 | 93 432 → 8 021 | 188 256 → 106 836 |
| consultant | 14 329 → 13 432 | 89 918 → 100 593 | 93 432 → 8 021 | 197 679 → 122 046 |
| curator | 34 554 → 78 507 | 99 080 → 100 593 | 93 432 → 8 021 | 227 066 → 187 121 |
| editor | 13 635 → 13 156 | 95 672 → 106 347 | 93 432 → 8 021 | 202 739 → 127 524 |
| ontocoder | 13 262 → 15 875 | 85 175 → 86 650 | 93 432 → 8 021 | 191 869 → 110 546 |
| orchestrator | 155 302 → 97 760 | 131 331 → 116 808 | 93 432 → 8 021 | 380 065 → 222 589 |
| planner | 19 725 → 19 659 | 89 271 → 99 913 | 93 432 → 8 021 | 202 428 → 127 593 |
| reconciler | 22 825 → 21 171 | 90 127 → 91 621 | 93 432 → 8 021 | 206 384 → 120 813 |
| reviewer | 12 837 → 12 837 | 82 743 → 93 451 | 93 432 → 8 021 | 189 012 → 114 309 |
| shaper | 29 057 → 17 017 | 128 279 → 110 834 | 93 432 → 8 021 | 250 768 → 135 872 |

**The rows did not all move one way, and the log says so rather than reporting a reduction and leaving the rises unnamed.** Every total falls, because the shared cut is larger than any component's rise. Inside the rows, three prompt components stand **above** their arming figures and are re-armed upward with the rest — curator 34 554 → 78 507, ontocoder 13 262 → 15 875, coder 9 649 → 12 165 — and every rule component rose except the orchestrator's (−14 523) and the shaper's (−17 445), the largest rise being editor's at +10 675. The orchestrator prompt carries the one large fall, 155 302 → 97 760, banked by the v11 roster cut and never followed down until now.

**Every row now stands at zero slack**, which is the point of the event and the cost of it: the next byte added to `CLAUDE.md`, to an always-on rule file, or to any agent prompt must be paid for inside the same path. The next always-on addition in this repository therefore needs a cut of its own rather than finding 43 kB waiting. That is the instrument working as ruled, and no defect is filed against it.

### Rebuilding after TS changes

```bash
cd hooks && npm install && npm run build
```

The compiled `hooks/dist/` directory is committed to the repo and ships with the plugin, so it has to match the sources exactly — a deleted source whose compiled output stays behind is tracked in git, copied into `~/.fusion` by the installer, and readable there as a module the plugin no longer has. That happened once already, with the four files of the retired Bash classifier.

**That obligation is now a blocking gate.** `hooks/lib/__tests__/committed-dist.test.ts` extracts the committed source at `HEAD`, compiles it with the pinned compiler, and fails the suite when the result differs from the committed `hooks/dist/` — printing the differing files and the `npm run build` above as the remedy. See the gate table under `### Three gates that can fail the suite over text nobody compiled`.

`build` keeps that promise without deleting anything: it compiles to a staging directory, replaces each changed file in `dist/` atomically, and removes any output whose `.ts` source is no longer in the tree. The result is byte-identical to a wipe-and-rebuild, and `npm run build:clean` is still there (`rm -rf dist && tsc`) for the rare case where you want the wipe itself. `npm test` runs `build`, so a normal test run keeps `hooks/dist/` current exactly as it used to.

One consequence worth knowing before adding a file: nothing under `hooks/` may be a TypeScript source the build deliberately skips. The prune keeps a `dist/` entry whose `.ts` source still exists, and it cannot tell such a file's stale output from a concurrent run's fresh one. That is why the vitest configuration is `vitest.config.mjs`.

## Where to look when something breaks

| Symptom | Likely cause |
|---|---|
| `Agent type 'X' not found` / `denied by permission rule 'Agent(...)' from settings` | Agent dispatch with bare name, or `tools:` allowlist missing namespace prefix. Every dispatch must use `<plugin>:<name>` form. |
| Agent reports it can't find a rule file | Rule discovery not run, or the expected file is project-specific — the plugin ships only its own `rules/` directory (the always-on set plus the conditional emissions in `bin/fusion-rules`); domain rules must live in `./rules/` or `.claude/rules/` of the consuming project. Problem 10. |
| Agent halts at Setup with "no fusion workbench found above pwd" | The project never ran `/fusion:setup`, or setup ran in a different directory. Run `/fusion:setup` at the project root once. The setup skill writes `fusion-workbench/.fusion-setup` which all subsequent agents and hooks look for. |
| Stray empty `fusion-workbench/.guard-state/` directories appearing in random folders | Pre-v2.5 behavior. Hooks bootstrapped the workbench from any `process.cwd()` they fired in. Fixed in v2.5: hooks no-op when no `.fusion-setup` marker is found upward. Safe to delete the stray directories. |
| All sub-agents fail to load after a fusion upgrade | Likely a frontmatter-syntax breakage. v2.8.1 added `disallowedTools: [Agent]` to 12 agents based on docs that turned out incorrect for our parser; agent loading broke entirely. Rolled back in v2.8.3. **Lesson:** changes to agent frontmatter beyond `name`/`description`/`tools` need empirical verification (run a sub-agent dispatch end-to-end) before release, not just docs-driven inference. |
| Orchestrator skipped Setup, dashboard never refreshed | "MUST run Setup" in agent prompt was overridden by user task urgency. The fix is `/fusion:setup`. Problem 11. |
| `/plugin install` keeps returning an old version | Marketplace cache clone at `~/.claude/plugins/marketplaces/` is stale; manual git pull required. Problem 12. |
| SessionStart banner not visible | Hook used plain stdout instead of `systemMessage` JSON. Problem 13. |
| SessionStart prints "fusion: restart this session at the project root" | Working as designed: a `fusion-workbench/.fusion-setup` marker was found in an ancestor of the working directory rather than at it. One fusion check still resolves against cwd — the plugin-repo detection in `bin/fusion-plugin-cwd`, which the three work-tree-preferring `bin/` helpers ask — so from a subdirectory it inspects the wrong directory and all three read the installed plugin copy instead of the work tree. Restart at the named root. The warning itself changes no behaviour. Two sharper instances stood here and are gone: the protected-path deny on 260812, and on 260816 the write-tool PreToolUse path computation together with the module that performed it. The issue that tracked the first, `260804-2100_*_from-a-subdirectory-cwd-the-protected-list-matches-nothing-while-fail-closed-still-denies.md`, is moot rather than fixed. |
| Setup prints no work-item hint although the backlog holds items | The backlog store is absent (a workbench set up before it existed), or `SCAN_PACKAGES` was not substituted into Setup's read, which reports itself as a fusion bug rather than as a zero |
| Orchestrator falls back to `Agent(general-purpose)` (denied) when trying to ask the user — at interrupted-session prompt, Coherence gate, Rebalance Grounding sub-flow, etc. | `AskUserQuestion` missing from `agents/orchestrator.md` `tools:` allowlist. Fixed in v3.0.1. The skill body (`/fusion:setup`) had `AskUserQuestion` in its `allowed-tools` so the skill path worked, but the orchestrator agent itself was denied — and sub-agents (shaper, planner) inheriting from the orchestrator would also have been denied if reached. **Cannot recur at HEAD:** the `tools:` line went on 260913 and no agent declares one, so every agent inherits the session's tools and nothing has to be listed anywhere (`260913-0909_*_may-the-orchestrator-reach-every-tool-and-may-an-agent-dispatch-another.md`). |
| Monitor times displayed off by user's TZ offset | `orchestrator-events.jsonl` timestamps are UTC but written by `date -u +%Y-%m-%dT%H:%M:%S` without the `Z` designator. JavaScript `new Date(ts)` parses such strings as local time per ECMA-262, so any rendering via `getHours()/getMinutes()` is wrong by the user's TZ offset. **Fix at consumer:** the `parseUTCTs()` helper in `bin/monitor` (or append `Z` before parsing) — `bin/monitor` now routes the ETA (v3.9.0, `6209645`) and the event-list/warning times through it. **Lesson:** any future tool consuming `orchestrator-events.jsonl` for time-based computation (JS, Python, Go) must defensively parse — the emit convention is intentional (clean strings in the file) but transfers the parsing burden to every consumer. |
| A protected path was changed and the guard wrote it back, halting the write tools | Cannot happen at HEAD. The whole protected-path half — the write-tool deny, the before/after fingerprint, the write-back, the `FUSION_ALLOW_RULES_WRITE` exemption and the `guard.protectedPaths` configuration leaf — was removed on 260812. If you meet this, you are running an installed copy older than that; `fusion --update` and restart, since the hooks always run from the installed copy and are pinned for the whole session. |
| A consuming project is halted and nothing at HEAD could have raised it | Expected, and it is the migration case. A halt raised by a removed mechanism is a value in that project's `escalation.json`, and it outlives the code that wrote it. At this version it blocks nothing: the halt check, the consecutive-block counter, the escalation module and the halt-clearing script all went on 260816, and no shipped code reads or rewrites the file. `/fusion:setup` offers to delete it. All three properties — nothing blocks, nothing rewrites the file, the offer exists — are pinned by `hooks/lib/__tests__/legacy-halt-clearing.test.ts` against both triggers no surviving code produces (`protected_path`, `protected_path_measured`). |
| A consuming project sees an advisory on every tool call naming `fusion-guard.json`, or one naming `guard`, `decisions`, `escalation`, `churn`, `orchestrator.maxTurns` or `orchestrator.dispatchMinutes` | Working as designed, and the two are the v10 migration. The first says that project's root still carries `fusion-guard.json`, which fusion no longer reads a byte of: **copy nothing across** — every setting that file could carry is retired too — and delete it. That instruction was the reverse until 260910, when the Turn budget it told you to rescue was itself retired. The second says the new file still carries a retired key: one of the four top-level ones (`RETIRED_TOP_LEVEL_KEYS` in `hooks/lib/config.ts`) or one of the two retired leaves inside `orchestrator` (`RETIRED_LEAF_KEYS`); delete the key. Nothing else in either file is affected while the advisory stands. `docs/upgrading-to-v10.md` states both at length. |
| `npm test` goes red on a citation, a plan or a compiled file I did not edit | Working as designed, and it is one of the three blocking gates documented in `README-hooks.md` `### Three gates that can fail the suite over text nobody compiled`. `hooks/lib/__tests__/workbench-citation-lint.test.ts` recomputes its corpus from the tree on every run and carries no approvable baseline, so an archive sweep or a newly filed record with a bad citation reddens the suite for somebody who touched nothing — correct the path, or restate the token as a statement rather than a pointer; that cost was accepted rather than mitigated by `260819-1645_*_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md`. `hooks/lib/__tests__/plan-stopping-section-lint.test.ts` fails on a live (`_o_`/`_p_`) plan whose `## Where this work stops` is missing, empty or still the shipped placeholder; it judges presence only. `hooks/lib/__tests__/committed-dist.test.ts` fails when the committed `hooks/dist/` is not the compilation of the committed source — run `npm run build` — or, separately, when the installed TypeScript is not the exact version `hooks/package.json` pins, which the failure names as a toolchain mismatch and not an artifact defect. |
| An agent writes a plan / issue / decision to the workbench root or into the wrong Circle (v4.0.0+) | The prompt named a store directory as a path literal instead of resolving it through `bin/fusion-paths`. Store paths are NOT written in prompts — the agent resolves `$OUT_*` / `$SCAN_*` at Setup step 2 (alongside `bin/fusion-rules`) and the resolver names the item's container or `shared/`. Two guards make a hard-coded literal impossible to ship: the resolver exits 4 (not a silent `KEY=`) when a prompt names a key it can't value, and the path-lint test (`hooks/lib/__tests__/path-literal-lint.test.ts`) fails `npm test` if any type-folder literal reappears in an `agents/*.md` or `skills/*/SKILL.md`. The files allowed to name store literals are enumerated as `DEFINITION_SITES` in that same test — `rules/fusion-workbench-conventions.md`, `bin/fusion-paths`, and the rule file that inherited a definition when the conventions file was partitioned (`workbench-path-resolution.md`). They sit outside the gate's file set by construction, so the list grants nothing; it exists so that a fifth site has to be added by hand rather than arriving because the gate never looked. If a write still lands wrong, check whether the consumer actually calls `fusion-paths <name>` under its own name (agents AND skills share one flat namespace). |

## Origin

Ported from Fusion's guard system (`fusion/reactor/pkg/guard/`), and almost nothing of the port is left:
- `decision_guard.go` → `guard.ts` + `lib/config.ts` + `lib/paths.ts`. The verdict went on 2026-08-16, the configuration under it shrank to one non-guard setting, and `lib/paths.ts` kept one case-folding helper out of the glob compiler it ported.
- `escalation.go` → `lib/escalation.ts`, deleted 2026-08-16 with the halt.
- `event_parser.go` → `lib/events.ts`, the one part still doing its original job.

The key difference at the time: Fusion intercepted via the SDK's `canUseTool` callback in a running Go server, the Compliance Guard via Claude Code's native hook system — external scripts invoked per tool call. The difference that matters now is larger. Fusion's guard decided; this one observes.
