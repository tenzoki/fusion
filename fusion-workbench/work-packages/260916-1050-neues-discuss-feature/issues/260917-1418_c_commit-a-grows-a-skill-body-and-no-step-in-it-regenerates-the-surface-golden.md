Commit A grows a skill body and no step in it regenerates the surface golden

---
Step A5 of `260917-1124_*_implementation-fusion-discuss-a-two-agent-discussion-loop.md` adds 19 bytes to
`skills/cadence/SKILL.md`. `hooks/lib/__tests__/fixtures/surface-growth.golden` records every bounded file's
size per file, so that edit turns `surface-growth-bound.test.ts > matches the checked-in golden, surface by
surface` red. Commit A has no step that regenerates that fixture: step A7 regenerates
`hooks/lib/__tests__/fixtures/rules-emission.golden` and nothing else, and the step that regenerates the
surface golden is B13, in the other commit. So the plan's `## Approach` claim that commit A "lands on a green
suite" is false as the steps stand.

The head-room bound itself is not the problem and passes: `holds skills inside its own head-room of 24911
bytes` is green after A5, at 213 678 bytes against a budget of 213 679.

Measured at A5's completion: `cd hooks && npm test` exits 1 with 2 of 56 files failing — the expected
`rules-emission-golden.test.ts`, and the golden-match case of `surface-growth-bound.test.ts` on
`cadence/SKILL.md 22252` against a file now measuring 22271.

This is the same shape as `260917-1252_*_four-steps-of-commit-a-carry-a-green-suite-acceptance-criterion-that-only-step-a7-can-satisfy.md`
and it is not covered by it: that record names one fixture and asserts A7 clears it, and closes with the
instruction that "only a failure *other* than the named golden" is evidence of a defect. Read against the tree
A5 leaves, that instruction routes a correct executor return into the blocked branch.

Acceptance test: commit A carries a step, permitted to touch `hooks/lib/__tests__/fixtures/surface-growth.golden`,
that regenerates it with `cd hooks && UPDATE_SURFACE_GOLDEN=1 npx vitest run lib/__tests__/surface-growth-bound.test.ts`,
reviews the diff and re-runs; and A7's acceptance names both goldens rather than one. No baseline and no
head-room constant moves with it — regenerating the golden records growth and never absolves it.

---
**Filed by:** ontocoder, Kai Stalmann <ks@qantr.com>

**Cross-references:** `260917-1124_*_implementation-fusion-discuss-a-two-agent-discussion-loop.md`, `260917-1252_*_four-steps-of-commit-a-carry-a-green-suite-acceptance-criterion-that-only-step-a7-can-satisfy.md`

---
Resolved: step A7's dispatch was widened to regenerate both stale fixtures rather than the one the plan names. Both were regenerated by their own `UPDATE_*` commands, each diff was read line by line against steps 1 to 6, and the suite returned exit 0 over 944 tests. The plan text still names one fixture; the gap it describes is closed for this work and would return for any later work that reads step A7 as written.
