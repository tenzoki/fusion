The deferred state has no value in the work item's status set
---
`**Status:**` takes `open | claimed | done | dropped`. The Circle vocabulary's `_d_`, deferred, maps to none of them. The migration body proposes `dropped` and marks every such Circle as formerly deferred so the user can say `open` instead before confirming, but after the migration the live distinction between "we decided not to" and "not now" is gone.
---
**Filed by:** orchestrator, Kai Stalmann <ks@qantr.com>
**Cross-references:** 76d833be (the status set and its justification); 07961552 (the migration body); 260909-1843_*_implementation-cut-fusion-to-a-working-minimum.md step D1

**Evidence.** The status set was chosen deliberately and its reasoning is in the conventions: `dropped` collapses the issue vocabulary's `_c_`/`_d_` and the Circle vocabulary's `_c_`/`_b_`/`_s_`/`_d_`, because the body says which happened and the marker only ever abbreviated it. That argument holds for the closed-versus-bounded pair. It is weaker for deferred, because deferred is not a way of finishing: a dropped item is not coming back and a deferred one is waiting.

**Why it matters at D1 and not before.** The migration is the one-way step, and it is where every deferred Circle in this workbench takes a status it did not have. The body's `[was deferred]` marking makes the choice visible at the confirmation, which is the right place — but the choice offered is between two values, neither of which means what the old one did.

**Acceptance.** Before D1 runs, the conventions say either that `open` is the correct target for a deferred Circle and why the distinction is not worth a value, or that the status set gains a fifth. The migration body then names the target rather than proposing one. Whichever, the answer is written down before the one-way step, not after it.

---
Resolved: `**Status:**` gained a fifth value, `paused`, and the distinction the defect named is live again: `dropped` still means no longer live, `paused` means set aside and expected back. The shape was ruled at a user gate and is recorded in `260915-2028_*_what-shape-does-the-work-items-fifth-status-value-take.md` (option 1: the word `paused`, `**Claim:**` cleared, a `**Depends-on:**` entry naming a paused item still live and blocking, the body obliged to say *what* the item waits for and never a date). It was implemented against `260915-2028_*_a-fifth-status-value-for-work-items.md`. What moved: `rules/fusion-workbench-conventions.md` `## Backlog entries — work items` states the five values, the transitions into and out of `paused` and the absent claim; `hooks/lib/work-graph.ts` reads the node set as the live statuses rather than the enumeration `{open, claimed}` and `Readiness` gained a third value so a paused node is never printed `ready` and its dependents are printed `blocked`; `hooks/order.ts` prints the widened readiness column; `skills/archive/SKILL.md`'s dependency scan counts a paused item as a live prerequisite; `skills/migrate/SKILL.md` names the answer in its deferred paragraph. The cardinality moved in the conventions, in `CLAUDE.md` and in `agents/orchestrator.md`.

Corrected 260915: this note first read that `bin/fusion-work-order` printed the widened column and that the cardinality had moved in all three places stating it as a numeral. Neither held. The helper's own header still described the pre-`paused` report — the node set, the readiness column and the `ready=`/`roots=` relation — and the numeral stood in a fourth place, `skills/memo/SKILL.md`, while two further surfaces spelled the set out as four values. Both were found by `260915-2146-reviewer-pre-tag-pass-over-the-fifth-status-value-and-the-active-plan-field.md` and fixed under `260915-2140_*_the-work-order-wrappers-header-still-says-the-node-set-is-open-or-claimed-and-a-closure-note-says-it-moved.md` and `260915-2141_*_three-shipped-surfaces-still-state-the-status-set-as-four-and-the-plan-counted-only-the-numerals.md`. The header now states the live node set as `open`, `claimed` or `paused`; `skills/memo/SKILL.md` states no cardinality at all; `CLAUDE.md` and `README.md` name all five values.

The Acceptance clause above is left exactly as it stands, and this clause annotates it rather than editing it: the urgency it rested on is gone. It required the answer *before D1 runs*, because the migration was then a one-way step that would give every deferred container a status it did not have. The migration no longer does that — it converts live records only and leaves a terminal record alone, so no `_d_` container ever takes a status and nothing was lost by answering afterwards. The question was therefore settled on its own merits rather than against that deadline, and the answer is the one the Acceptance named as the second branch: the status set gained a fifth value. Editing the Acceptance to match would erase the reversal instead of pointing at it.
