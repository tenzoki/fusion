Die Circle-Aktivierung (_a_→_t_) gehört drei Parteien, und keine hat einen vollständigen Ablauf
---
Der Übergang `_a_→_t_` (Record-Umbenennung + `.active-circle`-Schreiben) wird an drei Stellen drei verschiedenen Parteien zugeschrieben, und die Invariante der Konventionsdatei ist bereits gebrochen:

1. `rules/fusion-workbench-conventions.md:75`: „the orchestrator **writes** it once on `_a_→_t_` activation … **Nothing else touches it.**"
2. `skills/next/SKILL.md` Schritt 6.2/6.3: der Skill führt `mv "$CDIR/_a_circle.md" "$CDIR/_t_circle.md"` und `printf '%s\n' "<candidate-dirname>" > "$WORKBENCH/.active-circle"` selbst aus — der einzige ausformulierte Aktivierungspfad läuft NICHT durch den Orchestrator. „Nothing else touches it" ist damit falsch.
3. `agents/orchestrator.md:175,194,779` beansprucht die Aktivierung als eigene Arbeit (u.a. Plane-Call-Point 1 „On `_a_`→`_t_` activation, immediately after writing the pointer"), aber das Phasenmodell (Phase 0→0b→1→2→3→4) enthält keinen Schritt, der sie ausführt — Plane-Call-Point 1 ist im dokumentierten Ablauf unerreichbar.
4. `agents/playmaker.md:158-159` benennt als ausführende Partei „the orchestrator, via the Phase 4 ping after a `_t_→_c_/_b_` rename" — der Phase-4-Ping ist Portfolio-Refresh nach einer SCHLIESSUNG, nicht die Aktivierung.
---
Schweregrad: Medium. Befund des Prompt-Kohärenz-Workstreams (Analyst, von coderev nachgemessen: Zitat conventions:75, mv/printf in skills/next/SKILL.md Schritte 6.2/6.3, Scope-Zeilen im Orchestrator). Folge: ein Orchestrator, der einen `_a_`-Circle aktivieren soll, hat Erlaubnis und Plane-Anweisung, aber keinen Prozedurschritt — er improvisiert oder verweigert. Fix-Richtung: EINE Entscheidung, wem die Aktivierung gehört (Skill oder Orchestrator), dann conventions:75, orchestrator (Phasenmodell + Plane-Call-Point 1), playmaker:158-159 und /fusion:next auf diese eine Antwort ziehen. Hängt zusammen mit dem Schwester-Issue zum unerreichbaren shaper-portfolio-activation-Modus (gleicher Zeitstempel).
---
Resolved: 2026-08-06 — D2 Option (b) realisiert: der geschlossene Writer-Kreis für `.active-circle` ist in den Konventionen enumeriert (Orchestrator-Aktivierung/-Löschung, /fusion:next interaktiv, vier Lifecycle-Skills mit je einer gebundenen Ausnahme; `rules/fusion-workbench-conventions.md:75`); shaper renamed/schreibt nie. Decision `260806-0015_*_wem-gehoert-die-circle-aktivierung.md` (`_i_`). Commit 81d4154 (Plan-Schritt 9).
