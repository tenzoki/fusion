# Planner — implementation plan for `260801-1244-guard-rules-write`

**Date:** 2026-08-02 18:56
**Agent:** planner (dispatched, domain `code`, executors `coder` + `ontocoder`)
**Output:** `260802-1856_*_plan-guard-rules-write.md`

## What was planned

The C5a rules-write exemption on both guarded write surfaces, and the C5b project-level
guard configuration at `fusion-guard.json`. Ten steps, nine `coder` and one `ontocoder`,
with a proposed two-Turn split at the Step 5 / Step 6 boundary.

## The four open questions, as answered

1. **No circular import exists.** `hooks/lib/workbench-root.ts` imports only `node:fs`
   and `node:path`; `escalation.ts:17` and `events.ts:13` already import it. The real
   constraint is that `config.ts` must stop resolving its source path at module load
   (`CONFIG_PATH`, line 34), because `findWorkbenchRoot()` defaults to `process.cwd()`.
2. **Format:** identical schema to `hooks/config.json`, same five optional top-level keys,
   same parser. The merge rule the spec fixes is only meaningful if the keys match.
3. **Cache:** keyed on the resolved source pair, not deleted. The current cache is keyed on
   nothing and ignores its own `configPath` argument after the first call.
4. **Seed in this repository: yes.** The file is not inert here — `escalation.blocksBeforeHalt`
   reaches the git branch-switch deny path, which does not stand down (`guard.ts:388-391`).
   Not seeding it would need a plugin-repo special case inside `/fusion:setup`.

## Verified against HEAD `e8988d9`, not taken from the spec

- `findConfigPath()` at `hooks/lib/config.ts:21-32` — spec citation correct.
- The branch-switch advisory is at `hooks/guard.ts:293-309`, **not** `:159-177`. The cited
  range is docstring prose. The plan quotes the real block and names the four properties
  that constitute "the same shape".
- `MutationOptions.exempt` at `bash-mutation-guard.ts:168-169`, consulted at `:1243` and
  `:1252`, not in the fail-closed pass. Unwired at HEAD.
- `guard_advisory` is **absent** from `WARNING_EVENT_TYPES` in `bin/monitor:80-84`, so the
  dashboard shows no advisory today — including the branch-switch override's. The Circle
  Directive promises it does. Step 5 is one line.

## The fixture question the Circle named as its dominant risk

`guard-bash-integration.test.ts` plus `helpers/guard-harness.ts` were read in full. The
harness already gives: a realpath-asserted throwaway project root, a plugin-root variant
for the stand-down, spawned-subprocess isolation, arbitrary env overrides on every call,
state read-back, and a `dist` entry mode. Three gaps block this Circle: no way to place a
file outside `SEED_FILES`, no way to pre-seed guard state for the halt criterion, and
`runGuard` does not strip `FUSION_ALLOW_RULES_WRITE`. Step 1 closes all three and comes
first.

## Escalated to the user

The self-protection floor cannot be unconditional and still let `/fusion:setup` create the
file it seeds. The plan recommends "floor applies once the file exists" and records the
residual, but flags it as a decision record to file, because it weakens a sentence the spec
states as an invariant. `bin/fusion-paths planner` emits no `OUT_DECISION` key
(`260717-0107`), so the path was not assumed.

## Sizing verdict

Does not fit one Turn loop. Nineteen non-workbench paths against two consecutive overruns
on adjacent code. Split proposed, not imposed.
