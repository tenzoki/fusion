The reconciler's `state Directive` recommendation overlaps the `coherent` row, and the orchestrator's gate never surfaces it
---
Commit `b0fd2f0` (step 11) made the verdict set four values, disjoint and complete, and added a recommendation `state Directive`. The recommendation mapping it wrote is not disjoint. `agents/reconciler.md:175` says "either Directive edge `not evaluable` because no Directive was stated → `state Directive`, whatever the verdict"; `agents/reconciler.md:176` says "`coherent` → `none`". A session with no Directive whose one evaluable edge is OK has verdict `coherent` (`:115`, "every evaluable edge OK") and matches both rows. Two readers then disagree about it. `agents/orchestrator.md:754` (commit `d5c34cd`, step 12) fires the Rebalance gate on any verdict but `coherent`, so under the reading that produces `coherent`, `state Directive` is computed, written to history and never put to the user. Under the other reading the gate fires, and `:754` lists the same four options as before: Revise Artifact / Revise Grounding / Revise Directive / Accept Bounded Closure. `agents/reconciler.md:175` itself says no Rebalance option addresses a Directive that does not exist, so the gate offers four options none of which is the recommendation, and the orchestrator has no instruction for what a user picks in that case.
---
**Filed by:** coderev (person half absent: the installed plugin at `$FUSION_PLUGIN_ROOT` carries no `bin/fusion-identity`, so attribution was dropped rather than composed)

Scope: `agents/reconciler.md` `## Coherence` mapping (`:173-181`) and `agents/orchestrator.md` Phase 3 step 3 (`:754`), the gate-rules row at `:975`, and the Rebalance Gate section at `:992-1020`. The `Resolved:` notes on `shared/issues/260817-1836_*` and `260823-1446_*` (row for "two of its three edges are then unevaluable") claim the session-with-no-Directive case is closed by `state Directive`; the mapping's overlap and the orchestrator's silence on the recommendation mean the case is named but not routed.

Fix direction: cut the case so it is disjoint. Either `state Directive` is a fifth verdict (a session that stated no Directive is neither coherent nor drifted), with the orchestrator firing a gate whose options include "state a Directive" and re-entering Step 0b.1; or it stays a recommendation and the `coherent → none` row is narrowed to "coherent and every edge evaluable", with `:754` firing on `coherent` plus `state Directive` and naming the option the user gets. `rules/critical-stance.md` §4 is the rule this is measured against.

Severity: Medium. No data is lost; a stated fix for a filed case does not reach the user.
---
Resolved: fixed — the second option: the `coherent → none` row is narrowed to `coherent` with every edge evaluable, and the orchestrator fires the Rebalance gate on `coherent` with recommendation `state Directive` too, naming Revise Directive (re-entry at Step 0b.1) as the option that states one, in Phase 3 step 3 and in the gate-rules row; `agents/reconciler.md` `## Coherence` mapping, `agents/orchestrator.md` Phase 3 step 3 (paid for by cuts in the same two files: agents surface net -193 bytes)
