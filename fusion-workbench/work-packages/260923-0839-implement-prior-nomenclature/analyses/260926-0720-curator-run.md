# Curator run 260926-0720 — survey of the three normative surfaces

**Date:** 2026-09-26 07:20
**Status:** Complete (survey 260926-0720; apply pass 2026-09-26, `**Approved:** all`)
**HEAD read:** `80a3d988`
**Mode:** survey (`**Scope:** anchored`, `**Placement:** off`, `**Edges:** off`). Dispatched by `/fusion:curate` with no flags. The dispatch named the trigger: the periodic `claude-md` check measured `CLAUDE.md` `## Conventions` at 4 078 bytes against a 4 000-byte threshold.
**Anchor:** `last_curator_run` = `32950b63` (2026-09-22 17:43:39 +0200), 62 commits behind HEAD.
**Previous curator run:** `260922-1859-curator-run.md`, 2026-09-22, survey plus four apply passes; its HEAD read is the anchor this run is bounded by. Found by walking `$WORKBENCH` for `*-curator-run.md`, never through `$SCAN_ANALYSES`.
**Work package in scope:** `260923-0839-implement-prior-nomenclature` (claimed by this checkout), so `$OUT_ANALYSIS`, `$OUT_ISSUE` and `$OUT_DECISION` resolve into its container and `$SCAN_DECISIONS` names that container's store (absent) and `shared/decisions`.

No placement was classified and no work package was read as a subject, so this file carries neither of those sections.

**What bounds the window and what does not.** 7d2ea602 renamed every container store (`circles/` to `work-packages/`, `planning/` to `plans/`), so mtime and `git diff --name-only` both report the whole workbench as touched. The bound used below is `git diff --name-status -M 32950b63..HEAD` with pure renames (`R100`) dropped: a file whose bytes did not change since a pass that read it needs no re-read, whatever directory it now sits in.

## 1. Evidence-source counts

| # | Source | Read | Bound |
|---|---|---|---|
| 1 | Work packages (`$SCAN_PACKAGES` = `work-packages`) | 41 containers enumerated for `**Status:**`: 1 claimed, 1 open, 1 paused, 13 done, 1 dropped, 24 legacy (no work-package value). 4 item records changed in content in the window and were read: `260922-1038-prior-mapping`, `260922-0137-measure-the-reconciler-on-a-smaller-model`, `260923-0839-implement-prior-nomenclature`, `260924-1216-own-command-for-new-packages-and-per-person-memos`. Plus the two specs of `260922-1038-prior-mapping` for the scheduling of one ruling (`## 3a`). | content changed since anchor |
| 2 | Decision records | Scan set: `shared/decisions` 96 records (`_i_` 76, `_a_` 15, `_d_` 4, `_s_` 1); the container store of the item in scope does not exist. Across all containers a further 153 (`_i_` 92, `_a_` 35, `_d_` 17, `_o_` 6, `_s_` 3). **31** records were added or changed in content in the window, and every resolution line among them was read; the 12 that carry a ruling on a store, a name or a rule (the eight of `260922-1038-prior-mapping`, the two added in `shared`, and the two moved to `_s_`) were read in full. | content changed since anchor |
| 3 | Git history | `git log 32950b63..HEAD` = 62 commits. `CLAUDE.md`: 5 commits (`77a6d73b`, `b26abe2b`, `7727987a`, `0123919a`, `d4089705`), every hunk read. `rules/`: 14 files changed, 234 insertions, 216 deletions, nearly all the v12 vocabulary pass; every hunk read. Outside the window, `git log -S` on two strings a Tier 1 check turned up (`Step 0c` in `skills/setup/SKILL.md`, `OUT_HISTORY` in `skills/`). | `<anchor>..HEAD` |
| 4 | Reviews and analyses | 209 files in the live tree; **3** changed in content in the window: the previous run file, `260923-0800-reconciler-haiku-versus-session-model.md` (read for its result), and one review whose change is a citation respelling. | content changed since anchor |
| 5 | `orchestrator-events.jsonl` | 4 906 rows, **198** with a `ts` past the anchor. Corroborating only; none was needed as evidence. | ts > anchor |
| 6 | Archive | 5 sweep directories, **0** added since the anchor; 3 archived files changed in content (citation respellings), read by diff. | changed since anchor |
| 7 | Provenance headers | **15 of 15** rule files carry one in the first ten lines. Every record citation in `rules/` and `CLAUDE.md` (51 distinct) was resolved by the workbench-wide lookup: none resolves to a `_s_` record, so no Tier 2 retirement arises from a header. Two resolve to nothing and both are the invented examples in `rules/decision-record-examples.md`; two resolve to open specs (`_o_` plans), which retire nothing. | full |

**Unreadable:** none. **Absent:** `.claude/rules/` does not exist, so the project-owned rule surface is `./rules/` alone.

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 7 987 | 66 | 7 885 | 66 |
| `./rules/*.md` | 15 | 213 718 | 2 147 | 213 884 | 2 148 |
| Decision records (`$SCAN_DECISIONS`) | 96 | 713 202 | 8 799 | 713 202 | 8 799 |

Commands: `wc -c -l CLAUDE.md`; `cat rules/*.md | wc -c -l`; `cat fusion-workbench/shared/decisions/*.md | wc -c -l`.

**What the four entries would do to sizes, measured on drafts in the scratchpad.** L02 and L04 together take `CLAUDE.md` from 7 987 to 7 885 bytes, lines unchanged, and `bin/fusion-claude-md-weight --root <draft>` then reports `over=0`: `## Conventions` falls under the 4 000-byte threshold. L01 adds 159 bytes to `rules/fusion-workbench-conventions.md`; L03 adds 7 to `rules/workbench-path-resolution.md`. Removals by tier: none. L01 narrows a constraint and removes no text.

**Blast radius:** the ledger proposes no deletion and no relocation, so 0 bytes leave any surface. No separate scale prompt is owed.

**The dispatch-path bound decides which subsets of the ledger are safe to apply.** `hooks/lib/__tests__/fixtures/dispatch-path.baseline` holds each path at zero head-room, and `CLAUDE.md` and `rules/fusion-workbench-conventions.md` are both charged to all eleven. Slack measured at `80a3d988` (agent prompt + `bin/fusion-rules <agent>` output + `CLAUDE.md`, against the row's total): orchestrator 104 bytes, requirements-designer 185, implementation-planner 366, policy-curator 378; every other path above 560. Per path, the entries cost: L01 +159, L02 +10, L04 −112, L03 0 (that file is emitted to no agent). So:

- **L01 alone turns `npm test` red** on the orchestrator path (+159 against 104). Every other path absorbs it.
- **L01 with L04** is +47 per path, inside every row. **L01, L02 and L04** is +57, inside every row, leaving the orchestrator 47 bytes.
- L02, L03 and L04 are each safe alone.

**One fixture moves if L01 is applied.** `hooks/lib/__tests__/fixtures/rules-emission.golden` pins `fusion-workbench-conventions.md` at 68 273 bytes in 11 rows. Regenerating it is `code-implementer` work: `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`. No other entry touches a pinned file.

## 3. Comparison counts

Every verdict below covers the pairs its selection rule reached, not the corpus.

| Pair | Selection rule | Pairs produced | Pairs read | Verdict |
|---|---|---|---|---|
| 1 decision vs decision | the 31 records changed in content in the window, each against the records its resolution line names, and the 12 rulings against each other | 31 + 12 | 43 | **no contradiction.** The two `_s_` transitions of the window (`260910-2145_*_does-the-container-store-keep-the-directory-name-circles.md`, `260816-0119_*_can-anything-carry-the-rename-to-citation-obligation-when-a-record-marker-moves.md`) each cite their successor. Last run's `### 3a` contradiction is resolved by the second of them. |
| 2 decision vs rule file | the 12 rulings of the window against the rule passages on the same store, name or mechanism (found by grepping each ruling's subject noun across `rules/`); plus the 51 record citations in `rules/` and `CLAUDE.md` resolved for their marker | 12 + 51 | 63 | **one contradiction**, fixed on its superseded side: L01. `inference:` the issues-marker ruling (`260922-1059_*_is-a-record-in-issues-a-candidate-or-an-admitted-work-item.md`, `_p_` read as an admitted item) and the rule's `_p_` "in progress" describe the same state in two vocabularies and do not conflict. |
| 3 decision vs `CLAUDE.md` | the same 12 rulings against `CLAUDE.md` in full | 12 | 12 | **no contradiction.** The stilwerk ruling keeps the name `CLAUDE.md` uses; the container-store ruling is what `CLAUDE.md` already says. |
| 4 rule vs rule | the 14 rule files changed in the window, hunk by hunk; a grep of all 15 files for every pre-v12 name the rename commits list (`coder`, `ontocoder`, `shaper`, `planner`, `reconciler`, `curator`, `editor`, `circles/`, `planning/`, `consult/`, `OUT_BACKLOG`, `SCAN_BACKLOG`, `fusion-claimed-item`); every repo path in backticks checked for existence (46); every `file.md` `## Heading` anchor checked (40) | 14 + 13 names + 46 + 40 | all | **one stale reference**, L03: `rules/workbench-path-resolution.md` names `OUT_BACKLOG` as the key the container store is written under, and `grep -c OUT_BACKLOG bin/fusion-paths` is 0. Every other surviving old name is historical and says so. One path is absent and correct (`rules/context-manifest.yaml` is a consuming project's). Five anchors do not match a heading exactly and all five are prefixes of a heading that exists. |
| 5 rule vs `CLAUDE.md` | every checkable assertion in `CLAUDE.md` run against the tree; its 16 anchors opened | 66 lines, 16 anchors | all | **one falsified claim**, L02. Held: 11 agent prompts, none with a `tools:` line; 15 skill directories; `EXEMPT_SKILLS` = `{setup, migrate}`; tag `v11.11.1` exists; `hooks/lib/workbench-root.ts`, `fusion.json` and `templates/fusion.json` exist. |
| 6 `CLAUDE.md` vs itself | all 66 lines against each other | 1 | 1 | **no contradiction.** |

### 3a. The history-store ruling against `## Session history`

`260922-1059_*_is-the-frozen-history-store-audit-evidence-or-typed-record-history.md` (marker `_a_`) answers: "option 2: the frozen `history/` corpus moves into `archive/` in one sweep, basenames unchanged, so storeless citations keep resolving". `rules/fusion-workbench-conventions.md` `## Session history` says: "Every `history/` directory in this workbench and in `archive/` stays where it is and stays readable." An agent asked to run the ruled sweep cannot obey both.

The ruling is later (filed 2026-09-22, ruled in `21087d8a` on 2026-09-24; the rule sentence predates both) and is the user's, so the rule side is the superseded one and L01 fixes it. The sweep itself is not scheduled: `260922-1045_*_spec-prior-nomenclature-plugin-source.md` `### C5` keeps every Review-class store in place for the current item, and `260922-1106_*_spec-prior-nomenclature-consumer-migration.md` lists `history/` as never opened. So L01 does not say the corpus has moved. It narrows "stays where it is" from permanent to "until that sweep", which is what both specs and the ruling say together.

### 3b. Found outside the window and reported, not proposed

- `README-agents.md` states four things the tree no longer does (setup's Step 0c concurrency warning, `/fusion:memo` as a filing route, stamps in `.fusion-setup`, `history/` in seven agents' Writes column). Outside this remit: `260926-0720_*_readme-agents-states-four-things-the-tree-no-longer-does.md`, for `code-implementer`. L02's pointer lands in the first of them.
- `bin/fusion-paths` still carries the `OUT_HISTORY` arm that `rules/workbench-path-resolution.md` `## The key table` says goes with the last prompt naming it; none has named it since `115be68d`. The rule is right and the code lags: `260926-0720_*_the-out-history-arm-outlived-the-last-prompt-naming-it.md`, for `code-implementer`.
- `260910-1600_*_does-the-periodic-check-cache-belong-in-a-file-that-travels-between-checkouts.md` stands at `_a_` while `421d0f89` realised it. Advancing the marker is the state-auditor's.

## 4. Pre-edit content

No entry modifies a decision record, so no pre-edit content is owed.

## 5. The ledger

### L01 — `## Session history` says every history directory stays where it is, and the user has ruled that the corpus moves into the archive

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md` `## Session history`
- **Tier:** 2
- **Citation:** `260922-1059_*_is-the-frozen-history-store-audit-evidence-or-typed-record-history.md`, its `Answered:` line: "option 2: the frozen `history/` corpus moves into `archive/` in one sweep, basenames unchanged, so storeless citations keep resolving; no file is reclassified by content; ruled by user, Kai Stalmann". It overturns the sentence "Every `history/` directory in this workbench and in `archive/` stays where it is and stays readable." Scheduling: `## 3a`.
- **Consequence group:** constraint removal
- **Constraint removed:** history directories stay where they are, permanently (narrowed to: until the ruled sweep)
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Applies safely only with L04:** alone it is +159 bytes on every dispatch path against 104 bytes of slack on the orchestrator's (`## 2`). Moves `rules-emission.golden` (`## 2`).

**Before:**
> **The existing corpus is kept, not deleted.** Every `history/` directory in this workbench and in
> `archive/` stays where it is and stays readable. A citation of a history file that already exists
> resolves exactly as it did (`## Filename Patterns`, the markerless form `YYMMDD-HHMM-<topic>.md`),
> so the resolution lines already written into records remain true. What no longer happens is a new
> file arriving in one of those directories.

**After:**
> **The existing corpus is kept, not deleted.** Every `history/` directory in this workbench and in
> `archive/` stays readable, and stays where it is until one sweep moves the corpus into `archive/`,
> basenames unchanged (`260922-1059_*_is-the-frozen-history-store-audit-evidence-or-typed-record-history.md`).
> A citation of a history file that already exists resolves exactly as it did (`## Filename Patterns`,
> the markerless form `YYMMDD-HHMM-<topic>.md`), so the resolution lines already written into records
> remain true. What no longer happens is a new file arriving in one of those directories.

### L02 — `CLAUDE.md` says setup warns about a second orchestrator, and setup no longer runs that check every session

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` `## Conventions`
- **Tier:** 1
- **Citation:** `grep -n -iE 'session-marker|concurrent' skills/setup/SKILL.md` returns nothing. `skills/check/SKILL.md` `## concurrency` holds the warning and says: "a session starting inside the 30-day window at an unchanged plugin version never runs it, so the collision warning is not reached". Moved out of setup at `71c0c873` (2026-09-10). The pointer target `README-agents.md` `## Invariants` still says setup Step 0c; filed as `260926-0720_*_readme-agents-states-four-things-the-tree-no-longer-does.md`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> - **One orchestrator per project**: advisory; setup warns when another session's marker is live. Detail: `README-agents.md` `## Invariants`.

**After:**
> - **One orchestrator per project**: advisory; `/fusion:check` warns when another session's marker is live. Detail: `README-agents.md` `## Invariants`.

### L03 — the key-retirement paragraph names `OUT_BACKLOG` as the key the container store is written under

- **Surface:** project rule file
- **File:** `rules/workbench-path-resolution.md` `### Retiring a key, and the worked case for it`
- **Tier:** 1
- **Citation:** `grep -c OUT_BACKLOG bin/fusion-paths` → `0`; `grep -n 'OUT_PACKAGES)' bin/fusion-paths` → `483:    OUT_PACKAGES)                 printf '%s' "$CONTAINER_STORE" ;;`. The same file's `## The key table` already says v12 renamed the pair; this sentence was left behind.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/workbench-path-resolution.md`

**Before:**
> that store's return: the directory is written to again, under `OUT_BACKLOG`, and the retired

**After:**
> that store's return: the container store is written to again, under `OUT_PACKAGES`, and the retired

### L04 — the workbench-bootstrap bullet in `## Conventions`, said in 112 fewer bytes with every constraint kept

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` `## Conventions`
- **Tier:** consolidation
- **Citation:** none required for a consolidation. Constraint by constraint, the After keeps: setup's exclusivity since v2.5.0; the marker path and its JSON content; the walk up from `pwd`; both locators by path; the halt text and the silent hook no-op; the stray-workbench purpose; v12 setup reporting and not refusing; `/fusion:migrate` as the only workflow naming both layouts, and why; the pre-v4 conversions at `v11.11.1`; setup's probe naming the legacy store; `EXEMPT_SKILLS` enumerating the two. The bullet is a preserve-list item (a setup invariant), which forbids deletion and not a rewrite that loses nothing.
- **Consequence group:** consolidation
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> - **Workbench bootstrap is exclusive to `/fusion:setup`**: since v2.5.0, **only** the setup workflow creates a workbench. Setup writes `fusion-workbench/.fusion-setup` (a JSON marker with timestamp + plugin version). Every agent and hook locates the workbench by walking up from `pwd` looking for that marker (`bin/fusion-workbench-root` for agents, `hooks/lib/workbench-root.ts` for hooks). If no marker is found: agents halt with "run /fusion:setup", hooks no-op silently. This prevents stray workbench creation when a Claude session's cwd happens to be in any non-fusion directory. Since v12.0.0, setup **reports a legacy store during the transition window and does not refuse**. `/fusion:migrate` performs the store-name migration and is the only workflow allowed to name both layouts literally, because it is the transition between them; the pre-v4 conversions live at the `v11.11.1` tag. Setup names the legacy store too, in the probe that reports it; `EXEMPT_SKILLS` in `hooks/lib/__tests__/path-literal-lint.test.ts` still enumerates the two.

**After:**
> - **Workbench bootstrap is exclusive to `/fusion:setup`** (since v2.5.0): only setup creates a workbench, writing the marker `fusion-workbench/.fusion-setup` (JSON: timestamp + plugin version). Agents (`bin/fusion-workbench-root`) and hooks (`hooks/lib/workbench-root.ts`) locate the workbench by walking up from `pwd` to that marker; with none found, agents halt with "run /fusion:setup" and hooks no-op silently, so a session whose cwd is in a non-fusion directory never creates a stray workbench. Since v12.0.0 setup **reports a legacy store during the transition window and does not refuse**. `/fusion:migrate` performs the store-name migration and is the only workflow allowed to name both layouts literally, being the transition between them; the pre-v4 conversions live at the `v11.11.1` tag. Setup's probe names the legacy store too, and `EXEMPT_SKILLS` in `hooks/lib/__tests__/path-literal-lint.test.ts` still enumerates the two.

## 6. Outcomes

Apply pass 2026-09-26, dispatched by `/fusion:curate` with `**Approved:** all`. Each before-text was re-read from disk and matched exactly once; each written region was re-read and matched the After block byte for byte.

- **L01** applied — `rules/fusion-workbench-conventions.md` `## Session history`
- **L02** applied — `CLAUDE.md` `## Conventions`
- **L03** applied — `rules/workbench-path-resolution.md`
- **L04** applied — `CLAUDE.md` `## Conventions`

**Checks after apply.** `bin/fusion-claude-md-weight`: `bytes=7885 over=0`. `npx vitest run lib/__tests__/dispatch-bytes.test.ts`: 10 of 10 pass. `npx vitest run lib/__tests__/rules-emission-golden.test.ts`: 1 of 22 fails, the size pin on `fusion-workbench-conventions.md` moved by L01, as `## 2` predicted. Regenerating it is `code-implementer` work: `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`. Not run here.

**Revert:** `git checkout -- CLAUDE.md rules/fusion-workbench-conventions.md rules/workbench-path-resolution.md`.
