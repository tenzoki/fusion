# Code review — C2 Turn 2: the repairs of Turn 1

**Reviewed-range:** `2f1e3a6..b8a4c1a`
**Not-opened:** none

**Date:** 2026-08-23
**Sender:** coderev
**Circle:** `260823-0023-settle-what-travels-between-checkouts`
**Plan:** `260823-0800_*_c2-what-travels-between-checkouts-is-settled.md`, steps 8 and 9 plus the Turn 1 review repairs
**Range spelling:** the dispatch named the span `e41393e..b8a4c1a` and listed `e41393e` among its six commits. Written that way the exclusive-from form leaves `e41393e` untiled, and `bin/fusion-review-coverage` reports it uncovered. The header therefore spells the same six commits as `2f1e3a6..b8a4c1a`, which is where the Turn 1 review's own range ends.
**Carried from an earlier pass:** nothing. The Turn 1 review declared `**Not-opened:** none`, so `bin/fusion-review-coverage` reports `carried=none` and no file entered this scope by inheritance.

---

## Summary

The repairs are real. Every one of the eleven Turn 1 findings was traced to what landed, and each landed repair does what its record says: the citation gate is green in a fresh clone for the right reason, the marker guard is correct including its self-heal claim, the four-class partition now tiles the whole layout tree, the two decision records cite four commits and three headings that all resolve, and the citation baseline's log and constant agree at HEAD for the first time in this Circle.

Six new findings, none of them a release blocker and none of them a repair that failed. Five are in workbench records and shipped documentation rather than in mechanism. The one that matters is the answer to the question the dispatch asked last: the renames of this Turn broke ten citations in the Turn 1 review file, and no gate's corpus reaches a review file.

## Totals

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 1 |
| Medium | 1 |
| Low | 4 |

## Findings by theme

### Theme 1: a marker move broke ten pointers and every gate stayed green

**High. Ten `Record:` citations in the Turn 1 review dangle.** `.../260823-1110-coderev-c2-turn-1.md:36`, `:41`, `:46`, `:49`, `:52`, `:57`, `:69`, `:74`, `:77`, `:80`. Each names a defect record with its `_o_` marker spelled out; `e7454e3`, `d23c706` and `18974bc` renamed all ten to `_c_`. The corpus predicate at `hooks/lib/__tests__/workbench-citation-lint.test.ts:163-172` selects Circle records, open issues, live decisions, live plans and the portfolio, so a file under `reviews/` is outside it entirely.

`18974bc` met the identical fault one file away, in the `Also seen:` line it had appended to an open issue, and repaired it because the gate went red. Its commit message calls that "the second instance of it in this range" while ten further instances stood in the file it names as its own `Source:`. The pattern to take from it is the one Turn 1 already stated about `portfolio.md` and which repeats here in a second form: a repair pass finds what its gates show it, and the corpus is a smaller set than the tree.

Record: `260823-1318_*_ten-record-citations-in-the-turn-1-review-dangle-after-this-turns-renames-and-no-gate-covers-reviews.md`

**Low. The claim that nothing else cites the two decisions by a hard marker is false.** `a76ee8f` recorded it as checked, in its commit message and at `.../260823-1243-coder-two-decision-records-close-as-implemented.md:53-55`. Four files cite them by `_o_`: `260822-2239-reconciliation.md:52`, `.../260822-2219-analyst-two-checkout-isolation-measurement.md:34`, `.../260822-2204-orchestrator-session.md:122`, `.../260822-2219-what-two-checkouts-of-one-project-actually-share.md:265`. All four were already dangling at range start, so the rename broke nothing new, which is exactly why the false half survived: a check for the marker the rename moved from finds nothing and reads as a clean result. Same coverage gap underneath, since history files and analyses are outside the corpus too.

Record: `260823-1318_*_the-closure-claim-that-nothing-else-cites-the-two-decisions-by-a-hard-marker-is-false-in-four-files.md`

### Theme 2: a case split that names more than it tests

**Medium. Step 0i detects `MULTIPLE-ACTIVE` only when the pointer is absent.** `skills/setup/SKILL.md:354` short-circuits on `[ -f ./fusion-workbench/.active-circle ] ||`, so `find` never runs with a pointer present. `:360` nonetheless names the branch as "the condition `agents/playmaker.md` names", and `agents/playmaker.md:94` defines that condition with no pointer precondition. The half it cannot see is the likelier one under the arrangement C2 ships: the record travels, the pointer does not, so a checkout that activated something of its own holds a pointer and pulls a colleague's `_t_` record straight into the blind branch.

The narrowing itself holds and is judged separately below.

Record: `260823-1318_*_step-0i-detects-multiple-active-only-when-the-pointer-is-absent-while-naming-the-whole-condition.md`

### Theme 3: records and documentation that describe the mechanism inexactly

**Low. The plan is `Status: Complete` with all nine steps `[DONE]` and still carries `_o_`.** `.../260823-0800_*_c2-…:4`. `rules/fusion-workbench-conventions.md:387` states one instruction with two parts and `b8a4c1a` performed one. It may be deliberate, because `_c_` takes the plan out of both the citation gate and the stopping-section lint, and this Circle has already recorded once what a rename out of a corpus costs. Either the rename is owed or the rule sentence needs the closure clause it does not have.

Record: `260823-1318_*_the-plan-is-status-complete-with-every-step-done-and-still-carries-the-open-marker.md`

**Low. The portfolio corpus assertion passes the constant to itself.** `hooks/lib/__tests__/workbench-citation-lint.test.ts:286` asserts `inCorpus(PORTFOLIO)` where `inCorpus` tests `rel === PORTFOLIO`, so the line is invariant under a change to what `PORTFOLIO` names. It catches a deleted clause, which is what the commit message claims for it, and no longer catches a wrong one, which the line it replaced did catch. One token repairs it: pass the literal.

Record: `260823-1318_*_the-portfolio-corpus-assertion-passes-the-constant-to-itself-and-no-longer-pins-the-name.md`

**Low. `README-hooks.md:369` omits live plans from the corpus, the fifth kind the predicate selects.** Pre-existing since `ac01c90`, which added the row and `LIVE_PLAN_RE` in one commit, and filed here because `e7454e3` re-authored that exact sentence to qualify `portfolio.md` and left the omission standing. It is not academic now: this Circle's own plan is the file that clause selects.

Record: `260823-1318_*_the-readme-corpus-description-omits-live-plans-the-fifth-kind-the-predicate-selects.md`

## The six repairs, judged

Recorded at length because the dispatch's question was whether the repairs are real, and a verdict without its evidence is not checkable.

**1. `e7454e3` refused the record's own recommendation, and the refusal is right.** All three grounds hold. Class L is defined in `rules/workbench-tracking.md` as what git carries between checkouts and says nothing about what a gate may read, and `agentstate.yaml` is the cited counter-example that separates the two properties. The corpus is a user's recorded answer under decision `260819-1645`, so narrowing it inside a defect repair would be the move that decision exists to refuse. And ground three is true as stated: `hooks/lib/__tests__/portfolio-citation-form-lint.test.ts` reads `agents/playmaker.md`, the generating prompt, and not a produced portfolio, so dropping the file from this corpus would leave a generator emitting a dead path unopposed. The precedent argument holds too, in the file's own words at `:154-160`: the predicate is written pure so that constructed paths this tree does not carry can be put to it. Coverage is genuinely unchanged, because `corpusFiles()` filters what is on disk. The one shortfall is the token, filed above.

**2. `d23c706`'s self-heal claim is true, traced through the shipped condition.** With an existing marker carrying `"plugin_version":""` and a readable `$V`, the guard at `skills/setup/SKILL.md:94` passes, `grep -qF '"plugin_version":"<V>"'` finds no match against the empty field, the `&&` fails, the `||` fires and the marker is rewritten. So repair code would duplicate a repair that happens anyway, which is what the commit claims. The guard's own case is right as well: an empty `$V` prints the token and exits before the comparison, so an absence can no longer arrive dressed as a match. The unreadable-and-no-marker outcome, where the workbench ends with no marker and every agent halts, is stated in the step rather than left to be met.

**3. The four-class partition now tiles the whole tree.** Checked entry by entry against `rules/fusion-workbench-conventions.md:26-64`, not only against the entry that was reported. Fifteen tree entries plus the two frozen stores: `circles/`, `shared/`, `archive/`, `stilwerk/`, `stashes/` and `.migration-v2-backup/` in R1; `orchestrator-events.jsonl` in R2; `.fusion-setup` and `.asset-provenance` in R3; `agentstate.yaml`, `orchestrator-live.md`, `.session-marker`, `.active-circle`, `.commit-lock/`, `monitor`, `portfolio.md` and `.guard-state/` in L. Seventeen entries, each in exactly one class, none in none, none in two. The archive-roll paragraph now says why the roll does not split an entry across classes, which is the sentence the old text got wrong. `.gitignore:76-83` matches the class L set exactly, all eight and nothing else, and `git ls-files fusion-workbench | awk -F/ 'NF==2'` returns exactly the three R2 and R3 entries.

**4. The `MULTIPLE-ACTIVE` narrowing is a complete treatment of the smaller decision, on the ground the step gives rather than the one the commit gives.** The shipped text at `skills/setup/SKILL.md:360` grounds it on "which of several to run here is a portfolio judgement", and that distinguishes the one-record case correctly: a yes or no about the single Circle the project already calls active is not a choice among Circles, so Setup keeping the one-path offer while refusing the many-path offer is coherent. The commit message argues it more weakly, as "the activation choice belongs to the skill that owns it", which taken alone would forbid the one-path offer that Setup keeps and that `rules/fusion-workbench-conventions.md:82` enumerates it as the fifth writer for. The claim about byte cost is checkable and checked: `skills/` stands at 240 067 of 240 439, 372 free, and the branch cost 400 of the 931 spent, so a full treatment did not fit. What is incomplete is the detection, not the treatment, which is Theme 2.

**5. All four commit hashes and all three headings resolve.** `c9eba48` is the union merge driver in Step 0h, `2f1e3a6` sorts the Phase-4 sequence diagram by `ts`, `18974bc` gives the `set` branch its own words in both the step and the rule, `25f60eb` is Step 0i and touches `rules/fusion-workbench-conventions.md:82` in the same commit, which is the fourth constraint that record placed on any answer adding a writer. The headings are `rules/workbench-tracking.md` `## The event log carries a union merge driver`, `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout`, and `## Grounding snapshot` at `.../_t_circle.md:15`. Each was opened rather than inferred, because the note that renaming to `_i_` takes both records out of the gate's corpus is correct and the green suite says nothing about them.

**6. The citation baseline's log and constant agree at HEAD, and every move since is accounted.** The chain reads 1284 at the v10.6 release, then 1286 (step 1), 1287 (`c9eba48`, retrospective), 1288 (step 4), 1291 (step 5), 1291 (head-room cut, paths unmoved), 1292 (`1400402`, retrospective), 1293 (the Critical repair), 1294 (the text-correctness pass). `const BASELINE = { paths: 1294, anchors: 180, records: 117 }` at `hooks/lib/__tests__/reference-resolution-lint.test.ts:583`. Anchors tile 175 to 180 and records 116 to 118 to 117 across the same entries. The two retrospective blocks are labelled as out of sequence and move the constant by nothing, which is right: they are the gap the other entries left against it, written down rather than added to. The suite measures 1294 today, so the log is not merely self-consistent.

## What else was verified and holds

- **The staging-drift example is producible.** Both authoring sites now print `record M circles/<dir>/_t_circle.md UNSTAGED (a Circle record)`, and `classify` at `hooks/lib/staging-drift.ts:426-428` returns exactly `{ klass: "record", why: "a Circle record" }` for that path. The retired example was replaced with one the classifier can emit, `why` string included, not with a second unproducible one.
- **The repaired line citations land where they claim.** `rules/fusion-workbench-conventions.md:66` now cites `skills/setup/SKILL.md:65`, the bash probe, and `:58`, the paragraph recording the permanent-refusal cost. Both exact. The six `skills/next/SKILL.md` citations in `README-agents.md` were checked one by one: `:97` and `:147` are the two `**Domain:**` lines, `:144-153` spans the second dispatch block, `:153` is `**Proposal source:**`, `:156` is the contract restatement.
- **No growth-bound baseline moved.** `git diff e41393e..b8a4c1a` over `helpers/growth-bound.ts`, `surface-growth-bound.test.ts` and `rules-emission-golden.test.ts` returns nothing.
- **The suite is green in this working tree**, 41 files and 724 tests, and the analysis report at `.../260823-1302-…:19` records 724 passing in a fresh clone, which is the measurement the Critical finding needed.
- **The step-9 analysis is bounded honestly.** Its `## Scope` names the simulated sessions, the local-only remote and the small clean workbench as its three gaps, and its monitor finding is filed rather than repaired with the fork stated as a fork. Its verdict that sorting by `ts` does not repair the monitor is supported by a measurement in which sorting moved the answer from vague to wrong, which is a stronger claim than the plan expected and is the right one to have filed.
- **The nine closed defect records each carry a `Resolved:` note**, and the ninth carries an appended note explaining why it stays `_o_` rather than a `Resolved:` line it has not earned.

## Cross-cutting observations

**The same coverage gap produced three of this Turn's six findings.** A review file, a history file and an analysis all cite records by name, none is in the citation gate's corpus, and all three carried a stale pointer at HEAD. Turn 1's cross-cutting note said a file leaving the index has consumers that read the disk rather than the index. The Turn 2 form is narrower and sharper: a record leaving one marker has citers the gate does not read, and the gate's silence is what the repair pass took for a clean result. Both instances of it this Turn were recorded, in good faith, as checked.

**Two of the six repairs improved on the record they answered, and one of those improvements is under-argued.** `e7454e3` refused a smaller diff on grounds that hold, and `18974bc` chose the smaller of two treatments on a ground that is right in the step and weaker in the commit message. Where a record's recommendation is refused the argument is the deliverable, and it is worth putting the strongest form of it in the shipped text rather than in the commit, because the commit message is the copy nobody re-reads.

**Nothing in this range regressed a Turn 1 repair.** Every mechanism Turn 1 verified was re-checked where this Turn touched it, and the mechanisms it did not touch were left alone.

## Recommended sequencing

**Before the Circle closes.** Theme 1's High finding, because the Turn 1 review is this Circle's durable record of its own findings and ten of its pointers name nothing, and the Low record beside it, because the same pass fixes both.

**With the closure work.** Theme 3's plan-marker question, since whoever closes the Circle has to answer it anyway.

**Cleanup, at the user's convenience.** The one-token assertion repair, the README row, and Theme 2's detection gap, which is a scope question rather than a bug in what shipped.

## One fact carried from Turn 1, still true and still not a defect

This repository has no `.gitattributes` and `git check-attr merge -- fusion-workbench/orchestrator-events.jsonl` returns `unspecified`. Setup has still not run here since the driver shipped as Step 0h. The step-9 verification declared the driver in each of its two scratch clones and measured the mechanism correctly, so the criterion is met; the project that ships it is simply not yet a project that has it.
