# Curator run 260922-0703 — survey with the work-item edge subject on

**Date:** 2026-09-22 07:03
**Status:** Complete
**HEAD read:** `2c826154`
**Mode:** survey (`**Edges:** on`, `**Scope:** anchored`, `**Placement:** off`), then apply on 2026-09-22 08:45 with `**Approved:** all` (`## 7`)
**Anchor:** `last_curator_run` = `99fbaa8a` (2026-09-16 13:34:31 +0200), 132 commits behind HEAD
**Previous curator run:** `260918-0806-curator-run.md`, 2026-09-18, the apply pass of `260918-0738-curator-run.md`; both found by walking `$WORKBENCH` for `*-curator-run.md` (14 run files across the tree, none through `$SCAN_ANALYSES`). The anchor was not advanced by those two runs, so this run's evidence bound is the 260916-1612 run's commit.
**Work item in scope:** `260917-2253-depends-on-edges-proposed-and-confirmed.md` (`claimed`, checkout `5e8248d7`)

The dispatch asked for the fresh edge survey the item's release note owes. The three normative surfaces were surveyed too, bounded by the anchor; the edge corpus was not bounded, as `agents/curator.md` `### The two anchor rules` requires.

## 1. Evidence-source counts

| # | Source | Read | Bound |
|---|---|---|---|
| 1 | Work items | 10 of 35 item records carry an mtime past the anchor and were read as evidence; all 35 were enumerated for `**Status:**`, which the edge subject needs. 24 containers carry a legacy Circle record, 11 a native item record. | mtime > anchor date |
| 2 | Decision records | 52 of 96 under `$SCAN_DECISIONS` changed since the anchor (2 in the container, 50 shared); every added or removed line of the 52 was read via `git diff 99fbaa8a..HEAD`. The one `_o_` shared record (`260827-1305_*_which-agents-run-on-a-smaller-model.md`) was read in full. | `git diff` since anchor |
| 3 | Git history | `git log 99fbaa8a..HEAD` over `rules/` and `CLAUDE.md`: 6 files touched (`CLAUDE.md` 3 commits, `fusion-workbench-conventions.md` 12, `context-lean-claude-md.md` 3, `commit-lock.md`, `user-facing-output.md`, `workbench-path-resolution.md` 1 each); every hunk read. | `<anchor>..HEAD` |
| 4 | Reviews and analyses | 9 files carry an mtime past the anchor (4 in the container, 5 shared); the container's four were read in full, the shared five were read by name only. | mtime > anchor date |
| 5 | `orchestrator-events.jsonl` | 657 rows with `ts` past the anchor, of 4 483; corroborating only, none was needed as evidence. | ts > anchor |
| 6 | Archive | 4 sweep directories; **0** files with an mtime past the anchor. Seven citations in the edge corpus resolve into the archive and were read as endpoints only. | mtime > anchor date |
| 7 | Provenance headers | 15 of 15 rule files carry one in line 3. Ten cite a work item or decision, five carry the admission-plus-commit form. None of the cited records carries the superseded marker (checked by resolving each cited basename). | full |

**Unreadable:** none. **Absent:** `.claude/rules/` does not exist, so the project-owned rule surface is `./rules/` alone (15 files).

**Suppression read** (`bin/fusion-edge-answers`, present at `$FUSION_PLUGIN_ROOT/bin`): `run-files=14 edge-entries=2 pairs=2 unreadable=0 verdict=answers`. Both prior entries (`260917-2253-… -> 260908-2018-…` and `260917-2253-… -> 260909-1700-…`, each into `**Cross-references:**`) carry outcome `none`: no apply dispatch ever ran on `260918-0738-curator-run.md`, so no gate was answered and **nothing is suppressed**. Both are re-proposed below.

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 8 021 | 66 | 8 021 | 66 |
| `./rules/*.md` | 15 | 209 671 | 2 115 | 209 804 | 2 115 |
| Decision records (`$SCAN_DECISIONS`) | 96 | 725 135 | 8 978 | 725 135 | 8 978 |

Commands: `wc -c -l CLAUDE.md`; `wc -c -l rules/*.md | tail -1`; `cat <every file in $SCAN_DECISIONS> | wc -c -l`. The "after" column was filled by the apply pass of 2026-09-22 (`## 7`): `context-lean-claude-md.md` 15 760 → 15 817 (+57, L01), `fusion-workbench-conventions.md` 67 405 → 67 481 (+76, L02); no line count moved. Removals by tier: none.

**Blast radius:** the ledger proposes no deletion and no relocation, so 0 bytes leave any surface. The two Tier 1 entries each rewrite one sentence in one rule file, growing the surface by 133 bytes (57 and 76). No separate scale prompt is owed.

## 3. Work-item edges

### 3a. The corpus

Two live items, read as `### The corpus, and the live/terminal bound` defines the corpus: the item record, every file inside the container, and every record **the item record** cites.

**Owner A — `260917-2253-depends-on-edges-proposed-and-confirmed.md` (`claimed`).** Container: 24 files (the record, 2 planning, 12 issues, 2 decisions, 1 discussion, 1 review, 3 analyses, two of them prior curator run files of 64 544 and 108 619 bytes). Cited by the record and outside the container: 5 records — the two work-item records `260918-0706-strike-unconfirmed-depends-on-entry.md` and `260917-1338-depends-on-kanten-automatisch-erzeugen.md`, and three issues, `260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md`, `260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md` and `260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md`. 28 files, 401 927 bytes in the container plus the five.

**How the corpus was read, stated honestly.** Test 1 is a lookup, and it was run mechanically over all 28 files: 91 distinct record basenames were extracted (`grep -oE` over the stamp-and-slug shape, marker wildcarded), each resolved with `find "$WORKBENCH" -name`, and each hit classified by the container it landed in. The 26 files other than the two prior run files were read sentence by sentence for test 2. The two prior run files (173 KB, ledgers about normative text) were read for test 2 by an ordering-vocabulary grep (`prerequisite|blocked by|blocks|waits on|before … may start|depends-on`, 21 hits, each read) rather than sentence by sentence. Every hit was the first run's own account of the readings listed below, so nothing new came out of them; a reader who wants the sentence-by-sentence read of those two files does not have it here.

**What test 1 returned for owner A.** Of the 91 basenames, 35 resolve into `shared/`, 7 into the archive, 2 resolve nowhere (one a deleted planning record, stamp `260907-2358`, slug `a-planning-layer-for-fusion-positions-edges-and-computed-order`; the other a truncated stamp `260812-0022` inside a quoted exhibit), 22 into owner A's own container, and 25 into eleven other containers:

| Container | Status | Test 1 verdict |
|---|---|---|
| `260801-1244-curator` | legacy `closed` | not a work item here (legacy vocabulary) |
| `260801-1244-guard-rules-write` | legacy `closed (coherent)` | not a work item here |
| `260805-2005-textschicht-gegen-code-nachziehen` | legacy `closed (coherent)` | not a work item here |
| `260813-0858-playmaker-maintains-backlog-store` | legacy `closed` | not a work item here |
| `260815-0007-remove-eight-mechanisms-and-cap-growth` | legacy `closed` | not a work item here |
| `260824-1853-close-every-open-defect` | legacy, no `**Status:**` line | not a work item here |
| `260908-2018-prerequisites-confirmed-once-order-computed` | `done` | work item, 12 citations, **not in either field** |
| `260909-1700-cut-fusion-to-working-minimum` | `done` | work item, 3 citations, **not in either field** |
| `260912-0438-human-facing-docs-leave-claude-md` | `done` | work item, 2 citations, **not in either field** |
| `260917-1338-depends-on-kanten-automatisch-erzeugen` | `dropped` | work item, already in `**Cross-references:**` — no entry |
| `260918-0706-strike-unconfirmed-depends-on-entry` | `done` | work item, already in `**Depends-on:**` — see 3c |

The count reproduces the measurement in `260918-0824_*_the-container-hop-voided-the-only-stated-mitigation-for-flooding-the-gate.md`: eleven containers, six excluded by the legacy rule, five work items, two already carried.

**Owner B — `260922-0137-measure-the-reconciler-on-a-smaller-model.md` (`open`).** Container: the record alone. Cited by the record: `260827-1305_*_which-agents-run-on-a-smaller-model.md` (shared decision, `_o_`) and `260827-1305-does-agent-frontmatter-model-reach-the-dispatch.md` (shared analysis). Three files, read in full. Test 1 over them: 4 basenames, one resolves into `shared/decisions`, one into `shared/analyses`, one nowhere (stamp `260827-0335`, slug `bookkeeping-cost-repair-plan`, cited with a `refactor/` prefix in the decision's head; no file in the tree carries that name), and one — `260911-1218-curator-run.md`, named in the decision's reconciliation note of 260921-2230 — into the container of `260909-1700-cut-fusion-to-working-minimum` (`done`).

### 3b. Readings, and the entries they produced

One entry per (dependent, target, field, reading kind). The `**Edge:**` line on each entry records how each endpoint was fixed.

| Target | Reading | Sentence it rests on | Route | Entry |
|---|---|---|---|---|
| `260908-2018-…` | citation | owner A's record, Directive: "Six further points are unsettled and named in `260909-1020_*_…`", and "two open defects (`260911-1747_*_…` and `260911-0715_*_…`) record that the field's one live value asserts an ordering where the prose it was derived from asserts a substantive conflict" — both records sit in that container | `**Cross-references:**`, citation group | L03 |
| `260908-2018-…` | ordering, inferred | owner A's spec, head: "This spec does not replace `260911-1528_*_spec-prerequisites-confirmed-once-order-computed.md`. That document specified the whole mechanism … What stands unbuilt is its C3 … This spec takes C3 as its subject, inherits C3's settled decisions rather than re-opening them" — that spec sits in the `260908-2018` container | target terminal, so `**Cross-references:**`; **ordering group** | L04 |
| `260909-1700-…` | citation | owner A's spec, `## The question standing in front`: "`260909-1700-cut-fusion-to-working-minimum.md`, status `done`, carries `**Depends-on:** 260908-2018-…`, derived by the migration from a section whose own words are 'Conflicting …'" (named); owner A's record cites `260911-0715_*_…`, which sits in that container (hop) | `**Cross-references:**`, citation group | L05 |
| `260912-0438-…` | citation | `260918-0824_*_the-container-hop-voided-the-only-stated-mitigation-for-flooding-the-gate.md`, the measurement paragraph, names the basename in its list of five admissible targets (named); `260918-0738-curator-run.md` head names `260916-1612-curator-run.md`, which sits in that container (hop) | `**Cross-references:**`, citation group | L06 |
| `260909-1700-…` (from owner B) | citation | `260827-1305_*_which-agents-run-on-a-smaller-model.md`, reconciliation note 260921-2230: "the only later mention is the curator survey `260911-1218-curator-run.md`, which rules nothing" — that run file sits in the `260909-1700` container | `**Cross-references:**`, citation group | L07 |

**Why L04 is an ordering entry and not a second citation.** Test 2 asks whether the sentence fixes one side reaching a finished state before the other may start. "Inherits C3's settled decisions rather than re-opening them" is one supportable reading of that shape: the decisions of `260908-2018` were settled, that item reached `done`, and this item's spec starts from what it left. The words do not say "before", so the tier is `inferred`, and the entry is the direction call the gate's two edge groups exist to let a user refuse separately. The citation reading of the same corpus stands beside it as L03, so refusing L03 loses nothing but the direction.

**Not proposed, and why.** `260917-1338-…` (dropped) already stands in owner A's `**Cross-references:**`; `260918-0706-…` (done) already stands in owner A's `**Depends-on:**`, and 3c is its report. No `**Depends-on:**` entry is proposed: every target this corpus names is terminal, and the node-set ruling puts each outside the graph, so the ordering field could carry none of them without a dangle.

### 3c. Residue

One line per ordering reading this pass may not propose, with the record it came from.

| # | Sentence (short) | Record | Reason |
|---|---|---|---|
| R1 | "The correction half left as `260918-0706-strike-unconfirmed-depends-on-entry.md`, which this item now names as its prerequisite" | owner A's record, split note | Not residue in the strict sense: the ordering is already confirmed in `**Depends-on:**`. Reported under the "target's status changed" rule: the target reached `done` on 260918, so the confirmed edge is a dangle by the node-set ruling; `bin/fusion-work-order` prints `unresolved-edges=1 … unresolved=260917-2253-… wants 260918-0706-…`. Revising or retracting it is the user's act. |
| R2 | "`260911-1747_*_…` states in explicit words that `260909-1700-…` carries a `**Depends-on:**` on `260908-2018-…`. Two work items, both named, direction fixed" | `260918-0738-curator-run.md`, `## 3b` | Dependent is `260909-1700`, not the corpus owner — third bullet of the ordering cell. Also historical: that entry was struck at `2a9cd013`. |
| R3 | "Until a declined edge is remembered, which is open at `260918-0712_*_…`, a stale acceptance line costs a re-presentation on every pass" | owner A's spec, `### C1` | The other end is a decision record, not a work item (`empty, yes`). Also stale: that record is `_i_` since 260918. |
| R4 | "The fourth asks for an order over more than one node, and the store holds one live item, so it is reachable only once the backlog grows — which is the user's act" | owner A's record, release note | The other end is the store's growth, not a work item (`empty, yes`). Note that the store now holds two live items, so the premise has moved. |
| R5 | "The decision `260827-1305_*_which-agents-run-on-a-smaller-model.md` has waited four weeks on an experiment no session starts by itself … The item is reached when … the decision record carries an `Answer located:` line" | owner B's record, Directive | The other end is a decision record, not a work item (`empty, yes`). |

### 3d. What a confirmed edge's prose says now

The one confirmed ordering edge in the store (`260917-2253 -> 260918-0706`) has a terminal target; R1 above reports it. No confirmed edge's underlying prose changed; the split note that carries it stands as written.

## 4. Comparison counts

Every verdict below covers the pairs the selection rule reached, not the corpus.

| Pair | Selection rule | Pairs produced | Pairs read | Verdict |
|---|---|---|---|---|
| 1 decision vs decision | the 52 records changed since the anchor, compared by their appended annotation lines (`Answered:`, `Implemented:`, `Retired:`, `Deferred:`, `Answer located:`) for two records taking opposite positions on one subject; plus the 2 container records against the shared record each returns from (`260911-1833_*_…`, `_d_`) | 52 annotation sets, 1 return pair | 52 + 1 | no contradiction; the return pair is a new record filed against a terminal one, which is the sanctioned route |
| 2 decision vs rule file | the 52 annotation sets against the 5 rule files changed since the anchor (hunks) and `fusion-workbench-conventions.md` in full | 52 × 6 | 52 × 6 | no live rule contradicts an annotated position; every `Retired:` line names a mechanism no rule still binds (grep over `rules/` for `orchestrator-resume`, `playmaker`, `backlog-entries.md`, `circle-records.md`, `skills/next`, `skills/direct`, `log-activity`, `skip claude-md`, `shared/backlog`: only historical mentions) |
| 3 decision vs CLAUDE.md | the 52 annotation sets against `CLAUDE.md` in full | 52 | 52 | none |
| 4 rule vs rule | the 5 changed files' hunks against each other and against the 4 files emitted to this agent (read in full) plus `rule-file-provenance.md` and `context-lean-claude-md.md` | 5 × 10 | 5 × 10 | none; the discussions store is added consistently across `fusion-workbench-conventions.md` (tree, patterns table, record-filing table, marker rules) and `workbench-path-resolution.md` (`OUT_DISCUSSION`, no `SCAN_DISCUSSIONS`) |
| 5 rule vs CLAUDE.md | `CLAUDE.md` in full against the same 11 rule files | 11 | 11 | none |
| 6 CLAUDE.md vs itself | all 66 lines | 1 | 1 | none |

Corpus counts derived by: `git diff --name-only 99fbaa8a..HEAD -- <stores>` (52), `ls rules/*.md | wc -l` (15), `git diff --name-only 99fbaa8a..HEAD -- rules/ | wc -l` (5), `wc -l CLAUDE.md` (66).

**Precedence-undecided:** not applicable; `.claude/rules/` is absent, so there is one rule root.

**Open defects as a cross-check.** Three open defects under `$SCAN_ISSUES`. `260918-0824_*_the-container-hop-voided-…` (container) describes the unbounded yield this run's 3a reproduces, and contradicts nothing proposed here. `260921-2238_*_a-curator-survey-spells-a-decisions-marker-letter-…` names a stale-marker token in `260918-0738-curator-run.md`; that file is an analysis, outside the three surfaces, so this run does not touch it and the defect stays where it is. `260831-0748_*_a-storeless-bracket-marked-citation-is-invisible-…` is unrelated to any entry.

## 5. Pre-edit content

No decision record is proposed for modification. Nothing to record.

## 6. The ledger

Groups at the gate, most consequential first: tier-1 (2), work-item ordering edge (1), work-item citation edge (4). Candidates: 1, not on offer.

### L01 — the placement rule's worked example states a CLAUDE.md preamble size that moved

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`
- **Tier:** 1
- **Citation:** `awk '/^## /{exit} {print}' CLAUDE.md | wc -l -c` → `7 411`; `"$FUSION_PLUGIN_ROOT/bin/fusion-claude-md-weight"` → row `under 411 7 (preamble)`. The sentence says "as that file stands" and was true at `06f47b9d` (`git show 06f47b9d:CLAUDE.md | awk … | wc -c` → 414); `bb44a56f` ("CLAUDE.md drops its 28 em-dashes by repunctuation alone, 84 bytes smaller") took three of those bytes out of the preamble. The 43-byte figure for the two declarations still holds (`grep -E '^\*\*(Language|Artifact language):' | wc -c` → 43), and so does "seven lines".
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/context-lean-claude-md.md`
- **Derive over correct:** the sentence needs the number to show that the declarations are a small part of a passage that stays whole. `bin/fusion-claude-md-weight` derives the passage size on every run and prints the level it divided at, and the same file already names that helper two paragraphs above as the implementation of Step 1. The replacement names the helper's row and stamps the figure as the fallback, so the next repunctuation does not falsify the rule again. No code is needed.
- **Fixture note:** this file is emitted to no agent (`bin/fusion-rules` and `rules/context-manifest.yaml` carry no `context-lean` entry), so `hooks/lib/__tests__/fixtures/rules-emission.golden` does not pin it and no regeneration is owed.

**Before:**
> so its preamble — everything above the first `## ` — is one passage: seven lines
> and 414 bytes, holding the document title, the two language declarations

**After:**
> so its preamble — everything above the first `## ` — is one passage: seven lines
> and the bytes the helper's `(preamble)` row prints (411 at `bb44a56f`), holding the document title, the two language declarations

### L02 — the work-item grammar counts three marker vocabularies where the file enumerates two

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, `## Backlog entries — work items`, the paragraph under the `**Status:**` table
- **Tier:** 1
- **Citation:** `grep -c '^## State Markers' rules/fusion-workbench-conventions.md` → 2 (`## State Markers — issues and planning`, `## State Markers — decisions`); the same file's `## Marker globs` reads "every marker in both vocabularies (`_o_`, `_p_`, `_c_`, `_d_` on issues, plans and discussions; `_o_`, `_a_`, `_i_`, `_s_`, `_d_` on decisions)", and its opening paragraph names "the issue/planning and decision marker vocabularies". `git log -S'none of the three marker vocabularies'` → the sentence arrived at `76d833be` (2026-09-10), when the same paragraph still compared `dropped` against "the six-marker Circle vocabulary's `_c_`, `_b_`, `_s_` and `_d_`"; `8ef78ffc` (2026-09-21) cut that clause and left the count. The third vocabulary the count referred to is no longer named anywhere in the file.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Derive over correct:** the count is a cardinality beside its list, which `rules/critical-stance.md` §5 says to enumerate rather than assert. The two vocabularies are enumerated by the file's own two `## State Markers` sections, so the replacement names them by anchor and drops the numeral.
- **Fixture note:** this file is emitted to every agent, so the edit moves the byte size `hooks/lib/__tests__/fixtures/rules-emission.golden` pins in all eleven blocks and the dispatch-path rows read. Regeneration of that fixture after apply is **coder work**; the command is the one that fixture's own header names. The change is 76 bytes larger than the text it replaces, charged against the dispatch-path rows.

**Before:**
> It is none of the three marker vocabularies this project carries, and the differences are the reason.

**After:**
> It is neither of the marker vocabularies this project carries (`## State Markers — issues and planning`, `## State Markers — decisions`), and the differences are the reason.

### L03 — owner A cites two defects filed inside `260908-2018`'s container

- **Surface:** work item head field
- **File:** `260917-2253-depends-on-edges-proposed-and-confirmed.md`
- **Tier:** quoted
- **Citation:** the record's Directive: "Six further points are unsettled and named in `260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md`" and "two open defects (`260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md` and `260911-0715_*_…`) record that the field's one live value asserts an ordering where the prose it was derived from asserts a substantive conflict". `find "$WORKBENCH" -name '260909-1020_*_three-source-aspects-…'` and the same for `260911-1747_*_the-migration-…` each resolve into the container of `260908-2018-prerequisites-confirmed-once-order-computed` (`**Status:** done`).
- **Consequence group:** work-item citation edge
- **Constraint removed:** none
- **Edge:** `260917-2253-depends-on-edges-proposed-and-confirmed.md` (container) -> `260908-2018-prerequisites-confirmed-once-order-computed.md` (hop), into `**Cross-references:**`
- **Revert path:** `git checkout -- "$(git ls-files ':(top)*260917-2253-depends-on-edges-proposed-and-confirmed.md')"` — note that the record carries an uncommitted claim line (`260922-0700`) in the working tree, which this command would also discard.

**Before:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md

**After:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md, 260908-2018-prerequisites-confirmed-once-order-computed.md

### L04 — owner A's spec continues the unbuilt C3 of `260908-2018`, an inferred ordering

- **Surface:** work item head field
- **File:** `260917-2253-depends-on-edges-proposed-and-confirmed.md`
- **Tier:** inferred
- **Citation:** `260917-2256_*_spec-depends-on-edges-proposed-and-confirmed.md`, head: "**This spec does not replace `260911-1528_*_spec-prerequisites-confirmed-once-order-computed.md`.** That document specified the whole mechanism, and the half it specified as C2 is built and shipped as `bin/fusion-work-order`. What stands unbuilt is its C3, the proposal pass, which stopped at its own first stopping condition on a measurement. This spec takes C3 as its subject, inherits C3's settled decisions rather than re-opening them". `find "$WORKBENCH" -name '260911-1528_*_spec-prerequisites-…'` resolves into the container of `260908-2018-prerequisites-confirmed-once-order-computed` (`done`). The other side: that item's spec `### C3` stopped on the threshold measurement recorded in `260911-1915-candidate-prerequisite-edges.md`, and the item closed with C3 unbuilt.
- **Consequence group:** work-item ordering edge
- **Constraint removed:** none
- **Edge:** `260917-2253-depends-on-edges-proposed-and-confirmed.md` (container) -> `260908-2018-prerequisites-confirmed-once-order-computed.md` (hop), into `**Cross-references:**`
- **Why this field:** the target is `done`, so the node-set ruling (`260908-2018_*_is-a-closed-prerequisite-a-satisfied-edge-or-no-edge-and-what-is-an-archived-one.md`) forbids a `**Depends-on:**` entry; the relation goes to the field that admits any work item, and the group stays the ordering one because the reading is a direction call. It writes the same basename L03 writes; approving both is one line written, and refusing this one alone refuses the direction.
- **Revert path:** `git checkout -- "$(git ls-files ':(top)*260917-2253-depends-on-edges-proposed-and-confirmed.md')"` — same caveat as L03.

**Before:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md

**After:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md, 260908-2018-prerequisites-confirmed-once-order-computed.md

### L05 — owner A's spec names `260909-1700` as the record whose field held the wrong value

- **Surface:** work item head field
- **File:** `260917-2253-depends-on-edges-proposed-and-confirmed.md`
- **Tier:** quoted
- **Citation:** `260917-2256_*_spec-depends-on-edges-proposed-and-confirmed.md` `## The question standing in front: a relation the field cannot carry`: "The store's only live value is still the wrong one. `260909-1700-cut-fusion-to-working-minimum.md`, status `done`, carries `**Depends-on:** 260908-2018-prerequisites-confirmed-once-order-computed.md`, derived by the migration from a section whose own words are 'Conflicting, and the conflict is substantive rather than an ordering nicety'". The basename is spelled. A second sentence reaches the same target by hop: the record's head cites `260911-0715_*_a-depends-on-entry-asserts-an-ordering-…`, which `find` resolves into the `260909-1700` container. The strike that sentence describes landed at `2a9cd013` through `260918-0706`, so the sentence is history; the relation it records — this item's subject was that record's field — is not.
- **Consequence group:** work-item citation edge
- **Constraint removed:** none
- **Edge:** `260917-2253-depends-on-edges-proposed-and-confirmed.md` (container) -> `260909-1700-cut-fusion-to-working-minimum.md` (named), into `**Cross-references:**`
- **Revert path:** `git checkout -- "$(git ls-files ':(top)*260917-2253-depends-on-edges-proposed-and-confirmed.md')"` — same caveat as L03.

**Before:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md

**After:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md, 260909-1700-cut-fusion-to-working-minimum.md

### L06 — owner A's corpus names `260912-0438` twice, both times as a measurement

- **Surface:** work item head field
- **File:** `260917-2253-depends-on-edges-proposed-and-confirmed.md`
- **Tier:** quoted
- **Citation:** `260918-0824_*_the-container-hop-voided-the-only-stated-mitigation-for-flooding-the-gate.md`, the paragraph "Those 21 citations into another container name 11 distinct containers … The remaining five are work items, all terminal and therefore all admissible as `**Cross-references:**` targets: `260908-2018-…`, `260909-1700-…`, `260912-0438-human-facing-docs-leave-claude-md.md`, …" (named). And `260918-0738-curator-run.md`, head: "**Previous curator run:** `260916-1612-curator-run.md`", which `find` resolves into the `260912-0438` container (hop). **Stated plainly:** both sentences are this mechanism describing itself — one enumerates what a survey would find, the other is a ledger's previous-run pointer. Test 1 is a lookup and returns the item either way; whether "work this item touches" reaches a measurement that lists it is the user's call, and this is the entry to refuse if not.
- **Consequence group:** work-item citation edge
- **Constraint removed:** none
- **Edge:** `260917-2253-depends-on-edges-proposed-and-confirmed.md` (container) -> `260912-0438-human-facing-docs-leave-claude-md.md` (named), into `**Cross-references:**`
- **Revert path:** `git checkout -- "$(git ls-files ':(top)*260917-2253-depends-on-edges-proposed-and-confirmed.md')"` — same caveat as L03.

**Before:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md

**After:**
> **Cross-references:** 260917-1338-depends-on-kanten-automatisch-erzeugen.md, 260909-1020_*_three-source-aspects-unnamed-and-the-proposal-pass-has-no-agent-or-surface.md, 260911-1747_*_the-migration-wrote-a-conflict-relation-into-depends-on-and-nobody-confirmed-it.md, 260911-0715_*_a-depends-on-entry-asserts-an-ordering-where-the-section-it-was-derived-from-asserts-a-conflict.md, 260912-0438-human-facing-docs-leave-claude-md.md

### L07 — owner B's decision names a curator run filed inside `260909-1700`'s container

- **Surface:** work item head field
- **File:** `260922-0137-measure-the-reconciler-on-a-smaller-model.md`
- **Tier:** quoted
- **Citation:** `260827-1305_*_which-agents-run-on-a-smaller-model.md`, the paragraph "**Reconciliation 260921-2230 …** Still no `model:` key in any of the eleven prompts, and no transcript-verified candidate run exists in the tree; the only later mention is the curator survey `260911-1218-curator-run.md`, which rules nothing." `find "$WORKBENCH" -name 260911-1218-curator-run.md` resolves into the container of `260909-1700-cut-fusion-to-working-minimum` (`done`). The relation is one mention inside a reconciliation note on the record this item exists to answer; it is the thinnest of the four citation entries, and the `**Edge:**` line says so by fixing both ends off a container.
- **Consequence group:** work-item citation edge
- **Constraint removed:** none
- **Edge:** `260922-0137-measure-the-reconciler-on-a-smaller-model.md` (container) -> `260909-1700-cut-fusion-to-working-minimum.md` (hop), into `**Cross-references:**`
- **Revert path:** `git checkout -- "$(git ls-files ':(top)*260922-0137-measure-the-reconciler-on-a-smaller-model.md')"`

**Before:**
> **Cross-references:** 260827-1305_*_which-agents-run-on-a-smaller-model.md, 260827-1305-does-agent-frontmatter-model-reach-the-dispatch.md

**After:**
> **Cross-references:** 260827-1305_*_which-agents-run-on-a-smaller-model.md, 260827-1305-does-agent-frontmatter-model-reach-the-dispatch.md, 260909-1700-cut-fusion-to-working-minimum.md

### C01 — "the four hooks" in CLAUDE.md counts events, not scripts (candidate)

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, `## Layout`, the `hooks/` row: "the four hooks and what each writes"
- **Tier:** 1
- **Citation:** `hooks/hooks.json` registers four hook events (SessionStart, PreToolUse, PostToolUse, SubagentStop) that invoke six compiled scripts (session-start, session-id, identity-notice, guard, tracker, subagent-stop); `ls hooks/*.ts` → 13 entry points; `README-hooks.md` `## Files`, the section the row points to, tables all thirteen and states no count of four.
- **Consequence group:** candidate
- **Why a candidate:** the check does not falsify the sentence — four is the count of registered events, and "hook" can mean that. It is reported because a reader following the pointer meets thirteen files and no four. If the user wants it resolved, the derive-over-correct form is a pointer with no count ("the hooks `hooks/hooks.json` registers, and what each writes").
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

## 7. Outcomes

Written by the apply pass of 2026-09-22 08:45, HEAD `2c826154` (unchanged since the survey), `**Approved:** all`. One line per entry. Every before-text was re-read from disk before its write and matched the ledger; every written region was re-read after its write and compared byte for byte against the text the ledger, or the write-time computation for an edge entry, says it must carry. Working-tree edits only; nothing committed.

| Id | Outcome | Note |
|---|---|---|
| L01 | applied | `rules/context-lean-claude-md.md` lines 256–257 match the After block; 15 760 → 15 817 bytes. |
| L02 | applied | `rules/fusion-workbench-conventions.md` line 219 carries the After text once, the Before text zero times; 67 405 → 67 481 bytes. **Consequence outside this remit:** the file is emitted to every agent, so `hooks/lib/__tests__/fixtures/rules-emission.golden` now pins a stale size in all eleven blocks. Regeneration is coder work: `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, then review the diff and re-run without the flag. |
| L03 | applied | Preconditions: record resolves once; `**Status:** claimed`; exactly one `**Cross-references:**` line, as Before names. `260908-2018-prerequisites-confirmed-once-order-computed.md` appended last. Post-write: one field line, byte-equal to the computed text. |
| L04 | applied | Nothing written: the basename L04 adds is the one L03 wrote into the same line, and a basename already present is `applied` with no write. The ordering reading the entry records stands approved. |
| L05 | applied | `260909-1700-cut-fusion-to-working-minimum.md` appended last to the line as L03 left it. Post-write: one field line, byte-equal to the computed text. |
| L06 | applied | `260912-0438-human-facing-docs-leave-claude-md.md` appended last to the line as L05 left it. Post-write: one field line, byte-equal to the computed text. The line now reads `…, 260908-2018-…, 260909-1700-…, 260912-0438-…`, which is the three approved basenames applied to the survey-time line in id order. |
| L07 | applied | `260922-0137-measure-the-reconciler-on-a-smaller-model.md`: record resolves once; `**Status:** open`; one `**Cross-references:**` line. `260909-1700-cut-fusion-to-working-minimum.md` appended last. Post-write: one field line, byte-equal to the computed text. |
| C01 | not-offered | Candidate; never put at the gate. |

**Revert paths, restated for what the working tree holds now.** Owner A's record carried an uncommitted `**Status:** claimed` and `**Claim:**` line before this pass, so the ledger's `git checkout` on that file would discard those two lines together with L03–L06. The narrower revert is to remove the three appended basenames from the `**Cross-references:**` line by hand; the line as it stood at survey time is the Before block of L03. Owner B's record had no uncommitted change, so its ledger revert path stands as written. The two rule files had no uncommitted change either; `git checkout -- rules/context-lean-claude-md.md rules/fusion-workbench-conventions.md` restores both.

**`bin/fusion-work-order` after apply:** `items=2 edges=0 unresolved-edges=1 verdict=acyclic`; the one unresolved edge is R1 of `## 3c`, the confirmed `**Depends-on:**` on the terminal `260918-0706-…`, which this pass did not touch and which stays the user's call. Nothing this pass wrote enters the ordering graph, since all seven writes went to `**Cross-references:**` or to rule prose.

**Anchor:** advanced to `2c826154` after this apply, as `### The seven evidence sources` prescribes for a run whose survey read the three normative surfaces.
