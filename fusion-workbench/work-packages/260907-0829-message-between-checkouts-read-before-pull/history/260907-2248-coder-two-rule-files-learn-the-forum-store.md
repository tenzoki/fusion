# The two rule files learn the `shared/forum` store

**Status:** Complete
**Filed by:** coder, Kai Stalmann <ks@qantr.com>

Task S2, step 2 of `260907-1942_*_message-between-checkouts-read-before-pull.md`: teach
`rules/fusion-workbench-conventions.md` and `rules/workbench-path-resolution.md` the new
store, inside a 900-byte growth budget on the first file, touching nothing else.

## What was done

`rules/fusion-workbench-conventions.md`, 58 445 -> 58 762 bytes, **+317 against the 900
allowed**:

- `## fusion-workbench Layout`: a `forum/` line in the `shared/` block of the tree, between
  `backlog/` and `checkouts/`, comment "messages left for another checkout — always shared,
  see below". The tree is inside a fence, so its em-dash is not prose.
- The sentence beneath the tree gains `forum/` in its list and the clause "a forum message
  is addressed to another checkout", which is the reason the following sentence ("none of
  them is produced by executing a Directive") then generalises.
- `## Filename Patterns`: a `Forum entry` row, written to `$OUT_FORUM`, pattern
  `YYMMDD-HHMM-<checkout>-<slug>.md`, no state marker; and "forum entry" added to the
  markerless list in the citation paragraph.
- `## Path Resolution`, invariant 2: `SCAN_FORUM` joins `SCAN_CONSULT` and `SCAN_BACKLOG`
  as an apparent exception, with its reason in the parenthesis.
- One correction the edit forced: the same paragraph said `memos/` is "shared like the
  other two", a cardinality that a third shared-only scan key makes false. Rewritten as
  "shared the same way" (`rules/critical-stance.md` §5), which also costs fewer bytes.

`rules/workbench-path-resolution.md`, 14 699 -> 15 112 bytes, no bound on this file:

- `OUT_FORUM` and `SCAN_FORUM` rows in the key table, both `shared/forum`.
- The section heading and its opening sentence go from three kinds to four, as the plan
  step directs, with `OUT_FORUM` named in the enumeration beside each numeral; one sentence
  gives the new kind's reason in the same shape as the others'.
- The closing general form now reads "whoever adds the sixth such kind".
- One correction the edit forced: the retirement paragraph opened "There were four until
  2026-08-15", which collides with a heading that now also says four. Rewritten as "One
  further kind stood here until 2026-08-15", which drops the collision and leaves the
  sixth derivable from the four named plus the one that left.

No numeral was introduced anywhere except the three the plan step mandates, and each of
those stands beside its own enumeration.

## Verification

`cd hooks && npx vitest run lib/__tests__/reference-resolution-lint.test.ts lib/__tests__/workbench-citation-lint.test.ts`
— exit 1. The same command was run **before** the edit and exited 0, so both failures are
attributable rather than pre-existing:

1. `skills/archive/SKILL.md` cites the renamed heading by its old text, and the gate
   resolves an anchor by prefix match. Caused by this step, repairable only in a file this
   dispatch was told not to touch. Filed as
   `260907-2248_*_renaming-the-shared-kinds-heading-dangles-the-archive-skills-anchor-citation.md`.
2. The pinned `paths` count moved 1646 -> 1658. Not this step's: the diff of these two
   files adds no plugin-shaped path (every such token in it appears on both sides of the
   diff), and the twelve are the citations in the new `bin/fusion-forum` header, which
   appeared in the tree between the baseline run and this one. Re-approving that pin
   belongs to the work that added the file.

`bin/fusion-prose-metric` on both files, exit 0: the conventions file is unchanged at 27
em-dashes in 7 907 prose words, and the resolution file unchanged at 37, its rate falling
16.7 -> 16.3 per 1000 as the word count rose. Both files were already over the ceiling
before this edit; neither was made worse. The two key-table rows were first written with
the surrounding rows' em-dash punctuation and rewritten with a colon and a comma once the
metric showed them raising the count.

The rules-emission golden is expected to mismatch and is regenerated by step 12.
