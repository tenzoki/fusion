# Shaper session: capture the C4 follow-on as an anticipated Circle

**Date:** 2026-08-26 16:13
**Agent:** shaper (anticipated-circle mode)
**Status:** Complete
**Invoked by:** the user, through `/fusion:direct`
**Domain:** code

## The draft

A draft Directive to work through the open defects from the C4 environment: nineteen records, fourteen open inside the closed Circle `260825-2023-presence-travels-monitor-filters-own-checkout` and five filed into `shared/issues/` after it closed. The draft proposed three capacities in a stated order, with the hook-test cut first.

## What the clarification changed

Three rounds ran before this dispatch, and their answers arrived with it. Two of them changed the shape of the Circle rather than confirming it.

**The first two capacities were reversed.** The draft put the surface cut first, because five findings cannot be built without hook-test lines. The user chose instead to answer `260826-1252_*_how-does-this-project-keep-a-cardinality-stated-in-prose-true-when-seven-passes-could-not.md` first, then to measure once and cut once for that answer and the nineteen findings together. Three of the four options in that record demand shipped text of their own, so a cut priced before the answer exists is priced wrong.

**The cut widened from one surface to three.** All three growth-bounded surfaces are measured and cut in one pass, under one user gate. `skills/` has no ranked reserve of candidates, so finding them became work inside that capacity rather than a lookup.

The remaining answers held the draft's shape: all four options of the cardinality question stay in play, the nine older open defects in the shared store stay outside this Circle, and scope is the fixed list of nineteen plus whatever this work finds in its own changes, with the reviewer deciding per finding.

## What was measured rather than carried over

**The three bounded surfaces, at 260826-1613.** Hook tests 20 349 of 20 375, `agents/` 417 796 of 417 843, `skills/` 240 423 of 240 439. These reproduce the figures the cardinality record's Constraints section carries.

**The tenth instance of the cardinality pattern.** `portfolio.md` line 225 attributes an estimate of 125 to 185 lines to the C4 closure note. The closure note carries no such estimate. The span was invented and given a real source, and it reached the draft this Circle was shaped from as a requirement. The user chose to admit it as the tenth instance rather than discard it, and the Circle record carries the operational consequence: the line requirement is unmeasured, it is measured inside capacity 2, and it must not be re-derived from a regenerated portfolio.

**The citation gate.** `npx vitest run lib/__tests__/workbench-citation-lint.test.ts` passes in the working tree, 10 of 10, so `260826-1331_*_npm-test-is-red-at-head-on-a-one-word-slug-drift-in-the-record-that-reports-the-eighth-count.md` closes with a resolution line rather than a fix. The Circle record states that this is a working-tree reading and should be re-taken at activation.

## Result

Circle created at `260826-1613-cardinality-answered-cut-once-nineteen-cleared`, record `_a_circle.md`, anticipated, unclaimed, with the six artifact subdirectories. The record's own citation set was verified against the citation gate and its prose carries no em-dash.

No spec was written: in this mode the Circle record is the artifact. No decision record was filed, because the user deferred nothing across the three rounds. No backlog entry was promoted, because the draft came as raw text rather than as a stored entry. No existing Circle was modified.

Activation is the user's separate step, through `/fusion:next`.

## Voice profiles

Both emitted profiles were read: `fusion-workbench/stilwerk/chat-voice-de.yaml` for the chat report and `fusion-workbench/stilwerk/default-voice-en.yaml` for the record's prose. No fallback line was printed on standard error.
