# Curator run — 260817-1925

---
**Status:** Complete. The survey pass ran at 260817-1925 and the apply pass at 260817-1940, as a second dispatch carrying `**Mode:** apply`, this file as `**Ledger:**` and `**Approved:** all`. All three entries applied; the four candidates were not applied, by rule. Outcomes and measured sizes are at `## 7`.
**Mode:** survey, then apply (`**Approved:** all`)
**Date:** 2026-08-17
**HEAD read:** `e59dea21a12f8f75436444857dcadf42efa65dda`
**Previous curator run:** `circles/260816-1741-guard-becomes-observation-only/history/260817-0845-curator-run.md`, 2026-08-17, HEAD `c65e1cf4678bb85ae359f132a665514b715a37a3`. The run before that was `shared/history/260816-1251-curator-run.md`, 2026-08-16.
**Dispatched by:** the user, top-level, with `**Mode:** survey` and nothing else.
**Domain:** code

**What changed in the interval.** Eighteen commits, `c65e1cf`..`e59dea2`. They released v10.0.0 and v10.0.1, closed the guard Circle at Bounded Closure, ran two reconciliations, and ran a scoped archive pass that moved 291 files into `archive/260817-1907-safe-cleanup-scoped/`. **Neither `CLAUDE.md` nor any file under `rules/` was touched in the interval** — `git log c65e1cf..HEAD -- CLAUDE.md rules/` returns one commit, `5763550`, which is the previous run's own apply. So the two text surfaces stand exactly as that run left them, and every finding below is a claim that was already false then and that its selector did not reach.

**Why this run's selector is different from the last one's.** The previous run compared the guard-removal vocabulary exhaustively and stated plainly that a stale statement outside that vocabulary would not have been found. This run takes a disjoint selector — who may do what — so the two together cover more than either repeated twice would.

---

## 1. Evidence-source counts

| # | Source | Files read | Note |
|---|---|---|---|
| 1 | Circle records (`$SCAN_CIRCLES` = `circles/`) | 11 of 11 listed, 0 opened | Every `*_circle.md` filename and marker read (`ls circles/*/*_circle.md`): ten terminal `_c_`/`_s_`, one `_b_`. **No Circle record body was opened.** No entry below rests on a Directive, a Grounding snapshot, a Turn log or a Closure note. Stated rather than glossed: this is the largest unread source in the run after the histories. |
| 2 | Decision records | 90 of 90 listed, 6 opened in full | `$SCAN_DECISIONS` resolves to `shared/decisions/` alone (no Circle is active), 47 records. The Circle stores hold a further 43, read as evidence through `$SCAN_CIRCLES` rather than as an editable surface. All 90 filenames and `**Status:**` fields were read by script. Six opened in full: `260801-1020_i_may-any-fusion-writer-touch-rules`, `260806-1152_a_stash-manifest-…`, `260815-2312_a_should-the-circle-records-status-field-exist-…`, `260816-1707_o_to-whom-is-the-new-workbench-tracking-rule-emitted…`, `260817-1613_o_does-a-plan-stated-precondition-get-any-mechanism…`, and `circles/260805-2005-…/decisions/260806-0015_i_zitierform-fuer-workbench-records`. |
| 3 | Session histories | 1 of 487 opened | Only the previous curator run, opened in full. 486 were not opened. **This is the largest gap in the run.** |
| 4 | Git history | full log per file | `git log --follow --oneline` over `CLAUDE.md` and all 12 files in `rules/`, plus `git log --oneline c65e1cf..HEAD` and `git diff --stat` over the interval. `git blame` was not run: no entry below rests on a single paragraph's authorship. |
| 5 | Reviews and analyses | 0 of 72 reviews, 0 of 22 analyses opened | Not opened. No entry below rests on a review or an analysis. |
| 6 | `orchestrator-events.jsonl` | 1 file, not read | 1 712 lines. Corroborating-only by rule; nothing here needed corroboration a stronger source did not already carry. |
| 7 | Archive store (`$WORKBENCH/archive`) | 291 files listed, 1 opened | **No longer empty** — it was on the previous run. `archive/260817-1907-safe-cleanup-scoped/MANIFEST.md` was read in full; the 290 archived artifacts beside it were listed but not opened. Read directly, because no `SCAN_*` key reaches the store — which is itself the subject of open defect `shared/issues/260801-1020_o_scan-keys-never-reach-the-archive-store.md`. |
| 8 | `**Provenance:**` headers | 12 of 12 | Every file in `rules/` carries one within its first ten lines (`head -10 rules/*.md | grep -c '^\*\*Provenance:\*\*'` → 12). Four cite Circles (`260718-1924-v5x-overhaul`, `260801-1244-guard-rules-write` ×3), all `_c_` rather than `_s_`, so no header resolves to a superseded record and no Tier 2 retirement follows from this source. |

**Open defect corpus, used as a cross-check and not as a fourth surface:** 150 open defect records across both stores. Two hits materially changed this run's output and are recorded at `## 6` and `## 8`.

**Unreadable surfaces:** none.

**Absent surfaces:** `.claude/rules/` does not exist in this project (`ls .claude/rules` → No such file or directory). There are therefore no project-wide rule files, and the project-owned rule surface is `./rules/` alone, 12 files. Note the scope call this repository forces: cwd **is** the fusion plugin's own source tree, so `./rules/` here is the project's own rule directory and in remit; the install copy at `$FUSION_PLUGIN_ROOT` (`/Users/k1/.fusion`) is out of remit and was not touched.

**The gates were run read-only, and all of them are green.** `cd hooks && npx vitest run` → **653 tests, 35 files, all passing**. In particular `reference-resolution-lint.test.ts` passes 34 of 34, so no citation on any shipped surface dangles, including after today's archive pass. That is why no entry below is a stale-reference finding: the mechanical class is already covered, and this run went looking where no gate reads.

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after (if all approved) | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 61 708 | 137 | ~62 000 | 137 |
| Project rule files (`./rules/`) | 12 | 149 780 | 1 754 | ~150 700 | 1 761 |
| Decision records (`$SCAN_DECISIONS` = `shared/decisions/`) | 47 | 354 960 | 4 661 | 354 960 | 4 661 |

L03 replaces one whole line in place, so `CLAUDE.md`'s line count does not move. L01 and L02 replace multi-line paragraphs and the replacements are longer, so the rule surface gains roughly seven lines. **No file is deleted and no decision record is modified.** The "after" figures are estimates from the drafted replacement text and are marked as such; the apply pass measures them.

## 3. Blast radius

**Verdict: far below the threshold on both readings. No separate scale confirmation is due.**

- **Bytes deleted outright: zero, on all three surfaces.** Every entry is a replacement, and all three replacements grow.
- **Bytes rewritten in place:** 453 of `CLAUDE.md`'s 61 708 (0.73 %), and 1 157 of the 149 780 across `rules/` (0.77 %), concentrated in two files — 804 in `circle-records.md`, 353 in `fusion-workbench-conventions.md`.

Under the strict reading, where a rewritten byte counts as a deletion plus an insertion, the largest single-surface figure is 0.77 %. The 20 % threshold is not approached.

## 4. Comparison counts

**Selection rule, one rule for all six pairs.** A statement enters the comparison when it (a) names one of the 15 agents or one of the 12 skills in `/fusion:<name>` form, **and** (b) carries a modal of obligation, prohibition, ownership or exhaustiveness — `must`, `never`, `only`, `may not`, `cannot`, `owns`, `forbidden`, `no agent`, `nobody`, `always`, `refuse*`, `halt*`. A pair enters the comparison when both members name **the same** agent or skill; two statements about different agents cannot contradict each other by this route, and the shared-subject clause is what makes the cross product readable rather than merely countable. Everything outside that rule was not compared, and this run makes no claim about it.

**The corpus counts are derived, not stated.** `ls -1 agents/*.md | wc -l` → 15; `ls -1 skills/ | wc -l` → 12; `ls -1 rules/*.md | wc -l` → 12; `awk 'END{print NR}' CLAUDE.md` → 137; `ls -1 fusion-workbench/shared/decisions/*.md | wc -l` → 47. The selection itself was executed as a script over `CLAUDE.md` and all 12 rule files and produced **64 statements**, 32 on `CLAUDE.md` and 32 across `rules/`, grouped into 27 subject groups. **All 64 were read.**

| # | Pair | Pairs the rule produced | Read | Findings |
|---|---|---|---|---|
| 1 | decision × decision | 15 (C(6,2) over the 6 records opened in full) | 15 | 0. No live record overturns another. Two records that *look* like supersession candidates are already reconciled and annotated — see `## 8`. |
| 2 | decision × rule file | 6 (6 opened records × the rule files they name) | 6 | 1 not offered — `260815-2312_a_` against `rules/circle-records.md:70`. Tier 2 evidence exists and the entry is withheld; the reason is candidate **C3**. |
| 3 | decision × `CLAUDE.md` | 0 (no opened record names a `CLAUDE.md` line) | 0 | 0. |
| 4 | rule file × rule file | 48 across files, plus 25 within one file | 73 | 0 direct contradiction. One pair was examined closely and cleared: `rules/design-diagrams.md:9` rejects ASCII for structural design while `rules/user-facing-output.md:41-56` prefers an ASCII sketch, and both bind `planner`, `analyst`, `taskplanner` and `shaper`. They do **not** contradict: the first scopes Mermaid to documents and ASCII to throwaway sketches that never enter a deliverable, the second scopes ASCII to chat and explicitly reserves Mermaid for files. The two surfaces are already reconciled. |
| 5 | rule file × `CLAUDE.md` | 250 | 250 | 1 — the shared falsified premise behind L01/L02/L03. It is not a contradiction *between* the surfaces: all three are false in the same direction, which is coherence about a tree that moved. |
| 6 | `CLAUDE.md` × itself | 202 | 202 | 0. |

**The verdict covers the pairs this rule reached, not the corpora.** 84 of 90 decision records were never opened. 486 of 487 session histories, 72 reviews and 22 analyses were never opened. 98 of `CLAUDE.md`'s 137 lines carry no agent or skill name and entered no pair. The archive's 290 artifacts were listed, not read. A contradiction outside the who-may-do-what vocabulary would not have been found by this run, and one inside a Circle record body would not have been found at all.

**One finding sits outside the six pairs and is reported rather than proposed:** the decision-record `**Status:**` field against its own filename marker, 35 of 90 records disagreeing. It is not a ledger entry, and `## 6` says why.

## 5. Pre-edit content of decision records this run would modify

**None.** This run proposes no change to any decision record. The obligation is unconditional and is discharged by the count: zero records are in the ledger, so there is no pre-edit content to preserve.

## 6. The ledger

3 entries and 4 candidates. Ids are stable and per-entry approval is by id.

| Group | Ids | Count |
|---|---|---|
| Constraint removal | — | 0 |
| Tier 3 | — | 0 |
| Tier 2 | — | 0 |
| Tier 1 | L01, L02, L03 | 3 |
| Consolidation | — | 0 |
| **Candidate (not on offer, never applied)** | C1–C4 | **4** |

**All three entries are one finding on three surfaces, and they should be approved together or not at all.** Each of them states, in its own words, that `bin/fusion-rules` emits `rules/circle-records.md` to a *derived* audience, derived from the property "this agent's prompt names a Circle-scoped resolver key". That property no longer picks out those three agents. Approving one or two leaves the surfaces disagreeing with each other where today they agree, which is worse than leaving all three.

### The measurement all three rest on

```
$ for a in analyst bugfixer coder coderev consultant curator editor ontocoder ontorev \
           orchestrator planner playmaker reconciler shaper taskplanner; do
    k=$(./bin/fusion-paths $a | grep -E '^(OUT_CIRCLE|SCAN_CIRCLES|PORTFOLIO)=' | cut -d= -f1 | tr '\n' ' ')
    [ -n "$k" ] && printf "%-12s %s\n" "$a" "$k"
  done
curator      SCAN_CIRCLES
orchestrator SCAN_CIRCLES PORTFOLIO
playmaker    OUT_CIRCLE SCAN_CIRCLES PORTFOLIO
shaper       OUT_CIRCLE SCAN_CIRCLES
```

**Four agents name a Circle-scoped key, not three.** Corroborated from the other direction:

```
$ grep -lE '\$SCAN_CIRCLES\b' agents/*.md
agents/curator.md  agents/shaper.md  agents/playmaker.md  agents/orchestrator.md
```

And the emission is unchanged at three:

```
$ for a in <all 15>; do ./bin/fusion-rules $a | grep -c circle-records; done
orchestrator 1   playmaker 1   shaper 1   (curator 0, every other agent 0)
```

`agents/curator.md` names `$SCAN_CIRCLES` because Circle records are evidence source 1 of its own evidence pass. It reads Circle records and never transitions or ranks one, so the emission list of three is **right** and only its stated justification is wrong. Nothing below proposes a change to which rule files load for which agent — that is `bin/fusion-rules`'s question and exclusion 4 of this agent's remit.

`inference:` that the emission list is the correct side and the derivation story is the wrong side. No record states it in those words. The reasoning is that `circle-records.md` exists to govern Circle *transitions*, the curator performs none, and emitting a transition vocabulary to an agent that cannot transition would add bytes to a dispatch for no obligation. If the user reads it the other way, the change is a `bin/fusion-rules` edit and this run does not make it.

---

### L01 — `rules/circle-records.md` claims exactly three agents name a Circle-scoped key, and four do

- **Surface:** project rule file
- **File:** `rules/circle-records.md`, lines 9–18 (the whole paragraph; the apply pass matches the full block, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** the two commands under *The measurement all three rest on* above, with their output. `bin/fusion-paths curator` emits `SCAN_CIRCLES`; `bin/fusion-rules curator` emits no `circle-records.md`. `curator` was added in v8.2.0, after this paragraph was written.
- **Consequence group:** tier-1
- **Constraint removed:** none. The audience statement, the pointer to the conventions file, and the enumeration of the three citing skills are all preserved verbatim in substance; only the false derivation is replaced.
- **Revert path:** `git checkout -- rules/circle-records.md`
- **Note:** Derive over correct — the replacement refuses to state the key-naming set and names the command that produces it, on the reasoning `CLAUDE.md:62` already applies to its always-on byte floor and the `templates/` and `docs/` rows apply to their inventories. Approve with L02 and L03. Triggers the fixture consequence in `## 8` item 1.

**Before:**

> Its audience is bounded by a mechanism rather than by a guess: `bin/fusion-paths` derives
> each consumer's key set by reading that consumer's own prompt, and exactly three agents
> name a Circle-scoped key — `orchestrator` (`$SCAN_CIRCLES`, `$PORTFOLIO`), `playmaker`
> (`$OUT_CIRCLE`, `$SCAN_CIRCLES`, `$PORTFOLIO`) and `shaper` (`$OUT_CIRCLE`,
> `$SCAN_CIRCLES`). Those three are who `bin/fusion-rules` emits this file to. The other
> agents work inside a Circle without ever transitioning one, and reach this file through
> the pointer in `rules/fusion-workbench-conventions.md`. Skills reach rule text by direct
> citation, never through `bin/fusion-rules`, which serves agents only and exits 2 on any
> other name; the skills that cite this file today are `/fusion:next`, `/fusion:direct` and
> `/fusion:migrate`.

**After:**

> `bin/fusion-rules` emits this file to three agents — `orchestrator`, `playmaker` and
> `shaper` — the three that transition or rank a Circle. That audience used to be *derived*
> rather than authored: the same three were once exactly the agents whose prompts named a
> Circle-scoped resolver key, so the emission list restated a property of the prompts.
> **The derivation no longer holds.** `curator` names `$SCAN_CIRCLES` as well, to read
> Circle records as evidence, and it neither transitions nor ranks a Circle — so naming a
> Circle-scoped key stopped picking out the agents that move one. The key-naming set is not
> restated here, because it moves with every prompt edit: measure it with a grep for
> `$OUT_CIRCLE`, `$SCAN_CIRCLES` and `$PORTFOLIO` over `agents/*.md`. Whether `curator`
> should join the emission list is `bin/fusion-rules`'s question and not this file's. The
> other agents work inside a Circle without ever transitioning one, and reach this file
> through the pointer in `rules/fusion-workbench-conventions.md`. Skills reach rule text by
> direct citation, never through `bin/fusion-rules`, which serves agents only and exits 2 on
> any other name; the skills that cite this file today are `/fusion:next`, `/fusion:direct`
> and `/fusion:migrate`.

### L02 — the conventions file's pointer repeats the same broken derivation

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, lines 376–379 (the whole paragraph)
- **Tier:** 1
- **Citation:** the same two commands. This paragraph is the pointer left behind when the Circle vocabulary was partitioned out; it inherited the derivation sentence and the derivation broke afterwards.
- **Consequence group:** tier-1
- **Constraint removed:** none.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Note:** This file is in the **always-on** set, so its size counts against the hard growth bound. Measured, not assumed: the always-on core stands at 91 021 bytes against a `RULE_BASELINE` sum of 86 573, so growth is 4 448 of the 12 000 head-room and this entry adds roughly 250. The bound is not at risk. Approve with L01 and L03.

**Before:**

> `bin/fusion-rules` emits that file to `orchestrator`, `playmaker` and `shaper` — the three
> agents whose prompts name a Circle-scoped resolver key, and therefore the three that
> transition or rank a Circle. If you are not one of them you work inside a Circle without
> ever changing its state, and `## fusion-workbench Layout` above is the part you need.

**After:**

> `bin/fusion-rules` emits that file to `orchestrator`, `playmaker` and `shaper` — the three
> agents that transition or rank a Circle. Those three were once exactly the agents whose
> prompts named a Circle-scoped resolver key, and the key no longer picks them out:
> `curator` names `$SCAN_CIRCLES` to read Circle records as evidence and moves no Circle.
> The audience is authored in `bin/fusion-rules` rather than derived, and
> `rules/circle-records.md` carries the reason. If you are not one of the three you work
> inside a Circle without ever changing its state, and `## fusion-workbench Layout` above is
> the part you need.

### L03 — the `CLAUDE.md` Layout row calls the audience derived and names it "the only three"

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 46 (whole line)
- **Tier:** 1
- **Citation:** the same two commands. The row makes the strongest form of the claim of the three: "exactly the three agents whose prompts name a Circle-scoped `fusion-paths` key, and therefore the only three that can transition or rank a Circle." The premise is false at four, and the inference from it was never valid — `curator` names the key and can transition nothing.
- **Consequence group:** tier-1
- **Constraint removed:** none.
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The conclusion is kept, because it is true on its own evidence: three agents transition or rank a Circle. Only the derivation that reached it is replaced. Approve with L01 and L02.

**Before:**

> | `rules/circle-records.md` | The **single authoring home** for the Circle state vocabulary (`_a_/_t_/_c_/_b_/_s_/_d_`), its worked transitions, the Circle record template and the `portfolio.md` template. Emitted to `orchestrator`, `playmaker`, `shaper` — a *derived* audience: exactly the three agents whose prompts name a Circle-scoped `fusion-paths` key, and therefore the only three that can transition or rank a Circle. Added v5.x (same step). |

**After:**

> | `rules/circle-records.md` | The **single authoring home** for the Circle state vocabulary (`_a_/_t_/_c_/_b_/_s_/_d_`), its worked transitions, the Circle record template and the `portfolio.md` template. Emitted to `orchestrator`, `playmaker`, `shaper` — the three agents that transition or rank a Circle. That audience was once *derived*, from naming a Circle-scoped `fusion-paths` key, and the derivation broke when `curator` (v8.2.0) began naming `$SCAN_CIRCLES` to read Circle records as evidence without ever moving one. The list is authored in `bin/fusion-rules` now; `rules/circle-records.md` carries the reason. Added v5.x (same step). |

---

### C1 — `rules/workbench-path-resolution.md:72` names two consumers of `SCAN_CIRCLES` and there are seven

- **Surface:** project rule file
- **File:** `rules/workbench-path-resolution.md`, line 72 (the `SCAN_CIRCLES` row of the key table)
- **Tier:** 1
- **Citation:** the row's Notes column reads `Portfolio-wide scans (playmaker, /fusion:next).` `grep -lE '\$SCAN_CIRCLES\b' agents/*.md skills/*/SKILL.md` returns seven consumers: `agents/curator.md`, `agents/shaper.md`, `agents/playmaker.md`, `agents/orchestrator.md`, `skills/archive/SKILL.md`, `skills/next/SKILL.md`, `skills/setup/SKILL.md`.
- **Why it is a candidate:** the parenthetical carries no word of exhaustiveness — no "only", no "exactly" — so reading it as a complete enumeration rather than as two examples is a reading of prose, not a check. The file *is* the definition of what each key means, which argues the other way, and that tension is exactly what a candidate is for. Raise it as an entry on the next run only if the user says the Notes column is meant to enumerate.

### C2 — `rules/context-lean-claude-md.md:11` describes itself as input to work that has since closed

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`, line 11
- **Tier:** 3
- **Citation:** the line reads "This is the input to the reference conversion (a later Circle-B step) and to the docs pass (Circle E)." Circle B and Circle E are tracks of `circles/260718-1924-v5x-overhaul`, whose record is `_c_circle.md`. A live rule file points forward at steps of a Circle that closed.
- **Why it is a candidate:** Tier 3 requires naming **when** the practice stopped and **what replaced it**, and this run can name neither without reading the v5x Circle's 19 session histories, which it did not open. Downgraded per the tier rule rather than guessed at.

### C3 — `rules/circle-records.md:70` still carries the `**Status:**` field an answered decision says to drop

- **Surface:** project rule file
- **File:** `rules/circle-records.md`, line 70 (the Circle record template)
- **Tier:** 2
- **Citation:** `shared/decisions/260815-2312_a_should-the-circle-records-status-field-exist-at-all-now-that-both-transitions-maintain-it.md`, `Answered:` line — "option 1: drop the field, filename marker becomes the only source; timed to the next Circle that touches Circle records for another reason. User answered inline 2026-08-16." The overturned text is the template line `**Status:** <anticipated | active | closed | bounded | superseded | deferred>`.
- **Why it is a candidate, and not an entry:** the same answer that overturns the line also **times its removal**, to a Circle that has not run. Applying it now would realise a decision the user deliberately deferred. It is also not a text edit: the answer requires the template, both transition sites (`skills/next/SKILL.md` Step 6.2, `agents/orchestrator.md` `## Circle head fields`) and two mechanical readers to move together, and every one of those is outside this agent's remit. The reconciler recorded the same reading at HEAD `2552586`. Reported, not offered.

### C4 — an open decision record's premise counts two rule files emitted to no agent, and there are four

- **Surface:** decision record
- **File:** `shared/decisions/260816-1707_o_to-whom-is-the-new-workbench-tracking-rule-emitted-when-its-consumers-are-a-human-and-a-skill.md`, the `**Cross-references:**` line and option 1's Cons
- **Tier:** 1
- **Citation:** the record calls `rules/workbench-path-resolution.md` and `rules/rule-file-provenance.md` "the two files emitted to no agent by design" and option 1 "a third such file". Running `bin/fusion-rules` for all 15 agents and collecting the union shows **four** of the 12 rule files are emitted to nobody: those two plus `rules/context-manifest.md` and `rules/context-lean-claude-md.md`.
- **Why it is a candidate:** the count is verified, but whether the `context-*` pair belongs to the record's class is a reading, not a check. The class the record works with may be narrower — a file whose own text declares it unemitted and which the conventions file points at — and the `context-*` pair satisfies neither half of that narrower reading. Editing a user's live open question over a contested reading of a side remark is not proportionate, and the record's substance, to whom the new file would be emitted, does not turn on whether the precedent count is two or four. Reported so the user holds the number when they answer.

## 7. Outcomes

**Apply pass, 260817-1940.** Dispatched with `**Mode:** apply`, this file as `**Ledger:**`, `**Approved:** all`. Approval of `all` covers the three ledger entries and nothing else: **C1–C4 are candidates and were not applied**, per the rule that a candidate is never offered and never applied.

**Pre-apply staleness check.** HEAD at the apply pass is `e59dea21a12f8f75436444857dcadf42efa65dda`, the same commit the survey read, and `git status --porcelain -- CLAUDE.md rules/` was empty, so neither text surface had moved. Each entry's before-text was then re-read from disk independently and matched the ledger byte for byte; the replacement was executed as a single exact-string substitution per file, asserting exactly one occurrence, so a drifted byte would have failed the entry rather than patching a near-match.

| Id | Outcome | File |
|---|---|---|
| L01 | **applied** | `rules/circle-records.md` |
| L02 | **applied** | `rules/fusion-workbench-conventions.md` |
| L03 | **applied** | `CLAUDE.md` |
| C1 | not applied — candidate | `rules/workbench-path-resolution.md` |
| C2 | not applied — candidate | `rules/context-lean-claude-md.md` |
| C3 | not applied — candidate | `rules/circle-records.md` |
| C4 | not applied — candidate | `shared/decisions/260816-1707_o_…` |

No entry was `stale`, `skipped` or `failed`. No write was denied.

**Measured surface sizes, superseding the estimates at `## 2`.** The estimates were within 200 bytes on both surfaces.

| Surface | Bytes before | Bytes after | Delta | Lines before | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 61 708 | 61 904 | +196 | 137 | 137 |
| Project rule files (`./rules/`, 12 files) | 149 780 | 150 497 | +717 | 1 754 | 1 763 |
| — of which `rules/circle-records.md` | 11 650 | 12 100 | +450 | | |
| — of which `rules/fusion-workbench-conventions.md` | 55 835 | 56 102 | +267 | | |
| Decision records (`shared/decisions/`, 47 files) | 354 960 | 354 960 | 0 | 4 661 | 4 661 |

**Removals by tier: zero, on every surface.** No constraint was removed by any entry, no file was deleted, and no decision record was touched. `git diff --stat` over the three files reads `23 insertions(+), 14 deletions(-)`, and every one of those 14 deleted lines is a replaced line inside one of the three matched blocks.

**Searchable line for a later grep, per the wrong-prune mitigation:** *no constraint was removed in run 260817-1925; all three entries replaced a false derivation of `rules/circle-records.md`'s emission audience and preserved the audience statement itself.*

**Revert path:** `git checkout -- CLAUDE.md rules/circle-records.md rules/fusion-workbench-conventions.md`. All three files are git-tracked, so the path is real for every entry.

### Two gates went red as a consequence, both foreseen at `## 8`, both coder work

Run read-only after the apply, no fixture and no baseline was touched by this agent (`hooks/` is exclusion 6):

1. **`rules-emission-golden.test.ts` fails**, exactly as `## 8` item 1 predicted: `fusion-workbench-conventions.md 55835 → 56102` in every agent's block, and the per-agent totals with it. Regenerate with `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, then read the diff. **Executor: `coder`.**

2. **`reference-resolution-lint.test.ts` fails on its count pin** — `paths: 1120 → 1124`, `anchors` and `records` unchanged. **`## 8` item 5 predicted this would hold, and it was wrong.** The reasoning there was that the replacement adds no path token the current text does not already carry, which is true of the *set* of tokens and irrelevant to the pin, which counts *occurrences*: the replacement text repeats `bin/fusion-rules`, `agents/*.md` and the two rule-file paths more often than the text it replaced. All 34 resolution assertions still pass — nothing dangles, only the count moved. Re-approve `BASELINE.paths` to 1124 in that test file and commit it with this edit. **Executor: `coder`.**

3. `surface-growth-bound.test.ts` is **green**, as predicted: the always-on set grew by 267 bytes against 7 552 of head-room, and `agents/`, `skills/` and the hook tests were not touched.

The other four lint gates — `derivable-enumerations-lint`, `path-literal-lint`, `provenance-header-lint`, and the non-count half of `reference-resolution-lint` — are green.

**Nothing was committed.** Working-tree edits only.

## 8. Work this agent may not do, and who owns each

Five items. None is a ledger entry, and no new record was filed for any of them, because each already has an owner or a filed record.

1. **`hooks/lib/__tests__/fixtures/rules-emission.golden` goes red if L01 or L02 is applied.** The golden pins the byte size of every emitted rule file per agent; `circle-records.md` (11 650) and `fusion-workbench-conventions.md` (55 835) both move. This is the fixture case rather than the out-of-remit case: the edits are in remit and gated, and only the consequence is somebody else's. Regenerate with `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, then review the diff. **Executor: `coder`. This agent does not run it.** The growth *bound* is not at risk, and that is measured rather than assumed: the always-on core is 91 021 bytes against a `RULE_BASELINE` sum of 86 573, leaving 7 552 of the 12 000 head-room, and L02 spends about 250 of it. `circle-records.md` is role-specific and reaches the report rather than the hard bound.

2. **The decision record's `**Status:**` field disagrees with its own filename marker in 35 of 90 live records** — 16 of the 47 in `shared/decisions/`, 19 of the 43 in Circle stores, measured this run by comparing each filename marker against the head field under the template's own vocabulary. Two currently-binding statements in one rule file produce it: `rules/fusion-workbench-conventions.md` `## State Markers — decisions` makes the marker the state, and `## Decision Record Template` mandates a `**Status:**` field, while `## Inline State Tracking` → *Decision files* tells a writer to append the annotation and rename the marker and says **nothing** about the field. Nothing says which governs, and nothing assigns the field an owner.

   **This is the precedence-undecided contradiction this agent files a decision record for, and it did not file one, because one is already filed as a defect and it forbids exactly the repair.** `shared/issues/260811-2146_o_half-the-decision-records-carry-a-status-that-disagrees-with-their-marker-and-twelve-keep-the-unfilled-template-stub.md` states the question ("owned" or "dropped"), names the two coherent answers, and says in terms: *"This is a decision to put to the user, not a repair to start"* and *"Do not hand-correct the 34 without answering the question first — the disagreements are the measurement."* Its reconciliation of 260817-1836 confirms no decision answers it and that the adjacent `260815-2312` is scoped to the *Circle* record's field, a different question. **A second record would duplicate it and a repair would destroy its evidence.** What this run adds is one fresh number at this HEAD, recorded here for whoever answers: 35 of 90, against the 34 of 67 the defect measured on 2026-08-11 and the 18 of 56 the reconciler measured six hours ago before the archive pass moved nine terminal records out of the shared store. **Executor: the user, at a decision gate; then `coder`.**

3. **Both open decision records are genuinely open and neither is stale.** `260816-1707` (to whom the workbench-tracking rule is emitted) and `260817-1613` (whether a plan-stated precondition gets a mechanism) were each reconciled at 260817-1836 and each verified unanswered. This run makes no marker claim about either — advancing a decision marker on ground-truth verification is `agents/reconciler.md`'s, exclusion 1.

4. **Nothing in this run changes the size of `agents/`, `skills/` or the hook tests**, so `surface-growth-bound.test.ts` is untouched by it. It is green at this HEAD, as is the whole suite.

5. **`reference-resolution-lint.test.ts`'s count pin may need re-approving after the apply.** The replacement text adds no new path token that the current text does not already carry — `agents/*.md`, `bin/fusion-rules`, `rules/circle-records.md` and `rules/fusion-workbench-conventions.md` all appear in the before-text of some entry — so the `paths` count is expected to hold. If it moves, the constant is hand-edited and re-approving it is **coder work** in the same commit. This agent does not edit it (`hooks/` is exclusion 6).

## 9. Confidence, stated per the critical-stance rule

- **Verified**, with the command and its output in the entry or in `## 6`: that four agents name a Circle-scoped resolver key and three receive `circle-records.md`; that 35 of 90 decision records carry a `**Status:**` disagreeing with their marker; that the hook suite is 653 green including the citation lint; that `CLAUDE.md` and `rules/` were untouched in the interval; that four rule files are emitted to no agent; that the always-on growth head-room is 7 552 bytes; that the workbench `.gitignore` split matches `rules/fusion-workbench-conventions.md:76-77` entry for entry, and that today's rolled guard log is tracked under `archive/`, as `:81` claims.
- **Inference**, and labelled: that the emission list of three is the correct side of L01–L03 and the derivation story the wrong side. It rests on the curator transitioning no Circle, not on a record saying so.
- **Inference**, and labelled: that `rules/design-diagrams.md:9` and `rules/user-facing-output.md:41-56` do not contradict each other. Both were read in full and each scopes itself to a different surface, but "chat is not a deliverable" is a reading of two prose passages and no record states it.
- **No speculation is carried in any entry.** C2 is the one Tier 3 shape in the run and it is downgraded precisely because the speculation it would need is not available.

## 10. Where the ledger's own reach stops

84 of 90 decision records were not opened. 486 of 487 session histories were not opened. 72 reviews, 22 analyses and all 11 Circle record bodies were not opened. 290 of the archive's 291 files were listed and not read. 98 of `CLAUDE.md`'s 137 lines entered no pair. If a stale normative statement in this project sits outside the who-may-do-what vocabulary and outside the previous run's guard-removal vocabulary, neither run looked for it, and this one does not claim it is not there.
