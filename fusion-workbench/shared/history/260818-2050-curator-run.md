# Curator run — 260818-2050-curator-run.md

---
**Status:** Complete. Both passes have run. The survey pass produced the ledger below and returned its gate question to the caller; the user approved `all`, and the apply pass of 260818-2102 applied the single entry L01 and recorded its outcome in `## 7`.
**Mode:** survey (this file), then apply (second dispatch, 260818-2102, `**Ledger:**` this file, `**Approved:** all`)
**Date:** 2026-08-18
**HEAD read:** `e14b6ca764b47e1007c5a2f555cedc2104c9106b` (v10.2.0)
**Previous curator run:** `260817-1925-curator-run.md`, 2026-08-17, HEAD `e59dea21a12f8f75436444857dcadf42efa65dda`. The run before that was `260817-0845-curator-run.md`, 2026-08-17, HEAD `c65e1cf4678bb85ae359f132a665514b715a37a3`.
**Dispatched by:** the user, top-level, with `**Mode:** survey` and nothing else.
**Domain:** code

**What changed in the interval.** Eighteen commits, `e59dea2`..`e14b6ca`, shipping v10.0.2, v10.1.0 and v10.2.0. Three of them touched a surface this agent owns, all in `rules/`: `1dc062d` added the `Revised by:` footer for a closed defect whose reasoning a later record reverses, `e7ca60f` replaced the exempt-surface path list with a criterion, and `95bebe1` made a Circle record's `## Directive` a pointer once a spec exists and dropped the `**Status:**` head field from the record template. `83d3b04` added one clause to `rules/user-facing-output.md`. **`CLAUDE.md` was not touched at all** — `git log e59dea2..HEAD -- CLAUDE.md` returns one commit, `8d477f2`, which is the previous run's own apply. So `CLAUDE.md` has stood unchanged across two feature releases while `rules/` moved, which is where this run looked hardest.

**One candidate from the previous run has been resolved by the tree rather than by this agent.** C3 there was the `**Status:**` field in the Circle record template, withheld because the answering decision timed its removal to the next Circle that touched Circle records for another reason. That Circle ran: `95bebe1` removed the field and `260815-2312_*_should-the-circle-records-status-field-exist-at-all-…` now carries the implemented marker. The other three candidates (C1, C2, C4) were re-verified at this HEAD and all three still hold; they are carried forward below as C1, C2 and C3.

**Why this run's selector is different from the previous two.** Run `260817-0845-curator-run.md` compared the guard-removal vocabulary; run `260817-1925-curator-run.md` compared who-may-do-what. This run takes a third disjoint selector: **claims that pin a measurement of the tree which no lint gate reads.** The whole hook suite is green at this HEAD (672 tests, 36 files), so every mechanically checkable class a gate covers is already clean, and looking there again would buy nothing. What no gate covers is stated in `## 4`.

---

## 1. Evidence-source counts

| # | Source | Files read | Note |
|---|---|---|---|
| 1 | Circle records (`$SCAN_CIRCLES` = `circles/`) | 11 of 11 listed, **0 bodies opened** | Every `*_circle.md` name and marker read: nine `_c_`, one `_s_` (`260804-1205-shell-reachability-model`), one `_b_` (`260816-1741-guard-becomes-observation-only`). **All eleven are terminal**, so no Circle is active and `$SCAN_DECISIONS` collapses to `shared/decisions/`. No Directive, Grounding snapshot, Turn log or Closure note was opened. This is the second-largest unread source in the run. |
| 2 | Decision records | 94 of 94 listed, 1 opened in full, 7 opened in part | `$SCAN_DECISIONS` = `shared/decisions/` alone, 51 records — the editable surface. The Circle stores hold a further 43, read as evidence through `$SCAN_CIRCLES` and **not** as an editable surface; that boundary is stated rather than assumed. All 94 filenames, markers and `**Status:**` fields were read by script. Opened in full: `260816-1707_*_which-install-path-is-the-authoritative-one-for-end-users.md`. Opened in part: `260818-1504_*_…directive…`, `260818-1512_*_…portfolio-activation…`, `260810-1635_*_…obligation…`, `260812-0254_*_…absolute-path…`, `260816-0119_*_…rename-to-citation…`, `260811-1522_*_…readme-hooks-lib-table…`, `260816-0711_*_…tracked-workbench-split…`. |
| 3 | Session histories | 1 of 507 opened in full | 278 in `shared/history/`, 229 across the Circle stores. Only `260817-1925-curator-run.md`, the previous run, was opened. **506 were not opened. This is the largest gap in the run.** |
| 4 | Git history | full `git log --follow` per file, plus the interval | `git log --follow --oneline` over `CLAUDE.md` (89 commits) and each of the 12 files in `rules/` (1 to 72 commits each), and `git log --oneline e59dea2..HEAD` with per-commit `--stat` on the nine feature commits. `git show e59dea2:agents/shaper.md` and `git show 82a860d:agents/shaper.md` were read to date L01's breakage. `git blame` was not run. |
| 5 | Reviews and analyses | 0 of 75 reviews, 0 of 23 analyses opened | 30 + 45 reviews, 17 + 6 analyses. Not opened. No entry below rests on a review or an analysis. |
| 6 | `orchestrator-events.jsonl` | 1 file, **not read** | 1 763 lines. Corroborating-only by rule, and nothing below needed corroboration a stronger source did not already carry. |
| 7 | Archive store (`$WORKBENCH/archive`) | 291 files listed, **0 opened** | One batch, `archive/260817-1907-safe-cleanup-scoped/`. Listed only; the previous run read its manifest. Read directly, because no `SCAN_*` key reaches the store — itself the subject of open defect `260801-1020_*_scan-keys-never-reach-the-archive-store.md`. |
| 8 | `**Provenance:**` headers | 12 of 12 read in full | Seven cite a Circle, five use the admission-plus-commit form. **Every cited Circle carries `_c_`; none carries `_s_`.** No Tier 2 retirement follows from this source. |

**Open defect corpus, used as a cross-check and not as a fourth surface:** 152 open defect records across both stores. Two of them changed this run's output materially and are named in the entries that cite them.

**Unreadable surfaces:** none. Every file this run tried to read was readable.

**Absent surfaces:** `.claude/rules/` does not exist in this project (`ls .claude/rules` → No such file or directory). There are therefore no project-wide rule files, and the project-owned rule surface is `./rules/` alone, 12 files. The scope call this repository forces is unchanged from the previous run: cwd **is** the fusion plugin's own source tree, so `./rules/` here is the project's own rule directory and in remit, while the install copy at `$FUSION_PLUGIN_ROOT` (`/Users/k1/.fusion`) is out of remit and was not touched.

**The gates were run read-only and all of them are green.** `cd hooks && npx vitest run` → **672 tests across 36 test files, all passing**, exit 0. That includes `reference-resolution-lint`, `derivable-enumerations-lint`, `path-literal-lint`, `provenance-header-lint`, `rules-emission-golden`, `surface-growth-bound`, and the `sentence-identifier-containment` gate added in the interval. Nothing on any shipped surface dangles by any check those gates perform.

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after (if L01 approved) | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 61 904 | 137 | 61 904 | 137 |
| Project rule files (`./rules/`) | 12 | 155 980 | 1 808 | 155 981 | 1 808 |
| Decision records (`$SCAN_DECISIONS` = `shared/decisions/`) | 51 | 380 118 | 4 982 | 380 118 | 4 982 |

L01 replaces the two characters `89` with the three characters `100` inside one line of one file. No file is deleted, no line is added or removed, `CLAUDE.md` is not touched, and no decision record is touched. The "after" figure for `rules/` is arithmetic on a two-character substitution, not an estimate.

## 3. Blast radius

**Verdict: nothing approaching the threshold. No separate scale confirmation is due.**

- **Bytes deleted outright: 2**, on the rule surface, of 155 980 — 0.0013 %.
- **Bytes rewritten in place: 3**, in `rules/fusion-workbench-conventions.md`.
- `CLAUDE.md` and the decision store are byte-identical under every approval outcome, because no entry touches them.

## 4. Comparison counts

**Selection rule, one rule for all six pairs.** A statement enters the comparison when it asserts something a command decides against the tree today **and no lint gate already reads it**. Three shapes qualify:

- **(a) a citation that pins a line number** — `path:N` or `path:N-M`. `hooks/lib/__tests__/reference-resolution-lint.test.ts` resolves plugin-file paths, adjacent heading anchors and workbench-record wildcards, and its `resolveToken` "strips nothing and reads nothing past the path". A line suffix is none of the three, so every such citation on every surface is unchecked. This is not this agent's inference: it is the subject of open defect `260808-0030_*_line-number-citations-into-rule-files-go-stale-and-no-gate-reads-them.md` and of the closed `260818-1512_*_the-dispatch-parameter-tables-prompt-line-citations-are-resolved-by-no-gate-and-two-are-already-ten-lines-off.md`.
- **(b) a cardinal count or an enumeration of a set** — of files, agents, skills, directory entries, configuration keys, or emission audiences.
- **(c) a version string, a file's existence, or a command's stated output.**

A pair enters the comparison when two statements assert the same measurement. Everything outside that rule was not compared, and this run makes no claim about it.

**The corpus counts are derived, not stated.** `ls -1 agents/*.md | wc -l` → 15; `ls -1 skills/ | wc -l` → 12; `ls -1 rules/*.md | wc -l` → 12; `awk 'END{print NR}' CLAUDE.md` → 137; `ls -1 fusion-workbench/shared/decisions/*.md | wc -l` → 51; `ls -1 fusion-workbench/circles/*/decisions/*.md | wc -l` → 43.

The selection was executed as greps over `CLAUDE.md`, all 12 rule files and all 94 decision records. Shape (a) produced **115** line-pinned citations in total; shape (b) produced **41** counts and enumerations on `CLAUDE.md` and the rule files. Of the 115, **101 sit inside terminal decision records** (`_i_`, `_s_`, `_d_`) and were excluded by rule, not by sampling: a terminal record is Grounding-Historie and states what was true when it was written, so correcting a coordinate inside one would rewrite history rather than repair it. **14 sit in live (`_o_`/`_a_`) records, 6 in rule files and 1 in `CLAUDE.md`, and all 21 of those were resolved by hand against the cited file.**

| # | Pair | Pairs the rule produced | Read | Findings |
|---|---|---|---|---|
| 1 | decision × decision | 0 | 0 | No two decision records assert the same measurement of the tree. The selector produced no pair here, which is a property of the selector and not a verdict on the store. |
| 2 | decision × rule file | 3 | 3 | 0 entries, 1 candidate (C3). The three are `260816-0711_*_…tracked-workbench-split` against `rules/fusion-workbench-conventions.md:72-85` (**resolves exactly** — `:72` is the section heading, `:85` its last line), `260807-0158_*_…filename…` against `rules/fusion-workbench-conventions.md:18` (**stale**, counted in C4), and `260816-1707_*_…emitted-to-whom` against the rule corpus (C3). |
| 3 | decision × `CLAUDE.md` | 1 | 1 | 0. `260810-1635_*_where-does-the-obligation-sit-to-update-the-artefact-that-explains-a-behaviour-when-the-behaviour-changes.md_a:63` cites `CLAUDE.md:30`; line 30 is still the `fusion.json` row the record is about, so the coordinate holds even though the row's wording moved on. |
| 4 | rule file × rule file | 6 line pins resolved individually, plus 11 shared-subject enumeration pairs | 17 | **1 entry (L01)**, 1 candidate (C4 in part), 1 candidate (C5). Five of the six line pins resolve correctly — `skills/log-activity/SKILL.md:82`, `skills/archive/SKILL.md:96`, `agents/playmaker.md:61`, `skills/setup/SKILL.md:60` and `:67` were each opened and each holds the sentence the citing text describes. The sixth is L01. |
| 5 | rule file × `CLAUDE.md` | 22 | 22 | 0. Every enumeration `CLAUDE.md` shares with a rule file agrees at this HEAD — the five always-on rules, the four partitioned topics, the three skills citing `circle-records.md`, the three helpers consuming `bin/fusion-plugin-cwd`, the four `DEFINITION_SITES`, the four growth-bound numbers, the six Circle subdirectories, the four `stilwerk/` profiles, the four staging-drift classes, the seven parameterised agents, and the four version surfaces. Each was checked with a command; the commands are in `## 9`. |
| 6 | `CLAUDE.md` × itself | 19 | 19 | 0. The one pair worth naming and clearing: `CLAUDE.md:38` calls `bin/fusion-turn-budget` a "thin bash wrapper over `hooks/dist/turn-budget.js`, resolved relative to itself", and `CLAUDE.md:42` says there were **three** such wrappers until 2026-08-15 with two surviving — which would be false at four if the class were "thin bash wrapper". It is not. `:42` scopes the class by "the same stdout-verdict rule its one remaining sibling carries", and `grep -n verdict bin/fusion-{staging-drift,review-coverage,turn-budget}` returns hits in the first two only: both carry `verdict=` on stdout and exit 0 whatever they found, while `fusion-turn-budget` carries no `verdict=` and distinguishes its outcomes by exit code. The class is coherent and the count is right. |

**The verdict covers the pairs this rule reached, not the corpora.** 101 of the 115 line pins were excluded by the terminal-record rule and never resolved. 506 of 507 session histories, 75 reviews, 23 analyses, all 11 Circle record bodies and 291 archived files were not opened. Statements on all three surfaces that assert no measurement — every rule of conduct, every rationale paragraph, every worked example — entered no pair at all, and they are the bulk of the text. A contradiction outside the measurement vocabulary, and outside the guard-removal and who-may-do-what vocabularies the two previous runs took, has now not been looked for by any of the three.

## 5. Pre-edit content of decision records this run would modify

**None.** This run proposes no change to any decision record. The obligation is unconditional and is discharged by the count: zero records are in the ledger, so there is no pre-edit content to preserve.

## 6. The ledger

1 entry and 5 candidates. Ids are stable and per-entry approval is by id.

| Group | Ids | Count |
|---|---|---|
| Constraint removal | — | 0 |
| Tier 3 | — | 0 |
| Tier 2 | — | 0 |
| Tier 1 | L01 | 1 |
| Consolidation | — | 0 |
| **Candidate (not on offer, never applied)** | C1–C5 | **5** |

---

### L01 — the conventions file cites the shaper's backlog-promotion rename at a line that now holds a sentence about path re-resolution

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, line 214 (the `_d_` row of the backlog marker table)
- **Tier:** 1
- **Citation:** the claim is that the shaper's promotion path "renames `_o_` or `_p_` and nothing else (`agents/shaper.md:89`)".

  ```
  $ sed -n '89p' agents/shaper.md
     `rules/fusion-workbench-conventions.md` `## Path Resolution` → *Where the call belongs*.
  $ sed -n '100p' agents/shaper.md
       Rename its marker `_o_` or `_p_` to `_c_` — only the marker changes — and append
  $ git show e59dea2:agents/shaper.md | sed -n '89p'
       Rename its marker `_o_` or `_p_` to `_c_` — only the marker changes — and append
  ```

  The citation was correct at `e59dea2`, the HEAD the previous curator run read, and broke in the interval: `95bebe1` inserted eleven lines above it when mode 3 gained the `**Scope:**` parameter and the directive-only branch. `git log --oneline -5 -- agents/shaper.md` names `95bebe1` as the most recent commit to the file.
- **Consequence group:** tier-1
- **Constraint removed:** none. The sentence's claim — that the shaper's promotion path renames `_o_` or `_p_` and nothing else, so no `_d_`→`_c_` transition exists — is preserved word for word. Only the coordinate under it moves.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Cross-check against the open defect corpus, and why it does not stop this entry.** `260808-0030_*_line-number-citations-into-rule-files-go-stale-and-no-gate-reads-them.md` is open and ranks three fix directions, of which option 1 is to drop the number in favour of the heading anchor the lint already resolves. **This entry does not take option 1**, because that record says the choice "needs a decision about scope before implementation: whether the preference binds fusion's own shipped text only, or also the records agents write" — and answering that by acting would decide it. What the entry takes is the repair the same class's closed record already establishes for shipped text: `260818-1512_*_the-dispatch-parameter-tables-prompt-line-citations-are-resolved-by-no-gate-and-two-are-already-ten-lines-off.md` was closed on the user's instruction by correcting the measured drift and explicitly leaving the design question open — *"the two rows above are wrong today and should be corrected independently of the design question."* This is the same act on the same kind of surface.
- **Derive over correct, stated rather than skipped.** A line number is a measurement of a file that moves, so the derive rule would normally ask for the derivation instead of the corrected value. The derivation exists — it is the adjacent heading form `` `agents/shaper.md` `## <heading>` `` — and it is exactly option 1 of the open record above. It is therefore not this run's to take, and the corrected value is proposed as the documented fallback, with the derivation named here so the user holds it.

**Before:**

> `_d_`→`_c_` by the shaper, because its promotion path renames `_o_` or `_p_` and nothing else (`agents/shaper.md:89`).

**After:**

> `_d_`→`_c_` by the shaper, because its promotion path renames `_o_` or `_p_` and nothing else (`agents/shaper.md:100`).

---

### C1 — `rules/workbench-path-resolution.md:72` names two consumers of `SCAN_CIRCLES` and there are seven

- **Surface:** project rule file
- **File:** `rules/workbench-path-resolution.md`, line 72 (the `SCAN_CIRCLES` row of the key table)
- **Tier:** 1
- **Citation:** re-verified at this HEAD. The row's Notes column reads `Portfolio-wide scans (playmaker, /fusion:next).` `grep -lE '\$SCAN_CIRCLES\b' agents/*.md skills/*/SKILL.md` returns seven consumers: `agents/curator.md`, `agents/shaper.md`, `agents/playmaker.md`, `agents/orchestrator.md`, `skills/archive/SKILL.md`, `skills/next/SKILL.md`, `skills/setup/SKILL.md`.
- **Why it is a candidate:** unchanged from the previous run, which raised it as C1 and withheld it for the same reason. The parenthetical carries no word of exhaustiveness — no "only", no "exactly" — so reading it as a complete enumeration rather than as two examples is a reading of prose and not a check. The file *is* the definition of what each key means, which argues the other way. Raise it as an entry only if the user says the Notes column is meant to enumerate.

### C2 — `rules/context-lean-claude-md.md:11` describes itself as input to work that has since closed

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`, line 11
- **Tier:** 3
- **Citation:** the line reads "This is the input to the reference conversion (a later Circle-B step) and to the docs pass (Circle E)." Circle B and Circle E are tracks of `260718-1924-v5x-overhaul`, whose record is `_c_circle.md` (verified this run by `ls`).
- **Why it is a candidate:** unchanged from the previous run, which raised it as C2. Tier 3 requires naming **when** the practice stopped and **what replaced it**, and this run can name neither without reading that Circle's session histories, which it did not open. Downgraded per the tier rule rather than guessed at. It has now been carried twice; if it is to be resolved, the cheapest path is a dispatch that reads that one Circle's histories.

### C3 — an open decision record's premise counts two rule files emitted to no agent, and there are four

- **Surface:** decision record
- **File:** `260816-1707_*_to-whom-is-the-new-workbench-tracking-rule-emitted-when-its-consumers-are-a-human-and-a-skill.md`, the `**Cross-references:**` line and option 1's Cons
- **Tier:** 1
- **Citation:** re-verified at this HEAD by running `bin/fusion-rules` for all 15 agents and collecting the union. Four of the 12 rule files are emitted to nobody: `context-lean-claude-md.md`, `context-manifest.md`, `rule-file-provenance.md`, `workbench-path-resolution.md`. The record calls the last two "the two files emitted to no agent by design" and option 1 "a third such file".
- **Why it is a candidate:** unchanged from the previous run, which raised it as C4. The count is verified; whether the `context-*` pair belongs to the record's class is a reading rather than a check, and the record's substance does not turn on whether the precedent count is two or four. Editing a user's live open question over a contested reading of a side remark is not proportionate. Reported so the user holds the number when they answer.

### C4 — line pins inside live decision records, two of them past the end of the file they cite

- **Surface:** decision record
- **Files:** 14 line-pinned citations across 10 records carrying `_o_` or `_a_` in `shared/decisions/`
- **Tier:** 1
- **Citation:** all 14 were resolved by hand against the cited file. Four are unambiguously dead:

  | Citing record | Cites | What is there |
  |---|---|---|
  | `260810-1635_*_…obligation…:19` | `hooks/tracker.ts:770-771` | the file is **499 lines** — past EOF |
  | `260816-0119_*_…rename-to-citation…:22` | `hooks/guard.ts:307` | the file is **223 lines** — past EOF |
  | `260812-0254_*_…absolute-path…:61` | `agents/orchestrator.md:537` | a blank line (the file is 1 343 lines) |
  | `260807-0158_*_…unique-filename…:78` | `rules/fusion-workbench-conventions.md:18` | the store-literal paragraph, not `## Filename Patterns`, which is what the record describes |

  Nine of the remaining ten resolve to plausible or exactly correct text; `260816-0711_*_…count-pinning…:20` cites `hooks/lib/__tests__/domain-cascade.test.ts:784-794` and line 784 is a comment separator, which is uncertain rather than clearly wrong. The second row is the sharpest: `260816-0119_a` cites `hooks/guard.ts:307` while *arguing that the citation resolves*, and the file has since lost more than a third of its length.
- **Why it is a candidate, and not an entry:** this is the half of the class that the open defect explicitly leaves undecided. `260808-0030_*_…` frames the scope question as "whether the preference binds fusion's own shipped text only, or also **the records agents write**", and these are the records agents write. It also says of the older corpus that such citations "are not worth repairing individually", and ranks the repair-on-reconciliation approach third precisely because it samples the failure instead of removing it. Correcting fourteen coordinates now would answer the scope question by performing it, and would spend the evidence the open record is accumulating. **What this run adds is the measurement at this HEAD**, for whoever answers: 115 line pins on the three surfaces, 101 inside terminal records, 14 inside live ones, of which 4 are dead and 2 point past the end of their file.

### C5 — the Circle vocabulary's `_a_ → _t_` transition names one writer of `.active-circle` and the conventions file authors two

- **Surface:** project rule file
- **File:** `rules/circle-records.md`, line 56
- **Tier:** 1
- **Citation:** `rules/circle-records.md:56` reads "`_a_ → _t_` — playmaker proposes activation; user confirms; orchestrator renames the record and writes `.active-circle` with the directory name." `rules/fusion-workbench-conventions.md:91` authors the writer set and says it is closed: "On the activation path there are **two** writers: the orchestrator … ; `/fusion:next` writes it in its user-confirmed interactive-activation branch." The second writer is real and performs the identical pair of writes — `skills/next/SKILL.md:11` ("the `_a_→_t_` record rename, the `.active-circle` write"), `### 6.3 — Write `.active-circle`` at `:228`, and the command itself at `:233`.
- **Why it is a candidate:** the two statements do not contradict each other in the sense the remit requires — an agent obeying the worked transition violates nothing in the conventions file — so this is an incomplete enumeration, not a conflict. It is a candidate rather than an entry for a second reason that matters more: `rules/circle-records.md:5` declares itself "**the definition** for the Circle state vocabulary, its transitions", while `rules/fusion-workbench-conventions.md:91` declares the `.active-circle` writer set "closed and enumerated here". **Nothing says which governs where the two overlap**, and that is the precedence-undecided shape. This run does not resolve it by picking one and does not file a decision record for it, because a one-clause addition to `:56` would close the gap without touching precedence at all, and asking the user for a precedence rule they do not need is the more expensive answer. Offered here so the user can direct it either way.

## 7. Outcomes

**Apply pass, dispatched 260818-2102, approving `all`.** The approval set resolved to the one entry in the ledger. The five candidates C1 to C5 are not approvable and were not applied, which is a property of their kind and not of the approval.

| Id | Outcome | Detail |
|---|---|---|
| L01 | **applied** | The before-text was re-read from disk before the write and matched the ledger byte for byte, so the entry was not stale. `rules/fusion-workbench-conventions.md:214` now cites `agents/shaper.md:100`. |
| C1–C5 | not applicable | Candidates. Never offered for approval, never applied. |

**The tree the apply ran against was the tree the survey read.** `git rev-parse HEAD` → `e14b6ca764b47e1007c5a2f555cedc2104c9106b`, identical to the survey's HEAD, and `git status --porcelain` before the write showed only `fusion-workbench/orchestrator-events.jsonl` modified and this run file untracked. Nothing had moved under the entry in the interval.

**The corrected coordinate was re-verified at apply time, not carried over from the survey.** `sed -n '100p' agents/shaper.md` → `Rename its marker `_o_` or `_p_` to `_c_` — only the marker changes — and append`, which is the promotion rename the citing sentence describes.

**Surface sizes after, measured rather than predicted:**

| Surface | Files | Bytes before | Bytes after | Lines before | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 61 904 | 61 904 | 137 | 137 |
| Project rule files (`./rules/`) | 12 | 155 980 | 155 981 | 1 808 | 1 808 |
| Decision records (`shared/decisions/`) | 51 | 380 118 | 380 118 | 4 982 | 4 982 |

Each figure is `wc -c` and `wc -l` over the surface at HEAD after the write. The rule surface moved by exactly the +1 byte the ledger's arithmetic named; the other two are byte-identical, as `## 2` said they would be under every approval outcome. `git diff --stat rules/fusion-workbench-conventions.md` → 1 file changed, 1 insertion, 1 deletion.

**Removals by tier: none, in any tier.** No constraint was removed, no file was deleted, no line was added or removed.

**One consequence is outstanding and is not this agent's to run.** `hooks/lib/__tests__/fixtures/rules-emission.golden` pins the byte size of every emitted rule file, so the +1 byte turns `npm test` red. Confirmed read-only after the write: `cd hooks && npx vitest run lib/__tests__/rules-emission-golden.test.ts` → 1 failed, 14 passed, the diff being `fusion-workbench-conventions.md 58103` → `58104` and the per-agent total `98873` → `98874` in each of the fifteen agents' blocks. Regeneration is `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, then read the diff. **Executor: `coder`.** This is the fixture case named in `## 8` item 5: the edit was in remit and approved, and only its consequence belongs to someone else. The failure is loud rather than silent, so what this line adds is the owner, not the warning.

**Searchable line for a later grep, per the wrong-prune mitigation:** *no constraint was removed in run 260818-2050-curator-run.md; the single entry replaces the two characters `89` with `100` inside one citation in `rules/fusion-workbench-conventions.md` and preserves the sentence around it verbatim.*

**Revert path, as applied:** `git checkout -- rules/fusion-workbench-conventions.md`. The file is under version control, so the revert is available. Nothing was committed by this agent; the edit stands in the working tree.

## 8. Work this agent may not do, and who owns each

Six items. None is a ledger entry. **No new record was filed this run**, because each item already has an owner or a filed record, and the conventions' own filing rule says to append to what exists rather than open a duplicate.

1. **The `path:N` citation class has no gate and no chosen fix.** `260808-0030_*_…` is open, was re-verified reproducible by the reconciler at HEAD `2552586`, and ranks three options of which none is chosen. This run adds a fresh measurement to the record's evidence (see C4) but does not append it, because editing a defect record is not this agent's write. **Executor: the user, at a decision gate; then `coder`** for whichever of the three options is chosen. The cheapest of them, option 2, would have caught two of C4's four instances mechanically: both cite a line past the end of the file.

2. **An answered decision has not been realised on any of the three surfaces, and one of them is `CLAUDE.md`.** `260816-1707_*_which-install-path-is-the-authoritative-one-for-end-users.md` was answered on 2026-08-16 — option 1, `heads/main` is the standard install path and a tag is an opt-in pin. Its own reconciliation at 260817-1836 states: *"No sentence in `README.md`, `CLAUDE.md` or `docs/` states that `heads/main` is the standard and the tag an opt-in pin."* Re-verified at this HEAD: `CLAUDE.md:109` describes the default (`heads/main`) and the pin example without ever saying which is policy. **This is not a ledger entry, and the reason is worth stating precisely.** It is neither of the two things this agent changes text for: nothing on any surface contradicts the answer, and nothing has been made obsolete by it. Writing the policy sentence would be *realising* an answered decision — an addition, not a repair — and the marker walk that follows it belongs to `agents/reconciler.md`. **Executor: `coder`** for the sentence, on the user's instruction; the marker then moves `_a_`→`_i_` by the reconciler.

3. **`.guard-state/` holds two inert leftovers that no surface names and `/fusion:setup` does not offer to delete.** `ls fusion-workbench/.guard-state/` returns six files: `review-coverage.json` and `staging-drift.json` (the two live throttle records), `events.jsonl` (the record), `escalation.json`, **`churn.json` (109 454 bytes)** and **`state-drift.json`**. Nothing at HEAD writes the last three — `grep` over `hooks/*.ts hooks/lib/*.ts bin/ skills/` finds only header comments describing what they used to be. `rules/fusion-workbench-conventions.md:79` and `skills/archive/SKILL.md:94` each name `escalation.json` as the leftover a stale workbench may hold, and `skills/setup/SKILL.md:305-317` offers to delete that one alone. **This is not proposed as a rule-file edit**, because neither sentence claims to enumerate every leftover, so reading them as falsified is a reading of prose rather than a check — the same test that made C1 a candidate. **And whether Setup should widen its offer is a decision, not a defect:** the existing offer exists to reassure a user that a halt flag is not blocking them, which is a different reason from general cleanup, so extending it invents a mandate rather than completing one. Reported here rather than filed, so the user decides whether it is worth a record. **Executor, if it becomes one: the user, then `coder`** for `skills/setup/SKILL.md`.

4. **The decision-record `**Status:**` field disagrees with its own filename marker in 44 of 94 live records**, measured this run by comparing each filename marker against the head field under the template's own vocabulary. That is up from 35 of 90 at the previous run and 34 of 67 when the defect was filed on 2026-08-11, on a corpus that grew by four records in the interval — so the class is still producing. One of the 44 is a false positive under strict equality: `260809-2004_*_…` reads `implemented (marker `_i_`; header corrected by the reconciler …)`, which a prefix comparison would accept, giving 43. **No record was edited and no decision record was filed**, for the reason the previous run gave and which still holds: `260811-2146_*_half-the-decision-records-carry-a-status-that-disagrees-with-their-marker-and-twelve-keep-the-unfilled-template-stub.md` is open, states the question ("owned" or "dropped"), and says in terms *"Do not hand-correct the 34 without answering the question first — the disagreements are the measurement."* **What this run adds beyond the number is a precedent the record did not have when it was filed:** the analogous question for the *Circle* record's `**Status:**` field was answered on the same reasoning and implemented in the interval — `260815-2312_*_should-the-circle-records-status-field-exist-at-all-now-that-both-transitions-maintain-it.md`, dropped in `95bebe1` because the filename marker is the state. That is evidence for the "dropped" branch of the open question, not a decision this agent takes. **Executor: the user, at a decision gate; then `coder`.**

5. **Nothing in this run moves a fixture, a golden or a growth baseline, and that is measured rather than assumed.** L01 changes `rules/fusion-workbench-conventions.md` by **+1 byte**. `hooks/lib/__tests__/fixtures/rules-emission.golden` pins that file's byte size in every agent's block, so it **will** go red on the apply and must be regenerated with `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, then the diff read. This is the fixture case, not the out-of-remit case: the edit is in remit and gated, and only its consequence is somebody else's. **Executor: `coder`. This agent does not run it.** The growth *bound* is not at risk at one byte against a 12 000-byte budget, and `reference-resolution-lint`'s count pins do not move, because the replacement adds no path, anchor or record token — it changes two digits inside an existing citation.

6. **Five of the six line pins in the rule corpus were verified sound and are recorded here so a later run need not re-open them**: `skills/log-activity/SKILL.md:82`, `skills/archive/SKILL.md:96`, `agents/playmaker.md:61`, `skills/setup/SKILL.md:60` and `skills/setup/SKILL.md:67` each hold the sentence the citing text describes. They are correct **today**, which by the open defect's own reasoning says only that nothing has been inserted above them yet.

## 9. Confidence, stated per the critical-stance rule

**Verified**, each with the command and its output in the entry or above:

- that `agents/shaper.md:89` holds a sentence about path re-resolution, that the promotion rename is at `:100`, and that `:89` held it at `e59dea2` and was moved by `95bebe1` (`sed -n`, `git show`, `git log --oneline -- agents/shaper.md`);
- that the hook suite is 672 tests across 36 files, all green, exit 0;
- that `CLAUDE.md` was untouched in the interval apart from the previous run's own apply (`git log e59dea2..HEAD -- CLAUDE.md` → `8d477f2` alone);
- that all eleven Circle records are terminal, and that all twelve rule files carry a provenance header citing a `_c_` Circle or a commit, none citing a superseded record;
- that four rule files are emitted to no agent and seven consumers name `$SCAN_CIRCLES`;
- that `hooks/tracker.ts` is 499 lines and `hooks/guard.ts` is 223, so two live records cite past the end of the file;
- that 44 of 94 decision records carry a `**Status:**` disagreeing with their marker;
- that the four version surfaces all read `10.2.0` — `.claude-plugin/plugin.json:3`, the marketplace clone's `marketplace.json:42`, `install.sh:27`, `README.md:26`;
- that `bin/fusion-rules coder | xargs wc -c` prints 101 392, exactly the always-on set (94 039) plus this project's chat profile (7 353), as `CLAUDE.md:62` says it does;
- that `bin/fusion-turn-budget` carries no `verdict=` while both surviving drift wrappers do, which is what makes `CLAUDE.md:42`'s count of three correct;
- that nothing at HEAD writes `.guard-state/churn.json` or `.guard-state/state-drift.json`;
- that the cleanup selector vocabulary carries `claude-md` and not `curate`, and that Steps 4, 5 and 6 are the archive, `CLAUDE.md` and activity-log steps.

**Inference**, and labelled as such:

- `inference:` that L01's correction rather than L01's conversion to a heading anchor is the proportionate act. It rests on reading the closed record `260818-1512` as a precedent for shipped text and the open record `260808-0030` as reserving the workbench-record half. Both readings are of prose, and no record states either in those words.
- `inference:` that `rules/circle-records.md:56` and `rules/fusion-workbench-conventions.md:91` do not contradict each other but leave a precedence gap (C5). Both were read in full and the second writer was verified in `skills/next/SKILL.md`, but "an incomplete enumeration is not a contradiction" is a judgement about two prose passages.
- `inference:` that the terminal-record exclusion in `## 4` is the right cut. It follows from the marker vocabulary's own Grounding-Historie reading, which no record applies to citations specifically.

**No speculation is carried in any entry.** C2 is the one Tier 3 shape in the run and it is a candidate precisely because the speculation it would need is not available.

## 10. Where the ledger's own reach stops

506 of 507 session histories were not opened. All 11 Circle record bodies, 75 reviews, 23 analyses and 291 archived files were not opened. 101 of the 115 line-pinned citations were excluded by rule and never resolved. The 43 decision records in Circle stores were listed and read as markers and head fields only. Every statement on all three surfaces that asserts no measurement of the tree entered no pair, and that is most of the text. Three curator runs have now taken three disjoint selectors — guard removal, who-may-do-what, and measurements no gate reads — and a stale statement outside all three has not been looked for by any of them.
