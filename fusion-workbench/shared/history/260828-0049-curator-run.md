# Curator run — 260828-0049

---
**Status:** Complete. Survey and apply passes both done; `## 7` carries the apply outcomes.
**Mode:** survey, then apply (`**Approved:** all`)
**Scope:** full. `bin/fusion-cadence-anchor get last_curator_run` exited 1 (no value held), so no anchor bounded the pass; the previous run file's HEAD is used below only to date the interval, never to skip a read.
**Date:** 2026-08-28
**HEAD read:** `36cd5744b5c910bb42b4e34def6589fa63ea8e32`
**Previous curator run:** `shared/history/260825-1453-curator-run.md`, 2026-08-25, HEAD `3d4b1816a2397a4a351ae11a319e0143e6fd078a`.
**Dispatched by:** the `CLAUDE.md` step of `/fusion:cleanup` (`**Mode:** survey`). This agent held no gate; the four gate items are returned to the dispatcher.
**Domain:** code
**Filed by:** curator, Kai Stalmann <ks@qantr.com>

**Working tree at survey time.** `git status --porcelain` lists 67 entries, 62 of them deletions under `fusion-workbench/` from the archive sweep `archive/260828-0043-safe-cleanup-tier-1` that ran before this pass, plus this session's own history and record edits. `git status --porcelain CLAUDE.md rules/` is empty: every surface this ledger proposes to change is clean at HEAD.

**What changed in the interval.** `3d4b181..36cd574` spans 2026-08-25 to 2026-08-28 and ships v10.8.0 through v10.14.0. Four rule files were added (`backlog-entries.md`, `project-language.md`, `orchestrator-resume.md`, `orchestrator-rebalance.md`), taking `rules/` from 14 files to 18. Every entry below is a Tier 1 falsification of a `CLAUDE.md` enumeration by a change that landed in the interval and did not touch that sentence.

**The suite at HEAD.** `cd hooks && npm test` ran twice. The first run (00:45) reported `44 files, 1 failed | 784 passed`, the failure being `workbench-citation-lint.test.ts` (dangling citations in live workbench records); a rerun of that file alone at 00:48 passed `10/10`, and the full-suite failure line carried no record path this agent could act on. `inference:` the first run overlapped a concurrent write to the workbench corpus. Nothing in this ledger rests on that test either way; `reference-resolution-lint`, `derivable-enumerations-lint` and `provenance-header-lint` passed on both runs, so every plugin-file path, heading anchor, skill token, agent count and `bin/` roster claim in `CLAUDE.md` is verified true at HEAD without this pass re-deriving it. All four entries below sit in gaps those gates do not cover: a hook-event list, an agent list inside one table row, a subcommand list, and a root-entry list.

---

## 1. Evidence-source counts

| # | Source | Read |
|---|---|---|
| 1 | Circle records, `$SCAN_CIRCLES` | 19 Circle directories enumerated (`ls circles`): 15 `_c_`, 3 `_b_`, 1 `_s_`, none live. The two records modified since the previous run were read at Closure-note level: `260825-2023-presence-travels-monitor-filters-own-checkout/_b_circle.md`, `260826-1613-cardinality-answered-cut-once-nineteen-cleared/_c_circle.md`. The other 17 were not opened. |
| 2 | Decision records | `$SCAN_DECISIONS` resolves to `shared/decisions` alone (no active Circle): 72 records, 554 179 bytes: 5 `_o_`, 30 `_a_`, 34 `_i_`, 2 `_d_`, 1 `_s_`. All 5 `_o_` read at `## Question` level; the `_s_` record's `Superseded by:` line read; `260827-0830_a_*` read at option level (it is the citation for L02). Circle stores hold a further 80 (18 `_o_`, 7 `_a_`, 53 `_i_`, 1 `_d_`, 1 `_s_`), enumerated only, outside this run's decision surface. Every decision cited by wildcard or bare stamp from `CLAUDE.md` or a rule file was resolved and its marker read: 27 wildcard citations, 17 bare stamps; one resolves to an `_o_` record (`260820-2314`, cited by `CLAUDE.md` as deliberately open), none to `_d_`, none to `_s_`. |
| 3 | Session histories, `$SCAN_HISTORY` and Circle stores | 767 files enumerated (`find … -name '*.md' | wc -l`), 65 newer than the previous run file. Read: the previous curator run's head, `## 6` and `## 7`. The current session's own history was not read as evidence. |
| 4 | Git | `git log --follow --oneline` counted per rule file and `CLAUDE.md` (18 + 1 files; `CLAUDE.md` 114 commits, `fusion-workbench-conventions.md` 93); `git log -S` on the four falsified sentences to date each and its falsifier (`d7cdfa7f`, `8ac9a533`, `cc070d92`/`ce4175c2`, `3fda8290`); `git log --since=2026-08-25` in full. |
| 5 | Reviews and analyses | 104 review files and 38 analyses enumerated across `shared/` and every Circle store. **None opened.** No entry rests on either. |
| 6 | `orchestrator-events.jsonl` | 2 534 lines. **Not read.** Corroborating-only, and nothing here needed corroboration. |
| 7 | Archive store | 3 sweep directories, 453 markdown files, 22 decision records. Enumerated; used once, to confirm that every citation from a surface resolves in the live tree and none has been swept. |
| 8 | `**Provenance:**` headers | **18 of 18** rule files read at header level. All carry the line on line 3. None cites a record carrying `_s_`, so no rule file is a Tier 2 retirement candidate on that ground. |

**Sources reported zero:** none. **Sources unreadable:** none. **Deliberately not opened:** 5 and 6.

**Surfaces.** Decision records: readable, 72 files. Project-owned rule files: `./rules/` readable, 18 files; `.claude/rules/` **absent** (`ls: .claude/rules: No such file or directory`). `CLAUDE.md`: readable, read in full. This repository is the plugin's source, so `./rules/` here is the project's own rule directory and exclusion 7 does not apply to it; `bin/fusion-rules` emitted work-tree paths, confirming the preference is in force. `rules/rule-file-provenance.md` was read from `$FUSION_PLUGIN_ROOT`.

---

## 2. Surface sizes

| Surface | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|
| `CLAUDE.md` | 80 196 | 149 | (apply pass) | (apply pass) |
| Project rule files (`rules/*.md`, 18 files) | 207 481 | 2 220 | unchanged proposed | unchanged proposed |
| Decision records (`shared/decisions`, 72 files) | 554 179 | not counted | unchanged proposed | unchanged proposed |

Every entry lands in `CLAUDE.md`. No rule file and no decision record is touched, so no `rules-emission.golden` regeneration is owed by this ledger.

---

## 3. Comparison counts, per surface pair

Counts derived with the commands named; each verdict covers the pairs its rule reached, not the corpus.

| Pair | Selection rule | Produced | Read | Verdict |
|---|---|---|---|---|
| 1 decision vs decision | each of the 5 `_o_` shared records against the `_a_`/`_i_` records its `## Question` names | 5 questions, 0 named counterparts overturning them | 5 | no live record overturns another among the pairs reached |
| 2 decision vs rule file | every decision cited from a rule file (`grep -oE` over `rules/*.md`, 31 citations after dedup), marker read off the resolved filename | 31 | 31 markers | none `_s_`; no rule rests on a superseded position |
| 3 decision vs `CLAUDE.md` | same rule over `CLAUDE.md` (13 wildcard + 12 bare stamps) | 25 | 25 markers, `260827-0830` read at option level | one contradiction, L02 |
| 4 rule file vs rule file | the conventions' authoring-home table (7 rows) against the emitting `case` blocks in `bin/fusion-rules` | 7 | 7 | consistent |
| 5 rule file vs `CLAUDE.md` | every `rules/*.md` row in the `CLAUDE.md` Layout table (13 rows) against `bin/fusion-rules` emission and the file's own header | 13 | 13 | one contradiction, L02; one enumeration mismatch, L04 |
| 6 `CLAUDE.md` vs itself | every enumeration inside one table row checked against the row's own later sentences | 3 rows (`hooks/`, `bin/fusion-cadence-anchor`, `fusion-workbench/`) | 3 | one self-contradiction, L03 |

Not compared: the 80 Circle-store decisions against anything (outside `$SCAN_DECISIONS` this run); `rules/context-manifest.md`, `context-lean-claude-md.md`, `design-diagrams.md`, `workbench-path-resolution.md`, `orchestrator-rebalance.md` body text against `CLAUDE.md` beyond their Provenance headers and the Layout-table rows.

---

## 4. Pre-edit content

No decision record is proposed for modification. Nothing to record.

---

## 5. Blast radius

Zero bytes deleted on any surface. Net effect on `CLAUDE.md` is an addition estimated under 400 bytes across four in-line replacements; no line is added or removed. **No blast-radius stop.**

---

## 6. The ledger

Consequence groups: constraint removals 0, tier-3 0, tier-2 0, tier-1 4, consolidations 0. Candidates: 2, not on offer.

### L01 — the `hooks/` row names three hook events and `hooks.json` registers four

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (Layout table, `hooks/` row)
- **Tier:** 1
- **Citation:** `grep -oE '"(SessionStart|PreToolUse|PostToolUse|SubagentStop)"' hooks/hooks.json | sort -u` → `"PostToolUse" "PreToolUse" "SessionStart" "SubagentStop"`. The fourth was registered in `d7cdfa7f` (2026-08-27, "the backgrounded dispatch's task_done moves to its real completion"), running `hooks/dist/subagent-stop.js`; `README-hooks.md` already names it twice (`grep -c SubagentStop README-hooks.md` → 2). The sentence dates from `b05b423a` (2026-05-04) and was never revisited.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> SessionStart, PreToolUse, PostToolUse hooks.

**After:**
> SessionStart, PreToolUse, PostToolUse and SubagentStop hooks; `hooks/hooks.json` is the registration and the list to read.

### L02 — the transition-agent list for `decision-record-examples.md` names four and the emitter names five

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (Layout table, `rules/decision-record-examples.md` row)
- **Tier:** 1 (also a pair-3 and pair-5 contradiction)
- **Citation:** `sed -n 250p bin/fusion-rules` → `orchestrator|shaper|playmaker|planner|reconciler) IS_DECISION_TRANSITION_AGENT=1 ;;`. The binding record agrees with the emitter, not with `CLAUDE.md`: `shared/decisions/260827-0830_*_…md` line 24, option 1 as realised, names "orchestrator, shaper, playmaker, planner, and reconciler, whose act worked transition 1 is". Both the emitter line (`8ac9a533`) and the `CLAUDE.md` sentence (`ae00e84e`) landed on 2026-08-27; the sentence copied the record's `## Options` list of four instead of its realised list of five.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> Conditionally emitted since 260827 to the transition agents — orchestrator, shaper, playmaker, planner — after

**After:**
> Conditionally emitted since 260827 to the transition agents — orchestrator, shaper, playmaker, planner and reconciler, the `IS_DECISION_TRANSITION_AGENT` case in `bin/fusion-rules` — after

### L03 — the `bin/fusion-cadence-anchor` row lists three subcommands and then names a fourth

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (Layout table, `bin/fusion-cadence-anchor` row)
- **Tier:** 1 (pair 6, `CLAUDE.md` against itself)
- **Citation:** `grep -nE '^#   (get|set|changed-files|changed-since) ' bin/fusion-cadence-anchor` → lines 40, 42, 44, 58: four subcommands. The same row's later sentence already says the reconciler "reads `changed-files`", and `agents/reconciler.md:67` calls `fusion-cadence-anchor changed-files last_reconcile_commit`. `changed-files` entered the helper in `ce4175c2` (2026-08-27), after the row was written in `cc070d92` the same day.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> the subcommands (`get`/`set`/`changed-since`), the key roster

**After:**
> the subcommands (`get`/`set`/`changed-files`/`changed-since`), the key roster

### L04 — the `fusion-workbench/` row's root-entry list is a second copy of the conventions' tree, four entries short

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (Layout table, `fusion-workbench/` row)
- **Tier:** 1 (pair 5, rule file against `CLAUDE.md`)
- **Citation:** `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout`, tree lines 52–67 and the paragraph "The list is exhaustive as written, and it is a list rather than a count on purpose". `diff <(grep -oE '`[^`]+`' <the CLAUDE.md sentence>)` against the tree: the sentence omits `.fusion-setup`, `.asset-provenance`, `.checkout-id` and `.cadence-anchors`; `.cadence-anchors` entered the tree in `3fda8290` (2026-08-27). Derive-over-correct applies: the tree is the definition and the sentence is a copy that drifted, so the entry proposes the pointer rather than a third copy.
- **Consequence group:** tier-1
- **Constraint removed:** none. The pointer replaces an enumeration; the following sentence about `.active-circle` and the Origin-Rule sentence are untouched.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> Session/hook state stays at the workbench root: `agentstate.yaml`, `orchestrator-live.md`, `orchestrator-events.jsonl`, `.guard-state/`, `.commit-lock/`, `.session-marker`, `.active-circle`, `portfolio.md`, `monitor`.

**After:**
> Session/hook state stays at the workbench root; which entries those are is enumerated once, in the tree under `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout`, and a copy here is what drifted.

### C01 — 18 open (`_o_`) decision records sit in terminal Circles' stores

- **Surface:** decision record
- **File:** `circles/*/decisions/*_o_*` (18 files; `ls circles/*/decisions/*_o_* | wc -l`)
- **Tier:** candidate
- **Citation:** enumeration only. Every Circle is terminal (15 `_c_`, 3 `_b_`, 1 `_s_`), so these records are outside `$SCAN_DECISIONS` for a run with no active Circle and outside this run's decision surface.
- **Why it is a candidate:** no surface pair reached them, and a record's marker moves on ground-truth verification are the reconciler's, not this agent's. Reported so the next run with a Circle in scope, or a `**Scope:** full` run that widens the decision surface by hand, knows the corpus exists.
- **Constraint removed:** none
- **Revert path:** not applicable; nothing proposed.

### C02 — the five candidates of the previous run stand unexamined

- **Surface:** CLAUDE.md
- **File:** `shared/history/260825-1453-curator-run.md` `## 6`, C01–C05
- **Tier:** candidate
- **Citation:** the previous run file. `grep -c design-diagrams CLAUDE.md` → 0, so its C04 (the one shipped rule file `CLAUDE.md` never names) is still true at HEAD; the other four were not re-derived.
- **Why it is a candidate:** each was a candidate for a stated reason that no record in the interval addresses; re-deriving them adds no citation.
- **Constraint removed:** none
- **Revert path:** not applicable; nothing proposed.

---

## 7. Outcomes

**Apply pass:** 2026-08-28, dispatched by the `CLAUDE.md` step of `/fusion:cleanup` with `**Approved:** all`. HEAD at apply time `624a7cc80961ea3de657322caf4a6042dad39223`; `CLAUDE.md` was clean at HEAD before the pass, and every Before block was found exactly once on disk before its write.

| Id | Outcome |
|---|---|
| L01 | applied — After block re-read byte-identical |
| L02 | applied — After block re-read byte-identical |
| L03 | applied — After block re-read byte-identical |
| L04 | applied — After block re-read byte-identical |
| C01 | not on offer; nothing applied |
| C02 | not on offer; nothing applied |

**Surface sizes after:** `CLAUDE.md` 80 363 bytes, 149 lines (before: 80 196 bytes, 149 lines). Rule files and decision records untouched. Removals by tier: none.

**Revert path:** `git checkout -- CLAUDE.md`.

**Fixture consequence, coder work.** `hooks/lib/__tests__/reference-resolution-lint.test.ts` pins the number of references it resolves, and the four After blocks add three plugin-file paths (`hooks/hooks.json`, `bin/fusion-rules`, `rules/fusion-workbench-conventions.md`) and one heading anchor (`## fusion-workbench Layout`). Every reference resolves; only the pin moved: `BASELINE` paths 1511 → 1514, anchors 212 → 213. The test's own failure text names re-approving the baseline as the expected response. That is an edit to a test file, outside this agent's remit; whoever commits this `CLAUDE.md` change writes the two numbers into `BASELINE` in the same commit. `derivable-enumerations-lint` passed after the edits.

**Anchor:** `fusion-cadence-anchor set last_curator_run 624a7cc80961ea3de657322caf4a6042dad39223` run after this section was written.

---

## 8. Notes for the dispatcher

- Gate items: this file at `shared/history/260828-0049-curator-run.md`; tier-1 4, every other group 0; candidates 2, not on offer; no blast-radius stop.
- After the apply pass (or after a rejection), the anchor is set: `"$FUSION_PLUGIN_ROOT/bin/fusion-cadence-anchor" set last_curator_run "$(git rev-parse HEAD)"`, guarded by `[ -x ]`. This survey did not set it, because the mark claims a pass that saw everything and the gate has not been answered.
- No entry invalidates a fixture: `CLAUDE.md` is pinned by no golden, and `derivable-enumerations-lint` reads none of the four sentences (verified on the green run at HEAD, and none of the four After blocks introduces a `/fusion:` token, a `bin/` name or an agent count).
