# Curator run — 260816-1251

---
**Status:** Complete
**Mode:** survey (12:51), then apply (12:58) on a second dispatch
**Date:** 2026-08-16
**HEAD read:** `49210261f7460132805e104e115d1235e796e914`
**Previous curator run:** `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/history/260815-1706-curator-run.md`, 2026-08-15. The run before that was `circles/260801-1244-curator/history/260814-1332-curator-run.md`, 2026-08-14. Neither HEAD is recorded in a form this run re-read.
**Domain:** code

**Scope of this run is narrow by dispatch and is not a reconciliation of the three surfaces.**
One defect record and one rule section were surveyed. `CLAUDE.md` was not surveyed, the
decision store was not swept, and no surface pair was compared exhaustively. Read every
count below as scoped to that dispatch.

---

## 1. Evidence-source counts

| # | Source | Files read | Note |
|---|---|---|---|
| 1 | Circle records (`circles/`) | 0 | Not swept. Out of dispatch scope. Two prior curator run files were located there by filename and not read as evidence. |
| 2 | Decision records (`shared/decisions/`) | 1 of 53 | `260816-0740_a_does-the-prose-register-get-a-measurable-gate-and-which-surface-does-it-measure.md`, read in full. The directory was listed; nothing else was opened. |
| 3 | Session histories (`shared/history/`) | 0 | Listed only, to locate the previous curator run. |
| 4 | Git history | 1 file | `git log --follow -- rules/user-facing-output.md`: 10 commits, all read as one-line subjects. `git blame` was not run. `CLAUDE.md` history was not read. |
| 5 | Reviews / analyses | 1 of the analyses, 0 reviews | `shared/analyses/260816-0740-rhetorical-register-of-agent-output.md` lines 355-450 (finding 13 and its lead-in). The remaining 528 lines were not read. `shared/reviews/` was not opened. |
| 6 | `orchestrator-events.jsonl` | 0 | Not read. Corroborating-only source; nothing here needed corroboration it could give. |
| 7 | Archive store (`fusion-workbench/archive`) | 0 | **The directory exists and is empty.** Explicit zero, not a skipped read. |
| 8 | `**Provenance:**` headers | 1 | `rules/user-facing-output.md` carries *"No motivating record recoverable; introduced in `git:c18a946`."* **The Tier 2 provenance path is unavailable for this file by construction:** the header names no record, so there is no record whose marker could retire the rule. |

Also read, as the dispatch required: the defect record
`shared/issues/260816-0740_o_the-gate-contract-never-requires-an-option-to-state-what-it-forecloses.md`
in full, and one open defect from the cross-check corpus (see section 4).

**Sources not read at all: 1, 3, 6, and the reviews half of 5.** That is a property of the
narrow dispatch, not of the corpus.

## 2. Surface sizes

| Surface | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|
| `rules/user-facing-output.md` | 16 788 | 186 | 17 546 | 188 |
| `CLAUDE.md` | not surveyed | not surveyed | not surveyed | not surveyed |
| Decision records | not surveyed | not surveyed | not surveyed | not surveyed |

**Proposed deletions: 0 bytes on every surface.** Both ledger entries are pure additions.
The "after" column was filled by the apply pass; the survey pass wrote nothing.

**Growth-bound accounting, verified.** The universal core is the five unindented
`emit_if_exists` lines in `bin/fusion-rules` (lines 383-387). Measured at this HEAD:

```
$ wc -c rules/agent-setup.md rules/fusion-workbench-conventions.md \
        rules/decision-record-examples.md rules/user-facing-output.md \
        rules/critical-stance.md
    3499 rules/agent-setup.md
   55184 rules/fusion-workbench-conventions.md
    4291 rules/decision-record-examples.md
   16788 rules/user-facing-output.md
    9941 rules/critical-stance.md
   89703 total
```

`RULE_BASELINE` for the same five in `hooks/lib/__tests__/rules-emission-golden.test.ts:465-469`
sums to 86 573. Growth is 3 130 against a `GROWTH_BUDGET` of 12 000
(`rules-emission-golden.test.ts:251`), so head-room is **8 870 bytes**. That reproduces the
figure the dispatch stated for commit `b18a8cf` and confirms nothing has moved it since.

Both entries applied would cost **758 bytes**, leaving 8 112.

## 3. Comparison counts

| Surface pair | Selection rule | Pairs produced | Pairs read |
|---|---|---|---|
| decision vs decision | none applied | 0 | 0 |
| decision vs rule file | the one decision record the dispatch named, against the one rule section it named | 1 | 1 |
| decision vs `CLAUDE.md` | none applied | 0 | 0 |
| rule file vs rule file | intra-file only: `## Questions and gates` against the file's own opening paragraph and `## Information architecture` | 2 | 2 |
| rule file vs `CLAUDE.md` | none applied | 0 | 0 |
| `CLAUDE.md` vs itself | none applied | 0 | 0 |

**The verdict covers the four pairs the two rules above reached, not the corpus.** The corpus
these selectors sampled from was derived with commands: `ls fusion-workbench/shared/decisions
| wc -l` gives 53 decision records, `ls fusion-workbench/shared/issues | wc -l` gives 329
defect records of which `grep -c "_o_"` gives 87 open. A run that swept them would produce a
comparison count three orders of magnitude larger. This one did not sweep, by dispatch.

**No contradiction was found and no open decision record was filed by this run.**

## 4. Cross-check against the open defect corpus

The corpus is a check on my own claims, not a fourth surface. Neither entry claims a
supersession or a stopped practice, so the stop condition in `agents/curator.md`
`## How the defect corpus is used` does not fire. Two open defects bear on the entries and
neither blocks them:

- `shared/issues/260814-1419_o_the-tightened-chat-profile-caps-contradict-the-length-section-of-the-rule-that-owns-them.md`
  is a **live, unresolved contradiction inside the target file's own governing pair**: the
  chat profile says gate prompts run to 6 lines, `rules/user-facing-output.md:99` says 8, and
  nothing states which governs. This matters to entry L01 and is written into that entry's
  design note rather than left here.
- `shared/issues/260812-0253_o_rules-lose-their-effect-during-a-long-dispatch.md` asserts that
  always-on rule text loses force over a long dispatch. If it holds, a clause added to this
  file inherits that weakness. It is not a reason to decline the clause; it is a reason not to
  expect the clause alone to fix the reported behaviour.

## 5. Pre-edit content of decision records this run intends to modify

**None.** This run proposes no change to any decision record. The requirement is discharged
by that statement rather than by an empty section.

## 6. The ledger

Two entries. **Neither carries a tier from `## Evidence tiers`, and both are therefore
candidates under my own rules rather than changes I may offer at a gate.** The reason is
stated per entry and is the same in both cases: my remit changes a normative statement for a
cross-surface contradiction or for history-grounded obsolescence, and authoring a *new*
normative requirement is neither. The citations are present and strong; it is the tier that
is absent. See section 7 for what that means for the gate.

---

### L01 — `## Questions and gates` gains a fourth required property: every option states what it forecloses

- **Surface:** project rule file
- **File:** `rules/user-facing-output.md`, `## Questions and gates` (lines 87-95)
- **Tier:** **none.** Not Tier 1 (no statement in the section is false), not Tier 2 (the
  motivating record carries the open `_o_` marker, and an open record retires nothing), not
  Tier 3 (no trajectory shows a practice stopped), not a consolidation (a consolidation
  preserves the original's constraints and adds none). **Candidate.**
- **Citation:**
  - `shared/issues/260816-0740_o_the-gate-contract-never-requires-an-option-to-state-what-it-forecloses.md`,
    the proposed-fix paragraph.
  - `shared/analyses/260816-0740-rhetorical-register-of-agent-output.md` finding 13, the
    four-members list and the worked replacement prompt.
  - Verified by reading: `rules/user-facing-output.md:89-95` requires exactly three properties
    and requires nothing about consequence. Confirmed at this HEAD.
- **Consequence group:** candidate
- **Constraint removed:** none. This entry adds a constraint and removes none.
- **Byte cost:** 410 bytes. **Em-dashes in the new text: 0**, counted with
  `t.count(chr(8212))` over the exact string below.
- **Revert path:** `git checkout -- rules/user-facing-output.md`

**Before:** (the section's last bullet, after which the new bullet is inserted)

> - **Default options should be the most-likely choice.** Don't make the user pick between four equal-weight options if 90% of the time it's option A. Mark the recommended default explicitly.

**After:** (that bullet unchanged, plus one new bullet appended to the section)

> - **Default options should be the most-likely choice.** Don't make the user pick between four equal-weight options if 90% of the time it's option A. Mark the recommended default explicitly.
> - **Every option says what it forecloses.** Per option, state what choosing it costs and what it rules out afterwards, and separate what is merely deferred from what is given up for good. Carry it in the `AskUserQuestion` option `description` field, or on the option's own line when the gate is plain chat text. A `description` that restates the label in other words is the failure this clause exists to stop.

**Where I depart from the record's proposed wording, and why.** The record proposes a text; I
was asked to judge it rather than transcribe it. Four changes:

1. **Cut "An option list without foreclosures is a menu without prices."** It is an aphorism
   with no normative content, and *sententia* is one of the thirteen figures the motivating
   analysis inventories as the register fault. Putting an aphorism into the rule that governs
   register, in the same week the corpus is repunctuated for register, argues against itself.
   Saved about 60 bytes.
2. **Cut "what leaves the board entirely."** The board is a metaphor, and the record's phrasing
   is a three-part list (costs / unavailable / leaves the board) whose third member restates
   the second. I kept the real distinction underneath it, which is between deferred and
   permanent, and stated it in those words.
3. **Do not characterise the `AskUserQuestion` schema.** The record says the `description` field
   "is specified for exactly this" and quotes its specification. **I could not verify that
   quotation.** I hold no `AskUserQuestion` on this dispatch and the schema is not reachable
   from here. A rule file that asserts what an external tool's specification says acquires a
   Tier 1 falsification the day that wording changes. The entry names the field as the place to
   put the content and asserts nothing about its documentation. `confidence: verified` that I
   could not check it; `confidence: inference` that the record's quotation is accurate.
4. **Added the anti-restatement sentence and the chat-text branch.** Neither is in the record.
   The record itself observes that `description` is "filled, when it is filled at all, with a
   restatement of the label", so a bare requirement to fill the field reproduces the failure it
   names; the clause has to forbid the restatement explicitly. And many fusion gates are chat
   text with a bulleted option list rather than an `AskUserQuestion` (the worked example in
   finding 13 is one), so an `AskUserQuestion`-only clause would read as not applying to them.

**A design objection the user should weigh, and it is not small.** A per-option foreclosure
costs roughly one line per option in a chat-text gate. Three options is three extra lines
against a cap that is **currently in dispute**: `rules/user-facing-output.md:99` says 8 lines,
the always-on chat profile says 6, and defect `260814-1419_o_*` records that nothing says which
governs. The `AskUserQuestion` `description` field does not compete for those lines; the chat
branch does. If the user wants the collision avoided rather than inherited, the second sentence
of the clause can be narrowed to the `description` field alone, at a cost of about 70 fewer
bytes and a gap for chat-text gates. I recommend keeping the chat branch and resolving the cap
in `260814-1419`, because a gate that is undecidable inside its cap is worse than a gate that
runs a line long.

**Retained by design: I did not fold this into the "self-contained" bullet.** That was the
cheaper option and I rejected it. The first bullet glosses "self-contained" entirely as *do not
make the user scroll up for context*, which is narrower than the word; the hole could be closed
by widening that gloss. But the whole defect is that the requirement was invisible, and a
widened gloss is more skimmable than a bullet of its own. Visibility wins over bytes here.

---

### L02 — `## Questions and gates` binds the "nothing to decide" case from `## Information architecture`

- **Surface:** project rule file
- **File:** `rules/user-facing-output.md`, `## Questions and gates` (lines 87-95)
- **Tier:** **none.** Same reasoning as L01: an addition, not a correction or a retirement.
  **Candidate.**
- **Citation:**
  - `shared/issues/260816-0740_o_the-gate-contract-never-requires-an-option-to-state-what-it-forecloses.md`,
    the "second, smaller gap" paragraph.
  - `shared/analyses/260816-0740-rhetorical-register-of-agent-output.md` finding 13: the sample
    "arrived at a response moment carrying neither a decision nor an explicit statement that
    none was needed."
- **Consequence group:** candidate
- **Constraint removed:** none.
- **Byte cost:** 348 bytes. **Em-dashes in the new text: 0.**
- **Revert path:** `git checkout -- rules/user-facing-output.md`

**Before:** (the section heading and its first bullet, before which the new bullet is inserted)

> ## Questions and gates
>
> - **Every `AskUserQuestion` question must be self-contained.** The user is reading chat scrollback. They should not have to scroll up to understand the question. Include the relevant Circle name, file path, task title, or context inside the question text itself.

**After:**

> ## Questions and gates
>
> - **A response moment is either a question or an explicit "nothing to decide".** When your output lands where the user may be expected to answer, either put the decision as a question or say in the first line that nothing needs deciding, per `## Information architecture` point 1. Output that does neither reads as a demand the user cannot locate.
> - **Every `AskUserQuestion` question must be self-contained.** The user is reading chat scrollback. They should not have to scroll up to understand the question. Include the relevant Circle name, file path, task title, or context inside the question text itself.

**The record's premise for this item is half wrong, and the correction changes the entry.**
The record states that `## Information architecture` point 1 *"governs status reports and is
not referenced from `## Questions and gates`"*. The second half is true. **The first half is
false**, and this is a Tier 1 check I ran:

```
$ sed -n '5p' rules/user-facing-output.md
Every piece of output the user reads — status reports, gate prompts, `AskUserQuestion` text,
session summaries, error messages, skill confirmations, activation banners — must be
self-contained, plain-English, and action-first.

$ sed -n '58p' rules/user-facing-output.md
## Information architecture (in this order)
```

The file's opening sentence names gate prompts and `AskUserQuestion` text among the output every
clause governs, and `## Information architecture` carries no narrowing scope of its own. Point 1
therefore **already binds gate prompts today**. The gap is salience, not binding.

**So this entry is redundancy bought for visibility, and it should be judged on that.** It is
348 bytes added to a corpus that carries a standing cleanup request
(`hooks/lib/__tests__/rules-emission-golden.test.ts:418-432`), to restate a binding that
already exists. I still recommend it, for one reason the record does not give: the gap that
actually bites is not that point 1 goes unreferenced, it is that **nothing tells an agent to
decide which of the two shapes a moment is**. Point 1 covers the nothing-to-do case;
`## Questions and gates` covers how to write a question once you have decided to ask one.
Neither covers the choice between them, and the reported sample fell in exactly that gap. The
wording above is aimed at the choice rather than at the cross-reference, which is why it earns
its bytes where a bare pointer would not.

`confidence: inference` on the recommendation. `confidence: verified` on the premise
correction, which is the two commands above.

---

## 7. What this run does not propose, and one thing it refuses

**No test, lint or enforcement mechanism is proposed for either clause.** Decision record
`260816-0740_a_does-the-prose-register-get-a-measurable-gate-and-which-surface-does-it-measure.md`
was answered as **option 4** by the user at an orchestrator gate on 2026-08-16: no measurable
gate is built now. Both entries are normative text and nothing else. That record's third
constraint also warns that a rule file explaining a gate would raise the number the gate
measures; the same logic prices these 758 bytes and is why both entries were written short.

**Neither entry is offered at a gate by me.** Under `agents/curator.md` `## Evidence tiers`, a
change with no tier is a candidate, and *"a candidate is never offered for approval and is
never applied."* The dispatcher holds the gate and may put them to the user anyway. If the user
approves either, that approval is the user overriding the curator's evidence requirement for
this run, and it should be recorded as such rather than as a tiered change. I name it rather
than route around it: the evidence rule exists so that I never invent normative text on my own
authority, and it is doing its job here, because the authority for this text is the user's
report and not the project's history.

**Coder work implied by neither entry.** No derivation, no helper, no fixture. The universal
core's byte figures are pinned in `hooks/lib/__tests__/rules-emission-golden.test.ts`, but that
test measures growth against a budget rather than pinning an exact size, and 758 bytes sits
inside the 8 870 of head-room, so **no fixture regeneration is required and the suite stays
green.** Verified by the arithmetic in section 2.

**One incidental citation drift, reported and not filed.** Finding 13 cites the eight-line gate
cap as `rules/user-facing-output.md:101`; it is at `:99` at this HEAD. Analyses are not one of
my three surfaces and I propose no edit. This is one more instance of open defect
`260808-0030_o_line-number-citations-into-rule-files-go-stale-and-no-gate-reads-them.md`.

**A side effect worth one line.** Adding 132 words with no em-dash moves this file from
38 em-dashes per 2 563 words (14.8 per 1000) to 38 per 2 695 (14.1). That is noise against a
stated ceiling of 1 and does not touch defect
`260816-0740_o_the-always-on-rule-corpus-runs-at-sixteen-times-the-em-dash-ceiling-it-states.md`.
The queued repunctuation sweep is unaffected by either entry: both arrive at zero.

## 8. Outcomes

**Apply pass, 2026-08-16.** Dispatched with `**Mode:** apply`,
`**Ledger:** shared/history/260816-1251-curator-run.md`, `**Approved:** L01, L02`.

| Entry | Outcome |
|---|---|
| L01 | applied |
| L02 | applied |

The before-text of each entry was re-read from disk immediately before its write. Both matched
the ledger byte for byte, so neither entry was stale. No entry was skipped and none failed.
Both texts were applied in the wording the ledger carries, with no edit made on this pass.

## 9. The user's override of the evidence requirement

**Both entries were applied as candidates carrying no evidence tier, and neither is
retroactively assigned one.** The classification in section 6 stands unchanged and for the
unchanged reason: my remit changes a normative statement for a cross-surface contradiction or
for history-grounded obsolescence, authoring a new normative requirement is neither, and so
`## Evidence tiers` leaves me no tier to assign. Section 7 refused to offer either entry at a
gate on exactly that ground.

They were applied because **the user overrode that evidence requirement for this run**. The
override is the user's own act and not the orchestrator's. The orchestrator held the gate and
proxied the question, which is the dispatched path in `agents/curator.md` `## Tool Discipline`;
the decision recorded here is the user's.

- **Gate:** an orchestrator gate on 2026-08-16, the same gate section 7 anticipated when it
  wrote that the dispatcher may put the candidates to the user anyway.
- **What the user saw before approving:** both proposed texts in full, and the tier reasoning
  above, including the statement that neither entry carries a tier.
- **What the user weighed on L01:** the design objection in that entry. A per-option foreclosure
  costs roughly one line per option in a chat-text gate, against a line cap that is in dispute
  between `rules/user-facing-output.md:99`, which says 8 lines, and the always-on chat profile,
  which says 6, unresolved in defect `260814-1419_o_*`. The user was offered the narrowed
  variant with no chat branch and took the clause with the chat branch in it.
- **What the user weighed on L02:** 348 bytes restating a binding that already exists, together
  with my verified premise correction, that the file's opening sentence already names gate
  prompts, so point 1 of `## Information architecture` binds them today and the gap is salience
  rather than binding. The approval rests on my recommendation, whose ground is that nothing
  tells an agent to decide which of the two shapes a response moment is. It does not rest on the
  missing cross-reference, which was the record's ground and which the correction removed.

**This is recorded as an override rather than as a tiered change**, which is what section 7
asked for in advance. The evidence rule did its job: the authority for these 758 bytes is the
user's report and this approval, not the project's recorded history, and the ledger now says so
where a later reader looks for it.

One accuracy note, because it bears on how much this account is worth. I did not observe the
gate. What I record of it is what the apply dispatch reported to me.

## 10. Verification after the apply pass

**Placement.** Both bullets sit in `rules/user-facing-output.md` `## Questions and gates` at the
positions the ledger specifies. L02 is the section's first bullet, now line 89, directly under
the heading and before the self-contained-question bullet. L01 is the section's last bullet, now
line 96, directly after the default-options bullet, with the blank line and `## Length` following
it unchanged. Both fit the surrounding list without any adjustment to their wording.

**Em-dashes in the two new bullets: 0.** Counted with

```
$ sed -n '89p;96p' rules/user-facing-output.md | grep -o '—' | wc -l
       0
```

**Byte growth of the file: +758**, from 16 788 to 17 546, and +2 lines, from 186 to 188. That is
the sum the ledger forecast, 410 for L01 and 348 for L02, with no rounding.

**Head-room against the universal-core growth budget: 8 112 bytes.** The five always-on files now
measure 90 461 bytes:

```
$ wc -c rules/agent-setup.md rules/fusion-workbench-conventions.md \
        rules/decision-record-examples.md rules/user-facing-output.md \
        rules/critical-stance.md
    3499 rules/agent-setup.md
   55184 rules/fusion-workbench-conventions.md
    4291 rules/decision-record-examples.md
   17546 rules/user-facing-output.md
    9941 rules/critical-stance.md
   90461 total
```

Against `RULE_BASELINE` 86 573 that is growth of 3 888 on a `GROWTH_BUDGET` of 12 000, leaving
8 112. It matches section 2's forecast exactly.

**Verification:** `cd hooks && npm test` exits **1**. 763 of 764 tests pass; one fails.

**Section 7 was half wrong about the suite, and the correction belongs here rather than in a
silent edit.** Its claim that "no fixture regeneration is required and the suite stays green"
holds for the growth budget, which has 8 112 bytes of head-room and never came near failing. It
does not hold for the golden fixture. `hooks/lib/__tests__/rules-emission-golden.test.ts` also
carries a checked-in per-agent emission table pinning the **exact** byte size of every emitted
rule file, and that table went red on the first agent it compares:

```
FAIL  lib/__tests__/rules-emission-golden.test.ts > rules emission golden
      > matches the checked-in golden, agent by agent
-   user-facing-output.md 16788
+   user-facing-output.md 17546
-   total 94537
+   total 95295
```

A budget with head-room and a fixture pinning an exact size are two different gates over one
number, and the entry's reasoning only priced the first.

**The regeneration is coder work and is deliberately not done on this dispatch.** `hooks/` is
outside my remit under exclusion 6, and the dispatch instructed that the fixture be left red: a
repunctuation sweep over this same file is queued directly behind this run and will move its
size again, so one regeneration after both passes costs less than two. The failure is loud
rather than silent, so what this note adds is the owner and the sequencing, not the warning.
The regenerating command is named in the header of that test file. The user sequences it.

---
**Note appended 260821-0322** (coder, plan step 16 of
`circles/260820-2051-style-rules-arrive-and-get-measured/planning/260820-2324_*_plan-style-rules-arrive-and-get-measured.md`).
The run above is not rewritten. This note records that one premise it reasons from has been
superseded.

**The superseded premise.** Two passages, the design objection under L01 and the gate record under
section "What the user weighed on L01", price a per-option foreclosure against a line cap they
describe as **in dispute**: `rules/user-facing-output.md` says 8 lines for a gate prompt, the
always-on chat profile says 6, and nothing said which governed. The dispute is over. The rule owns
the numbers alone and the chat profile states none.

**What holds now**, read at HEAD `86edaac`. `rules/user-facing-output.md` `## Length` gives gate
prompts at most 8 lines, `AskUserQuestion` question stems at most 6, an `AskUserQuestion` option
`description` at most 2, and a chat reply at most 12. `fusion-workbench/stilwerk/chat-voice-de.yaml`
entry C04 now points at that section by name and does not repeat a number, and the English profile
matches it. The same rule's `## Questions and gates` section prices the foreclosure explicitly, at one line of
question stem plus three option lines plus three foreclosure lines, seven against the cap of eight,
which is the arithmetic this run left to the user.

**Consequence for what is recorded above.** The clause the user approved as L01 fits the cap it was
weighed against; the objection stands as filed and its "currently in dispute" no longer describes
the surface. The recommendation this run made, to keep the chat branch and resolve the cap in
`shared/issues/260814-1419_*_the-tightened-chat-profile-caps-contradict-the-length-section-of-the-rule-that-owns-them.md`,
was followed on both halves.
