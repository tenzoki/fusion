# Curator run — 260829-1114

---
**Status:** Complete. Survey and apply passes done; outcomes in `## 7`.
**Mode:** survey, then apply (same file)
**Scope:** anchored. `bin/fusion-cadence-anchor get last_curator_run` printed `65cf23be659b14bf82c9dc07c97a935e30be4712`; git history is read as `65cf23be..HEAD` per file, records by stamp or mtime after 2026-08-28 07:01 (the anchor commit's date), the archive store for sweeps since it.
**Date:** 2026-08-29
**HEAD read:** `7a2361aa27582706bcca9f8ff95ea4b976eb81e7` (v10.19.2) plus the then-uncommitted archive sweep `archive/260829-1110-safe-cleanup-tier-1`; the pipeline committed that sweep, the 260828-0846 reconciliation and the activity log as `733bce6c..4727506d` during the survey, and `last_curator_run` was set to `4727506d`, which holds everything this run read
**Previous curator run:** `shared/history/260828-0049-curator-run.md`, 2026-08-28, HEAD `36cd5744b5c910bb42b4e34def6589fa63ea8e32`, scope full.
**Dispatched by:** the `CLAUDE.md` step of `/fusion:cleanup` (survey). This agent held no gate; the four gate items are returned to the dispatcher.
**Domain:** code
**Filed by:** curator, Kai Stalmann <ks@qantr.com>

---

## 1. Evidence-source counts

Everything under the bound is what changed since the anchor; the three surfaces themselves changed in no commit of the interval (`git log --oneline 65cf23be..HEAD -- CLAUDE.md rules/` prints 0 lines), so the previous run's full read of them stands.

| # | Source | Corpus | Read this run |
|---|---|---|---|
| 1 | Circle records | 20 records (`ls circles/*/*_circle.md`) | 1, the new anticipated Circle `circles/260828-2342-citation-form-drops-store-segment/_a_circle.md` (the only Circle record touched in the interval) |
| 2 | Decision records | 66 in `shared/decisions/`, 80 in `circles/*/decisions/` after the sweep | 5 new `_a_` records `260828-0904_*` read in full; the 18 records the triage commit `05ad24bf` annotated read through that commit's diff; the tail of `260819-1645_i_what-defines-the-citation-gates-corpus…` |
| 3 | Session histories | 355 shared, 423 in Circles | 7 new since the anchor listed; `260828-0846-orchestrator-session.md` read in full (three `## Coherence` sections, no design-decision section); the head of the previous run file |
| 4 | git | `git log 65cf23be..HEAD`: 11 commits | all 11 subjects and the full `--stat`; per-file log on the three surfaces: 0 commits |
| 5 | Reviews and analyses | 34 reviews, 23 analyses | 0 reviews (none new); 1 analysis, `260828-0859-citation-bookkeeping-defect-report-measured-against-fusions-own-corpus.md` (headings and Instance 1) |
| 6 | `orchestrator-events.jsonl` | 2562 lines, 28 added in the interval | counted only; corroborating, not cited |
| 7 | Archive store | 4 sweeps; 1 since the anchor | `archive/260829-1110-safe-cleanup-tier-1/MANIFEST.md` read in full (151 files, 0 Circles) |
| 8 | `**Provenance:**` headers | 18 of 18 rule files carry one | all 18 resolved; the 7 that cite a record resolve to markers `a` or `i`, none to `_s_` |

Unreadable: none. Absent: `.claude/rules/` (there are no project-wide rule files; `./rules/` is the plugin's own rule directory, which in this repository is the project-owned surface).

## 2. Surface sizes (before; no apply pass has run)

| Surface | Files | Bytes | Lines |
|---|---|---|---|
| `CLAUDE.md` | 1 | 80 363 | 149 |
| `rules/*.md` | 18 | 207 481 | 2 220 |
| `.claude/rules/` | 0 | 0 | 0 |
| `shared/decisions/` | 66 | 464 360 | 6 140 |
| `circles/*/decisions/` | 80 | 558 879 | 7 065 |

Commands: `wc -c`/`wc -l` over each set. Proposed deletions: 0 bytes on every surface, so the blast-radius stop is not reached.

## 3. Comparison counts

Each verdict covers the pairs the rule reached, not the corpus.

| Pair | Selection rule | Produced | Read | Result |
|---|---|---|---|---|
| decision / decision | each record whose marker moved in the interval (8 to `_a_`, 13 to `_d_`, 5 new) against the records its own body or the triage annotation cross-references | 8 | 8 | 1 pair: `260819-1645_i_` (archived citation targets resolve through `unsweep()`) against `260828-0904_a_is-an-archived-record-a-citation-target.md` (option 3, moot under the storeless form). The superseding record exists, so the marker move is the reconciler's reactive supersession, not mine (`## 5`, C02) |
| decision / rule file | every rule sentence the 8 newly answered records name, plus `grep -nE '[0-9]{6}-[0-9]{4}.{0,160}(in\|under) .\$SCAN_' rules/*.md` | 4 | 4 | L02, L03 (Tier 2, offered); C01 (candidate) |
| decision / `CLAUDE.md` | every `CLAUDE.md` citation of a record whose marker moved in the interval (`grep` of the 21 stamps) | 1 | 1 | L01 |
| rule / rule | each emission-target statement in `rules/*.md` against the `case` tables in `bin/fusion-rules` | 6 | 6 | 0 contradictions |
| rule / `CLAUDE.md` | the same six emission lists as stated in `CLAUDE.md` `## Conventions` and the Layout table, against the rule text and `bin/fusion-rules` | 6 | 6 | 0 contradictions |
| `CLAUDE.md` / itself | not re-read this run: the file is byte-identical to the one the 260828-0049 full pass read (`git log` on it is empty across the interval) | 0 | 0 | no new claim |

Tier 1 sweep over both surfaces, run this pass: every wildcarded record citation in `CLAUDE.md` and `rules/*.md` resolves in the live tree except the fictional example `260716-1910_*_plan-foo.md` in `rules/circle-records.md:238` (an exhibit, not a pointer); every cited `bin/`, `hooks/`, `docs/`, `skills/`, `agents/` path exists except three the text itself names as removed or consumer-side (`bin/fu`, `bin/fusion-state-drift`, `rules/context-manifest.yaml`); the four version surfaces all read `10.19.2`; the five `skills/*:line` citations in the conventions file still point at the lines they describe; `agents/*.md` counts 15. Four bare stamps in the surfaces name no live record: `260810-0844` and `260817-1907` are a session and a sweep, not records; `260811-0109` and `260811-1712` were moved to `archive/` by this run's sweep, which under `260828-0904_a_is-an-archived-record-a-citation-target.md` (option 3) is not a dangling citation.

## 4. Pre-edit content

No decision record is proposed for modification in this run. Nothing to record.

## 5. Ledger

### L01 — `CLAUDE.md` says the em-dash-ceiling record "stays open"; it was deferred on 2026-08-29

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 42, the `bin/fusion-prose-metric` row)
- **Tier:** 2 (also Tier 1: `ls circles/260820-2051-style-rules-arrive-and-get-measured/decisions/ | grep em-dash` prints `260820-2314_d_is-the-em-dash-ceiling-read-per-file-or-across-the-always-on-corpus.md`)
- **Citation:** commit `05ad24bf` ("triage the eighteen open decisions left in terminal Circles") renamed the record `_o_`→`_d_` and appended `Deferred: a later Circle that meets the question again — user 2026-08-29`. The sentence it overturns is the one in **Before**.
- **Consequence group:** tier-2
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> Whether the ceiling is read per file or across the always-on corpus is answered per file, by the orchestrator during an unattended run and not by the user, and the record stays open so the user meets it as a live question (`fusion-workbench/circles/260820-2051-style-rules-arrive-and-get-measured/decisions/260820-2314_*_is-the-em-dash-ceiling-read-per-file-or-across-the-always-on-corpus.md`).

**After:**
> Whether the ceiling is read per file or across the always-on corpus is answered per file, by the orchestrator during an unattended run and not by the user; the record was deferred on 2026-08-29 to a later Circle that meets the question again (`fusion-workbench/circles/260820-2051-style-rules-arrive-and-get-measured/decisions/260820-2314_*_is-the-em-dash-ceiling-read-per-file-or-across-the-always-on-corpus.md`).

### L02 — the conventions file places one of fusion's own decisions "in `$SCAN_DECISIONS`"

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md` (line 218, `## Project language`)
- **Tier:** 2
- **Citation:** `shared/decisions/260828-0904_a_are-shipped-record-citations-provenance-or-pointers-for-a-consuming-agent.md`, `Answered:` line, option 1: shipped record citations "name fusion's own workbench and never a consumer resolver key". The sentence it overturns is the parenthesis in **Before**; the open defect `shared/issues/260828-0900_*` lists this line as one of its twenty-one and agrees rather than contradicts. The full storeless filename follows `260828-0904_a_does-the-mandated-citation-form-include-the-store-segment.md`, option 1.
- **Consequence group:** tier-2
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Overlap:** the anticipated Circle `260828-2342` plans this same rewrite among its twenty-one lines; applying it here costs the Circle nothing and removes one line from its sweep. +91 bytes on the always-on floor; `hooks/lib/__tests__/fixtures/rules-emission.golden` pins that set's sizes, so if `npm test` reddens after apply the regeneration is coder work (`## 8`).

**Before:**
> decision `260827-1056` (in `$SCAN_DECISIONS`) fixed this partition and what the core above must carry.

**After:**
> decision `260827-1056_*_which-parts-of-the-language-and-backlog-rules-does-every-dispatch-still-carry.md`, a record in fusion's own workbench, fixed this partition and what the core above must carry.

### L03 — the review contract places one of fusion's own issues "under `$SCAN_ISSUES`"

- **Surface:** project rule file
- **File:** `rules/review-contract.md` (line 45)
- **Tier:** 2
- **Citation:** the same `Answered:` line of `260828-0904_a_are-shipped-record-citations-provenance-or-pointers-for-a-consuming-agent.md`, option 1; the sentence overturned is the last one of the paragraph in **Before**. Listed in `shared/issues/260828-0900_*` (`rules/review-contract.md:45`). The record itself now sits in `archive/260829-1110-safe-cleanup-tier-1/shared/issues/`, which the storeless form makes irrelevant to the citation.
- **Consequence group:** tier-2
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/review-contract.md`
- **Overlap:** as L02. Conditional emission (`IS_REVIEWER_AGENT`), so not on the always-on floor.

**Before:**
> The record is `260810-1205` under `$SCAN_ISSUES`.

**After:**
> The record is `260810-1205_*_seven-of-sixteen-commits-in-the-session-range-never-reached-a-review-pass-and-nothing-measures-the-gap.md`, in fusion's own workbench.

### C01 — `## Filename Patterns` and `rule-file-provenance.md` form 1 do not yet state the storeless citation form (candidate, not on offer)

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md` `## Filename Patterns` ("Cite a record by its full filename…", "measured 260824 over 876 records"); `rules/rule-file-provenance.md` form 1 (store-prefixed example)
- **Tier:** 2 evidence exists: `260828-0904_a_does-the-mandated-citation-form-include-the-store-segment.md` (option 1) and `260828-0904_a_should-the-uniqueness-claim-state-its-scope.md` (option 1).
- **Consequence group:** candidate
- **Why a candidate:** the implementation is owned by the anticipated Circle `circles/260828-2342-citation-form-drops-store-segment/_a_circle.md`, whose Directive lands the rule text, the uniqueness test, the three gates and the archive filter together, and the store-segment record's own `## Constraints` requires the gates to recognise the form. A lone rule edit today would make the conventions file forbid the store-prefixed form its own six citations and `rule-file-provenance.md` form 1 use, and the uniqueness figure with its scope and commit is a measurement the Circle re-takes at planning (its `### The four clarifications`). Reported so the Circle's planner can read it; nothing to approve.
- **Constraint removed:** none
- **Revert path:** n/a

### C02 — `260819-1645_i_` is overturned by `260828-0904_a_is-an-archived-record-a-citation-target.md` (reconciler's move)

- **Surface:** decision record
- **File:** `circles/260819-1645-four-constraints-on-deep-change/decisions/260819-1645_i_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md`
- **Consequence group:** candidate
- **Why a candidate:** a superseding record already exists (option 3, "the gate needs no archive lookup"), so this is the reactive supersession the reconciler owns, and the `unsweep()` it implemented is still in the code at HEAD until the Circle removes it. The `Superseded by:` annotation and the `_i_`→`_s_` rename belong to that moment. Edit neither.

### C03 — `rules/agent-setup.md` lacks the `OUT_*`-joins-to-`$WORKBENCH` line an open defect asks for (out of remit)

- **File:** `rules/agent-setup.md` `## What fusion-paths emits`
- **Why not mine:** `shared/issues/260828-1041_o_a-coder-dispatch-wrote-its-history-entry-to-shared-history-at-the-project-root.md` asks the rule to gain a constraint. That is a defect fix adding a statement, not a contradiction or an obsolescence; owner `coder`, through the issue. No new defect record filed, since the requirement is already on file there.

## 6. Contradictions filed as decision records

None. No pair of live, defensible positions was found; the one live pair (C02) has its superseding record.

## 7. Outcomes

Apply pass, 2026-08-29, dispatched by the `CLAUDE.md` step of `/fusion:cleanup` with `Approved: all`, HEAD `4727506dbc2117ebb32de355f62ca099ad96853c`. Each entry's before-text was re-read from disk (one exact match each) and the written region compared against the After block (one exact match, before-text gone).

- L01 — applied (`CLAUDE.md` line 42)
- L02 — applied (`rules/fusion-workbench-conventions.md` line 218)
- L03 — applied (`rules/review-contract.md` line 45)
- C01, C02, C03 — candidates, never on offer, not applied

Surface sizes after: `CLAUDE.md` 80 383 bytes / 149 lines (+20); `rules/*.md` 207 692 bytes / 2 220 lines (+211: conventions 50 719, review-contract 6 813); `.claude/rules/` 0; decision records unchanged. Removals by tier: 0. Constraints removed: none.

Revert: `git checkout -- CLAUDE.md rules/fusion-workbench-conventions.md rules/review-contract.md`. Working-tree edits only; nothing committed.

## 8. Work outside this remit, named

- **Confirmed after apply:** `hooks/lib/__tests__/rules-emission-golden.test.ts` fails (run with `npx tsx --test` in `hooks/`) because L02 moved `rules/fusion-workbench-conventions.md` by +97 bytes. Regenerating `hooks/lib/__tests__/fixtures/rules-emission.golden` is coder work; not run here.
- If L02 is applied and `npm test` reddens on `hooks/lib/__tests__/rules-emission-golden.test.ts`, regenerate `hooks/lib/__tests__/fixtures/rules-emission.golden` per that test's own instructions: coder work.
- The remaining nineteen `$SCAN_*` self-citation lines (`agents/orchestrator.md`, `agents/curator.md`, `agents/planner.md`, `skills/archive/SKILL.md`, `skills/next/SKILL.md`) are agent prompts and skill bodies: owner `coder`, tracked in `shared/issues/260828-0900_*` and `260828-0907_*`, planned by Circle `260828-2342`.
