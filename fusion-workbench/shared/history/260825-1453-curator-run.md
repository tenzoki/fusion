# Curator run — 260825-1453

---
**Status:** Complete. The survey pass finished 260825-1456; the apply pass finished 260825-1654 and appended its per-entry outcomes to `## 7`. Three entries proposed, three approved at the gate, three applied, none stale, none failed. The five candidates in `## 6` were neither offered nor applied. One consequence of an applied edit falls outside this agent's remit and is named in `## 7` as coder work.
**Mode:** survey, then apply. Two dispatches, one run file: the survey pass wrote `## 1` to `## 6`, `## 8` and `## 9`, and the apply pass wrote `## 7` and the after columns of `## 2`.
**Date:** 2026-08-25
**HEAD read:** `3d4b1816a2397a4a351ae11a319e0143e6fd078a` (plugin.json 10.7.0)
**Previous curator run:** `shared/history/260821-1244-curator-run.md`, 2026-08-21, HEAD `2f654fe04373d17e167bf0cdf1f1298a13b818ad` (plugin.json 10.4.0). The two before that were `shared/history/260818-2050-curator-run.md` (HEAD `e14b6ca`) and `shared/history/260817-1925-curator-run.md` (HEAD `e59dea2`).
**Dispatched by:** the `CLAUDE.md` step of `/fusion:cleanup`, twice. The first dispatch surveyed and returned the gate question; the dispatcher put it to the user and re-dispatched with the approvals, which is the pass `## 7` records. The dispatcher held the gate; this agent never prompted.
**Domain:** code
**Filed by:** curator, Kai Stalmann <ks@qantr.com>

**Working tree at survey time.** `git status --porcelain` shows four modified and four untracked files, all under `fusion-workbench/`: this session's own history, the reconciler's appended notes on three issue records, and four records this session filed. No file this run proposes to change is dirty. `CLAUDE.md` and every file under `rules/` are clean at HEAD.

**What changed in the interval.** 142 commits, `2f654fe..3d4b181`, shipping v10.5.0, v10.6.0 and v10.7.0. On the three surveyed surfaces: `CLAUDE.md` moved by 23 lines across five commits, ten rule files were modified, and one rule file was added, `rules/review-contract.md` in `181dd8a` (2026-08-22), taking the shipped rule corpus from 13 files to 14. That single addition is the direct cause of entry L02 below: it made a count in `CLAUDE.md` false without touching `CLAUDE.md`.

**The suite is green at HEAD.** `cd hooks && npm test` → 43 files, 760 tests, all passing. That is load-bearing for this survey rather than incidental: `reference-resolution-lint.test.ts` resolves every plugin-file path and every adjacent heading anchor in the shipped text, `workbench-citation-lint.test.ts` resolves every record citation in the live workbench corpus, and `derivable-enumerations-lint.test.ts` pins, in both directions, `CLAUDE.md`'s skill listing against `skills/*/`, its agent counts against `agents/*.md`, its `DEFINITION_SITES` echo, and its `bin/` helper roster. Every claim those gates cover is therefore verified true at HEAD without this pass re-deriving it, and the three Tier-1 entries below are all in the gaps between them: a file list no gate closes, a count of a conditional-emission set, and a relation between releases and documents.

---

## 1. Evidence-source counts

| # | Source | Read |
|---|---|---|
| 1 | Circle records, `$SCAN_CIRCLES` | 18 Circle directories enumerated; 18 `*_circle.md` records present. Read at record level for the four `_a_` decisions of `circles/260821-1042-reply-bounded-whole-question-answered/`, which is the Circle whose Grounding routes work to this pass. |
| 2 | Decision records, `$SCAN_DECISIONS` and every Circle store | **138** records enumerated by path segment (`*/decisions/*.md`), 1 090 890 bytes: 22 `_o_`, 24 `_a_`, 86 `_i_`, 3 `_s_`, 3 `_d_`. All 24 `_a_` filenames read; 12 selected `_a_`/`_i_` pairs read in full at the answer level; every `**Provenance:**` and `Binding decision:` citation in the 14 rule files resolved to its record and its marker read off the resolved filename. |
| 3 | Session histories, `$SCAN_HISTORY` | 728 files enumerated. Read: the previous curator run's head (`shared/history/260821-1244-curator-run.md`) and the citations reached from decision footers. The current session's own history was **not** read as evidence: an unrecorded session fact is not a change this agent may propose. |
| 4 | Git | `git log --oneline --follow` on `rules/design-diagrams.md`, `rules/review-contract.md`, `rules/commit-lock.md`; `git log -S` on the `CLAUDE.md` sentence in L02; `git show 1a36fe4:bin/fusion-rules`; `git tag` in full; `git diff --stat` and `--name-status` over the interval; `git log -1 --format` on six commits. Full history per file, not bounded by the previous run's date. |
| 5 | Reviews and analyses | `$SCAN_REVIEWS` 101 files enumerated, `$SCAN_ANALYSES` 33 enumerated. **Neither was opened.** No entry below rests on either, and no verdict here covers them. This is the largest single gap in this survey. |
| 6 | `orchestrator-events.jsonl` | Present, 358 110 bytes. **Not read.** Corroborating-only by mandate, and nothing here needed corroboration it could give. |
| 7 | Archive store, `$WORKBENCH/archive` | 1 archived Circle (`260817-1907-safe-cleanup-scoped`), 290 markdown files, of which 16 are decision records. Enumerated; not read file by file. No entry below rests on an archived record. |
| 8 | `**Provenance:**` headers | **14 of 14** rule files read. All 14 carry the header on line 3, inside the ten-line window `rules/rule-file-provenance.md` defines. No header cites a record carrying the `_s_` marker, so no rule file is a Tier-2 retirement candidate on that ground. |

**Sources reported zero:** none was empty. **Sources unreadable:** none. **Sources deliberately not opened:** 5 and 6, named above with what that costs.

**Surfaces.** Decision records: readable, 138 files. Project-owned rule files: `./rules/` readable, 14 files; `.claude/rules/` **absent** (`ls: .claude/rules/: No such file or directory`), which is a measurement and not a fault. `CLAUDE.md`: readable, read in full.

**One note on which rule directory is this project's own.** This repository is the fusion plugin's source, not a fusion-consuming project, so `./rules/` here **is** the project's own rule directory and exclusion 7 of this agent's mandate does not apply to it. `bin/fusion-rules` confirmed the work-tree preference is in force: it emitted `/Users/k1/Projects/productive/fusion/rules/…`, not `$FUSION_PLUGIN_ROOT/rules/…`. `rules/rule-file-provenance.md` was nevertheless read from `$FUSION_PLUGIN_ROOT` as the mandate requires, and `diff` reports it byte-identical to the work-tree copy, so the documented residual is zero on this run.

---

## 2. Surface sizes

| Surface | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|
| `CLAUDE.md` | 72 018 | 143 | **72 311** | **143** |
| Project rule files (`rules/*.md`, 14 files) | 191 969 | 2 134 | unchanged | unchanged |
| Decision records (138 files) | 1 090 890 | not counted | unchanged | unchanged |

No entry in this ledger touches a rule file or a decision record. All three proposed changes land in `CLAUDE.md`.

**Measured after the apply pass.** `CLAUDE.md` moved 72 018 -> 72 311 bytes, **+293**, over the same 143 lines: every edit replaced text inside an existing line and no line was added or removed. The rule files stand at 191 969 bytes over 2 134 lines and the decision records at 1 090 890 bytes, both **byte-identical to the survey measurement**, and `git status --porcelain rules/` is empty. The `## 8` estimate of +471 bytes was wrong by 178: it overstated the three After blocks. Measured per entry, L01 is +150, L02 is **-6** and L03 is +149. The direction the blast-radius verdict rested on is unchanged, a net addition with nothing deleted, so the verdict stands and the arithmetic under it did not.

**What that spares, and it is worth naming.** Every file under `rules/` has its byte size pinned per agent in `hooks/lib/__tests__/fixtures/rules-emission.golden`, and `rules-emission-golden.test.ts` asserts `render(a, measured) === golden.get(a)` for each of the 15 agents. A one-byte edit to any rule file turns `npm test` red until the golden is regenerated with `UPDATE_RULES_GOLDEN=1`, which is coder work this agent may not do. Because this ledger proposes no rule-file edit, **no test regeneration is owed by any entry below.** `CLAUDE.md` is pinned by no fixture; `derivable-enumerations-lint` reads it, but only for the skill tokens, the agent counts, the `DEFINITION_SITES` echo and the `bin/` roster, none of which any entry touches.

---

## 3. Comparison counts, per surface pair

Each row states the selection rule, what it produced, what was read, and what the verdict therefore covers. No row claims an exhaustive comparison, because on this corpus none was performed.

**1. Decision × decision.** Corpus 138 records; binding set (`_a_` + `_i_`) = 110; possible pairs 5 995. **Rule:** tokenise each filename slug, drop stop-words and tokens of three characters or fewer, and select a pair when the two slugs share **three or more** remaining tokens. **Produced 12 pairs; 12 read** at the `## Answer` level. Ten are sequential refinements inside one Circle or one topic and agree by construction; two were checked in full and are coherent (`260815-2312_i_` / `260818-2212_i_` on the `**Status:**` field, the second citing the first as precedent; `260810-0921_i_` / `260810-1544_a_`, a deliberate split of one record's parts, with part (c) still unanswered by design). **No live record overturns another among the 12 pairs this rule reached.** A pair of records that contradict each other while sharing fewer than three slug tokens is outside what this rule can see, and nothing here rules one out.

**2. Decision × rule file.** **Rule:** for each of the 14 rule files, resolve every `**Provenance:**` and `Binding decision:` citation and read the marker off the resolved filename; separately, read all 24 `_a_` records and match each against the rule file whose topic it names. **14 of 14 files' citations resolved; 24 of 24 `_a_` records matched.** No provenance citation resolves to an `_s_` record. One `_a_` record's answer prescribes future behaviour that two rule sentences will falsify once it is built; see `## 6, verified and dropped`, lead 1. No contradiction found.

**3. Decision × `CLAUDE.md`.** **Rule:** every decision record `CLAUDE.md` cites by wildcarded filename, plus the 24 `_a_` set. `CLAUDE.md` carries **15** distinct record citations; `reference-resolution-lint` resolves all 15 at HEAD, and their markers were read. **One contradiction found**, and it is the one this survey could not resolve by editing either side: entry D01 in `## 5`.

**4. Rule file × rule file.** 14 files, 91 possible pairs. **Rule:** read every file's heading structure, then read in full the files that carry a partition pointer, a cross-file definitional claim, or an emission-audience claim. **All 14 read at heading level; 9 read in full** (`agent-setup`, `critical-stance`, `user-facing-output`, `rule-file-provenance`, `workbench-tracking`, `context-lean-claude-md`, `context-manifest`, plus the four pointer sections of `fusion-workbench-conventions` and the audience paragraphs of `circle-records`). Each of the four partition points defers cleanly to its authoring home and carries no competing definition. **No contradiction found among the pairs this rule reached.** The five files read only at heading level (`circle-records` in full, `commit-lock`, `design-diagrams`, `review-contract`, `workbench-path-resolution`, `decision-record-examples`) are not covered by that verdict beyond their headings and their audience claims, each of which was verified against `bin/fusion-rules`.

**5. Rule file × `CLAUDE.md`.** **Rule:** for each of the 14 rule files, read every `CLAUDE.md` sentence naming it. Counts: `fusion-workbench-conventions` 9, `user-facing-output` 4, `circle-records` 3, `context-manifest` 2, `context-lean-claude-md` 2, `critical-stance` 2, `decision-record-examples` 2, `workbench-path-resolution` 2, `workbench-tracking` 2, `agent-setup` 1, `commit-lock` 1, `review-contract` 1, `rule-file-provenance` 1, `design-diagrams` **0**. **All 32 mentions read.** Every audience claim (`circle-records` to three named agents, `review-contract` behind `IS_REVIEWER_AGENT`, `design-diagrams` to four, `decision-record-examples` as one of five always-on) was checked against `bin/fusion-rules` and holds. **No contradiction found.** The zero on `design-diagrams` is candidate C04.

**6. `CLAUDE.md` × itself.** 143 lines, 72 018 bytes, **read in full**. Checked the guard account against itself across four separate passages, the `.gitignore`/tracking account against the layout row, the skills account against the administrative-surface claim, and the four version surfaces against each other. **No internal contradiction found.** Three internal claims were falsified against the tree instead, and those are L01 to L03.

---

## 4. Pre-edit content of decision records this run intends to modify

**None.** This run proposes no change to any decision record. It creates one new open record (`## 5`, D01), which is a create rather than a change and needs no pre-edit capture.

---

## 5. The ledger

Three entries, all Tier 1, all on `CLAUDE.md`, all constraint-preserving. No entry removes a constraint. No entry is a consolidation. No entry touches a preserve-list item.

### L01 — `bin/fusion-source-root`'s reader list names four skill bodies and five read it

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (Layout table, the `bin/fusion-source-root` row, line 37)
- **Tier:** 1
- **Citation:** `grep -rln "fusion-source-root" skills/` at HEAD `3d4b181` returns **five** paths: `skills/archive/SKILL.md`, `skills/cleanup/SKILL.md`, `skills/help/SKILL.md`, `skills/next/SKILL.md`, `skills/setup/SKILL.md`. The row names four and omits `archive`. `skills/archive/SKILL.md:39-45` is a guarded `bin/fusion-source-root` call assigning `FUSION_SRC`, used at `:45` to `cat "$FUSION_SRC/rules/workbench-tracking.md"`. `git log --oneline -S"fusion-source-root" -- skills/archive/SKILL.md` names `06ab15b`, dated 2026-08-19 by `git log -1 --format='%ad' --date=short`. The row has therefore been one body short for six days, across three releases.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Derive-over-correct.** The falsified thing is a file list a command produces, so the entry proposes the derivation rather than a fifth name, and keeps the one fact the surrounding sentence needs: that the branch is read once per body instead of being restated in each. Naming the command also makes the next addition self-correcting, which a list of five would not be.

**Before:**
> The root a citation of a plugin-shipped **file** resolves against, read at the first step of `/fusion:setup`, `/fusion:next`, `/fusion:cleanup` and `/fusion:help` instead of each body restating the branch.

**After:**
> The root a citation of a plugin-shipped **file** resolves against, read at the first step of every skill body that opens one, instead of each body restating the branch. Which bodies those are is a list that moves: `grep -rl fusion-source-root skills/` names them, and `skills/archive/SKILL.md` joined the original four on 260819 without this row noticing.

### L02 — the conditional-emission set is counted at three and holds four

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (Conventions, the "No guard rule is emitted any more" bullet, line 67)
- **Tier:** 1
- **Citation:** `grep -n "^[[:space:]]\+emit_if_exists" bin/fusion-rules` at HEAD returns **four** lines: `:473` `design-diagrams.md`, `:491` `circle-records.md`, `:507` `commit-lock.md`, `:516` `review-contract.md`. The same grep against the tree at the commit that last wrote this sentence, `git show 1a36fe4:bin/fusion-rules`, returns **three**. `1a36fe4` is dated 2026-08-14; `rules/review-contract.md` was added in `181dd8a` on 2026-08-22, which is the commit that made the count false. The sentence was correct when written and was falsified by a change to a different file.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Derive-over-correct.** This paragraph already refuses to state the always-on floor and names the command instead, and it says why in its own words: a number written into it is stale before it is committed. The count of conditional files is the same kind of number, and the derivation is already named in the same sentence, the **indented** `emit_if_exists` lines. So the entry deletes the number rather than moving it to four, which is the form the paragraph itself argues for two clauses earlier.

**Before:**
> The indented ones are conditional emissions and are not part of the floor; the same `wc -c` over those three files gives what they add, which has been about a third again.

**After:**
> The indented ones are conditional emissions and are not part of the floor; the same `wc -c` over those files gives what they add, which has been about a third again.

### L03 — the migration notes are claimed one per release and eight cover eleven

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (Layout table, the `docs/` row, line 34)
- **Tier:** 1
- **Citation:** `git tag | sort -V` lists **eleven** releases from v9 forward: v9.0.0, v10.0.0, v10.0.1, v10.0.2, v10.1.0, v10.2.0, v10.3.0, v10.4.0, v10.5.0, v10.6.0, v10.7.0. `ls docs/` lists **eight** notes: `upgrading-to-v9.md`, `-v10.md`, `-v10-2.md`, `-v10-3.md`, `-v10-4.md`, `-v10-5.md`, `-v10-6.md`, `-v10-7.md`. v10.0.1, v10.0.2 and v10.1.0 have none. Corroborated inside the plugin's own shipped text: `skills/help/SKILL.md:107` already tells a reader "not every release has one, and no filename is derivable from a version string", which is the correct statement of the same fact, so the two surfaces disagree and the tree settles it against `CLAUDE.md`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**On the trailing clause.** "which was the first this repository ever shipped" reads two ways. Taken of the note it is true, and taken of the release it is false, because `git tag` runs back to v5.5.0. The After keeps the true reading explicitly rather than deleting the clause, so nothing a reader could have learned from it is lost.

**Before:**
> The `upgrading-to-vN.md` files are per-release migration notes, one per release since v9, which was the first this repository ever shipped.

**After:**
> The `upgrading-to-vN.md` files are migration notes starting at v9, whose note was the first this repository ever shipped. Not every release has one and no filename is derivable from a version string, which is what `skills/help/SKILL.md`'s update topic tells a reader hunting an older one.

---

## 6. Candidates — reported, never offered for approval, never applied

### C01 — `CLAUDE.md`'s register repair is routed to this pass, and no evidence tier here covers it

`circles/260821-1042-reply-bounded-whole-question-answered/decisions/260821-1108_*_is-claude-mds-register-repair-inside-this-circle.md` was answered by the user at a gate on 260821: **option 1**, the repair does not happen inside that Circle, it "goes through `/fusion:cleanup --only claude-md`, which dispatches the curator with a user gate". This run is that dispatch.

`verified:` the repair has not happened. `bin/fusion-prose-metric CLAUDE.md` at HEAD reads 126 prose em-dashes over 9 646 prose words, 13.1 per 1000 against a permit of 9, verdict `over`. Eight of the 14 rule files are over as well.

`verified:` no evidence tier in this agent's mandate covers a register edit. A tier is a claim about evidence, and there is no command whose output makes an em-dash false. Repunctuating a paragraph is a change justified by re-reading the current text, which is the one ground this agent's own rules name as never sufficient.

**Why it is a candidate and not an entry.** The routing is a user's answer and stands; the two-reason rule is this agent's mandate and stands; both are live and defensible, and picking one would be resolving a contradiction this agent is told to file rather than settle. Filed as **D01** below.

### C02 — the two-session shape is stated for agents and not for helpers

`shared/issues/260825-1329_*_every-session-runs-one-release-behind-on-a-bin-helper-the-same-repository-just-added.md` measures a 21-hour window in which `bin/fusion-identity` was in this work tree and not in the install, during which every `[ -x "$FUSION_PLUGIN_ROOT/bin/fusion-identity" ]` call site took its miss branch.

`verified:` `CLAUDE.md` `## Release process` states the mechanism correctly. It says the work-tree preference covers three helpers and "never which helper it runs", and it states the two-session shape for **agents**. What it does not state is the consequence for a helper this repository has just added. Nothing in `CLAUDE.md` is false; a paragraph is silent where it could speak.

**Why it is a candidate.** There is no falsified claim to fix, so no Tier 1. There is no record retiring anything, so no Tier 2. And the class is under an open defect whose own candidate direction 3 is "extend the `CLAUDE.md` two-session paragraph to helpers, which costs nothing and fixes nothing", with none of its three preferred and the governing question, part (c) of `shared/decisions/260810-1544_*_should-prompt-called-bin-helpers-get-one-guarded-call-convention-and-does-the-work-tree-preference-extend-to-them.md`, still unanswered. An open defect contradicting a change is a stop by this agent's own defect cross-check.

### C03 — `.guard-state/`'s leftover enumeration names one of the three this workbench holds

`rules/workbench-tracking.md:30` classifies `.guard-state/` per file, states that the throttle stores "are the two measurement throttle records, and they are the whole of it", and names `escalation.json` as the one leftover a workbench set up under an older fusion may still hold.

`verified:` `ls -la fusion-workbench/.guard-state/` at HEAD lists six files: `churn.json` (109 454 bytes), `escalation.json`, `events.jsonl`, `review-coverage.json`, `staging-drift.json`, `state-drift.json`. Two of them, `churn.json` and `state-drift.json`, are leftovers of mechanisms removed on 2026-08-15 and are unmentioned by the paragraph that exists to classify every file in that directory.

**Why it is a candidate.** The sentence has two readings. Scoped to the module the rule cites, `hooks/lib/guard-state-file.ts:83`, whose own comment says "the files that DO route through here", it is true. Scoped to the directory, which is what the surrounding paragraph is about, it is false. Reporting an ambiguous reading as a checked falsification is the pattern `rules/critical-stance.md` §3 names as the most damaging of the three, so it is measured here and not proposed. The partition is not broken either way: both leftovers are class L, and this repository's `.gitignore` excludes `.guard-state/*` wholesale.

### C04 — `rules/design-diagrams.md` is the one shipped rule file `CLAUDE.md` never names

`verified:` `grep -c "design-diagrams.md" CLAUDE.md` returns 0. The other 13 rule files are named between one and nine times each. `derivable-enumerations-lint.test.ts` closes `CLAUDE.md`'s `bin/` roster in both directions and its skill listing in both directions, and carries no equivalent check for `rules/`, so this is invisible to every gate.

**Why it is a candidate.** `CLAUDE.md`'s Layout table makes no exhaustiveness claim for `rules/`, so nothing is falsified, and adding a row is an addition rather than a correction. It is reported because the asymmetry is the same shape as the two inventory rows that went stale on 2026-08-15 and that the `templates/` and `docs/` rows now record as a known hole.

### C05 — `CLAUDE.md` is 72 018 bytes against a lean-`CLAUDE.md` convention this project authors

`verified:` `wc -c CLAUDE.md` reads 72 018 bytes over 143 lines. `rules/context-lean-claude-md.md`'s worked example holds up a "259 lines, ~43 kb" file as the problem the convention exists to solve, and prescribes an always-on surface of identity, the two language lines, a handful of every-session rules and a pointer table.

**Why it is a candidate and why no entry follows from it.** The rule scopes itself to "a consuming project's `CLAUDE.md`", and this repository's own `CLAUDE.md` opens by saying it is not a fusion-consuming project. And more decisively: a shrink justified by re-reading the file is exactly the removal this agent may never propose. Several passages here describe mechanisms that were deliberately removed and read oddly without their history, which is the case that rule was written for. Measured and left standing.

---

## What was examined and dropped — the dispatcher's four leads

**Lead 1, the two answered `.gitignore` decisions. Examined. No contradiction; nothing proposed.**

`verified:` both records are `_a_` in `shared/decisions/` and were read in full. No sentence in `rules/workbench-tracking.md`, in `rules/fusion-workbench-conventions.md` or in `CLAUDE.md` is false at HEAD because of them. Two sentences **will** be false the moment the repair is built, and they are named here for whoever builds it rather than proposed now, because a sentence that is true today is not a Tier-1 finding:

- `rules/workbench-tracking.md:7` — "Its consumers are a human writing a consuming project's `.gitignore` … and the archive step of `/fusion:cleanup`."
- `rules/fusion-workbench-conventions.md:76` — "its two readers are a human writing a project's `.gitignore` and the archive step of `/fusion:cleanup`."

Both name exactly two readers, and `260825-1030_*_does-setup-repair-a-gitignore…` puts the check in Setup, which makes `/fusion:setup` a third. Where that obligation sits is itself a recorded question, `shared/decisions/260810-1635_*_where-does-the-obligation-sit-to-update-the-artefact-that-explains-a-behaviour-when-the-behaviour-changes.md`.

One thing checked and found sound rather than contradictory: the second record's answer leans on `rules/workbench-tracking.md` `## Whether to track the workbench at all` for the claim that declining to track an R1 store is the project's choice, while the class R1 row of the same file says "Track them." Those coexist. The row says what a tracked workbench tracks; the answer says what Setup repairs. A project may be in breach of the first without the second acting, and the record's own `## Residual` already names the case where that reads oddly.

**Lead 2, the helper window.** Examined; became candidate C02 above. `CLAUDE.md` states the mechanism correctly and is silent on the consequence, which is an incompleteness under an open defect and an unanswered decision, not a change this pass may make.

**Lead 3, the archive safety filter. Examined. The lead's premise about `CLAUDE.md` does not hold; dropped.**

The dispatcher asked whether `CLAUDE.md`'s recorded acceptance "still reads correctly beside an unattended step". `verified:` it reads correctly as an account of the record it cites. `circles/260819-1645-four-constraints-on-deep-change/decisions/260819-1645_*_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md` `Answered:` says the user chose option 1, "zero dangling over the corpus, recomputed on every run … Step 10 of the plan therefore does not exist: `skills/archive/SKILL.md` is not made a writer of records it did not create, and the archive half of the annotation obligation is not built." `CLAUDE.md`'s troubleshooting row says exactly that the cost "was accepted rather than mitigated by" that record. The sentence is a true report of a real answer.

What `shared/issues/260825-1440_o_*` measures is a different and real thing: filter 3 of `skills/archive/SKILL.md` and the "safe-by-construction" claim in `skills/cleanup/SKILL.md` Step 4. Both are skill bodies. Neither is one of this agent's three surfaces, both are owned by `coder`, and the defect is already filed with three candidate directions. Nothing for this ledger.

**Lead 4, the Rebalance gate. Verified real at both sites, and outside this agent's six pairs; dropped from the ledger.**

`verified:` `agents/orchestrator.md` `### Rebalance Gate` says the gate "presents **four explicit options**" and lists four. `rules/user-facing-output.md:97` says "A gate carries at most three options, on whatever surface" and adds that a decision needing a fourth is too big for one gate. Both bind the orchestrator, which loads the rule on every dispatch. The defect record's account is accurate in every particular this pass could check.

It is dropped for a boundary reason, not an evidential one. One side is `agents/orchestrator.md`, an agent prompt. Agent prompts are exclusion 5 of this agent's mandate and are not among the three normative surfaces, so this pair is not one of the six this agent compares. Fixing it by amending `rules/user-facing-output.md` would mean picking a side of a live contradiction, which this agent may not do, and the rule's cap is not falsified by anything: `shared/issues/260825-1259_o_*` shows it is derived from the eight-line budget in `## Length` plus the two foreclosure clauses, so widening it reopens that derivation. `grep -rn "Rebalance\|four options" rules/` finds no rule file mandating four, so there is no rule-file-versus-rule-file instance of it either. The record already names three candidate directions and needs a decision, not a curator edit.

---

## 7. Outcomes

**Apply pass, 260825-1654.** Dispatched with `**Mode:** apply`, `**Ledger:** shared/history/260825-1453-curator-run.md`, `**Approved:** L01, L02, L03`. The user was shown all three entries with their evidence and the blast-radius verdict and approved all three; no entry was rejected, and none of the five candidates in `## 6` was put on offer or approved. Every approved id is carried by this ledger, so no id was refused as unknown.

**The tree the apply pass ran against is the tree the survey read.** HEAD is `3d4b1816a2397a4a351ae11a319e0143e6fd078a`, unchanged, and `git status --porcelain CLAUDE.md` was empty when the pass began. Each entry's before-text was re-read from disk before its write and matched the ledger exactly, occurring **once** in the file in all three cases. **No entry came back stale.**

| Id | Outcome | Post-write verification |
|---|---|---|
| L01 | **applied** | After block present once, before-text residual 0. +150 bytes. |
| L02 | **applied** | After block present once, before-text residual 0. -6 bytes. |
| L03 | **applied** | After block present once, before-text residual 0. +149 bytes. |

**What the post-write check actually checked.** After each write the region was re-read from disk and compared byte for byte against this ledger's After block, which is the check the before-text comparison cannot perform and which a doubled period once got through (`260815-1943`). All three After blocks are present verbatim and exactly once; all three Before texts are gone. `git diff -U0 CLAUDE.md` reports **three changed lines and nothing else**, one per entry, at lines 37, 55 and 67.

The seams were checked separately, because an exact substring match sees the block and not what abuts it. Five punctuation and whitespace faults were scanned for across the whole file and the counts compared against `git show HEAD:CLAUDE.md`: doubled period 3 at HEAD and 3 now, doubled space 4 and 4, space-before-period 3 and 3, doubled comma 0, doubled semicolon 0. Every hit is present at HEAD and every one is a false positive of the scan, from `...` inside code spans, the indented fenced block in `## Release process`, and `claude plugin validate .`. **No seam fault was introduced.**

### One consequence of an applied edit lands outside this agent's remit — coder work, not done here

`hooks/lib/__tests__/reference-resolution-lint.test.ts` **fails at this working tree**, and the cause is entry L01 and entry L03 together.

- **Verified:** `npx vitest run lib/__tests__/reference-resolution-lint.test.ts` reports 56 passed, 1 failed. The failing assertion is `resolved exactly the pinned number of references in each plugin class` at `:525`, expecting `{ paths: 1374, anchors: 192 }` and receiving `{ paths: 1376, anchors: 192 }`.
- **The half that matters passed.** The gate's first test, `passes on the whole surface — no dangling reference of any class`, is green. Both citations the entries added resolve: `skills/archive/SKILL.md` in L01 and `skills/help/SKILL.md` in L03. Nothing dangles; only the count moved, by exactly the +2 those two paths account for.
- **The owner.** `BASELINE` is a hand-approved constant at `hooks/lib/__tests__/reference-resolution-lint.test.ts:479`. It is under `hooks/`, which is exclusion 6 of this agent's mandate, and it is a hook test, which is exclusion 5. **This agent did not touch it and did not run the re-approval.**
- **What the owner does.** There is no regeneration command and no `UPDATE_*` environment variable for this baseline: the file's own `BASELINE_MESSAGE` states that on a legitimate move "RE-APPROVING THE BASELINE IS THE EXPECTED RESPONSE: check the received numbers against the edit you made, then write them into BASELINE in this file and commit that with the edit." The move is legitimate and is the `UP` case that message names, citations added. `coder` writes `{ paths: 1376, anchors: 192 }` into `:479` with a note attributing the +2 to these two entries, and commits it **with** the `CLAUDE.md` edit.
- **This is the fourth case in `## Reporting work you may not do`, not the second.** The edit was inside this agent's remit and was approved at the gate; only its consequence belongs to somebody else. No defect record was filed for it, because that section routes this case to the run report rather than to `$OUT_ISSUE`. The failure is loud rather than silent, so what this entry adds is the owner and the exact number, not the warning.
- **Measured against the survey's own green baseline.** `## 1` records `cd hooks && npm test` at HEAD as 43 files, 760 tests, all passing. After this pass the same command reports **42 files passed, 1 failed; 759 tests passed, 1 failed**, and the one failure is the pinned count above. `workbench-citation-lint` and `derivable-enumerations-lint`, the two other gates that read text this pass touched, were run separately and are **green**, so the run file written here introduced no dangling citation and the `CLAUDE.md` edits moved no derivable enumeration.
- **The survey pass should have predicted this and did not.** `## 2` states that "`CLAUDE.md` is pinned by no fixture" and names only `derivable-enumerations-lint` as a reader of it. That is incomplete: `reference-resolution-lint` pins a count over a surface that includes `CLAUDE.md`, and a citation added to it moves that count. `derivable-enumerations-lint` itself passed, so the part of the claim that was checked held. Recorded here rather than corrected above, so the miss stays visible to the next run.

**Nothing was committed.** This pass leaves working-tree edits only, per this agent's exclusion 8. `/fusion:cleanup` commits at its Step 7.

**Revert path for the whole pass:** `git checkout -- CLAUDE.md`. It is the revert path each of the three entries names, and one invocation undoes all three, because all three land in that one file, which is tracked and was clean at HEAD.

---

## 8. Blast radius

Proposed deletions, per surface, against the 20 per cent default:

| Surface | Bytes | Bytes proposed for deletion | Share |
|---|---|---|---|
| `CLAUDE.md` | 72 018 | 386 replaced, 0 removed net (the three After blocks are longer than their Before blocks by 471 bytes in total) | 0.0 % |
| Project rule files | 191 969 | 0 | 0 % |
| Decision records | 1 090 890 | 0 | 0 % |

**Verdict: well under the threshold on every surface, and no separate scale confirmation is owed.** No entry deletes a constraint, no entry deletes a file, and the ledger's net effect on `CLAUDE.md` is to add about 471 bytes while removing three false claims. `CLAUDE.md` is under no growth bound: the four armed bounds cover the always-on rule set, `agents/`, `skills/` and the hook tests, and this file is in none of them.

---

## 9. Records this run created without a gate

- `shared/decisions/260825-1456_*_does-claude-mds-register-repair-reach-the-curators-pass-and-under-what-evidence.md` — the unresolvable contradiction from candidate C01. Open, unresolved by this pass, edits neither side.
- `shared/issues/260825-1456_*_three-shipped-surfaces-say-the-retired-configuration-key-set-is-three-and-the-loader-holds-four.md` — work outside this agent's remit; see below.

**I01, reported and not done.** `hooks/lib/config.ts:349-361` defines `RETIRED_TOP_LEVEL_KEYS` with **four** members: `guard`, `decisions`, `escalation` and `churn`, the last added on 2026-08-24 with the comment naming issue `260815-1247`. Three shipped surfaces still say three:

- `fusion.json` `_retired` — "the three top-level keys that held it — guard, decisions, escalation"
- `templates/fusion.json` `_retired` — the same sentence, byte-identical, and held so by `config.test.ts`'s `PROJECT_SET_KEYS` comparison
- `agents/orchestrator.md:140` — "a retired top-level key inside `fusion.json` (`guard`, `decisions`, `escalation`)"

`CLAUDE.md` is **correct** on this and says four in both places it mentions the set, so no entry above touches it. The two JSON files are structured data and belong to `ontocoder`; the agent prompt belongs to `coder`. Filed rather than fixed, per this agent's exclusion 5.
