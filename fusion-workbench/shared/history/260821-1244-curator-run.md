# Curator run — 260821-1244-curator-run.md

---
**Status:** Complete. Survey pass on 2026-08-21 at 12:44; apply pass at 13:37 on the user's approval of **all** eight entries. All eight applied; none stale, none skipped, none failed. `## 7` carries the per-entry outcomes and the measured sizes.
**Mode:** survey, then apply (`**Ledger:** 260821-1244-curator-run.md`, `**Approved:** all`)
**Date:** 2026-08-21
**HEAD read:** `2f654fe04373d17e167bf0cdf1f1298a13b818ad` (plugin.json 10.4.0, work tree carrying unreleased changes)
**Previous curator run:** `260818-2050-curator-run.md`, 2026-08-18, HEAD `e14b6ca764b47e1007c5a2f555cedc2104c9106b` (v10.2.0). The two before that were `260817-1925-curator-run.md` (HEAD `e59dea2`) and `260817-0845-curator-run.md` (HEAD `c65e1cf`).
**Dispatched by:** the user, top-level, with `**Mode:** survey` and nothing else.
**Domain:** code

**Working tree at survey time.** `git status --porcelain` showed `fusion-workbench/.fusion-setup` and `fusion-workbench/orchestrator-events.jsonl` modified, and `260821-1219-orchestrator-session.md` untracked. All three are this session's own Setup, not work under any ledger entry. No file this run proposes to change was dirty.

**What changed in the interval.** 63 commits, `e14b6ca`..`2f654fe`, shipping v10.3.0 and v10.4.0. This is by far the widest interval any curator run has faced: 47 shipped files moved, `rules/fusion-workbench-conventions.md` alone by 205 lines, and one rule file was added (`rules/workbench-tracking.md`, in `b200902`, taking the corpus from 12 files to 13). `CLAUDE.md` moved in five commits and by nine lines, so the ratio this run had to read against is a rule corpus that was rewritten and a project index that was not.

**Two Circles closed in the interval and one was shaped.** `260819-1645-four-constraints-on-deep-change` closed coherent; `260820-2051-style-rules-arrive-and-get-measured` closed **bounded**, leaving eleven open decision records behind it; `260821-1042-reply-bounded-whole-question-answered` was filed anticipated with four scoping decisions already answered. One of those four names this agent's own mandate: `260821-1108_*_is-claude-mds-register-repair-inside-this-circle.md` answers option 1, no, `CLAUDE.md`'s register repair goes through `/fusion:cleanup --only claude-md`, which is the curator behind a user gate. **This run does not perform that repair.** Its entries are eight targeted corrections, and `## 8` says why the register pass is not among them.

## Why this run's selector is the fourth disjoint one

The three previous runs took the guard-removal vocabulary, then who-may-do-what, then measurements of the tree that no lint gate reads. This run takes a fourth: **statements that characterise something outside themselves**, so that opening the thing characterised decides them. Three shapes qualify, and `## 4` gives the rule in full.

The selector was chosen from the interval's own shape rather than from a list. When a rule corpus is rewritten and the index over it is not, what breaks is not the corpus and not the index but the sentences in each that describe the other. Nothing in the tree gates that class: `reference-resolution-lint` resolves a path and an adjacent heading anchor, `derivable-enumerations-lint` reads a fixed four-file surface list, and neither reads whether a summary still summarises.

---

## 1. Evidence-source counts

| # | Source | Files read | Note |
|---|---|---|---|
| 1 | Circle records (`$SCAN_CIRCLES` = `circles/`) | 14 listed, **5 bodies opened** (4 in full, 1 in part) | Ten `_c_`, two `_b_`, one `_s_`, one `_a_`. Opened in full: `260819-1645-four-constraints-on-deep-change/_c_circle.md`, `260820-2051-style-rules-arrive-and-get-measured/_b_circle.md`, `260816-1741-guard-becomes-observation-only/_b_circle.md`, `260821-1042-reply-bounded-whole-question-answered/_a_circle.md`. Opened in part: `260718-1924-v5x-overhaul/_c_circle.md` `## Closure note`, which is the evidence behind L02. **No Circle is active** (`.active-circle` absent), so `$SCAN_DECISIONS` collapses to `shared/decisions/`. Nine record bodies were not opened. |
| 2 | Decision records | **115 listed, 36 read in full** | `shared/decisions/` holds 52 (18 `_a_`, 31 `_i_`, 2 `_d_`, 1 `_s_`, and **no `_o_` at all**); the Circle stores hold 63 (7 `_a_`, 11 `_o_`, 43 `_i_`, 1 `_d_`, 1 `_s_`). All 115 filenames and markers were read by script. Every one of the 36 **live** records (`_o_` or `_a_`) was opened in full. No terminal record was opened in full. The corpus grew by 21 records since the previous run, which read 94. `$SCAN_DECISIONS` is the editable surface and is `shared/decisions/` alone; the 63 Circle records were read as evidence through `$SCAN_CIRCLES` and are **not** an editable surface this run, which is stated rather than assumed. |
| 3 | Session histories | **82 of 572 opened** | 299 in `shared/history/`, 273 across the Circle stores. Opened: all 80 stamped 260818 or later (55 shared, 25 in Circles), plus `260818-2050-curator-run.md` (the previous run) and `260821-1219-orchestrator-session.md` (this session's own Setup log). **490 were not opened, and that is the largest single gap in the run.** |
| 4 | Git history | **full `git log --follow` on all 14 files, plus the interval** | `CLAUDE.md` 94 commits; the 13 rule files 1 to 80 commits each (`fusion-workbench-conventions.md` 80, `user-facing-output.md` 15, `circle-records.md` 13, `critical-stance.md` 8, `workbench-path-resolution.md` 8, `agent-setup.md` 7, `decision-record-examples.md` 7, `context-manifest.md` 4, `design-diagrams.md` 4, `context-lean-claude-md.md` 3, `rule-file-provenance.md` 3, `workbench-tracking.md` 3, `commit-lock.md` 1). Plus `git log --oneline e14b6ca..HEAD` with `--stat`, `git ls-tree` diff over `rules/`, `git log -S` on two strings, and `git show e14b6ca:` and `git show b05b423:` reads of two files. `git blame` was not run. |
| 5 | Reviews and analyses | **5 of 80 reviews, 0 of 25 analyses** | 32 shared plus 48 Circle reviews; 17 shared plus 8 Circle analyses. The five opened are all stamped 260819 or later. Entries L01, L03 and L04 each rest partly on a review or a reconciliation; the rest do not. |
| 6 | `orchestrator-events.jsonl` | 1 file, **not read** | 1 857 lines. Corroborating-only by rule, and nothing below needed corroboration a stronger source did not already carry. |
| 7 | Archive store (`$WORKBENCH/archive`) | **291 files listed, 1 opened** | One batch, `archive/260817-1907-safe-cleanup-scoped/`. Read directly, because no `SCAN_*` key reaches it, which is itself the subject of open defect `260801-1020_*_scan-keys-never-reach-the-archive-store.md`. The one file opened is the archived record of the severed Circle `260719-1536-brest-unite-co-creator-conversion`, which is the second of L02's two Tier 3 sources. |
| 8 | `**Provenance:**` headers | **13 of 13 read in full** | Six cite a Circle, six use the admission-plus-commit form, one cites two decision records (`workbench-tracking.md`, new in this interval). Every cited Circle carries `_c_`; both cited decision records now carry `_i_`. **No Tier 2 retirement follows from this source**, and the previous run's candidate C3, which turned on one of those two records being open, is resolved by the tree rather than by this agent. |

**Open defect corpus, used as a cross-check and not as a fourth surface:** 170 open records (94 shared, 76 in Circles). Three changed this run's output materially and are named in the entries that cite them: `260819-0028_*_the-project-language-rule-names-a-record-status-pairing-that-no-record-kind-still-has.md` (corroborates L03), `260821-0042_*_the-always-on-rule-states-two-things-about-the-voice-profile-fallback-that-stopped-being-true.md` (corroborates L04, and its acceptance criteria are written for this agent), `260808-0030` (bounds L05 to a correction rather than a redesign).

**Unreadable surfaces:** none. Every file this run tried to read was readable.

**Absent surfaces:** `.claude/rules/` does not exist (`ls .claude/rules/` returns "No such file or directory"). There are no project-wide rule files, and the project-owned rule surface is `./rules/` alone, now 13 files. The scope call this repository forces is unchanged: cwd **is** the fusion plugin's own source tree, so `./rules/` here is the project's own rule directory and in remit, while the install copy at `$FUSION_PLUGIN_ROOT` (`/Users/k1/.fusion`) is out of remit and was not touched.

**The gates were run read-only and all of them are green.** `cd hooks && npx vitest run` returns **718 tests across 40 test files, all passing**, exit 0, in 72 seconds. That includes `reference-resolution-lint`, `derivable-enumerations-lint`, `path-literal-lint`, `provenance-header-lint`, `rules-emission-golden`, `surface-growth-bound`, and the three gates armed in this interval (`workbench-citation-lint`, `plan-stopping-section-lint`, `committed-dist`). Nothing on any shipped surface dangles by any check those gates perform, which is the reason this run looked where no gate does.

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after, all approved | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 66 626 | 140 | see below | 140 |
| Project rule files (`./rules/`) | 13 | 166 597 | 1 902 | 166 553 | 1 901 |
| Decision records (`$SCAN_DECISIONS` = `shared/decisions/`) | 52 | 446 460 | 5 789 | 446 460 | 5 789 |

The decision store is byte-identical under every approval outcome: **no entry touches a decision record.** `## 5` discharges the pre-edit-content obligation on that count.

The rule surface moves by **+59 bytes on `rules/fusion-workbench-conventions.md`** (L03 −15, L04 +73, L05 +1) and **−103 bytes on `rules/context-lean-claude-md.md`** (L02), for −44 net and one line removed. `CLAUDE.md` gains roughly 400 bytes across L01, L06, L07 and L08 and loses no line; the exact figure is arithmetic on the four replacement strings and is reported as measured, not predicted, after any apply.

**The always-on growth bound, reported against head-room because the open defect asks for it.** The five always-on rule files stand at 95 007 bytes against a `RULE_BASELINE` of 86 573, so growth is +8 434 of a 12 000-byte budget and head-room is **3 566 bytes**. The three entries on `rules/fusion-workbench-conventions.md` spend **59** of that, leaving 3 507. `rules/context-lean-claude-md.md` is emitted to no agent and carries no baseline entry, so L02 spends nothing and buys nothing.

## 3. Blast radius

**Verdict: nothing approaching the threshold. No separate scale confirmation is due.**

- **Bytes deleted outright: 118** (103 in `rules/context-lean-claude-md.md`, 15 in `rules/fusion-workbench-conventions.md`), of 166 597 on the rule surface: **0.07 per cent**.
- **On `CLAUDE.md`, four sentences are rewritten and none is deleted.** The largest single deletion is the ten-word clause named in L01.
- The decision store is untouched under every outcome.

## 4. Comparison counts

**Selection rule, one rule for all six pairs.** A statement enters the comparison when it **characterises something outside itself**, so that opening the thing characterised decides whether the statement still holds. Three shapes:

- **(a) a disposition claim about a workbench record.** The citing sentence says the record is settled, decided, answered, unanswered, open, deliberately not decided, tracked, or moot. Decided by resolving the citation's marker wildcard against the store and reading the record's body.
- **(b) a summary of another file's content.** A named enumeration of its sections, norms, examples, transitions, audience, or emission target, or a criterion it says governs a set of files. Decided by opening that file.
- **(c) a `path:N` line pin.** The characterised thing is "the sentence at that coordinate". Decided by `sed -n`.

A pair enters the comparison when one member characterises the other, or when two members characterise the same third thing. **Everything outside that rule was not compared, and this run makes no claim about it.**

**The corpus counts are derived, not stated.** `ls -1 agents/*.md | wc -l` → 15; `ls -1 -d skills/*/ | wc -l` → 12; `ls -1 rules/*.md | wc -l` → 13; `ls -1 docs/ | wc -l` → 7; `awk 'END{print NR}' CLAUDE.md` → 140; `ls -1 fusion-workbench/shared/decisions/*.md | wc -l` → 52; `ls -1 fusion-workbench/circles/*/decisions/*.md | wc -l` → 63.

**What the selector produced, executed as scripts over `CLAUDE.md`, all 13 rule files and all 115 decision records.**

- Shape (a): **25** wildcard record citations on the two text surfaces. **All 25 resolve**, which is itself worth recording because the archive sweep of 260817 broke six such citations elsewhere. Twenty-one resolve to a terminal marker, four to `_o_`. Of those four, **two carry a disposition claim the record's own body contradicts** (L01 is not one of them; L06 is, and C4 is the other).
- Shape (b): **30** summaries, 12 on `CLAUDE.md` describing a rule file or a test constant, 18 inside `rules/` describing another rule file or a shipped mechanism. **All 30 were opened and decided.** Four are false (L03, L06, L07, L08).
- Shape (c): **7** line pins on the two text surfaces, an exhaustive count and a small one. **All 7 were resolved by `sed -n`.** One is dead (L05); the other six hold the sentence the citing text describes.

| # | Pair | Pairs the rule produced | Read | Findings |
|---|---|---|---|---|
| 1 | decision × decision | 36 live records read in full, with every pair examined for a position an agent could not hold twice | 36 | **0 entries.** No two live records state positions an agent could not obey at once. Two live records overturn another live record in substance with no `Superseded by:` footer, and both are candidates rather than entries: see C5 and C6. |
| 2 | decision × rule file | 9 | 9 | **1 entry (L03)**, 2 candidates (C2, C5). The nine are the live records whose answer names a rule file as the place it must land. |
| 3 | decision × `CLAUDE.md` | 5 | 5 | **2 entries (L06, L07)**, 2 candidates (C4, C7). The five are the records `CLAUDE.md` cites by wildcard plus `260816-1707_a`, which names `CLAUDE.md` in its own reconciliation. |
| 4 | rule file × rule file | 18 summaries plus 5 line pins | 23 | **2 entries (L04, L05)**, 1 candidate (C1). Five of the six rule-corpus line pins hold; the sixth is L05. Every emission claim inside `rules/` was checked against `bin/fusion-rules` run for all 15 agents and all five are correct. |
| 5 | rule file × `CLAUDE.md` | 12 | 12 | **2 entries (L01, L08)**, 0 candidates. Eight of the twelve hold exactly: the five-topic partition table, the `_a_`/`_t_`/`_c_`/`_b_`/`_s_`/`_d_` vocabulary, the three-agent `circle-records.md` audience, the three "emitted to no agent" claims, `agent-setup.md` emitted first, the `DEFINITION_SITES` count of four, the five always-on rules, and the archive skill's citation of `workbench-tracking.md`. |
| 6 | `CLAUDE.md` × itself | 6 | 6 | **0.** The pair worth naming and clearing: `CLAUDE.md:37` says part (c) of decision `260810-1544_*_should-prompt-called-bin-helpers-get-one-guarded-call-convention-and-does-the-work-tree-preference-extend-to-them.md` "is deliberately unanswered" and `CLAUDE.md:97` says the same of the same part. Both are correct, and `260810-1544_*_...` confirms it in terms at its own footer: "Part (c) ... is **not** answered here." |

**The verdict covers the pairs this rule reached, not the corpora.** 490 of 572 session histories, 75 of 80 reviews, all 25 analyses, nine Circle record bodies and 290 of 291 archived files were not opened. Seventy-nine terminal decision records were read as markers only. Every statement on all three surfaces that characterises nothing outside itself, which is most of the prose on each, entered no pair at all. Four curator runs have now taken four disjoint selectors, and a stale statement outside all four has still not been looked for by any of them.

## 5. Pre-edit content of decision records this run would modify

**None.** No entry in this ledger touches a decision record, so there is no pre-edit content to preserve. The obligation is unconditional and is discharged by the count rather than by the workbench's tracking state, which is what the rule asks for.

## 6. The ledger

**8 entries and 7 candidates.** Ids are stable; per-entry approval is by id.

| Group | Ids | Count |
|---|---|---|
| Constraint removal | L01 | **1** |
| Tier 3 | L02 | 1 |
| Tier 2 | L03 | 1 |
| Tier 1 | L04, L05, L06, L07, L08 | 5 |
| Consolidation | — | 0 |
| **Candidate (not on offer, never applied)** | C1–C7 | **7** |

---

### L01 — the `docs/` row states a criterion for writing an upgrade note that the last two releases do not meet

- **Surface:** `CLAUDE.md`
- **File:** `CLAUDE.md`, line 52, the `docs/` row of the Layout table
- **Tier:** 1
- **Consequence group:** **constraint removal**
- **Citation:** the row says the upgrade notes are "written only when a release removes something an installed base already has on disk". Checked against the tree:

  ```
  $ ls -1 docs/
  philosophy.md  upgrading-to-v10-2.md  upgrading-to-v10-3.md
  upgrading-to-v10-4.md  upgrading-to-v10.md  upgrading-to-v9.md  working-model.md

  $ head -6 docs/upgrading-to-v10-3.md
  # Upgrading to fusion v10.3
  v10.3 changes two things you will meet, and rewrites nothing. ...
  Nothing in your workbench stops working when you upgrade, and no file is rewritten for you.

  $ head -8 docs/upgrading-to-v10-4.md
  # Upgrading to fusion v10.4
  v10.4 arms three blocking gates over text that nobody compiles ...
  **All three run in fusion's own test suite, over fusion's own repository. None of them can fire in your project**
  ```

  Two of the five upgrade notes state in their own opening lines that they remove nothing from an installed base, so the criterion is false of 40 per cent of the set it describes.

  **This entry was handed to the curator by name.** `260818-1759-coder-v10-2-renumbering-swept-and-golden-regenerated.md` `## Finding reported, not acted on` states it and routes it: "This release's page does not meet it ... The row was already inaccurate before the renumbering, so it is not fallout from it, and `CLAUDE.md` is a curator surface behind a user gate. **Left for `/fusion:cleanup --only claude-md`.**" That is the only sentence in the whole interval that names this cleanup selector.
- **Constraint removed:** the restriction that an `upgrading-to-vN.md` note is written **only** when a release removes something an installed base already holds on disk. Nothing replaces it as a rule; the replacement states the observed practice instead. **Searchable line:** *run 260821-1244-curator-run.md removed the "written only when a release removes something an installed base already has on disk" restriction from `CLAUDE.md`'s `docs/` row, because v10.3 and v10.4 each say in their own opening lines that they remove nothing, and no step of `## Release process` states the restriction as a procedure.*
- **Why it is offered rather than withheld under the preserve list.** The preserve list bars removing a critical procedure such as a release-flow step. This clause is not one: `## Release process` runs steps 0 to 6 and `grep -i 'upgrad\|docs/'` over that section returns nothing, so no release step asks for the note. The clause sits in the Layout table and describes a set of files. It is classified in the loudest consequence group all the same, so that a user who reads it the other way meets it first and can reject it.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**

> The `upgrading-to-vN.md` files are per-release migration notes, written only when a release removes something an installed base already has on disk — v9 was the first this repository ever shipped, and each is pointed at from `README.md` `## Install` and from `/fusion:help`'s update topic.

**After:**

> The `upgrading-to-vN.md` files are per-release migration notes, one per release since v9, which was the first this repository ever shipped. A note is not conditional on the release removing something an installed base holds: `docs/upgrading-to-v10-3.md` opens by saying it rewrites nothing and `docs/upgrading-to-v10-4.md` that none of its three new gates can fire in your project. Each is pointed at from `README.md` `## Install` and from `/fusion:help`'s update topic.

---

### L02 — a rule file describes itself as input to two pieces of work that both concluded on 2026-07-19

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`, line 11
- **Tier:** 3
- **Consequence group:** tier-3
- **Citation, two independent sources of different kinds.** The line reads: "This is the input to the reference conversion (a later Circle-B step) and to the docs pass (Circle E)." Circle B and Circle E are tracks of `260718-1924-v5x-overhaul`.

  1. **The Circle's own closure note**, `260718-1924-v5x-overhaul` `## Closure note`: "**Closed-coherent (`_c_`)** on 2026-07-19. The fusion-side v5.x overhaul is complete: A (coordination analysis), B (context mechanism, v5.1.0), C (editor agent, v5.2.0), D (per-agent prompt revision, v5.3.0), **E (docs cleanup + working-model explainer, v5.4.0)**." And, for the other consumer: "**B-rest severed:** the unite-co-creator reference conversion (a separate repo) is carried forward as its own anticipated Circle rather than blocking this umbrella."
  2. **The archive store**, read directly because no `SCAN_*` key reaches it. The severed Circle's record is `archive/260817-1907-safe-cleanup-scoped/260719-1536-brest-unite-co-creator-conversion`, so that Circle has itself closed and been archived. `ls` on that directory returns `_c_circle.md`, `decisions`, `history`, `planning`, `reviews`.

  **When it stopped:** 2026-07-19 for both consumers. The docs pass shipped as v5.4.0 on that date; the reference conversion was severed to its own Circle on that date. **What replaced it:** nothing. Neither consumer is pending, and the severed Circle closed and was archived on 2026-08-17.
- **Constraint removed:** none. The sentence names who consumes the document; it states no rule, imposes no obligation, and the two sentences that follow it, which do carry the document's own position, are untouched.
- **Carried twice before this run.** It was candidate C2 in `260817-1925-curator-run.md` and again in `260818-2050-curator-run.md`, each time downgraded because Tier 3 requires naming when the practice stopped and what replaced it and neither run had opened the Circle. That run named the cheapest path to resolving it: "a dispatch that reads that one Circle's histories". This run read its closure note and its archived successor instead, which is cheaper still and is what the tier requires.
- **Revert path:** `git checkout -- rules/context-lean-claude-md.md`

**Before:**

> This is the input to the reference conversion (a later Circle-B step) and to the
> docs pass (Circle E). It is a convention, not machinery: nothing in the plugin
> enforces a line count. The goal is a `CLAUDE.md` a reader can hold in their head.

**After:**

> It is a convention, not machinery: nothing in the plugin
> enforces a line count. The goal is a `CLAUDE.md` a reader can hold in their head.

---

### L03 — the Project language rule offers a head label that no record kind still carries

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, line 262
- **Tier:** 2
- **Consequence group:** tier-2
- **Citation: the record, and the sentence in the current text it overturns.**

  The overturning record is `260818-2212_*_should-the-decision-records-status-field-exist-at-all-now-that-the-circle-records-has-been-removed.md`, which carries the implemented marker and whose answer took the `**Status:**` head field off the decision-record template. Its predecessor, `260815-2312_*_should-the-circle-records-status-field-exist-at-all-now-that-both-transitions-maintain-it.md`, did the same for the Circle record. The removal is stated in the same file this entry edits, at `rules/fusion-workbench-conventions.md:530`: "**There is no `Status:` head field, and you do not write one.**"

  The sentence overturned is at `:262`: a label defined in a shipped template, "the plan head's `**Decidability:**`, **a record's `**Status:**`** and `**Domain:**`", is English in every project. After both removals no record kind has a `**Status:**` field, so the example names a pairing that no longer exists, in a file that ships to every agent on every dispatch.

  **Corroborated by an open defect that asserts the same thing, not the opposite:** `260819-0028_*_the-project-language-rule-names-a-record-status-pairing-that-no-record-kind-still-has.md`. The executor who removed the field named the residual and declined to fix it under a task whose change surface the decision states precisely: "**It is worth a one-line correction in some later pass**" (`260819-0023-t2-status-field-leaves-the-decision-record-template.md` `## One finding for the orchestrator, not acted on`). The Turn 1 reviewer reached it independently as finding L2 and added only that the passage is always-on (`260819-0044-coderev-turn-1-....md`).
- **Constraint removed:** none. The rule the sentence states, that a head label follows the file that defines it rather than the artifact it sits in, is preserved word for word, as is its conclusion about `**Decidability:**`. One of three examples is dropped because its subject no longer exists.
- **Byte cost against head-room:** −15 bytes on the always-on corpus.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`

**Before:**

> A label defined in a shipped template (the plan head's `**Decidability:**`, a record's `**Status:**` and `**Domain:**`) is English in every project, because the template lives in an exempt file.

**After:**

> A label defined in a shipped template (the plan head's `**Decidability:**` and a record's `**Domain:**`) is English in every project, because the template lives in an exempt file.

---

### L04 — the always-on rule states two things about the voice-profile fallback that stopped being true

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, line 250, the `## Project language` paragraph on the missing-variant fallback
- **Tier:** 1
- **Consequence group:** tier-1
- **Citation: the check, with the command and its output.** The sentence claims that `bin/fusion-rules` "emits only the resolved path, so an agent cannot today tell a fallback from a project that declared `en`, and the history line this rule asks for is unreachable until the helper says so".

  ```
  $ git show --stat --oneline 1c1178d | head -2
  1c1178d feat(rules): the voice-profile fallback says so on stderr, and the record it
          was meant to close stays open
   bin/fusion-rules | 22 ++++-

  $ sed -n '316,318p' bin/fusion-rules
  #   THE FALLBACK ANNOUNCES ITSELF ON STANDARD ERROR, and only that branch does.
  #   Stdout is one path per line, so a fallback and a project that declared `en`
  #   arrive there identically: an agent instructed to record the fallback could not …
  ```

  The event is detectable and was measured to be so. The reconciler ran it in a scratch project rather than reading it out of the commit: "A project declaring `de` with only the `-en` variants present receives `./fusion-workbench/stilwerk/chat-voice-en.yaml` on standard output and `fusion-rules: voice profile chat-voice: requested variant de is absent, resolved to en` on standard error. The same project switched to `en` receives the same standard output and an empty standard error" (`260821-0416-reconciliation.md` `## Records that could not close`).

  The second claim is the load-bearing one. Commit `1a36fe4` replaced the sentence that asked an agent to record the fallback with a description of the defect, so the paragraph refers to "the history line this rule asks for" while the rule asks for no such line.
- **This entry's acceptance criteria are written for this agent.** `260821-0042_*_the-always-on-rule-states-two-things-about-the-voice-profile-fallback-that-stopped-being-true.md` `## Acceptance` asks for three things: that the paragraph state what the helper does today, that it not refer to an obligation no surface states, and that the always-on byte cost be reported against the head-room. All three are met here. The record explains why it proposes no wording for the second: whether an agent should be obliged to record a fallback is the open question of `260814-1332_*_the-voice-profile-fallback-is-performed-by-the-helper-so-the-agent-cannot-record-it.md`, and answering it by acting would decide it.
- **Constraint removed:** none, and this is the point on which the entry could be got wrong. The obligation to record a fallback was removed from the rule text by `1a36fe4`, by a party that is not this agent, and what stands at `:250` is a reference to a constraint that is already gone. The replacement neither restores it nor deliberately drops it: it says in terms that the question is open and keeps the record citation, so a later reader reaches the live question rather than a dangling pointer. Restoring the obligation would be adding a constraint on this agent's own judgement, which is as far outside the remit as removing one.
- **Byte cost against head-room:** +73 bytes on the always-on corpus.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`

**Before:**

> It emits only the resolved path, so an agent cannot today tell a fallback from a project that declared `en`, and the history line this rule asks for is unreachable until the helper says so (tracked as `circles/260801-1244-curator/issues/260814-1332_*_the-voice-profile-fallback-is-performed-by-the-helper-so-the-agent-cannot-record-it.md`).

**After:**

> Standard output carries the resolved path alone, so the fallback names itself on standard error instead, giving the family, the requested variant and the resolved one. Whether an agent is obliged to record that event is open, and no surface states such an obligation today (`circles/260801-1244-curator/issues/260814-1332_*_the-voice-profile-fallback-is-performed-by-the-helper-so-the-agent-cannot-record-it.md`).

---

### L05 — the layout rule cites the archive skill at a line that is now blank

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, line 66, the legacy-stores paragraph
- **Tier:** 1
- **Consequence group:** tier-1
- **Citation: the check, with the command and its output.**

  ```
  $ sed -n '96p' skills/archive/SKILL.md
  (blank line)

  $ grep -n 'stashes' skills/archive/SKILL.md
  102:   - `$WORKBENCH/monitor`, `$WORKBENCH/stilwerk/`, `$WORKBENCH/stashes/`

  $ git show e14b6ca:skills/archive/SKILL.md | sed -n '96p'
     - `$WORKBENCH/monitor`, `$WORKBENCH/stilwerk/`, `$WORKBENCH/stashes/`

  $ git log --oneline e14b6ca..HEAD -- skills/archive/SKILL.md
  06ab15b fix(rules): the archive skill actually reads the rule it was the named consumer of
  b200902 feat(rules): a tracked workbench gets its own rule file, and a plan's stopping section gets a reader
  ```

  The citation was correct at `e14b6ca`, the HEAD the previous curator run read, and broke in the interval: the two commits above inserted six lines above it.

  **The other four pins in the same sentence were re-resolved and all four hold:** `skills/log-activity/SKILL.md:82` carries the `find` with `-not -path '*/stashes/*'`, `agents/playmaker.md:61` names both frozen stores, and `skills/setup/SKILL.md:60` and `:67` carry the bracket-marker probe note and the probe itself. The sentence's other claim, that all but the archive skill also exclude `.migration-v2-backup/`, was checked by `grep -rn 'migration-v2-backup' skills/ agents/ rules/` and holds.
- **Constraint removed:** none. The sentence's claim, that three shipped consumers exclude the two frozen stores by path, is preserved word for word. Only the coordinate under it moves.
- **Derive over correct, stated rather than skipped.** A line number is a measurement of a file that moves, so the derive rule would normally ask for the derivation instead of the corrected value. The derivation exists, the adjacent heading form the reference lint already resolves, and it is option 1 of `260808-0030_*_line-number-citations-into-rule-files-go-stale-and-no-gate-reads-them.md`, which is open and states that the choice "needs a decision about scope before implementation". Taking it would answer that by acting. The corrected value is proposed as the documented fallback, on the same precedent the previous run used for its own single entry, which the user approved on 2026-08-18.
- **Byte cost against head-room:** +1 byte on the always-on corpus.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`

**Before:**

> `skills/log-activity/SKILL.md:82`, `skills/archive/SKILL.md:96` and `agents/playmaker.md:61` exclude `stashes/` by path

**After:**

> `skills/log-activity/SKILL.md:82`, `skills/archive/SKILL.md:102` and `agents/playmaker.md:61` exclude `stashes/` by path

---

### L06 — `CLAUDE.md` calls a question settled that the record it cites says is deliberately not settled

- **Surface:** `CLAUDE.md`
- **File:** `CLAUDE.md`, line 41, the last sentence of the `bin/fusion-prose-metric` row
- **Tier:** 1
- **Consequence group:** tier-1
- **Citation: the check, with the command and its output.**

  ```
  $ ls -1 fusion-workbench/circles/260820-2051-style-rules-arrive-and-get-measured/decisions/ \
      | grep em-dash-ceiling
  260820-2314_o_is-the-em-dash-ceiling-read-per-file-or-across-the-always-on-corpus.md
  ```

  The marker is `_o_`, open, and the record's closing section says why in terms:

  > "## Who answered this, and under what authority
  > The user asked for an unattended run and left for two to three hours. A dispatched shaper holds no `AskUserQuestion`, so its questions came back to the orchestrator instead of to the user. The orchestrator answered. **This is not the user's answer and must not be read as one.** The record is filed open (`_o_`) rather than answered so that the user meets it as a live question and can overrule it without unpicking the work that assumed it."

  `CLAUDE.md:41` reads it as one. The record is the authority on its own disposition, so `CLAUDE.md` is the falsified side of the pair.

  **The substance is not in dispute and is not what changes.** `bin/fusion-prose-metric` does read the ceiling per file, which is option 2, and the entry preserves that. What the sentence claims wrongly is that the question is closed.
- **One related gap, reported here and not proposed.** `rules/user-facing-output.md:141` states the ceiling as "One `—` per ~1000 words" with no per-file or per-corpus scoping, so the record is reflected on one normative surface and not the other. Adding the scoping to the rule would be realising an open record's orchestrator-authored answer into always-on rule text, which is an addition rather than a repair and is not this agent's act. Named in `## 8`.
- **Constraint removed:** none.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**

> Whether the ceiling is read per file or across the always-on corpus is settled per file (`fusion-workbench/circles/260820-2051-style-rules-arrive-and-get-measured/decisions/260820-2314_*_is-the-em-dash-ceiling-read-per-file-or-across-the-always-on-corpus.md`).

**After:**

> Whether the ceiling is read per file or across the always-on corpus is answered per file, by the orchestrator during an unattended run and not by the user, and the record stays open so the user meets it as a live question (`fusion-workbench/circles/260820-2051-style-rules-arrive-and-get-measured/decisions/260820-2314_*_is-the-em-dash-ceiling-read-per-file-or-across-the-always-on-corpus.md`).

---

### L07 — the `decision-record-examples.md` row names a transition the file has not shown since this interval

- **Surface:** `CLAUDE.md`
- **File:** `CLAUDE.md`, line 50, the `rules/decision-record-examples.md` row
- **Tier:** 1
- **Consequence group:** tier-1
- **Citation: the check, with the command and its output.**

  ```
  $ grep -n '^## Example' rules/decision-record-examples.md
  9:## Example 1: Happy path: `_o_ → _a_ → _i_`
  71:## Example 2: Supersession: `_i_ → _s_`
  92:## Example 3: User defers: `_o_ → _d_`

  $ git show b05b423:rules/decision-record-examples.md | grep -n '^## Example'
  7:## Example 1 — Happy path: `[o] → [a] → [i]`
  68:## Example 2 — Supersession: `[a] → [s]`
  89:## Example 3 — User defers: `[o] → [d]`

  $ git log --oneline -S'Example 2: Supersession' -- rules/decision-record-examples.md
  b393a45 fix(rules): three always-on rule files reach their ceiling, and the pass
          returns bytes rather than spending them
  ```

  `CLAUDE.md` says the file shows `_a_→_s_`. It showed that at v2.3.0 and shows `_i_→_s_` now. The change landed in `b393a45`, inside this interval. The distinction is not cosmetic: `_i_→_s_` is the one allowed terminal-to-terminal transition, which is exactly what Example 2 is written to demonstrate.
- **Constraint removed:** none. The row's other two claims, that the file carries worked examples and that it is one of the five always-on rules loaded by every agent, were both re-verified and hold: `bin/fusion-rules` run for all 15 agents emits it to every one of them.
- **Derive over correct, stated rather than skipped.** A command does produce this enumeration, `grep -n '^## Example' rules/decision-record-examples.md`, and `CLAUDE.md` carries two worked instances of preferring the derivation, in its `templates/` and `docs/` rows. Both of those replace an inventory of a directory that changes every session. This enumeration has changed once in the file's life, so the correction is proposed and the derivation is named here rather than taken, and the user can direct it the other way.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**

> Worked examples of the decision-marker transitions (_o_→_a_→_i_, _a_→_s_, _o_→_d_).

**After:**

> Worked examples of the decision-marker transitions (_o_→_a_→_i_, _i_→_s_, _o_→_d_).

---

### L08 — the critical-stance bullet numbers three norms and the rule has stated four since 2026-08-07

- **Surface:** `CLAUDE.md`
- **File:** `CLAUDE.md`, line 63, the Critical stance bullet
- **Tier:** 1
- **Consequence group:** tier-1
- **Citation: the check, with the command and its output.**

  ```
  $ grep -n '^## ' rules/critical-stance.md
  9:## 1. Errors are yours to own, not the user's to be praised for
  23:## 2. No premature solutions: the Research Gate
  36:## 3. Calibrated certainty
  47:## 4. A case split is disjoint and complete, or the question is cut wrong

  $ grep -n 'four norms' rules/critical-stance.md
  7:The four norms below are not style preferences; they are conditions for the tool
    being worth relying on.

  $ git log --oneline -S'The four norms below' -- rules/critical-stance.md
  327d0b6 feat(hooks,rules): die Wache misst nachher, und MECE steht als Prinzip

  $ git show -s --format='%ci' 327d0b6
  2026-08-07 10:31:03 +0200
  ```

  The rule counts itself at four and has done for fourteen days. `CLAUDE.md` numbers three and stops. This is not a summary that declines to be exhaustive: it is a numbered list, `(1) … (2) … (3)`, against a file that numbers its own sections and states the count in its lede.

  **The missing norm is the one with a shipped consequence.** §4 is what puts the mandatory `**Decidability:**` line in every plan head (`rules/critical-stance.md:62`, `agents/planner.md:105` and `:158`), and `CLAUDE.md` names that label nowhere. So the bullet's omission is not only a count: it drops the only one of the four norms that reaches a shipped template.
- **Constraint removed:** none. The three existing clauses are preserved word for word and a fourth is added.
- **Derive over correct, stated rather than skipped.** The same reasoning as L07: `grep -n '^## ' rules/critical-stance.md` produces the list, but a semantic enumeration that has moved once in the file's life is not the churning-directory case the two worked derivations in `CLAUDE.md` were written for. The derivation is named so the user can direct it the other way.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**

> this rule governs *reasoning substance*: (1) own your errors instead of praising the user who caught them, (2) no premature/half-baked solutions — research the root cause and prefer the clean integrated fix over additive workarounds, (3) calibrated certainty — separate verified from inference from speculation, label the latter two, and never dress an unchecked claim as a checked one.

**After:**

> this rule governs *reasoning substance*, in four norms: (1) own your errors instead of praising the user who caught them, (2) no premature/half-baked solutions — research the root cause and prefer the clean integrated fix over additive workarounds, (3) calibrated certainty — separate verified from inference from speculation, label the latter two, and never dress an unchecked claim as a checked one, (4) a case split is disjoint and complete or the question is cut wrong, which is what puts the mandatory `**Decidability:**` line in every plan head.

---

### C1 — `rules/workbench-path-resolution.md:72` names two consumers of `SCAN_CIRCLES` and there are seven

- **Surface:** project rule file
- **File:** `rules/workbench-path-resolution.md`, line 72, the `SCAN_CIRCLES` row of the key table
- **Tier:** 1
- **Citation:** re-verified at this HEAD, unchanged from the two previous runs. The Notes column reads `Portfolio-wide scans (playmaker, /fusion:next).` `grep -lE '\$SCAN_CIRCLES\b' agents/*.md skills/*/SKILL.md` returns seven: `agents/curator.md`, `agents/shaper.md`, `agents/playmaker.md`, `agents/orchestrator.md`, `skills/archive/SKILL.md`, `skills/next/SKILL.md`, `skills/setup/SKILL.md`.
- **Why it is a candidate, carried a third time:** the parenthetical carries no word of exhaustiveness, so reading it as a complete enumeration rather than as two examples is a reading of prose and not a check. The file being the definition of what each key means argues the other way. Raise it as an entry only if the user says the Notes column is meant to enumerate.

### C2 — `rules/circle-records.md:56` names one writer of `.active-circle`, the conventions file now names four touching parties, and nothing says which governs

- **Surface:** project rule file
- **File:** `rules/circle-records.md`, line 56
- **Tier:** 1
- **Citation:** `:56` reads "`_a_ → _t_` — playmaker proposes activation; user confirms; orchestrator renames the record and writes `.active-circle` with the directory name." `rules/fusion-workbench-conventions.md:82` authors the writer set and declares it closed: two writers on the activation path, the orchestrator and `/fusion:next`, plus two lifecycle skills that touch it outside activation, `/fusion:migrate` and `/fusion:cleanup`. The second activation writer is real and performs the identical pair of writes (`skills/next/SKILL.md`, its `### 6.3` step).
- **What this run adds, and it is the reason the candidate is worth carrying:** the coordinate moved and the gap widened. The previous run cited the conventions passage at `:91`, where it named **two** parties; the tracked-workbench partition shifted it to `:82`, where it names **four**. Meanwhile `rules/circle-records.md:56` still names one.
- **Why it is a candidate and not an entry, carried a third time:** the two statements do not contradict each other in the sense the remit requires, since an agent obeying the worked transition violates nothing in the conventions file. It is the precedence-undecided shape: `rules/circle-records.md:5` declares itself "**the definition** for the Circle state vocabulary, its transitions", while `rules/fusion-workbench-conventions.md:82` declares the writer set "closed and enumerated here", and nothing says which governs where the two overlap. This run does not resolve it by picking one, and does not file a decision record for it, on the previous run's reasoning that a one-clause addition to `:56` closes the gap without touching precedence at all. `inference:` that reasoning is now weaker than it was, because the gap is four parties wide rather than two. **A third carry is the signal that one of the two should be chosen rather than deferred again**, and the choice is the user's, not this agent's.

### C3 — the `**Status:**` field disagrees with its own filename marker across the live decision corpus

- **Surface:** decision record
- **Tier:** 1
- **Why it is a candidate:** unchanged in kind from the two previous runs, and the standing instruction is unchanged with it. `260811-2146_*_half-the-decision-records-carry-a-status-that-disagrees-with-their-marker-and-twelve-keep-the-unfilled-template-stub.md` says in terms: "Do not hand-correct the 34 without answering the question first — the disagreements are the measurement." **What this run adds is that the record was closed on one of its two halves**, which the reconciler found and filed: `shared/issues/260819-0836_*` records that the template's unfilled placeholder block is still prescribed in an always-on rule file, "now five lines rather than the four the record quoted, `Retired:` having been added since", and that "a later reader grepping for the stub problem finds a closed record and stops". **Executor: the user, at a decision gate; then `coder`.**

### C4 — `CLAUDE.md:37` says a question was decided and the record it cites carries no answer

- **Surface:** `CLAUDE.md`
- **File:** `CLAUDE.md`, line 37, the `bin/fusion-source-root` row
- **Tier:** 1
- **Citation:** the row says "It was decided for that comparison alone (`circles/260820-2051-.../260820-2324_*_is-the-work-tree-the-refresh-source-when-setup-runs-in-the-plugins-own-repository.md`, option 1)". The record resolves to `260820-2324_*_...`: open, carrying a `## Recommendation` for option 1 and no `## Answer` section, with `Answered:`, `Implemented:`, `Deferred:` and `Superseded by:` all empty.
- **Why it is a candidate and not an entry, and the difference from L06 matters.** The em-dash record disclaims its own answer in terms, so `CLAUDE.md` contradicts it. This record does not disclaim: the plan was written against option 1, the mechanism shipped in `3464575` and `dc78da2`, and Step 0e exists. So the substance of `CLAUDE.md:37` is right and what lags is the record's marker, which is the reconciler's walk against ground truth and is exclusion 1 of this agent's remit. The same holds for its sibling `260820-2324_*_where-does-the-copied-asset-provenance-record-live.md`, whose option 1 shipped in full while the record shows no answer at all. **Executor: `reconciler`.**

### C5 — an open record amends an answered record's terminal condition with no annotation on either

- **Surface:** decision record
- **Files:** `260816-0740_*_does-the-prose-register-get-a-measurable-gate-and-which-surface-does-it-measure.md` and `260820-2314_*_what-threshold-does-the-registered-measurement-use-and-which-marker-does-each-outcome-earn.md`
- **Tier:** 2 in shape, and that is precisely why it fails
- **Citation:** `260816-0740_a` sets its own terminal condition: "This record stays answered rather than implemented until that measurement exists." `260820-2314_o` replaces that single condition with a three-branch marker scheme and calls the change substantive itself: "**The marker scheme is amended too, and this is the substantive half.**" Neither record annotates the other.
- **Why it is a candidate:** the amending record carries the open marker, and this agent's own tier rule says an open record is not Tier 2 evidence, because an open question retires nothing. There is no second, non-open source. **Executor: the user, when they meet the eleven open records of that Circle.**

### C6 — a later user answer demotes an earlier user answer's mechanism, with no `Superseded by:` on the earlier

- **Surface:** decision record
- **Files:** `260810-2032_*_should-the-drift-checks-four-sentences-be-pinned-to-an-approved-baseline-instead-of-screened-by-a-blacklist.md` and `260816-0711_*_is-count-pinning-the-convention-for-every-gate-that-reports-what-it-examined.md`
- **Tier:** 2
- **Citation:** the earlier record's answer, given by the user on 2026-08-10, chose option 2, pin the four sentences to an approved baseline. The later record's answer, given by the user on 2026-08-16, chose "probe-assertion is the convention, count-pinning the fallback where no probe exists", which demotes the earlier record's chosen mechanism from convention to fallback. The earlier record's own reconciliation concedes the substance without recording the transition: "this record's *instance* is dead and its *principle* is live under another record's name."
- **Why it is a candidate:** the earlier record already carries a `Retired:` line, its subject having been deleted in `f45f76a`. A record that is both retired and superseded is a state the marker vocabulary has never been asked for, and the two records answer different questions rather than the same one. `inference:` that this is a demotion of a mechanism rather than a supersession of a decision. Editing a user-answered record over a contested reading is not proportionate, and the judgement is the user's.

### C7 — the user has overturned an open decision and only a Circle record says so

- **Surface:** decision record
- **File:** `260820-2314_*_does-this-circle-take-the-structural-half-of-the-verbosity-record.md`
- **Tier:** 2 in shape
- **Citation:** `260821-1042-reply-bounded-whole-question-answered` `### The decision this Circle overturns` states it plainly: "It was filed open and answered by the orchestrator during an unattended run, and its own closing paragraph says it is not the user's answer. **The user has now overturned it.** ... A later reader meeting that decision should read it as reversed rather than as forgotten, and the reversal is dated 2026-08-21." The decision record itself carries no annotation of any kind.
- **Why it is a candidate:** Tier 2 admits a decision record, a Circle **closure** note, or a session history's design-decision section as the citation. The scoping section of an anticipated Circle's record is none of the three, and no session history records the reversal. So the evidence for the annotation exists and is not of a kind this agent's tier rule accepts.
- **Why it is the candidate most worth acting on.** A reader who opens that decision record today sees an orchestrator's answer with nothing saying the user reversed it, which is the exact failure the reversal note asks a later reader to avoid. **Executor: `reconciler`**, which may write the annotation on the strength of the Circle record. Eleven such records sit behind that bounded Circle and `260821-0426-playmaker-orchestrator-phase4.md` warning 6 says they "await the user's confirmation or reversal".

## 7. Outcomes

**Apply pass, 2026-08-21 13:37, HEAD `2f654fe` — unchanged from the survey pass, so no entry could go stale by a moved HEAD.** The dispatch carried `**Mode:** apply`, `**Ledger:** 260821-1244-curator-run.md` and `**Approved:** all`. Nothing was re-derived: the eight entries below are the ones this file already carried.

**Each entry's before-text was re-read from disk before it was applied**, by exact-string match with an occurrence count, and every one matched at exactly one site. **No entry was stale.** The two surface totals measured immediately before the first write were byte-identical to the ones `## 2` recorded at survey time (`CLAUDE.md` 66 626 bytes / 140 lines; `./rules/` 166 597 bytes / 1 902 lines), so nothing moved under the run in the fifty-three minutes between the passes.

| Id | Surface | File | Outcome |
|---|---|---|---|
| L01 | `CLAUDE.md` | `CLAUDE.md`, `docs/` row | **applied** |
| L02 | project rule file | `rules/context-lean-claude-md.md` | **applied** |
| L03 | project rule file | `rules/fusion-workbench-conventions.md` | **applied** |
| L04 | project rule file | `rules/fusion-workbench-conventions.md` | **applied** |
| L05 | project rule file | `rules/fusion-workbench-conventions.md` | **applied** |
| L06 | `CLAUDE.md` | `CLAUDE.md`, `bin/fusion-prose-metric` row | **applied** |
| L07 | `CLAUDE.md` | `CLAUDE.md`, `rules/decision-record-examples.md` row | **applied** |
| L08 | `CLAUDE.md` | `CLAUDE.md`, Critical stance bullet | **applied** |

**8 applied, 0 skipped, 0 stale, 0 failed.** The seven candidates C1–C7 were never on offer and none was applied.

### Surface sizes, measured after the apply rather than predicted

| Surface | Files | Bytes before | Bytes after | Delta | Lines before | Lines after |
|---|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 66 626 | **67 104** | **+478** | 140 | 140 |
| Project rule files (`./rules/`) | 13 | 166 597 | **166 553** | **−44** | 1 902 | **1 901** |
| Decision records (`shared/decisions/`) | 52 | 446 460 | **446 460** | **0** | 5 789 | 5 789 |

Per file: `rules/context-lean-claude-md.md` 6 811 → 6 708 (**−103**, one line), `rules/fusion-workbench-conventions.md` 57 055 → 57 114 (**+59**, no line change). Both are the figures `## 2` predicted, to the byte. `CLAUDE.md` gained 478 bytes where `## 2` said "roughly 400" and declined to predict it exactly; the figure is reported as measured, which is what that section asked for.

**The decision store is byte-identical, as `## 2` and `## 5` both said it would be.** 52 files, 446 460 bytes, 5 789 lines, confirmed after the apply. No entry touched a decision record and none was created.

**Removals, by tier, for whoever greps these files later:** one constraint removal (L01, Tier 1 evidence), one Tier 3 removal of a non-normative sentence (L02). Both name the removed text in their own entry above, in the `**Constraint removed:**` line. Bytes deleted outright: 118, unchanged from the survey's blast-radius figure.

**Always-on head-room, re-measured:** the five always-on rule files stand at **95 066 bytes** against a `RULE_BASELINE` of 86 573, so growth is +8 493 of the 12 000-byte budget and head-room is **3 507 bytes** — exactly the figure `## 2` predicted the three conventions entries would leave.

### Fixture consequence: two gates went red, and only one of them was foreseen

`cd hooks && npx vitest run` after the apply returns **2 failed of 718 tests across 40 files**. Both failures are count pins moved by an approved in-remit edit, and neither is a dangling reference or a broken assertion of substance. **Both are coder work and this agent ran neither regeneration.**

1. **`rules-emission-golden.test.ts` — foreseen, and `## 8` item 7 named it.** `hooks/lib/__tests__/fixtures/rules-emission.golden` pins `fusion-workbench-conventions.md` at 57 055 in each of the fifteen agents' blocks and the file is now 57 114, so every block's total moves by the same +59. Regenerate with `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, then read the diff: the only legitimate change is +59 on that one filename and +59 on each agent's total. **Executor: `coder`.**

2. **`reference-resolution-lint.test.ts` — NOT foreseen, and the survey should have caught it.** Its `BASELINE` pins the resolved-reference counts and `paths` moved **1 255 → 1 257**, `anchors` and `records` unchanged at 162 and 115. The +2 is L01: the replacement sentence cites `docs/upgrading-to-v10-3.md` and `docs/upgrading-to-v10-4.md` where the original cited no path. **Both new citations resolve** — the gate's 36 other assertions all pass, so nothing dangles; what failed is the pin that exists to notice a count moving, doing exactly its job. The test's own message states the expected response in terms: re-approve the baseline by writing the received numbers into `BASELINE` and committing that **with** this edit, which is not a way around the gate. **Executor: `coder`.**

**The miss is worth stating plainly rather than filing away.** `## 8` item 7 reasoned about the golden because L03, L04 and L05 changed a rule file's byte size, and it reasoned about `CLAUDE.md` only far enough to say it "is in no fixture and under no growth bound", which is true and was not the whole question. `CLAUDE.md` is in the reference lint's corpus, and L01 added two path citations to it. The general rule the next run should carry: **an entry that adds or removes a citation moves the reference lint's count pin, whichever surface it sits on, and byte size is not the only thing a fixture pins.** No new defect record is filed for this — it is a note to the next curator run, and the run file is where such notes have been left before.

**Revert paths, all three files git-tracked and none committed by this agent:**

```
git checkout -- CLAUDE.md
git checkout -- rules/context-lean-claude-md.md
git checkout -- rules/fusion-workbench-conventions.md
```

Reverting all three also returns both gates to green with no fixture regeneration. Working-tree edits only; this agent committed nothing.

## 8. Work this agent may not do, and who owns each

Seven items. None is a ledger entry. **No new record was filed this run.** Every item already has an owner or an open record, and the conventions' own filing rule says to append to what exists rather than open a duplicate.

1. **`CLAUDE.md`'s prose register is the largest remaining fault on any always-on surface, and it is parked by an open question that is not this run's to answer.** Measured with `bin/fusion-prose-metric`: the six emitted files carry 8 prose em-dashes over 13 292 prose words, 0.6 per 1000, all under the ceiling; `CLAUDE.md` carries 126 over 8 892, **14.2 per 1000**. It is about 40 per cent of the prose an agent holds and 94 per cent of the em-dashes left in it. It was excluded from the style Circle deliberately, by `circles/260820-2051-.../260820-2314_*_is-claude-md-inside-the-corpus-this-circle-repairs.md` option 3, which parked it "for the Circle that runs the measurement". The new anticipated Circle does not run the measurement and says so, "so whether that repair belongs here is open and is question 3 of the batch" (`circles/260821-1042-.../_a_circle.md`). **This run does not repair it**, and the four `CLAUDE.md` entries above are targeted corrections that add no em-dash beyond the two already inside L08's preserved clauses. **Executor: the user, at question 3 of that Circle's batch; then whoever that Circle names.**

2. **`agents/curator.md:212` and `skills/curate/SKILL.md:110` state a capability no shipped code has had since 2026-08-16.** Both still call a write denied by the project's guard configuration a `failed` entry, and the guard has decided nothing since `260816`. This is the single unmet Directive clause behind a Bounded Closure: `260816-1741-guard-becomes-observation-only` `## Closure note`, re-verified byte-unchanged across four releases by `260819-1400-reconciliation-circles.md`. Both files are outside this agent's remit by exclusion 5, and both lines were verified standing at this HEAD. Open record: `260817-1505_*_the-curator-and-its-skill-still-say-a-projects-guard-configuration-can-deny-a-write.md`. **Executor: `coder`.** No duplicate filed.

3. **An answered decision has still not been realised on any surface, and one of them is `CLAUDE.md`.** `260816-1707_*_which-install-path-is-the-authoritative-one-for-end-users.md` answered option 1 on 2026-08-16: `heads/main` is the standard install path and a tag is an opt-in pin. Re-verified at this HEAD: `CLAUDE.md` describes the default and the pin example without ever saying which is policy. This was item 2 of the previous run's own list and is unmoved. It is neither of the two things this agent changes text for: nothing contradicts the answer and nothing is obsolete because of it. Writing the policy sentence would be realising an answered decision, an addition rather than a repair. **Executor: `coder` for the sentence, on the user's instruction; then `reconciler` for the marker.**

4. **Three answered decisions are realised on disk and their markers have not moved.** All three were verified this run with the command in hand: `260815-2056_a` (the `Retired:` widening) is realised at `rules/fusion-workbench-conventions.md:317`, `:435` and `:527`; `260814-1915_a` (the audit line required on every mode-3 run, self-test deleted) is realised at `agents/shaper.md:66`, which now reads "there is no self-test that waives it"; `circles/260815-0007-.../260815-1845_a` (analyst gets a rule pattern) is realised at `bin/fusion-rules:181`, `analyst) PATTERNS="analyst" ;;`. Each record's most recent reconciliation note says the opposite, because each was written before the work landed. Advancing a marker on ground-truth verification is exclusion 1. **Executor: `reconciler`.**

5. **Part (c) of `260810-1544_a` has never been filed as its own record, and four shipped surfaces point at a question with no home.** The record's own footer says what clears it: "File part (c) as its own decision record ... and this record then moves to `_i_` on part (b) alone." This agent's authorisation to write a decision record is bounded to a contradiction between two live and defensible positions, which this is not. **Executor: the user.**

6. **`rules/user-facing-output.md:141` states the em-dash ceiling with no per-file scoping**, while `CLAUDE.md:41` and the open record both read it per file. Adding the scoping would realise an orchestrator-authored answer from an open record into always-on rule text. **Executor: the user, when they answer that record; then `coder`.**

7. **The `rules-emission.golden` fixture will go red on any approval that includes L03, L04 or L05.** `hooks/lib/__tests__/fixtures/rules-emission.golden` pins `fusion-workbench-conventions.md`'s byte size in each of the fifteen agents' blocks, so the net +59 turns `npm test` red until the golden is regenerated with `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, and the diff read. This is the fixture case, not the out-of-remit case: the edits are in remit and gated, and only the consequence belongs to somebody else. `rules/context-lean-claude-md.md` is emitted to no agent and appears in no block of that golden, so L02 moves nothing. `CLAUDE.md` is in no fixture and under no growth bound, so L01, L06, L07 and L08 move nothing. **Executor: `coder`. This agent does not run it.**

## 9. Confidence, stated per the critical-stance rule

**Verified**, each with the command and its output in the entry or above:

- that `skills/archive/SKILL.md:96` is blank, that the sentence it used to hold is now at `:102`, and that it held it at `e14b6ca` (`sed -n`, `git show`, `git log --oneline`);
- that `rules/decision-record-examples.md` Example 2 reads `_i_ → _s_` today and read `[a] → [s]` at `b05b423`, and that `b393a45` made the change inside this interval;
- that `rules/critical-stance.md` carries four numbered sections and says "The four norms below", and that §4 arrived in `327d0b6` on 2026-08-07;
- that `bin/fusion-rules` announces the voice-profile fallback on standard error, with standard output unchanged;
- that `docs/upgrading-to-v10-3.md` and `docs/upgrading-to-v10-4.md` each state in their opening lines that they remove nothing from an installed base, and that `## Release process` names no step that writes such a note;
- that the em-dash-ceiling record carries `_o_` and says its answer is not the user's;
- that all 25 wildcard record citations on the two text surfaces resolve, and that four of them resolve to an open record;
- that all 7 line pins on those surfaces resolve to a line that exists, and that six of the seven hold the sentence described;
- that the always-on set stands at 95 007 bytes against a baseline of 86 573, leaving 3 566 bytes of the 12 000-byte head-room;
- that the hook suite is 718 tests across 40 files, all green, exit 0;
- that `bin/fusion-rules` emits five rule files to every one of the 15 agents, `circle-records.md` to three, `commit-lock.md` to one, `design-diagrams.md` to four, the writing profile to eight, and five rule files to no agent at all;
- that all 13 provenance headers cite a `_c_` Circle, an implemented decision record, or a commit, and none cites a superseded record;
- that `agents/shaper.md:66`, `bin/fusion-rules:181` and the three `Retired:` sites in the conventions file each carry the realisation their record's reconciliation says is absent.

**Inference**, and labelled as such:

- `inference:` that L06's sentence is falsified rather than merely loose. It rests on reading "is settled" as a claim about the question's disposition rather than about the tool's behaviour. The tool does read per file, so a reader could take the sentence the other way. The record's own "must not be read as one" is what decides it here, and that is a reading of prose.
- `inference:` that L01's clause is not a preserve-list critical procedure. It rests on `## Release process` naming no step that writes an upgrade note, which is a verified absence, and on reading a Layout-table row as descriptive. A user who reads it as a release rule should reject L01, which is why it sorts first.
- `inference:` that C2's precedence gap widened rather than merely moved. The conventions passage now names four touching parties where the previous run read two, but the git history of that paragraph was not walked to confirm the two lifecycle-skill clauses are new rather than pre-existing.
- `inference:` that C6 is a demotion of a mechanism rather than a supersession of a decision.
- `inference:` that L07's and L08's corrections, rather than their conversions to a derivation, are the proportionate act. It rests on distinguishing a semantic enumeration that has moved once from a directory listing that moves every session, and no record states that distinction in those words.

**No speculation is carried in any entry.** L02 is the run's only Tier 3, and it is an entry rather than a candidate precisely because the two facts the tier demands, when the practice stopped and what replaced it, are both citable this time.

## 10. Where the ledger's own reach stops

490 of 572 session histories, 75 of 80 reviews, all 25 analyses, nine of fourteen Circle record bodies and 290 of 291 archived files were not opened. Seventy-nine terminal decision records were read as markers and filenames only. Every statement on all three surfaces that characterises nothing outside itself entered no pair, and on `CLAUDE.md` that is most of a 66 626-byte file. Four curator runs have now taken four disjoint selectors, guard removal, who-may-do-what, measurements no gate reads, and characterisations of another surface, and a stale statement outside all four has not been looked for by any of them.

One reach limit is worth stating separately, because it is new. The interval left **eleven open decision records behind a closed Circle**, eight of them carrying answers written by an orchestrator in the user's absence, and six of those eight are already shipped into always-on rule text or into `CLAUDE.md`. This run repairs the two places a normative surface calls one of them settled. It does not and cannot decide whether the user would have chosen those answers, and every one of the eleven stays live until the user meets it.
