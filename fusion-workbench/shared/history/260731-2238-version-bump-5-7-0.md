# T3 — version bump to 5.7.0 and release-pin refresh

**Agent:** coder
**Status:** Complete
**Date:** 260731-2238

## What was done

Two files changed, no commit (user commits after verifying).

1. `.claude-plugin/plugin.json` — `"version"` `5.6.0` → `5.7.0`. Feature addition: the
   `/fusion:cadence` skill landed in tasks T1 (`a4c37b2`) and T2 (`8c1c9f8`).
2. `install.sh` line 27 — the `FUSION_REF` pin example `tags/v5.5.1` → `tags/v5.7.0`.
   The example was two releases stale: `git tag --list` shows `v5.5.0`, `v5.5.1`,
   `v5.6.0`, so the v5.6.0 release did not refresh it. T5 creates the `v5.7.0` tag.

## Not changed, deliberately

`plugin.json` `description` left as-is. It enumerates agents, guard, monitor and the
Plane mirror, and names no skill at all. Adding cadence alone would break that level of
abstraction, and the string is duplicated verbatim in the marketplace entry (T4), so any
edit has to be mirrored there.

No changelog file exists in this repo (`grep -rn CHANGELOG` over `*.md`/`*.json`/`*.sh`
returns nothing), so no changelog entry was added.

## Verification

- `claude plugin validate .` — passed with the pre-existing root-CLAUDE.md warning.
- `npm test` in `hooks/` — 316 tests, 12 files, all passed.
- `git status --porcelain` — exactly the two modified files plus untracked
  `fusion-workbench/`.
- `grep -rn "5\.6\.0"` over `*.md`/`*.json`/`*.sh` (excluding `fusion-workbench/`) — two
  hits left, both in `hooks/package.json` (`@types/node ^25.6.0`, `typescript ^5.6.0`),
  unrelated npm dependency ranges.
