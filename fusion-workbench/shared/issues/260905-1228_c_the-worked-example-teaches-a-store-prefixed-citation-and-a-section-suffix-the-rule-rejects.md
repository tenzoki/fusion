The worked example teaches a store-prefixed citation and a section suffix the rule rejects
---
`rules/decision-record-examples.md:53` shows `Answered: analyses/260501-1730-vector-store-comparative.md §5 — …`. The path half carries the store segment `analyses/`, which `rules/fusion-workbench-conventions.md` `## Filename Patterns` calls a violation the gates report, and the locator half is `§5` where the same paragraph mandates a `:line` suffix. The file is emitted to the five decision-transition agents, so the wrong form is taught to every agent that performs a transition.
---
**Filed by:** consultant, Kai Stalmann <ks@qantr.com>

**Where it comes from.** A consuming project reported the divergence on 2026-09-05 and the user relayed it. The conventions half of that report was repaired the same day in commit `505ac4df`, which replaced the contradicting sentence in the `_a_` marker row with a pointer to `## Filename Patterns`. The example file was not touched and still teaches what the pointer now forbids.

**Evidence.** `rules/decision-record-examples.md:53`. The rule it contradicts is `rules/fusion-workbench-conventions.md` `## Filename Patterns`, in the paragraph beginning "Cite a record by its storeless basename". `STORES` at `hooks/lib/citation-scan.ts:137` includes `analyses`, and the header at `:153` states that every `REC_RE` match is reported `store-prefixed`, so the token would be a reported violation in any file the gate judges.

**Why the gate does not report it.** Two independent exemptions cover the line, either alone sufficient. `hooks/lib/citation-scan.ts:212` lists this file in `RECORD_EXAMPLE_FILES`, and `:563` applies that exemption ahead of every other check, so every token in the file returns `exempt` with reason `record-example-file`. The line also sits inside a fenced code block, exempted at `:579`. The exemption's granularity is a separate defect, filed as `260905-1228_*_the-record-example-exemption-silences-a-verdict-that-does-not-depend-on-resolution.md`.

**One half is blocked.** The store segment is settled and can be corrected now: the storeless basename is `260501-1730-vector-store-comparative.md`, since a markerless artifact keeps its dashed name. The locator is not settled. `:line` is what the rule says and what one record in 30 writes; 11 of 30 use a heading anchor. That question is filed as `260905-1228_*_does-a-resolution-line-cite-path-line-or-a-heading-anchor.md`, and correcting the example to `:line` before it is answered would pin the teaching file to a form the corpus has abandoned.

**Acceptance.** The example's path half carries no store segment. Its locator half matches whatever the decision record above answers, and the correction lands in the same change as that answer rather than ahead of it.

---
Resolved: both halves of the line corrected in one change, in `rules/decision-record-examples.md` `## Example 1: Happy path: _o_ → _a_ → _i_`. The path half lost the `analyses/` segment and is now the storeless basename `260501-1730-vector-store-comparative.md`; the locator half is a heading anchor rather than `§5`, because `260905-1228_*_does-a-resolution-line-cite-path-line-or-a-heading-anchor.md` answered option 1 and the anchor is now the mandated form on a resolution line (`rules/fusion-workbench-conventions.md` `## Filename Patterns`). The same line also gained `ruled by`, per `260905-1228_*_does-an-answered-record-carry-who-ruled-now-that-only-the-orchestrator-may-transition-it.md`. Example 3's `Deferred:` line gained the same field; Example 2's `Superseded by:` and Example 1's `Implemented:` needed no change — they cite a storeless basename and a commit hash, neither takes a locator, and neither carries a ruler. The exemption defect stays open and is untouched.
