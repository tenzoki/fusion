CLAUDE.md kennt weder `bin/fusion-plugin-cwd` noch die Repo-Präferenz, und die D3-Verhaltensregel (a) fehlt in der Release-Prozedur
---
CLAUDE.md ist nach dem Textschicht-Circle an drei Stellen hinter dem Code zurück, alle drei aus derselben Änderung (D3/S8, Commit `c45fb44`):

1. **Die Layout-Tabelle führt jeden `bin/`-Helper einzeln — außer dem neuen.** `bin/fusion-plugin-cwd` (neu in `c45fb44`, mit eigener `.gitignore`-Ausnahme) hat keine Zeile; `monitor`, `fusion-rules`, `fusion-paths`, `fusion-workbench-root`, `fusion-session-mark`, `fusion-plane` haben alle eine (CLAUDE.md:24–30). Genau die Klasse „abschließende Aufzählung blieb stehen", die dieser Circle eliminiert hat — vom Circle selbst neu erzeugt. Der Enumerations-Lint prüft die Skill-Liste und `DEFINITION_SITES` in CLAUDE.md, die bin-Tabelle nicht.
2. **Der „Rules loading"-Bullet (CLAUDE.md:49) beschreibt nur die Drei-Wurzeln-Suche.** Er sagt nichts davon, dass die Helfer im Plugin-Repo seit `c45fb44` die Work-Tree-`rules/` der installierten Kopie vorziehen und dass `protected-path-internals.md` nur noch im eigenen Repo emittiert wird. README-agents.md:156 trägt beides; CLAUDE.md — dessen Leser exakt die Sitzungen in diesem Repo sind, also die einzigen, für die die Präferenz überhaupt gilt — trägt nichts.
3. **Die D3-Verhaltensregel (a) ist nirgends gelandet.** Entscheidung `circles/260805-2005-textschicht-gegen-code-nachziehen/decisions/260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen` hält fest: „Verhaltensregel (a) bleibt als Dokumentation bestehen … ihr Zuhause ist die Release-Prozedur in CLAUDE.md" — weil Guard und Tracker weiterhin aus der installierten Kopie laufen, bleibt „`fusion --update` plus Neustart vor Regelarbeit" relevant. Gemessen: `grep -iE "stale|update" CLAUDE.md` trifft keine solche Zeile; die Release-Prozedur (Schritte 0–6) trägt den Guard-Check aus dem Vorgänger-Circle, den Update-Check nicht.
---
Kontext: Befund einer Implementation-vs-Intention-Review über `4a8fea0..cde5319` (Review-Datei `shared/reviews/260806-*-coderev-textschicht-delta*.md`). Plan-Schritt 11 (Batch B, `9a96466`) hat CLAUDE.md korrigiert, lief aber im selben Turn-Umfeld wie die D3-Realisierung (`c45fb44`) und hat deren Niederschlag nicht nachgezogen. Fix: eine Tabellenzeile, eine Bullet-Ergänzung, eine Zeile in der Release-Prozedur — reine Textarbeit in einer nicht ausgelieferten Datei (Installer kopiert CLAUDE.md nie).
