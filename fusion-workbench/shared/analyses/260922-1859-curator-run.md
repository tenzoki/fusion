# Curator run 260922-1859 — survey of the three normative surfaces

**Date:** 2026-09-22 18:59
**Status:** Complete
**HEAD read:** `32950b63`
**Mode:** survey (`**Scope:** anchored`, `**Placement:** off`, `**Edges:** off`), then apply at 2026-09-22 19:52 with `**Approved:** all`, which is the five ledger entries; the two candidates were never on offer and took `not-offered`. The apply pass read HEAD `32950b63`, the same commit the survey read.
**Anchor:** `last_curator_run` = `2c826154` (2026-09-22 06:58:04 +0200), 72 commits behind HEAD
**Previous curator run:** `260922-0703-curator-run.md`, 2026-09-22, survey plus apply, which set the anchor this run is bounded by. Found by walking `$WORKBENCH` for `*-curator-run.md`: 16 run files across the tree, 9 of them outside `$SCAN_ANALYSES`.
**Work item in scope:** none. `bin/fusion-claimed-item` prints nothing at exit 0, so `$SCAN_DECISIONS` and `$OUT_ANALYSIS` resolve to the shared store alone.

The dispatch asked for the three normative surfaces against the project's recorded history, bounded by the anchor. No work item was read as a subject and no placement was classified, so this file carries neither of those sections.

## 1. Evidence-source counts

| # | Source | Read | Bound |
|---|---|---|---|
| 1 | Work items (`$SCAN_BACKLOG` = `circles`) | 39 containers enumerated for `**Status:**`; 3 are live (`260917-2253-…` paused, `260922-0137-…` open, `260922-1038-…` open). 9 item records carry an mtime past the anchor and were read. | mtime > anchor date |
| 2 | Decision records (`$SCAN_DECISIONS` = `shared/decisions`) | 94 records, marker census `_i_` 74, `_a_` 15, `_d_` 4, `_o_` 1. **1** changed since the anchor (`260830-1842_*_may-the-grammar-resolve-a-bracket-marked-record-that-a-frozen-store-keeps-permanently.md`, read in full; the change fences an exhibit in a blockquote and moves no position). The 16 non-terminal records were read in full as the pair-2 and pair-3 selector. | `git diff` since anchor, plus every non-terminal record |
| 3 | Git history | `git log 2c826154..HEAD` = 72 commits. Over `CLAUDE.md` and `rules/`: 9 commits, 4 rule files changed (`agent-setup.md`, `context-lean-claude-md.md`, `fusion-workbench-conventions.md`, `workbench-path-resolution.md`), `CLAUDE.md` **0**. Every hunk read. 12 decision records filed or transitioned inside work-item containers in this window were read in full through this source rather than through source 2, which no longer reaches a container. | `<anchor>..HEAD` |
| 4 | Reviews and analyses | 207 files in the live tree; **13** carry an mtime past the anchor. Read by name; the 3 that name a rule file or `CLAUDE.md` were opened. | mtime > anchor date |
| 5 | `orchestrator-events.jsonl` | 4 706 rows, **210** with a `ts` past the anchor. Corroborating only; none was needed as evidence. | ts > anchor |
| 6 | Archive | 5 sweep directories; **1** added since the anchor, `260922-1514-backlog-store-frozen`, holding the 2 entries of the retired `shared/backlog/` store. Both read. | archived since anchor |
| 7 | Provenance headers | **15 of 15** rule files carry one within the first ten lines. Six cite a decision record, four a work-item container, four the admission-plus-commit form, one a partition note with two records. Every cited record was resolved: none carries the superseded marker, so no Tier 2 retirement candidate arises here. All four cited commit hashes resolve (`git cat-file -t`). | full |

**Unreadable:** none. **Absent:** `.claude/rules/` does not exist, so the project-owned rule surface is `./rules/` alone.

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 8 021 | 66 | 8 021 | 66 |
| `./rules/*.md` | 15 | 212 505 | 2 129 | 213 207 | 2 132 |
| Decision records (`$SCAN_DECISIONS`) | 94 | 701 274 | 8 691 | 701 274 | 8 691 |

Commands: `wc -c -l CLAUDE.md`; `wc -c -l rules/*.md | tail -1`; `ls -1 rules/*.md | wc -l`; `cat fusion-workbench/shared/decisions/*.md | wc -c -l`, the same four re-run after the apply pass. `CLAUDE.md` and the decision store repeat their "before" figures because no entry touched either; the rule corpus grew by 702 bytes and 3 lines across the two files the five entries rewrote, and its file count is unchanged, no rule file having been created or deleted. Removals by tier: none.

**Blast radius:** the ledger proposes no deletion and no relocation, so 0 bytes leave any surface. The five entries rewrite five sentences in two rule files. No separate scale prompt is owed.

**Two notes on what the edits would and would not cost.** Both target files, `rules/workbench-tracking.md` and `rules/context-lean-claude-md.md`, are emitted to **no agent** (checked by running `bin/fusion-rules` for all eleven agent names), so neither the per-dispatch-path bound nor `hooks/lib/__tests__/fixtures/rules-emission.golden` counts a byte of either, and no fixture regeneration is owed. And `bin/fusion-prose-metric` puts `workbench-tracking.md` at 2 em-dashes against a permit of 2, verdict `ok`: every After block below is written without one, so applying them leaves that verdict where it stands. (`context-lean-claude-md.md` reads `over` at 37 against a permit of 2 before this run touches it; that is a standing debt of the file, not something an entry here creates or clears.)

## 3. Comparison counts

Every verdict below covers the pairs its selection rule reached, not the corpus.

| Pair | Selection rule | Pairs produced | Pairs read | Verdict |
|---|---|---|---|---|
| 1 decision vs decision | slug-token overlap over all 94 shared records: two records pair when their slugs share at least three content words of four letters or more, stop words dropped. 4 371 total pairs, 6 selected. Plus the 1 record changed since the anchor against the 2 records its own body names. | 6 + 1 | 7 | **no contradiction.** All six selected pairs are complementary rather than opposed, and four of the twelve records carry a `Retired:` line naming the same cut. The one changed record is a `_d_` whose deferral names two targets: both have since been ruled (`260921-1718` and `260921-2002`, each `_i_` as of 260922-1420), the third condition (a citation count in a consuming project) has not been met, so the deferral is not discharged. Advancing that marker is ground-truth verification and belongs to the reconciler, not here. |
| 2 decision vs rule file | (a) the 16 non-terminal shared records read in full against the rule corpus; (b) the 24 `Retired:` lines across all 94 shared records, each naming a removed mechanism, grepped against `rules/` and `CLAUDE.md` for a rule that still binds it; (c) the 12 container decision records of this window against the 4 rule files changed in it. | 16 + 24 + 12 | 52 | **two contradictions.** The first is fixed on its falsified side (L01, L02). The second is reported and neither side is edited: `## 3a` below. Selector (b) returned three surviving mentions of a retired mechanism, and all three name it as removed rather than binding it: `playmaker` in `workbench-path-resolution.md`, the Turn loop and `agentstate.yaml` in `orchestrator-rebalance.md`, the stash protocol in `commit-lock.md`. Selector (c) found `rules/fusion-workbench-conventions.md` updated in step with every ruling of the window, and `rules/workbench-tracking.md` left behind by one of them. |
| 3 decision vs `CLAUDE.md` | the same 16 + 24 + 12 against `CLAUDE.md` in full | 52 | 52 | **no contradiction.** `CLAUDE.md` did not change in this window and none of the window's rulings reaches a sentence in it. C02 below records the one sentence that sits closest to the line and why it is reported rather than proposed. |
| 4 rule vs rule | the 4 files changed since the anchor, hunk by hunk, against each other and against the 11 unchanged rule files read for the passage each hunk touches; plus every path-shaped token in all 15 files checked for existence | 4 × 14 + 67 tokens | 4 × 14 + 67 | **one contradiction** (L01, L02): `workbench-tracking.md` `## The four classes` counts the frozen stores at two where `fusion-workbench-conventions.md` `## fusion-workbench Layout` now names three. Of the 67 path-shaped tokens, 2 name a file absent from this tree and both are correct: `./rules/context-manifest.yaml` is a **consuming project's** optional manifest, which this repository is not required to carry. |
| 5 rule vs `CLAUDE.md` | every checkable assertion in `CLAUDE.md`'s 66 lines run as a command against the tree, then each pointer's target section opened and read for the thing the pointer says it carries | 66 lines, 22 pointers | 66 + 22 | **no contradiction, and no stale reference.** All 22 heading anchors resolve (`grep -Fxc` on each). The counted claims hold: 11 agent prompts, none declaring a `tools:` line; 14 skill directories each holding a `SKILL.md`; 25 `bin/` entries against 25 roster rows in `README-hooks.md` `### The bin/ helper roster`; `EXEMPT_SKILLS` = `{setup, migrate}`; `CLAUDE.md` charged to all eleven dispatch paths at `DISPATCH_HEAD_ROOM = 0`. C01 and C02 are the two sentences that did not clear cleanly, and both are reported rather than proposed. |
| 6 `CLAUDE.md` vs itself | all 66 lines against each other | 1 | 1 | **no contradiction.** |

### 3a. The second pair-2 contradiction, and why nothing here touches it

`260816-0119_*_can-anything-carry-the-rename-to-citation-obligation-when-a-record-marker-moves.md` (marker `_a_`) carries this `Answered:` line: "option 1: nothing new; the reference lint remains the whole mechanism. **No fusion mechanism rewrites shipped text.** User answered inline 2026-08-16." That last clause was the record's own third constraint, which called a shipped-text rewriter "a threshold, not a detail".

**The clause is false at HEAD in this repository.** `bin/fusion-citation-sweep` rewrites citations across a declared corpus, and `fusion.json` declares `citations.extraPaths` as `["bin/*", "hooks/*.ts", "hooks/*.mjs", "hooks/lib/*.ts"]`, all of it shipped text. Two sentences in the rule corpus now describe that rewriter as normative: `rules/fusion-workbench-conventions.md` `## Marker globs` ("The fence does keep the sweep off it, so an exhibit is never machine-rewritten") and the same file's `## Terminal states are history` ("respelling a citation's pre-v4 bracket marker to the storeless wildcard names the same target and writes no state, as the sweep already does for an underscore one here"). An agent holding the record's constraint and the rule file's description cannot obey both.

**Neither side is edited, and no record is filed, for three reasons that each stand alone.** The `Answered:` line records what the user answered on 2026-08-16 and rewriting it would erase the reversal rather than point at it, which is the reason `## Inline State Tracking` gives for leaving a `Resolved:` note unedited. The rule file's two sentences are true. And the record already carries the question: its reconciliation note of 260921-2230, written by the reconciler, states that the ground moved, names the sweep, and ends "This is the user's call: accept the class of one as never-realisable, or file the dissolution as the answer." Filing an open decision record here would put a second file behind a statement one file already carries, which `rules/fusion-workbench-conventions.md` `## Record filing` calls the duplication to refuse. Advancing that record's marker on what the tree now shows is the reconciler's work, not this pass's.

**Precedence-undecided:** not applicable. `.claude/rules/` is absent, so there is one rule root and nothing to order.

**Open defects as a cross-check.** `$SCAN_ISSUES` = `shared/issues`, 137 records, of which the open ones were listed by name and the 3 naming a rule file or `CLAUDE.md` were read. None asserts the opposite of anything proposed here, so no entry is downgraded on that ground. No `_i_` decision in the shared store is contradicted by an open defect describing its implementation as absent.

**What this run did not read, stated plainly.** The 94 shared decision records were not read in full: 78 are terminal and were read by their annotation lines alone, which is what selectors (a) and (b) of pairs 1 and 2 consume. The 13 reviews and analyses past the anchor were read by name except the 3 that name a surface. The 210 new event rows were counted, not read. The pair-1 verdict covers 7 of 4 371 possible pairs, and no claim here is stronger than that.

## 4. Pre-edit content

No decision record is proposed for modification. Nothing to record.

## 5. The ledger

Groups at the gate, most consequential first: tier-1 (5). No constraint removal, no tier-3, no tier-2, no relocation, no edge, no consolidation. Candidates: 2, not on offer.

Four of the five entries sit in one file, `rules/workbench-tracking.md`, and each is one sentence with a before-text that is unique in that file (verified by counting occurrences), so the four are independently approvable and no write falsifies another's before-text.

### L01 — the partition's stated domain counts the frozen stores at two, and the paragraph it points at names three

- **Surface:** project rule file
- **File:** `rules/workbench-tracking.md`, `## The four classes`, first sentence
- **Tier:** 1
- **Citation:** `grep -c 'Three legacy stores are absent from this tree on purpose' rules/fusion-workbench-conventions.md` → `1`; that paragraph, which is the one "beneath the tree" this sentence points at, names `stashes/`, `.migration-v2-backup/` **and** `shared/backlog/`. It was rewritten from "Two legacy stores" to "Three legacy stores" at commit `e47977ca` (2026-09-22 15:19), which realises `260922-0922_*_what-becomes-of-the-two-entries-in-the-unread-shared-backlog-store.md` (marker `_i_`), option 1 ruled by the user on 260922-1223. That commit touched `rules/fusion-workbench-conventions.md` and did not touch `rules/workbench-tracking.md`: `git show --stat e47977ca`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Derive over correct:** the figure is a cardinality standing beside a list the *other* file holds, which `rules/critical-stance.md` §5 says to enumerate or derive rather than assert. The sentence already points at the enumeration, so the numeral carries nothing and is dropped rather than corrected. The next store frozen then falsifies nothing here.
- **Revert path:** `git checkout -- rules/workbench-tracking.md`

**Before:**
> together with the two frozen stores named in the paragraph beneath the tree.

**After:**
> together with the frozen stores named in the paragraph beneath the tree.

### L02 — the R1 row enumerates two frozen stores where a workbench may hold three

- **Surface:** project rule file
- **File:** `rules/workbench-tracking.md`, `## The four classes`, the R1 row of the class table
- **Tier:** 1
- **Citation:** same as L01: `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout` names three legacy stores a workbench may carry, the third being `shared/backlog/`, frozen at `e47977ca`. `find fusion-workbench/archive/260922-1514-backlog-store-frozen -type f` → the store's two entries, now under `archive/`; `ls fusion-workbench/shared/backlog` → `No such file or directory`, the commit message stating "The emptied directory is removed."
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Why the After does not simply add a third name:** the row already carries "`shared/` with every store inside it", so `shared/backlog/` is classified R1 by that clause and listing it again would be a second answer to a question the row has answered. The repair says where the third one is covered instead of double-listing it.
- **Revert path:** `git checkout -- rules/workbench-tracking.md`

**Before:**
> and where a workbench still holds them the two frozen stores `stashes/` and `.migration-v2-backup/`

**After:**
> and where a workbench still holds them the frozen stores `stashes/` and `.migration-v2-backup/`; the third, `shared/backlog/`, sits inside `shared/` and the clause above already carries it

### L03 — "the two that have done so" counts the entries that left the layout tree at two, and four have left it

- **Surface:** project rule file
- **File:** `rules/workbench-tracking.md`, `## The four classes`, third sentence of the opening paragraph
- **Tier:** 1
- **Citation:** the tree's root entries at the commit before the 2026-09-10 cut, against the tree today. `git show 6357ebfc^:rules/fusion-workbench-conventions.md | sed -n '/^fusion-workbench\//,/^```$/p' | grep -E 'agentstate|orchestrator-live|portfolio|active-circle'` prints four rows: `portfolio.md`, `.active-circle`, `agentstate.yaml`, `orchestrator-live.md`. The same extraction over the file today: `sed -n '/^fusion-workbench\//,/^```$/p' rules/fusion-workbench-conventions.md | grep -cE 'agentstate|orchestrator-live|portfolio|active-circle'` → `0`. So four entries have left the tree, not two. `grep -cE 'agentstate|orchestrator-live' rules/workbench-tracking.md` → `0`: two of the four are named nowhere in the file that promises to name them.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Derive over correct:** the numeral is a cardinality beside a list (`rules/critical-stance.md` §5) and it has now been wrong once. It is dropped rather than moved to four, so the next entry to leave the tree falsifies nothing here; the enumeration below is what a reader counts. The clause "because one of them is still on disk" follows the same change, since three of the four are on disk in this workbench today.
- **Revert path:** `git checkout -- rules/workbench-tracking.md`

**Before:**
> An entry that leaves the tree leaves the partition with it, and the two that have done so are named below rather than dropped silently, because one of them is still on disk in workbenches that were set up earlier.

**After:**
> An entry that leaves the tree leaves the partition with it, and the entries that have done so are named below rather than dropped silently, because some of them are still on disk in workbenches that were set up earlier.

### L04 — the paragraph that names the departed entries names two of the four, and the two it omits are on disk in this workbench

- **Surface:** project rule file
- **File:** `rules/workbench-tracking.md`, `## The four classes`, the paragraph opening "Two entries left this class by leaving the layout"
- **Tier:** 1
- **Citation:** the four-entry measurement of L03, plus what is on disk and what writes it. `ls -1 fusion-workbench/agentstate.yaml fusion-workbench/orchestrator-live.md fusion-workbench/portfolio.md` → all three present; `ls fusion-workbench/.active-circle` → `No such file or directory`, which is what the paragraph already says of that one. `git ls-files fusion-workbench/agentstate.yaml fusion-workbench/orchestrator-live.md fusion-workbench/portfolio.md` → empty, so none of the three is tracked here. Nothing writes either omitted file: a grep over `agents/`, `skills/`, `hooks/*.ts`, `hooks/lib/*.ts` and `bin/` returns only comments recording the removal, and `agents/orchestrator.md` `## Setup` states it as a prohibition, "Do not write `fusion-workbench/agentstate.yaml`, do not write `fusion-workbench/orchestrator-live.md`, and do not re-create either under another name." `rules/orchestrator-rebalance.md` `## What the cut took out of this file` dates the removal: "The Turn loop, the Turn budget, the per-Turn Coherence check, the circuit-breaker table and the session state file were all removed from fusion on 2026-09-10." Nothing deletes either: a grep over `skills/` returns no match, and `/fusion:check`'s `leftovers` selector reads `.guard-state/` only (`skills/check/SKILL.md` `## leftovers`).
- **Consequence group:** tier-1
- **Constraint removed:** none
- **What this entry adds, said plainly:** it writes a class verdict for two entries the file does not currently mention. Neither verdict is invented here. "Do not track them" is what the tree already does, measured above, and the reason given is the one this same section already gives `.session-marker` and `.commit-lock/` under *Why each class L entry is in class L*: a file describing the session running at the moment it was written is, restored by a checkout, a statement about a session that has ended. If that reads as an authoring call the user would rather make themselves, refusing this entry and taking L03 leaves the false count fixed and the enumeration short, which is the smaller of the two defects.
- **Revert path:** `git checkout -- rules/workbench-tracking.md`

**Before:**
> **Two entries left this class by leaving the layout, and they left it in opposite ways.** `.active-circle` named the active Circle. It stopped being read when the Circle container was cut on 2026-09-10, and when the per-work-item container was restored the day after, what replaced it was not a second pointer but a field: scope is a work item's own `**Claim:**` (`rules/fusion-workbench-conventions.md` `## Path Resolution`). Nothing creates the file, and `/fusion:migrate` deletes one it finds, so no workbench keeps one and there is nothing left to classify. `portfolio.md` is the other case and is still worth an answer: it was regenerated in full on every ranking run until v11 removed the agent that wrote it, nothing deletes it, so a workbench set up under an older fusion still holds one. Do not track it. A committed copy is a briefing about what one checkout had pulled at the moment it ran; merging two of them settles nothing the next run does not overwrite, and there is no next run.

**After:**
> **Four entries left this class by leaving the layout**: `.active-circle`, `portfolio.md`, `agentstate.yaml` and `orchestrator-live.md`, and they did not all leave it the same way. `.active-circle` named the active Circle. It stopped being read when the Circle container was cut on 2026-09-10, and when the per-work-item container was restored the day after, what replaced it was not a second pointer but a field: scope is a work item's own `**Claim:**` (`rules/fusion-workbench-conventions.md` `## Path Resolution`). Nothing creates the file, and `/fusion:migrate` deletes one it finds, so no workbench keeps one and there is nothing left to classify. The other three are still worth an answer, because nothing writes any of them and nothing deletes one either, so a workbench set up under an older fusion still holds all three. `portfolio.md` was regenerated in full on every ranking run until v11 removed the agent that wrote it. `agentstate.yaml` and `orchestrator-live.md` went at the 2026-09-10 cut that took the Turn loop and the session state file with it, and `agents/orchestrator.md` `## Setup` now states as a prohibition that neither is written nor re-created under another name. Do not track any of the three. A committed copy of one of them is a briefing about what one checkout held at the moment it ran; merging two settles nothing the next run overwrites, and for all three there is no next run.

### L05 — the placement criterion says the weight helper prints `heading-level=0` for a headingless file, and it prints no level line at all unless the file is over the threshold

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`, `### Step 1 — divide the file by heading, before judging anything`, item 4 of the numbered list
- **Tier:** 1
- **Citation:** two runs of the helper on a headingless file. Under the threshold: a 54-byte file with no heading gives `claude-md=clean root=… bytes=54 lines=2 headings=0 threshold=4000 over=0 writes=none` and **no `heading-level=` line**. Over it: a 15 692-byte file with no heading gives `headings=0`, `heading-level=0`, `over=1` and one row, `over 15692 200 (preamble)`. The helper's own source is the reason: `echo "heading-level=$level"` sits inside the `claude-md=weighed` branch, and the `over -eq 0` branch exits one line earlier having printed neither the level nor any row. The sentence as written carries no threshold condition, so it is false for the ordinary case, a small file with no heading. Item 4 arrived at commit `1fbf501e` (2026-09-22, "docs(rules): Step 1 of the placement criterion names the no-heading case").
- **Consequence group:** tier-1
- **Constraint removed:** none. The division rule itself, level 0 and one passage, is unchanged and is what the helper does implement; only the claim about what the helper prints is narrowed to the branch on which it prints it.
- **Not a derivation:** the sentence states a program's behaviour, not a measurement of the tree, so `### Derive over correct` does not reach it and no helper produces the value.
- **Fixture note:** `bin/fusion-rules` emits this file to no agent, so `hooks/lib/__tests__/fixtures/rules-emission.golden` does not pin it and the dispatch-path rows do not count it. No regeneration is owed.
- **Revert path:** `git checkout -- rules/context-lean-claude-md.md`

**Before:**
> 4. A file with no heading at any level is one passage, the preamble, and the
>    level is 0; `bin/fusion-claude-md-weight` prints `heading-level=0` for it.

**After:**
> 4. A file with no heading at any level is one passage, the preamble, and the
>    level is 0. `bin/fusion-claude-md-weight` prints `heading-level=0` for it
>    only where it prints rows at all, which is where some passage stands over
>    the threshold; under the threshold it reports `headings=0` and no level
>    line, having no rows to say the level of.

### C01 — "the four hooks" in `CLAUDE.md` counts registered events, not scripts (candidate)

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, `## Layout`, the `hooks/` row: "the four hooks and what each writes"
- **Tier:** 1
- **Citation:** `hooks/hooks.json` registers four hook events, SessionStart, PreToolUse, PostToolUse and SubagentStop, which between them invoke six compiled scripts; `ls -1 hooks/*.ts | wc -l` → `14`; the section the row points at, `README-hooks.md` `## Files`, tables 60 rows and states no count of four anywhere.
- **Consequence group:** candidate
- **Why a candidate:** the check does not falsify the sentence. Four is the count of registered events and "hook" can mean an event. It is reported because a reader following the pointer meets fourteen entry points, sixty table rows and no four, and because `rules/critical-stance.md` §5 asks for a cardinality to be enumerated or derived rather than asserted, which this one is not. The derive-over-correct form, if the user wants it resolved, is a pointer carrying no count: "the hooks `hooks/hooks.json` registers, and what each writes". **This candidate was raised by the 260922-0703 run and recorded `not-offered` there**; nothing about it has moved since, and it is repeated rather than dropped.
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

### C02 — `CLAUDE.md` says agents write only to the workbench, and four of the eleven write outside it (candidate)

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, `## Conventions`, the **Workbench writes** bullet
- **Tier:** 1
- **Citation:** read as an absolute, the first clause is false for four agents. `agents/coder.md` owns `.go`, `.rs`, `.ts`, `.tsx`, `.py`, `.js`, `.java` and the build manifests; `agents/ontocoder.md` owns structured data wherever it sits; `agents/editor.md` writes customer deliverables project-side; `agents/curator.md` writes `CLAUDE.md` and the project's own `./rules/`. A rule file states the last of these as an obligation rather than a permission: `rules/rule-file-provenance.md` `## Provenance headers on rule files` reads "An agent that creates a rule file gives it a header in the same edit" and "This obligation falls first on the curator". That is a direct contradiction in the sense `## Contradictions` defines: an agent obeying the `CLAUDE.md` bullet would never write a rule file, and the rule file tells it to.
- **Consequence group:** candidate
- **Why a candidate, and not an entry:** the bullet falls under preserve-list category 1, a "do not do X" rule, and the preserve list admits only a Tier 2 change with an explicit superseding record against such an item. The evidence here is Tier 1, which that clause says is not sufficient, so the entry is not offered at the gate. The second reason is honesty about the reading: the bullet's own second sentence, "Sub-agents share no memory; everything persists through workbench files", supports a narrower reading in which the first clause is about where *durable state* goes, and on that reading nothing is false. `inference:` the absolute reading is the one a reader unfamiliar with the sentence's history would take, which is what makes it worth reporting; I cannot verify which was meant.
- **Constraint removed:** the repair would remove the constraint that an agent writes nothing outside `fusion-workbench/`. The form that keeps what is load-bearing is "agents write their records only to `fusion-workbench/`", which preserves the no-stray-record-store rule and drops the false absolute. The user rules on that, not this pass.
- **Revert path:** `git checkout -- CLAUDE.md`

## 6. Outcomes

| Entry | Outcome | Note |
|---|---|---|
| L01 | `applied` | `rules/workbench-tracking.md`, the numeral dropped from the partition's stated domain. |
| L02 | `applied` | `rules/workbench-tracking.md`, the R1 row now says where the third frozen store is covered. |
| L03 | `applied` | `rules/workbench-tracking.md`, the departed-entry count dropped in favour of the enumeration below it. |
| L04 | `applied` | `rules/workbench-tracking.md`, the enumeration now names all four and carries a class verdict for the two it omitted. |
| L05 | `applied` | `rules/context-lean-claude-md.md`, the helper's `heading-level=` claim narrowed to the branch that prints it. |
| C01 | `not-offered` | A candidate, so the gate never put it. Unchanged from the 260922-0703 run, which recorded the same outcome for it. |
| C02 | `not-offered` | A candidate, so the gate never put it. |

**Every entry passed both checks.** Each before-text was re-read from disk and agreed with the ledger, so no entry went `stale`; each written region was re-read and compared byte for byte against its After block, with one occurrence found for each and none of the five before-texts surviving anywhere in either file, so no entry went `failed`.

**What the two edited files measure afterwards.** `bin/fusion-prose-metric` puts `rules/workbench-tracking.md` at 2 em-dashes against a permit of 3, verdict `ok`, the permit having risen with the file's word count; `rules/context-lean-claude-md.md` reads `over` at 37 against a permit of 2, exactly where it stood before this run, which is the standing debt section 2 already named. The three tests that could have gone red on a rule-file edit pass: `surface-growth-bound`, `rules-emission-golden` and `provenance-header-lint`, 61 tests, all green. No fixture regeneration is owed and no coder work arises from this run.

**Revert path for the whole run:** `git checkout -- rules/workbench-tracking.md rules/context-lean-claude-md.md`. Both files are tracked. Nothing was committed; the edits stand in the working tree.

**Anchor:** advanced. `last_curator_run` moves from `2c826154` to `32950b63`, the HEAD this run read, now that the ledger is closed.
