# Code review — Turn 4, `b261d83..951c809`

---
**Sender:** coderev
**Reviewed-range:** `b261d83..951c809`
**Not-opened:** none
**Commits:** 10 — `36984d7`, `9f84254`, `29d62e2`, `9c58c4d`, `a0bd3fb`, `bb9d66d`, `61bd21f`, `b53c7dd`, `282ef42`, `951c809`
**Files in range:** 100 (56 outside `fusion-workbench/`, of which 17 are `hooks/dist/`)
**Cross-references:** issues `260811-2142_*_the-unresolved-turn-budget-leaves-the-phase-2-loop-with-no-monotone-bound-while-the-prompt-says-it-is-bounded.md` … `260811-2151_*_bin-fusion-turn-budget-ships-with-no-claude-md-layout-row-while-its-sibling-added-in-the-same-turn-got-one.md` filed by this pass; open records `260811-1614_*_the-drift-checks-turn-row-is-satisfied-by-a-turn-start-alone-so-a-turn-that-emits-nothing-else-reads-clean.md`, `260811-2105`, `260810-0410_*_the-layout-tree-calls-itself-exhaustive-and-omits-the-two-plane-runtime-files.md`, `260810-0510_*_two-of-the-queue-ground-lints-negative-controls-re-implement-the-logic-instead-of-calling-it.md`, `260811-1734`, `260802-0920_*_next-skill-activates-a-circle-without-updating-its-status-field.md`, `260808-0030`

---

## What "not opened" means here, and the carried list

**Nothing is carried forward as unopened.** `hooks/dist/**` (17 files) was not read line by line; it was verified by the stronger method the last three passes used — `cd hooks && npx tsc --outDir /tmp/tsccheck`, then `diff -rq dist /tmp/tsccheck`: **no differences, exit 0**. The committed build is the current build of the current source, so reviewing the `.ts` sources reviewed what ships. That is also the disposition of the carried `**Not-opened:** hooks/dist/**` from the Turn 3 review (`260811-1618-coderev-turn-3-range-7d9efc8-adaa545.md`), which this dispatch did not name but the standing obligation does.

Everything else was opened: the 39 source, prompt, rule, skill and configuration files as their diff plus the definition each one changes, and all 44 `fusion-workbench/` files. `agents/orchestrator.md` was read at all seven touched sections. `hooks/lib/config.ts`, `hooks/lib/git.ts`, `hooks/turn-budget.ts`, `bin/fusion-turn-budget`, `bin/fusion-source-root`, `bin/fusion-plugin-cwd`, `hooks/lib/state-drift.ts`, `hooks/lib/rules-write-exemption.ts` `boundedList`, and the three new test files were read in full.

`cd hooks && npx vitest run`: **52 files, 1335 tests, exit 0**, 92.8 s. `tsc` compiles clean.

---

## Summary

The two large mechanisms — the Turn budget becoming a configuration leaf and the Circle head fields getting an owner — are both well built, and the four new or rewritten gates are the most honest this session has produced: each carries its own vacuity control, its own negative controls, and a failure message naming the legitimate route. What did not travel with them is the *reasoning* in two of the new branches. The unresolved-budget branch states a safety property it does not have, and the record-counts split is disjoint in one direction only. Separately, the range's own measurements are firing at HEAD for a reason nobody has looked at: the drift check has reported a false `progress.turn` DRIFT on every tool call since this session resumed, because two rows of one report anchor "this session" differently.

## Totals

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 2 |
| Medium | 4 |
| Low | 4 |

Ten findings, ten records filed under `shared/issues/260811-214*` and `260811-215*`.

---

## The four claims the dispatch asked me to check, answered first

**1. The Turn-budget lint is non-vacuous — measured, not accepted.** I extracted `agents/orchestrator.md` and `skills/setup/SKILL.md` at `bb9d66d` and ran `budgetLiteralHits`'s seven patterns over them. The orchestrator produced **8 hits, and all seven patterns fired**:

```
orch.md:364  Maximum N Turns   -> Maximum 5 Turns
orch.md:368  counter/max       -> Turn:** <N>/5
orch.md:519  table row         -> | Max Turns reached | 5 |
orch.md:866  N-Turn            -> 5-Turn
orch.md:866  (default N)       -> max_turns` (default 5)
orch.md:868  reached(N/N)      -> reached (5/5)
orch.md:941  max_turns: N      -> max_turns: 5
orch.md:1092 counter/max       -> Turn:** --/5, Turn:** 1/5
```

The executor's claim holds. One qualification worth recording: the *setup-skill* case of the same gate produced **0 hits at `bb9d66d`** and produces 0 today, so that case has never measured anything — it is a regression guard on a surface a copy could land on, which is legitimate, but it is not evidence the gate works. The evidence is the orchestrator case. The remaining assertions (helper call, `[ -x ]` guard, unresolved branch, `progress.max_turns` in the schema, the setup skill citing rather than copying, `maxTurns` assigned once, absent from `hooks/config.json`, `_turnBudget` in both guard files) all fail against `bb9d66d` too.

**2. Both new lints are non-vacuous, and both name the remedy.** `portfolio-citation-form-lint` fires on `agents/playmaker.md` at `61bd21f` (two `260510-0930_o_` citations, removed by `b53c7dd`), carries an explicit anti-vacuity case, eight negative controls that pin the pointer-versus-naming distinction, three positive controls, and an injection test asserting the message names file, line, token and fix. `turn-budget-lint` carries a shared `REMEDY` constant appended to every failure, naming both legitimate routes (a project's `fusion-guard.json`, or `DEFAULTS.orchestrator.maxTurns`) and stating that neither is a number in a prompt. `deliverable-language-lint` does the same. The `commit-message-path` rewrite in `9c58c4d` goes further than any of them: its positive control reads the shipped prompts through the gate's own scan and asserts the flagged set is *exactly* the one allow-listed path, so both "the gate reaches real lines" and "only the allow-list spares them" fail loudly and separately. That is the shape the other gates should be measured against.

**3. `282ef42`'s three boundary sites are consistent, no fourth stands, and the "only" was already false.** The three are `agents/orchestrator.md:237-241` (the You-may bullet, now enumerating three permitted content writes), `agents/orchestrator.md:786` (Phase 4 step 3, Status set in the same edit as the Closure note) and `skills/next/SKILL.md:249` (Boundaries). I searched `agents/`, `skills/`, `rules/`, `docs/`, `README*.md` and `CLAUDE.md` for every remaining statement of a Circle-record write boundary: `agents/playmaker.md:10,53`, `agents/shaper.md:28`, `skills/direct/SKILL.md:13,110` and `skills/seed-from-plane/SKILL.md:11,138` each bound *their own* writes, not the orchestrator's, and none is falsified. **No fourth site states the narrow form.**

The claim that the drift check already required a Circle Turn-log write is **confirmed**. `hooks/lib/state-drift.ts:465-482` has measured `Circle Turn log` — record entries against Turns run — since `8a49fd5`, and `agents/orchestrator.md:1095` lists the row. At `b261d83` the bullet read "the only Circle-record content write the orchestrator performs"; a check that had been demanding a second one for weeks makes "only" false, exactly as the commit message says.

**4. The pattern this session has hit three times has hit it a fourth.** `9f84254` widened `## Project language` from three cases to four, inserting the customer-deliverable case as the **third** bullet. The owning file gets its own ordinals right twice (`:206`, `:224`). Two surfaces written in the same commit call it the **fourth case** — `agents/orchestrator.md:430` and `CLAUDE.md:54`. The fourth bullet says text shipping to consuming projects is English *whatever either declaration says*, which is the opposite rule; the citation does not merely miss, it resolves to a contradiction. Filed as `260811-2145_*_the-deliverable-language-case-is-the-third-bullet-and-two-citing-surfaces-send-the-reader-to-the-fourth.md`. This is not drift over time — all three surfaces were authored in one sitting and the ordinal was counted twice, differently.

**5. No other record from this Turn missed its commit.** Checked three ways. `git status --porcelain` carries one modified file, `orchestrator-events.jsonl`, whose five uncommitted lines are this review's own dispatch events. All 14 records renamed to a terminal marker in the range have a rename similarity of 57–89 %, never 100, so every one carried content with its rename. And each of the 14, plus the recovered `260811-0932_*_die-circle-aktivierung-zieht-die-kopffelder-des-datensatzes-nicht-nach.md`, was opened at its tail: all 12 issues carry a `Resolved:` annotation and all three decisions carry both `Answered:` and `Implemented:`. The staging failure `951c809` records is the only one in this Turn.

---

## Findings by theme

### Theme 1 — a stated safety property that does not hold

**`260811-2142_*_the-unresolved-turn-budget-leaves-the-phase-2-loop-with-no-monotone-bound-while-the-prompt-says-it-is-bounded.md` — the unresolved Turn budget leaves the Phase-2 loop with no monotone bound. High.**
`agents/orchestrator.md:127` says "The loop is still bounded — the other five conditions and the Step 3e convergence check all still exit it" and then, in the same sentence, "so watch the Turn count yourself and stop and ask the user if the session runs long." Reading the table at `:588-596`: *Max Turns reached* was the only **monotone** condition — the only one whose satisfaction follows from the passage of Turns rather than from the work taking a particular shape. A session in a resolve-one-file-one steady state satisfies none of the remaining five (`1 > 1` is false, so Net-negative never trips; the queue never empties, so Step 3e never fires). The branch is not a corner case: the helper-absent route is what every consuming project meets until it next runs `fusion --update`. The fix is not a substituted budget — it is a stop-and-ask interval, which is a human gate rather than a bound the prompt invented, and so leaves `260811-1712_*_max-turns-is-hardcoded-in-eight-places-and-cannot-be-set-per-project.md`'s answer intact.

### Theme 2 — the range's own measurements, firing at HEAD

**`260811-2143_*_the-drift-checks-turn-row-and-commits-row-use-two-different-session-anchors-so-every-resume-reports-a-permanent-false-drift.md` — the drift check's Turn row and commits row use two different session anchors. High.**
`commitsSince` (`hooks/lib/state-drift.ts:276`) counts against `session.git_head_at_start`, which a resume does not rewrite. `turnsRun` (`:244-273`) counts `turn_start` events since the last `session_start` **line in the event log**, which a resume does rewrite, and compares that against `progress.turn`, which is cumulative. Measured at `951c809`:

```
$ ./bin/fusion-state-drift
  progress.commits  surface=32  record=32 (git 7785330..HEAD)
  progress.turn     surface=4   record=0 (turn_start events this session)  DRIFT
```

Nothing is stale — ten commits landed in Turn 4 after the resume and `progress.turn: 4` is correct. The row has been reporting DRIFT on every guarded tool call for the whole session. That is precisely the disqualifying shape `hooks/tracker.ts:776-857` wrote into itself in `36984d7`: *"a check that speaks on its commonest path is one its reader learns to read past, which destroys the other measurements' credibility along with its own."* `turnsRun` is not modified by this range; the defect predates it and is reported here because it is live, because the range wrote the doctrine it violates, and because `260811-1614_*_the-drift-checks-turn-row-is-satisfied-by-a-turn-start-alone-so-a-turn-that-emits-nothing-else-reads-clean.md` is already open against the same row from the under-reporting side.

**`260811-2144_*_a-resumed-session-re-enters-an-in-flight-turn-without-a-turn-start-so-the-boundary-drift-check-that-rides-it-never-fires.md` — a resumed session emits no `turn_start`. Medium.**
`agents/orchestrator.md:438` says the drift check "rides the emission … This fires in **every** Turn, Turn 1 included." Setup step 1's **Continue** branch (`:95`) names no emission, and a resume re-enters mid-Turn, past step 2. Measured: zero `turn_start` events after the resume `session_start` at `orchestrator-events.jsonl:1213`, against 10 `task_done` and 8 `commit`. The tracker still measures drift after every tool call, so the 2026-08-01 freeze is not back — what is lost is the boundary *read*, the moment the prompt designed to be unmissable, on the session that most needs it because the state it inherited was written by a session that is gone.

### Theme 3 — a split that is disjoint in one direction

**`260811-2149_*_the-record-counts-block-still-gates-both-halves-together-when-session-started-is-missing-and-a-test-pins-it.md` — the record-counts block still gates both halves together when `session.started` is missing. Low.**
`29d62e2` states at `agents/orchestrator.md:734` that "the two halves fail separately, so they are gated separately." True for a missing anchor: `records=partial`, filed count taken. Not true for a missing `session.started`: the whole `else` is skipped, so the `now_` half — which needs only the anchor and never touches `T` — is discarded although it is measurable. `records=partial` already exists and would be the right header; the branch is simply not written. And `record-counts-measurement.test.ts:322-327` pins it, with a comment true about the filed half and silent about the other, so a later reader has a green test telling them the branch is right. Narrow input, but it is the mirror of the defect the same commit fixed. The four-case split (A usable × T present) is disjoint and complete and is what the two-input structure actually has.

**`260811-2148_*_the-next-skills-status-guard-and-its-sed-action-ask-different-questions-so-a-valueless-field-is-skipped-silently.md` — `/fusion:next`'s Status guard and its `sed` action are different predicates. Low.**
`skills/next/SKILL.md:186-193` guards with `^\*\*Status:\*\*` and acts with `^\*\*Status:\*\*[[:space:]].*$`. Measured with the exact snippet: a line reading `**Status:**` with no value passes the guard, is not rewritten, and the `else` branch that exists specifically to be loud about a missing field never fires. Also, `:200` claims "a rename cannot land without it" — the `mv` and the rewrite are two commands in one block; the rename can land alone. Asking the *result* rather than the input closes both.

### Theme 4 — a class fixed on one record type and unowned on the larger one

**`260811-2146_*_half-the-decision-records-carry-a-status-that-disagrees-with-their-marker-and-twelve-keep-the-unfilled-template-stub.md` — half the decision records carry a `**Status:**` their marker contradicts. Medium.**
`282ef42` closed exactly this defect for Circle records. Measured over all 67 live decision records (archive excluded), comparing filename marker against head field using the template's own vocabulary: **34 mismatched, and 12 non-open records still carry the unfilled template stub** `Answered: <set when status moves to _a_>` above their filled annotation. Two of the three decisions this range drove `_a_`→`_i_` are among the 34 — `260807-2131_*_which-language-governs-a-customer-deliverable.md` and `260811-1534_*_does-the-guard-event-log-get-an-upper-bound-and-what-happens-to-the-evidence-in-it.md` both still read `**Status:** open`. The stub half has no "the marker wins" defence: the marker says nothing about which of two `Answered:` lines to read, and `grep '^Answered:'` returns the placeholder first. This is a decision for the user, not a repair to start — `agents/orchestrator.md:288` says the disagreeing records are the evidence `260802-0920_*_next-skill-activates-a-circle-without-updating-its-status-field.md` will be decided against, and at 51 % the evidence should go in front of whoever decides. Own the field or drop it; do not hand-correct 34 records first.

### Theme 5 — gates absent where the failure is silent

**`260811-2147_*_nothing-pins-the-gitignore-bin-exception-list-against-the-contents-of-bin-and-the-failure-is-a-helper-missing-from-the-tarball.md` — nothing pins `.gitignore`'s `!bin/…` list against `bin/`. Medium.**
`bin/*` plus 15 named exceptions, guarded by a comment asking the author to "verify with `git ls-files bin/`" by hand. This range needed that step twice and got it right twice — verified at HEAD, all 15 tracked, none ignored. But the failure is invisible at author time and surfaces only for an end user, and the `[ -x ]` convention `260810-0921_*_how-should-a-prompt-call-a-bin-helper-that-the-installed-copy-may-not-have.md` introduced makes it *quieter*: a missing helper now takes a documented absent branch instead of erroring. One test comparing `git ls-files bin/` against `ls bin/` closes it, and asking git rather than re-reading `.gitignore` avoids the re-implementation defect `260810-0510_*_two-of-the-queue-ground-lints-negative-controls-re-implement-the-logic-instead-of-calling-it.md` is open against.

**`260811-2151_*_bin-fusion-turn-budget-ships-with-no-claude-md-layout-row-while-its-sibling-added-in-the-same-turn-got-one.md` — `bin/fusion-turn-budget` has no `CLAUDE.md` Layout row. Low.**
Two shipped helpers landed this Turn. `9f84254` added `bin/fusion-source-root` and its row; `61bd21f` added the larger mechanism and no row. The table is not an inventory (9 of 15 have rows), so this is not a broken promise — it is an inconsistency inside one Turn, on the helper a project owner needs to find in order to discover the budget is theirs to set.

### Theme 6 — a claim about a consumer that is stronger than the consumer

**`260811-2150_*_circle-stash-does-not-handle-an-absent-max-turns-it-renders-an-empty-right-hand-side-and-the-prompt-says-otherwise.md` — `/fusion:circle-stash` survives an absent `max_turns` rather than handling it. Low.**
`agents/orchestrator.md:125` justifies omitting the key with "an absent key is a case it already handles." `skills/circle-stash/SKILL.md:126-133` has no such branch: `grep` matches nothing, `TURN_MAX` is empty, and the manifest at `:172` renders `**Turn:** 3/` — a trailing slash with nothing after it. The decision to omit rather than write a word is still right, and for the stated reason (a word makes `grep` match while the `sed` substitution does not, splicing the whole line into the manifest). One `[ -z "$TURN_MAX" ] && TURN_MAX="--"` makes the sentence true and makes the stash manifest say what the dashboard says.

---

## What is good, and should be read as the standard

Three things in this range are better than the average of what they replaced, and naming them matters because two of them are patterns worth copying.

- **`9c58c4d`'s positive control is the best gate design this session has produced.** It reads the *shipped* prompts through the gate's own scan and asserts two facts separately — the scan reaches real lines, and the allow-list is the only thing sparing them — each with a message naming which one broke. A fixture string could not do that, and the record says so. Every gate in this repository that asserts `toEqual([])` should be measured against it.
- **`36984d7`'s trigger doctrine** (`hooks/tracker.ts:776-857`) states a criterion with checkable answers instead of a taste, states the trip-wire for when the chassis gets built, and states what the chassis must *not* do. Finding `260811-2143_*_the-drift-checks-turn-row-and-commits-row-use-two-different-session-anchors-so-every-resume-reports-a-permanent-false-drift.md` is a violation of its own criterion 2, which is an argument for the doctrine, not against it.
- **`bin/fusion-source-root`'s header** documents the one behavioural difference from the snippet it replaces (the branch order) and then explains why that difference is unreachable from the documented call sites. That is the shape of an honest refactor note.

The `.gitignore` archive claim was checked rather than taken: I created `fusion-workbench/archive/<stamp>-test/.guard-state/events-<stamp>.jsonl` and confirmed `git check-ignore` does not match it — the `fusion-workbench/.guard-state/*` pattern is path-anchored, so a rolled log lands tracked at its destination exactly as the comment says.

---

## Cross-cutting observations

**The batching damage has changed shape.** Turn 2's review named it as a claim corrected in one file and left in its neighbour; Turn 3 found it three more times. This Turn's instance (`260811-2145_*_the-deliverable-language-case-is-the-third-bullet-and-two-citing-surfaces-send-the-reader-to-the-fourth.md`) is different in kind: all three surfaces were written in one commit and the ordinal was counted twice, differently, in one sitting. That is not a propagation failure — it is a fact restated in three places at all, which `260811-1734` is open against. Deleting the ordinals is the fix; correcting them buys one release.

**Two branches in this range state a property they do not have** (`260811-2142_*_the-unresolved-turn-budget-leaves-the-phase-2-loop-with-no-monotone-bound-while-the-prompt-says-it-is-bounded.md`, `260811-2149_*_the-record-counts-block-still-gates-both-halves-together-when-session-started-is-missing-and-a-test-pins-it.md`), and both are the *new* branch added by the commit that fixed the old defect. The pattern is worth naming: the correction gets the care and the fallback gets the sentence. `rules/critical-stance.md` §3 and §4 both bear on this, and the second one — a split disjoint and complete — is the cheaper test to apply, because it is mechanical.

**A measurement's credibility is the shared resource.** `260811-2143_*_the-drift-checks-turn-row-and-commits-row-use-two-different-session-anchors-so-every-resume-reports-a-permanent-false-drift.md` has been printing DRIFT on every tool call this session while nothing was wrong. This Turn also closed `260811-1610_*_the-unmeasured-branches-discard-the-filed-count-which-needs-no-git-and-a-test-now-pins-the-discard.md` and `260811-1616_*_the-unmeasured-cause-list-assigns-a-project-outside-git-to-the-branch-that-cannot-reach-it.md`, both about the same family reporting a number it could not take. The doctrine `36984d7` wrote is right and the family should be held to it row by row, not module by module — the false positive here is one row of one otherwise-sound report.

**The version has not moved.** `.claude-plugin/plugin.json` reads `7.2.0` at both ends of the range, across ten commits, two new shipped `bin/` helpers, a new compiled `dist/` entry point and a new configuration leaf. `CLAUDE.md:27` says "Bump version on every change"; the release process bumps at release. Those two are in tension and always have been, so this is an observation rather than a finding — but a consumer's `fusion --update` is what makes the two new helpers reachable, and until the version moves nothing tells them to run it.

**Out-of-range but in the tree.** `bugreports/` is untracked at the project root and holds the three transferred findings plus `krk-meldungen.txt`. `bb9d66d` transferred all three into the workbench; the source directory is the user's and is outside the staging check's reach (it only walks `fusion-workbench/`). Not a defect — noted so it is not mistaken for one later. Separately, `bin/fusion-review-coverage` reports `unusable=1`: `260810-1918-coderev-turn-1-range-5ef92eb-940d522.md` carries no `**Reviewed-range:**` line. It predates the mandate and is out of this range.

---

## Recommended sequencing

**Before the next release**

1. `260811-2142_*_the-unresolved-turn-budget-leaves-the-phase-2-loop-with-no-monotone-bound-while-the-prompt-says-it-is-bounded.md` — the unbounded loop. It is a stated safety property that does not hold, in the branch every un-updated consumer will take.
2. `260811-2143_*_the-drift-checks-turn-row-and-commits-row-use-two-different-session-anchors-so-every-resume-reports-a-permanent-false-drift.md` — the false DRIFT. It is live on every tool call of every resumed session, and it costs the whole measurement family its credibility, not just its own row. Take `260811-1614_*_the-drift-checks-turn-row-is-satisfied-by-a-turn-start-alone-so-a-turn-that-emits-nothing-else-reads-clean.md` with it; they are the two sides of one row.

**Next Turn**

3. `260811-2145_*_the-deliverable-language-case-is-the-third-bullet-and-two-citing-surfaces-send-the-reader-to-the-fourth.md` — delete the two ordinals. One-line edit, plus the lint case that stops the next one.
4. `260811-2144_*_a-resumed-session-re-enters-an-in-flight-turn-without-a-turn-start-so-the-boundary-drift-check-that-rides-it-never-fires.md` — the resumed Turn's missing `turn_start`. Smaller than `2143` and partly overlapping in symptom; the two fixes are independent and both are wanted.
5. `260811-2147_*_nothing-pins-the-gitignore-bin-exception-list-against-the-contents-of-bin-and-the-failure-is-a-helper-missing-from-the-tarball.md` — the `bin/` tracking gate. One test, and it closes a class rather than an instance.

**Put to the user, not scheduled**

6. `260811-2146_*_half-the-decision-records-carry-a-status-that-disagrees-with-their-marker-and-twelve-keep-the-unfilled-template-stub.md` — the 34 decision records. The repair depends on an answer to `260802-0920_*_next-skill-activates-a-circle-without-updating-its-status-field.md`, and the measurement is itself the input to that answer.

**Cleanup, any time**

7. `260811-2148_*_the-next-skills-status-guard-and-its-sed-action-ask-different-questions-so-a-valueless-field-is-skipped-silently.md`, `260811-2149_*_the-record-counts-block-still-gates-both-halves-together-when-session-started-is-missing-and-a-test-pins-it.md`, `260811-2150_*_circle-stash-does-not-handle-an-absent-max-turns-it-renders-an-empty-right-hand-side-and-the-prompt-says-otherwise.md`, `260811-2151_*_bin-fusion-turn-budget-ships-with-no-claude-md-layout-row-while-its-sibling-added-in-the-same-turn-got-one.md`.

---

## Reconciliation 260811-2330 — disposition of the ten findings

Verified against the records on disk at HEAD `31746d1`. Findings are not restated or revised here;
this is only what became of each.

| Record | State | Evidence |
|---|---|---|
| `260811-2142_*_the-unresolved-turn-budget-leaves-the-phase-2-loop-with-no-monotone-bound-while-the-prompt-says-it-is-bounded.md` unbounded loop | closed | `500f51f` — the unresolved-budget check-in at Phase 2 step 3d; `hooks/lib/__tests__/turn-budget-lint.test.ts` |
| `260811-2143_*_the-drift-checks-turn-row-and-commits-row-use-two-different-session-anchors-so-every-resume-reports-a-permanent-false-drift.md` false `progress.turn` DRIFT | closed | `e61e24a` — both drift rows anchor on the session identity carried in the event log |
| `260811-2144_*_a-resumed-session-re-enters-an-in-flight-turn-without-a-turn-start-so-the-boundary-drift-check-that-rides-it-never-fires.md` resumed Turn has no `turn_start` | closed | `e61e24a` |
| `260811-2145_*_the-deliverable-language-case-is-the-third-bullet-and-two-citing-surfaces-send-the-reader-to-the-fourth.md` two ordinals | closed | `a6b4928` |
| `260811-2146_*_half-the-decision-records-carry-a-status-that-disagrees-with-their-marker-and-twelve-keep-the-unfilled-template-stub.md` 34 decision records | **open, correctly** | measurement reproduced this pass (34 by exact equality, 29 substantive); the stub half repaired, the ownership question left to the user — see that record's reconciliation section |
| `260811-2147_*_nothing-pins-the-gitignore-bin-exception-list-against-the-contents-of-bin-and-the-failure-is-a-helper-missing-from-the-tarball.md` `bin/` tracking gate | open | no commit touches `.gitignore`'s `bin` exception list in `e3da397..HEAD` |
| `260811-2148_*_the-next-skills-status-guard-and-its-sed-action-ask-different-questions-so-a-valueless-field-is-skipped-silently.md` `/fusion:next` status guard | open | — |
| `260811-2149_*_the-record-counts-block-still-gates-both-halves-together-when-session-started-is-missing-and-a-test-pins-it.md` record-counts block | closed | `a6b4928` |
| `260811-2150_*_circle-stash-does-not-handle-an-absent-max-turns-it-renders-an-empty-right-hand-side-and-the-prompt-says-otherwise.md` circle-stash absent Max-Turns | open | — |
| `260811-2151_*_bin-fusion-turn-budget-ships-with-no-claude-md-layout-row-while-its-sibling-added-in-the-same-turn-got-one.md` `bin/fusion-turn-budget` layout row | closed | `a6b4928` |

Six of ten closed, all within Turn 5, and the sequencing this review recommended was followed: the
two release blockers first, then the cleanup. The one finding put to the user rather than scheduled
is the one still open with a reason.

Reconciled by `reconciler`, `260811-2330-reconciliation.md`.
