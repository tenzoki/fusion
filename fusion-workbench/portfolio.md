# Portfolio

**Generated:** 260821-0426 (by playmaker session 260821-0426-playmaker-orchestrator-phase4)
**Domain bias:** code
**git HEAD at run:** `ff8d15e` (the closure this file reports is committed; the workbench tree is clean)

**This run holds no user confirmation.** It was dispatched non-interactively at Phase 4, after
`260820-2051-style-rules-arrive-and-get-measured` moved from active to Bounded Closure. Its mandate
is the narrow one: rank, regenerate this file in full, and rename backlog markers where the ranking
calls for it. It performed no split, merge, close or deferral, and proposes none. Every figure below
was measured against disk on this run.

**The headline: the board is empty and both bounded Circles are holding a leftover nobody carries.**
Thirteen Circle directories, every one terminal. Nothing is anticipated, nothing is active. Two of
the thirteen ended in Bounded Closure, and each left a named piece of work that only a new Circle can
pick up. The style-rules Circle deferred its fourth outcome with an exact pre-registration that loses
value if a later Circle re-derives the threshold instead of inheriting it. The guard Circle's unmet
clause is six days old. Neither is an anticipated Circle, neither is a backlog entry, and no
mechanism in fusion turns one into either. The user files, or they sit.

**The second thing worth reading before anything else: three of the four growth budgets are spent.**
The dispatch named two. Measured on this run, `agents/*.md` is the third, at 1 638 bytes of 18 000.
Any Circle that would edit a skill body, an agent prompt or a hook test now needs a cut before it can
land, and no cut has been taken. That is a real dependency on every candidate this file could
otherwise recommend, and it is written up under `## Warnings`.

## Active (_t_)

**(none).** `.active-circle` is absent and no Circle record carries the active marker. The two agree,
so no pointer warning is raised. This is the normal state after a closure.

The last active Circle was `260820-2051-style-rules-arrive-and-get-measured`, which reached Bounded
Closure on 2026-08-21. Its record cites the reconciliation at
`circles/260820-2051-style-rules-arrive-and-get-measured/history/260821-0416-reconciliation.md` and
the session history at `shared/history/260820-2103-orchestrator-session.md`.

## Anticipated (_a_) — ranked

**(none).** No Circle record carries the anticipated marker, so there is no `Recommended next:` line,
and no `## Activation proposal` was written to any record on this run.

**The store emptied by being worked, not by being cleared.** The previous refresh already held no
candidate, and the Circle that ran overnight was filed and activated inside that gap rather than
drawn from the portfolio. The forward path this file can offer is therefore the backlog, and the
backlog holds two ideas.

**What this agent would recommend if it could recommend anything is not in either store.** The
strongest next Circle on the evidence is the one that takes a cut across the three spent growth
budgets, because it unblocks a set of already-filed defects that currently cannot be repaired. The
second strongest is the one that inherits the style-rules pre-registration and closes the
measurement. Neither exists as an anticipated Circle or as a backlog entry, and this agent originates
neither: filing is the user's act, by hand, through `/fusion:memo`, or straight to a Directive with
`/fusion:direct <draft>`.

## Backlog — ranked

**Recommended to shape: `shared/backlog/260814-1733_*_bounded-executor-dispatches.md`, the only live
idea that can be shaped today without a user act having to come first.**

    /fusion:direct shared/backlog/260814-1733_*_bounded-executor-dispatches.md

**Live and ranked (2).** Ranking recomputed on this run under the code-domain bias, which prefers an
idea whose cited evidence is already on disk and whose dependencies are clear over one that must wait
on an unresolved record. Both entries carry citations that resolve; every path in both was expanded
and checked against the store on this run.

1. **`shared/backlog/260814-1733_*_bounded-executor-dispatches.md`** (recommended, `_p_`). Bound how
   long an executor runs before it returns to the orchestrator. Its evidence is on disk and already
   sized. `shared/analyses/260812-0303-simplify-speed-and-why-rules-do-not-hold.md` adopts the
   bounded-dispatch half of the user's filed proposal on cost grounds, at roughly a fourfold saving
   in re-sent tokens, and refutes the re-injection half by finding that the rules did not decay over
   a long dispatch but were never in force to begin with. That split is what makes the entry ready
   rather than raw. Shaping it means putting a Directive narrower than the filed wording to the user
   and getting agreement on the narrowing, which is the conversation `/fusion:direct` exists to hold.
   Its companion record, `shared/issues/260812-0253_*_rules-lose-their-effect-during-a-long-dispatch.md`,
   is open and is context for the Directive rather than a gate on it.

   **The Circle that just closed is a sixth data point, and it argues for the entry more sharply than
   the fifth did.** Its two Turns produced 22 filed findings, of which 19 are still open, and its own
   closure note records that the repunctuation returned 470 bytes while the new clauses spent 2 608.
   A Turn that grows the surface it was sent to shrink is the failure mode a bounded dispatch is
   meant to interrupt: the executor never returned to a point where the ratio could be read and acted
   on. Against that, the Circle's gates held and its reviewer caught both High findings, so length
   alone is still not established as the mechanism.

   *Nothing is proposed for this entry.* It states one idea, can be promoted whole, and already
   carries the marker this run's ranking gives it.

2. **`shared/backlog/260814-1733_*_attach-the-rule-to-the-act.md`** (open, `_o_`). A rule lands with
   an executable check or it does not land. Second **on shapeability, not on merit**, and the gap is
   unchanged across six consecutive refreshes: on the evidence this is the best-supported idea in the
   store, and it still cannot be shaped today.

   *The obstruction, re-verified on disk this run, and it has not moved since the previous refresh.*
   The decision this entry depends on,
   `shared/decisions/260810-0710_*_should-a-rule-be-allowed-to-land-without-the-check-that-enforces-it.md`,
   carries the deferred marker. Its deferral names three records as the condition for reviving it,
   two are settled, and
   `shared/issues/260810-0510_*_two-of-the-queue-ground-lints-negative-controls-re-implement-the-logic-instead-of-calling-it.md`
   is still open at Low severity. Reviving a deferred decision is the user's own act, so the entry
   stays open.

   *This run found the idea's strongest evidence yet, and it is a fresh instance rather than an
   argument.* Three decision records were answered and realised in `30d6f0a`, and the marker move
   that records the realisation produced three files whose names carry a literal asterisk instead of
   the marker letter. The realisation is real and the records still read as unrealised. The
   convention that was violated is written down in two rule files; no gate reads it against the
   workbench. That is this entry's thesis, reproduced in the week it was ranked second for want of a
   user act. See `## Warnings`.

   *Nothing is proposed for this entry either.* No split applies, since it states one idea. A merge
   with the entry above would consolidate two distinct Directives and lose one of them. The idea is
   live, so a close is wrong. Deferring it would cost the user two later acts, reviving the decision
   and then reviving the entry by hand, where leaving it open costs one.

**Proposed and not performed (0).** This run proposes no split, merge, close or deferral, for the
reasons stated per entry above rather than left to be inferred.

**Performed this run: nothing.** Both live entries already carry the markers this run's ranking gives
them, so the autonomous rename between open and recommended had nothing to do.

**The store also holds one closed entry**, `shared/backlog/260811-0826_*_observations.md`, the user's
hand-written dump of about a dozen ideas. It was split on 260814-1733 into three entries and retired.
Two of the three are the live entries above; the third became Circle
`260815-0007-remove-eight-mechanisms-and-cap-growth` and closed. Its body still carries the two
dangling citations reported at the previous refresh, both written by a playmaker run in the
spelled-out marker form. Repairing a closed entry's body is not one of this agent's operations.

## Recently closed (_c_ / _b_)

The five most recent, newest first. The heading names two markers, and the letters in it are the
statement rather than pointers; the paths in the entries are pointers and carry the wildcard.

1. **`260820-2051-style-rules-arrive-and-get-measured`**. Bounded Closure, 2026-08-21. Three of four
   Directive outcomes reached: a stylometric profile changed in the plugin's source now reaches a
   project set up before the change, the always-on rule corpus fell from 171 prose em-dashes over
   13 018 words to 8 over 13 292, and `rules/user-facing-output.md` now states the condition under
   which an opening sentence fails its fact-first requirement. The fourth outcome, a measured number
   on the prose-register decision, has no window until a Circle conditioned by the repaired corpus
   has written session prose. The Bounded-Closure Artifact is the pre-registration itself: a threshold
   fixed at 5.0 prose em-dashes per 1000 words and a pre-repair window captured before the first
   repair commit. Record:
   `circles/260820-2051-style-rules-arrive-and-get-measured/_*_circle.md`.
2. **`260819-1645-four-constraints-on-deep-change`**. Closed coherent, 2026-08-20. Five gates built,
   each demonstrated failing before it was accepted.
3. **`260816-1741-guard-becomes-observation-only`**. Bounded Closure, 2026-08-17. The compliance
   guard decides nothing; one Directive clause about two shipped surfaces was not reached and is
   still open.
4. **`260815-0007-remove-eight-mechanisms-and-cap-growth`**. Closed coherent, 2026-08-15. Eight
   mechanisms removed and the four growth bounds armed. Three of those four bounds are what
   `## Warnings` reports as nearly spent.
5. **`260813-0858-playmaker-maintains-backlog-store`**. Closed coherent, 2026-08-13. This agent's
   backlog mandate and the two dispatch paths it runs under.

## Archived (_s_ / _d_)

One record carries the superseded marker. None carries deferred.

- **`260804-1205-shell-reachability-model`** (superseded). The shell reachability model for the
  write-path classifier. Superseded by `260807-0923-guard-misst-statt-orakelt`, which replaced
  prediction with measurement; the classifier it was going to fix has since been removed outright.

This section lists live Circle records under `circles/` carrying one of those two markers. It has
nothing to do with the `archive/` store, whose contents never appear in this file.

## Warnings

**Pointer state: clean.** `.active-circle` is absent and no Circle record carries the active marker.
None of `STALE-POINTER`, `POINTER-MISMATCH`, `MISSING-POINTER` or `MULTIPLE-ACTIVE` applies.

**Dependency cycles: none, on a graph with no nodes.** The graph is built from the `## Dependencies`
sections of anticipated and active Circles, and there are none. The check ran and had nothing to
examine. No `## Dependency warning` was appended to any record.

**Bounded-Closure propagation: no flag raised, and the empty candidate set is the reason.** Both
bounded Circles were scanned for. The check looks for anticipated or active Circles whose Grounding
snapshot cites the bounded Circle's directory name or the Artifact named in its closure note, and
there are no anticipated or active Circles. No `## Parent grounding stale` section was written, and
no `parent-grounding-stale` event was emitted.

**The propagation check's silence is itself the condition to act on.** A bounded Circle's leftover
reaches a future Turn only through a Circle that cites it, and the route from a leftover to a Circle
runs through the user. Two leftovers now sit with no carrier. The style-rules pre-registration is the
more perishable of the two: its value is that the threshold was chosen without knowledge of the
outcome, and a later Circle that re-derives a threshold instead of inheriting this one destroys
exactly that property. The guard Circle's clause,
`circles/260816-1741-guard-becomes-observation-only/issues/260817-1505_*_the-curator-and-its-skill-still-say-a-projects-guard-configuration-can-deny-a-write.md`,
is six days old and unchanged.

**Three of four growth budgets are effectively spent, and this bears directly on what can be
recommended next.** Measured on this run against `ff8d15e`, reproducing the arithmetic in
`hooks/lib/__tests__/helpers/growth-bound.ts` over the baselines declared in
`hooks/lib/__tests__/surface-growth-bound.test.ts` and
`hooks/lib/__tests__/rules-emission-golden.test.ts`:

| Surface | Head-room left | Budget |
|---|---|---|
| the skill bodies, in bytes | 30 | 20 000 |
| the hook tests, in lines | 32 | 2 500 |
| the agent prompts, in bytes | 1 638 | 18 000 |
| the always-on rule core, in bytes | 3 566 | 12 000 |

Both bound test files were executed on this run and both pass, so nothing is red today. The
`agents/*.md` figure is the one the closure note does not name, and 1 638 bytes is roughly one
paragraph of prompt text across all fifteen prompts together.

Four open defects in the just-closed Circle write into surfaces with no room. `260821-0148` and
`260821-0302` both repair `skills/setup/SKILL.md`, which has 30 bytes; `260821-0143` names the same
file; `260821-0144` states in its own title that the hook-test surface had 43 of 2 500 lines left
when it was filed, and the figure is 32 today. A red bound is cleared by a cut and never by editing a
baseline, which is authored in `hooks/lib/__tests__/helpers/growth-bound.ts`. **Nobody has taken the
cut**, and until somebody does, those four defects are filed and unfixable. The Circle's own closure
note reports this and files the underlying gap as a decision,
`circles/260820-2051-style-rules-arrive-and-get-measured/decisions/260821-0414_*_does-a-corpus-repair-circle-carry-a-budget-for-what-its-own-new-clauses-may-spend.md`:
what is missing is any bound on a Circle's own growth.

**Three decision records exist twice, and the second copy of each carries a glob metacharacter in its
filename.** Committed in `30d6f0a`, whose message reports three answered decisions being realised.
The realisation landed. The marker move that should have recorded it did not: instead of renaming
each record from answered to implemented, the run created a second file per record holding only the
`Implemented:` footer, named with a literal asterisk where the marker letter belongs. The three
names, fenced because the spelling is the datum rather than a pointer:

```
circles/260801-1244-curator/decisions/260814-1915_i_*.md
circles/260815-0007-remove-eight-mechanisms-and-cap-growth/decisions/260815-1845_i_*.md
circles/260815-0007-remove-eight-mechanisms-and-cap-growth/decisions/260815-2056_i_*.md
```

Two consequences. Every scanner reads those three decisions as answered and not yet realised, because
the original records still carry the answered marker and their full bodies; the previous refresh
listed all three as carrier-less open questions, and a reader following that up today would conclude
the work is still to do. And three filenames now carry the metacharacter that
`rules/fusion-workbench-conventions.md` `## Marker globs` exists to keep out of filenames, which is
the trap that cost one session five silent failures.

No gate sees it. `marker-format-lint.test.ts` was executed on this run and passes: it reads
`agents/*.md` and `skills/*/SKILL.md` for the retired bracket form, never walks the workbench, and
does not look for an asterisk. `workbench-citation-lint.test.ts` was executed and passes too.
Repairing this means a rename and an append into three decision records, both outside this agent's
scope, so the condition is surfaced and nothing was written.

**Eleven open decisions sit in the just-closed Circle, and every one was answered by the orchestrator
in the user's absence.** They are open on purpose. The user confirms or overturns them; until then
none of them is settled Grounding, and any Circle that inherits from this one should treat them as
questions rather than as answers. Two carry weight beyond their own subject:
`.../decisions/260820-2314_*_can-this-circle-close-coherent-when-its-fourth-outcome-has-no-measurement-window.md`
is the record behind the Bounded Closure this file reports, and
`.../decisions/260821-0414_*_does-a-corpus-repair-circle-carry-a-budget-for-what-its-own-new-clauses-may-spend.md`
is the growth question above. All eleven are under
`circles/260820-2051-style-rules-arrive-and-get-measured/decisions/`.

**The shared decision store holds nothing open for the first time in this file's history, and that is
not the good news it looks like.** All eleven open questions are inside a Circle that is finished, so
no session reaches them by any route except a user opening the directory.

**Open defect volume, measured this run: 169, against 153 at the previous refresh.** Ninety-three are
open in `shared/issues/` against 139 closed, and 76 across all Circle issue stores against 293
closed. The overnight Circle filed 22 findings and closed 3 of them, which is where the rise comes
from. The judgement of the 2026-08-19 reconciliation passes still holds on the remainder: the
Circle-store records are not stale, they reproduce as filed, and the reason they sit unworked is that
they are filed under Circles that are finished. The problem is placement, not truth.

**The bounded record states a contradiction it cannot repair, and says so.**
`circles/260820-2051-style-rules-arrive-and-get-measured/_*_circle.md` `## Dependencies` describes
`260819-1645-four-constraints-on-deep-change` as active. It closed in `5faed26`, before the
style-rules Circle was shaped. The record's own closure note names the contradiction and leaves it
standing, because the underlying defect is that no party may write that section:
`shared/issues/260813-0913_*_a-dependency-between-two-circles-can-only-be-recorded-on-one-side-because-nobody-may-write-the-other.md`.
This file reproduces the record's own account rather than the record's claim.

**The Bounded-Closure marker overstates what happened here, for the second time in five days.** The
marker is defined as "Directive judged not reachable". This closure note says something different:
three of four outcomes were reached and verified, and the fourth is deferred with its precondition
named. The gap is filed as
`shared/issues/260817-1613_*_the-reconcilers-verdict-vocabulary-has-no-case-for-a-directive-that-is-reachable-but-deliberately-not-reached.md`,
opened when `260816-1741-guard-becomes-observation-only` hit the same wall. It is raised here because
this file renders that marker and the entry under `## Recently closed` would otherwise mislead.

**Four records touching this agent's own surfaces remain open**, three in
`260813-0858-playmaker-maintains-backlog-store` and one in
`260815-0007-remove-eight-mechanisms-and-cap-growth`. The split-line form record is inert this run,
since no split is proposed. One more was added on 260820:
`shared/issues/260820-1133_*_the-playmaker-writes-the-portfolio-citing-a-history-file-it-has-not-written-yet.md`,
which this run worked around by writing its history log before this file. The window it names is
closed for this run's output and the defect is still open, because the prompt still specifies the two
writes in the other order.
