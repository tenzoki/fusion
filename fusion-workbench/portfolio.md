# Portfolio

**Generated:** 260813-2346 (by playmaker session 260813-2346-playmaker-direct-dispatch)
**Domain bias:** code

**This file is shorter than the one it replaces, deliberately.** The previous portfolio ran to
about 350 lines, and the eighth consecutive run restating an unchanged argument at full length is
itself an instance of the complaint the backlog's own top entry makes about this project. Every
load-bearing statement below is carried forward or re-measured. What is dropped is the re-arguing.
Where a condition has not moved since 260813-2334, it is named in one line and dated rather than
explained again.

## Active (_t_)

(none). No Circle record carries the active marker, and the pointer file `.active-circle` is
absent. The two agree, so this is the ordinary state after a closure and not a fault.

An orchestrator session started at 260813-2345 and has run Setup without yet naming a goal
(`shared/history/260813-2345-orchestrator-session.md`). It has opened no Circle.

Activation runs through `/fusion:next`.

## Anticipated (_a_) — ranked

Recommended next: 260801-1244-curator — the only anticipated Circle left, and it needs a shaper
pass before it can be activated.

### 1. `260801-1244-curator`

> "The curator reconciles the three normative surfaces, and proves it on fusion's own conventions
> file."

| Signal | Value this run |
|---|---|
| Dependencies | all three closed coherent |
| Open decision records cited in the Grounding | none |
| Circle directory contents | the record only, no artifact subdirectories |
| Grounding measurements re-checked | 2 of 5 still falsified, 8th consecutive run |

This Circle ranks first because it is the only candidate. First place carries no comparison here
and should not be read as one.

On the code heuristic the Circle scores clean, and the heuristic is misleading. It reads what a
record says, not whether the record still holds. Two load-bearing measurements in the Grounding
snapshot are falsified: the snapshot states that `rules/fusion-workbench-conventions.md` holds
54 401 bytes across 32 second-level headings, where the file holds 51 920 bytes across 24, measured
this run at HEAD `d7786eb`. The whole scoping argument is sized against those two numbers. The
reasoning in the record still stands; the numbers under it do not.

The sanctioned repair is unchanged across four runs: a shaper pass in portfolio-activation mode
before activation, producing a Directive without capability C9, a fresh validation case, a
Grounding measured this week, and an answer on whether the Directive needs a component that bounds
the rate at which the conventions file regrows. Whether an orchestrator may reach that shaper mode
from inside a session is the open record
`shared/decisions/260813-0027_*_should-the-orchestrator-be-able-to-dispatch-the-shapers-portfolio-activation-mode.md`.
The user can run the shaper directly with the mode contract regardless.

**No `## Activation proposal` section was appended to this record on this run.** It already carries
three, the newest written 20 minutes ago at run 260813-2326, and nothing in this Circle's inputs
moved between the two runs. A fourth section restating the third would add noise to a record the
shaper has to read whole.

## Backlog — ranked

Recommended to split first: `shared/backlog/260811-0826_*_observations.md`. It holds 13 ideas, and
the top one is `bounded-dispatches-and-re-injected-context`.

No `/fusion:direct` line is written for this entry. That command promotes an entry whole, which
would make one Circle of all 13 ideas and retire the lot.

**The split could not be performed on this run**, for the fifth consecutive time and the same
reason. This run's prompt came from the installed plugin copy, which forbids every backlog write,
while the resolver that ran at the same Setup emitted the write key. See
`installed-copy-predates-the-backlog-mandate` under `## Warnings`. The remedy is one command and a
session restart.

1. `shared/backlog/260811-0826_*_observations.md` (`_o_`). "Raw observations, hand-written by the
   user: about a dozen distinct ideas in one dump, awaiting consolidation by the playmaker."
   Byte-identical to the copy the previous run read, checked by content hash rather than by
   timestamp. Of its 13 distinct ideas, 7 are already carried by a filed record, 3 duplicate
   another idea inside the same entry, and 3 are live and shapeable. Proposed split, live ideas
   first:

   - `bounded-dispatches-and-re-injected-context`. Agents must not run long operations without
     returning to fusion, and the working context must be re-sent with every request. Ranked top
     because it rests entirely on records already on disk and could be shaped today. The competing
     remedies are named in
     `shared/issues/260812-0253_*_rules-lose-their-effect-during-a-long-dispatch.md`, and
     `shared/analyses/260812-0303-simplify-speed-and-why-rules-do-not-hold.md` measures that the
     handoff between dispatches costs nothing while shorter dispatches would cut cost roughly
     fourfold.
   - `radical-simplification-of-fusion`. Whether fusion has become a system that mostly maintains
     itself, and what would have to go. Rests on
     `shared/analyses/260812-0022-where-the-complexity-comes-from-and-what-would-have-to-go.md`.
     Ranked second because the analysis is on disk but the idea is a question rather than a
     direction, so shaping it means choosing a cut first.
   - `operations-take-unbearably-long`. Every operation, not only Setup. Ranked third because it
     needs measurement before it could be sized, and because it is defect-shaped. See
     `backlog-idea-only-partly-filed` under `## Warnings`.
   - The 7 ideas already carried by a filed record, listed so a split does not re-file them: Setup
     duration, agent verbosity, rules losing effect mid-session, the missing estimated time of
     arrival, the monitor unreachable on localhost, and the orchestrator's wrong instructions to
     sub-agents, all six dated `260812-0253` in `shared/issues/`, plus absolute artifact paths at
     `shared/decisions/260812-0254_*_should-a-cited-artifact-path-be-absolute-so-an-editor-can-open-it.md`,
     which still carries the open marker.
   - The 3 duplicate groups, named rather than merged. The bounded-dispatch remedy is stated twice,
     fullest in the entry's `>>>` line. The self-repair complaint and the radical-simplification
     question are one idea, fullest in the entry's three closing questions. The observation that
     unverified claims get relayed upward duplicates
     `shared/issues/260812-0253_*_the-orchestrators-instructions-to-sub-agents-are-often-wrong.md`.
   - Two fragments that are not ideas and are excluded from the count of 13: a bare file path, and
     a note of the three most-changed files from the churn ranking. Five quoted agent transcripts
     are evidence for the verbosity and unverified-claims ideas, not ideas of their own.

Nothing was written into the backlog store on this run. No entry was created, renamed, split,
merged, closed, or deferred.

## Recently closed (_c_ / _b_)

| Circle | Marker | Closure |
|---|---|---|
| `260813-0910-documentation-matches-shipped-plugin` | `_b_` | Bounded Closure on 260813 after five Turns and sixteen commits. Nine of ten plan steps landed. The Bounded-Closure Artifact is step 10, the `docs/plane-setup.md` verification the Directive promises, which was never begun. The note records why the user closed bounded over a reconciler verdict that said the Directive was reachable: the Turn budget was spent at 5 of 5, and the remaining work is a fresh unit rather than a correction to this one. Committed at `602fa1b`, after the previous portfolio was generated. |
| `260813-0858-playmaker-maintains-backlog-store` | `_c_` | Closed coherent 260813 after four Turns. The playmaker holds a write key to the backlog store, and the recommended-for-promotion marker has exactly one named writer. The end-to-end acceptance run is the one thing not demonstrated. |
| `260807-0923-guard-misst-statt-orakelt` | `_c_` | Closed coherent 260807-1650. The static classifier is gone. The guard measures what changed instead of predicting what will change. |
| `260805-2005-textschicht-gegen-code-nachziehen` | `_c_` | Closed coherent 260806-1105. The plugin's text layer says what the code does again, held there by two new lints. |
| `260801-1244-guard-rules-write` | `_c_` | Closed coherent 260805-2359. A project can permit rule-file writes deliberately, per session, and never silently. |

**Worth knowing when reading the two guard closures.** Both delivered mechanisms that have since
been removed, on 260809 and 260812, each on its own measurement. The closures were correct. What
they built was later deleted deliberately. A reader who meets those rows without this note will
read them as stale.

## Archived (_s_ / _d_)

| Circle | Marker | Note |
|---|---|---|
| `260804-1205-shell-reachability-model` | `_s_` | Superseded 260807-0923 by `circles/260807-0923-guard-misst-statt-orakelt/`. Neither closed nor bounded: the user changed the mechanism, so the Directive stopped being the right one to reach. |

No Circle carries the deferred marker.

## Warnings

### New this run

**`chat-voice-caps-tightened-in-the-template-only`.** The two shipped chat voice profiles were
edited in the working tree and not committed: `stilwerk/chat-voice-de.yaml` and
`stilwerk/chat-voice-en.yaml` both cut rule C04's line caps from 8 to 6 for gate prompts and from
12 to 8 for chat replies, and the German file additionally asks for plain wording without jargon.
The workbench copies under `fusion-workbench/stilwerk/` still carry the old caps, and the workbench
copy is what `bin/fusion-rules` emits to every agent. Measured this run: the German template holds
7 358 bytes against the workbench copy's 7 353, the English template 6 800 against 6 801. So every
agent dispatched in this session, this run included, is working to the looser caps the user has
just replaced. Whoever commits the templates should copy them into the workbench in the same step,
or the edit reaches no agent.

### Standing conditions, unchanged since 260813-2334

These were verified against disk this run and have not moved. Each is stated once, with its
evidence, and not re-argued.

**`installed-copy-predates-the-backlog-mandate`.** The plugin copy this session executes is older
than the working tree, and the gap covers exactly the capability that closed on 260813.
`/Users/k1/.fusion/agents/playmaker.md` is 27 597 bytes and contains no occurrence of the backlog
write key; `agents/playmaker.md` in the working tree is 39 155 bytes and contains four. Both
`.claude-plugin/plugin.json` files read 8.1.0, so the version number does not show the difference.
The remedy is in `CLAUDE.md` under `## Release process`, in the paragraph beginning "And between
releases": run `fusion --update` and restart the session. This is not a defect in any Circle. It is
the known cost of the deferred release.

**`release-8-2-0-is-now-blocked-on-a-judgement-rather-than-on-work`.** The version bump was deferred
at the backlog Circle's Turn-3 release gate so that one release would carry both Circles. The
second Circle closed bounded rather than coherent, with one Directive clause unmet. Both manifests
still read 8.1.0, verified this run, so the playmaker backlog capability is committed at HEAD
`d7786eb` and unreleased. Whether a bounded closure is enough to ship on is the user's call, and it
is the decision standing between the working tree and every installed copy.

**`backlog-acceptance-run-still-not-performed`.** The end-to-end acceptance run for the backlog
capability was planned as the successor to step 9, step 9 was deferred, and the run went with it.
Six playmaker runs have now had the opportunity and none could take it, for the reason named two
warnings above. The gap has a known cause rather than an open question.

**`plane-setup-verification-outlives-its-circle`.** The Directive clause the bounded Circle did not
reach is on disk as
`circles/260813-0910-documentation-matches-shipped-plugin/issues/260813-2305_*_the-directive-promises-plane-setup-verification-and-step-10-was-deferred-with-no-record.md`,
and the closure note names it as the Bounded-Closure Artifact. The record sits inside a terminal
Circle, so nothing schedules it. All 466 lines of `docs/plane-setup.md` remain unverified against
`bin/fusion-plane`.

**`ten-open-defects-and-two-open-decisions-outlive-their-terminal-circle`.** Counted off the disk
this run: the bounded Circle's own stores hold 10 issue records carrying the open marker and 2 open
decision records, against 16 issues already closed there. The 10 include the step-10 record named
immediately above, so the two warnings describe overlapping sets rather than adding up. The two
open decisions are whether the planner should accept the domain parameter that three documented
surfaces already promise it, and how fusion's own documentation should treat a hand-measured number
that decays. A terminal Circle is terminal, so these reach a successor Circle by citation or they
reach the task queue. They reach neither by sitting there.

**`the-bounded-circles-own-acceptance-record-is-still-open`.**
`shared/issues/260813-0825_*_the-v8-1-0-documentation-step-reached-three-files-and-the-feature-reached-seven-surfaces.md`
holds the bounded Circle's acceptance conditions and still carries the open marker while the Circle
it governs has closed. It sits in `shared/`, outside the terminal Circle, so unlike the ten above it
is still reachable by an ordinary reconciler pass.

**`write-key-defect-record-open-after-its-circle-closed`.**
`shared/issues/260813-0825_*_the-playmaker-is-charged-with-backlog-upkeep-and-holds-no-write-key-to-the-store.md`
still carries the open marker, and its four acceptance conditions read as met against the working
tree. Whether it is held open deliberately until the release is not stated in it.

**`curator-grounding-measurements-falsified`.** Detailed under `## Anticipated` above. Eighth
consecutive run reporting it.

**`curator-circle-missing-artifact-subdirectories`.** `circles/260801-1244-curator/` holds only its
record. The six artifact subdirectories the Circle record template requires are absent, so the first
agent dispatched there has to invent them. Both Circles created on 260813 carry all six, so this is
a gap in the older Circle rather than a convention that changed.

**`one-sided-dependency-is-now-frozen-on-both-sides`.** The dependencies section of
`circles/260813-0858-playmaker-maintains-backlog-store/_*_circle.md` still asks for the
documentation Circle's directory name to be filled in once it exists. That Circle existed, the name
was never added, and both Circles are now terminal. Filed as
`shared/issues/260813-0913_*_a-dependency-between-two-circles-can-only-be-recorded-on-one-side-because-nobody-may-write-the-other.md`,
whose scope needs widening to cover the terminal case.

**`fusion-direct-cannot-run-the-flow-it-documents`.**
`shared/issues/260813-1334_*_fusion-direct-documents-a-shaper-clarification-flow-that-a-dispatched-sub-agent-cannot-run.md`
records that `/fusion:direct` promises a shaper clarification dialogue a dispatched sub-agent cannot
hold. Surfaced here because the backlog recommendation above ends at `/fusion:direct` once the entry
is split, so the path from a backlog idea to a Circle runs through this defect.

**`backlog-idea-only-partly-filed`.** The observation that every operation takes unbearably long,
not only Setup, exists on disk only as a witness line inside a record scoped to Setup
(`shared/issues/260812-0253_*_setup-takes-far-too-long-and-nothing-measures-it.md`). It is
defect-shaped rather than idea-shaped, so it does not belong in the backlog split above, and no open
record covers it as stated. The playmaker files no issues. This one is for the user to decide.

### Graph checks

**Dependency cycles: none.** One Circle is non-terminal, `260801-1244-curator`, and every dependency
it names points at a Circle that is already closed. The non-terminal graph has one node and no
edges, so a cycle is not constructible in it. No `## Dependency warning` section was appended to any
record.

**Parent-grounding-stale events: none.** The propagation scan ran against the one Circle carrying
`_b_`, `260813-0910-documentation-matches-shipped-plugin`, and found no parent. The one non-terminal
Circle cites neither that Circle's directory name nor the Artifact its closure note names, verified
by reading the record. The only Circle that did cite it,
`260813-0858-playmaker-maintains-backlog-store`, is itself terminal and therefore outside the scan.
No `## Parent grounding stale` section was appended to any record.
