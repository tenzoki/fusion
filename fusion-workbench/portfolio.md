# Portfolio

**Generated:** 260806-1103 (by playmaker session 260806-1103-playmaker-orchestrator-phase4)
**Domain bias:** code

## Active (_t_)

(none) — `260805-2005-textschicht-gegen-code-nachziehen` closed coherent this morning and `.active-circle` was cleared. The portfolio is between Turns; activation of the next Circle goes through `/fusion:next`.

## Anticipated (_a_) — ranked

Recommended next: 260804-1205-shell-reachability-model — every precondition its standing proposal named is now met, and the over-deny it closes is live in shipped v5.9.2 while the observed friction balance (17 false alarms, zero real hits) raises the pressure on the guard's deny behaviour.

1. **260804-1205-shell-reachability-model** — "The mutation classifier asks whether the shell guarantees a segment, not what one adjacent operator says".

   Both anticipated Circles pass the dependencies-closed check and both cite zero open decision records, because the workbench holds none anywhere; under the code-domain heuristic the tie breaks here. First, this Circle is activation-ready as written: its one hard dependency `260801-1244-guard-rules-write` is closed and shipped (v5.9.0–v5.9.2, tags pushed), which also answers the sequencing question its record had left open — the shipped-first path was taken, so the flat-joiner over-deny it closes (`circles/260801-1244-guard-rules-write/issues/260804-0839_o_the-flat-joiner-model-ignores-shell-precedence-so-a-pipeline-and-an-if-body-degrade-a-cd-the-shell-guarantees.md`) is now live for consuming projects. Second, the just-closed text-layer Circle's source reports measured what that liveness costs in practice: 17 fail-closed guard blocks against zero real hits in four days of the observed consuming project (`circles/260801-1244-guard-rules-write/analyses/260805-1830-zweck-nutzung-und-stand-des-plugins.md` §3). Whoever activates this Circle absorbs that balance into its Grounding, including the honest bound that the unresolvable-operand class (`mv "$f"`) stays out of a reachability model's reach; the record's appended proposal of run 260806-1103 restates both activation items. Third, the rival Circle is not activatable without prior re-shaping (next entry). Dependencies: `260801-1244-guard-rules-write` (closed) — clean.

2. **260801-1244-curator** — "The curator reconciles the three normative surfaces, and proves it on fusion's own conventions file". All dependencies closed (`260801-1244-rule-provenance-header` hard, `260801-1244-guard-rules-write` soft), but activation stays blocked on a shaper re-shape: its closing work C9 was partly done by hand during the guard Circles, which voids spec decision D-g and the Circle's designated validation case, as recorded in `circles/260805-2005-textschicht-gegen-code-nachziehen/_c_circle.md` `## Dependencies`.

## Recently closed (_c_ / _b_)

- **260805-2005-textschicht-gegen-code-nachziehen** — closed coherent (`_c_`) 260806: four code fixes, citation-form decision before the mechanical batches, two new lint tests, internals scoping at zero emissions from a consuming cwd; 60 of 66 corpus findings closed, suite at 1611 tests.
- **260801-1244-guard-rules-write** — closed coherent (`_c_`) 260805: deliberate per-session rule-file writes, all twelve acceptance criteria verified, shipped as v5.9.0–v5.9.2 with tags pushed.
- **260801-1244-rule-provenance-header** — closed coherent (`_c_`) 260802: provenance headers plus lint gate, eight commits, all eight acceptance criteria verified against the tree.
- **260801-1244-guard-bash-inspection** — closed coherent (`_c_`) 260801: the protected-path list now binds file-mutating Bash commands, not only the four write tools.
- **260719-1536-plane-mirror-integration** — closed coherent (`_c_`) 260720: `bin/fusion-plane` push-only mirror plus `/fusion:seed-from-plane`, offline-proven; two go-live follow-ups deliberately left open.

Four older closed Circles fall outside the last-5 cutoff: `260719-1536-brest-unite-co-creator-conversion`, `260718-1924-v5x-overhaul`, `260717-1638-marker-format-ohne-glob-metazeichen`, `260716-1847-workbench-umbau`.

## Archived (_s_ / _d_)

(none)

## Warnings

- **The curator Circle cannot be activated as it stands.** `260801-1244-curator` needs a shaper re-shape first (C9 done by hand, spec decision D-g void); that re-shape is its own shaper job and nobody owns it yet. Convene the shaper before offering this Circle at an activation gate.
- **One unowned residual from the closed text-layer Circle.** `circles/260805-2005-textschicht-gegen-code-nachziehen/issues/260806-0022_o_setup-klammer-probe-und-migrate-reformat-decken-verschiedene-baeume.md` is open with no owner; the closure note names it a candidate for a follow-up batch. The user decides whether it rides along with the next Circle or waits.

No dependency cycles detected, no bounded-closure Circles, and the `.active-circle` pointer state is consistent (absent while no Circle carries the active marker).
