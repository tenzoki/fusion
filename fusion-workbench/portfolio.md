# Portfolio

**Generated:** 260819-1732 (by playmaker session 260819-1732-playmaker-direct-dispatch)
**Domain bias:** code
**git HEAD at run:** `b91c01c`

**This run holds no user confirmation.** It was dispatched directly, with no confirmed-operations
block on the dispatch prompt and no channel through which to put a question. Its mandate is therefore
the narrow one: rank, regenerate this file in full, and rename backlog markers where the ranking calls
for it. It performed no split, merge, close or deferral. Every figure below was measured against disk
on this run; nothing is carried forward from the previous portfolio.

**The headline: the portfolio has a candidate again, and it is ready.**
`260819-1645-four-constraints-on-deep-change` was shaped at 16:45 today and is the only non-terminal
Circle in the store. It is recommended for activation. Its Grounding was measured at the same commit
this run reads, every one of its 21 workbench citations resolves on disk, and exactly one open
decision stands against it, blocking a quarter of the work rather than the plan.

## Active (_t_)

**(none).** `.active-circle` is absent and no Circle record carries the active marker. The two agree,
so no pointer warning is raised. This is the normal state between a closure and the next activation.

The last active Circle was `260816-1741-guard-becomes-observation-only`, bounded on 2026-08-17.

## Anticipated (_a_) — ranked

**Recommended next: `260819-1645-four-constraints-on-deep-change` — the only candidate in the store,
and it scores clean on dependencies with a single open decision that blocks one half of one of its
four constraints.**

1. **`260819-1645-four-constraints-on-deep-change`** (rank 1 of 1, `_a_`) — *A deep change to fusion
   fails loudly instead of silently.* Four ways a deep change to fusion can go wrong unobserved are
   closed: the committed `hooks/dist` is asserted to be the compilation of the committed source; all
   four write tools the guard names reach an integration case; the orchestrator forbids whole-tree git
   commands on every executor dispatch; and citations that workbench records make of other workbench
   records are resolved by a gate rather than by nobody.

   **Dependencies: nothing blocks it.** The section names no blocking Circle. It cites three terminal
   Circles as lineage, and each resolves to an existing directory:
   `circles/260816-1741-guard-becomes-observation-only` (bounded),
   `circles/260801-1244-guard-rules-write` (closed-coherent) and
   `circles/260805-2005-textschicht-gegen-code-nachziehen` (closed-coherent). One of the three carries
   the bounded rather than the closed-coherent marker, which a strict reading of the heuristic would
   flag; this run does not raise it as a flag, because the citation is reach under the Origin Rule
   rather than a precondition, and the bounded Circle's unreached clause is unrelated to what this
   Circle takes from it.

   **Open decisions cited in the Grounding: one, and the record declares it itself.** Three of the
   four decisions the Grounding cites carry the answered marker, and an answered-and-unrealised
   decision is the input this Circle consumes rather than a block on it. The fourth,
   `circles/260819-1645-four-constraints-on-deep-change/decisions/260819-1645_*_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md`,
   is open. What it blocks is bounded and worth stating precisely, because it decides how the Circle
   is planned rather than whether it can be. Constraints 1 to 3 do not touch it. Constraint 4 splits:
   the repair of the 245 dangling citations is unblocked and, by the Grounding's own sequencing, comes
   first in any case, since a gate armed against today's tree is red on the commit that arms it. Only
   the blocking test waits on the corpus answer. Activating now buys a plan for three and a half
   constraints and puts the open question to the user at the moment it becomes load-bearing.

   **Its evidence is unusually solid, and this run measured that rather than assumed it.** All 21
   workbench-record paths cited in the Directive, Grounding and Dependencies were expanded and
   resolved against the store; none dangles. The reconciliation of 260819-1400 found 18 dangling
   citations across the eleven older Circle records, so this is the twelfth record and the first clean
   one — direct evidence for the premise of its own fourth constraint. The Grounding states HEAD
   `b91c01c` as its measurement point, which is HEAD on this run, and its open-issue figure of 152 was
   re-counted here and matches exactly: 95 open in `shared/issues/` and 57 across the Circle stores.

   **One flag was appended to its record and it appears benign on reading.** See
   **Bounded-Closure propagation** under `## Warnings`.

   *Activation proposal appended to*
   `circles/260819-1645-four-constraints-on-deep-change/_a_circle.md`. Renaming the record and writing
   `.active-circle` are not this agent's writes; `/fusion:next` or the orchestrator commits them.

## Backlog — ranked

**Recommended to shape: `shared/backlog/260814-1733_*_bounded-executor-dispatches.md` — the only live
idea whose path is clear of a user act it cannot perform itself.**

    /fusion:direct shared/backlog/260814-1733_*_bounded-executor-dispatches.md

**Read this recommendation second, not first.** For the first time in four refreshes the portfolio
holds both a ready anticipated Circle and a ready backlog entry. They are not in competition, because
shaping produces a second anticipated Circle rather than a second active one, and the store can hold
any number of those. They are in sequence: activating
`260819-1645-four-constraints-on-deep-change` is the forward move, and shaping this entry is what to
do next if the user wants a second track in preparation rather than more work in flight.

**Live and ranked (2).** Ranking recomputed this run against the code-domain bias, which prefers an
idea whose cited evidence is already on disk and whose dependencies are clear over one that must wait
on an unresolved record.

1. **`shared/backlog/260814-1733_*_bounded-executor-dispatches.md`** (recommended, `_p_`) — bound how
   long an executor runs before returning to the orchestrator. Its evidence is on disk and already
   sized. `shared/analyses/260812-0303-simplify-speed-and-why-rules-do-not-hold.md` adopts the
   bounded-dispatch half of the user's filed proposal on cost grounds, at roughly a fourfold saving in
   re-sent tokens, and refutes the re-injection half by finding that the rules did not decay over a
   long dispatch but were never in force to begin with. That split is what makes the entry ready
   rather than raw: shaping it means putting a Directive narrower than the filed wording to the user
   and getting agreement on the narrowing, which is the conversation `/fusion:direct` exists to hold.
   Its companion record,
   `shared/issues/260812-0253_*_rules-lose-their-effect-during-a-long-dispatch.md`, is open and is
   context for the Directive rather than a gate on it.

   *Nothing is proposed for this entry.* It states one idea, can be promoted whole, and already
   carries the marker this run's ranking gives it.

2. **`shared/backlog/260814-1733_*_attach-the-rule-to-the-act.md`** (open, `_o_`) — a rule lands with
   an executable check or it does not land. Second **on shapeability, not on merit**, and the gap is
   unchanged across four consecutive refreshes: on the evidence this is the best-supported idea in the
   store, and it still cannot be shaped today.

   *The obstruction, re-verified on disk this run.* The decision it depends on,
   `shared/decisions/260810-0710_*_should-a-rule-be-allowed-to-land-without-the-check-that-enforces-it.md`,
   carries the deferred marker, and the record it waits on,
   `shared/issues/260810-0510_*_two-of-the-queue-ground-lints-negative-controls-re-implement-the-logic-instead-of-calling-it.md`,
   is still open. Reviving a deferred decision is the user's own act, and the entry's own text says the
   revival has to precede shaping it.

   *Today's evidence strengthens the idea again without moving its rank.* The reconciliation of
   260819-1400 measured 18 citations in the live Circle records that resolve to nothing, six of them
   created by an archive pass whose filter reads shipped text and never the workbench, and recorded
   that no gate looks in that direction. That is this entry's thesis on a fifth independent case: a
   norm written as prose governs nothing without something that executes it at the moment of the act.
   The rank does not move, because rank here is shapeability and the obstruction is untouched.

   *Nothing is proposed for this entry either.* It states one idea, so no split applies; both live
   entries cite the same rules-decay analysis but remain distinct Directives, one bounding dispatch
   length and the other binding a rule to an executable check, so a merge would consolidate two ideas
   into one and lose one of them. The idea is live, so it is not a close, and deferring it would cost
   the user two later acts where leaving it open costs one.

**Proposed and not performed (0).** This run proposes no split, merge, close or deferral, for the
reasons stated per entry above rather than left to be inferred.

**Performed this run: nothing.** Both live entries already carry the markers this run's ranking gives
them, so even the autonomous rename between open and recommended had nothing to do. No entry was
created, split, merged, closed or deferred.

## Recently closed (_c_ / _b_)

1. **`260816-1741-guard-becomes-observation-only`** (`_b_`, bounded 2026-08-17) — the compliance guard
   decides nothing: no verdict is reached on any path, and the decision-governed check, the halt, the
   block counter, the escalation module and the halt-clearing script are gone. Bounded rather than
   coherent on one unmet Directive clause, scoped out by the user. Read the caveat under
   `## Warnings`: this closure is the case the marker vocabulary has no word for.
2. **`260815-0007-remove-eight-mechanisms-and-cap-growth`** (`_c_`, closed 260815-2115) — nine
   mechanisms left the shipped plugin and a failing growth cap now covers `agents/`, `skills/` and the
   hook test lines; four of the Circle's own Grounding claims did not survive its closing measurement,
   which the Closure note records as the measurement working rather than failing.
3. **`260801-1244-curator`** (`_c_`, closed 260814-2200) — the curator reconciles the three normative
   surfaces and was proved on fusion's own conventions file; 29 commits over six Turns, coherent at
   the third reconciliation pass.
4. **`260813-0858-playmaker-maintains-backlog-store`** (`_c_`, closed 260813-1618) — the playmaker
   maintains the backlog store it is charged with; closed after the user took the reconciler's
   `review-needed` recommendation to revise the Artifact.
5. **`260807-0923-guard-misst-statt-orakelt`** (`_c_`, closed 260807-1650) — the guard stopped
   predicting which file a shell command would write and began measuring afterwards which one changed;
   34,793 lines deleted against 4,441 added.

The live store holds twelve Circle directories, where the previous refresh counted sixteen. The
difference left through an archive pass, and archived Circles are deliberately absent from every
section of this file: nothing in the archive store is a portfolio item. One of them, the
documentation Circle (`archive/260817-1907-safe-cleanup-scoped/circles/260813-0910-documentation-matches-shipped-plugin/_b_circle.md`),
stood in this section at the previous refresh and is gone from it for that reason and not because it
was reopened.

## Archived (_s_ / _d_)

- **`260804-1205-shell-reachability-model`** (`_s_`, superseded 260807-0923) — superseded by
  `circles/260807-0923-guard-misst-statt-orakelt/`, not closed and not bounded: the user replaced the
  mechanism rather than judging the Directive reached or unreachable.

No Circle carries the deferred marker.

## Warnings

**Pointer state: clean.** `.active-circle` is absent and no Circle record carries the active marker.
That raises none of `STALE-POINTER`, `POINTER-MISMATCH`, `MISSING-POINTER` or `MULTIPLE-ACTIVE`.

**Dependency cycles: none, on a graph with one node.** The graph is built from the `## Dependencies`
sections of non-terminal Circles. Exactly one such Circle exists,
`260819-1645-four-constraints-on-deep-change`, and its section names no blocking Circle. The three
Circles it cites as lineage are all terminal and so contribute no outgoing edge. No cycle is
reachable and no `## Dependency warning` section was appended to any record. Unlike the previous
refresh, which had no node at all to examine, this check ran against a real one.

**Bounded-Closure propagation: one flag raised, and on reading it appears benign.**
`260816-1741-guard-becomes-observation-only` carries the Bounded-Closure marker, and
`260819-1645-four-constraints-on-deep-change` cites it by directory name in its `## Grounding
snapshot`, under Constraint 2. That is the propagation condition, so a `## Parent grounding stale`
section was appended to the anticipated Circle's record and a `parent-grounding-stale` event was
logged. The check fires on the citation alone. The substantive reading is the opposite of a staleness
condition: the Grounding cites an issue that is still open inside the bounded Circle's store and
proposes to close it, which is the correct way to reach into a bounded Circle, and the clause that
Circle deliberately did not reach is neither cited nor depended on. **Nothing is triggered.** The user
reads the flag and decides whether the Grounding needs a line before activation.

**The unmet Directive clause still has nothing carrying it forward, and four releases have now
shipped over it.** `circles/260816-1741-guard-becomes-observation-only/issues/260817-1505_*_the-curator-and-its-skill-still-say-a-projects-guard-configuration-can-deny-a-write.md`
records that `agents/curator.md` and `skills/curate/SKILL.md` still describe a write denied by a
project's guard configuration, which no shipped code can now do. The reconciliation of 260819-1400
confirmed it untaken across four releases since. It sits in the issue store of a terminal Circle,
where nothing routes it to a session. Whether it becomes a Circle, a task batch, or is left standing
is the user's call; filing and dispatching are not this agent's acts.

**The bounded closure's own marker overstates what happened, and the defect is filed.** The
Bounded-Closure marker is defined as "Directive judged not reachable"; that Closure note says the
opposite, that the Directive was reachable and one clause deliberately not reached, with the rest
verified against the tree. The gap is filed as
`shared/issues/260817-1613_*_the-reconcilers-verdict-vocabulary-has-no-case-for-a-directive-that-is-reachable-but-deliberately-not-reached.md`.
It is raised here because this portfolio renders that marker and the entry under `## Recently closed`
would otherwise mislead.

**Eighteen citations in the live Circle records resolve to nothing, and no gate looks in that
direction.** Measured by the reconciliation of 260819-1400 across the eleven Circle records that
existed then: 11 broken by marker drift, 6 broken by the archive sweep of 260817-1907 whose filter
reads shipped text and never the workbench, 1 pointing at the wrong store. The twelfth record,
`260819-1645-four-constraints-on-deep-change`, was resolved on this run and is clean. This is the
condition the recommended Circle's fourth constraint exists to close, measured there at 245 dangling
tokens across a wider corpus. Repairing a record's body is not among this agent's operations, so the
finding is surfaced and nothing is written.

**Four decision records stand open, three of them in the stores of terminal Circles where nothing is
carrying them.** The shared decision store now has none open, down from two at the previous refresh:
both were implemented. The four are:

- `circles/260819-1645-four-constraints-on-deep-change/decisions/260819-1645_*_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md`
  — the only one with a carrier, since the Circle that filed it is the activation candidate.
- `circles/260801-1244-curator/decisions/260814-1915_*_should-mode-3-require-the-audit-line-on-every-run-instead-of-testing-whether-it-was-dispatched.md`
  — the reconciliation of 260819-1400 found this one gating three open defects in its own Circle as
  their stated closing condition, so leaving it unanswered holds four records rather than one.
- `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/decisions/260815-2056_*_what-marks-an-answered-decision-whose-answer-can-no-longer-be-realised.md`
- `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/decisions/260815-1845_*_does-analyst-get-a-project-local-rule-pattern-now-that-the-investigator-fold-orphaned-one.md`

**One answered decision's operative half reaches nothing on disk.**
`circles/260801-1244-guard-rules-write/decisions/260805-1548_*_wie-soll-ein-circle-verschwinden-duerfen-den-jemand-absichtlich-loescht.md`
was answered on 2026-08-05: a deliberately deleted Circle vanishes entirely, and the obligation moves
to the surviving references, which are annotated with the fact and the date. No rule file, prompt or
skill carries that obligation, so it is reachable by nobody about to delete a Circle. The recommended
Circle takes this on as the second half of its fourth constraint, which is one more reason to activate
it rather than leave it standing.

**Open defect volume, measured this run: 152, down from 168 at the previous refresh.** 95 records are
open in `shared/issues/` against 133 closed, and 57 across all Circle issue stores against 268 closed.
The fall is the work of the three reconciliation passes of 260819-1400, which closed 12 and
re-verified the rest against the tree at HEAD. Their own judgement on the remainder is worth carrying
here: the 57 Circle-store records are not stale, they reproduce exactly as filed, and the reason they
sit unworked is that they are filed under Circles that are finished. The problem is placement, not
truth.

**Two records touching this agent's own surfaces remain open.**

- `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/issues/260815-1633_*_eight-shipped-surfaces-still-present-the-three-demoted-skill-names-as-user-commands.md`
  — the presentational half of the command collapse. One row of its table is `agents/playmaker.md`.
- `circles/260813-0858-playmaker-maintains-backlog-store/issues/260813-1545_*_the-split-line-form-cannot-express-a-partial-split-which-is-the-only-case-the-store-actually-holds.md`
  — the portfolio's split-line form cannot express a partial split. It is inert this run, since no
  split is proposed, and would bind the next run that proposes one.

**One warning from the previous refresh is resolved.**
`circles/260815-0007-remove-eight-mechanisms-and-cap-growth/issues/260815-1247_*_a-backlog-entrys-related-line-points-at-a-marker-the-playmaker-has-since-moved.md`
— the dangling citation inside the backlog store this agent maintains — now carries the closed marker.
