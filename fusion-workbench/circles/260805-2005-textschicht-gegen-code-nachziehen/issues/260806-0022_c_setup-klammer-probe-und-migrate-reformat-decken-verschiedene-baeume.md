Setup's bracket probe and migrate's reformat pass cover different trees — a marker file outside shared/ and circles/ can still deadlock

---
The shape mismatch between the two is fixed (plan step 6, this Circle): both now select with the identical `\[[oatcibspd]\]-` basename filter. A scope mismatch remains. `/fusion:setup`'s bracket-marker probe walks the whole workbench tree minus the three frozen stores (`archive/`, `stashes/`, `.migration-v2-backup/`), while `/fusion:migrate`'s reformat pass visits only `shared/` (any depth) and `circles/` from depth 2 down, plus whatever the type-folder moves deposit into `shared/` first. A bracket-marker file anywhere else — at the workbench root, or in a directory the layout does not name — is flagged by setup but never renamed by migrate: setup refuses, migrate reports nothing to do, and the refuse→migrate→nothing-to-do deadlock reappears through the scope side instead of the shape side.

---
Found while executing plan step 6 of `planning/260805-2353_p_plan-textschicht-gegen-code.md` (the probe-shape fix), reading `skills/migrate/SKILL.md:85` (the reformat candidate `find` is `shared/` + `circles/ -mindepth 2` only) against `skills/setup/SKILL.md` Step 0's whole-tree `find "$WB"`. Pre-existing, not introduced by the shape fix; the shape fix reduces the incidence (only genuine `[x]-` marker names can now trigger it) but does not close the scope gap. Two candidate resolutions: scope setup's probe to the union of trees migrate actually converts (shared/, circles/ depth ≥ 2, and the eleven pre-v4 type folders), or extend migrate's reformat pass to the same tree setup probes. Either way the probe-consistency rule ("the detector must only look for things the executor can remove") is the criterion; the two files must move together.

---
Reconciliation 260806-1057 (final pass before Circle closure): confirmed a genuine open residual, not closed. Re-verified at HEAD fbd8c4d: skills/setup/SKILL.md:43 probes the whole workbench tree minus the three frozen stores, while skills/migrate/SKILL.md:85 reformats only shared/ (any depth) and circles/ from depth 2 — a bracket-marker file at the workbench root or outside the named trees is still flagged by setup and never renamed by migrate. The shape halves are unified (both use the \[[oatcibspd]\]- filter, plan step 6); only the scope halves differ. No commit of this session touches the scope gap. Note: the reference above cites the plan as 260805-2353_p_...; the plan is now _c_ — cite it in wildcard form 260805-2353_*_plan-textschicht-gegen-code.md per D1. Route: unowned defect, stays open in this Circle; candidate for a shared re-file or a follow-up batch at the user's discretion.

---
Resolved: Setup's bracket probe is narrowed to the union of trees `/fusion:migrate` converts — `shared/` at any depth and `circles/` from depth 2 — which is the same candidate list migrate's reformat pass renames (`skills/setup/SKILL.md:67` against `skills/migrate/SKILL.md:54,87`). The record offered two directions and named the deciding question: which tree a bracket-marked file could legitimately live in and still be live content. Only those two, so narrowing is the direction taken.

Why not the other direction. Widening migrate's reformat pass to setup's whole-tree probe would send a rename pass into `archive/`, `stashes/`, `.migration-v2-backup/`, the root-anchored session surfaces and `stilwerk/` — the trees migrate's own Guardrails forbid it to touch. To widen safely it would need its own exclusion list, and the two blacklists would then have to be kept in sync, which is the drift this defect already is. Narrowing also turns the criterion positive: the three frozen stores were named as `-not -path` exceptions to a whole-tree walk, and the tree bound subsumes all three and needs no fourth entry when a fourth frozen store appears.

The deadlock is closed at both ends. A bracket-marked file at the workbench root, or a flat `circles/*.md` whose name does not parse as a Circle stamp, no longer sets `OLD=1`: nothing converts it, so nothing refuses over it. Verified on a fixture workbench carrying one file of each kind — the probe at HEAD returned `OLD=1` naming `circles/notes[o]-unparsable.md`, the narrowed probe returns `OLD=0`, while a genuine pre-v4 flat Circle file still fires through the Circle-file probe and live bracket files under `shared/issues/` and `circles/<dir>/issues/` still fire through the bracket probe.

Residual filed, not fixed here: `rules/fusion-workbench-conventions.md:64` still counts setup among four consumers that exclude `stashes/` by path. That file was outside this task's permitted set — see `shared/issues/260816-0058_o_the-conventions-still-count-setup-among-the-consumers-that-exclude-stashes-by-path.md`. The two line numbers that record cites into this skill (`:60`, `:67`) were held valid by keeping the paragraph count unchanged.
