# Coder session — plan steps 14 and 15: the two text-layer lints

**Status:** Complete
**Plan:** circles/260805-2005-textschicht-gegen-code-nachziehen/planning/260805-2353_p_plan-textschicht-gegen-code.md (steps 14, 15 — both marked [DONE])
**Decision applied:** circles/260805-2005-textschicht-gegen-code-nachziehen/decisions/260806-0015_*_zitierform-fuer-workbench-records.md (D1, wildcard citation form — it is the reference lint's citation grammar)

## What landed

### Step 14 — `hooks/lib/__tests__/reference-resolution-lint.test.ts` (22 tests)

Resolves three reference classes across the shipped text surface (`rules/**`,
`agents/`, `skills/*/SKILL.md`, `README*`, `CLAUDE.md`, `docs/`, `templates/`,
plus the comment lines of `bin/*` shell scripts and `install.sh`):

- (a) plugin-file paths, including `$FUSION_PLUGIN_ROOT/<path>`; `./rules/…`
  spellings are the consuming project's and are never checked.
- (b) heading anchors in the adjacent `` `file.md` `## Section` `` form,
  prefix-matched; bare `## X` without a file is out of scope by design.
- (c) workbench-record citations in D1's wildcard grammar, resolved against
  THIS repo's `fusion-workbench/` (the stated bound: the lint runs only in the
  plugin suite, and the cited records legitimately live only here). Exact
  markers resolve exactly; a record that moved on is reported as "stale
  marker" with the `_*_` fix. Ellipsis truncations match any infix. Absent
  workbench: syntax-only, with a loud degradation test.

Exemptions are pattern-first (blockquotes, `e.g.`-announced illustrations,
`foo` slugs, footer-template spans, placeholder tokens) plus two enumerated
lists with per-entry reasons and honesty guards: EXAMPLE_PATHS (guard-doc
fabricated operands; a test asserts none exists in the tree, another that each
is still cited) and RECORD_EXAMPLE_FILES (`decision-record-examples.md`,
`skills/migrate/SKILL.md`; a test asserts each still carries fabricated
citations).

**Falsifier run:** files reverted to HEAD, the lint caught all 17 pre-fix
defects (5 dead playmaker citations, 5 stale markers, 1 dead-slug citation,
1 wrong store path, 1 wrong-file heading anchor, 6 dangling paths + 1 prose
false positive). Restored, green.

### Step 15 — `hooks/lib/__tests__/derivable-enumerations-lint.test.ts` (17 tests)

Diffs documented enumerations against the tree: skill roster (CLAUDE.md both
directions, README-agents table row-per-skill, phantom `/fusion:x` sweep over
READMEs + docs), five digit agent-count claims (derived N and N−1, never a
literal), always-on rule list + emission order vs the unindented
`emit_if_exists` block, conditional emission sets derived from the `IS_*`
case arms (including the internals file's own-repo-only condition), the
README-hooks `lib/` table, the stash-manifest field count (both word-number
claims vs the yaml schema's key count), and CLAUDE.md's DEFINITION_SITES echo.
Boundary documented in the header: prose set-membership is checked as
co-mention on one line (extra agents in a doc line are not caught), and
spelled-out word counts in code comments are out of scope. Live mutation check
with a scratch skill directory: 3 assertions fail, clean after removal.

## HEAD findings fixed in-step (all mechanical, per the calibration duty)

- `agents/playmaker.md` — five citation sites of four `260511-1031_a_*`
  records that exist nowhere in the workbench (the open half of finding
  `circles/260801-1244-guard-rules-write/issues/260805-1839_*_ausgelieferte-texte-zitieren-acht-workbench-records-die-nirgends-existieren.md`;
  the stash-and-lock half was fixed in Batch A). Rewritten per the 260805-0709
  rule with Batch A's "did not survive the workbench reorganisations" wording.
  Step 17 can now close that finding citing this step's commit.
- `README-hooks.md` — three stale-marker citations (`260804-1815_a_`,
  `260804-0841_p_`, `260803-1402_a_`) → `_*_`, and one garbled truncated slug
  (`260803-1803_a_…-cdpath-in-the-ambient-environment.md` matched no record;
  now `_*_…-cdpath-is-set-in-the-ambient-environment.md`).
- `rules/protected-path-internals.md:70` — `260804-1815_a_` → `_*_`
  (byte-identical length; the file is emitted only in the plugin repo, so no
  golden impact either way).
- `bin/fusion-plane` comment — `260719-2313_o_` → `_*_`.
- `skills/setup/SKILL.md` — pre-v4 path `fusion-workbench/issues/260717-0115…`
  → `fusion-workbench/shared/issues/…` (the record's real location).
- `skills/next/SKILL.md` — `## Marker globs` was cited against
  `rules/circle-records.md`; the section lives in
  `rules/fusion-workbench-conventions.md`.
- `README-agents.md` — the six "typical project-local rule files" table cells
  spelled bare `rules/…`; now `./rules/…`, the file's own convention for
  project-local paths (makes the plugin-vs-project distinction machine-readable).
- `bin/fusion-workbench-root` comment — "skills/scripts" (prose meaning
  "skills or scripts") read as a path; reworded.

No lint false positives survived calibration; the two patterns added during it
(footer-template spans, the migrate exemption) are documented in the test
header with reasons and guarded for dead weight.

## Verification

- Both lints green at HEAD after the fixes.
- Full suite: `cd hooks && npx vitest run` — **29 files, 1599 tests, all
  green** (baseline 1560 + 22 + 17).
- No emitted rule file changed size except a 1-byte-neutral marker swap in
  `protected-path-internals.md` (plugin-repo-only emission) — no golden
  regeneration needed; the golden fixture is untouched.
- Nothing committed, per instruction.
