# Step 10 — `skills/*/SKILL.md`: thirteen defect records closed on the skill bodies

**Date:** 260824-2047-coder-step-10-skills.md
**Agent:** coder
**Plan:** `260824-1905_*_plan-close-every-open-defect.md`, step 10
**Status:** Complete

## What was done

- The three inline `agentstate.yaml` domain reads (`next` Step 2, `direct` Step 3, `cleanup` Step 1) became one guarded call each to `bin/fusion-session-domain`, printing `domain=` and `source=`; a missing helper prints `source=helper-missing` so the fallback is reported (record 19).
- `archive`: the source-root call site takes the four-line guarded shape of its siblings (78); safety filter 1 names `.migration-v2-backup/` (46); the event-log sentence names the three live row kinds and the three historical ones (180).
- `curate`: the guard-denial clause is gone; a `failed` entry carries whatever reason it had (178's skill half).
- `next`: Step 5b reads the operation lines from the portfolio, not the report (148's skill half); a split is relayed one produced entry per line under a `split <entry path>:` header and approved per entry (149's relay); Step 6.1 sends the partial-identity claim to `rules/circle-records.md` `### The claim field` (213's read); Step 6.5 stops asserting a Directive read and names the Coherence gate's chain (75), and its printed message tells the user to say "go" when nothing follows (119, verified at HEAD that the imperative instruction already stood).
- `direct`: the Tone line describes the relay (51). `README-agents.md` gains the `**Answers:**` row, the Domain row cites both pass sites, the Mode and Draft rows cite the current lines; `agents/shaper.md` parses `**Answers:**` beside `**Draft:**`.
- `setup`: Step 0g reports a malformed settings file and writes nothing, and unions the allow list for an already-bypassing project (52); the domain bullet drops the retired input names and names `playmaker` (53); Step 0e guards the replace `cp`, stamps the destination, names `replace-failed`, and case 4 states two ways out (189); Step 0i's pointer-present branch says what it does (205) and names the own-identity claim case (219); the one-space line is deleted (215's skill half); 186's skill half was already true at HEAD.

## Budget

`skills/` head-room 3 220 bytes before, 1 770 after (baseline 220 439 + 20 000 − 238 669). No baseline map moved. `hooks/lib/__tests__/fixtures/surface-growth.golden` regenerated with `UPDATE_SURFACE_GOLDEN=1`.

`reference-resolution-lint` BASELINE re-approved on its existing line to `{ paths: 1350, anchors: 189 }`, from the concurrent step-12 re-approval's 1348/189; no net line added.

## Residuals, not mine to fix here

- `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout` still says the archive skill excludes `stashes/` only; after 46 it excludes both (step 13).
- `agents/playmaker.md:207` and `rules/circle-records.md` still carry the single-line split form; `skills/next/SKILL.md:140` now carries the header form 149 recommends (rule half is step 13).
- `npm test` is red on one test outside this step: `committed-dist` "every helper in bin/ is tracked", because `bin/fusion-session-domain` (step 7) is untracked until committed; its `.gitignore` line is present.
