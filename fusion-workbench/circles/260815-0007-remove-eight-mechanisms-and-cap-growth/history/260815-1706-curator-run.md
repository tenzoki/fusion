# Curator run — survey (gate G1) and apply

**Date:** 2026-08-15 17:06
**Status:** Complete (both passes; the survey ran at 17:06, the apply pass at 17:50 — see §9)
**Git HEAD read:** `5f2171eab43e6a0f0e2f981b291d7b40858e598f` (`5f2171e`)
**Mode:** survey, then apply (two dispatches, one run file)
**Previous curator run:** 2026-08-14 13:32, at HEAD `ae21c87d67d42140b796d4a1addb4ca46ad664e1`, run file `circles/260801-1244-curator/history/260814-1332-curator-run.md`. Found by `find fusion-workbench -name '*curator-run*'` across `$SCAN_HISTORY` and every other Circle; exactly one match.

**The interval.** 28 commits, `9a7da8e..HEAD`, all on 2026-08-15, all inside Circle `260815-0007-remove-eight-mechanisms-and-cap-growth`. Eight mechanisms left the plugin and the administrative surface collapsed to three names. That is the whole reason this run exists, and it is why every entry below is dated 2026-08-15 or cites something that happened in that range.

**Project:** the fusion plugin's own source repository. `./rules/` here *is* the plugin's shipped rule corpus, which makes two consequences named under §2 apply that would not apply in a consuming project.

**Git-history reads are not windowed.** The full `git log --follow` was taken per rule file and for `CLAUDE.md`, not the interval alone. The interval is reported so a reader can see what moved since the last run; it did not bound the evidence.

---

## 1. Evidence-source counts

| # | Source | Resolved location | Present | Read | Notes |
|---|---|---|---|---|---|
| 1 | Circle records | `$SCAN_CIRCLES` = `circles` | 15 | 1 in full (`260815-0007-…/_t_circle.md`, including its Turn log), 15 enumerated by marker | 14 carry a terminal marker; one is `_t_`. The active record's Turn log is the only written account of Turn 4, per §7 |
| 2 | Decision records | `$SCAN_DECISIONS` = `circles/260815-0007-…/decisions shared/decisions` | 48 in the editable surface; 90 in the whole workbench | 48 read at head level (marker, `**Status:**`, title, `Cross-references:`, every resolution annotation); 7 read in full | The editable surface grew from 46 to 48 since the last run: three new records in this Circle, minus none removed, plus one Circle-decision reclassification. See §4 |
| 3 | Session histories | `$SCAN_HISTORY` = `circles/260815-0007-…/history shared/history` | 231 (17 + 214) | 3 in full (`260815-1617-coder-collapse-administrative-surface.md`, `260815-1403-coder-fold-investigator-into-analyst.md` head, `260815-0729-coder-before-measurement.md` head), all 231 enumerated | 449 history files across the whole workbench. **The session history `shared/history/260814-2306-orchestrator-session.md` has no Turn 4 section**, because a Turn's section is written at the next Turn's boundary and this gate stands inside Turn 4 |
| 4 | Git | the repository | — | `git log --oneline 9a7da8e..HEAD` (28 commits, each subject read); `git log --follow` on `CLAUDE.md` and on each of the 12 files in `rules/`; `git rev-parse HEAD` | Full history per file, not windowed |
| 5 | Reviews and analyses | `$SCAN_REVIEWS` = 31 (9 + 22), `$SCAN_ANALYSES` = 15 (0 + 15) | 46 | 2 read in substance (`260815-1637-coderev-turn-4-…`, `260815-1501-coderev-turn-3-…`), 4 read in part, 46 enumerated | The Circle's own `analyses/` is **empty — zero files**, stated explicitly. The Turn 4 coderev's §D and `## Recommended sequencing` are the routing this run acted on |
| 6 | `orchestrator-events.jsonl` | `$WORKBENCH` root | 1 553 lines | today's slice parsed and aggregated by type; corroborating only | 63 events dated 2026-08-15: 13 `task_start`, 12 `task_done`, 8 `commit`, 6 `state_drift`, 6 `review_start`, 6 `review_done`, 4 `turn_start`, 4 `gate_response`, 3 `turn_end`, 3 `coherence_review`, 2 `planner_done`, 1 each of `planner_start`, `conceptrev_done`, `bugfix_start`, `bugfix_failure`. **No finding below rests on this source alone.** Two retired event types still appear in today's slice (`state_drift`, `conceptrev_done`), written before their steps landed — history, not a defect |
| 7 | Archive store | `$WORKBENCH/archive` | **0 files** | — | Explicitly zero. `find fusion-workbench/archive -type f` returns nothing. Unchanged since the previous run |
| 8 | Provenance headers | the 12 files in `rules/` | 12 | 12 | All present, all on line 3. 4 cite a Circle directory, 8 use the admission-plus-commit form. **`rules/commit-lock.md` carries `**Provenance:** circles/260801-1244-guard-rules-write` and kept it through the 2026-08-15 rename from `workbench-stash-and-lock.md`** — correct, the Circle still exists and is still the file's reason. No rule file cites a record carrying the superseded marker; the workbench's one `_s_` record (`shared/decisions/260812-0254_s_should-the-investigator-get-case-folders-with-a-status-per-case.md`, transitioned by step 8 today) is cited by no rule file |

**Nothing was unreadable.** No permission error, no file that errored. `.claude/rules/` is **absent** in this repository — reported as zero, not as a fault, so the project-owned rule surface is `./rules/` alone.

**One source of noise, stated.** `$FUSION_PLUGIN_ROOT` is `/Users/k1/.fusion`, the installed copy, while the work tree is `/Users/k1/Projects/productive/fusion`. `rules/rule-file-provenance.md` was read from the installed copy as `agents/curator.md` Setup requires, then diffed against the work-tree copy: **byte-identical**. The documented residual did not bite this run.

---

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|---|
| Decision records (`$SCAN_DECISIONS`) | 48 | 360 939 | 4 760 | 360 939 | 4 760 |
| Project-owned rule files (`rules/`) | 12 | 145 656 | 1 738 | 146 677 | 1 740 |
| `CLAUDE.md` | 1 | 54 441 | 134 | 55 963 | 134 |

`.claude/rules/` does not exist, so the rule-file surface is `./rules/` alone.

**Blast-radius verdict: clear, and not close.** No entry deletes a section, a paragraph, or a rule. The largest single deletion is one clause of one sentence (L05, about 50 bytes); summed deletions across all three surfaces are **under 900 bytes** against 561 036, and under 1.7 percent of the smallest of the three surfaces (`CLAUDE.md`). Nothing approaches the 20 percent threshold, so the separate scale-confirmation prompt is **not** raised. The net effect of the ledger is **growth**, not shrinkage: eight of the twelve entries add more text than they remove.

### Two consequences of an approved rule-file edit, named because neither owner is me

Both bite only in this repository, where `./rules/` is the plugin's own shipped corpus. In a consuming project `./rules/` is that project's own directory and neither applies.

1. **The golden fixture goes stale.** `hooks/lib/__tests__/fixtures/rules-emission.golden` pins the byte size of every file in `rules/`, and `hooks/lib/__tests__/rules-emission-golden.test.ts` fails on any change to the emitted path set, order or size. Entries **L10, L11 and L12** all touch `rules/fusion-workbench-conventions.md`, so approving any of them makes `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts` mandatory before the suite is green again. That regeneration is **coder work** — I do not run it.

2. **The armed growth bound is charged.** `RULE_BASELINE`'s five core entries sum to 86 573, re-cut at the 2026-08-14 arming; the five files measure **86 897** at HEAD, so the corpus stands **324 bytes above baseline** with **11 676 of the 12 000-byte `GROWTH_BUDGET` unspent**. L12 is the only entry that adds materially — about **480 bytes** — taking the overshoot to roughly 804 and leaving about 11 196. It does not approach the bound. `RELEASE_CAP` and `DRIFT_CEILING` are untouched and are not read by this run.

---

## 3. Comparison counts

Each verdict covers the pairs its stated selection rule reached, **not the corpus**. Corpus counts are derived by command, named beside each rule.

| # | Surface pair | Possible pairs | Selection rule | Selected | Read |
|---|---|---|---|---|---|
| 1 | decision × decision | 1 128 (`48·47/2`, from `ls shared/decisions/*.md circles/260815-0007-…/decisions/*.md \| wc -l`) | one record's text contains the other's `YYMMDD-HHMM` stamp **and** its slug | 22 | 22 |
| 2 | decision × rule file | 576 (`48·12`) | the record names a `rules/` path, or a rule file names the record's slug | 31 | 31 |
| 3 | decision × `CLAUDE.md` | 48 | `CLAUDE.md` names the record's stamp or slug, or the record names `CLAUDE.md` | 14 | 14 |
| 4 | rule × rule | 66 (`12·11/2`) | **exhaustive** — all 12 files read in full or grepped end-to-end for every removed mechanism | 66 | 66 |
| 5 | rule × `CLAUDE.md` | 12 | **exhaustive** | 12 | 12 |
| 6 | `CLAUDE.md` × itself | 1 surface | **exhaustive** — read in full, all 134 lines, section against section | — | — |

**Pair 1's verdict, with its selector attached:** across the 22 citation-linked pairs the rule reached, **no live record overturns another**, and none of the three decision records this Circle filed cites an earlier record at all. The previous run reached 19 pairs in a 46-record surface and returned the same verdict; this run reaches 22 in a 48-record surface and returns it again. Both are statements about the pairs the selector found, not about the corpus.

**A separate mechanical scan, run because pair 1 cannot see it.** Every `rules|agents|skills|bin|docs|templates|hooks|stilwerk`-shaped citation inside the 48-record surface was resolved against the tree. **17 records carry at least one dangling shipped-path citation**, and the count rose today with the eight removals. **Not one of them is proposed for editing**, and the reason is a rule rather than an omission: a decision record that decided to delete `hooks/lib/churn.ts` correctly cites `hooks/lib/churn.ts`, and rewriting that citation would destroy the record rather than repair it. A dangling citation inside a historical record is expected. The scan is reported as a measurement.

---

## 4. Pre-edit content of every decision record this run intends to modify

**None.** This run proposes **zero** changes to any decision record. There is therefore no pre-edit content to preserve, and the obligation is satisfied by the emptiness rather than waived.

Why zero, stated so a later reader does not read it as a gap — this is the largest finding of the run and it is in §7.

---

## 5. The ledger — 12 proposed changes

Presented most consequential first. **Zero constraint removals.** No entry removes a constraint, and no entry rests on a re-reading of the current text alone.

| Group | Count | Ids |
|---|---|---|
| Constraint removals | **0** | — |
| Tier 3 | **0** | — |
| Tier 2 | **3** | L07, L11, L12 |
| Tier 1 | **8** | L01–L06, L09, L10 |
| Consolidations | **1** | L08 |
| **Total offered** | **12** | |
| Candidates (never offered, never applied) | **1** | C01 |

---

### L01 — the `templates/` Layout row names two files that are gone, and stops naming any

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 49)
- **Tier:** 1
- **Citation:** `ls templates/` → `fusion-guard.json` — one file. The row names three. `plane.config.yaml` left with the Plane mirror (step 2, `d0ddabb`); `investigator-capture-layout.md` left with the investigator fold (step 8, `7260bbc`). Both spellings are bare filenames, which is why `reference-resolution-lint` cannot parse them as paths and `derivable-enumerations-lint` has no check for the row — recorded, with the widening this row took today, in `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/issues/260815-0803_o_two-claude-md-inventory-rows-went-stale-and-neither-lint-gate-can-see-them.md`, which routes the row itself to this gate.
- **Consequence group:** tier-1
- **Constraint removed:** none — the deleted text is an inventory, not a rule.
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note — this is a derivation, not a corrected number.** The row already carries its own command (`ls templates/`). Naming the one surviving file would produce a third inventory that goes stale at the next `/fusion:setup` template, which is exactly what the two worked precedents in this file refuse: the always-on floor paragraph at `:62` and the deleted workbench file count at `:53`.

**Before:**
> | `templates/` | Starter files `/fusion:setup` seeds into consuming projects; each has its own destination. Currently: `investigator-capture-layout.md` (the user copies it to the project's `./rules/`; required by `investigator` to know where its evidence captures live), `fusion-guard.json` (setup seeds it to the project root, git-tracked), `plane.config.yaml` (setup seeds it into the workbench). Check `ls templates/` and the setup skill for the current set. |

**After:**
> | `templates/` | Starter files `/fusion:setup` seeds into consuming projects; each has its own destination. **The set is deliberately not written here.** Run `ls templates/`, and read the setup skill for where each file goes — it names the destination beside the copy step, which an inventory in this row cannot. An inventory *did* stand here and went stale twice in one day on 2026-08-15, once per removal, invisibly to both lint gates: a bare filename is not a path, so `reference-resolution-lint` has nothing to resolve, and `derivable-enumerations-lint` carries no check for this row (defect `fusion-workbench/circles/260815-0007-remove-eight-mechanisms-and-cap-growth/issues/260815-0803_*_two-claude-md-inventory-rows-went-stale-and-neither-lint-gate-can-see-them.md`). Do not restore one. Two entries left that day: `plane.config.yaml` with the Plane mirror, and the investigator's capture-layout template with the agent that required it. |

---

### L02 — the `docs/` Layout row names a deleted file

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 50)
- **Tier:** 1
- **Citation:** `ls docs/` → `philosophy.md  working-model.md`. The row names three; `plane-setup.md` was deleted with the Plane mirror on 2026-08-15 (step 2, `d0ddabb`). Same defect record as L01, and the plan routed this row to this gate by hand at its line 173.
- **Consequence group:** tier-1
- **Constraint removed:** none.
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note — why this row keeps its two names where L01 keeps none.** The `docs/` row's payload is which file `/fusion:help` points at, which `ls docs/` cannot tell a reader. The row already declares `ls docs/` authoritative for the set; only the phantom entry goes.

**Before:**
> | `docs/` | Conceptual docs that are useful both inside Claude Code (pointed at by skills) and as standalone reading. Currently: `philosophy.md` (the load-bearing ideas + domain parameter, pointed at by `/fusion:help`), `working-model.md` (how the working model operates — Circle flow, gates, guard), and `plane-setup.md` (the Plane bridge install doc). `ls docs/` is the authoritative set. |

**After:**
> | `docs/` | Conceptual docs that are useful both inside Claude Code (pointed at by skills) and as standalone reading. Currently: `philosophy.md` (the load-bearing ideas + domain parameter, pointed at by `/fusion:help`) and `working-model.md` (how the working model operates — Circle flow, gates, guard). A third, the Plane bridge install doc, went with the mirror on 2026-08-15. `ls docs/` is the authoritative set, and it is authoritative because this row is not: no lint gate reads a bare filename in it. |

---

### L03 — `bin/fusion-staging-drift` is said to have two siblings and has one

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 42)
- **Tier:** 1
- **Citation:** `grep -l 'verdict=' bin/*` → `bin/fusion-review-coverage`, `bin/fusion-staging-drift`. Two, so `bin/fusion-staging-drift` has **one** sibling. `bin/fusion-state-drift` and `hooks/lib/state-drift.ts` were deleted on 2026-08-15 (step 11, `f45f76a`); `ls hooks/lib/*.ts` no longer lists `state-drift.ts`. The trio the phrase counted was state-drift, staging-drift and review-coverage.
- **Consequence group:** tier-1
- **Constraint removed:** none.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> including the same stdout-verdict rule its two siblings carry.

**After:**
> including the same stdout-verdict rule its one remaining sibling carries. There were three of these thin wrappers until 2026-08-15, when `bin/fusion-state-drift` went with the counters it measured; `bin/fusion-review-coverage` is the other survivor.

---

### L04 — the domain value list is two values too long

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 16)
- **Tier:** 1
- **Citation:** `grep -n 'code | data' agents/taskplanner.md agents/reconciler.md agents/playmaker.md` → all three prompts accept `code | data` and nothing else (`agents/taskplanner.md:25`, `:38`; `agents/reconciler.md:30`, `:43`; `agents/playmaker.md:27`, `:36`). `README-agents.md` `## Dispatch parameters` and `docs/philosophy.md:19` already read `code | data`; **this line and L05 are the last two carriers of the retired values in the shipped tree.** Removed by step 9 (`0894d0d`); the measurement behind the removal is item 6 of the Circle's Grounding snapshot — `strategic` never passed, `knowledge` passed twice, both on 12 May, across the 138 dispatches where the parameter means anything.
- **Consequence group:** tier-1
- **Constraint removed:** none — the values name behaviour no prompt implements.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> Three (`reconciler`, `taskplanner`, `playmaker`) are parameterised by domain (`code | data | strategic | knowledge`) since v1.11.0;

**After:**
> Three (`reconciler`, `taskplanner`, `playmaker`) are parameterised by domain (`code | data`) since v1.11.0 — `strategic` and `knowledge` left on 2026-08-15, measured at zero and two dispatches respectively;

---

### L05 — the dispatch-parameter bullet states a derivation this Circle deleted

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 58)
- **Tier:** 1
- **Citation:** `agents/orchestrator.md:406` — *"Prefix **every** planner dispatch with `**Executors:** coder, ontocoder, analyst` on its own line, with no condition in front of it… You do not hold the input for that judgement at this phase, which is why you no longer make it."* `agents/planner.md:27` states the same from the other side. Decision `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/decisions/260815-0029_*_what-triggers-the-analyst-executor-set-once-strategic-and-knowledge-are-gone.md`, option 1, `Implemented: 0894d0d`. Filed against this exact clause by the executing coder as `issues/260815-1447_*_claude-mds-dispatch-parameter-bullet-asserts-orchestrator-behaviour-step-9-inverted-not-just-a-value-list.md`, which says the clause needs *deleting or rewriting*, not merely trimming.
- **Consequence group:** tier-1
- **Constraint removed:** none. The deleted clause asserts a mechanism that no longer exists; a reader acting on it would reinstate a branch this Circle removed on a user-answered decision, which is the reverse of a constraint.
- **Revert path:** `git checkout -- CLAUDE.md`
- **The other half of this pair is not mine.** `README-agents.md:79` carries the identical false clause while its own table row at `:66` states the truth. That file is outside the curator's surface; filed this run as `issues/260815-1706_o_readme-agents-closing-paragraph-contradicts-its-own-table-row-about-the-executor-derivation.md`, owner `coder`. **Approving L05 without that fix leaves the two documents disagreeing in the opposite direction from today.**

**Before:**
> First: `**Domain:** <code|data|strategic|knowledge>` changes behaviour in `taskplanner`, `reconciler` and `playmaker` only, defaulting to `code`; `shaper` accepts the line in anticipated-circle mode and merely copies it into the record it writes; `planner` takes `**Executors:** coder, ontocoder, analyst` (default `[coder, ontocoder]`), which the orchestrator derives from the detected domain.

**After:**
> First: `**Domain:** <code|data>` changes behaviour in `taskplanner`, `reconciler` and `playmaker` only, defaulting to `code`; `shaper` accepts the line in anticipated-circle mode and merely copies it into the record it writes; `planner` takes `**Executors:** coder, ontocoder, analyst` (default `[coder, ontocoder]`), which the orchestrator passes on **every** planner dispatch with no condition in front of it. The domain-conditional derivation was deleted on 2026-08-15: whether a step needs `analyst` is a question the plan answers, and no caller upstream of the plan holds the input to answer it.

---

### L06 — the Circle-directory rationale still names the Plane mirror as a beneficiary

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 71)
- **Tier:** 1
- **Citation:** `ls bin/fusion-plane` → no such file; `ls docs/plane-setup.md` → no such file; `find . -name 'fusion-plane*'` inside `hooks/` returns nothing. The mirror was removed on 2026-08-15 (steps 2 and 3, `d0ddabb` and `7c12d6a`), 5 330 lines of code and test and 44 121 bytes of prose, on zero successful pushes ever recorded in either measured project.
- **Consequence group:** tier-1
- **Constraint removed:** none — the surviving half of the rationale is the load-bearing half and is kept verbatim.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> (the directory name is stable across the whole lifecycle, so references into a Circle never break, and the later Plane mirror gets an immutable natural key)

**After:**
> (the directory name is stable across the whole lifecycle, so references into a Circle never break). A second reason was given at the time — an immutable natural key for the Plane mirror — and it went with the mirror on 2026-08-15; the first reason stands on its own and always did.

---

### L09 — the project-rule examples still name the investigator's capture layout

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 59)
- **Tier:** 1
- **Citation:** `ls templates/` → no `investigator-capture-layout.md`; `ls agents/` → no `investigator.md`. Both left on 2026-08-15 (step 8, `7260bbc`). A capture layout is now a rule nothing in the shipped plugin reads, so offering it as the leading example of a fusion-agent-specific project rule invites a consuming project to write a file no agent will load.
- **Consequence group:** tier-1
- **Constraint removed:** none — one example of two goes; the category and its second example stay.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> the project's `./rules/` (fusion-agent-specific rules — capture layouts, taskplanner priority overrides, anything no other Claude session would care about)

**After:**
> the project's `./rules/` (fusion-agent-specific rules — taskplanner priority overrides, anything no other Claude session would care about)

---

### L10 — the layout tree presents `/fusion:archive` as a command

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md` (line 47)
- **Tier:** 1
- **Citation:** The token is not false — `skills/archive/` still exists, so `/fusion:archive` still resolves, and that is precisely why no lint sees it (`issues/260815-1633_o_eight-shipped-surfaces-still-present-the-three-demoted-skill-names-as-user-commands.md`, which names `rules/fusion-workbench-conventions.md:47,79` as two of the eight and says the sweep *"could reasonably ride with the curator's pass, since `rules/fusion-workbench-conventions.md` is one of that agent's three surfaces"*). What is falsified is the **presentation**: `CLAUDE.md:21`, `CLAUDE.md:51`, `README-agents.md:229-246` and `skills/help/SKILL.md:76-79` were all rewritten on 2026-08-15 to present `archive` as Step 4 of the cleanup pipeline, reachable as `/fusion:cleanup --only archive`. The Turn 4 coderev routes exactly this to gate G1 under `## Recommended sequencing`.
- **Consequence group:** tier-1
- **Constraint removed:** none — the target of the store is unchanged, only how the writer is named.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Charged:** ~+22 bytes to an always-on rule; golden regeneration required (§2).

**Before:**
> ├── archive/                           # /fusion:archive target

**After:**
> ├── archive/                           # target of cleanup's archive step

---

### L11 — the guard-log preservation paragraph names `/fusion:archive` twice as the user's route

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md` (line 79)
- **Tier:** 2
- **Citation:** Superseded by the recorded position in `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/history/260815-1617-coder-collapse-administrative-surface.md` `## What "collapse" does and does not mean`, which states that the collapse is *"what fusion presents and documents"* and that `CLAUDE.md`'s skill line and Layout row say so. The sentence it overturns is this paragraph's *"What preserves that record is `/fusion:archive`"* — a demoted name presented as the user's route, in the file that is the layout's single authoring home. The step's `## Judgement calls, named` item 4 routes exactly this class to the curator's narrative pass. Same defect record as L10.
- **Consequence group:** tier-2
- **Constraint removed:** none. The mechanism, the rolling behaviour, the no-ceiling argument and the tracked/untracked consequence are all preserved word for word; only the two name tokens change.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Charged:** ~+46 bytes to an always-on rule; golden regeneration required (§2).

**Before:**
> **What preserves that record is `/fusion:archive`, not a ceiling and not tracking the live file.** The log has no line or byte limit anywhere, deliberately: every such limit discards the oldest lines first, and the oldest lines are the block, halt and clear events, which are the only ones recording the guard enforcing anything. `/fusion:archive` rolls the live log into the archive store under a dated name and starts a fresh empty one, so the rolled copies are ordinary archived files and are kept wherever the archive store is kept.

**After:**
> **What preserves that record is the archive step of `/fusion:cleanup`, not a ceiling and not tracking the live file.** The log has no line or byte limit anywhere, deliberately: every such limit discards the oldest lines first, and the oldest lines are the block, halt and clear events, which are the only ones recording the guard enforcing anything. That step — the whole pipeline, or `/fusion:cleanup --only archive` alone — rolls the live log into the archive store under a dated name and starts a fresh empty one, so the rolled copies are ordinary archived files and are kept wherever the archive store is kept.

---

### L12 — the layout definition no longer knows about two legacy stores that four shipped consumers exclude

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md` (insertion after the layout tree, before *"**The root-anchored surfaces are not negotiable.**"*)
- **Tier:** 2
- **Citation:** Commit `5d29b6d` removed `stashes/` from this file's layout tree with the two stash skills. Four shipped consumers still exclude it, all verified present at HEAD: `skills/setup/SKILL.md:67`, `skills/log-activity/SKILL.md:82`, `skills/archive/SKILL.md:96`, `agents/playmaker.md:61`. The recorded position that supersedes the tree's current silence is `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/reviews/260815-1251-coderev-turn-2-build-churn-and-stash.md` and the defect it filed, `issues/260815-1251_o_four-shipped-consumers-exclude-a-stashes-store-the-layout-definition-no-longer-knows-about.md`, whose **Owner** line reads *"`coder`, or the `curator` at gate G1 if it is treated as normative prose"* and which names the wording, the placement and the one over-claim not to repeat. The cost it names is concrete: `skills/setup/SKILL.md:60` records that dropping the exclusions turns the bracket-marker probe into a permanent Setup refusal routing to a `/fusion:migrate` that reports nothing to do — *"a deadlock rather than a mere false positive, and it was hit twice on one consuming project"*.
- **Consequence group:** tier-2
- **Constraint removed:** **none — this entry adds one.** It is the only entry in the ledger that is a net addition of normative text, and it is offered on the defect record's own routing rather than on the curator's initiative. Routing it to `coder` instead is a legitimate answer at this gate.
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Charged:** ~+480 bytes to an always-on rule. That is the ledger's whole material cost against the armed bound (§2), taking the corpus from 324 to roughly 804 bytes above `RULE_BASELINE`, against 12 000 of head-room. Golden regeneration required.
- **`speculation:` deliberately avoided.** The reason written below is the principle (*frozen content is not live content*), **not** a measured hit. `skills/setup/SKILL.md:60`'s 1 146 matches were all under `archive/` and `.migration-v2-backup/` and none under `stashes/`; the commit message for `5d29b6d` states the reverse, and the defect record flags that as an inference dressed as a measurement. This wording does not repeat it.

**Before:**
> **The root-anchored surfaces are not negotiable.**

**After:**
> **Two legacy stores are absent from this tree on purpose, and four shipped consumers still exclude them.** A workbench may carry `stashes/`, written by the two Circle stash-and-restore skills until they were removed on 2026-08-15, and `.migration-v2-backup/`, left by the retired `/fusion:migrate-workbench-v2` (fusion v2.3–v2.5) as its rollback copy. Nothing shipped writes to either any more, which is why neither is in the tree above: a line here would read as a store the plugin still creates. The exclusions stay all the same — `skills/setup/SKILL.md:67`, `skills/log-activity/SKILL.md:82`, `skills/archive/SKILL.md:96` and `agents/playmaker.md:61` — on the principle that frozen content is not live content. Do not read their absence from this tree as permission to drop one: `skills/setup/SKILL.md:60` records what dropping them costs, a Setup that refuses permanently and routes to a migration with nothing to do.
>
> **The root-anchored surfaces are not negotiable.**

---

### L07 — the Circle-layout row still presents three review kinds

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 53)
- **Tier:** 2
- **Citation:** Superseded by `rules/fusion-workbench-conventions.md:294`, which is the authoring home for the filename patterns and now reads *"`<sender>` on a review file is `coderev` or `ontorev`… Older files may carry a third sender, `conceptrev`, retired with its agent on 2026-08-15."* The sentence it overturns is `CLAUDE.md:53`'s parenthetical `(code+onto+concept merged)`, which presents the three-way merge as the current composition. `ls agents/conceptrev.md` → no such file (step 7, `a17cc8c`).
- **Consequence group:** tier-2
- **Constraint removed:** none — the merge is still described, and the retired sender is preserved as history for the reader who meets an old file.
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> `reviews/` (code+onto+concept merged) and `analyses/`

**After:**
> `reviews/` (code and onto merged; a third sender, `conceptrev`, was retired with its agent on 2026-08-15 and older review files still carry it) and `analyses/`

---

### L08 — the three step selectors are claimed to exist and never spelled, and one of them differs from its skill name

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md` (line 21)
- **Tier:** consolidation
- **Citation:** `skills/cleanup/SKILL.md:44-53` — the selector vocabulary is `issues`, `commit`, `reconcile`, `archive`, `claude-md`, `log-activity`, `commit-housekeeping`, and *"A name the table does not carry is an error: say which name and list the valid ones."* `CLAUDE.md:21` names the three demoted bodies as `/fusion:archive`, `/fusion:curate` and `/fusion:log-activity` and then says each is *"reachable alone as `/fusion:cleanup --only <step>`"* without giving the substitution. Two of the three substitute correctly; the third does not — `curate`'s selector is `claude-md`, and `--only curate` is required to be rejected. Filed as `issues/260815-1633_o_the-claude-md-steps-only-selector-is-claude-md-and-the-documents-that-say-reachable-alone-never-spell-it.md`.
- **Consequence group:** consolidation
- **Constraint removed:** **none.** This is a consolidation in the strict sense: every constraint the sentence expresses is preserved and one under-specification is closed. It adds information and removes none.
- **Revert path:** `git checkout -- CLAUDE.md`
- **The sibling half is not mine.** `skills/curate/SKILL.md:2` is the other surface the defect names, and it is a skill body — `coder`'s. This entry does not depend on it.

**Before:**
> each reachable alone as `/fusion:cleanup --only <step>`

**After:**
> each reachable alone as `/fusion:cleanup --only archive`, `--only claude-md` and `--only log-activity`. **Two of those three selectors are the body's own name and one is not** — the `CLAUDE.md` step's selector is `claude-md`, and `--only curate` is rejected by the selector's error path

---

## 6. Candidates — reported, never offered for approval, never applied

### C01 — 13 decision records now assert a mechanism that no longer ships, and the marker question that governs them is open

- **Surface:** decision records
- **Files:** 13 in the editable surface, listed below
- **Tier:** would be 3 — the stop-date is statable (2026-08-15, per step) and so is the successor (none in every case)
- **Why it is a candidate and not an entry:** an **open decision record owns this exact question** and answering it by acting is what my own remit forbids. `circles/260801-1244-curator/decisions/260814-1332_o_what-marks-an-implemented-decision-whose-implementation-was-later-deleted.md` — filed by the previous curator run, still `_o_` — asks *"What marks an implemented decision whose implementation was later deleted, with no superseding decision to cite?"*, sets out four options, recommends option 3 (a new `Retired:` annotation) at moderate confidence, and explicitly declines option 2 (`_i_` → `_s_`) because it would put a rename in the curator's hands on the weakest tier. Any annotation or rename I proposed here would pre-empt it.

**The 13, each with the step that deleted its subject.** All are inside `$SCAN_DECISIONS`; the record above estimates roughly eleven more in closed Circles that no run with this Circle in scope can reach.

| Record | Marker | Subject deleted by |
|---|---|---|
| `shared/decisions/260716-1847_i_offline-verhalten-bei-plane-ausfall.md` | `_i_` | steps 2–3, the Plane mirror |
| `shared/decisions/260716-1847_i_plane-rolle-source-of-truth.md` | `_i_` | steps 2–3 |
| `shared/decisions/260719-2141_i_plane-rolle-push-only-vs-bounded-readback-martin.md` | `_i_` | steps 2–3 |
| `shared/decisions/260722-2230_i_thin-mirror-vs-comment-borne-full-spec.md` | `_i_` | steps 2–3 |
| `shared/decisions/260810-0718_d_should-rebuild-map-merge-with-the-existing-map-or-replace-it.md` | `_d_` | steps 2–3 (`.plane-map.json`) |
| `shared/decisions/260809-2004_i_should-the-latching-churn-and-cross-file-criticals-be-bounded-or-dropped.md` | `_i_` | steps 4–5, churn |
| `shared/decisions/260810-0920_i_what-should-a-churn-key-be-anchored-to-and-what-happens-to-the-535-entries-already-recorded.md` | `_i_` | steps 4–5 |
| `shared/decisions/260806-1152_a_stash-manifest-dirname-and-pointer-content-duplicate.md` | `_a_` | step 6, the stash pair |
| `shared/decisions/260810-2032_a_should-the-drift-checks-four-sentences-be-pinned-to-an-approved-baseline-instead-of-screened-by-a-blacklist.md` | `_a_` | step 11, the drift machinery |
| `shared/decisions/260811-1146_i_does-the-measurement-family-get-a-shared-chassis-before-the-fourth-module.md` | `_i_` | steps 4 and 11 |
| `shared/decisions/260810-1822_i_should-the-queue-ground-procedure-become-a-rule-file-when-one-of-its-three-consumers-cannot-be-emitted-to.md` | `_i_` | step 10, the persisted queue |
| `shared/decisions/260810-1544_a_should-prompt-called-bin-helpers-get-one-guarded-call-convention-and-does-the-work-tree-preference-extend-to-them.md` | `_a_` | step 4 (its worked example, `bin/fusion-churn-rank`) |
| `shared/decisions/260801-1020_i_where-does-normative-consistency-live.md` | `_i_` | step 12 (an option's citation only — **the record's answer, the curator, is alive and correct**; listed for completeness, not as a retirement) |

**Two of the thirteen are a shape the open record does not cover, and that is worth saying at the gate.** `260806-1152` and `260810-2032` carry `_a_`, not `_i_`. An `_a_` record is Grounding-**Stand** by this project's own definition — *"the current best-of-knowledge the project is working with"* — and both are awaiting a realisation that can now never occur. The open record's question is scoped to `_i_`. If option 3 is adopted, whoever writes it should decide whether a `Retired:` annotation reaches `_a_` as well, or those two stay in the live-Grounding filter permanently.

**Recommendation, `inference:`, moderate confidence.** Answer `260814-1332` before the next curator run. It was filed on a corpus of two affected records in the editable surface; today's Circle took that to thirteen in one day, which is a sixfold move in the fact the record was filed about, and every future removal Circle will add more. No pass can repair the corpus while the vocabulary question is open, and the cost of the wait is that a reader filtering on `_i_` cannot distinguish a decision whose implementation ships today from one deleted this morning.

---

## 7. Work this run may not do, and who owns it

Each is reported rather than performed. **Nothing here is on the ledger.**

| Requirement | Owner | Record |
|---|---|---|
| Seven of the eight shipped surfaces presenting a demoted skill name as a user command: `agents/orchestrator.md:1289`, `agents/curator.md:3`/`:57`/`:344`, `agents/playmaker.md:61`, `skills/cadence/SKILL.md:255`, `docs/philosophy.md:15`, `README.md:150`. The eighth is `rules/fusion-workbench-conventions.md:47,79` and is L10/L11 above | `coder` | existing: `issues/260815-1633_o_eight-shipped-surfaces-still-present-the-three-demoted-skill-names-as-user-commands.md` |
| `agents/orchestrator.md:1289` in particular — it contradicts `README-agents.md:246` outright and is more than presentational; the Turn 4 coderev asks for it to be fixed directly and separately | `coder` | same record; flagged there as the one exception to the sweep |
| `README-agents.md:79`'s closing paragraph contradicting its own table row at `:66` on the executor derivation — the twin of ledger entry L05 | `coder` | **filed this run:** `issues/260815-1706_o_readme-agents-closing-paragraph-contradicts-its-own-table-row-about-the-executor-derivation.md` |
| `skills/curate/SKILL.md:2` giving no selector while its two siblings give theirs — the twin of ledger entry L08 | `coder` | existing: `issues/260815-1633_o_the-claude-md-steps-only-selector-is-claude-md-and-the-documents-that-say-reachable-alone-never-spell-it.md` |
| Regenerating `hooks/lib/__tests__/fixtures/rules-emission.golden` after any of L10, L11, L12 lands. **This is the invalidated-fixture case:** the edit is inside my remit and approved, only its consequence is not. Command: `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, which rewrites and then deliberately fails, forcing a second clean run | `coder` | existing: `circles/260801-1244-curator/issues/260814-1023_c_an-approved-rule-file-edit-in-this-repo-leaves-the-golden-fixture-stale-and-the-curator-cannot-say-so.md` |
| The two lint checks the `templates/` and `docs/` rows want, so a third inventory row cannot go stale invisibly. L01 and L02 fix the rows; they do not build the gate | `coder` | existing: `issues/260815-0803_o_two-claude-md-inventory-rows-went-stale-and-neither-lint-gate-can-see-them.md` |
| The four cardinal words counting deleted items in `README-agents.md:185`, `skills/help/SKILL.md:103`, `bin/fusion-rules:408` and `:437`. **Checked in my own surfaces and found clean:** every spelled-out count in `rules/*.md` and `CLAUDE.md` was enumerated and verified, including `commit-lock.md`'s "three agents" and "two skills", `circle-records.md`'s "three agents", `CLAUDE.md:62`'s "sixteen agents" (historically correct for 260812) and `CLAUDE.md:134`'s "two rule files"/"a fifth site" (verified against `DEFINITION_SITES`, which holds exactly four entries, two of them rule files) | `coder` | existing: `issues/260815-1501_o_four-cardinal-words-still-count-items-the-removals-deleted-and-no-gate-reads-a-cardinal-word.md` |

**No derivation on this ledger needs new code.** L01's derivation reuses `ls templates/`, a command the row already carried.

**Nothing here is a refusal of the dispatch.** The dispatch routed `skills/cadence/SKILL.md:255`, `README.md:150` and `docs/philosophy.md:15` to this pass. A skill body, a `README*.md` and a file under `docs/` are exclusions 5 and 6 of `agents/curator.md` `## Remit`, and the owner is `coder` in all three cases. They are already covered by the existing record above, so no duplicate was filed. The rule-file member of that same sweep is on the ledger, which is the half of the routing this agent can execute.

---

## 8. What this run did not read, or read only in part

- **The archive store is empty** — zero files, reported explicitly rather than skipped.
- **The Circle's `analyses/` is empty** — zero files.
- **`shared/history/260814-2306-orchestrator-session.md` has no Turn 4 section.** Turn 4's account was taken from the Circle record's Turn log, the four per-step coder history entries, the two Turn 4 review files and today's slice of the event log, and every Turn 4 finding below cites one of those rather than the session history.
- **Session histories: 231 present in `$SCAN_HISTORY`, 3 read in full.** The rest were enumerated by name and date and searched by grep for the removed mechanisms. A design decision recorded in one of the 228 unread files would not have been found.
- **Decision records: 48 read at head level, 7 in full.** Pair 1's verdict covers the 22 pairs its selector reached, not all 1 128.
- **Reviews and analyses: 46 present, 2 read in substance.** The two are the Turn 3 and Turn 4 coderev files, chosen because they are the ones that route work to this gate.
- **Both lint gates were read rather than run.** `npm test` was not executed by this run; the green-suite claims quoted from `circles/260815-0007-…/history/260815-1617-coder-collapse-administrative-surface.md` (39 files, 739 tests) are that agent's measurement, cited, not mine. Every Tier 1 citation above is a command I ran myself and its output.

---

## 9. Outcomes

**Apply pass, 2026-08-15.** Dispatch parameters: `**Mode:** apply`, this file as `**Ledger:**`, `**Approved:** L01–L12` (all twelve). Ran at git HEAD `5f2171eab43e6a0f0e2f981b291d7b40858e598f` — **the same HEAD the survey read**, verified with `git rev-parse HEAD` and `git log --oneline 5f2171e..HEAD` (empty) before any write. `git diff --stat 5f2171e..HEAD -- CLAUDE.md rules/` was also empty, so neither surface moved between the two passes.

**Every entry's before-text was re-read from disk before it was applied**, per `agents/curator.md` `## Pass 2`. All twelve matched byte for byte and **each matched exactly once** in its file, so no entry was ambiguous and none went stale.

| Id | Surface | Outcome |
|---|---|---|
| L01 | `CLAUDE.md` | applied |
| L02 | `CLAUDE.md` | applied |
| L03 | `CLAUDE.md` | applied |
| L04 | `CLAUDE.md` | applied |
| L05 | `CLAUDE.md` | applied (the curator half only; the `README-agents.md:79` twin is `coder`'s, filed as `issues/260815-1706_o_readme-agents-closing-paragraph-contradicts-its-own-table-row-about-the-executor-derivation.md`) |
| L06 | `CLAUDE.md` | applied |
| L07 | `CLAUDE.md` | applied |
| L08 | `CLAUDE.md` | applied |
| L09 | `CLAUDE.md` | applied |
| L10 | `rules/fusion-workbench-conventions.md` | applied |
| L11 | `rules/fusion-workbench-conventions.md` | applied |
| L12 | `rules/fusion-workbench-conventions.md` | applied |
| C01 | decision records | candidate — not offered, not applied. All thirteen records named in §6 are byte-identical at HEAD; `git status` over both decision stores is empty |

**Measured effect, by surface.**

| Surface | Bytes before | Bytes after | Delta | Lines before | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 54 441 | 55 963 | **+1 522** | 134 | 134 |
| `rules/` (12 files) | 145 656 | 146 677 | **+1 021** | 1 738 | 1 740 |
| Decision records (48 files) | 360 939 | 360 939 | **0** | 4 760 | 4 760 |

Net **+2 543 bytes**. The ledger's prediction held in direction and in scale: the run is a net addition, no section or paragraph was deleted, and the only removals are the clauses named per entry.

**Removed constraints: none.** Restated here as the searchable line the wrong-prune mitigation asks for: **no entry on this ledger removed a constraint, and every `Constraint removed:` field above reads `none`.** The text deleted across the twelve entries is inventory, a retired parameter value, a mechanism assertion this Circle inverted, and four name tokens.

**The armed growth bound, re-measured after the writes.** The five core files now sum to **87 918** bytes (`wc -c` over `agent-setup.md`, `fusion-workbench-conventions.md`, `decision-record-examples.md`, `user-facing-output.md`, `critical-stance.md`), against `RULE_BASELINE`'s core of 86 573. The corpus stands **1 345 bytes above baseline** with **10 655 of the 12 000-byte `GROWTH_BUDGET` unspent**. The pre-apply measurement was 86 897, which reproduces §2's figure exactly. §2 predicted about +480 from L12 alone; the three rule-file entries together added **1 021** (L12 alone is **929** bytes including its paragraph break, L10 +10, L11 +82), so the estimate was low by roughly a factor of two and the conclusion is unchanged — the hard gate blocks at 98 573 and the corpus is 10 655 below it. `RELEASE_CAP` and `DRIFT_CEILING` were not read.

**The golden fixture is now stale, and regenerating it is `coder` work.** L10, L11 and L12 all changed the byte size of `rules/fusion-workbench-conventions.md`, which `hooks/lib/__tests__/fixtures/rules-emission.golden` pins, so `hooks/lib/__tests__/rules-emission-golden.test.ts` fails until it is regenerated: `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, which rewrites the fixture and then deliberately fails, forcing a second clean run. **This run did not execute it and did not run `npm test`.** Record: `circles/260801-1244-curator/issues/260814-1023_c_an-approved-rule-file-edit-in-this-repo-leaves-the-golden-fixture-stale-and-the-curator-cannot-say-so.md`.

**Revert path for the whole apply pass:** `git checkout -- CLAUDE.md rules/fusion-workbench-conventions.md`. Both files are git-tracked; nothing was committed by this run.

**Nothing in §7 was performed.** The seven out-of-remit requirements stand with their owners and their records, unchanged by this pass.
