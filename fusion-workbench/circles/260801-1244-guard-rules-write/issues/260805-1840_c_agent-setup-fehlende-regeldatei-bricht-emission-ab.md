agent-setup.md verspricht "missing files are skipped silently" — bin/fusion-rules bricht bei fehlender Always-on-Datei mitten in der Emission ab
---
Schweregrad: hoch. rules/agent-setup.md:26-27 ("Missing files are skipped silently — read what is present") und der Header von bin/fusion-rules:12-13 versprechen Überspringen. Tatsächlich: `emit_if_exists()` (bin/fusion-rules:215-217) gibt unter `set -eu` (Zeile 131) bei fehlender Datei Status 1 zurück und beendet das gesamte Skript — Exit 1, keine stderr-Meldung, Teilausgabe bereits emittiert. Ausgeführt belegt: `FUSION_PLUGIN_ROOT=~/.fusion bin/fusion-rules orchestrator` (installierte Kopie ohne die neuen Shards) endet nach den Voice-Profilen mit rc=1; circle-records.md und workbench-stash-and-lock.md werden nie emittiert.
---
Betroffen ist jeder Konsument mit Versions-Skew zwischen Plugin-Root und Regelbestand. Vermutlich ist der Code der Fehler (das Versprechen ist das gewollte Verhalten für projektlokale Dateien), aber heute ist der ausgelieferte Satz falsch. Der Skip funktioniert nur, weil `emit_if_exists` als letztes Kommando einer Funktion in Bedingungskontexten läuft — für die Always-on-Sektion tut es das nicht. Klasse 1, verifiziert (Ausführung durch Prüf-Analyst). Reichweite: agent-setup.md wird als erste Regeldatei in jeden Agenten geladen. Analyse: circles/260801-1244-guard-rules-write/analyses/260805-1840-doku-gesamtpruefung-gegen-code.md
---
Resolved: 2026-08-06 — `emit_if_exists` kehrt bei fehlender Datei mit `return 0` zurück (`bin/fusion-rules:247`); Regressionstests gegen eine gestrippte Plugin-Kopie in `context-manifest.test.ts`. Commit 7ef2715, Circle 260805-2005-textschicht-gegen-code-nachziehen (Plan-Schritt 5).
