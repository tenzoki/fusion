README-hooks: die Advisory-only-Zeile behauptet weiter "fail-closed rule included", obwohl der eben ergänzte Floor genau diesen Pass am Leben hält
---
Schweregrad: hoch. README-hooks.md:137 (Tuning-Tabelle, Zeile "Advisory-only (warns, never blocks)") wurde in Commit 373f5ed umgeschrieben und sagt jetzt: "An empty list also stands the shell protected-path check down, fail-closed rule included — only the `fusion-guard.json` self-protection floor survives, where that file exists." Der zweite Halbsatz widerlegt den ersten: wo die Datei existiert — und sie muss existieren, um die leere Liste überhaupt zu deklarieren; `/fusion:setup` seedet sie in jedem Projekt — hält der Floor die effektive Liste bei zwei Einträgen, und der Fail-closed-Pass ist an genau diese Liste gebunden (`hooks/lib/bash-mutation-guard.ts:3112`, `if (opts.protectedPaths.length > 0)` über die effektive Liste aus `hooks/guard.ts:403`). Gemessen gegen den kompilierten Klassifizierer mit Floor-only-Liste: `mv $A $B` und `sed -i "s/$A/$B/" notes.txt` verweigern fail-closed; mit tatsächlich leerer Liste erlauben beide. "Warns, never blocks" ist damit für jedes Projekt mit geseedeter `fusion-guard.json` falsch — und die beobachtete Konsumenten-Bilanz (17 von 17 Blocks waren Fail-closed-Fälle, Befund 260805-1830) zeigt, dass genau diese Klasse die Nutzer trifft.
---
Ursache: Der Step-7-Durchgang (373f5ed) hat den Floor-Vorbehalt an die Zeile angehängt, den mitgeschleppten Halbsatz "fail-closed rule included" aber stehen lassen. Schwester-Befund: `issues/260805-1840_o_ppd-leere-liste-steht-den-check-nicht-ab.md` (offen, Circle 260805-2005-textschicht) nennt denselben Satz in `rules/protected-path-discipline.md:41` — der Step-7-Durchgang hat diesen Absatz ebenfalls angefasst (Leaf-Merge, `guard.enabled`, Floor ergänzt) und den bekannten falschen Halbsatz "it stands this whole check down, fail-closed included" auch dort behalten. Fix-Richtung für beide Stellen: entweder den Halbsatz auf die Wahrheit kürzen ("steht die Protected-Path-Treffer ab; der Fail-closed-Pass bleibt aktiv, solange die Datei existiert, weil der Floor die Liste nie leer werden lässt") oder — als Code-Frage an den Human Gate — den Fail-closed-Pass an die deklarierten statt an die effektiven Einträge binden, was ein Newly-Allow wäre und darum nicht als Patch gehört.
---
Resolved: README-hooks.md:138 rewritten — the Advisory-only row (label and body) now states that an empty `protectedPaths` list narrows the shell check to the `fusion-guard.json` self-protection floor (both spellings, `hooks/lib/config.ts:687-692`) instead of standing it down, and that the fail-closed pass stays live against those two entries (`mv $A $B` still denies). Verified: no surviving README sentence claims the empty list disables fail-closed. Sibling `rules/protected-path-discipline.md:41` deliberately untouched — Textschicht issue 260805-1840.
