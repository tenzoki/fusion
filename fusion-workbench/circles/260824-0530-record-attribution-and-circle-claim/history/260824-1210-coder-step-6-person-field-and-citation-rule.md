# Step 6: the person on the record templates, and the citation rule

**Status:** Complete
**Agent:** coder
**Circle:** `circles/260824-0530-record-attribution-and-circle-claim`
**Plan:** `circles/260824-0530-record-attribution-and-circle-claim/planning/260824-0613_*_c3-attribution-on-records-and-a-claim-on-the-circle.md`, step 6

## What was verified before writing

The step text carries three things already settled at commit `3ba7a46` (step 4), and all three were
checked in the tree rather than assumed. `.checkout-id` is present in the layout tree of
`rules/fusion-workbench-conventions.md` (line 55, with its explanatory paragraph at line 85), in
class L of `rules/workbench-tracking.md` (line 24, with the load-bearing reason at line 26), and in
`.gitignore` (line 85). Nothing was added a second time.

The step's stamp figure of 84 is `260807-0158`'s 2026-08-07 measurement over 579 files. The rule
text carries step 2's re-measurement instead: 111 multiply-occupied stamps of 545 distinct, over 876
record files, and zero files sharing a full basename once the marker is normalised. That last figure
is why the rule text says the naming convention holds and only the citation form was at fault.

## What was written

`rules/fusion-workbench-conventions.md`, four edits:

- `## Issue and Decision Filing — MANDATORY` gains `### Who filed it`, the single definition of the
  person: the `PERSON=` line of `bin/fusion-identity`, in git's `Name <email>` form, composed
  nowhere and substituted never. It carries **both** halt states, because the open decision was
  answered after the step was written. Exit 1, a git work tree with the identity unset, halts and
  files nothing. Exit 4, not a git work tree at all, files normally with the person half absent
  rather than empty. Binding record:
  `circles/260824-0530-record-attribution-and-circle-claim/decisions/260824-0613_*_does-a-filing-agent-halt-in-a-tree-that-is-not-a-git-work-tree-at-all.md`,
  option 2.
- The same subsection states the precondition the identity answer names as its mitigation and says
  no code checks: one git identity per person across every machine. It sits where a reader of the
  person field meets it, because that is where the false positive it prevents is legible.
- `## Decision Record Template`: `**Filed by:** <agent name or "user">, <person>`.
- The issue file format gains a `**Filed by:**` line in the same form, in the context region where
  the corpus already puts `**Found by:**`. That field did not exist for issues before this step.
- `## Filename Patterns` gains the citation rule, stated normatively: cite by full filename with the
  marker wildcarded, a bare stamp is not a citation, no pattern above changes.

`rules/decision-record-examples.md`: the one worked `**Filed by:**` takes the new form, so the
corpus's only worked example agrees with the template it illustrates.

## Two gates re-approved, neither a baseline

`hooks/lib/__tests__/fixtures/rules-emission.golden` and `fixtures/surface-growth.golden` were
regenerated by their own documented commands; regeneration records movement and moves no baseline.
`reference-resolution-lint.test.ts`'s BASELINE went `paths: 1303 -> 1304`, the one new token being
`bin/fusion-identity`. Its re-approval note is one line and not five: the five-line version put the
hook-test surface 3 lines over its bound, and the note was cut rather than the bound moved.

## Measurements

| Surface | Before | After | Bound |
|---|---|---|---|
| always-on five | 95 789 | **97 392** | 97 652 (step 6 cap); 260 bytes of the allowance unspent |
| hook tests | 20 373 | 20 374 | 20 375; **1 line left** |

The second row is worth a reader's attention beyond this step. The hook-test surface now stands one
line below its budget, so the next test-side edit of any size fails the suite. It was already at two
when this step began.

## Verification

`cd hooks && npm test` — exit 0, 42 files, 732 tests.
