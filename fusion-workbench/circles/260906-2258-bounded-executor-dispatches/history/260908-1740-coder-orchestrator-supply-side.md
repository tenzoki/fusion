# Coder — Step 9: the orchestrator's supply side

**Status:** Complete
**Agent:** coder
**Date:** 2026-09-08
**Source:** `260907-1450_*_plan-bounded-executor-dispatches.md` `### 9`

## What was done

Two edits to `agents/orchestrator.md`, both inside the step's own scope.

**Setup Step 2, the Turn-budget bullet.** The sentence that read "It prints one line,
`max_turns=<n>`" now names both lines the helper prints. One paragraph was added at the
end of the same bullet, after the check-in-interval note: it holds the resolved
`dispatch_minutes=<n>` as `<dispatch-minutes>` for the session and states that the three
failure branches already written for the Turn budget carry over unchanged to a different
consequence. Unresolved, no stopping time reaches any dispatch prompt and every dispatch
runs to its natural end, which is today's behaviour, so the branch needs no check-in, no
dashboard change and no substitute: the orchestrator says it once in the Setup-complete
summary and proceeds. No second code block and no second guarded call were added.

**A new `### Bounded dispatches` block, immediately before `### Step 3a`.** It names the
seven agents that carry a stopping time and states that no other dispatch does and that
the orchestrator is the only supplier, with the reason (nothing in a skill body or a plain
session computes one, and a bound nothing continues truncates work). It carries the one
node line that computes the value, the reason it is node and not `date`, the
`**Stop by:** <that value>` placement, and the instruction to compute it fresh at every
dispatch including continuations. It closes on recognising a bounded return, read before
the site's own handling of an incomplete run at every site, and sends the reader to
`rules/bounded-dispatch.md` `## For the orchestrator: continuing a bounded return` in the
shape the Rebalance gate already uses, with the absent-file fallback.

The five continuation sites, the return's four statements and any number of minutes are
deliberately absent; Step 3a, Step 3b, Phase 3, Phase 4 and the error-handling table were
not touched.

## Measurements

| Item | Before | After | Delta |
|---|---|---|---|
| `agents/orchestrator.md` | 151 756 | 153 349 | +1 593 (budget 1 600) |
| `agents/*.md` total | 413 599 | 415 192 | +1 593 |

`agents/` head-room after this step: **2 651 bytes** (415 192 - 399 843 = 15 349 spent
against 18 000). Step 10 is budgeted 2 190 of it, so it fits with 461 to spare.

The first draft came in at 1 745 and was cut to 1 593 by tightening wording only; no
mandated content was dropped.

## Fixtures and pins regenerated

- `hooks/lib/__tests__/fixtures/surface-growth.golden` — two numbers,
  `orchestrator.md 151756 -> 153349` and `total 413599 -> 415192`. Regenerated by its own
  failing-on-purpose run (`UPDATE_SURFACE_GOLDEN=1`), diff read and confirmed.
- `hooks/lib/__tests__/reference-resolution-lint.test.ts` `BASELINE` re-approved
  1723/244/14 -> 1724/245/14, rewritten in place with no line added (the file stands at
  1 005 lines, as before). The +1/+1 is one sentence: the on-demand pointer names the
  bounded-dispatch rule file and puts that file's continuing-a-bounded-return heading in
  backticks immediately beside it. Attributed by single-file revert against the full tree:
  with the orchestrator prompt alone restored to HEAD and every other file left as the
  working tree holds it, the gate reads 1723/244/14 and passes.
- `hooks/lib/__tests__/fixtures/rules-emission.golden` — untouched, because no rule file
  changed.

None of the four named baselines moved: `git diff` over `surface-growth-bound.test.ts`,
`rules-emission-golden.test.ts` and `helpers/growth-bound.ts` is empty, so
`AGENT_BASELINE`, `RULE_BASELINE`, `RELEASE_CAP` and `DRIFT_CEILING` are byte-identical.

## Verification

- `wc -c agents/orchestrator.md`: 151 756 before, 153 349 after. Delta 1 593, at or under
  the 1 600 cap.
- `grep -n "Stop by:" agents/orchestrator.md`: one line, 460, exit 0.
- `grep -cE '\b20 minutes\b' agents/orchestrator.md`: 0.
- `cd hooks && npm test`: run four times after the golden and the pin were brought up to
  date. Two runs were entirely green (53 files, 924/924). Two exited 1, each failing only
  files from the known intermittent set of `260908-1719_*_three-harness-spawning-tests-fail-intermittently-so-the-suite-cannot-answer-the-circles-green-clause.md`:
  once `fusion-commit-lock` and `staging-drift` together, once nothing else. Each passes
  in isolation (`fusion-commit-lock` alone exit 0, `staging-drift` alone exit 0). No file
  outside that set ever failed. `surface-growth-bound.test.ts` and
  `reference-resolution-lint.test.ts` are green in every run.

## Not done

Nothing in the dispatch was left undone. The dispatch named `agents/orchestrator.md` as
the only file; the golden and the pin were changed because the dispatch itself directed
that, and the plan step's `[IN PROGRESS]` marker was moved to `[DONE]` under the standing
executor bookkeeping contract.
