# Coder — Step 13: tests for the reading and for the bound-agent set

**Status:** Complete
**Agent:** coder
**Date:** 2026-09-08
**Checkout:** 5e8248d7
**Filed by:** Kai Stalmann <ks@qantr.com>
**Source:** `260907-1450_*_plan-bounded-executor-dispatches.md` `### 13`
**Dispatch bound:** `**Stop by:** 2026-09-08T16:39Z` — the first bounded dispatch this
project made. Not reached: the work finished at 16:26Z. No bounded return was written.

## Files

- `hooks/lib/__tests__/fusion-events.test.ts` — extended, +153 lines
- `hooks/lib/__tests__/bound-agent-set.test.ts` — new, 96 lines
- `hooks/lib/__tests__/fixtures/surface-growth.golden` — regenerated, +249 lines in two
  rows and the total

No implementation file was touched. `hooks/lib/events-query.ts`, `hooks/events-query.ts`
and `bin/fusion-events` are byte-identical to what Step 12 committed.

## The three groups

**Unit cases for `measureDispatchDurations`**, twelve of them, all fixture strings and no
workbench on disk. One per branch the plan lists — inside the threshold, over it, unpaired,
unattributable, before the cutoff, an agent outside the set, a malformed line, an unzoned
`ts` — plus four the code has and the plan's list does not: a dispatch carrying no
`session_id` at all, an unreadable stamp on either row reaching `unstamped`, the scoping of
`unpaired` by the two filters, and a partition case asserting `counted = within + longer`
and that the four outcomes tile the rows.

**The set pairing**, its own file rather than a section, following
`review-coverage-mandate.test.ts`. The seven names are derived from `bin/fusion-rules` with
the same `/^\s*([a-z|]+)\)\s*IS_([A-Z_]+)_AGENT=1/gm` that `derivable-enumerations-lint`
reads every case arm with, so a reshape of the script fails both gates instead of silently
emptying one. Three assertions: the arm is still parseable (anti-vacuity — the gate cannot
pass by finding nothing), exact set equality both directions, and every bound agent having
a unit row in `rules/bounded-dispatch.md`. The failure message names which side to change
per direction and why the two copies exist: shell decides who is told, a pure function
decides who is read, neither can query the other.

**The `limit=` lines**, one case through the entry point by subprocess: all three keys on
stdout, none of them on stderr, exactly three `limit=` lines.

## Where the implementation differs from the plan's prose

Each was already recorded in `260908-1811-coder-c4-reading.md`; this step tested the code
and not the prose, and says so at the assertion.

1. **Four outcomes, not two.** The plan spells the fifth field `<longer|within>`. The code
   also carries `unattributable` and `unpaired`. Tested as four, with the note at the
   unpaired case.
2. **The cutoff and agent filters scope `unpaired`.** The plan lists that finding ahead of
   the two filters. Its own case proves the scoping: a fixture with three unpaired starts
   reports `unpaired=1`.
3. **`unstamped` is returned and its sentence goes to stderr.** Covered as a returned count;
   the stderr sentence is not asserted, only the three `limit=` lines are.

One correction inside this step: the unzoned-`ts` case first asserted a 30-minute pair
against a 20-minute threshold and expected `within`. The code was right and the expectation
wrong; the fixture is 15 minutes now. The assertion is a mixed-zone pair — unzoned start,
`Z`-stamped completion — so a local-time reading of the first shifts the duration by the
runner's offset. A both-unzoned fixture would have proven nothing, because `parseTs` shifts
both stamps and the cutoff equally.

## Verification

1. `cd hooks && npm test -- fusion-events` — exit 0, 49 passed.
2. `cd hooks && npm test` — exit 1 on the first run, on `surface-growth-bound.test.ts`
   alone, over the golden fixture the dispatch anticipated. Regenerated with
   `UPDATE_SURFACE_GOLDEN=1 npm test -- surface-growth` (which fails by design so a
   regeneration run is never green), diff read and confirmed as exactly the two files this
   step changed. Re-run: **exit 0, 54 files, 940 tests, none of the three intermittent
   harness-spawning tests failing.**
3. `wc -l hooks/lib/__tests__/*.test.ts | tail -1` — **21 075**. The golden's own
   `hook-tests lines` total is 22 287, over a wider set than that glob; the two are not the
   same measurement and neither is wrong.
4. The pairing gate proven able to fail: `analyst` added as an eighth name in
   `hooks/lib/events-query.scratch.ts`, a copy of the gate importing it, run alone — exit
   1, two failures, the message naming `onlyInConstant: ["analyst"]` and both remedies.
   Both scratch files and their two compiled artifacts deleted; `git status` clean of them.
   The live `events-query.ts` was never edited.

No baseline moved: `git diff` over `surface-growth-bound.test.ts` and
`helpers/growth-bound.ts` is empty, so `AGENT_BASELINE`, `RULE_BASELINE`, `SKILL_BASELINE`,
`TEST_LINE_BASELINE`, `RELEASE_CAP` and `DRIFT_CEILING` are untouched.

## One finding, not a defect of this step

`"$FUSION_PLUGIN_ROOT/bin/fusion-rules" coder` did **not** emit
`rules/bounded-dispatch.md`: the installed copy of the script predates the `IS_BOUND_AGENT`
arm (`grep -c IS_BOUND_AGENT` on it is 0). The work-tree copy `./bin/fusion-rules coder`
emits it. This is the standing one-release-behind cost of a `bin/` helper, recorded at
`260825-1329_*_every-session-runs-one-release-behind-on-a-bin-helper-the-same-repository-just-added.md`,
and it means **no dispatch of this session reaches an executor with the rule attached
through Setup**. The rule was read from the work tree instead, which is what the dispatch
prompt named it by. Nothing to fix here; worth knowing before the Circle's proof clause is
answered off a Setup emission.
