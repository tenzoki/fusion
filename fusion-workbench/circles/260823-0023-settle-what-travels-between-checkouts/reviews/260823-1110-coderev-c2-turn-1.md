# Code review — C2 Turn 1: what travels between checkouts is settled

**Reviewed-range:** `3ee8eaf..2f1e3a6`
**Not-opened:** none

**Date:** 2026-08-23
**Sender:** coderev
**Circle:** `260823-0023-settle-what-travels-between-checkouts`
**Plan:** `circles/260823-0023-settle-what-travels-between-checkouts/planning/260823-0800_*_c2-what-travels-between-checkouts-is-settled.md`, steps 1 to 7 plus the T2b scope widening
**Carried from an earlier pass:** nothing. `bin/fusion-review-coverage` reported `carried=(not recorded)`; no earlier review declared a `**Not-opened:**` list.

---

## Summary

Steps 1 to 7 do what the plan asks and the mechanisms hold under measurement: the merge driver's branch set is disjoint and complete over the real value space of `git check-attr merge`, the append is idempotent across three runs and preserves a neighbouring rule in a file with no final newline, the conditional marker write leaves a legacy marker alone exactly as `## Data Structures` prescribes, the `.active-circle` writer enumeration was updated in the same commit with the right condition and the right count, and no growth-bound baseline moved anywhere in the range.

Two findings are release blockers. `npm test` is red in every fresh clone of this repository, because step 2 untracked and ignored a file that a blocking gate asserts by name; the suite is green here only because the working-tree copy survived `git rm --cached`. And the conditional marker write lost the `$FUSION_PLUGIN_ROOT` guard every sibling block in the same skill carries, so an unresolved plugin root now silently destroys the recorded `plugin_version` rather than skipping.

Eleven defects are filed, all in the Circle's own store. One `Also seen:` line was appended to an existing shared record rather than filing a duplicate.

## Totals

| Severity | Count |
|---|---|
| Critical | 1 |
| High | 1 |
| Medium | 5 |
| Low | 4 |

## Findings by theme

### Theme 1 — a tracked file left the index and a gate still names it

**Critical. `npm test` fails in every fresh clone.** `00ce4f0` untracked and ignored `fusion-workbench/portfolio.md`; `hooks/lib/__tests__/workbench-citation-lint.test.ts:263` asserts `rels.has("portfolio.md")` against a corpus built from disk. `describe.runIf(WORKBENCH_PRESENT)` does not skip, because `WORKBENCH_PRESENT` reads `.fusion-setup`, which is class R3 and still tracked. Measured by moving the file aside: the gate fails with `portfolio.md is in the corpus by name: expected false to be true`. This blocks step 9 of this Circle's own plan, which builds two clones. `README-hooks.md:369` describes the corpus the same way.
Record: `260823-1110_*_the-untracked-portfolio-turns-npm-test-red-in-every-fresh-clone-of-this-repository.md`

### Theme 2 — a guard that every sibling carries and this block does not

**High. The conditional marker write destroys `plugin_version` when the plugin root does not resolve.** `skills/setup/SKILL.md:92-94`. `$V` now decides whether anything is written; an empty `$V` fails the `grep -qF` and rewrites the marker as `{"setup_at":"…","plugin_version":""}`. Measured in a scratch tree with `FUSION_PLUGIN_ROOT` unset. The same skill at `:54` names that exact loss as the second reason the pre-v4 check stops Setup before this point. Steps 0b, 0d, 0e and 0f each guard the variable; this block does not. The second run is quiet, so the loss is silent and permanent.
Record: `260823-1110_*_the-conditional-marker-write-has-no-plugin-root-guard-so-an-empty-version-wipes-the-record.md`

### Theme 3 — case splits that name fewer cases than exist

**Medium. Step 0i collapses `MULTIPLE-ACTIVE` into `head -1`.** `skills/setup/SKILL.md:346-352`. The branch table reads "A path printed", presuming one; `find … | head -1` picks arbitrarily among several and says nothing about the rest. Two active records is a named condition in this project's vocabulary, rendered by `/fusion:next`, and more likely under exactly the multi-checkout arrangement C2 builds.
Record: `260823-1110_*_step-0i-collapses-multiple-active-to-head-1-and-names-one-circle-arbitrarily.md`

**Medium. `.guard-state/events.jsonl` falls in no class of a partition claiming every entry falls in exactly one.** `rules/workbench-tracking.md:17` states the tiling; `:24` excludes the log from class L; `:30` calls it "a **record**", the retired vocabulary; `:32` says it is split across two classes. Every other layout entry tiles correctly, verified one by one. `.gitignore:70` carries the same retired word.
Record: `260823-1110_*_the-guard-event-log-falls-in-no-class-of-a-partition-that-claims-every-entry-falls-in-one.md`

**Low. Step 0h's catch-all misnames two attribute values as drivers.** `skills/setup/SKILL.md:325`. `unset` (written `-merge`) means merging is switched off, and `set` (a bare `merge`) means git's default text merge, which is the behaviour Step 0h exists to prevent. Both are reported as "already has merge driver '<value>'", and `rules/workbench-tracking.md:51` enumerates only the first. Behaviour is correct in both branches; the report and the enumeration are not.
Record: `260823-1110_*_step-0h-reports-unset-and-set-as-merge-driver-names-and-the-rule-enumerates-neither.md`

### Theme 4 — the cut moved text that other files address by line

**Medium. Two line citations in an always-on rule were shifted and one lands on a blank line.** `rules/fusion-workbench-conventions.md:66` cites `skills/setup/SKILL.md:67` and `:60`. Both were exact at `3ee8eaf`; `57eaf85` removed two paragraphs above them, so `:67` is now blank and `:60` is the neighbouring paragraph. The lint cannot see it: `scanPluginPaths` ends at `existsSync`, never resolving the `:N`. The same commit also removed the sentence that supported the first half of that conventions clause. `1400402` shifted five `skills/next/SKILL.md` citations in `README-agents.md` two lines further; those five were already stale at range start and are named in the record rather than filed twice.
Record: `260823-1110_*_the-cut-shifted-two-line-citations-in-an-always-on-rule-and-one-now-points-at-a-blank-line.md`; `Also seen:` appended to `shared/issues/260818-1637_*_no-gate-resolves-a-path-line-citation-and-thirteen-drifted-in-a-single-change.md`

**On the eleven passages themselves: the criterion held.** Each of the eleven justifications was read against the diff and each is accurate. The two most checkable are correct as stated: passage 7's "repetition of the conventions file" is verbatim the claim at `rules/fusion-workbench-conventions.md:84`, and passage 8's "repetition of the file's own header" is carried by the `**What the root does *not* cover.**` paragraph at the top of the same skill, exception, reason and part (c) caveat included. Passage 11's operative consequence survives at `skills/setup/SKILL.md`'s Step 0g JSON block as "**Bare tool names only** — no scoped path form may be written here under any wording". Passage 6's claim is structural and true: Step 0e's loop iterates the four stilwerk profiles and cannot reach `monitor`. Passage 4 is the weakest of the eleven, cutting each probe's justification while keeping "All three probes are now bounded by construction, and none may be given a path exception"; the instruction survives, the reasoning that would stop a later widening does not. That is within the stated criterion and is noted rather than filed.

### Theme 5 — the driver's cost reaches a second reader

**Medium. Only one of two positional event-log readers in `agents/orchestrator.md` was repaired.** `:876` and `:1332` now sort by `ts`. `:519` and `:1083` define the Turn count as `turn_start` since **this session's** `session_start`, which is positional and unreliable after a union merge. The consequence lands on the C4 record's own proposed fix, which says to find the last `session_start` and count after it — written on 2026-08-22, before the driver existed. Plan step 7's exclusion is correct as far as it reasons: the `grep -c` at `:91` is order-independent. `bin/monitor` is the third reader and is assigned to step 9.
Record: `260823-1110_*_the-merge-driver-unsorts-a-second-event-log-reader-whose-repair-direction-is-positional.md`

### Theme 6 — the pin's audit trail

**Medium. Two of six `BASELINE` re-approvals carry no accounting.** `hooks/lib/__tests__/reference-resolution-lint.test.ts:488` states the invariant "EVERY MOVE IS STILL ATTRIBUTED HERE, above the constant". `c9eba48` moved it with no block, acknowledged one commit later; `1400402` moved it with no block anywhere, its accounting living only in a workbench history file. At HEAD the log's last entry states `paths 1291, anchors 179` and the constant beneath reads `1292, 180`. The four blocks that do exist were each checked token by token and are accurate.
Record: `260823-1110_*_two-of-six-baseline-re-approvals-carry-no-accounting-and-the-log-now-contradicts-the-constant.md`

### Theme 7 — retired vocabulary and retired examples left standing

**Low. `.gitignore:64-68` still describes the two-group split.** It calls `rules/workbench-tracking.md` the authoring home of a "records / live state" split that `21ae170` replaced one commit earlier, and `00ce4f0` edited the same block without touching the description. The `KEPT:` line beneath it is correct and matches `git ls-files` exactly.
Record: `260823-1110_*_the-gitignore-comment-still-describes-the-two-group-split-its-authoring-home-replaced.md`

**Low. The retired staging-drift example survives at two authoring sites, not one.** `hooks/staging-drift.ts:23` and `bin/fusion-staging-drift:14` both print `record M portfolio.md UNSTAGED`, which `classify` can no longer produce. The closure note on `260823-0800_*_…` names the first and calls it "a third site"; the second is a separate authoring surface, and `CLAUDE.md` makes that script's header its authoritative documentation.
Record: `260823-1110_*_the-retired-staging-drift-example-survives-at-two-authoring-sites-and-the-closure-names-one.md`

**Low. The Done report enumeration omits Step 0i.** `skills/setup/SKILL.md:354` says "Name the branch that ran in the Done report"; the closed enumeration at `:470` lists Steps 0e, 0g and 0h and not 0i. `c9eba48` added Step 0h to that list in the same commit; `25f60eb` did not.
Record: `260823-1110_*_the-done-report-enumeration-omits-step-0i-while-step-0i-instructs-it-to-report.md`

## What was verified and holds

Recorded because a review that names only faults leaves the reader unable to tell a checked claim from an unchecked one.

- **The merge driver's branch set is disjoint and complete.** Run against six scratch repositories: no attributes file (`unspecified`, writes), `-merge` (`unset`, left alone), bare `merge` (`set`, left alone), `*.jsonl merge=union` (`union`, writes nothing), `merge=ours` (left alone, named), and a file with no final newline (writes, and `git check-attr binary` on the pre-existing rule still returns `set` afterwards). Three consecutive runs produced the rule line exactly once with the neighbour intact.
- **Step 0h carries Step 0g's bounds.** Append-only `>>`, no overwrite, no removal, `./.gitattributes` in the directory Setup ran in, outside `fusion-workbench/`, and it asks the user nothing.
- **The `.active-circle` enumeration was updated in the same commit** (`25f60eb` touches `rules/fusion-workbench-conventions.md:82`), the condition it names matches what Step 0i does, and the count is right: three writers on the activation path plus two lifecycle skills, so `/fusion:setup` is the fifth.
- **The legacy marker case matches the plan.** A marker carrying `setup_pwd` at the current version is left untouched, which is what `## Data Structures` prescribes. Measured.
- **No growth-bound baseline moved.** `git diff 3ee8eaf..2f1e3a6` over `helpers/growth-bound.ts`, `surface-growth-bound.test.ts` and `rules-emission-golden.test.ts` returns nothing, so `AGENT_BASELINE`, `SKILL_BASELINE`, `RULE_BASELINE` and `TEST_LINE_BASELINE` all stand.
- **The byte-budget design claim holds.** `bin/fusion-rules` emits `rules/workbench-tracking.md` to no agent (`grep` returns nothing), so its 69 added lines cost no bounded surface. The only always-on movement in `rules-emission.golden` is `fusion-workbench-conventions.md 57114 → 57335`, the `.active-circle` clause.
- **`classify`'s ordering claim is true as documented.** `hooks/lib/staging-drift.ts:404-434` reads `LIVE_STATE` and `LIVE_PREFIXES` first, then `stashes/`, then `ROOT_RECORDS`, then the store test, then the name test — which is the order the `ROOT_RECORDS` comment claims.
- **`hooks/dist/` is the compilation of the committed source**, and the full suite is green in this working tree: 41 files, 724 tests.

## Cross-cutting observations

**One reclassification touched five surfaces and two were found by somebody other than the plan.** `portfolio.md` moving to class L reached `rules/workbench-tracking.md`, `.gitignore`, `hooks/lib/staging-drift.ts`, `agents/orchestrator.md` and the test suite. The plan named the first two; a filed defect caught the next two; and the fifth, the citation gate asserting the file by name, was caught by neither and is the Critical finding above. The pattern to take from it: a file leaving the index has consumers that read the disk, not the index, and those are invisible to a search for the path in tracked configuration.

**Retired vocabulary outlives the rewrite that retired it, by one commit each time.** The two-group split survives in `.gitignore:64`, in `rules/workbench-tracking.md:30`'s own prose about the guard log, and in the `record` word both use. Each was edited in this range by a commit that had the file open.

**Line-numbered citations broke twice in nine commits, in two different files, and no gate can see either.** That is fresh evidence for `260818-1637`, which is why the `Also seen:` line went there rather than a third record being filed about the design.

## Recommended sequencing

**Before any tag or push.** The Critical finding, because the plan's `## Where this Circle stops` requires `npm test` green and step 9 requires two clones. The High finding, because it is a silent destructive write in the step that every project runs first.

**Within Turn 1, before steps 8 and 9.** Theme 3's two Medium findings and Theme 4's citation corrections, all of which touch files this Turn already opened.

**Cleanup, at the user's convenience.** Theme 6's accounting block, Theme 7's three Low findings, and Theme 5's note, which is a message onto C4 rather than work here.

## One fact about this checkout, not a defect

This repository has no `.gitattributes` and `git check-attr merge -- fusion-workbench/orchestrator-events.jsonl` returns `unspecified`. The Circle shipped the driver as a Setup step and Setup has not run since, so the project most likely to be cloned twice does not yet have the rule its own tracked event log needs. The stopping clause naming `.gitattributes` cannot be answered yes until a Setup run happens here.
