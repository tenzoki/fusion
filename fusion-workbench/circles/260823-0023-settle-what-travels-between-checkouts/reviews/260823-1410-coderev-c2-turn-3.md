# Code review: C2 Turn 3, the closing pass

**Reviewed-range:** `b8a4c1a..a2a18f9`
**Not-opened:** none

**Date:** 2026-08-23
**Sender:** coderev
**Circle:** `260823-0023-settle-what-travels-between-checkouts`
**Plan:** `circles/260823-0023-settle-what-travels-between-checkouts/planning/260823-0800_*_c2-what-travels-between-checkouts-is-settled.md`, all nine steps plus the Turn 2 review repairs
**Carried from an earlier pass:** nothing. The Turn 2 review declared `**Not-opened:** none`, and `bin/fusion-review-coverage` reports `carried=none`.

---

## Summary

The two things the dispatch asked me to distrust hold. All fourteen citation repairs resolve to exactly one file each, and Step 0i's widened probe detects both halves of `MULTIPLE-ACTIVE`, exits 0 in every branch its table names, and now says what it does. The plan's eight stopping clauses all hold, six against the tree at HEAD and two against run records rather than tree state, which is stated below rather than glossed.

Seven findings, none of them a release blocker and none of them a repair that failed. Three are about closure rather than about code: this Circle is about to put four open records and one explicitly-deferred user question somewhere no agent's scan reaches, and one correction that is possible today becomes impossible at the rename. The fourth is the same citation fault this Turn repaired, still standing in three of the four files the repair opened.

## Totals

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 0 |
| Medium | 3 |
| Low | 4 |

## What was checked and holds

**The fourteen citation repairs.** Every workbench-relative citation in all thirty in-scope files was resolved as written against the tree, fenced blocks excluded. Every repaired citation matches exactly one file; none is ambiguous. The four extra rewrites the commit message claims are real and each is a pointer that would have died: the Turn 1 review's `**Plan:**` field at `:9` named the plan by `_o_` and the same pass renamed it to `_c_`, `:64` names the one Turn 1 record still open, `:57` carries a second citation beside its `Record:` line, and `:76` is an elided form a slug search misses. The Turn 2 review's own seven were rewritten before the six records they name were closed, which is the recurrence Turn 2 warned about met one Turn early rather than one Turn late.

**Step 0i's ordering claim.** Verified by running the shipped block, not by reading it. Six scratch trees:

| Tree | Output | Exit |
|---|---|---|
| two `_t_` records, pointer present | `pointer-present` + both paths | 0 |
| two `_t_` records, no pointer | both paths | 0 |
| one `_t_` record, no pointer | one path | 0 |
| empty `circles/` | nothing | 0 |

The pointer-present two-record case is the one that was blind at `b8a4c1a`, and it is detected. The ordering claim holds as stated: with the two commands swapped, the block exits 1 whenever no pointer is present. The step's prose and its probe now describe the same condition, and the Done report enumeration at `skills/setup/SKILL.md:481` still names the three branches the table produces.

Two things about that block are **not** findings and are recorded so nobody re-derives them. The block exits 1 when `./fusion-workbench/circles` does not exist, pointer or not, because `find` is last; Step 0's `mkdir -p` at `skills/setup/SKILL.md:80` runs first, so the state is unreachable in the ordinary flow. And the two other repairs in this range are correct: `inCorpus("portfolio.md")` now takes the literal and therefore fails if the constant stops naming the file the workbench writes, and `README-hooks.md`'s corpus row now matches `LIVE_PLAN_RE` including the `_o_`/`_p_` reading.

**The plan's `## Where this Circle stops`, clause by clause.** This is the section put to the user at closure, so each clause is answered separately with what answered it.

| Clause | Verdict | Evidence |
|---|---|---|
| `rules/workbench-tracking.md` places every layout-tree entry in exactly one of four classes | **holds** | 15 root entries plus the two frozen stores = 17; the four class rows name 6 + 1 + 2 + 8 = 17, no entry twice, none missing |
| `git ls-files fusion-workbench \| awk -F/ 'NF==2'` returns exactly `.asset-provenance`, `.fusion-setup`, `orchestrator-events.jsonl`, and the `KEPT:` comment names the same three | **holds** | run at HEAD; `.gitignore:70` reads `# KEPT: orchestrator-events.jsonl, .fusion-setup, .asset-provenance.` |
| `portfolio.md` exists in the working tree and produces no `git status` entry | **holds** | file present, 14 963 bytes; `git status --porcelain` on it is empty |
| Two consecutive Setup runs at one plugin version leave `.gitattributes` and `.fusion-setup` byte-identical | **holds, by run record and not by this tree** | this repository has no `.gitattributes`, because Setup has not been re-run here since the change. Measured in the step-9 clones: `.fusion-setup` unchanged in content, hash and mtime; Step 0h's second run wrote nothing |
| A project whose `.gitattributes` already carried rules keeps every one of them, and the union line appears exactly once | **holds, by run record** | the step-9 verification exercised only the create case. The amend case was measured at Turn 1 step 4 against the committed block: comment plus one unrelated rule, three runs, rule line count 1, `git check-attr binary -- foo.png` still `set`, md5 unchanged after runs 2 and 3 |
| The two step-9 checkouts hold every event line from both sessions, no conflict resolved by hand | **holds** | 19 of 19 lines in each, `cmp` identical, both pulls exit 0, zero conflict markers, zero duplicate lines |
| Both decision records carry `Implemented:`, both defect records carry `Resolved:` | **holds** | `Implemented: c9eba48` and `Implemented: 25f60eb`; `Resolved 260823-0830` and `Resolved 260823-0841` |
| No growth-bound baseline moved, and `npm test` is green | **holds** | no diff in `growth-bound.ts` or either bound test across the range; suite run at `a2a18f9`: 41 files, 724 tests, exit 0 |

The section's own precondition, no tag or push before step 9 has run and its report is on disk, is met: the report is at `circles/260823-0023-settle-what-travels-between-checkouts/analyses/260823-1302-two-checkouts-one-event-log-and-what-the-monitor-makes-of-it.md` and no tag exists for this Circle.

## Findings by theme

### Closure buries what it defers

**1. Closing a Circle removes its open records from every agent's scan set.** Medium. `bin/fusion-paths` resolves every `SCAN_*` to the active Circle plus `shared/`; `SCAN_CIRCLES=circles` names records, not their stores. `agents/orchestrator.md:880-905` has no step that relocates or reports a closing Circle's `_o_` records. Measured across `circles/*/`: 75 open issues and 19 open decisions already sit outside every scan in eight closed Circles, none archived. This matters here because the deferral reasoning of two of C2's three open issues depends on a later reader finding them, and `shared/issues/260822-1136_*_two-definitions-of-the-turn-count-disagree-and-the-resume-snippet-counts-every-session-in-the-log.md`, which is C4's own plan input, carries no reference back to the C2 record that warns against its stated fix direction. Distinct from `shared/issues/260801-1020_*_scan-keys-never-reach-the-archive-store.md`, which is the same shape one store further on. Record: `circles/260823-0023-settle-what-travels-between-checkouts/issues/260823-1403_*_closing-a-circle-removes-its-open-records-from-every-agents-scan-set-and-no-closure-step-says-so.md`.

**2. A question left to the user has no open record anywhere.** Medium. `260823-1318_*_ten-record-citations-…`'s closure note says the corpus question "stays a question for the user", and the record is `_c_`. No `_o_` decision and no backlog entry exists for it, in the Circle store or in `shared/`. The question is real and recurred twice in two Turns, and the closure note already states both options and the cost the answer turns on, which is a decision record's content sitting in the wrong kind of file. Record: `circles/260823-0023-settle-what-travels-between-checkouts/issues/260823-1404_*_a-question-the-closing-pass-left-to-the-user-has-no-open-record-anywhere-only-a-closed-defects-prose.md`.

**3. The window to correct the Grounding's false claim closes with the Circle.** Low. `## Grounding snapshot` has one sanctioned writer, shaper in portfolio-activation mode, bounded to an `_a_` or `_t_` record and never a terminal one (`README-agents.md:25`, `agents/shaper.md:28`). The plan's `## Open Questions` at `:253` defers the correction to "the record's owner's call" with no deadline, and the rename at Phase 4 step 3 is the deadline. `## Closure note` is the one text still writable afterwards, and step 2b already uses it for exactly this shape. Record: `circles/260823-0023-settle-what-travels-between-checkouts/issues/260823-1405_*_the-window-to-correct-the-groundings-false-claim-closes-with-the-circle-and-nothing-says-so.md`.

### The repaired citation class, still standing

**4. Four hard-marker citations of the C1 Circle record dangle.** Medium. In three of the four C1 files this Turn opened to repair that exact class: the analysis at `:6` and `:294`, the analyst history at `:6`, the reconciliation history at `:5`, all spelling `_t_circle.md` for a record that has carried `_c_` since `4aaabc3`. All four are pointers by the project's own test; the one at `:294` sits third in a list whose first two entries already carry `_*_`. They predate the range. The repair pass searched for the two decision records it was about, and a Circle record is the citation most certain to die, since its marker moves at closure by construction. Record: `circles/260823-0023-settle-what-travels-between-checkouts/issues/260823-1402_*_four-hard-marker-citations-of-the-c1-circle-record-dangle-in-the-same-files-this-turn-repaired.md`.

### Text that a maintainer would reason from

**5. Step 0i's `pointer-present` branch reads a count as an identity.** Low. `skills/setup/SKILL.md:359` says "one path with `pointer-present` … this checkout activated it". The probe never reads what the pointer holds, and `bin/fusion-paths:255-278` validates emptiness, separators and directory existence but never the record's marker, so a pointer naming a different or terminal Circle passes both checks and the checkout is silently in `MISSING-POINTER` for the record it actually holds. The narrowing is inherited; the sentence claiming why is new in this range. Record: `circles/260823-0023-settle-what-travels-between-checkouts/issues/260823-1406_*_step-0is-pointer-present-branch-reads-a-count-as-an-identity-and-a-stale-pointer-passes-both-checks.md`.

**6. The live-plan clause's comment still says the tree carries no live plan.** Low. `hooks/lib/__tests__/workbench-citation-lint.test.ts:331-333`, forty lines below this range's own edit to the same file, and about the same clause whose README description this range corrected. Two `_o_` plans stand outside `archive/` at HEAD and both are in the corpus. Not a correctness fault: predicate, assertions and suite are all right. Record: `circles/260823-0023-settle-what-travels-between-checkouts/issues/260823-1407_*_the-live-plan-clauses-own-comment-still-says-the-tree-has-no-live-plan-and-two-stand-in-it.md`.

**7. The `**Active spec/plan:**` wildcard is right and unauthorised.** Low. The rewrite is correct: the field is a pointer, its target transitions `_o_ → _p_ → _c_`, nothing regenerates it, and no consumer opens it literally, across five consumer sites checked, all of them readers. But `rules/circle-records.md:135` and `:164` both spell a hard marker, and the wildcard rule at `:250-268` is scoped by its heading to `$PORTFOLIO` and grounded in regeneration. So the next writer of that field follows the template and re-creates the fault the rename just exposed. Record: `circles/260823-0023-settle-what-travels-between-checkouts/issues/260823-1408_*_the-plan-field-now-carries-a-wildcard-and-no-rule-authorises-one-in-a-circle-record-head-field.md`.

## Cross-cutting observations

**The gate corpus boundary is where three of these seven live.** Findings 4 and 6 and the buried question in finding 2 all sit in files the citation gate does not read: history, analyses, reviews, and test comments. This Turn repaired fourteen citations in exactly that zone by hand, and the same class immediately reappears four files away. That is the third measurement in this Circle of the same gap, and it is the gap the unfiled corpus question is about. The three are worth reading together before that question is answered, because they say what the gap costs rather than what widening it would cost.

**Two of the three records left open are correctly left open, and I checked rather than assumed.** `260823-1110_*_the-merge-driver-unsorts-a-second-event-log-reader-…` belongs to C4 by the specification's own assignment, and repairing it here would implement a Turn count in a Circle whose scope is transport; its appended note re-verified the condition at HEAD before leaving it. `260823-1302_*_the-monitor-attributes-a-merged-event-log-…` needs a user fork between two mechanisms, was excluded from C2 by plan step 9 by name, and its measurement is the reason the obvious repair, sorting by `ts`, is documented as insufficient. Neither is this Circle's own unfinished work. The third, the Grounding's false claim, **is** this Circle's own, and finding 3 is about it.

**What no clause of the stopping section covers.** Two of the eight are answered by run records in scratch trees rather than by this tree, and that is correct, since the tree cannot hold a two-checkout merge, but it means the user is being asked to accept a measurement rather than to read a state. Both measurements exist and both were opened for this review. Neither clause covers the amend path of `.gitattributes` in *this* repository, which still has none, because Setup has not been re-run here since the change landed. Nothing requires it to be, and no clause claims otherwise.

## Recommended sequencing

Nothing here blocks a release, and nothing here blocks closure on correctness grounds. What is time-bound is bound to the rename rather than to a tag.

**Before the `_t_ → _c_` rename**, because the rename forecloses them: finding 3 (correct the Grounding sentence in place, or carry it into the `## Closure note`) and finding 2 (file the corpus question as an `_o_` decision in `shared/`, where it outlives this Circle).

**At closure, as a report rather than a repair**: name C2's remaining open records to the user, per finding 1. That costs a sentence and makes the deferral something somebody saw.

**Afterwards, in any order**: findings 4, 5, 6 and 7. Finding 4 is worth one tree-wide search for Circle-record citations under a hard marker rather than four individual edits; findings 6 and 7 are single-paragraph corrections in text nobody executes.

---

**Reconciliation annotation, 260823-1446 (reconciler).** Findings only; no finding is rewritten.

**Confirmed and closed:** 4, 6 and 7 (records `260823-1402_c_`, `260823-1407_c_`, `260823-1408_c_`), plus
finding 2's remedy — the corpus question now stands as
`shared/decisions/260823-1414_*_does-the-workbench-citation-gates-corpus-cover-review-files.md`, in
`shared/` as the finding asked, and `260823-1404_c_` is closed against it. Finding 7's authorisation is on
disk at `rules/circle-records.md:274` (`### Citation form in a Circle record's head field`).

**Confirmed and still open:** 1, 3 and 5 (`260823-1403_o_`, `260823-1405_o_`, `260823-1406_o_`).

**Finding 1's figures were re-measured and two of the three are off, while the finding itself holds.**
Counted at HEAD over `circles/*/` with the Circle record's marker read from its own filename: **75 open
issues**, which matches exactly, across **10** non-active Circles rather than eight. **Open decisions are
12, not 19.** The 19 is the count of open *plus answered* decisions, that is the active-Grounding filter
`rules/fusion-workbench-conventions.md` `## State Markers — decisions` defines, over four Circles. Both
readings support the finding and the larger one arguably better, since an answered-but-unimplemented
decision is exactly the record a later Circle needs to find. The correction is recorded because the
figures will be quoted.

**Finding 3's proposed remedy does not discharge the record it is about, and this pass judged it rather
than assumed it.** Carrying the correction into `## Closure note` puts a true sentence into the record; it
does not unwrite the false one, which stands in `## Grounding snapshot` in emphatic form and is what a
reader meets first. `260823-0800_*_the-groundings-first-write-outside-the-workbench-claim-was-already-false-when-it-was-written.md`
is filed on the claim being false when written, and a note further down the same file leaves that true.
Both records stay `_o_` after a closure note, and this session's Coherence verdict flags the edge on it.
