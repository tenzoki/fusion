# Code review — C2 Turn 4: the four commits no review had opened, before the rename

**Reviewed-range:** `a2a18f9..2ec2bc2`
**Not-opened:** none

**Date:** 2026-08-23
**Sender:** coderev
**Circle:** `260823-0023-settle-what-travels-between-checkouts`
**Plan:** `260823-0800_*_c2-what-travels-between-checkouts-is-settled.md`
**Carried from an earlier pass:** nothing. The Turn 3 review declared `**Not-opened:** none`, so `bin/fusion-review-coverage` reports `carried=none` and no file entered this scope by inheritance.

---

## Summary

Four commits, thirty files, five of them shipped. **Nothing here should stop the rename on correctness grounds** — the mechanism is sound, the goldens match the tree byte for byte, and the one repair that touches shipped rules does exactly what its record asked. **One thing should stop it on a deadline**: the hand-written Grounding correction in `2ec2bc2` replaced a false claim with another one, and the Grounding stops being writable at the rename.

Eight findings, none Critical or High. Three are Medium and carry a deadline or a frozen record; five are Low. The dominant pattern is not defects in mechanism but **wrong numbers in summaries whose detail was right** — it appears in the Grounding, in the Turn log three times, in the reconciler's Coherence section three times, and in one closed defect record.

## Totals

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 0 |
| Medium | 4 |
| Low | 4 |

## What should stop the closure, and why it is only one thing

**`260823-1635_*_the-corrected-grounding-undercounts-setups-project-root-writes-and-omits-step-0f.md_*` — the corrected Grounding is still wrong, by one, and the window closes at the rename.**

`2ec2bc2` was a human edit made specifically because no agent was permitted to make it, and because `## Grounding snapshot` is Grounding-Stand only while the record carries `_a_` or `_t_` (`rules/circle-records.md:65`). The replacement says the merge driver is Setup's **third** write outside `fusion-workbench/` and names only Step 0g's two files. It is the fourth. `skills/setup/SKILL.md` Step 0f writes `./fusion.json` at the project root (`:245-248`), and it has done so since 2026-08-04 — nineteen days before the Circle record was created.

The Circle's own source edit already says so. `skills/setup/SKILL.md:319`, written in this Circle by `c9eba48`: *"Like Steps 0f and 0g, the write lands at the project root."* Two steps in the shipped text, one in the Grounding.

This is not a correctness risk to any code. It is a deadline: after the rename the sentence is frozen, which is the exact argument `2ec2bc2`'s own commit message makes for having done the correction by hand at all. A correction that arrives one commit before the deadline and is wrong by one is that deadline, unmet.

`260823-1642_*_the-hand-written-grounding-correction-breaks-the-paragraphs-own-two-costs-scaffolding-and-its-markup.md_*` names three further residuals in the same three lines — the paragraph's "two costs" scaffolding no longer parses, four literals lost their backticks, and the convention list over-states a guarantee `skills/setup/SKILL.md` deliberately scopes. They share the window, so they should be taken in the same edit or not at all.

## Findings by theme

### Theme 1 — a summary restates a measurement instead of citing it, and loses

Four findings, one shape. In every case the correct value was in the same pass's own detail file, or one `find` away.

**`260823-1636_*_all-three-turn-log-ranges-drop-their-own-first-commit-the-fault-this-circle-already-fixed-once.md_*` (Medium) — all three Turn-log ranges drop their own first commit.** `_t_circle.md:60-62`. Written `21ae170..e41393e`, `e7454e3..5fc3201`, `a2a18f9..7cd79f1`; read as git reads `..`, they cover 16 of the session's 19 commits and leave three gaps, each being that Turn's own first commit. Corrected to `3ee8eaf..e41393e`, `e41393e..5fc3201`, `5fc3201..7cd79f1` they tile exactly. The commit sets the author meant are right; the notation cannot express them.

The exclusive reading is this project's own: `bin/fusion-review-coverage` parses `..` as git does, and this Circle's three review headers tile under it. **The same fault was caught, documented and fixed inside this Circle**, at `260823-1318-coderev-c2-turn-2.md:10`, eight commits earlier. And the correct base was already on disk: `turn_start` carries `head 3ee8eaf`, `head e41393e`, `head 5fc3201`.

**`260823-1637_*_turn-log-entry-3-books-a-per-circle-reconciler-verdict-against-a-turn-that-had-no-coherence-gate.md_*` (Medium) — Turn-log entry 3 books a Circle-scoped verdict against a Turn that had no Coherence gate.** No `coherence_review` and no Coherence `gate_hit` exists for this session between `turn_start turn=3` (11:30:03) and `turn_end turn=3` (12:36:03). `review-needed` is the Phase-3 per-Circle reconciler verdict over `3ee8eaf..7cd79f1`, produced ten minutes after Turn 3 ended. Entries 1 and 2 carry per-Turn gate verdicts; entry 3 carries a different object, unmarked.

**`260823-1638_*_three-figures-in-the-reconcilers-coherence-section-are-wrong-and-one-contradicts-its-own-detail-file.md_*` (Medium) — three figures in the reconciler's `## Coherence` section are wrong.** `:92` says 75 open defect records across 10 non-active Circles; the 75 is exact and stable across the whole range, the Circles are 9. `:39` says 22 defect closures; there are 23. `:48` says `7cd79f1` touches four shipped files; it touches five — **and the same pass's own detail file, `260823-1446-reconciliation.md`, lists all five correctly.** That last one is load-bearing: it is the evidence offered for not flagging the uncovered review range.

The record also notes that the reconciler's correction of "19 open decisions" to 12 is exactly right, on both numbers and on the reason, but has not reached `260823-1403_*_closing-a-circle-removes-its-open-records-from-every-agents-scan-set-and-no-closure-step-says-so.md_*`, which is still `_o_` and still carries the 19.

**`260823-1641_*_the-wildcard-records-claim-that-every-other-circle-record-spells-a-hard-marker-is-false-six-already-star-it.md_*` (Low) — a closed record's supporting fact is false.** `260823-1408_*_the-plan-field-now-carries-a-wildcard-and-no-rule-authorises-one-in-a-circle-record-head-field.md_*` argues from "Every other Circle record in the live tree spells a hard marker in that field". Seven do; six already carry `_*_`. The rule it produced is right and I verified it independently; the evidence inverts the finding's character, from a uniform practice this Circle broke to a practice already split down the middle.

### Theme 2 — the citation class this Circle spent three Turns on, committed once more inside it

**`260823-1639_*_the-reconcilers-review-annotation-wrote-two-hard-marker-citations-and-one-died-one-commit-later.md_*` (Medium).** The reconciler's annotation appended to the Turn 3 review by `d2089e4` carries two hard-marker record citations. `2ec2bc2` — the next commit — renamed one of their targets `_o_` → `_c_`, so `260823-1410-coderev-c2-turn-3.md:125` resolves to nothing at HEAD. The second, at `:107`, cites the corpus decision by `_o_` and will die when it is answered.

These are the only two hard-marker record tokens in any review of a non-terminal Circle at HEAD, and both were written in this range.

It moves an open decision's own evidence. `shared/decisions/260823-1414_*` option 3's stated Pro is that its repair debt is *zero today*. Re-measured with the record's own method: 0 at `a2a18f9`, 0 at `1544224` and `7cd79f1`, **1 at HEAD**. The debt was created after the option was costed, by the pass that costed it. No gate sees it, because review files are outside `inCorpus` — which is the whole subject of that decision.

### Theme 3 — a measurement whose numbers are right and whose anchor is not

**`260823-1640_*_the-corpus-decisions-measured-block-declares-an-anchor-whose-tree-gives-different-file-counts.md_*` (Low).** `shared/decisions/260823-1414_*` `## Measured` declares "At HEAD `a2a18f9`". Its file counts are the tree at `1544224`/`7cd79f1`: 90 review files where `a2a18f9` has 89, and 3 non-terminal-Circle reviews where `a2a18f9` has 2. `## Question` carries the same 90.

**Every other figure reproduces exactly.** I rebuilt the scan independently from the record's description, against `git show` blobs at four commits: 522 tokens, 270 dangling in 64 files, 116 dangling in 22 of `shared/reviews/`'s 34, and zero in the non-terminal Circle's reviews — identical at `a2a18f9` and at `1544224`. Those five are what the options are costed on, so the cost of the wrong anchor is entirely to their credibility: a reader who does what the record invites and re-measures at `a2a18f9` finds the first number wrong and has no way to tell the rest are right.

The record is **genuinely unanswered**: `_o_`, all five trailer lines empty, `## Recommendation` reads "None, and deliberately so" and gives its reason, and the four options are argued with costs rather than steered.

## What I verified and found sound

Stated because a closing review that reports only faults tells the reader nothing about coverage.

- **The 26-place count in `260823-1408_*_the-plan-field-now-carries-a-wildcard-and-no-rule-authorises-one-in-a-circle-record-head-field.md_*` is exact.** 27 occurrences of `Citation form in the portfolio` in the tree; one is the heading itself at `rules/circle-records.md:250`; the remaining 26 split 20 `###` / 6 `##`, exactly as the record says. Four are in shipped text, as it says.
- **The new section `### Citation form in a Circle record's head field` (`rules/circle-records.md:274`) authorises what the field does, and contradicts nothing.** It is consistent with the template at `:135`, the worked example at `:164`, and the pointer-versus-statement test at `rules/fusion-workbench-conventions.md:358`. Adding a sibling rather than renaming the cited heading was the right call and is the class this Circle was repairing.
- **No residual in the field's writers.** `agents/orchestrator.md:275`/`:278` and `agents/shaper.md:57` write the field without spelling an example marker, so the template edit is the whole fix.
- **`workbench-citation-lint.test.ts:331-334`** — the tree claim was dropped rather than corrected, which is the right repair, and the pointer it leaves ("the dated measurement at the clause itself") resolves: `:151-156` carries its own `2026-08-20 at HEAD 8e7cae7` stamp.
- **`reference-resolution-lint.test.ts` `BASELINE.records` 117 → 118 is exact.** The assertion is equality, the suite is green, and the accounting comment names the one new token correctly.
- **Both goldens match the tree.** `rules/circle-records.md` is 20 172 bytes and `reference-resolution-lint.test.ts` is 1 103 lines, exactly as written. `circle-records.md` is role-specific (indented emission in `bin/fusion-rules:484`), so the always-on floor is untouched; the `[coder]` block sums to 95 252, the figure `260823-1408_*_the-plan-field-now-carries-a-wildcard-and-no-rule-authorises-one-in-a-circle-record-head-field.md_*` reports.
- **Prose metric on `rules/circle-records.md`: 39 em-dashes, 16.4 per 1000 prose words** — matching the record's claim exactly. Still over the ceiling, as it was before at 17.8.
- **The four C1 citation repairs** (`_t_circle.md` → `_*_circle.md`, three files) all resolve; C1 is `_c_`.
- **The reconciler's C2 acceptance-criteria verification is sound.** I spot-checked four of its seven independently: the tracked workbench root entries are exactly the three named, `.gitignore:70` matches them, `not machine-refreshed` appears nowhere under `rules/`, `hooks/`, `agents/` or `skills/`, and the four-class table tiles 6 + 1 + 2 + 8 = 17 with no entry twice and none missing.
- **`npm test`: 41 files, 724 tests, exit 0**, re-run after this review's own records were filed. One earlier run in this session failed `monitor-warnings-panel.test.ts:905` with `http://localhost:` where it expects `http://127.0.0.1:`, and the same command passed on re-run — so that test is intermittent on this machine, in a file this range never opened (`git diff --stat a2a18f9..2ec2bc2` on it is empty). Out of scope either way, and worth knowing only so a later red run on that line is not attributed here. All three blocking text gates are green, and the citation gate is green **for the wrong reason** on the dangling review citation, which is finding `260823-1639_*_the-reconcilers-review-annotation-wrote-two-hard-marker-citations-and-one-died-one-commit-later.md_*`.
- **This review's own eight records pass the citation gate.** Two did not on the first run — I abbreviated two citation slugs with an ellipsis, which is the same dangling-pointer class this review reports three times. Both are corrected; the mistake is recorded here rather than quietly fixed, because a reviewer who commits the class it is reporting should say so.

## Cross-cutting observations

**Retroactive bookkeeping got the range wrong, exactly where it was predicted to.** The Turn log was filled after three Turns in one pass and all three ranges are wrong. The check that catches an off-by-one is comparison against the previous entry's end, and filling three at once means there is never a previous entry. `shared/issues/260822-2236_*` is open on the four surfaces that freeze, and this is the same session's Turn log, corrected once and wrongly.

**Every wrong number in this range is in a summary, and every corresponding right one is in a detail file.** The reconciler's shipped-file list is right in `260823-1446-reconciliation.md` and wrong in `## Coherence`. The Grounding's corrected fact is wrong where `skills/setup/SKILL.md:319` — cited two sentences later as its evidence — is right. The corpus decision's anchor is wrong where its own scan is right. This is a restating-versus-citing problem, not a measuring problem, and none of the four passes was careless about measuring.

**The Circle's own defect class was committed three times inside its closing Turns**: the C1 record citations found at Turn 3, the ten Turn-1 review citations found at Turn 2, and now a fresh dangling citation written by the reconciler in `d2089e4`. The decision that would close the class, `shared/decisions/260823-1414_*`, is open and unanswered — correctly so. What it should not carry into the answer is a stale zero.

## Recommended sequencing

**Before the rename, in this order.** `260823-1635_*_the-corrected-grounding-undercounts-setups-project-root-writes-and-omits-step-0f.md_*` and `260823-1642_*_the-hand-written-grounding-correction-breaks-the-paragraphs-own-two-costs-scaffolding-and-its-markup.md_*` together, since they are the same three lines and the same frozen window. Then `260823-1636_*_all-three-turn-log-ranges-drop-their-own-first-commit-the-fault-this-circle-already-fixed-once.md_*` and `260823-1637_*_turn-log-entry-3-books-a-per-circle-reconciler-verdict-against-a-turn-that-had-no-coherence-gate.md_*`, because the Turn log freezes with the record too and is the only tiling a later reader has.

**Before the rename if a reconciler pass is available**, since `## Coherence` is reconciler-owned: the three figures in `260823-1638_*_three-figures-in-the-reconcilers-coherence-section-are-wrong-and-one-contradicts-its-own-detail-file.md_*`, and the 19 that still stands in the open `260823-1403_*_closing-a-circle-removes-its-open-records-from-every-agents-scan-set-and-no-closure-step-says-so.md_*`. Neither changes an edge verdict or the closure recommendation.

**Before the corpus decision is answered, in any order**: `260823-1639_*_the-reconcilers-review-annotation-wrote-two-hard-marker-citations-and-one-died-one-commit-later.md_*` (star the two markers, which restores option 3's measured cost to zero) and `260823-1640_*_the-corpus-decisions-measured-block-declares-an-anchor-whose-tree-gives-different-file-counts.md_*` (move the anchor to the commit the counts were taken at). Both are one edit each and neither is a closure blocker.

**Whenever**: `260823-1641_*_the-wildcard-records-claim-that-every-other-circle-record-spells-a-hard-marker-is-false-six-already-star-it.md_*`. The record is closed and its rule shipped; this stands as the correction beside it.

**Not a release blocker anywhere in this range.** No shipped mechanism is wrong, the suite's one red test is environmental, and the five shipped files touched by `7cd79f1` all do what their records say.

---

## Reconciler annotation — second Coherence pass, 260823-2130-reconciliation.md

All eight findings re-checked at their own sites rather than accepted. **Eight of eight confirmed**;
none was found overstated, and one was found to have been fixed between the review and this pass.

| Finding | State at HEAD |
|---|---|
| `260823-1635_*_the-corrected-grounding-undercounts-setups-project-root-writes-and-omits-step-0f.md_*` corrected Grounding wrong by one | **Confirmed, then fixed by `a40b330`.** Closed. |
| `260823-1636_*_all-three-turn-log-ranges-drop-their-own-first-commit-the-fault-this-circle-already-fixed-once.md_*` three Turn-log ranges | **Confirmed, then fixed by `71f47c1`.** Closed. |
| `260823-1637_*_turn-log-entry-3-books-a-per-circle-reconciler-verdict-against-a-turn-that-had-no-coherence-gate.md_*` entry 3's verdict object | **Confirmed, then fixed by `71f47c1`.** Closed. |
| `260823-1638_*_three-figures-in-the-reconcilers-coherence-section-are-wrong-and-one-contradicts-its-own-detail-file.md_*` three figures in `## Coherence` | **Confirmed, all three.** Corrected as errata. Closed. |
| `260823-1639_*_the-reconcilers-review-annotation-wrote-two-hard-marker-citations-and-one-died-one-commit-later.md_*` two hard-marker citations | **Confirmed.** Both starred; the Circle's reviews now hold zero. Closed. |
| `260823-1640_*_the-corpus-decisions-measured-block-declares-an-anchor-whose-tree-gives-different-file-counts.md_*` corpus decision's anchor | **Confirmed.** Open, no edge. |
| `260823-1641_*_the-wildcard-records-claim-that-every-other-circle-record-spells-a-hard-marker-is-false-six-already-star-it.md_*` closed record's supporting fact | **Confirmed.** Open, no edge. |
| `260823-1642_*_the-hand-written-grounding-correction-breaks-the-paragraphs-own-two-costs-scaffolding-and-its-markup.md_*` Grounding paragraph residuals | **Partly fixed by `a40b330`** (backticks, emphasis, the trailing space at `:29` — `grep -n ' $'` now returns nothing). Two stand: the "two costs" scaffolding, and the convention list. **Open, and it is the one finding that flags an edge.** |

**The one finding checked hardest, because the verdict turns on it.** `260823-1642_*_the-hand-written-grounding-correction-breaks-the-paragraphs-own-two-costs-scaffolding-and-its-markup.md_*`'s convention
claim was verified at the source rather than from this review. `_t_circle.md:30` offers six clauses;
five hold verbatim against `skills/setup/SKILL.md:270-313`. The sixth does not: Step 0g bounds it
itself — "that guarantee is about the `allow` list and reaches no other field" — and then replaces
`defaultMode`, "a scalar is replaced, not merged". The review's characterisation is exact.

**Two corrections to this review, neither material.** The `**Reviewed-range:**` `a2a18f9..2ec2bc2`
does not reach `71f47c1` or `a40b330`, so `bin/fusion-review-coverage` reports `uncovered=2` at HEAD
against those two — the review could not have covered the commit that carries it. And `260823-1636_*_all-three-turn-log-ranges-drop-their-own-first-commit-the-fault-this-circle-already-fixed-once.md_*`
reports the corrected ranges without the commit counts the entries now also carry; those were verified
separately with `git rev-list` at 10, 6 and 3.

**One thing this review got right that three earlier parties did not.** It caught the Grounding
correction wrong by one by opening `skills/setup/SKILL.md` Step 0f rather than reading the ordinal, and
it noticed that the Circle's own shipped text at `:319` had named both steps correctly all along. The
cross-cutting observation — every wrong number in this range sits in a summary while the right one sits
in a detail file — held once more in this pass: the dispatch that commissioned it restated the first
verdict's flagged edge and named the wrong one.
