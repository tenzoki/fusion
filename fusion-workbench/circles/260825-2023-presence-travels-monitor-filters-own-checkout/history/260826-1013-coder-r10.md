# R-10 — the fourth session_start emit template takes the `<ID>` fragment

**Status:** Complete
**Agent:** coder
**Circle:** circles/260825-2023-presence-travels-monitor-filters-own-checkout, Turn 3

## What was implemented

`skills/setup/SKILL.md` Step 5's `session_start` template carried the two identity fields literally,
created by `c2be6f8` and missed by `753932b`'s conversion of "all three" templates two commits later.
Three edits, all in that one file:

1. `:483` — `\"person\":\"<PERSON>\",\"checkout\":\"<CHECKOUT>\"` becomes `<ID>`, so the template
   executes the absent-rather-than-empty rule instead of emitting an empty-string field.
2. `:480` — the prose bullet reads "It carries `<ID>` from Step 0i" in place of "`person` and
   `checkout`, held from Step 0i's read and not read again". The sentence after it is untouched: it
   already cited `agents/orchestrator.md` `### 2. Structured Event Log` for the contract, the
   unresolved-half rule included.
3. `:352` — Step 0i, beside the `bin/fusion-identity` call, now ends "Hold the pair as `<ID>`, the
   fragment defined at `agents/orchestrator.md` Setup step 2." A pointer, not a second statement of
   the rule; restating it is the duplication `rules/critical-stance.md` names as a defect, and it was
   the shape the original defect described.

## The budget

`skills/` had 7 bytes of head-room. The fix is net negative: `skills/setup/SKILL.md` 48 074 -> 48 065,
**-9 bytes**. The code-block replacement pays for the Step 0i sentence on its own (-52 +4), and the
prose shortening at `:480` (-49) covers the +88 the new sentence costs. No baseline was moved.

## No fifth site

The finding's point is that a count of three was wrong, so this one was counted rather than assumed.
Grepped `agents/`, `skills/`, `rules/`, `bin/`, `docs/`, `templates/`, `README*.md`, `CLAUDE.md` and
the hook sources for `session_start`, for `>>` into `orchestrator-events.jsonl`, and for the string
`person`. Three emit templates exist and all three now carry `<ID>`: `agents/orchestrator.md:235`,
`agents/orchestrator.md:1322`, `skills/setup/SKILL.md:483`. `agents/orchestrator.md:953` and `:1279`
are prose; `:1267` is the `**Event schema:**` specimen, which shows resolved values and is correctly
literal. Nothing else emits an event line.

## Verification

`cd hooks && npx vitest run` — 2 failed, 774 passed (776).

- `surface-growth-bound.test.ts` — expected. The golden is a whole-tree measurement and two siblings
  hold uncommitted edits to `agents/orchestrator.md` and `agents/reconciler.md`; the diff names the
  `agents` surface. The orchestrator regenerates the golden once when the wave lands.
- `reference-resolution-lint.test.ts` — `paths` moved off its pin. **This edit's own share is +1 path,
  0 anchors**, measured by reverting this one file against the rest of the dirty tree: reverted the
  gate resolves 1424, the committed pin exactly, and restored it resolves 1425. The token is the
  `agents/orchestrator.md` citation added at `:352`, unrooted and naming no heading, hence a path and
  not a path-and-anchor. An earlier whole-suite run in the same minute read 1428, so siblings were
  carrying +3 at that moment; that share is theirs and moves with their files. `BASELINE` was
  deliberately **not** re-approved from this task — the pin is one number for the whole wave and two
  agents were still writing to `agents/` when it was read.

Nothing else red.

## Record

`260826-0906_c_a-fourth-session-start-emit-template-was-created-in-this-range-and-left-out-of-the-id-conversion.md`
carries a `Resolved:` note and was renamed `_o_` -> `_c_`. Nothing staged, nothing committed; the
`git mv` staged the rename and it was unstaged again under `bin/fusion-commit-lock`, because two
siblings share this index.
