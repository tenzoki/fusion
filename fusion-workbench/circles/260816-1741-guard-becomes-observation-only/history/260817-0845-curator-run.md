# Curator run — 260817-0845

---
**Status:** Complete. Both passes are run. The user confirmed the blast radius separately and then approved all 18 entries; all 18 were applied on 2026-08-17 against the same HEAD the survey read. Outcomes are in `## 7`.
**Mode:** survey, then apply (`**Approved:** L01…L18`, all 18)
**Date:** 2026-08-17
**HEAD read:** `c65e1cf4678bb85ae359f132a665514b715a37a3`
**Previous curator run:** `shared/history/260816-1251-curator-run.md`, 2026-08-16, HEAD `49210261f7460132805e104e115d1235e796e914`. The run before that was `circles/260815-0007-remove-eight-mechanisms-and-cap-growth/history/260815-1706-curator-run.md`, 2026-08-15.
**Dispatched by:** the orchestrator, as step 16 of `circles/260816-1741-guard-becomes-observation-only/planning/260816-1915_p_the-compliance-guard-becomes-observation-only.md`. Non-interactive: no `AskUserQuestion`, so the gate is returned to the dispatcher.
**Domain:** code

**What changed in the interval.** Sixteen commits, `3d41d4a`..`c65e1cf`, removed fusion's compliance guard as a deciding mechanism. That is the whole subject of this run, and it is why almost every entry below is Tier 1: the tree moved and two surfaces did not.

---

## 1. Evidence-source counts

| # | Source | Files read | Note |
|---|---|---|---|
| 1 | Circle records (`circles/`) | 16 of 16 | Every `*_circle.md`, title line and `## Closure note` read. The active Circle's record (`260816-1741-guard-becomes-observation-only/_t_circle.md`) was read in full through its Grounding snapshot: it is the Tier 2 spine of this run. Turn logs and Dependencies sections were not read. |
| 2 | Decision records | 58 of 58 listed, 13 opened | `$SCAN_DECISIONS` resolves to the active Circle's `decisions/` (3) and `shared/decisions/` (55). All 58 filenames read. The 13 matching the guard-removal vocabulary were opened; 3 of those in full, 10 at their `Answered:`/`Implemented:`/`Retired:`/`Superseded by:` block. The other 45 were not opened. |
| 3 | Session histories | 0 of 272 opened | Listed only, to locate the previous curator run. The two orchestrator sessions this Circle's decisions cite as their answer sites (`shared/history/260816-1500-*`, `circles/…/history/260816-1841-*`) were read at second hand, through the `Answered:` lines that quote them, not opened. **This is the largest gap in the run** and it is stated rather than glossed. |
| 4 | Git history | 13 files | `git log --follow --oneline` over `CLAUDE.md` (87 commits) and each of the 12 files in `rules/` (1 to 68 commits each), read as one-line subjects; the full log per file, not a window since the previous run. `git show --stat c65e1cf` read in full for the decision-annotation pass. `git blame` was not run: no entry below rests on a single paragraph's authorship. |
| 5 | Reviews and analyses | 0 of 29 reviews, 0 of 22 analyses opened | Not opened. `$SCAN_ANALYSES`'s Circle half is empty (0 files); `shared/analyses/` holds 16 and the other Circles 6. No entry below rests on a review or an analysis. |
| 6 | `orchestrator-events.jsonl` | 1 file, not read | 1 684 lines. Corroborating-only by rule, and nothing here needed corroboration that a stronger source did not already carry. Not read. |
| 7 | Archive store (`$WORKBENCH/archive`) | 0 files | **Empty.** `find archive -type f` returns 0. Nothing has been archived on this project yet. |
| 8 | `**Provenance:**` headers | 12 of 12 | Every file in `rules/` carries one. Three cite `circles/260801-1244-guard-rules-write`, a Circle whose record is `_c_` rather than `_s_`, so no header resolves to a superseded record and no Tier 2 retirement follows from this source. |

**Unreadable surfaces:** none. Every file named in this run was read successfully.

**Absent surfaces:** `.claude/rules/` does not exist in this project. There are therefore no project-wide rule files, and the project-owned rule surface is `./rules/` alone.

## 2. Surface sizes

| Surface | Files | Bytes before | Lines before | Bytes after (if all approved) | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 1 | 58 372 | 137 | 61 708 | 137 |
| Project rule files (`./rules/`) | 12 | 149 129 | 1 754 | 149 780 | 1 754 |
| Decision records (`$SCAN_DECISIONS`) | 58 | 428 913 | 5 592 | 428 913 | 5 592 |

Every entry is an in-place replacement of one whole line. No line is added and none is removed, so the line counts do not move and **no file is deleted**.

## 3. Blast radius

**Verdict: report both readings and let the user set the scale, because the two disagree on `CLAUDE.md`.**

- **Bytes deleted outright: zero, on all three surfaces.** Nothing is removed without a replacement, and both edited surfaces grow (`CLAUDE.md` +3 336 bytes, `rules/fusion-workbench-conventions.md` +651).
- **Bytes rewritten in place: 15 355 of `CLAUDE.md`'s 58 372, which is 26.3 percent**, and 1 010 of the conventions file's 55 184, which is 1.8 percent.

Under the strict reading, where a rewritten line counts as a deletion plus an insertion, `CLAUDE.md` crosses the 20 percent threshold and the scale confirmation is due **before** the ledger counts are shown. Under the net reading it is nowhere near it. The dispatcher should put the scale question first on the conservative reading: a quarter of this file's bytes are being rewritten in one pass, which is worth a pause whether or not the byte count falls the strict way.

## 4. Comparison counts

**Selection rule, one rule for all six pairs.** A statement enters the comparison when it carries a token from the guard-removal vocabulary — `guard`, `halt`, `escalat*`, `protectedPaths`, `categoryPaths`, `blocksBeforeHalt`, `guard.enabled`, `fusion-guard.json`, `hooks/config.json`, `isFusionPluginCwd`, `project-relative`, `clear-halt`, `CHECK ` — or when `reference-resolution-lint.test.ts` reports it as a dangling citation. A pair enters the comparison when both of its members do. Everything outside that vocabulary was not compared, and this run makes no claim about it.

**The corpus counts are derived, not stated.** `ls -1 rules/ | wc -l` = 12; `ls -1 circles/260816-1741-*/decisions/*.md shared/decisions/*.md | wc -l` = 58; `awk 'END{print NR}' CLAUDE.md` = 137 lines.

| # | Pair | Pairs the rule produced | Read | Findings |
|---|---|---|---|---|
| 1 | decision × decision | 78 (C(13,2) over the 13 records matching the vocabulary) | 78 | 0. The store was annotated in `c65e1cf` and no live record overturns another. |
| 2 | decision × rule file | 13 (13 records × 1 matching rule file) | 13 | 0 direct. The two rule-file findings are Tier 1 against the tree, not against a record. |
| 3 | decision × `CLAUDE.md` | 130 (13 records × 10 matching `CLAUDE.md` lines) | 130 | 1 — L11, `260812-1232_i_…stand-down…` against `CLAUDE.md:83`. |
| 4 | rule file × rule file | 0 (1 of 12 rule files matches the vocabulary, so the rule produces no pair) | 0 | 0. |
| 5 | rule file × `CLAUDE.md` | 30 (3 matching conventions lines × 10 matching `CLAUDE.md` lines) | 30 | 0 — the two surfaces are stale in the same direction rather than against each other. |
| 6 | `CLAUDE.md` × itself | 45 (C(10,2) over the 10 matching lines) | 45 | 0 direct contradiction. All ten are false together, which is coherence about a tree that has changed, not a contradiction between them. |

**The verdict covers the pairs this rule reached, not the corpora.** 45 of 58 decision records were never opened, and 127 of `CLAUDE.md`'s 137 lines were read once for falsified claims but not compared pairwise against anything. A contradiction outside the guard vocabulary would not have been found by this run.

**One cross-surface contradiction was found outside the six pairs, between a rule file and a skill body.** `rules/fusion-workbench-conventions.md:79` calls `.guard-state/escalation.json` a live counter while `skills/archive/SKILL.md:94` and `:142`, corrected at plan step 11, call it inert. Skill bodies are not one of the three surfaces, so this is not one of the six pairs; the rule file is the authoring home and the side that is wrong, and it is entry L16.

## 5. Pre-edit content of decision records this run would modify

**None.** This run proposes no change to any decision record. The obligation is unconditional, and it is discharged by the count: zero records are in the ledger, so there is no pre-edit content to preserve.

## 6. The ledger

18 entries. Ids are stable and per-entry approval is by id.

| Group | Ids | Count |
|---|---|---|
| Constraint removal | L09 | 1 |
| Tier 3 | — | 0 |
| Tier 2 | L11 | 1 |
| Tier 1 | L01–L08, L10, L12–L18 | 16 |
| Consolidation | — | 0 |
| **Candidate (not on offer, never applied)** | — | **0** |

Two approval couplings the gate should carry, because approving one half of either leaves a surface worse than not touching it:

- **L01 + L02** are one argument across two paragraphs of `CLAUDE.md`. Half of it reads as a contradiction.
- **L17 + L18** are the `rules/` half of a paired fix whose other half is `hooks/session-start.ts`, which is outside this agent's remit. See `## 8`.

### L01 — the write-tool stand-down is stated as live and was deleted on 260816

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 6 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `grep -n 'export ' hooks/lib/self-detect.ts` -> one line, `export function isFusionPluginRoot(dir: string): boolean` at :58. `isFusionPluginCwd()` is gone; `hooks/guard.ts:48-54` states in the past tense that the stand-down 'went with them on 2026-08-16, which is why the guard now behaves identically in this tree and in a consuming project'. Commits `2f624ca` (branch) and `ec3b6ad` (function).
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Approve with L02: the two paragraphs are one argument and half of it reads as a contradiction.

**Before:**

> This repository is the **source of the fusion Claude Code plugin** (`tenzoki-plugins/fusion`). It is *not* a fusion-consuming project. One part of the guard **self-disables** here: the write-tool branch of `hooks/guard.ts` recognises this repository by a `.claude-plugin/plugin.json` matching the plugin's own name, and stands down so a fusion developer can edit the plugin's own source. It was built because the guard used to protect `agents/**`, `rules/**` and the plugin manifest, which is correct for a project *using* fusion and wrong when developing fusion itself, where every edit to those files would have been blocked and written back. **That protection was removed on 260812**, along with the whole protected-path half of the guard, and the stand-down outlived it. A second stand-down covered the churn heatmap in `hooks/tracker.ts` and went with the heatmap on 260815.

**After:**

> This repository is the **source of the fusion Claude Code plugin** (`tenzoki-plugins/fusion`). It is *not* a fusion-consuming project. **No part of the guard self-disables here any more, because no part of it decides anything anywhere.** One stand-down used to: the write-tool branch of `hooks/guard.ts` recognised this repository by a `.claude-plugin/plugin.json` matching the plugin's own name and allowed every write, so a fusion developer could edit the plugin's own source. It was built because the guard used to protect `agents/**`, `rules/**` and the plugin manifest, which is correct for a project *using* fusion and wrong when developing fusion itself, where every edit to those files would have been blocked and written back. **That protection was removed on 260812**, along with the whole protected-path half of the guard, and the stand-down outlived it by four days: it went on 260816 with the guard's last verdict, so the hook now behaves identically in this tree and in a consuming project (`hooks/guard.ts`, the header's account of what it used to check). A second stand-down covered the churn heatmap in `hooks/tracker.ts` and went with the heatmap on 260815.

### L02 — `isFusionPluginCwd()` named as the surviving check, and self-detect said to keep both entry points

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 8 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** Same command as L01: `hooks/lib/self-detect.ts` exports one function, not two. The rule the paragraph states is untouched; only its two present-tense claims are false.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Approve with L01.

**Before:**

> **The rule the two established outlives both of them, and it is what the next stand-down is measured against:** a stand-down is evaluated in the coordinate space the mechanism keys its state by. The surviving write-tool check reads cwd (`isFusionPluginCwd()` in `hooks/lib/self-detect.ts`), because `guard.ts` computes its verdict in cwd's space; churn asked about the workbench root it walked up to (`isFusionPluginRoot(dir)`), because its keys were anchored there. A root-anchored mechanism with a cwd-anchored stand-down misses every session started one directory down, and the reverse is the reverse hole. Both halves of that were measured on the protected-path measurement — whose stand-down had to move up to the root in v6.0.1 when its measurement root did, or a session started in `fusion-workbench/`, the ordinary case here, would have begun reverting the developer's own files. Two mechanisms and their stand-downs are gone; `hooks/lib/self-detect.ts` keeps both entry points so the rule stays available to whoever needs the root-anchored form next.

**After:**

> **The rule the two established outlives both of them, and it is what the next stand-down is measured against:** a stand-down is evaluated in the coordinate space the mechanism keys its state by. The write-tool check read cwd (`isFusionPluginCwd()`, deleted from `hooks/lib/self-detect.ts` on 260816), because `guard.ts` computed its verdict in cwd's space; churn asked about the workbench root it walked up to (`isFusionPluginRoot(dir)`), because its keys were anchored there. A root-anchored mechanism with a cwd-anchored stand-down misses every session started one directory down, and the reverse is the reverse hole. Both halves of that were measured on the protected-path measurement — whose stand-down had to move up to the root in v6.0.1 when its measurement root did, or a session started in `fusion-workbench/`, the ordinary case here, would have begun reverting the developer's own files. Two mechanisms and their stand-downs are gone. `hooks/lib/self-detect.ts` now keeps `isFusionPluginRoot(dir)` alone and with no caller, so the root-anchored form stays available to whoever needs it next, and `bin/fusion-plugin-cwd` is the only surviving implementation of the cwd-anchored form.

### L03 — the product bullet still sells a guard that blocks the write tools and halts after three blocks

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 17 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `hooks/guard.ts:1-21`: 'The hook decides nothing. It receives the four write tools (Write/Edit/MultiEdit/NotebookEdit) and Bash, allows every one of them... There is no second verdict. Every path through `main` writes `{}`.' Decision `shared/decisions/260809-1224_i_is-the-decision-governed-escalation-check-3-a-live-feature.md` `Implemented:` cites `2f624ca` and `fab8a4b`; `shared/decisions/260812-1232_i_does-the-escalation-counter-survive-a-block-source-that-ships-inert.md` `Implemented:` cites `9c79202`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The rewrite keeps all three past halves, which is what a reader of an older tree or an existing `events.jsonl` needs. No deleted path is named, because `reference-resolution-lint` resolves every path token in this file.

**Before:**

> - Compliance guard with escalation tracking (TypeScript hooks, compiled to `hooks/dist/`) — blocks the four write tools against the decision-governed categories at `high` sensitivity, and halts after three consecutive blocks. It used to block them against a `protectedPaths` list as well, and to measure those paths around every guarded tool call so a change reaching them any other way was written back; that whole half was removed on 260812. It also used to count per-file churn in a heatmap and emit warning events for the monitor at configured thresholds; that half warned, never blocked, and was removed on 260815

**After:**

> - Compliance guard, **observation-only since 260816** (TypeScript hooks, compiled to `hooks/dist/`) — it decides nothing. The PreToolUse hook receives the four write tools and Bash, allows every one of them, and exists for two products: one `guard_allow` row per write-tool call in `.guard-state/events.jsonl`, which is what the monitor's panel renders, and one `guard_advisory` per problem the configuration loader hands back. Every path through it writes `{}`; the name is historical and is kept because the event vocabulary, the state directory and the monitor panel all carry it (`hooks/guard.ts`, header). What it used to do went in three steps. It blocked the four write tools against a `protectedPaths` list and measured those paths around every guarded call so a change reaching them any other way was written back; that half was removed on 260812. It counted per-file churn in a heatmap and emitted warning events for the monitor at configured thresholds; that half warned, never blocked, and was removed on 260815. It blocked writes into the decision-governed categories at `high` sensitivity and halted after three consecutive blocks; that check, the halt, the consecutive-block counter, the escalation module and the halt-clearing script were removed on 260816

### L04 — the `hooks/` Layout row cites a deleted module, names the old configuration filename, and says two checks remain

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 29 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `cd hooks && npx vitest run lib/__tests__/reference-resolution-lint.test.ts` reports `CLAUDE.md:29 'hooks/lib/project-relative.ts' names a plugin file that does not exist in the tree` (deleted in `3c2e1c6`). `ls fusion.json templates/fusion.json` succeeds and no `fusion-guard.json` exists (`6890ea2`). 'Two checks remain: the halt, and the decision-governed categories' is false per `hooks/guard.ts:1-21`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** One of the four citations keeping `reference-resolution-lint` red.

**Before:**

> | `hooks/` | TypeScript source + compiled `dist/`. SessionStart, PreToolUse, PostToolUse hooks. Uses `process.cwd()` not `__dirname` for project-local state. SessionStart runs three independent commands: the `FUSION_PLUGIN_ROOT` export, the static "Fusion loaded" banner, and `session-start.ts` — which warns, as a `systemMessage`, when a workbench root is found *above* the working directory rather than at it. That warning fixes none of the cwd-anchored resolutions listed under `bin/fusion-plugin-cwd` and `hooks/lib/project-relative.ts`; it makes the assumption they share audible at the one moment the working directory is still cheap to change. The PreToolUse guard inspects `Bash` for **nothing** and allows it immediately, writing no guard state at all **when the project's `fusion-guard.json` is valid**. A broken or retired-key config emits one `guard_advisory` per guarded call, Bash included, because the diagnostic loop sits above the Bash return (`hooks/guard.ts`, and the comment there states the departure). The last policy that read a command's text — the git branch/worktree guard, with the shell lexer and command-word resolver under it — was deleted on 260809; the last thing that watched what a command *did* — the protected-path measurement, a fingerprint of the whole protected list taken in `guard.ts` before the call and compared again in `tracker.ts` after it, with anything that moved written back and the guard halted — was deleted on 260812 with the rest of that half. Two checks remain: the halt, and the decision-governed categories. Documented for users in `README-hooks.md`. |

**After:**

> | `hooks/` | TypeScript source + compiled `dist/`. SessionStart, PreToolUse, PostToolUse hooks. Uses `process.cwd()` not `__dirname` for project-local state. SessionStart runs three independent commands: the `FUSION_PLUGIN_ROOT` export, the static "Fusion loaded" banner, and `session-start.ts` — which warns, as a `systemMessage`, when a workbench root is found *above* the working directory rather than at it. One cwd-anchored resolution is left for that warning to be about, the work-tree preference of the three `bin/` helpers listed under `bin/fusion-plugin-cwd`; the two sharper ones it used to name went with the protected-path deny on 260812 and with the guard's last verdict on 260816. The warning fixes none of it, and makes the assumption audible at the one moment the working directory is still cheap to change. The PreToolUse guard inspects `Bash` for **nothing** and allows it immediately, writing no guard state at all **when the project's `fusion.json` is valid**. A broken file, a retired top-level key inside it, or a leftover `fusion-guard.json` at the project root each emit one `guard_advisory` per guarded call, Bash included, because the diagnostic loop sits above the Bash return (`hooks/guard.ts`, and the comment there states the departure). The last policy that read a command's text — the git branch/worktree guard, with the shell lexer and command-word resolver under it — was deleted on 260809; the last thing that watched what a command *did* — the protected-path measurement, a fingerprint of the whole protected list taken in `guard.ts` before the call and compared again in `tracker.ts` after it, with anything that moved written back and the guard halted — was deleted on 260812 with the rest of that half. **No check remains**: the halt and the decision-governed categories both went on 260816. Documented for users in `README-hooks.md`. |

### L05 — the whole guard-configuration Layout row describes a file, a merge and a key that no longer exist

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 30 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `reference-resolution-lint` reports `CLAUDE.md:30 'templates/fusion-guard.json'` and `CLAUDE.md:30 'hooks/config.json'`, both deleted in `6890ea2`. `hooks/lib/config.ts:138` is `export const PROJECT_CONFIG_FILENAME = "fusion.json";`; `:324` `RETIRED_PROJECT_FILES` and `:343` `RETIRED_TOP_LEVEL_KEYS` are the two retirement scopes; `guard.enabled` is retired inside its container per `circles/260816-1741-guard-becomes-observation-only/decisions/260816-1915_i_how-much-of-the-configuration-loader-survives-when-its-only-leaf-is-the-turn-budget.md` `Implemented:`. `hooks/lib/__tests__/config.test.ts:586` still reads `PROJECT_SET_KEYS = ["orchestrator"]`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Two of the four citations keeping the lint red. The row's structure is kept; every claim in it is re-derived from the tree.

**Before:**

> | `fusion-guard.json` + `templates/fusion-guard.json` | The per-project guard configuration. `/fusion:setup` seeds the template into a consuming project's root, git-tracked so every change to how strictly the guard reads that project shows in a diff; the root copy here equals the template outside the top-level keys a project is meant to set for itself, and that is exactly what `config.test.ts` pins: it cuts the keys named in its `PROJECT_SET_KEYS` out of both sides and holds every remaining byte identical. This repository sets one of them, its own Turn budget (`orchestrator.maxTurns`). The loader merges per **leaf** key — project, then plugin `hooks/config.json`, then built-in defaults — so a declared value (including `"categoryPaths": {}`) wins outright and an omitted or invalid one inherits. `guard.enabled` is the one key the project layer may not set (declaring it earns a diagnostic). `guard.protectedPaths` is the one **retired** key: the loader knows it, drops it, and emits an advisory naming it on every guarded call until the line is deleted — retired for both layers, unlike `enabled`, because a key that no longer exists does not exist for the plugin either. Nothing in the guard defends this file from an agent any more; the git diff is the only bound left, which is what the template's `_gitTracked` note now says. |

**After:**

> | `fusion.json` + `templates/fusion.json` | The per-project configuration, renamed from `fusion-guard.json` on 260816 when the last guard setting left it. `/fusion:setup` seeds the template into a consuming project's root, git-tracked so every change to it shows in a diff; the root copy here equals the template outside the top-level keys a project is meant to set for itself, and that is exactly what `config.test.ts` pins: it cuts the keys named in its `PROJECT_SET_KEYS` out of both sides and holds every remaining byte identical. This repository sets the one key that constant names, its own Turn budget (`orchestrator.maxTurns`), which is now the loader's only live leaf. The merge is per **leaf** key across **two** layers — this file, then the built-in `DEFAULTS` in `hooks/lib/config.ts` — so a declared value wins outright and an omitted or invalid one falls straight through to what fusion ships. The plugin layer that used to sit between them went with the guard settings it carried. Retirement now has two scopes rather than one leaf: `RETIRED_PROJECT_FILES` names a leftover `fusion-guard.json` at the project root and `RETIRED_TOP_LEVEL_KEYS` names `guard`, `decisions` and `escalation` inside the new file; either earns one advisory per guarded call until the file or the key is deleted, and the file advisory names `orchestrator.maxTurns` and the JSON to copy across, because a budget left behind is silently not read. Nothing in the guard defends this file from an agent; the git diff is the only bound left, which is what the template's `_gitTracked` note says. |

### L06 — `bin/fusion-plugin-cwd` described as the shell half of a deleted TypeScript function, and one consumer missing

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 36 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `sed -n '1,30p' bin/fusion-plugin-cwd` already says it: 'This is the ONLY cwd-anchored implementation of the criterion. It used to be the shell half of hooks/lib/self-detect.ts `isFusionPluginCwd()`, and that function was deleted on 2026-08-16'. `grep -rn 'fusion-plugin-cwd' bin/` returns three consumers, including `bin/fusion-source-root:89`, which this row does not name.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The helper's own header was corrected at plan step 4; this row is the copy that was left.

**Before:**

> | `bin/fusion-plugin-cwd` | Answers "is cwd the fusion plugin's own source repo?" — exit 0/1, prints nothing. The shell half of `hooks/lib/self-detect.ts` `isFusionPluginCwd()`: one criterion, two implementations, change one change the other (the pairing is commented in the helper). Consumed by `bin/fusion-rules` (work-tree rules preference) and `bin/fusion-paths` (work-tree prompt preference) — see the Rules-loading convention below. Checks `./.claude-plugin/plugin.json` **at cwd, with no upward walk** — deliberately congruent with the TS half, so from a subdirectory of this repo the helpers behave as in a consuming project. That used to happen *silently*; since the SessionStart warning (`hooks/session-start.ts`) a session started below the root says so on its first screen, naming both directories. The behaviour is unchanged — only its visibility. Added v5.x per decision `260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen.md` (option c, textschicht Circle). |

**After:**

> | `bin/fusion-plugin-cwd` | Answers "is cwd the fusion plugin's own source repo?" — exit 0/1, prints nothing. **The only cwd-anchored implementation of the criterion.** It was the shell half of `hooks/lib/self-detect.ts` `isFusionPluginCwd()` until that function was deleted on 260816 with the guard's stand-down; what survives on the TypeScript side is the root-anchored `isFusionPluginRoot(dir)`, which is the other coordinate space rather than a duplicate. The criterion is still kept consistent across the two, and the helper's own header carries the pairing and its residual imprecision. Consumed by `bin/fusion-rules` (work-tree rules preference), `bin/fusion-paths` (work-tree prompt preference) and `bin/fusion-source-root` (the root a skill body's citations of shipped files open against) — see the Rules-loading convention below. Checks `./.claude-plugin/plugin.json` **at cwd, with no upward walk** — deliberately congruent with the root-anchored form's criterion, so from a subdirectory of this repo the helpers behave as in a consuming project. That used to happen *silently*; since the SessionStart warning (`hooks/session-start.ts`) a session started below the root says so on its first screen, naming both directories. The behaviour is unchanged — only its visibility. Added v5.x per decision `260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen.md` (option c, textschicht Circle). |

### L07 — the Turn budget's home is named as `fusion-guard.json` and merged 'like every guard setting'

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 38 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `sed -n '1,40p' bin/fusion-turn-budget` names `fusion.json {"orchestrator": {"maxTurns": 12}}`. `./bin/fusion-turn-budget` prints `max_turns=12` reading the root `fusion.json`. There are no other guard settings: `hooks/lib/config.ts` has one live leaf (`circles/260816-1741-guard-becomes-observation-only/decisions/260816-1742_i_where-does-the-orchestrators-turn-budget-live-once-the-guard-configuration-file-is-gone.md` `Implemented:`).
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** One clause of a long row; everything else in it is unchanged and still true.

**Before:**

> | `bin/fusion-turn-budget` | The orchestrator's Phase-2 Turn budget, resolved from configuration and read once at Setup (`agents/orchestrator.md` Setup Step 2, `skills/setup/SKILL.md` Step 2) instead of the number being written into the prompt — it *was* written into it, `5`, in seven places and four spellings, one of which already called itself a default while no source could override it (issue `260811-1712`). Thin bash wrapper over `hooks/dist/turn-budget.js`, resolved relative to itself so an install copy and a work tree each run their own build. **Its own header carries the authoritative usage block** — the `KEY=value` line it prints and the exit-code table are spelled there, and this row deliberately does not restate them. A project sets its own budget with `{"orchestrator": {"maxTurns": N}}` in `fusion-guard.json`, merged per leaf like every guard setting; the default is defined once, in `hooks/lib/config.ts` `DEFAULTS`. **The thing a reader gets wrong:** a failed read is a *state*, not a number to fill in. The orchestrator substitutes none — it writes no number anywhere in the budget's place (there is nowhere to write one: `agentstate.yaml` stopped carrying a `max_turns` field on 2026-08-15 with the rest of its counters), shows the dashboard's Turn field as `<current>/--`, treats the Max-Turns circuit breaker as not evaluable and says so, and bounds the loop by asking the user at every Turn boundary instead (`agents/orchestrator.md` Setup Step 2, Step 3d). |

**After:**

> | `bin/fusion-turn-budget` | The orchestrator's Phase-2 Turn budget, resolved from configuration and read once at Setup (`agents/orchestrator.md` Setup Step 2, `skills/setup/SKILL.md` Step 2) instead of the number being written into the prompt — it *was* written into it, `5`, in seven places and four spellings, one of which already called itself a default while no source could override it (issue `260811-1712`). Thin bash wrapper over `hooks/dist/turn-budget.js`, resolved relative to itself so an install copy and a work tree each run their own build. **Its own header carries the authoritative usage block** — the `KEY=value` line it prints and the exit-code table are spelled there, and this row deliberately does not restate them. A project sets its own budget with `{"orchestrator": {"maxTurns": N}}` in `fusion.json` at its root, merged per leaf over the built-in defaults; since 260816 that is the loader's only live setting, and the default is defined once, in `hooks/lib/config.ts` `DEFAULTS`. **The thing a reader gets wrong:** a failed read is a *state*, not a number to fill in. The orchestrator substitutes none — it writes no number anywhere in the budget's place (there is nowhere to write one: `agentstate.yaml` stopped carrying a `max_turns` field on 2026-08-15 with the rest of its counters), shows the dashboard's Turn field as `<current>/--`, treats the Max-Turns circuit breaker as not evaluable and says so, and bounds the loop by asking the user at every Turn boundary instead (`agents/orchestrator.md` Setup Step 2, Step 3d). |

### L08 — the `docs/` row enumerates three files and the tree holds four

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 50 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `ls -1 docs/` -> `philosophy.md`, `upgrading-to-v10.md`, `upgrading-to-v9.md`, `working-model.md`. `docs/upgrading-to-v10.md` landed at plan step 12 (`18c125b`).
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Derive over correct: the row already calls `ls docs/` authoritative and calls itself not authoritative, and the `templates/` row directly above it is the worked precedent for refusing to hold an inventory. The proposal drops the inventory rather than adding a fourth name to it, and keeps the two facts `ls` cannot produce.

**Before:**

> | `docs/` | Conceptual docs that are useful both inside Claude Code (pointed at by skills) and as standalone reading. Currently: `philosophy.md` (the load-bearing ideas + domain parameter, pointed at by `/fusion:help`), `working-model.md` (how the working model operates — Circle flow, gates, guard) and `upgrading-to-v9.md` (what an installed base upgrading over the v9 removals still holds on disk, and which of it to delete — the first per-release note this repository has ever shipped, written because v9 removed mechanisms consuming projects had *configured*; it is pointed at from `README.md` `## Install` and from `/fusion:help`'s update topic). A fourth, the Plane bridge install doc, went with the mirror on 2026-08-15. `ls docs/` is the authoritative set, and it is authoritative because this row is not: no lint gate reads a bare filename in it. |

**After:**

> | `docs/` | Conceptual docs that are useful both inside Claude Code (pointed at by skills) and as standalone reading. **The set is deliberately not written here.** Run `ls docs/`. An inventory stood in this row and named three files while the tree held four, invisibly: no lint gate resolves a bare filename here, which is the same hole the `templates/` row above records. Two things the command cannot tell you and this row therefore keeps. `philosophy.md` carries the load-bearing ideas and the domain parameter and is what `/fusion:help` points at. The `upgrading-to-vN.md` files are per-release migration notes, written only when a release removes something an installed base already has on disk — v9 was the first this repository ever shipped, and each is pointed at from `README.md` `## Install` and from `/fusion:help`'s update topic. A Plane bridge install doc went with the mirror on 2026-08-15. |

### L09 — the always-on-rules bullet says a halt is still reachable and names the clear-halt command

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 62 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `ls hooks/dist/` -> no `clear-halt.js`; `ls hooks/` -> no `clear-halt.ts`; both deleted in `9c79202`. `hooks/guard.ts:1-21` has no verdict branch, so no CHECK 3 block can be counted. `hooks/lib/__tests__/legacy-halt-clearing.test.ts:1-27` states all three of the old properties are now false and that the remedy is `/fusion:setup`'s offer.
- **Consequence group:** constraint removal
- **Constraint removed:** the `cd <project-root> && node <plugin-root>/hooks/dist/clear-halt.js` invocation string, and the statement that a legacy halt still blocks. The script does not exist and the flag blocks nothing; the surviving remedy is named in its place and in rows :135 and :136.
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Listed first because it removes a named remedy. It is a preserve-list category 3 item (a non-obvious failure mode), and it is offered only because the mechanism itself is verifiably gone rather than because the text reads long.

**Before:**

> - **No guard rule is emitted any more.** The always-on `protected-path-discipline.md` (under `rules/` until 260812) was the agent-facing half of the protected-path measurement and went with it, as its sibling rule for the branch policy had gone on 260809. It was 10 541 bytes on every one of the sixteen agents' every dispatch. **Measured on 260812, the day it went:** the always-on floor stood at 98 443 bytes per dispatch before the removal, 91 090 of shipped rule text plus this project's 7 353-byte chat voice profile, and the step took 10 420 off that rather than the full 10 541, because it also had to correct a present-tense claim in `rules/critical-stance.md` that the removal made false, at +121 bytes. **The floor as it stands today is deliberately not stated here.** It moves with every rule edit, so any number written into this line is stale before it is committed: by 260813 the two that stood here had each drifted 5 796 bytes low. Measure it when you need it, with `wc -c` over the always-on set — the **unindented** `emit_if_exists` lines in `bin/fusion-rules`, plus the project's chat voice profile. The indented ones are conditional emissions and are not part of the floor; the same `wc -c` over those three files gives what they add, which has been about a third again. In this repository `bin/fusion-rules coder | xargs wc -c` prints the same total in one command, because `coder` draws no conditional rule. **Consequence worth knowing:** no rule now tells a sub-agent what a halt is or how it is cleared. A halt is still reachable — three consecutive CHECK 3 blocks — and a legacy halt from the removed mechanism still blocks and still clears; what an agent has is the block message itself, which names the full `cd <project-root> && node <plugin-root>/hooks/dist/clear-halt.js` command. **The always-on set is an explicit `emit_if_exists` list in `bin/fusion-rules`** (not a pattern match), so a new always-on rule needs a line there or it will not load.

**After:**

> - **No guard rule is emitted any more.** The always-on `protected-path-discipline.md` (under `rules/` until 260812) was the agent-facing half of the protected-path measurement and went with it, as its sibling rule for the branch policy had gone on 260809. It was 10 541 bytes on every one of the sixteen agents' every dispatch. **Measured on 260812, the day it went:** the always-on floor stood at 98 443 bytes per dispatch before the removal, 91 090 of shipped rule text plus this project's 7 353-byte chat voice profile, and the step took 10 420 off that rather than the full 10 541, because it also had to correct a present-tense claim in `rules/critical-stance.md` that the removal made false, at +121 bytes. **The floor as it stands today is deliberately not stated here.** It moves with every rule edit, so any number written into this line is stale before it is committed: by 260813 the two that stood here had each drifted 5 796 bytes low. Measure it when you need it, with `wc -c` over the always-on set — the **unindented** `emit_if_exists` lines in `bin/fusion-rules`, plus the project's chat voice profile. The indented ones are conditional emissions and are not part of the floor; the same `wc -c` over those three files gives what they add, which has been about a third again. In this repository `bin/fusion-rules coder | xargs wc -c` prints the same total in one command, because `coder` draws no conditional rule. **Consequence worth knowing, and since 260816 it has shrunk to nothing:** no rule tells a sub-agent what a halt is or how it is cleared, and there is no longer anything to tell. No halt is reachable, the counter that raised one and the script that cleared one are both gone, and a legacy flag a consuming project still carries blocks nothing — the two migration rows at the end of this document say what to do about it. **The always-on set is an explicit `emit_if_exists` list in `bin/fusion-rules`** (not a pattern match), so a new always-on rule needs a line there or it will not load.

### L10 — the single-orchestrator advisory lists a race on a counter that no longer exists

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 68 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `grep -rn 'escalation.json' hooks/lib hooks/*.ts` returns only past-tense comments; `hooks/lib/guard-state-file.ts:82-84` states `escalation.json` 'was the third until 2026-08-16 and is written by nothing at this version'.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The advisory itself, its mechanism and its three other consequences are untouched. Only the impossible one goes.

**Before:**

> - **Single orchestrator per project** (advisory) — fusion has no concurrency lock. Two orchestrators running against the same project simultaneously can corrupt `agentstate.yaml` (interrupted-session resume becomes unreliable), double-dispatch the same work, race on the `.guard-state/escalation.json` counter, and produce interleaved commit history. The most damaging mode is two parallel sessions each completing half of the same task with overlapping edits. Mitigation since v2.8.0: `/fusion:setup` Step 0c checks for an active-session marker (`fusion-workbench/.session-marker` whose mtime is refreshed at each orchestrator Turn) and **warns** the user when one is detected — `running` if heartbeat ≤ 10 min ago, `stale` if older. The warning is advisory; the user can still proceed but takes responsibility for sequencing.

**After:**

> - **Single orchestrator per project** (advisory) — fusion has no concurrency lock. Two orchestrators running against the same project simultaneously can corrupt `agentstate.yaml` (interrupted-session resume becomes unreliable), double-dispatch the same work, and produce interleaved commit history. The most damaging mode is two parallel sessions each completing half of the same task with overlapping edits. Mitigation since v2.8.0: `/fusion:setup` Step 0c checks for an active-session marker (`fusion-workbench/.session-marker` whose mtime is refreshed at each orchestrator Turn) and **warns** the user when one is detected — `running` if heartbeat ≤ 10 min ago, `stale` if older. The warning is advisory; the user can still proceed but takes responsibility for sequencing.

### L11 — release step 0 justifies its off-repository guard verification by a stand-down that was dissolved

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 83 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 2
- **Citation:** `shared/decisions/260812-1232_i_does-the-write-guards-fusion-repo-stand-down-survive-the-loss-of-its-subject.md`. Its option 2 names this very sentence ('the special case is the one the project's own `CLAUDE.md` warns makes local testing unrepresentative'), and its `Implemented:` line records option 3, dissolution, realised in `2f624ca` and `ec3b6ad`. The overturned sentence is the trailing clause of `CLAUDE.md:83`.
- **Consequence group:** tier-2
- **Constraint removed:** none — the verification step is kept verbatim; only its stated reason is replaced.
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** This is a preserve-list category 1 item (release flow). It is offered because it is a Tier 2 change with an explicit superseding record, which is the single exception, and because it removes no constraint. Whether the step survives after plan step 15 establishes the identity it now asserts is a question this entry does not close.

**Before:**

> 0. **Validate first:** `claude plugin validate .` must report **passed** (warnings are fine). This catches broken agent frontmatter before it ships — e.g. an unquoted `:` in a `description` makes the whole frontmatter fail to parse and the agent loads with empty metadata. Also run a smoke test that the default agent resolves under the install mechanism: `claude --plugin-dir . --agent fusion:orchestrator -p "reply SMOKE-OK"` (a wrong agent name aborts Claude immediately at startup). If the release touches the guard, also confirm before tagging that its behaviour was verified against a project root that is **not** this repository (the guard-harness integration tests spawn such roots; a scratch consuming project works too) — the write guard's self-detect stand-down here makes local testing unrepresentative by construction.

**After:**

> 0. **Validate first:** `claude plugin validate .` must report **passed** (warnings are fine). This catches broken agent frontmatter before it ships — e.g. an unquoted `:` in a `description` makes the whole frontmatter fail to parse and the agent loads with empty metadata. Also run a smoke test that the default agent resolves under the install mechanism: `claude --plugin-dir . --agent fusion:orchestrator -p "reply SMOKE-OK"` (a wrong agent name aborts Claude immediately at startup). If the release touches the guard, also confirm before tagging that its behaviour was verified against a project root that is **not** this repository (the guard-harness integration tests spawn such roots; a scratch consuming project works too). **The reason for that step changed on 260816 and the step did not.** Until then the write guard's self-detect stand-down made local testing unrepresentative by construction; the stand-down is gone, so the hook is supposed to behave identically in both trees. The check is what establishes that identity against a real consuming root instead of asserting it.

### L12 — the installer asset list omits `LICENSE`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 107 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `sed -n '82,83p' install.sh` -> `for item in .claude-plugin agents skills rules hooks bin stilwerk templates docs \` / `README.md README-agents.md README-hooks.md LICENSE; do`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Derive over correct: the list is a copy of a loop in `install.sh`, so the proposal cites the loop instead of restating it. The sentence's actual point, that no permission file is among the copied assets, needs no list at all.

**Before:**

> - The installer copies plugin assets (`.claude-plugin agents skills rules hooks bin stilwerk templates docs README*.md`), and **no permission file is among them, because a plugin's own permission settings are not read under `--plugin-dir`.** Measured on Claude Code 2.1.226 by placing one identical `permissions.allow` entry in each of two locations: a project's `.claude/settings.local.json` honoured it, a plugin's own did not. fusion shipped such a file at its root for months, sixteen scoped auto-allows granting nothing on either install path; it was deleted on 2026-08-15 rather than annotated, so nothing in the tree looks like a grant while being inert. A fusion session's permissions come entirely from the consuming project's `.claude/` settings, which `/fusion:setup` Step 0g offers once to seed (defect `fusion-workbench/shared/issues/260810-0326_*_setup-must-seed-claude-settings-because-the-plugin-settings-json-is-not-a-permission-source.md`, decision `260815-0029_*_what-permission-grant-does-setup-seed-when-unlock-becomes-a-setup-step`). **Seed bare tool names only.** The same measurement found every directory-scoped form denied — `Write(fusion-workbench/**)` and each variant of it — even from a settings file that *is* read, so a scoped pattern grants nothing wherever it is written. The installer never copies `CLAUDE.md` or `.gitignore` (dev-only), and defensively drops `hooks/node_modules`.

**After:**

> - The installer copies plugin assets — the set is the copy loop in `install.sh`, and that loop is the authority rather than a list here — and **no permission file is among them, because a plugin's own permission settings are not read under `--plugin-dir`.** Measured on Claude Code 2.1.226 by placing one identical `permissions.allow` entry in each of two locations: a project's `.claude/settings.local.json` honoured it, a plugin's own did not. fusion shipped such a file at its root for months, sixteen scoped auto-allows granting nothing on either install path; it was deleted on 2026-08-15 rather than annotated, so nothing in the tree looks like a grant while being inert. A fusion session's permissions come entirely from the consuming project's `.claude/` settings, which `/fusion:setup` Step 0g offers once to seed (defect `fusion-workbench/shared/issues/260810-0326_*_setup-must-seed-claude-settings-because-the-plugin-settings-json-is-not-a-permission-source.md`, decision `260815-0029_*_what-permission-grant-does-setup-seed-when-unlock-becomes-a-setup-step`). **Seed bare tool names only.** The same measurement found every directory-scoped form denied — `Write(fusion-workbench/**)` and each variant of it — even from a settings file that *is* read, so a scoped pattern grants nothing wherever it is written. The installer never copies `CLAUDE.md` or `.gitignore` (dev-only), and defensively drops `hooks/node_modules`.

### L13 — the SessionStart-warning troubleshooting row cites a deleted module and a deleted deny

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 129 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `reference-resolution-lint` reports `CLAUDE.md:129 'hooks/lib/project-relative.ts'`, deleted in `3c2e1c6`. `hooks/session-start.ts:1-38` now names exactly one surviving cwd-anchored resolution, the work-tree preference of the three `bin/` helpers, and records both removals.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The last of the four citations keeping the lint red. The replacement names no deleted path.

**Before:**

> | SessionStart prints "fusion: restart this session at the project root" | Working as designed: a `fusion-workbench/.fusion-setup` marker was found in an ancestor of the working directory rather than at it. Several fusion checks resolve against cwd — the write-tool PreToolUse deny (`hooks/lib/project-relative.ts`) and the plugin-repo detection (`hooks/lib/self-detect.ts`, `bin/fusion-plugin-cwd`) — so from a subdirectory they inspect the wrong directory. Restart at the named root. The warning itself changes no behaviour; the residual is tracked in `260804-2100_*_from-a-subdirectory-cwd-the-protected-list-matches-nothing-while-fail-closed-still-denies.md`. |

**After:**

> | SessionStart prints "fusion: restart this session at the project root" | Working as designed: a `fusion-workbench/.fusion-setup` marker was found in an ancestor of the working directory rather than at it. One fusion check still resolves against cwd — the plugin-repo detection in `bin/fusion-plugin-cwd`, which the three work-tree-preferring `bin/` helpers ask — so from a subdirectory it inspects the wrong directory and all three read the installed plugin copy instead of the work tree. Restart at the named root. The warning itself changes no behaviour. Two sharper instances stood here and are gone: the protected-path deny on 260812, and on 260816 the write-tool PreToolUse path computation together with the module that performed it. The issue that tracked the first, `260804-2100_*_from-a-subdirectory-cwd-the-protected-list-matches-nothing-while-fail-closed-still-denies.md`, is moot rather than fixed. |

### L14 — the legacy-halt migration row says the flag still blocks at CHECK 1 and clears through `clear-halt.js`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 135 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `hooks/lib/__tests__/legacy-halt-clearing.test.ts:5-27`: 'its subject then was that a halt raised by the vanished mechanism still loaded, still blocked at CHECK 1, and still cleared through `clear-halt.js`. Every one of those three is now false'. The file now pins that nothing blocks, nothing rewrites the file, and `/fusion:setup` offers to delete it.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The row's purpose, telling a migrating project what its leftover flag means, is kept and made true.

**Before:**

> | A consuming project is halted and nothing at HEAD could have raised it | Expected, and it is the migration case. A halt raised by the removed mechanism is a value in that project's `escalation.json`, and it outlives the code that wrote it. It still blocks at CHECK 1 and still clears through `clear-halt.js`; the path is pinned by `hooks/lib/__tests__/legacy-halt-clearing.test.ts` against both triggers no surviving code produces (`protected_path`, `protected_path_measured`). |

**After:**

> | A consuming project is halted and nothing at HEAD could have raised it | Expected, and it is the migration case. A halt raised by a removed mechanism is a value in that project's `escalation.json`, and it outlives the code that wrote it. At this version it blocks nothing: the halt check, the consecutive-block counter, the escalation module and the halt-clearing script all went on 260816, and no shipped code reads or rewrites the file. `/fusion:setup` offers to delete it. All three properties — nothing blocks, nothing rewrites the file, the offer exists — are pinned by `hooks/lib/__tests__/legacy-halt-clearing.test.ts` against both triggers no surviving code produces (`protected_path`, `protected_path_measured`). |

### L15 — the advisory troubleshooting row names only the retired leaf, and the retirement moved to two other scopes

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, line 136 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `hooks/lib/config.ts:324` `RETIRED_PROJECT_FILES` (the file `fusion-guard.json`) and `:343` `RETIRED_TOP_LEVEL_KEYS` (`guard`, `decisions`, `escalation`); `:100` records that `guard.protectedPaths` 'now sits inside a retired container, so the' leaf table folded away. `templates/fusion.json` `_retired` states the same migration to a user.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The row keeps its shape and gains the half that carries the whole v10 migration: copy the Turn budget before deleting the old file.

**Before:**

> | A consuming project sees an advisory on every tool call naming `guard.protectedPaths` | Working as designed. That project's `fusion-guard.json` still declares the retired key. Delete the line; nothing else in the file is affected while it is there. |

**After:**

> | A consuming project sees an advisory on every tool call naming `fusion-guard.json`, or one naming `guard`, `decisions` or `escalation` | Working as designed, and the two are the v10 migration. The first says that project's root still carries `fusion-guard.json`, which fusion no longer reads a byte of: if that file set a Turn budget, copy `{"orchestrator": {"maxTurns": <n>}}` into `fusion.json` **first**, because a budget left behind is silently not applied, then delete the old file. The second says the new file still carries one of the three retired top-level keys; delete the key. Nothing else in either file is affected while the advisory stands. `docs/upgrading-to-v10.md` states both at length. |

### L16 — `escalation.json` is classified as a live counter under `.guard-state/`

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, line 79 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `hooks/lib/guard-state-file.ts:82-84`: 'The files that DO route through here are the two measurement throttle records, and they are the whole of it: `escalation.json` was the third until 2026-08-16 and is written by nothing at this version.' `grep -rn 'escalation.json'` over `hooks/lib` and `hooks/*.ts` finds no writer.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Note:** Cross-surface contradiction as well as a falsified claim: `skills/archive/SKILL.md:94` and `:142` were corrected at plan step 11 and now say the file is inert, while this rule file, which the skill's classification derives from, still calls it live. The rule file is the authoring home, so it is the side that is wrong.

**Before:**

> **`.guard-state/` is not one thing, and the directory is the wrong unit to classify.** The counters and throttle stores in it — `escalation.json` and the measurement throttle records — are rewritten in place and a past version of them answers nothing, so they are live state. `events.jsonl` beside them is appended to and never rewritten, and a past version answers a great deal: which tool call the guard blocked, when it halted, when a human cleared the halt. It is classified by what it is rather than by the directory it sits in, and it is a **record** (decision `260811-1534_*_does-the-guard-event-log-get-an-upper-bound-and-what-happens-to-the-evidence-in-it.md`).

**After:**

> **`.guard-state/` is not one thing, and the directory is the wrong unit to classify.** The throttle stores in it are the two measurement throttle records, and they are the whole of it: they are rewritten in place and a past version of them answers nothing, so they are live state. `escalation.json` was the third until 2026-08-16 and is written by nothing at this version (`hooks/lib/guard-state-file.ts`); a workbench set up under an older fusion may still hold an inert copy beside them, which nothing reads and `/fusion:setup` offers to delete. `events.jsonl` beside them is appended to and never rewritten, and a past version answers a great deal: which tool call the guard blocked, when it halted, when a human cleared the halt. No new row of those three kinds can arrive now that the guard decides nothing, which is a reason to keep the ones already written rather than to reclassify the log. It is classified by what it is rather than by the directory it sits in, and it is a **record** (decision `260811-1534_*_does-the-guard-event-log-get-an-upper-bound-and-what-happens-to-the-evidence-in-it.md`).

### L17 — the exempt-surfaces bullet illustrates itself with two operator strings this Circle removed

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, line 266 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** `circles/260816-1741-guard-becomes-observation-only/issues/260816-2115_o_the-why-the-message-is-english-argument-names-two-removed-mechanisms-as-its-examples.md`, filed by the coder executing plan step 4. The guard emits no deny reason (`hooks/guard.ts:1-21`, every leaf an allow) and there is no halt notice (`9c79202` deleted the halt and the clearing script). The replacements are live: the configuration advisories in `hooks/lib/config.ts` and the tracker's review-coverage and staging-drift notices.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Note:** PAIRED. Apply only in the same change as the `hooks/session-start.ts` half, which is coder work and outside this agent's remit. The filed issue's own instruction is that one surface fixed with its authoring home left stale is worse than both being stale. Approve L17 and L18 together or neither.

**Before:**

> - hook and CLI operator strings — banners, deny reasons, halt notices, helper usage and error text.

**After:**

> - hook and CLI operator strings — banners, configuration advisories, measurement notices, helper usage and error text.

### L18 — the worked-case sentence restates a count of a set that has shrunk three times

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`, line 268 (whole line; the apply pass matches the full line, so a byte that moves invalidates the entry rather than being silently patched)
- **Tier:** 1
- **Citation:** Same issue record as L17. The count 'fifteen' was taken before 2026-08-12; the set shrank on 2026-08-12, 2026-08-15 and 2026-08-16.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md`
- **Note:** PAIRED with L17 and with the same coder-owned `hooks/session-start.ts` edit. Derive over correct: the number is dropped rather than re-measured, on the reasoning `CLAUDE.md:62` already applies to its always-on byte floor.

**Before:**

> `hooks/session-start.ts` `## Why the message is English` is the worked case: a hook fires before any agent has read `CLAUDE.md`, and localising one operator string while the other fifteen stay English is the inconsistency, not the fix.

**After:**

> `hooks/session-start.ts` `## Why the message is English` is the worked case: a hook fires before any agent has read `CLAUDE.md`, and localising one of fusion's operator strings while the rest stay English is the inconsistency, not the fix. The count that stood in this sentence is deliberately not restated: the set has shrunk three times since it was measured, so a number written into prose about it is stale before it is committed.


## 7. Outcomes

**Apply pass, 2026-08-17.** Dispatched with `**Mode:** apply`, this file as `**Ledger:**`, and all 18 ids as `**Approved:**`. The user confirmed the 26.3 percent rewrite of `CLAUDE.md` in a separate, earlier answer, before the ledger counts were shown, which is the order `## 3` asked for.

**Staleness check before any write.** `git rev-parse HEAD` returned `c65e1cf4678bb85ae359f132a665514b715a37a3`, the HEAD the survey read, and no commit had landed in the interval. Each entry's before-text was re-read from disk at its recorded line number and compared byte for byte. **All 18 matched, and each before-text occurred exactly once in its file**, so no entry was applied against an ambiguous match. Nothing was marked `stale`.

| Id | Outcome | Note |
|---|---|---|
| L01 | applied | |
| L02 | applied | |
| L03 | applied | |
| L04 | applied | |
| L05 | applied | |
| L06 | applied | |
| L07 | applied | |
| L08 | applied | |
| L09 | applied | The one constraint removal. Approved with the removed remedy named. |
| L10 | applied | |
| L11 | applied | The one Tier 2 entry. |
| L12 | applied | |
| L13 | applied | |
| L14 | applied | |
| L15 | applied | |
| L16 | applied | |
| L17 | applied | Paired with L18; both applied, so the pairing holds. |
| L18 | applied | Paired with L17. |

**18 applied, 0 skipped, 0 stale, 0 failed.** No write was denied.

**Measured after, against the prediction in `## 2`.**

| Surface | Bytes before | Bytes after | Predicted after | Lines before | Lines after |
|---|---|---|---|---|---|
| `CLAUDE.md` | 58 372 | 61 708 | 61 708 | 137 | 137 |
| `rules/fusion-workbench-conventions.md` | 55 184 | 55 835 | 55 835 | 548 | 548 |
| Decision records | 428 913 | 428 913 | 428 913 | 5 592 | 5 592 |

Both predictions are exact. `git diff --stat` over the two files reports **18 insertions and 18 deletions** at the 18 recorded line numbers, which is one whole-line replacement per entry and confirms that no line was added or removed. **Removals by tier: one, L09, at Tier 1**, and its removed constraint is named in its own entry and again here in one searchable line: *the `cd <project-root> && node <plugin-root>/hooks/dist/clear-halt.js` invocation string, and the statement that a legacy halt still blocks*. Neither the script nor the halt exists at this version.

**Revert path for the whole run:** `git checkout -- CLAUDE.md rules/fusion-workbench-conventions.md`. Both files are git-tracked, so the pre-edit bytes are recoverable in full. No decision record was touched, which is why `## 5` is empty.

**Nothing was committed.** The working tree carries the edits; committing is the user's or the orchestrator's.

### Citation lint after the apply

`cd hooks && npx vitest run lib/__tests__/reference-resolution-lint.test.ts` — **33 of 34 pass, and all four dangling citations this run set out to fix are resolved.** Verified independently of the test as well: `hooks/lib/project-relative.ts`, `templates/fusion-guard.json`, `hooks/config.json` and `hooks/dist/clear-halt.js` now occur **zero** times in either edited file.

The one remaining failure is the gate's **count pin**, not a broken reference: `paths` moved 1 112 → 1 120 while `anchors` (139) and `records` (94) held. The eight are citations the replacement text added — among them `fusion.json`, `templates/fusion.json`, `bin/fusion-source-root`, `docs/upgrading-to-v10.md` and `hooks/lib/guard-state-file.ts` — every one of which resolves. Re-approving that baseline is **coder work** and is recorded as item 5 of `## 8`.

## 8. Work this agent may not do, and who owns each

Four items. None is a ledger entry, and three of the four already have an owner on record, so no new defect record was filed.

1. **`hooks/session-start.ts` `## Why the message is English`** — the same two dead examples and the same stale count as L17 and L18, in the file the rule cites as its worked case. `hooks/` is outside this agent's remit (exclusion 6). **Executor: `coder`.** Already filed as `circles/260816-1741-guard-becomes-observation-only/issues/260816-2115_o_the-why-the-message-is-english-argument-names-two-removed-mechanisms-as-its-examples.md`, which states the constraint this run inherits: both files in one change, or neither. **L17 and L18 must land in the same commit as this edit.**

2. **`hooks/lib/__tests__/fixtures/rules-emission.golden` will go red if L16, L17 or L18 is applied.** The golden pins the byte size of every always-on rule file, and the conventions file moves 55 184 → 55 835. This is the fixture case rather than the out-of-remit case: the edit is in remit and approved, only its consequence is somebody else's. Regenerate with `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts`, then review the diff. **Executor: `coder`. This agent does not run it.** The growth *bound* is not at risk and this is measured, not assumed: the always-on set stands at 90 370 bytes against a `RULE_BASELINE` of 86 573, so growth is 3 797 of the 12 000 head-room, and +651 takes it to 4 448. **Confirmed after the apply**, by running that file read-only: 14 of its 15 tests pass, the one failure is the byte-size golden alone (`fusion-workbench-conventions.md 55184` → `55835`, `[analyst] total 95204` → `95855`), and the growth-bound assertion inside the same file is among the passing 14. So the head-room held and only the golden needs regenerating.

3. **`.claude-plugin/plugin.json`'s `description` still sells "a compliance guard".** Not one of the three surfaces. Already owned by plan step 14, whose Changes line says to rewrite it "if the guard appears in it" and to read it against the marketplace entry. No action needed here.

4. **`hooks/lib/__tests__/fixtures/surface-growth.golden` is stale** — it still carries entries for `clear-halt-concurrent-halt.test.ts`, `escalation.test.ts`, `guard-escalation-shape.test.ts`, `guard-halt-event.test.ts` and `project-relative.test.ts`, all deleted. Step 10's, and named out of bounds by this run's dispatch. Untouched.

5. **`hooks/lib/__tests__/reference-resolution-lint.test.ts`'s `BASELINE` needs re-approving.** Found by the apply pass, not by the survey, so it is not in any ledger entry. It is the same fixture case as item 2 rather than an out-of-remit change: the edits were in remit and approved, and only the consequence is somebody else's. The gate's own message says re-approving is the expected response to a legitimate change. There is no regeneration command — the constant is hand-edited — so the change is `paths: 1112` → `paths: 1120`, with `anchors: 139` and `records: 94` unchanged. **Executor: `coder`, in the same commit as the edits that moved the count. This agent does not edit it** (`hooks/` is exclusion 6).

**Two failures in `lib/__tests__/surface-growth-bound.test.ts` were red before this pass and are untouched by it**, which the apply pass confirmed by running it read-only: five baseline entries for deleted hook tests, and the `agents` surface golden, where `orchestrator.md` reads 144 634 against a golden of 144 832. This run edited no file under `agents/`, `skills/` or `hooks/`, so neither failure can have come from it. Both are step 10's, per item 4.

**Nothing in this run changes the size of `agents/` or `skills/`.** Step 10 re-arms against a tree in which those two surfaces are byte-identical to what it would have measured before this pass. The hook-tests surface is likewise untouched. Only the always-on rule set moves, and only if L16, L17 or L18 is approved.

## 9. Confidence, stated per the critical-stance rule

- **Verified**, with the command and its output in each entry's `Citation` field: every Tier 1 claim below. Each rests on a `grep`, an `ls`, a `sed` over the file it cites, or the recorded output of `reference-resolution-lint.test.ts`.
- **Inference**, and labelled: that L11's replacement is the right shape. That the release step's stated reason is now false is verified; that the step itself should be *kept* while its basis is re-established at plan step 15 is a reading of the plan's step 15, not a recorded answer. If the user prefers the step to go, that is a different entry and this run does not make it.
- **Inference**, and labelled: that `rules/fusion-workbench-conventions.md:79`'s classification of `escalation.json` is the wrong side of the contradiction with `skills/archive/SKILL.md`. The rule file is the authoring home, so the skill derives from it and not the reverse — that is the reasoning, and no record states it in those words.
- **No speculation is carried in any entry.** Every reading of two prose passages as a supersession is L11's alone, and that one names both the record and the sentence it overturns.

## 10. Where the ledger's own reach stops

45 of 58 decision records were not opened. 272 session histories were not opened. 29 reviews and 22 analyses were not opened. The archive is empty, so it hid nothing. If a stale normative statement in this project sits outside the guard-removal vocabulary, this run did not look for it and does not claim it is not there.
