A coder ran `git reset --hard` in the live tree and an hour of uncommitted event-log rows is gone
---
During step 18 of `260921-1726_*_the-33-open-defects-of-the-11-9-1-survey-worked-autonomously-as-one-package.md` a scratch-repository measurement ran in `/Users/k1/Projects/productive/fusion` instead: a `sed` in an `&&` chain failed, the `cd` into the scratch repository never happened, and the commands that followed (an empty commit, `git reset --hard HEAD~1`, `git commit --amend` on `fcff85b8`, two branches, a cherry-pick) ran against the project. The coder repaired the commit graph before landing the step (`git reset --soft fcff85b8`, branches deleted, tree verified identical) and reported it. What the repair could not restore: every row appended to `fusion-workbench/orchestrator-events.jsonl` between its last commit (`73c11cfd`, about 18:02) and the repair (about 19:03), namely the lock-written `commit` rows of `42bed688`, `3db8e1a3`, `7c1d88e8`, `65ac72a5`, `19f7013f`, `d0ab9fae`, `a666cce2`, `32de95e1`, `b3c9047b`, `54335317`, `205e3e35`, `3342778b`, `a8b5bcd2`, `75f0c5d2`, `1906ceb3`, `fcff85b8` and the hook-written `task_start`/`task_done` rows of the three dispatches in that window. The log is the only cross-session trace fusion keeps, and those rows are the measurement corpus for the review-coverage and gate greps; the sixteen commits are still in git, so the loss is the rows, not the work.

Two facts, and the second is the defect. The dispatch prompt forbade every whole-tree git command by name, and the prohibition did not hold: a prompt line is the whole of the protection, and it lost to a shell chain whose failure mode (a missing `cd`) the author did not foresee. And a row the lock or a hook writes lives only in the working tree until somebody names the log in a staging list; the orchestrator stages it on its own commits, but a package whose executor commits per step under the lock leaves the log unstaged for as long as the package runs, so the exposure window is the package's length.

Acceptance: either every commit under the lock stages the event log (the `with` form appends its row and could add the log's path to the same index before `git commit`, which closes the window to zero for lock-written rows), or the conventions state that an executor committing under the lock names the log in every staging list; and the orchestrator's dispatch prompt template says that a scratch repository is entered with an absolute `cd` on its own line, verified by `pwd`, before any measurement that resets, amends or cherry-picks. Evidence: the coder's own report in this session; `git reflog` at `10124e3a` carries the stray objects.
---
**Filed by:** orchestrator, Kai Stalmann <ks@qantr.com>
Filed at 260921-1931 during the package's Package C run; the gap is between `73c11cfd` and `10124e3a`.
---
Resolved: the commit that carries this line adds two bullets to the dispatch-prompt list in `agents/orchestrator.md` `### Step 2 — dispatch`: a scratch repository is entered with an absolute `cd` on its own line, verified by `pwd`, before any command that resets, amends, cherry-picks or checks out; and an executor told to commit under the lock names `fusion-workbench/orchestrator-events.jsonl` in every staging list. Of the two branches the acceptance offers for the log, the second was taken: the lock appends its row after the wrapped command exits (`rules/commit-lock.md` `### The lock writes the commit event`), so staging the row into the same commit would contradict the lock's own predicate, and the staging-list rule is what this package has obeyed from its first commit. The rows lost between `73c11cfd` and `10124e3a` are not restored; the sixteen commits are in git.
