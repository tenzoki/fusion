# Is `CLAUDE.md`'s register repair inside this Circle?

---
**Domain:** code
**Filed by:** orchestrator
**Cross-references:** `260821-1042-reply-bounded-whole-question-answered`

---

## Question

`CLAUDE.md` is loaded into every agent's context as project instructions. It holds about 40 per cent of
the always-on prose an agent carries and, after the preceding Circle repaired the six emitted files, 94
per cent of the remaining em-dashes. It was left out of that Circle by a decision that deferred it to
"the Circle that runs the measurement", and this Circle is not that one.

## Options

1. **No.** The repair goes through `/fusion:cleanup --only claude-md`, which dispatches the curator with
   a user gate.
2. **Yes, as the Circle's last step**, separately revertible.

## Answer: option 1, no. It goes through the cleanup step.

## What this forecloses

The largest single conditioning text an agent reads stays unrepaired while this Circle runs, so the
Circle's own writers work under prose it did not fix. Anything this Circle measures about register is
measured against a corpus with that file still in it, and a later reader needs to know that.

The route chosen is not weaker for being outside the Circle: the curator is the sanctioned writer of
normative text and its pass carries an evidence tier and a citation per change, behind a gate.

---
Answered: this record — the user answered at a gate in session `260820-2103-orchestrator-session.md` on 260821, before the Circle was activated. Their words, not the orchestrator's reading of them.
