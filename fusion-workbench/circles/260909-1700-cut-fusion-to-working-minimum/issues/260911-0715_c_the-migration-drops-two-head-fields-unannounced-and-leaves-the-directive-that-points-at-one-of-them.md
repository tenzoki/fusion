The migration drops two head fields unannounced and leaves the Directive that points at one of them
---
`skills/migrate/SKILL.md`'s record conversion lists five head fields and carries the body "verbatim from `## Directive` down". A pre-v11 record's `**Active spec/plan:**` and `**Active session history:**` sit above `## Directive` and in neither half, so a consuming project's migration drops them silently, in a one-way step. Worse, a record whose Directive reads "See `**Active spec/plan:**` above" is left pointing at a field that no longer exists.
---
**Filed by:** orchestrator, Kai Stalmann <ks@qantr.com>
**Cross-references:** 2f8d1082 (the migration as shipped); a8f52f62 (step S10, which met this by hand); 260910-2011_*_a-work-item-has-no-field-for-the-plan-it-runs-on-so-the-closure-step-lost-its-source.md

**How it was found.** Step S10 converted this workbench's two live records by hand rather than by running the body, and the divergence between what the hand conversion had to do and what the body prescribes is the evidence. The claimed record carried both fields with real values, and its `## Directive` was the pointer literal naming the first of them.

**Two defects, not one.**

1. **The fields are dropped with no note.** The gap they leave is already filed as `260910-2011_*_a-work-item-has-no-field-for-the-plan-it-runs-on-so-the-closure-step-lost-its-source.md`, which asks for the format to gain a field or for the closure step to take the plan from elsewhere. This record is about the *migration's* behaviour in the meantime: dropping them unannounced, in a step that cannot be undone by a forward commit, is worse than dropping them with a line saying so.
2. **The pointer Directive is left dangling.** `rules/circle-records.md` mandated that a record whose spec exists replaces its Directive prose with `See **Active spec/plan:** above.` Every record written after that rule landed carries it, so this is not an edge case — it is the common shape for any Circle that got as far as a spec. After conversion that sentence names a field the record no longer has.

**What S10 did instead**, as one possible answer rather than the answer: carried both values verbatim into the body as a short block above `## Directive`, stating that the format defines no field for them and citing the open record. The pointer sentence then still resolves.

**Acceptance.** The migration either carries the two fields somewhere a reader can find them, or drops them and says so in the survey the user confirms. A record whose Directive is the pointer literal is not left naming a field that is gone — either the pointer is rewritten in the same pass, or the field it names survives. Whichever, the body says which, and `260910-2011_*` is cited so the two records are read together.

Also seen: 260915-1922 by analyst — a consuming project met the same fork; the pointer literal cites the field, and 14 records in this workbench carry it.

---
Resolved: the first branch of the disjunction, and the ruling on `260910-2011_*_a-work-item-has-no-field-for-the-plan-it-runs-on-so-the-closure-step-lost-its-source.md` (option 1, user, 260915) is what made it the cheap one. The item grammar now defines `**Active spec/plan:**` — `rules/fusion-workbench-conventions.md` `## Backlog entries — work items`, in the template, in the absent-when-empty list and in a paragraph carrying the ride-the-act rule the deleted `rules/circle-records.md` held — and it **keeps the Circle head field's name**. That choice decides both halves of this record at once. The field is no longer one the item head cannot hold, so `skills/migrate/SKILL.md` Step 4b copies its value into the head block it writes instead of dropping it; and because the name is unchanged, a `## Directive` reading `See **Active spec/plan:** above` points at a field the converted record still has. **The second defect, the dangling pointer, therefore disappears rather than being handled**: no pointer literal is rewritten, in this workbench's fourteen records or in any consuming project's, and the migration performs no rename of the field. The cost of keeping the name, stated rather than hidden: `active` is Circle-era wording, and the definition says what it qualifies — the spec in force, not the item, whose state is `**Status:**`.

`**Active session history:**` has no item field, the history store having closed to writes at v11, so it keeps the carry answer: its basenames join `**Cross-references:**`, the field the grammar already defines for a record the work rests on, and a value carrying anything more is written verbatim into the body above `## Directive`. One further edge, reproduced on the fixture: a `[m]` marker in a carried value is brought to `_m_`, because Step 4's reformat renames the file and leaves a container-form record's field behind. The user is told at the confirmation (Step 3, "And say the one thing that is not a move") and again in the Step 5 report.

Verified on a throwaway workbench holding a live `_t_circle.md` whose two fields carried real values and whose `## Directive` was the pointer literal, with the shipped bash blocks lifted out of the body and run as found: every head value survives, the body from `## Directive` down is byte-identical, the head carries only fields the grammar defines, and the pointer resolves to a field present in the file with nothing about it rewritten. What no gate asserts: that a run of the prose procedure follows it. Bounds after the change — the tightest dispatch path (`reviewer`) keeps 26 167 bytes of margin against its zero-head-room baseline, and `skills/` ends 256 bytes **below** where it stood. `cd hooks && npm test` exits 0, 55 files, 931 tests.
