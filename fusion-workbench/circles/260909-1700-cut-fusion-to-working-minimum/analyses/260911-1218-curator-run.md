Curator run — the v11.0.0 pre-tag pass over the three normative surfaces

**Status:** Complete
**Date:** 2026-09-11
**HEAD read:** `d874f944e85f77e3bdeceec4a91896cdfaa598bc`
**Mode:** survey, then apply (2026-09-11 12:24, same HEAD `d874f944`). All twenty-three entries were approved at the gate and applied; `C01` was never on offer. Outcomes per entry in `## 11`.
**Scope:** anchored
**Anchor:** `4727506dbc2117ebb32de355f62ca099ad96853c` (2026-08-29 11:12, `last_curator_run`), 236 commits behind HEAD. The anchor precedes `v10.26.0` (`e60d018b`, 2026-09-09), so the anchored range covers the whole v11 removal release and nothing was skipped on the strength of the bound.
**Previous curator run:** none findable across `$SCAN_ANALYSES`. `ls` over the container and shared analysis stores returns no earlier `*-curator-run.md`; the anchor above is the only trace of one, and it names a commit rather than a file. This is the first run file of this shape in this workbench.
**Work-item:** 260909-1700-cut-fusion-to-working-minimum

## 1. Evidence-source counts

| # | Source | Read | Note |
|---|---|---|---|
| 1 | Work items (`$SCAN_BACKLOG` = `circles`) | 26 item records in 26 containers | plus the 2 entries left in `shared/backlog/`, which no resolver key has reached since `0ec57d37` |
| 2 | Decision records (`$SCAN_DECISIONS`) | 101 files; 51 of them live (`_o_`/`_a_`) | 50 terminal records read by marker only — the dispatch bars editing them |
| 3 | `git log` over `CLAUDE.md` and `rules/` | 46 commits in `4727506d..HEAD` | plus `git show v10.26.0:` on `agents/`, `skills/` and `bin/fusion-paths` for the three roster comparisons below |
| 4 | Reviews and analyses | 36 reviews, 34 analyses | the load-bearing one is `260911-1132-reviewer-v11-closing-pass.md` |
| 5 | `orchestrator-events.jsonl` | 3 477 lines, read as corroboration only | no finding below rests on it |
| 6 | Archive store (`$WORKBENCH/archive`) | 605 files present, 565 archived since the anchor | opened by name, not by content: no finding below cites one, and none contradicted one |
| 7 | `**Provenance:**` headers | 15 of 15 rule files carry one | **zero** name a superseded record, so this source produced no Tier 2 retirement candidate |

**Defect corpus used as a cross-check, not as a surface:** 49 open defect records across `$SCAN_ISSUES`. Two of them predict findings in this ledger (`260911-1120`, `260911-1130`) and one supplies its Tier 2 citation across nine rule files (`260910-2146`). **No open defect contradicts any entry below**, so nothing was downgraded on that ground.

**Nothing was unreadable.** `.claude/rules/` does not exist in this repository — reported as absent, not as an error, and no contradiction claim below involves it.

## 2. Surface sizes

The "after" column was filled by the apply pass, measured on disk.

| Surface | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|
| `CLAUDE.md` | 91 613 | 151 | 91 369 | 151 |
| `rules/` (15 files) | 189 960 | — | 189 788 | — |
| decision records in `$SCAN_DECISIONS` | 101 files | — | 101 files, one of them 109 bytes larger | — |

**A correction to this table, found by the apply pass and owned rather than quietly fixed.** The survey wrote the `rules/` surface at **185 032** bytes and it was **189 960**: a 4 928-byte arithmetic error in the aggregate. Verified from git objects at the same HEAD the survey read (`git cat-file -s` summed over `git ls-tree HEAD rules/` returns 189 960 over 15 files), so the files did not move between the passes — the sum was simply wrong. **Nothing downstream rested on it:** every per-file figure in the table below was checked against disk before each edit and all ten matched, the blast-radius percentages in `## 8` are computed per file rather than from this total, and the dispatch-path figures in `## 3` are a separate per-emission measurement that the apply pass reproduced to the byte.

Per-file, for the ten files this ledger reaches:

| File | Bytes | Lines |
|---|---|---|
| `CLAUDE.md` | 91 613 | 151 |
| `rules/fusion-workbench-conventions.md` | 60 133 | 564 |
| `rules/context-manifest.md` | 11 056 | 208 |
| `rules/user-facing-output.md` | 10 877 | 98 |
| `rules/critical-stance.md` | 10 374 | 85 |
| `rules/commit-lock.md` | 8 363 | 84 |
| `rules/review-contract.md` | 6 823 | 81 |
| `rules/context-lean-claude-md.md` | 6 712 | 152 |
| `rules/design-diagrams.md` | 5 285 | 58 |
| `260827-1305_*_which-agents-run-on-a-smaller-model.md` | 2 972 | 38 |

## 3. The byte bound this pass was sized against

`CLAUDE.md` is a shared component of all eleven dispatch paths, and `hooks/lib/__tests__/fixtures/dispatch-path.baseline` holds every path at **zero head-room**. Measured by this pass at HEAD, running `bin/fusion-rules <agent>` per agent from the repository root and summing prompt + emitted rules + `CLAUDE.md`:

| Path | Total | Baseline row | Margin |
|---|---|---|---|
| reviewer | 188 842 | 189 012 | **170** |
| analyst | 198 520 | 198 789 | 269 |
| editor | 202 143 | 202 739 | 596 |
| planner | 201 820 | 202 428 | 608 |
| consultant | 196 935 | 197 679 | 744 |
| coder | 181 347 | 188 256 | 6 909 |
| ontocoder | 185 057 | 191 869 | 6 812 |
| curator | 217 609 | 227 066 | 9 457 |
| reconciler | 195 324 | 206 384 | 11 060 |
| shaper | 210 353 | 250 768 | 40 415 |
| orchestrator | 282 446 | 380 065 | 97 619 |

This reproduces the figures the dispatch stated, independently. **Every entry below carries its byte delta**, and the net is negative on every one of the eleven paths — see `## 7. Net byte effect`.

## 4. Comparison counts

The rule that chose the pairs, what it produced, and what was read. **Each verdict covers the pairs the rule reached, not the corpus.** The corpus figures are derived, and the command is named.

| Pair | Selection rule | Produced | Read | Verdict |
|---|---|---|---|---|
| 1. decision × decision | Live records only (`_o_`/`_a_`) in `$SCAN_DECISIONS`, then the subset naming a mechanism the v11 range removed: `grep -lE 'taskplanner\|playmaker\|bugfixer\|coderev\|ontorev\|maxTurns\|agentstate\|portfolio\|active-circle'` over those 51 files | 51 live records, 33 with a hit, 528 unordered live pairs | the 33 hit files opened; 2 read in full | No live record overturns another **among the 33 the grep reached**. 495 pairs among the 18 records with no hit were not compared. |
| 2. decision × rule file | Every live decision naming a rule file path, against that file | 51 × 15 = 765 candidate pairs; 9 produced by the rule | 9 | One finding, and it runs the other way: `260827-0910` fixes the user-facing audience at five agents and `rules/user-facing-output.md` names six (entry L08). |
| 3. decision × `CLAUDE.md` | Every live decision whose subject `CLAUDE.md` also states | 51 | 12 opened | One: `260827-1305` and `CLAUDE.md` line 16 give different agent rosters; `CLAUDE.md` is right and the record is stale (entries L21–L23). |
| 4. rule file × rule file | All 15 rule files, every unordered pair | 105 | 105, by targeted grep over the shared vocabulary (agent names, store paths, emission audiences); 9 opened in full | No direct contradiction found between two rule files. Every fault found was a rule against ground truth, not a rule against a rule. |
| 5. rule file × `CLAUDE.md` | Every `rules/*.md` path `CLAUDE.md` describes in its Layout table, against that file | 15 | 15 | Three: L08, L09/L10 and L12 each state something `CLAUDE.md` states correctly and the rule file states with a deleted agent's name. |
| 6. `CLAUDE.md` × itself | Every paragraph pair sharing a subject token, machine-selected on the shared vocabulary above | 151 lines, 11 sharing-subject groups | 11 | **Two direct self-contradictions**, both load-bearing: the work-item shape (lines 56 and 86 against line 67) and the `fusion-guard.json` migration instruction (line 30 against line 149). |

**What that leaves unclaimed.** Pair 1's verdict is bounded by a keyword selector; a supersession phrased in vocabulary neither record shares with the removal range would not have been reached. Pair 6's grouping is by shared token, so a contradiction between two paragraphs that never name the same thing is outside it. Both are stated because the alternative reads as coverage.

## 5. Pre-edit content of every decision record this run intends to modify

One record. Written out in full, unconditionally, whatever the tracking state of this workbench.

### `260827-1305_*_which-agents-run-on-a-smaller-model.md`

The concrete path this record sits at today, and the command that restores it, written inside a fence
because a store-prefixed path is a citation violation everywhere else in this file:

```sh
# path, relative to the workbench root that bin/fusion-workbench-root prints
shared/decisions/260827-1305_o_which-agents-run-on-a-smaller-model.md
# revert, from the project root
git checkout -- fusion-workbench/shared/decisions/260827-1305_o_which-agents-run-on-a-smaller-model.md
```

Complete pre-edit content follows.

```markdown
# Which agents run on a smaller model?

---
**Domain:** code
**Filed by:** claude-code (conditioning-load work; the last open lever of the 260827 optimisation round), Kai Stalmann <ks@qantr.com>
**Cross-references:** `260827-1305-does-agent-frontmatter-model-reach-the-dispatch.md` (the mechanism, measured: `model:` frontmatter is honoured, verified from transcripts) · `agents/reconciler.md` (the first candidate) · `refactor/260827-0335-bookkeeping-cost-repair-plan.md` (the audit that raised the lever)

---

## Question

All 14 sub-agents inherit the session's model. The mechanism for per-agent tiering exists and is measured; the cost lever is the largest one left that no byte-cut touches. Which roles, if any, move — and on what evidence?

## Candidates, ordered by how mechanical the role's judgement is

1. **reconciler** — verifies claimed record states against ground truth with grep/read/git; since 10.13.0 its inventory is live-scope only. The most checklist-shaped role.
2. **taskplanner** — orders records into a queue by declared priority axes.
3. **coderev** — since 10.14.0 one dispatch per Circle; a quality drop here costs the most, so it moves last if at all.

## The measurement this needs (why this record is filed open)

Not another probe: live evidence. After a few sessions on ≥ v10.15.0, run one reconciliation with the candidate on `model: haiku` (a one-line frontmatter change in a work tree, `claude --plugin-dir .`) against the same workbench state as a same-day run on the session model, and compare the two reconciliation history files: discrepancies found, discrepancies missed, wrong claims. The transcript names the model actually used (`subagents/*.jsonl`); the dispatch result and the agent's own say-so do not. Two clean candidate runs are the bar for moving a role; one miss a session model catches is the bar for moving it back.

## Constraints

- Verification is transcript-based, per the analysis; a tiering change ships only with its transcript evidence cited.
- The two-session shape applies: a frontmatter change is live only after `fusion --update` and a restart.
- Nothing here touches the orchestrator, shaper, curator or editor: judgement-heavy, user-facing, or gate-holding roles are out of scope for the first round by construction.

---
**Reconciliation 260905-2015 (reconciler, HEAD `5b84b13a`) — marker unchanged at `_o_`, no answer on
disk.** No agent prompt carries a `model:` frontmatter key: `grep -l '^model:' agents/*.md` is empty,
so all 14 sub-agents still inherit the session's model and no tiering has been tried. The live evidence
this record asks for — two candidate runs against the same workbench state, verified from the
transcripts — does not exist anywhere in the tree. No `Answer located:` line.

The first candidate this record names is the reconciler, and this pass is the kind of run it means. It
was made on the session model and is therefore a control rather than evidence.
```
## 6. The ledger

**Twenty-three proposed changes and one candidate.** Ids are `L01` upward and are the tokens the apply dispatch names.

**One stated deviation from the entry schema.** The Before and After texts are carried in fenced blocks rather than blockquotes. Two reasons, both mechanical. A blockquote prefix would have to be stripped on apply, and the exactness of the Before text is what the apply pass's staleness check rests on. And `hooks/lib/__tests__/workbench-citation-lint.test.ts` reads every `.md` under the workbench: several Before texts carry record citations and one carries a commit hash, and a fenced block is the exemption the suite already recognises (`hooks/lib/__tests__/fenced-code-exemption.test.ts`). Everything else in the schema is as written.

**Every entry's Before text was verified to occur exactly once in its file at HEAD `d874f944`**, by a script that counted occurrences and computed the delta; all 23 matched uniquely.

---

### L01 — the growth-bound bullet names a retired bound, three stale constants and the wrong number of re-baselining events

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** Tier 2
- **Citation:** `README-hooks.md` `### Growth bounds on the shipped text`, brought to three rows in the same change, whose table reads `agents/` 18 000, `skills/` 21 911, hook tests 2 595 lines, and whose next paragraph states the always-on row was retired on 2026-09-11. Confirmed in the code: `grep -n '_HEAD_ROOM =' hooks/lib/__tests__/surface-growth-bound.test.ts` prints `AGENT_HEAD_ROOM = 18_000`, `SKILL_HEAD_ROOM = 21_911`, `TEST_LINE_HEAD_ROOM = 2_595`, and `rules-emission-golden.test.ts:1080` holds `DISPATCH_HEAD_ROOM = 0`. The re-baselining count: `hooks/lib/__tests__/helpers/growth-bound.ts:18` reads `## Re-baselining: the three events at which a baseline moves`, and `:23` says "the heading is a count, meant literally" — the bullet says two. Open defect `260911-1120_*_claude-mds-growth-bound-bullet-names-four-failing-surfaces-and-three-remain.md` asks for exactly this repair and its acceptance test is what the After text satisfies.
- **Consequence group:** tier-2
- **Constraint removed:** none — every constraint in the sentence survives; the four figures are replaced by the pointer to the table that holds them, per the derive-over-correct rule, and the per-dispatch-path bound that actually binds this file is named for the first time
- **Byte delta:** -46
- **Revert path:** `git checkout -- CLAUDE.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
- **Four shipped surfaces have a failing growth bound (260815).** Editing `agents/*.md`, `skills/*/SKILL.md`, the hook tests, or an always-on rule file can now turn `npm test` red — not for what the text says, but for how much it grew. Each surface has its own baseline, its own head-room and its own failure, derived from that surface's own replayed history: 12 000 bytes for the always-on rule set (`hooks/lib/__tests__/rules-emission-golden.test.ts`), 18 000 for `agents/`, 20 000 for `skills/`, 2 500 lines for the hook tests (`hooks/lib/__tests__/surface-growth-bound.test.ts`). **They are four budgets, not one:** shrinking `skills/` buys `agents/` nothing, by construction. A shrink never trips a bound — this measures the rate of addition, which is what the project's own history says the binding constraint is. **The way out of a red bound is a cut, never an edit to the baseline:** a baseline moves at exactly two written-down moments, after a cleanup or at a one-time arming, and the rule is authored once in `hooks/lib/__tests__/helpers/growth-bound.ts`. Read `README-hooks.md` `### Growth bounds on the shipped text` before touching a number. What no bound covers is stated there too, rather than left to be discovered.
```

**After:**

```text
- **Shipped text carries failing growth bounds (260815).** Editing `agents/*.md`, `skills/*/SKILL.md`, the hook tests or a rule file can turn `npm test` red — not for what the text says, but for how much it grew. **Which surfaces are bounded, at what head-room, at what each is measured from, and what no bound covers is one table in `README-hooks.md` `### Growth bounds on the shipped text`.** Do not copy a figure out of it into this file: every one of them has moved. **The budgets are independent:** shrinking `skills/` buys `agents/` nothing, by construction. A shrink never trips a bound — they measure the rate of addition, which is what the project's own history says the binding constraint is. **One further bound works differently and reaches this file**: `hooks/lib/__tests__/fixtures/dispatch-path.baseline` charges `CLAUDE.md` to all eleven dispatch paths at **zero head-room**, so N bytes added here puts every path N over at once and must be paid for inside the same paths. **The way out of a red bound is a cut, never an edit to a baseline or a head-room constant**; both move only at the events named in `hooks/lib/__tests__/helpers/growth-bound.ts` `## Re-baselining`.
```

---

### L02 — the skill bullet counts three departed directories and four have gone

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** Tier 1
- **Citation:** `diff <(git ls-tree -d --name-only v10.26.0 skills/) <(ls -1d skills/*/)` returns four moves: `check` and `reconcile` arrived, `direct` and `next` left. `next` is named nowhere in `CLAUDE.md`. Its deletion commit is `2a785ba2` (`git log --diff-filter=D -- skills/next/SKILL.md`). Open defect `260911-1130_*_claude-mds-skill-bullet-says-three-directories-have-left-and-next-is-the-fourth.md`; the numeral is dropped rather than corrected, per `rules/critical-stance.md` §5.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** +31
- **Revert path:** `git checkout -- CLAUDE.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
Three directories have left. `revise-claude-md` and `unlock` went on 2026-08-15 with an earlier collapse,
```

**After:**

```text
Departed directories, `next` in this release at `2a785ba2`: `revise-claude-md` and `unlock` went on 2026-08-15 with an earlier collapse,
```

---

### L03 — "Neither name" carries a cardinality of two over a list of four

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** Tier 1
- **Citation:** Same enumeration as L02, now four members. `rules/critical-stance.md` §5: "`half`, `both`, `pair` carry cardinality as surely as a numeral." Applies to `Neither` in the same way. Depends on L02: applying L03 alone leaves a four-member list under a two-member pronoun.
- **Consequence group:** tier-1
- **Constraint removed:** none — the prohibition itself is unchanged, only its scope word
- **Byte delta:** +7
- **Revert path:** `git checkout -- CLAUDE.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
its whole body having dispatched the shaper mode that wrote one. Neither name may be written in the `/fusion:` form anywhere in this file
```

**After:**

```text
its whole body having dispatched the shaper mode that wrote one. None of those names may be written in the `/fusion:` form anywhere in this file
```

---

### L04 — the Layout row for the work-item grammar states the pre-v11 flat-file shape

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** Tier 1
- **Citation:** `bin/fusion-paths:387` reads `OUT_BACKLOG|SCAN_BACKLOG)     printf '%s' "circles"`, and this run's own `bin/fusion-paths curator` printed `SCAN_BACKLOG=circles`. `rules/fusion-workbench-conventions.md` `## Backlog entries — work items` — which this same row names as the authoring home — reads "**It is a directory, and its record lives inside it.** The container is `circles/YYMMDD-HHMM-<slug>/` ... and the record is `circles/YYMMDD-HHMM-<slug>/YYMMDD-HHMM-<slug>.md`". Also contradicts `CLAUDE.md` line 67, which states the container form correctly. `git show v10.26.0:bin/fusion-paths` confirms the row was true until `0ec57d37`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** +29
- **Revert path:** `git checkout -- CLAUDE.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
The **work item**: one file per unit of work in the backlog store, `YYMMDD-HHMM-<slug>.md` with no filename marker, its state the `**Status:**` head field
```

**After:**

```text
The **work item**: one directory per unit of work under `circles/`, the record inside it under the directory's own name and no marker on either, its state the `**Status:**` head field
```

---

### L05 — the work-item bullet states the same pre-v11 flat-file shape, with rationale the conventions rule already carries

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** Tier 1
- **Citation:** Same citations as L04. The two sentences cut are carried verbatim in substance at the anchor this bullet itself names in its next breath (`rules/fusion-workbench-conventions.md` `## Backlog entries — work items`: "One file per item rather than one list file, because two checkouts adding work at the same time then merge with no conflict. No marker, because an item's state is a head field: a state change edits the file instead of renaming it, so every citation of an item stays valid for the item's whole life."), so the cut is a consolidation carried inside a Tier 1 correction, not a removal.
- **Consequence group:** tier-1
- **Constraint removed:** none — the constraint (one record per item, no marker, state in the `**Status:**` head field) is stated in the After text; only the duplicated rationale goes, and it survives at the cited anchor
- **Byte delta:** -248
- **Revert path:** `git checkout -- CLAUDE.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
- **The work item is the unit of work (260910)** — one file per item in the backlog store, `YYMMDD-HHMM-<slug>.md`, **no marker on the filename**, its state the `**Status:**` head field. One file per item rather than one list file, because two checkouts adding work at the same time then merge with no conflict; no marker, because a state change then edits the file instead of renaming it and every citation of an item stays valid for the item's whole life. The four statuses are
```

**After:**

```text
- **The work item is the unit of work (260910)** — one directory per item under `circles/`, the record inside it under the directory's own name, **no marker on either**, its state the `**Status:**` head field. The four statuses are
```

---

### L06 — the configuration row tells a consuming project to copy a setting across that the loader says to abandon

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** Tier 1
- **Citation:** `hooks/lib/config.ts:419`, the `RETIRED_PROJECT_FILES` message itself: "If it sets \"orchestrator.maxTurns\", that setting no longer exists anywhere — the Turn loop it bounded was removed — so there is nothing to copy across for it". `CLAUDE.md` line 149 already says the same ("**copy nothing across** ... That instruction was the reverse until 260910"), so this is a direct self-contradiction inside `CLAUDE.md` with one falsified side.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -12
- **Revert path:** `git checkout -- CLAUDE.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
 and the file advisory names `orchestrator.maxTurns` and the JSON to copy across, because a budget left behind is silently not read
```

**After:**

```text
 and the file advisory says there is nothing to copy across, because every setting that file could carry is retired too
```

---

### L07 — "Four went at v11" stands over a list of five names

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** Tier 1
- **Citation:** `git ls-tree --name-only v10.26.0 agents/` lists 15 prompts; `ls -1 agents/*.md` lists 11. Five were removed (`bugfixer`, `coderev`, `ontorev`, `playmaker`, `taskplanner`) and one added (`reviewer`). The sentence then names all five as having gone. `hooks/lib/__tests__/fixtures/dispatch-path.baseline` states it in the same words: "FIVE ROWS WERE REMOVED, and only two of them by merger." `rules/critical-stance.md` §5.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -5
- **Revert path:** `git checkout -- CLAUDE.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
**Four went at v11 with the subjects they served and one arrived by merger.**
```

**After:**

```text
**Five prompts went at v11 and one arrived by merger, 15 agents to 11.**
```

---

### L08 — the user-facing style rule names `playmaker` among the agents it is emitted to

- **Surface:** project rule file
- **File:** `rules/user-facing-output.md`
- **Tier:** Tier 1
- **Citation:** `bin/fusion-rules:319` reads `orchestrator|consultant|shaper|editor|curator) IS_USER_FACING_AGENT=1`, and `:578` is the only emission site for this file. `agents/playmaker.md` does not exist (`ls agents/`). `CLAUDE.md` `## Conventions` already states the correct five, so this is also a rule-file-against-`CLAUDE.md` contradiction with one falsified side. Open defect `260910-2146_*_five-deleted-agents-are-still-named-as-live-in-twelve-shipped-files.md` classes it as the first of its three kinds — a rule naming a deleted agent as its own audience.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -11
- **Revert path:** `git checkout -- rules/user-facing-output.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
orchestrator, consultant, playmaker, shaper, editor, curator (gate
```

**After:**

```text
orchestrator, consultant, shaper, editor, curator (gate
```

---

### L09 — the review contract addresses two agents that no longer exist as its audience

- **Surface:** project rule file
- **File:** `rules/review-contract.md`
- **Tier:** Tier 1
- **Citation:** `bin/fusion-rules:284` reads `reviewer)                       IS_REVIEWER_AGENT=1`, and `:667` is the only emission site. Neither `agents/coderev.md` nor `agents/ontorev.md` exists. The reviewer reading this file is told it is addressed to somebody else. Named explicitly in `260910-2146` (the `Also seen:` line) and in `260911-1132-reviewer-v11-closing-pass.md` finding F. `CLAUDE.md`'s Layout row for this file already says "emits it behind `IS_REVIEWER_AGENT`, which is set for exactly `reviewer`".
- **Consequence group:** tier-1
- **Constraint removed:** none — the definition's single-authoring-home claim and the no-competing-definition claim both survive, restated for one prompt
- **Byte delta:** -17
- **Revert path:** `git checkout -- rules/review-contract.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
emits it to `coderev` and `ontorev`, the two agents that write review files, and to no other agent.
Neither prompt carries a competing definition. Each names its own domain-specific analysis steps and
its own sender segment, and cites this file for everything below.
```

**After:**

```text
emits it to `reviewer`, the one agent that writes review files, and to no other agent. That prompt
carries no competing definition: it names its own domain-specific analysis steps and its own sender
segment, and cites this file for everything below.
```

---

### L10 — the review contract tells the reviewer its own sender segment is `coderev` or `ontorev`

- **Surface:** project rule file
- **File:** `rules/review-contract.md`
- **Tier:** Tier 1
- **Citation:** Same as L09. This is the operative half: `<sender>` is written into every review filename, and `hooks/lib/review-coverage.ts:193` already recognises the new segment. The one review file this release produced writes `260911-1132-reviewer-v11-closing-pass.md`, so the prompt and the practice have already diverged.
- **Consequence group:** tier-1
- **Constraint removed:** none — the segment stays mandatory in every filename
- **Byte delta:** -5
- **Revert path:** `git checkout -- rules/review-contract.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
Two names are used throughout. `<sender>` is your own agent name, `coderev` or `ontorev`.
`$OUT_REVIEW` is the review store `bin/fusion-paths` resolved for you at Setup; both senders write
into the same one, which is why the sender segment is mandatory in every filename.
```

**After:**

```text
Two names are used throughout. `<sender>` is your own agent name, `reviewer`. `$OUT_REVIEW` is the
review store `bin/fusion-paths` resolved for you at Setup; the retired senders wrote into the same
one, which is why the sender segment is mandatory in every filename.
```

---

### L11 — the filename-pattern rule defines a review file's sender as `coderev` or `ontorev`

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md`
- **Tier:** Tier 1
- **Citation:** Same ground truth as L09/L10, in the always-on conventions this time, so every agent carries it. Named in `260911-1132-reviewer-v11-closing-pass.md` finding F as one of the two first-kind cases. The After text keeps the retired senders as history rather than deleting them, because older review files in `shared/reviews/` carry all three.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -39
- **Revert path:** `git checkout -- rules/fusion-workbench-conventions.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
`<sender>` on a review file is `coderev` or `ontorev`. It is what distinguishes the two review kinds now that they share one `reviews/` directory: it is mandatory, and the document header repeats it. Older files may carry a third sender, `conceptrev`, retired with its agent on 2026-08-15.
```

**After:**

```text
`<sender>` on a review file is `reviewer`. It is mandatory, and the document header repeats it. Older files carry the senders it replaced: `coderev` and `ontorev`, merged into `reviewer` at v11, and `conceptrev`, retired with its agent on 2026-08-15.
```

---

### L12 — the critical-stance rule attributes the honesty norms to two prompts that no longer exist

- **Surface:** project rule file
- **File:** `rules/critical-stance.md`
- **Tier:** Tier 1
- **Citation:** `ls agents/` — no `coderev.md`, no `ontorev.md`. `CLAUDE.md` `## Conventions` states the same fact in role terms and stays true: "It generalises to every agent (notably the orchestrator) the per-agent honesty lines that the consultation, analysis and review prompts already carried." The After text adopts `CLAUDE.md`'s own wording, which is what makes this a contradiction repair rather than a rewrite. This is `260910-2146`'s **second** kind (a citation as example, stale rather than unfollowable), and it is proposed only because the repair is net negative on an always-on file.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -24
- **Revert path:** `git checkout -- rules/critical-stance.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
the honesty norms already held by `consultant`, `analyst`, `coderev` and `ontorev` in their own prompts.
```

**After:**

```text
the honesty norms the consultation, analysis and review prompts already carried.
```

---

### L13 — the commit-lock rule lists `bugfixer` among the agents that may acquire the lock

- **Surface:** project rule file
- **File:** `rules/commit-lock.md`
- **Tier:** Tier 1
- **Citation:** `ls agents/` — no `bugfixer.md`. `grep -l 'fusion-commit-lock' agents/*.md` returns `coder.md`, `ontocoder.md`, `orchestrator.md`: three prompts carry the instruction inline, which is what `bin/fusion-rules:637` means by "the three that may commit directly" — that comment stays true after this edit and needs no change. `260910-2146` classes this as its first kind.
- **Consequence group:** tier-1
- **Constraint removed:** none — no agent loses the obligation; one that does not exist loses the mention
- **Byte delta:** -11
- **Revert path:** `git checkout -- rules/commit-lock.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
- **Coder / ontocoder / bugfixer** ONLY if they commit directly
```

**After:**

```text
- **Coder / ontocoder** ONLY if they commit directly
```

---

### L14 — the commit-lock tag vocabulary offers `bugfixer` as a legal tag

- **Surface:** project rule file
- **File:** `rules/commit-lock.md`
- **Tier:** Tier 1
- **Citation:** Same as L13. A tag naming a deleted agent can never be written, and it appears in stale-lock messages, so the list is read by a human at a failure.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -12
- **Revert path:** `git checkout -- rules/commit-lock.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
Format: the agent name (`orchestrator`, `coder`, `ontocoder`, `bugfixer`) or the committing skill's name
```

**After:**

```text
Format: the agent name (`orchestrator`, `coder`, `ontocoder`) or the committing skill's name
```

---

### L15 — the diagram rule names `taskplanner` among the agents it is loaded for

- **Surface:** project rule file
- **File:** `rules/design-diagrams.md`
- **Tier:** Tier 1
- **Citation:** `bin/fusion-rules:270` reads `planner|analyst|shaper)         IS_DIAGRAM_AGENT=1`, and `:619` is the only emission site. `agents/taskplanner.md` does not exist. First kind under `260910-2146`.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -15
- **Revert path:** `git checkout -- rules/design-diagrams.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
This rule is loaded for the planning and analysis agents (`planner`, `analyst`, `taskplanner`, `shaper`).
```

**After:**

```text
This rule is loaded for the planning and analysis agents (`planner`, `analyst`, `shaper`).
```

---

### L16 — the manifest rule's worked example routes a rule to `ontorev`

- **Surface:** project rule file
- **File:** `rules/context-manifest.md`
- **Tier:** Tier 1
- **Citation:** `ls agents/` — no `ontorev.md`. A consuming project copying this example gets a unit that matches no agent and loads for nobody. `bin/fusion-rules` matches manifest units on fusion agent names, so the name is not the project's to define. This file is emitted to **no agent** (it has no `emit_if_exists` line in `bin/fusion-rules`), so the repair costs nothing on any dispatch path. Second kind under `260910-2146`, which is why it is proposed only where it is free.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** +0
- **Revert path:** `git checkout -- rules/context-manifest.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
    agents: [ontocoder, ontorev, planner]   # or [*] for every agent
```

**After:**

```text
    agents: [ontocoder, reviewer, planner]  # or [*] for every agent
```

---

### L17 — the manifest rule's second example routes a rule to `coderev` and `bugfixer`

- **Surface:** project rule file
- **File:** `rules/context-manifest.md`
- **Tier:** Tier 1
- **Citation:** Same as L16.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -9
- **Revert path:** `git checkout -- rules/context-manifest.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
    agents: [coder, coderev, bugfixer, planner]
```

**After:**

```text
    agents: [coder, reviewer, planner]
```

---

### L18 — the lean-CLAUDE.md rule offers `taskplanner priority overrides` as the example of a fusion-agent-specific rule

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`
- **Tier:** Tier 1
- **Citation:** `ls agents/` — no `taskplanner.md`. `CLAUDE.md` `## Conventions` gives the live example for the same slot: "fusion-agent-specific rules — review priority overrides, anything no other Claude session would care about". The After text adopts it. Emitted to no agent.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -22
- **Revert path:** `git checkout -- rules/context-lean-claude-md.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
  outside a fusion-agent context (capture layouts, taskplanner priority
  overrides, and `context-manifest.yaml` itself).
```

**After:**

```text
  outside a fusion-agent context (review priority overrides, and
  `context-manifest.yaml` itself).
```

---

### L19 — the lean-CLAUDE.md topic map routes ontology rules to `ontorev`

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`
- **Tier:** Tier 1
- **Citation:** Same as L16. Emitted to no agent.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** +1
- **Revert path:** `git checkout -- rules/context-lean-claude-md.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
| ontology       | .claude/rules/ONTO-ENG-RULES.md     | ontocoder, ontorev, planner |
```

**After:**

```text
| ontology       | .claude/rules/ONTO-ENG-RULES.md     | ontocoder, reviewer, planner |
```

---

### L20 — the lean-CLAUDE.md manifest example routes rules to `coderev`, `bugfixer` and `ontorev`

- **Surface:** project rule file
- **File:** `rules/context-lean-claude-md.md`
- **Tier:** Tier 1
- **Citation:** Same as L16. Emitted to no agent.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -8
- **Revert path:** `git checkout -- rules/context-lean-claude-md.md` (the file is tracked; `git status` at the start of this run showed only `fusion-workbench/orchestrator-events.jsonl` modified)

**Before:**

```text
    agents: [coder, coderev, bugfixer, planner]
    topics: [always]                      # architecture binds every code edit
  - path: .claude/rules/ONTO-ENG-RULES.md
    agents: [ontocoder, ontorev, planner]
```

**After:**

```text
    agents: [coder, reviewer, planner]
    topics: [always]                      # architecture binds every code edit
  - path: .claude/rules/ONTO-ENG-RULES.md
    agents: [ontocoder, reviewer, planner]
```

---

### L21 — the model-tiering question counts 14 sub-agents and there are ten

- **Surface:** decision record
- **File:** `260827-1305_*_which-agents-run-on-a-smaller-model.md`
- **Tier:** Tier 1
- **Citation:** `ls -1 agents/*.md | wc -l` returns 11, of which `orchestrator` is the session agent, so ten sub-agents. `git ls-tree --name-only v10.26.0 agents/` returns 15, which is where the 14 came from. The figure is replaced by the command that derives it rather than by a new number, per the derive-over-correct rule and `rules/critical-stance.md` §5. The record stays `_o_`: this changes no answer and advances no marker.
- **Consequence group:** tier-1
- **Constraint removed:** none — the question is unchanged
- **Byte delta:** +38
- **Revert path:** the file is tracked; the concrete path and the `git checkout` command are in `## 5. Pre-edit content`, written once in a fenced block so that this file's own citations stay storeless.

**Before:**

```text
All 14 sub-agents inherit the session's model.
```

**After:**

```text
All sub-agents inherit the session's model (`ls agents/*.md` less the orchestrator).
```

---

### L22 — two of the three named tiering candidates are deleted agents

- **Surface:** decision record
- **File:** `260827-1305_*_which-agents-run-on-a-smaller-model.md`
- **Tier:** Tier 1
- **Citation:** `ls agents/` — no `taskplanner.md`, no `coderev.md`. `hooks/lib/__tests__/fixtures/dispatch-path.baseline` records the merge: "`agents/coderev.md` and `agents/ontorev.md` merged into `agents/reviewer.md` at step C8". The candidate that survives is `reviewer`, which inherits `coderev`'s position in the ordering and its argument; the two dead names are kept as a sentence saying they are gone, which is `260910-2146`'s third kind. **This is the one entry that changes an open question's option set**, and it is the one most worth a separate look at the gate.
- **Consequence group:** tier-1
- **Constraint removed:** none — one candidate is renamed to the agent that absorbed it and one is withdrawn because its subject does not exist; no constraint the record places on the measurement is touched
- **Byte delta:** +72
- **Revert path:** the file is tracked; the concrete path and the `git checkout` command are in `## 5. Pre-edit content`, written once in a fenced block so that this file's own citations stay storeless.

**Before:**

```text
2. **taskplanner** — orders records into a queue by declared priority axes.
3. **coderev** — since 10.14.0 one dispatch per Circle; a quality drop here costs the most, so it moves last if at all.
```

**After:**

```text
2. **reviewer** — one dispatch per unit of work; a quality drop here costs the most, so it moves last if at all.

Two further candidates this record named, `taskplanner` and `coderev`, no longer exist: the first was deleted at v11 and the second merged into `reviewer`.
```

---

### L23 — the record's reconciliation note repeats the 14 figure

- **Surface:** decision record
- **File:** `260827-1305_*_which-agents-run-on-a-smaller-model.md`
- **Tier:** Tier 1
- **Citation:** Same as L21. This line is inside the reconciler's own appended note; the change is to the cardinality only, and the note's verdict ("no tiering has been tried") is untouched and still true — `grep -l '^model:' agents/*.md` is still empty.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Byte delta:** -1
- **Revert path:** the file is tracked; the concrete path and the `git checkout` command are in `## 5. Pre-edit content`, written once in a fenced block so that this file's own citations stay storeless.

**Before:**

```text
so all 14 sub-agents still inherit the session's model and no tiering has been tried.
```

**After:**

```text
so every sub-agent still inherits the session's model and no tiering has been tried.
```

---
## 6b. Candidate — not on offer, never applied

### C01 — the workbench layout tree says two legacy stores are absent on purpose, and `shared/backlog/` is now a third

- **Surface:** project rule file
- **File:** `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout`
- **Tier:** 1 (the citation holds; see why this is a candidate anyway)
- **Citation:** `git show v10.26.0:bin/fusion-paths` resolves both backlog keys to `shared/backlog`; `bin/fusion-paths:387` at HEAD resolves them to `circles`. So every workbench upgraded from v10 carries a `shared/backlog/` that no resolver key reaches — this repository's own carries two entries. The tree names `stashes/` and `.migration-v2-backup/` as deliberately absent and says "**Two legacy stores are absent from this tree on purpose**", and its own next paragraph explains why silence there is harmful: "a line in the tree above would read as a store the plugin still creates", and the converse — a store the plugin no longer reads and never names — leaves a reader unable to tell live from dead. Supporting: open defect `260910-2020_*_the-two-existing-backlog-entries-keep-the-retired-marker-form-that-d1-migrates-into.md`, extended by `260911-1132-reviewer-v11-closing-pass.md` finding C with "the store moved again after it was filed, so the two entries are now unread rather than merely mis-shaped."
- **Consequence group:** candidate
- **Why it is a candidate and not a proposed change:** two reasons, both stated rather than worked around. It is the only finding in this pass that is an **addition** rather than a correction, roughly +150 bytes on an always-on rule file, which is charged to all eleven dispatch paths at zero head-room — a sizing decision that belongs to the user, not to this agent, and the dispatch bars me from proposing a head-room raise. And the paragraph it lands in carries a neighbouring cardinality I could not resolve from the text: "Do not drop the three that remain" sits two sentences later and I cannot determine from `rules/fusion-workbench-conventions.md`, `skills/setup/SKILL.md` or `skills/archive/SKILL.md` which three it counts, so I cannot say whether raising "Two" to "Three" above it leaves that sentence correct. Proposing an edit I cannot verify in full would be exactly the unearned certainty `rules/critical-stance.md` §3 names.
- **Byte delta if taken:** about +150, which would bring this run's net from -307 to about -157 and still leave every dispatch path further under its row than it is today.

---

## 7. Net byte effect

Measured, not estimated: each delta is `len(after) - len(before)` in bytes over the exact strings in the ledger.

| File | Net delta | Emitted to |
|---|---|---|
| `CLAUDE.md` | **-244** | all eleven paths |
| `rules/fusion-workbench-conventions.md` | -39 | all eleven (always-on) |
| `rules/critical-stance.md` | -24 | all eleven (always-on) |
| `rules/commit-lock.md` | -23 | orchestrator only |
| `rules/review-contract.md` | -22 | reviewer only |
| `rules/design-diagrams.md` | -15 | planner, analyst, shaper |
| `rules/user-facing-output.md` | -11 | orchestrator, consultant, shaper, editor, curator |
| `rules/context-lean-claude-md.md` | -29 | no agent |
| `rules/context-manifest.md` | -9 | no agent |
| `260827-1305_*_which-agents-run-on-a-smaller-model.md` | +109 | not a dispatch path component |

**Net across every file: -307 bytes.** Net across the three normative surfaces excluding the decision record, which no dispatch path loads: **-416 bytes**.

**Effect on the eleven dispatch paths.** Every path gets `CLAUDE.md` (-244) plus the always-on rule cuts (-63), so **no path gains a byte and each loses at least 307**:

| Path | Margin today | Delta | Margin after |
|---|---|---|---|
| reviewer | 170 | -329 | 499 |
| analyst | 269 | -322 | 591 |
| editor | 596 | -318 | 914 |
| planner | 608 | -322 | 930 |
| consultant | 744 | -318 | 1 062 |
| coder | 6 909 | -307 | 7 216 |
| ontocoder | 6 812 | -307 | 7 119 |
| curator | 9 457 | -318 | 9 775 |
| reconciler | 11 060 | -307 | 11 367 |
| shaper | 40 415 | -333 | 40 748 |
| orchestrator | 97 619 | -341 | 97 960 |

The dispatch asked whether a removal release's corrections run net negative. **They do**, on every path and on nine of the ten files. The one positive is the decision record, which no dispatch path loads, and it is positive because two of its three named candidates had to be replaced by a sentence saying they no longer exist rather than silently deleted.

## 8. Blast radius

**Verdict: clear, by a wide margin.** No surface loses more than **0.45 percent** of its bytes, against the 20 percent threshold that would require a separate earlier confirmation. Deleted bytes per surface, computed as the sum of `len(before) - len(after)` over the entries where that is positive:

| Surface | Bytes removed | Surface bytes | Percent |
|---|---|---|---|
| `rules/context-lean-claude-md.md` | 30 | 6 712 | 0.45 |
| `CLAUDE.md` | 311 | 91 613 | 0.34 |
| `rules/review-contract.md` | 22 | 6 823 | 0.32 |
| `rules/commit-lock.md` | 23 | 8 363 | 0.28 |
| `rules/design-diagrams.md` | 15 | 5 285 | 0.28 |
| `rules/critical-stance.md` | 24 | 10 374 | 0.23 |
| `rules/user-facing-output.md` | 11 | 10 877 | 0.10 |
| `rules/context-manifest.md` | 9 | 11 056 | 0.08 |
| `rules/fusion-workbench-conventions.md` | 39 | 60 133 | 0.06 |
| the decision record | 1 | 2 972 | 0.03 |

No separate scale confirmation is owed.

## 9. Constraints removed, one searchable line each

**None.** Every entry in this ledger carries `Constraint removed: none`, so there is no removal to make greppable. This section is written out rather than omitted, because a wrong prune is silent and the absence of removals is itself the thing a later reader needs to be able to establish from this file.

The nearest thing to a removal is **L05**, which cuts two sentences of rationale out of `CLAUDE.md`'s work-item bullet. They are not a constraint and they are not lost: `rules/fusion-workbench-conventions.md` `## Backlog entries — work items` carries the same two reasons in the same words, and the bullet cites that anchor in the sentence immediately after the cut.

## 10. Work outside my remit, reported rather than done

Four things, each with its owner named. **None of them was done, and none is in the ledger.**

1. **The golden fixture will go red on apply, and regenerating it is coder work.** `hooks/lib/__tests__/fixtures/rules-emission.golden` pins each emitted rule file's byte size (`critical-stance.md 10374`, `fusion-workbench-conventions.md 60133`, `user-facing-output.md 10877`, `design-diagrams.md 5285`, and the rest). Seven of this ledger's entries change one of those sizes, so `hooks/lib/__tests__/rules-emission-golden.test.ts` fails the suite on the next run. The regeneration is one command and it is **not mine to run**: `cd hooks && UPDATE_RULES_GOLDEN=1 npx vitest run lib/__tests__/rules-emission-golden.test.ts` (it rewrites the fixture and then fails on purpose, per `README-hooks.md`). Owner: `coder`. The failure is loud rather than silent, so what this line adds is the owner and the command, not the warning. **No defect record is filed for this**, because it is the ordinary consequence of an approved in-remit edit rather than a fault.
2. **The dispatch-path baseline needs no change and must not get one.** Every path moves further under its row, and a zero-head-room bound refuses only a rise. The dispatch bars editing it; nothing here wants to.
3. **The five-deleted-agents sweep runs wider than my remit.** `260911-1132-reviewer-v11-closing-pass.md` finding F measures 49 further hits across 22 files in `skills/`, `hooks/`, `docs/`, `README.md` and `README-agents.md`. All of those are exclusions 5 and 6 — skill bodies, hook sources, docs and READMEs belong to `coder`. Already filed as `260910-2146_*_five-deleted-agents-are-still-named-as-live-in-twelve-shipped-files.md` with the `Also seen:` extension; **no new defect filed**, because a second record for one subject is how a repair becomes unattributable, which is the reason that record itself gives for existing.
4. **The release's other pre-tag obligations are not mine.** Findings A, B and C of the closing review — the migration that does not detect a live container, the release named on none of the three upgrade surfaces, and the four documents placing a work item in `shared/backlog/` — are skill bodies and user-facing docs. `260911-1126` and `260911-1127` are already closed (`_c_`); `260911-1128` is open. Owner: `coder`. This pass reaches none of them, and approving this whole ledger does not clear the review's own "does anything stop the tag?" verdict.

## 11. Outcomes

Apply pass, 2026-09-11 12:24, at the same HEAD the survey read. **All twenty-three approved entries applied; none
was stale and none failed.** Each entry's Before text was re-read from disk immediately before its write and occurred
exactly once; after each write the file was re-read and compared byte for byte against the intended content, and the
After text was confirmed present. Working-tree edits only — nothing was staged and nothing was committed.

| Entry | Outcome | Measured delta |
|---|---|---|
| L01 | applied | -46 `CLAUDE.md` |
| L02 | applied | +31 `CLAUDE.md` |
| L03 | applied | +7 `CLAUDE.md` |
| L04 | applied | +29 `CLAUDE.md` |
| L05 | applied | -248 `CLAUDE.md` |
| L06 | applied | -12 `CLAUDE.md` |
| L07 | applied | -5 `CLAUDE.md` |
| L08 | applied | -11 `rules/user-facing-output.md` |
| L09 | applied | -17 `rules/review-contract.md` |
| L10 | applied | -5 `rules/review-contract.md` |
| L11 | applied | -39 `rules/fusion-workbench-conventions.md` |
| L12 | applied | -24 `rules/critical-stance.md` |
| L13 | applied | -11 `rules/commit-lock.md` |
| L14 | applied | -12 `rules/commit-lock.md` |
| L15 | applied | -15 `rules/design-diagrams.md` |
| L16 | applied | +0 `rules/context-manifest.md` |
| L17 | applied | -9 `rules/context-manifest.md` |
| L18 | applied | -22 `rules/context-lean-claude-md.md` |
| L19 | applied | +1 `rules/context-lean-claude-md.md` |
| L20 | applied | -8 `rules/context-lean-claude-md.md` |
| L21 | applied | +38 the decision record |
| L22 | applied | +72 the decision record |
| L23 | applied | -1 the decision record |
| C01 | not on offer; candidates are never applied | — |

**Every measured delta equals the one this ledger predicted**, entry by entry, and so does every per-file net in
`## 7`: `CLAUDE.md` -244, and -307 across all ten files, -416 across the three surfaces less the decision record.

**The eleven dispatch paths, re-measured after the writes** by the same method as `## 3` — `bin/fusion-rules <agent>`
from the repository root, prompt plus emitted rules plus `CLAUDE.md`. Every path landed on the margin `## 7`
predicted for it, to the byte, and none is above its row:

| Path | Total after | Baseline row | Margin after | Margin predicted |
|---|---|---|---|---|
| reviewer | 188 513 | 189 012 | 499 | 499 |
| analyst | 198 198 | 198 789 | 591 | 591 |
| editor | 201 825 | 202 739 | 914 | 914 |
| planner | 201 498 | 202 428 | 930 | 930 |
| consultant | 196 617 | 197 679 | 1 062 | 1 062 |
| coder | 181 040 | 188 256 | 7 216 | 7 216 |
| ontocoder | 184 750 | 191 869 | 7 119 | 7 119 |
| curator | 217 291 | 227 066 | 9 775 | 9 775 |
| reconciler | 195 017 | 206 384 | 11 367 | 11 367 |
| shaper | 210 020 | 250 768 | 40 748 | 40 748 |
| orchestrator | 282 105 | 380 065 | 97 960 | 97 960 |

**The suite after the apply: one failing assertion, and it is the one `## 10` named in advance.**
`rules-emission-golden.test.ts > matches the checked-in golden, agent by agent` reports
`fusion-workbench-conventions.md` 60 133 to 60 094, `critical-stance.md` 10 374 to 10 350 and `design-diagrams.md`
5 285 to 5 270. 914 of 915 tests pass and 52 of 53 files are green; the dispatch-path bound in that same file passes,
as do the citation lint, the path-literal lint and the plan-stopping lint. **The fixture was not regenerated and not
edited** — that is coder work, per `## 10` item 1, and the dispatch holds it there.
