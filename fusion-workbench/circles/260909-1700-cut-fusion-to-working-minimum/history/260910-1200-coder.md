# Coder — C2: rewrite the orchestrator prompt

**Date:** 2026-09-10
**Status:** Complete
**Task:** step C2 of `260909-1843_*_implementation-cut-fusion-to-a-working-minimum.md`, with the gate amendment recorded in `260910-0900-orchestrator-session.md` `## Rulings at the gate before C2`
**Dispatched by:** orchestrator

## What was done

`agents/orchestrator.md` was rewritten from a phase procedure into a five-step dispatch loop —
read the task, dispatch, read the return, commit, report and ask. Removed: the numbered phases,
the Turn and the Turn loop, the Turn budget and its `bin/fusion-turn-budget` read, the
circuit-breaker table, the convergence check, the per-Turn Coherence gate, the interrupted-session
resume, the work queue, the `agentstate.yaml` schema and its write points, and the live dashboard
together with every "REFRESH DASHBOARD" instruction. Kept: dispatch, the commit procedure and its
lock, the machine-written rows the prompt must not duplicate, the `tools:` allowlist including
`AskUserQuestion`, the Circle head fields and Circle closure, review coverage, the staging check,
the event log, and the four backlog operations, which are new to this prompt.

One line was added to the dispatch prompt and no more: `**Work-item:** <basename>`, present when
the session is working a claimed item and omitted when it is not.

Per the amendment the Rebalance gate survives with its four options, on one trigger: the Coherence
verdict of a reconciliation the user ran by hand. No automatic evaluation reaches it.
`rules/orchestrator-rebalance.md` was restored from `91179f35` and re-cut.

## Files changed

- `/Users/k1/Projects/productive/fusion/agents/orchestrator.md` — 155 302 → 103 302 bytes
- `/Users/k1/Projects/productive/fusion/rules/orchestrator-rebalance.md` — restored (17 301) and re-cut to 18 634 bytes
- `/Users/k1/Projects/productive/fusion/hooks/lib/__tests__/executor-verification-report-lint.test.ts` — anchors moved from `5. **Verify output.**` / `6. **Mark complete.**` to `### Step 3 — read the return`
- `/Users/k1/Projects/productive/fusion/hooks/lib/__tests__/review-coverage-mandate.test.ts` — anchors moved off `### Step 3c` / `Phase 4 step 2a` / the state-file schema
- `/Users/k1/Projects/productive/fusion/hooks/lib/__tests__/dispatch-bound-lint.test.ts` — the case pinning the deleted helper's second output line removed with its subject
- `/Users/k1/Projects/productive/fusion/hooks/lib/__tests__/fusion-paths.test.ts` — `orchestrator` moved from the no-backlog-keys case to a case of its own
- `/Users/k1/Projects/productive/fusion/hooks/lib/__tests__/fixtures/surface-growth.golden` — regenerated (not a baseline move)

## Bounds in the rebalance rule that named a removed mechanism

Four, each named in the file's new `## What the cut took out of this file` rather than deleted
silently: the Max-Turns count on *Revise Artifact* re-entries; the Phase-3 max-Turns bypass; the
Unresolved-budget check-in as the bound on retries; and `paused_at_task` in the state file. The
Directive-revisions cap survives, held in session context rather than persisted, with that cost
stated in the file.

## Verification

`cd hooks && npm run build && npm test` — build exit 0, tests exit 1 (4 failed, 929 passed).
The suite was red before the work at the same 4 failures in the same 3 files. Dangling references
fell from 24 to 18: the 11 in `agents/orchestrator.md` cleared, `CLAUDE.md`'s
`rules/orchestrator-rebalance.md` cleared by the restore, and six new section-heading anchors
appeared in files this step may not touch (`rules/workbench-tracking.md` ×2,
`agents/reconciler.md`, `docs/upgrading-to-v10-3.md`, `skills/setup/SKILL.md`,
`bin/fusion-session-domain`). No growth-bound baseline was edited; the dispatch-path bound is green
at 327 698 against 380 065.
