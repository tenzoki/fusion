# Coder: plan steps 9, 11, 12 (commit C) of `260829-1226_*_citation-form-drops-store-segment.md`

**Agent:** coder
**Status:** Complete

## Step 9 — cleanup verdict line, paid for by one cut

- `skills/cleanup/SKILL.md` Step 8 gains the `Citations:` bullet (262 bytes): the `verdict=`, `store-prefixed=` and `dangling=` lines of `bin/fusion-citation-check` behind `[ -x ]`, else `citations: helper-missing`.
- The cut: the Step 3 domain bullet, 844 bytes to 254 bytes (-590). It keeps its first two sentences plus the one-place statement; the `domain-cascade.test.ts`/README disagreement account is gone.
- `skills/` surface: 240 365 -> 239 859 bytes (-506; cleanup -328, help -178). Bound 240 439 holds. Golden regenerated; no baseline map edited.

## Step 11 — release texts and version

- `docs/upgrading-to-v10-20.md` (new). `README.md` `## Install`: v10.20 paragraph in, the v10.5 paragraph out, pin `tags/v10.20.0`. `skills/help/SKILL.md` update topic: v10.14->v10.20 paragraph in, the v10.5 paragraph out (17 127 -> 16 949 bytes). `install.sh:27` pin. `.claude-plugin/plugin.json` and the marketplace entry at `10.20.0`; the two `description` strings agree byte for byte.
- `hooks/lib/__tests__/reference-resolution-lint.test.ts` BASELINE re-approved: paths 1537 -> 1544, anchors 215 -> 216, shares stated on the line. The `$SCAN_` lint caught the first draft of the note (a stamp and `$SCAN_DECISIONS` on one line); the bullet was rephrased.

## Step 12 — records

Five `260828-0904_a_*` decisions -> `_i_`, issues `260828-0828`, `-0900`, `-0901`, `-0907` `_o_` -> `_c_`, Circle decision `260829-1225_a_*` -> `_i_`, each with an `Implemented:`/`Resolved:` line citing `f1099c5f` in the storeless form. Plan steps 9, 11, 12 marked `[DONE]`; plan `**Status:**` and marker left to the orchestrator.

Verification: `cd hooks && npm test` — exit 0 (794 tests). `bin/fusion-citation-check`: `store-prefixed=0`.
