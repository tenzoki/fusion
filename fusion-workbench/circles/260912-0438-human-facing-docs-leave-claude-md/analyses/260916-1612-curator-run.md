Curator run — the placement pass over `CLAUDE.md` for the human-facing-docs work item

**Status:** Complete
**Date:** 2026-09-16
**HEAD read:** `99fbaa8a45802e9c728c6060f158eeac2ff98978`
**Mode:** survey. **Placement:** on. No surface is written by this pass.
**Scope:** anchored
**Anchor:** `d874f944e85f77e3bdeceec4a91896cdfaa598bc` (2026-09-11 12:09, `last_curator_run`), 86 commits behind HEAD and an ancestor of it (`git merge-base --is-ancestor`, exit 0).
**Previous curator run:** `260911-1218-curator-run.md`, 2026-09-11, at HEAD `d874f944` — the same commit this run's anchor names, so the bound is exact: everything that pass saw, it saw at this anchor, and this pass re-reads only what moved since.
**Work-item:** 260912-0438-human-facing-docs-leave-claude-md

This is step 7 of `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md`. It classifies every
top-level section of this repository's `CLAUDE.md` under `rules/context-lean-claude-md.md`
`## How to tell "always-on" from "on-demand"` and proposes one relocation entry per moving passage.
The criterion was read explicitly: `bin/fusion-rules` emits that file to no agent, so it was not in the
set Setup loaded, and the dispatch named its path.

**Every destination below is outside this agent's remit.** `README*.md` is exclusion 5. No destination
is written by this pass and none will be written by the apply pass either: `## 9` names the executor
and the defect record, and the source-side removal waits on the destination already carrying the After
text byte for byte.

## 1. Evidence-source counts

| # | Source | Read | Note |
|---|---|---|---|
| 1 | Work items (`$SCAN_BACKLOG` = `circles`) | 28 item records in 28 containers | the claimed one is this run's, read in full with its spec and its plan |
| 2 | Decision records (`$SCAN_DECISIONS`) | 212 files present; 79 live (`_o_`/`_a_`); 19 filed or modified since the anchor, of which 13 are live | the anchored bound is what keeps this from 212 |
| 3 | `git log` over `CLAUDE.md` and `rules/` | 7 commits touch `CLAUDE.md` in `d874f944..HEAD`; 9 rule files touched in the same range, of 15 | `git log --follow --oneline d874f944..HEAD -- CLAUDE.md` |
| 4 | Reviews and analyses | 7 reviews and 9 analyses filed since the anchor, of 118 and 66 live | the load-bearing one is this container's `260916-1321-reviewer-pre-tag-pass-over-the-claude-md-weight-helper-and-the-curators-relocation.md` |
| 5 | `orchestrator-events.jsonl` | 3 829 rows, read as corroboration only | no finding below rests on it |
| 6 | Archive store (`$WORKBENCH/archive`) | 605 files present, **0 archived since the anchor** | nothing to re-read under the bound, and the zero is stated rather than implied |
| 7 | `**Provenance:**` headers | 15 of 15 rule files carry one | every cited decision record resolves to `_a_` or `_i_`; **zero** name a superseded record, so this source produced no Tier 2 retirement candidate |

**Defect corpus as a cross-check, not as a surface:** 105 open (`_o_`) defect records across `$SCAN_ISSUES`.
Three bear on this run and each is named in full at the entry it touches: candidate `C01` carries
`260916-1315_*_the-retired-gates-redundancy-argument-covers-one-direction-and-claude-md-still-claims-the-other.md`,
L14 carries `260916-1320_*_readme-hooks-promises-the-fourth-bound-a-section-of-its-own-and-the-file-has-none.md`,
and `## 5` carries
`260916-1316_*_three-surfaces-state-the-heading-division-rule-and-two-of-them-answer-differently-on-fusions-own-claude-md.md`.
**No open defect contradicts any entry below**, so nothing was downgraded on that ground.

**Absent, not unreadable:** `.claude/rules/` does not exist in this repository — `ls .claude/` returns
`settings.local.json` and `worktrees/` and no `rules/`. Reported as absent, and no claim below involves it.
`./rules/` exists and is this project's own rule directory, read in full. Nothing was unreadable, so no
finding is downgraded under the thin-spot rule.

## 2. Surface sizes

| Surface | Bytes before | Lines before | Bytes after | Lines after |
|---|---|---|---|---|
| `CLAUDE.md` | 62505 | 130 | unchanged — this pass writes nothing | unchanged |
| Project rule files (`./rules/*.md`, 15 files) | 202 961 | — | unchanged | unchanged |
| Decision records under `$SCAN_DECISIONS` | 212 files | — | unchanged | unchanged |

`wc -c CLAUDE.md` and `cat rules/*.md | wc -c`. A survey pass changes no existing statement on any of
the three surfaces, so every "after" is the "before".

## 3. Placement classification

The unit is the top-level heading, per `rules/context-lean-claude-md.md` `### Step 1 — divide the file by
heading, before judging anything`. This file's headings start at `## `, six of them, which is also what
`bin/fusion-claude-md-weight` picks (the shallowest level with at least two headings). The material above
the first `## ` is not a heading passage at all; it is classified anyway, because the dispatch asks for
every section head and because half of it moves. **A qualification this run does not paper over:** an open
defect says three shipped surfaces state the division rule in three different terms and that the rule's two
branches do not name a `# ` document title
(`260916-1316_*_three-surfaces-state-the-heading-division-rule-and-two-of-them-answer-differently-on-fusions-own-claude-md.md`).
On this file the three agree, because `## ` has six headings and `# ` has one; the disagreement is real and
does not bite here.

| Section | Bytes | Verdict | Destination |
|---|---|---|---|
| preamble (title, the two language lines, the identity paragraph, the stand-down history) | 3 074 | **split** — 218 stay, 2 856 move | `README-hooks.md` `## Concept` |
| `## What this is` | 5 779 | **split** — the three one-line helper pointers stay, three bullets move | `README-agents.md`, `README-hooks.md` |
| `## Layout` | 16 906 | **split** — the table stays as a table, 16 404 of row bodies move | `README-agents.md`, `README-hooks.md` |
| `## Conventions` | 16 524 | **split** — five bullets stay, 16 172 move | `README-agents.md`, `README-hooks.md` |
| `## Release process` | 10 372 | **moves** — heading stays as the pointer's anchor, 10 351 of body moves | `README-agents.md`, a new `## Releasing` |
| `## Testing during development` | 202 | **stays** | — |
| `## Where to look when something breaks` | 9 648 | **moves** — heading stays as the pointer's anchor, 9 608 of table moves | `README-hooks.md`, a new `## Where to look when something breaks` |

**What stays, and why each passage answers Step 2's first question yes.** The two language declarations
(`bin/fusion-rules` reads them at the start of every dispatch, whatever the work — the criterion's own
worked *it stays* example). The identity sentences. The pointer table at one line per path. Five conventions
that bind every edit regardless of topic: namespaced dispatch, agents write only to `fusion-workbench/`, the
workbench bootstrap is exclusive to setup, the `dir/*` `.gitignore` form, and critical procedures are skills
rather than "MUST" lines — plus the manifest-version bump, which is already a one-line Layout row, and a
one-line growth-bound warning (L14's pointer). `## Testing during development` is two sentences that bind
any session loading the plugin from disk.

**Not a target.** The plan infers a residue of 5 000 to 7 000 bytes and says explicitly that no step is
measured against it. Nothing here was chosen to hit a number; the figures below are what the classification
produced.

## 4. Comparison counts

Per surface pair: the candidate-selection rule, how many pairs it produced, how many were read. **Every
verdict covers the pairs its rule reached, not the corpus.** Corpus counts are derived, with the command named.

| Pair | Selection rule | Pairs | Read | Verdict |
|---|---|---|---|---|
| 1. decision × decision | The 13 live decisions modified since the anchor, each against the live decisions sharing a subject word (`find` over `$SCAN_DECISIONS` for `*_o_*`/`*_a_*` with `-newermt 2026-09-11`) | 13 | 13 | None overturns another. Qualified by the selector: 66 live records untouched since the anchor were not re-opened, on the bound the previous run's exhaustive pass establishes. |
| 2. decision × rule file | The same 13, against the 9 rule files touched in `d874f944..HEAD` | 13 | 13 | None. `260916-1006_*` and `rules/context-lean-claude-md.md` agree: the file cites that record as its binding decision. |
| 3. decision × `CLAUDE.md` | The same 13, against the sentences of `CLAUDE.md` naming the same subject | 13 | 6 opened | None. The four whose subject the file states — the reach ruling, the fifth status value, the cadence merge, the helper roster — are each reflected at HEAD. |
| 4. rule file × rule file | The 9 touched rule files against the other 14 | 9 | 9 | None between two rule files. One contradiction reaches a rule file from outside it and is already filed — see `## 5`. |
| 5. rule file × `CLAUDE.md` | The 9 touched rule files against `CLAUDE.md` | 9 | 9 | None binding. `rules/context-lean-claude-md.md` and this 62 505-byte file are not two binding statements in conflict; the file is the drift the rule describes, which is what this run addresses rather than reports. |
| 6. `CLAUDE.md` × itself | Every one of the 7 top-level passages against every other, read in full | 21 | 21 | None internal. One statement is false against the tree rather than against another sentence here — candidate `C01`. |

## 5. A contradiction reported and not resolved

`260916-1316_*_three-surfaces-state-the-heading-division-rule-and-two-of-them-answer-differently-on-fusions-own-claude-md.md`
is open and already carries an acceptance test. It sets `rules/context-lean-claude-md.md` — one of this
agent's three surfaces — against `agents/curator.md` and `bin/fusion-claude-md-weight`, which are exclusions
5 and 6. **No decision record is filed for it**, because it is not two defensible positions: one statement of
the division rule is incomplete, which is a defect, and the defect exists. Nothing is edited on any side.

## 6. Pre-edit content of every decision record this run intends to modify

**None.** No ledger entry below has a decision record as its surface, so there is no pre-edit content to
carry. The obligation is unconditional and it is discharged by this statement rather than skipped.

## 7. The ledger

**Two stated deviations from the entry schema**, both mechanical and both following the precedent of
`260911-1218-curator-run.md`.

1. The Before and After texts sit in fenced blocks rather than blockquotes. A blockquote prefix would have
   to be stripped on apply, and the exactness of the Before text is what the staleness check rests on;
   and `hooks/lib/__tests__/workbench-citation-lint.test.ts` reads every `.md` under the workbench, while
   several Before texts carry record citations with literal markers. The fence is the exemption that suite
   already recognises. The fences are five backticks, because one Before text contains a three-backtick
   fence of its own.
2. Where a grouped entry covers more than one table row or bullet, the `**Pointer left behind:**` is more
   than one line — one per path, which is the residue shape this work asks for. The entry names every row
   it covers, so the gate can decline it whole or ask for it split.

**The only transformation applied to a moving passage is `rows2bullets`**, and it is stated once here rather
than per entry: where the source is a `| ` + "`path`" + ` | body |` Layout row and the destination section is
prose, the row becomes `- **` + "`path`" + `** — body`. The body text is byte-identical across the change.
Every other entry's After text equals its Before text byte for byte, which is what "a passage that moves
moves as it reads" means in the spec.

**The preserve list forbids nothing here.** Every entry is a relocation, and all five categories permit one:
they guard the item's content, not the place it sits. Four entries carry preserve-list material and say so —
L14 and L16 (critical procedure, hidden coupling), L22 (nineteen non-obvious failure modes), and the Layout
pointers (authoritative pointers). None is deleted and none needs the Tier 2 exception.

### L01 — the guard stand-down history and the deleted branch-switch policy leave the preamble

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-hooks.md` `## Concept`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-hooks.md` `## Concept` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 2856
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The region starts mid-paragraph, at the sentence after the two identity sentences, which stay. The pointer continues that same paragraph, so no blank line is written or removed.

**Before:**

`````markdown
**No part of the guard self-disables here any more, because no part of it decides anything anywhere.** One stand-down used to: the write-tool branch of `hooks/guard.ts` recognised this repository by a `.claude-plugin/plugin.json` matching the plugin's own name and allowed every write, so a fusion developer could edit the plugin's own source. It was built because the guard used to protect `agents/**`, `rules/**` and the plugin manifest, which is correct for a project *using* fusion and wrong when developing fusion itself, where every edit to those files would have been blocked and written back. **That protection was removed on 260812**, along with the whole protected-path half of the guard, and the stand-down outlived it by four days: it went on 260816 with the guard's last verdict, so the hook now behaves identically in this tree and in a consuming project (`hooks/guard.ts`, the header's account of what it used to check). A second stand-down covered the churn heatmap in `hooks/tracker.ts` and went with the heatmap on 260815.

**The rule the two established outlives both of them, and it is what the next stand-down is measured against:** a stand-down is evaluated in the coordinate space the mechanism keys its state by. The write-tool check read cwd (`isFusionPluginCwd()`, deleted from `hooks/lib/self-detect.ts` on 260816), because `guard.ts` computed its verdict in cwd's space; churn asked about the workbench root it walked up to (`isFusionPluginRoot(dir)`), because its keys were anchored there. A root-anchored mechanism with a cwd-anchored stand-down misses every session started one directory down, and the reverse is the reverse hole. Both halves of that were measured on the protected-path measurement — whose stand-down had to move up to the root in v6.0.1 when its measurement root did, or a session started in `fusion-workbench/`, the ordinary case here, would have begun reverting the developer's own files. Two mechanisms and their stand-downs are gone. `hooks/lib/self-detect.ts` now keeps `isFusionPluginRoot(dir)` alone and with no caller, so the root-anchored form stays available to whoever needs it next, and `bin/fusion-plugin-cwd` is the only surviving implementation of the cwd-anchored form.

A **git branch-switch policy** used to run here too, and it was the one thing the stand-down never covered. It was deleted outright on 260809: it decided from a shell command's *text* whether the command would move HEAD, which is the same undecidable question the write-path classifier answered until v6.0.0, and in one afternoon it cost five patches, a sixth open entrance, and 24 consecutive false blocks against the agents' own verification commands. Nothing about a `Bash` command is read by the guard any more, in this repository or any other, and since 260812 nothing a `Bash` command *does* is measured either.
`````

**After** (what the destination must carry):

`````markdown
**No part of the guard self-disables here any more, because no part of it decides anything anywhere.** One stand-down used to: the write-tool branch of `hooks/guard.ts` recognised this repository by a `.claude-plugin/plugin.json` matching the plugin's own name and allowed every write, so a fusion developer could edit the plugin's own source. It was built because the guard used to protect `agents/**`, `rules/**` and the plugin manifest, which is correct for a project *using* fusion and wrong when developing fusion itself, where every edit to those files would have been blocked and written back. **That protection was removed on 260812**, along with the whole protected-path half of the guard, and the stand-down outlived it by four days: it went on 260816 with the guard's last verdict, so the hook now behaves identically in this tree and in a consuming project (`hooks/guard.ts`, the header's account of what it used to check). A second stand-down covered the churn heatmap in `hooks/tracker.ts` and went with the heatmap on 260815.

**The rule the two established outlives both of them, and it is what the next stand-down is measured against:** a stand-down is evaluated in the coordinate space the mechanism keys its state by. The write-tool check read cwd (`isFusionPluginCwd()`, deleted from `hooks/lib/self-detect.ts` on 260816), because `guard.ts` computed its verdict in cwd's space; churn asked about the workbench root it walked up to (`isFusionPluginRoot(dir)`), because its keys were anchored there. A root-anchored mechanism with a cwd-anchored stand-down misses every session started one directory down, and the reverse is the reverse hole. Both halves of that were measured on the protected-path measurement — whose stand-down had to move up to the root in v6.0.1 when its measurement root did, or a session started in `fusion-workbench/`, the ordinary case here, would have begun reverting the developer's own files. Two mechanisms and their stand-downs are gone. `hooks/lib/self-detect.ts` now keeps `isFusionPluginRoot(dir)` alone and with no caller, so the root-anchored form stays available to whoever needs it next, and `bin/fusion-plugin-cwd` is the only surviving implementation of the cwd-anchored form.

A **git branch-switch policy** used to run here too, and it was the one thing the stand-down never covered. It was deleted outright on 260809: it decided from a shell command's *text* whether the command would move HEAD, which is the same undecidable question the write-path classifier answered until v6.0.0, and in one afternoon it cost five patches, a sixth open entrance, and 24 consecutive false blocks against the agents' own verification commands. Nothing about a `Bash` command is read by the guard any more, in this repository or any other, and since 260812 nothing a `Bash` command *does* is measured either.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
Why no part of it stands down here any more, the coordinate-space rule the two retired stand-downs left behind, and the git branch-switch policy deleted on 260809: `README-hooks.md` `## Concept`.
`````

### L02 — the agent roster bullet leaves `## What this is`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## The agents`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## The agents` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 1673
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** `README-agents.md` `## The agents` already carries a per-agent table; this passage's account of the v11 merge and of who is parameterised belongs beside it.

**Before:**

`````markdown
- 11 specialized agents — orchestrator (top-level dispatcher) plus coder, ontocoder, reviewer, planner, shaper, reconciler, analyst, consultant, editor and curator. **Five prompts went at v11 and one arrived by merger, 15 agents to 11.** `taskplanner` with the session work queue, `playmaker` with the Circle portfolio, `bugfixer` with the phase procedure holding its one self-healing attempt — its diagnose-before-editing contract survives in `coder` and `ontocoder` — and `coderev` and `ontorev` merged into one `reviewer` taking a `**Review domain:**` line. Three further merges were attempted and **stopped on the dispatch-path byte bound**, each with its measurement in the C8 commit message, so `shaper`, `consultant` and `reconciler` stay separate. `reconciler` is the one agent still parameterised by domain (`code | data`) since v1.11.0; `strategic` and `knowledge` left on 2026-08-15 at zero and two dispatches. `planner` is **not** one and parses no `**Domain:**` line — it takes `**Executors:**`. Which agents read a dispatch parameter, and which lines each takes, is authored once and nowhere else: `README-agents.md` `## Dispatch parameters`. `editor` (added v5.2.0) is the produce-only Redakteur — it writes, revises, translates (en↔de) and renders customer-ready deliverables (Markdown, branded pptx) project-side; it never reviews, files issues, or dispatches. `curator` (added v8.2.0) reconciles a project's three normative surfaces — its decision records, its project-owned rule files and `CLAUDE.md` — against its own recorded history; every proposed change carries an evidence tier and a citation, and nothing lands before a user gate.
`````

**After** (what the destination must carry):

`````markdown
- 11 specialized agents — orchestrator (top-level dispatcher) plus coder, ontocoder, reviewer, planner, shaper, reconciler, analyst, consultant, editor and curator. **Five prompts went at v11 and one arrived by merger, 15 agents to 11.** `taskplanner` with the session work queue, `playmaker` with the Circle portfolio, `bugfixer` with the phase procedure holding its one self-healing attempt — its diagnose-before-editing contract survives in `coder` and `ontocoder` — and `coderev` and `ontorev` merged into one `reviewer` taking a `**Review domain:**` line. Three further merges were attempted and **stopped on the dispatch-path byte bound**, each with its measurement in the C8 commit message, so `shaper`, `consultant` and `reconciler` stay separate. `reconciler` is the one agent still parameterised by domain (`code | data`) since v1.11.0; `strategic` and `knowledge` left on 2026-08-15 at zero and two dispatches. `planner` is **not** one and parses no `**Domain:**` line — it takes `**Executors:**`. Which agents read a dispatch parameter, and which lines each takes, is authored once and nowhere else: `README-agents.md` `## Dispatch parameters`. `editor` (added v5.2.0) is the produce-only Redakteur — it writes, revises, translates (en↔de) and renders customer-ready deliverables (Markdown, branded pptx) project-side; it never reviews, files issues, or dispatches. `curator` (added v8.2.0) reconciles a project's three normative surfaces — its decision records, its project-owned rule files and `CLAUDE.md` — against its own recorded history; every proposed change carries an evidence tier and a citation, and nothing lands before a user gate.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **agents** — the 11 prompts, what each is for, which dispatch parameter each reads, and the five that went at v11. Detail: `README-agents.md` `## The agents`.
`````

### L03 — the compliance-guard bullet leaves `## What this is`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-hooks.md` `## Concept`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-hooks.md` `## Concept` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 1507
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** `## Concept` already carries the two removal subsections this passage summarises.

**Before:**

`````markdown
- Compliance guard, **observation-only since 260816** (TypeScript hooks, compiled to `hooks/dist/`) — it decides nothing. The PreToolUse hook receives the four write tools, Bash and the sub-agent dispatch tool, allows every one of them, and exists for three products: one `guard_allow` row per write-tool call in `.guard-state/events.jsonl`, which is what the monitor's panel renders, one `guard_advisory` per problem the configuration loader hands back, and (v10.8.0) the machine-written `task_start`/`task_done` pair per dispatch in `orchestrator-events.jsonl` — `hooks/lib/orchestrator-events.ts`, written only while an orchestrator session is in flight. Every path through it writes `{}`; the name is historical and is kept because the event vocabulary, the state directory and the monitor panel all carry it (`hooks/guard.ts`, header). What it used to do went in three steps. It blocked the four write tools against a `protectedPaths` list and measured those paths around every guarded call so a change reaching them any other way was written back; that half was removed on 260812. It counted per-file churn in a heatmap and emitted warning events for the monitor at configured thresholds; that half warned, never blocked, and was removed on 260815. It blocked writes into the decision-governed categories at `high` sensitivity and halted after three consecutive blocks; that check, the halt, the consecutive-block counter, the escalation module and the halt-clearing script were removed on 260816
`````

**After** (what the destination must carry):

`````markdown
- Compliance guard, **observation-only since 260816** (TypeScript hooks, compiled to `hooks/dist/`) — it decides nothing. The PreToolUse hook receives the four write tools, Bash and the sub-agent dispatch tool, allows every one of them, and exists for three products: one `guard_allow` row per write-tool call in `.guard-state/events.jsonl`, which is what the monitor's panel renders, one `guard_advisory` per problem the configuration loader hands back, and (v10.8.0) the machine-written `task_start`/`task_done` pair per dispatch in `orchestrator-events.jsonl` — `hooks/lib/orchestrator-events.ts`, written only while an orchestrator session is in flight. Every path through it writes `{}`; the name is historical and is kept because the event vocabulary, the state directory and the monitor panel all carry it (`hooks/guard.ts`, header). What it used to do went in three steps. It blocked the four write tools against a `protectedPaths` list and measured those paths around every guarded call so a change reaching them any other way was written back; that half was removed on 260812. It counted per-file churn in a heatmap and emitted warning events for the monitor at configured thresholds; that half warned, never blocked, and was removed on 260815. It blocked writes into the decision-governed categories at `high` sensitivity and halted after three consecutive blocks; that check, the halt, the consecutive-block counter, the escalation module and the halt-clearing script were removed on 260816
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **guard and hooks** — observation-only since 260816: what each hook writes, and the three checks that were removed. Detail: `README-hooks.md` `## Concept`.
`````

### L04 — the skill-bodies bullet leaves `## What this is`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `### `skills/` — one file per slash command`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `### `skills/` — one file per slash command` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 2273
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Carries a claim an open defect says is false — see `## 8`, candidate `C01`. The passage moves as it reads; the correction is that defect's, not this entry's. The pointer deliberately writes no slash-command token, because the enumeration lint reads every one in this file.

**Before:**

`````markdown
- Skill bodies, one per directory under `skills/`; that listing is the authoritative set. **Three names are the ordinary session surface** — `/fusion:setup` at the start, `/fusion:cleanup` at the end, `/fusion:cadence` to see what happened. Setup keeps two steps and hands the other nine to `/fusion:check` (260910), which runs the periodic installation checks behind a cached marker; `--only <selector>` runs exactly one. **`/fusion:cleanup` is commit and push under the commit lock and nothing else** (260910). The end-of-session pipeline is gone, and the `--only`/`--skip` selector vocabulary went with it, there being no steps left to select; its scheduled issue-filing step was **removed, not re-homed**, so nothing files a record on the user's behalf at a session's end. What that pipeline performed inline is four commands, each performing its own procedure and triggering no other: `/fusion:reconcile` (new — one reconciler dispatch, no gate opened), `/fusion:archive` (which now asks on every run, its exemption gone with the pipeline), `/fusion:curate` (the `CLAUDE.md` reconciliation, which keeps its user gate and the curator's write authority behind it) and `/fusion:post` (the note for whoever pulls this work next). The rest are situational: `/fusion:migrate`, `/fusion:help`, `/fusion:memo`, `/fusion:commit`, `/fusion:news` — the last reads what another checkout left, out of a fetched ref, through `bin/fusion-forum`. Departed directories, `next` in this release at `2a785ba2`: `revise-claude-md` and `unlock` went on 2026-08-15 with an earlier collapse, the first's autonomous rewrite of `CLAUDE.md` becoming the curator's gated pass and the second's settings merge becoming `/fusion:check`'s `permissions` selector; `direct` went on 260910 with the per-unit-of-work record it created, its whole body having dispatched the shaper mode that wrote one; `log-activity` went on 260916, its two procedures merged into `/fusion:cadence`, which now writes the activity log before it digests it. None of those names may be written in the `/fusion:` form anywhere in this file — the lint reads every such token, fails on one that names no directory, and asserts the match in the other direction too, which is why every body under `skills/` is named here.
`````

**After** (what the destination must carry):

`````markdown
- Skill bodies, one per directory under `skills/`; that listing is the authoritative set. **Three names are the ordinary session surface** — `/fusion:setup` at the start, `/fusion:cleanup` at the end, `/fusion:cadence` to see what happened. Setup keeps two steps and hands the other nine to `/fusion:check` (260910), which runs the periodic installation checks behind a cached marker; `--only <selector>` runs exactly one. **`/fusion:cleanup` is commit and push under the commit lock and nothing else** (260910). The end-of-session pipeline is gone, and the `--only`/`--skip` selector vocabulary went with it, there being no steps left to select; its scheduled issue-filing step was **removed, not re-homed**, so nothing files a record on the user's behalf at a session's end. What that pipeline performed inline is four commands, each performing its own procedure and triggering no other: `/fusion:reconcile` (new — one reconciler dispatch, no gate opened), `/fusion:archive` (which now asks on every run, its exemption gone with the pipeline), `/fusion:curate` (the `CLAUDE.md` reconciliation, which keeps its user gate and the curator's write authority behind it) and `/fusion:post` (the note for whoever pulls this work next). The rest are situational: `/fusion:migrate`, `/fusion:help`, `/fusion:memo`, `/fusion:commit`, `/fusion:news` — the last reads what another checkout left, out of a fetched ref, through `bin/fusion-forum`. Departed directories, `next` in this release at `2a785ba2`: `revise-claude-md` and `unlock` went on 2026-08-15 with an earlier collapse, the first's autonomous rewrite of `CLAUDE.md` becoming the curator's gated pass and the second's settings merge becoming `/fusion:check`'s `permissions` selector; `direct` went on 260910 with the per-unit-of-work record it created, its whole body having dispatched the shaper mode that wrote one; `log-activity` went on 260916, its two procedures merged into `/fusion:cadence`, which now writes the activity log before it digests it. None of those names may be written in the `/fusion:` form anywhere in this file — the lint reads every such token, fails on one that names no directory, and asserts the match in the other direction too, which is why every body under `skills/` is named here.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **skills** — one directory per slash command, which three are the ordinary session surface, which four the end-of-session pipeline became, and which directories have left. Detail: `README-agents.md`, the skills section.
`````

### L05 — the `agents/*.md` Layout row leaves the table

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## The agents`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## The agents` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 572
- **Transform:** rows2bullets
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**

`````markdown
| `agents/*.md` | The 11 agent prompts. **No agent declares a `tools:` line**; all 11 inherit tools and model from the parent session. The orchestrator's allowlist was the last one and went on 260913, so the top-level dispatcher reaches every MCP server, `ToolSearch`, `WebSearch` and every future tool without an enumeration anybody maintains; the two exclusions it carried, the consultant and no recursion, survive as prose in `agents/orchestrator.md` that nothing enforces (`260913-0909_*_may-the-orchestrator-reach-every-tool-and-may-an-agent-dispatch-another.md`). |
`````

**After** (what the destination must carry):

`````markdown
- **`agents/*.md`** — The 11 agent prompts. **No agent declares a `tools:` line**; all 11 inherit tools and model from the parent session. The orchestrator's allowlist was the last one and went on 260913, so the top-level dispatcher reaches every MCP server, `ToolSearch`, `WebSearch` and every future tool without an enumeration anybody maintains; the two exclusions it carried, the consultant and no recursion, survive as prose in `agents/orchestrator.md` that nothing enforces (`260913-0909_*_may-the-orchestrator-reach-every-tool-and-may-an-agent-dispatch-another.md`).
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
| `agents/*.md` | The 11 agent prompts; none declares a `tools:` line. Detail: `README-agents.md` `## The agents`. |
`````

### L06 — the `hooks/` and `fusion.json` Layout rows leave the table

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-hooks.md` `## Files`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-hooks.md` `## Files` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 4738
- **Transform:** rows2bullets
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Two paths, so the pointer left behind is two rows — one per path, which is the residue shape this work asks for. Declining the entry keeps both rows as they stand.

**Before:**

`````markdown
| `hooks/` | TypeScript source + compiled `dist/`. SessionStart, PreToolUse, PostToolUse and SubagentStop hooks; `hooks/hooks.json` is the registration and the list to read. Uses `process.cwd()` not `__dirname` for project-local state. SessionStart runs five independent commands: the `FUSION_PLUGIN_ROOT` export, the static "Fusion loaded" banner, `session-start.ts`, `hooks/session-id.ts` and (v10.8.0) the identity export — one `bin/fusion-identity` run whose `PERSON`/`CHECKOUT` land as `FUSION_PERSON`/`FUSION_CHECKOUT` in `$CLAUDE_ENV_FILE`; `session-id.ts` additionally exports `FUSION_SESSION_ID` there. The third warns, as a `systemMessage`, when a workbench root is found *above* the working directory rather than at it. One cwd-anchored resolution is left for that warning to be about, the work-tree preference of the three `bin/` helpers listed under `bin/fusion-plugin-cwd`; the two sharper ones it used to name went with the protected-path deny on 260812 and with the guard's last verdict on 260816. The warning fixes none of it, and makes the assumption audible at the one moment the working directory is still cheap to change. The fourth puts the Claude Code session identifier in front of the model on plain stdout, the channel measured to reach it, and it is its own command for a sharper reason than the other three are theirs: one process writes one stdout, and a recognised JSON envelope routes `systemMessage` to the user while leaving the model's copy empty, so the warning and the identifier cannot come out of one process. The PreToolUse guard inspects `Bash` for **nothing** and allows it immediately, writing no guard state at all **when the project's `fusion.json` is valid**; a sub-agent dispatch is allowed the same way, writing only its machine-written event row and never guard state. A broken file, a retired top-level key inside it, or a leftover `fusion-guard.json` at the project root each emit one `guard_advisory` per guarded call, Bash included, because the diagnostic loop sits above the Bash return (`hooks/guard.ts`, and the comment there states the departure). The last policy that read a command's text — the git branch/worktree guard, with the shell lexer and command-word resolver under it — was deleted on 260809; the last thing that watched what a command *did* — the protected-path measurement, a fingerprint of the whole protected list taken in `guard.ts` before the call and compared again in `tracker.ts` after it, with anything that moved written back and the guard halted — was deleted on 260812 with the rest of that half. **No check remains**: the halt and the decision-governed categories both went on 260816. Documented for users in `README-hooks.md`. |
| `fusion.json` + `templates/fusion.json` | The per-project configuration, renamed from `fusion-guard.json` on 260816 when the last guard setting left it. `/fusion:setup` seeds the template into a consuming project's root, git-tracked so every change to it shows in a diff; the root copy here equals the template outside the top-level keys a project is meant to set for itself, and that is exactly what `config.test.ts` pins: it cuts the keys named in its `PROJECT_SET_KEYS` out of both sides and holds every remaining byte identical. This repository sets one of the two keys that constant names: its declaration of the non-Markdown paths that carry record citations (`citations.extraPaths`, added 260831), which is the loader's **only** live leaf. The other, the `orchestrator` container, holds nothing but two retirements since 260910 — `orchestrator.maxTurns`, which went with the Turn loop, and `orchestrator.dispatchMinutes`, which went with the dispatch bound — and this repository declares neither. The template declares none of them and carries only the documentation note for each. The merge is per **leaf** key across **two** layers — this file, then the built-in `DEFAULTS` in `hooks/lib/config.ts` — so a declared value wins outright and an omitted or invalid one falls straight through to what fusion ships. The plugin layer that used to sit between them went with the guard settings it carried. Retirement now has two scopes rather than one leaf: `RETIRED_PROJECT_FILES` names a leftover `fusion-guard.json` at the project root and `RETIRED_TOP_LEVEL_KEYS` names `guard`, `decisions`, `escalation` and, since 260824, `churn` inside the new file; either earns one advisory per guarded call until the file or the key is deleted, and the file advisory says there is nothing to copy across, because every setting that file could carry is retired too. Nothing in the guard defends this file from an agent; the git diff is the only bound left, which is what the template's `_gitTracked` note says. |
`````

**After** (what the destination must carry):

`````markdown
- **`hooks/`** — TypeScript source + compiled `dist/`. SessionStart, PreToolUse, PostToolUse and SubagentStop hooks; `hooks/hooks.json` is the registration and the list to read. Uses `process.cwd()` not `__dirname` for project-local state. SessionStart runs five independent commands: the `FUSION_PLUGIN_ROOT` export, the static "Fusion loaded" banner, `session-start.ts`, `hooks/session-id.ts` and (v10.8.0) the identity export — one `bin/fusion-identity` run whose `PERSON`/`CHECKOUT` land as `FUSION_PERSON`/`FUSION_CHECKOUT` in `$CLAUDE_ENV_FILE`; `session-id.ts` additionally exports `FUSION_SESSION_ID` there. The third warns, as a `systemMessage`, when a workbench root is found *above* the working directory rather than at it. One cwd-anchored resolution is left for that warning to be about, the work-tree preference of the three `bin/` helpers listed under `bin/fusion-plugin-cwd`; the two sharper ones it used to name went with the protected-path deny on 260812 and with the guard's last verdict on 260816. The warning fixes none of it, and makes the assumption audible at the one moment the working directory is still cheap to change. The fourth puts the Claude Code session identifier in front of the model on plain stdout, the channel measured to reach it, and it is its own command for a sharper reason than the other three are theirs: one process writes one stdout, and a recognised JSON envelope routes `systemMessage` to the user while leaving the model's copy empty, so the warning and the identifier cannot come out of one process. The PreToolUse guard inspects `Bash` for **nothing** and allows it immediately, writing no guard state at all **when the project's `fusion.json` is valid**; a sub-agent dispatch is allowed the same way, writing only its machine-written event row and never guard state. A broken file, a retired top-level key inside it, or a leftover `fusion-guard.json` at the project root each emit one `guard_advisory` per guarded call, Bash included, because the diagnostic loop sits above the Bash return (`hooks/guard.ts`, and the comment there states the departure). The last policy that read a command's text — the git branch/worktree guard, with the shell lexer and command-word resolver under it — was deleted on 260809; the last thing that watched what a command *did* — the protected-path measurement, a fingerprint of the whole protected list taken in `guard.ts` before the call and compared again in `tracker.ts` after it, with anything that moved written back and the guard halted — was deleted on 260812 with the rest of that half. **No check remains**: the halt and the decision-governed categories both went on 260816. Documented for users in `README-hooks.md`.
- **`fusion.json` + `templates/fusion.json`** — The per-project configuration, renamed from `fusion-guard.json` on 260816 when the last guard setting left it. `/fusion:setup` seeds the template into a consuming project's root, git-tracked so every change to it shows in a diff; the root copy here equals the template outside the top-level keys a project is meant to set for itself, and that is exactly what `config.test.ts` pins: it cuts the keys named in its `PROJECT_SET_KEYS` out of both sides and holds every remaining byte identical. This repository sets one of the two keys that constant names: its declaration of the non-Markdown paths that carry record citations (`citations.extraPaths`, added 260831), which is the loader's **only** live leaf. The other, the `orchestrator` container, holds nothing but two retirements since 260910 — `orchestrator.maxTurns`, which went with the Turn loop, and `orchestrator.dispatchMinutes`, which went with the dispatch bound — and this repository declares neither. The template declares none of them and carries only the documentation note for each. The merge is per **leaf** key across **two** layers — this file, then the built-in `DEFAULTS` in `hooks/lib/config.ts` — so a declared value wins outright and an omitted or invalid one falls straight through to what fusion ships. The plugin layer that used to sit between them went with the guard settings it carried. Retirement now has two scopes rather than one leaf: `RETIRED_PROJECT_FILES` names a leftover `fusion-guard.json` at the project root and `RETIRED_TOP_LEVEL_KEYS` names `guard`, `decisions`, `escalation` and, since 260824, `churn` inside the new file; either earns one advisory per guarded call until the file or the key is deleted, and the file advisory says there is nothing to copy across, because every setting that file could carry is retired too. Nothing in the guard defends this file from an agent; the git diff is the only bound left, which is what the template's `_gitTracked` note says.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
| `hooks/` | TypeScript source and compiled `dist/`; the four hooks and what each writes. Detail: `README-hooks.md` `## Files`. |
| `fusion.json` + `templates/fusion.json` | The per-project configuration, its two-layer merge and its retired keys. Detail: `README-hooks.md`, the per-project configuration section. |
`````

### L07 — the `install.sh` Layout row leaves the table

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Releasing (new)`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Releasing (new)` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 400
- **Transform:** rows2bullets
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Goes with the release material rather than with the hooks: `### HTTPS installer (`install.sh`)` is today a subsection of `## Release process`, and the two move together.

**Before:**

`````markdown
| `install.sh` | HTTPS `curl \| bash` installer (repo root). Downloads the tarball into `~/.fusion`, writes the `~/.local/bin/fusion` launcher. The launcher takes an agent arg (`fusion coder`, default `orchestrator`, auto-namespaced), an opt-in `--yolo` (`--dangerously-skip-permissions`), and `--update`/`--uninstall`/`--where`. Replaced the old `bin/fu` project-local launcher (removed v3.20.0). |
`````

**After** (what the destination must carry):

`````markdown
- **`install.sh`** — HTTPS `curl \| bash` installer (repo root). Downloads the tarball into `~/.fusion`, writes the `~/.local/bin/fusion` launcher. The launcher takes an agent arg (`fusion coder`, default `orchestrator`, auto-namespaced), an opt-in `--yolo` (`--dangerously-skip-permissions`), and `--update`/`--uninstall`/`--where`. Replaced the old `bin/fu` project-local launcher (removed v3.20.0).
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
| `install.sh` | The HTTPS `curl \| bash` installer and the `fusion` launcher it writes. Detail: `README-agents.md` `## Releasing`. |
`````

### L08 — the nine `rules/*` Layout rows leave the table

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `### Adding rules`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `### Adding rules` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 5585
- **Transform:** rows2bullets
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** **The pointer collapses nine rows to one, and that is a judgement the gate may overturn.** `README-agents.md` `## Plugin structure` already enumerates the corpus file by file, so nine pointer rows would be nine pointers into one list. Declining this entry keeps all nine rows; approving it with a request for nine pointer rows is also available and costs only the pointer text.

**Before:**

`````markdown
| `rules/fusion-workbench-conventions.md` | The **single authoring home** for the workbench layout, the Origin Rule, the operative half of the `bin/fusion-paths` resolution contract (single resolution point, Setup-step-2 call, signature, exit codes, the two invariants), the issue/planning and decision marker vocabularies, marker globs, filename patterns, filing, inline tracking and the decision-record template. Auto-loaded into every agent. Since v4.0.0 no agent prompt or skill body carries a competing layout definition — they resolve paths at run time and cite this file. Its own header table names the five topics that were partitioned out of it and where each now lives. |
| `rules/workbench-path-resolution.md` | The resolver's **authoring** half: the `<name>` namespace (agent OR skill, one flat set), the full key table, and why a key set is derived by grepping the consumer's own prompt rather than declared. Emitted to **no agent** — read it when writing or editing a consumer prompt, or changing `bin/fusion-paths`. Added v5.x. |
| `rules/fusion-workbench-conventions.md` `## Backlog entries — work items` | The **work item**: one directory per unit of work under `circles/`, the record inside it under the directory's own name and no marker on either, its state the `**Status:**` head field (`open`/`claimed`/`paused`/`done`/`dropped`), its holder the `**Claim:**` field's eight hex characters, its edges the `**Depends-on:**` list of item basenames. Always-on, in the conventions rather than in a file of its own, and the three-sentence maintenance mandate sits beside it. Two rule files went on 260910: the Circle-record rule, carrying the state vocabulary and record templates, retired outright with the container; and the backlog-maintenance rule, folded back in here because the mandate is now three sentences rather than a file. Neither path is written out, because neither resolves — read them at the previous release's tag. |
| `rules/rule-file-provenance.md` | The **single authoring home** for the `**Provenance:**` header on rule files — placement, the three citation forms, what `provenance-header-lint.test.ts` checks and what it cannot, who carries the obligation. Emitted to **no agent** — read it before creating or editing any file under a `rules/` directory. Added v5.x (same step). |
| `rules/workbench-tracking.md` | The **single authoring home** for the record-versus-live-state split over a workbench's root entries — which of them a project that tracks its workbench should commit, which it should not, why `.guard-state/` is classified per file rather than per directory, and why the archive roll rather than a ceiling is what preserves the guard event log. Partitioned out of `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout` on 260818, which left a pointer there. Emitted to **no agent** — its two consumers are a human writing a project's `.gitignore` and the archive step of `/fusion:cleanup`, which cites it in `skills/archive/SKILL.md`. Added v10.x (decisions `260816-0711_*_where-does-the-tracked-workbench-split-live-now-that-the-home-it-was-meant-to-move-to-is-gone.md` for the move, `260816-1707_*_to-whom-is-the-new-workbench-tracking-rule-emitted-when-its-consumers-are-a-human-and-a-skill.md` for the emission target). |
| `rules/review-contract.md` | The **single authoring home** for what a review file contains: the mandated header fields (`**Reviewed-range:**`, which is what `bin/fusion-review-coverage` tiles, `**Not-opened:**` and `**Filed by:**`), the per-topic working files and the final consolidated review, lifted out of the two review prompts at v10.6.0 and outliving them: those two merged into `agents/reviewer.md` at v11. A **conditional** emission, not part of the always-on set: `bin/fusion-rules` emits it behind `IS_REVIEWER_AGENT`, which is set for exactly `reviewer`, and `review-coverage-mandate.test.ts` pins that. Every other agent would be carrying a contract it can never apply. |
| `rules/project-language.md` | The language cascade's reasoning and edge semantics (deliverable-language source, profile resolution incl. per-family fallback, the settled dashboard reading, fusion's own dual role). Emitted to `editor` only — its deliverable-language halt lives here; the operative core (declarations, fallback chain, four-surface assignment, exempt groups) stays always-on in the conventions' `## Project language`. Partitioned at decision `260827-1056_*_which-parts-of-the-language-and-backlog-rules-does-every-dispatch-still-carry.md`. |
| `rules/orchestrator-rebalance.md` | The Rebalance gate's per-option mechanics, **Rebalance bounding**, and the re-sharpening contract. Emitted to no agent: read through `$FUSION_PLUGIN_ROOT` at the gate or the re-sharpen trigger; the four option names and the trigger anchors stay in the prompt. Partitioned at decision `260827-1210_*_do-the-rare-orchestrator-flows-stay-in-every-sessions-context.md`. |
| `rules/decision-record-examples.md` | Worked examples of the decision-marker transitions (_o_→_a_→_i_, _i_→_s_, _o_→_d_). Conditionally emitted since 260827 to the transition agents — `orchestrator`, `shaper`, `planner` and `reconciler`, the `IS_DECISION_TRANSITION_AGENT` case in `bin/fusion-rules` — after the user gate in decision `260827-0830_*_do-the-decision-record-worked-examples-stay-on-the-always-on-floor.md` moved it off the always-on floor to make room for critical-stance §5; every other agent keeps the marker vocabulary (always-on, in the conventions) and the pointer at the template site. |
`````

**After** (what the destination must carry):

`````markdown
- **`rules/fusion-workbench-conventions.md`** — The **single authoring home** for the workbench layout, the Origin Rule, the operative half of the `bin/fusion-paths` resolution contract (single resolution point, Setup-step-2 call, signature, exit codes, the two invariants), the issue/planning and decision marker vocabularies, marker globs, filename patterns, filing, inline tracking and the decision-record template. Auto-loaded into every agent. Since v4.0.0 no agent prompt or skill body carries a competing layout definition — they resolve paths at run time and cite this file. Its own header table names the five topics that were partitioned out of it and where each now lives.
- **`rules/workbench-path-resolution.md`** — The resolver's **authoring** half: the `<name>` namespace (agent OR skill, one flat set), the full key table, and why a key set is derived by grepping the consumer's own prompt rather than declared. Emitted to **no agent** — read it when writing or editing a consumer prompt, or changing `bin/fusion-paths`. Added v5.x.
- **`rules/fusion-workbench-conventions.md` `## Backlog entries — work items`** — The **work item**: one directory per unit of work under `circles/`, the record inside it under the directory's own name and no marker on either, its state the `**Status:**` head field (`open`/`claimed`/`paused`/`done`/`dropped`), its holder the `**Claim:**` field's eight hex characters, its edges the `**Depends-on:**` list of item basenames. Always-on, in the conventions rather than in a file of its own, and the three-sentence maintenance mandate sits beside it. Two rule files went on 260910: the Circle-record rule, carrying the state vocabulary and record templates, retired outright with the container; and the backlog-maintenance rule, folded back in here because the mandate is now three sentences rather than a file. Neither path is written out, because neither resolves — read them at the previous release's tag.
- **`rules/rule-file-provenance.md`** — The **single authoring home** for the `**Provenance:**` header on rule files — placement, the three citation forms, what `provenance-header-lint.test.ts` checks and what it cannot, who carries the obligation. Emitted to **no agent** — read it before creating or editing any file under a `rules/` directory. Added v5.x (same step).
- **`rules/workbench-tracking.md`** — The **single authoring home** for the record-versus-live-state split over a workbench's root entries — which of them a project that tracks its workbench should commit, which it should not, why `.guard-state/` is classified per file rather than per directory, and why the archive roll rather than a ceiling is what preserves the guard event log. Partitioned out of `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout` on 260818, which left a pointer there. Emitted to **no agent** — its two consumers are a human writing a project's `.gitignore` and the archive step of `/fusion:cleanup`, which cites it in `skills/archive/SKILL.md`. Added v10.x (decisions `260816-0711_*_where-does-the-tracked-workbench-split-live-now-that-the-home-it-was-meant-to-move-to-is-gone.md` for the move, `260816-1707_*_to-whom-is-the-new-workbench-tracking-rule-emitted-when-its-consumers-are-a-human-and-a-skill.md` for the emission target).
- **`rules/review-contract.md`** — The **single authoring home** for what a review file contains: the mandated header fields (`**Reviewed-range:**`, which is what `bin/fusion-review-coverage` tiles, `**Not-opened:**` and `**Filed by:**`), the per-topic working files and the final consolidated review, lifted out of the two review prompts at v10.6.0 and outliving them: those two merged into `agents/reviewer.md` at v11. A **conditional** emission, not part of the always-on set: `bin/fusion-rules` emits it behind `IS_REVIEWER_AGENT`, which is set for exactly `reviewer`, and `review-coverage-mandate.test.ts` pins that. Every other agent would be carrying a contract it can never apply.
- **`rules/project-language.md`** — The language cascade's reasoning and edge semantics (deliverable-language source, profile resolution incl. per-family fallback, the settled dashboard reading, fusion's own dual role). Emitted to `editor` only — its deliverable-language halt lives here; the operative core (declarations, fallback chain, four-surface assignment, exempt groups) stays always-on in the conventions' `## Project language`. Partitioned at decision `260827-1056_*_which-parts-of-the-language-and-backlog-rules-does-every-dispatch-still-carry.md`.
- **`rules/orchestrator-rebalance.md`** — The Rebalance gate's per-option mechanics, **Rebalance bounding**, and the re-sharpening contract. Emitted to no agent: read through `$FUSION_PLUGIN_ROOT` at the gate or the re-sharpen trigger; the four option names and the trigger anchors stay in the prompt. Partitioned at decision `260827-1210_*_do-the-rare-orchestrator-flows-stay-in-every-sessions-context.md`.
- **`rules/decision-record-examples.md`** — Worked examples of the decision-marker transitions (_o_→_a_→_i_, _i_→_s_, _o_→_d_). Conditionally emitted since 260827 to the transition agents — `orchestrator`, `shaper`, `planner` and `reconciler`, the `IS_DECISION_TRANSITION_AGENT` case in `bin/fusion-rules` — after the user gate in decision `260827-0830_*_do-the-decision-record-worked-examples-stay-on-the-always-on-floor.md` moved it off the always-on floor to make room for critical-stance §5; every other agent keeps the marker vocabulary (always-on, in the conventions) and the pointer at the template site.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
| `rules/*.md` | The shipped rule corpus: which file authors which topic, and which agents each is emitted to. Detail: `README-agents.md` `## Plugin structure` and `### Adding rules`. |
`````

### L09 — the `templates/`, `docs/` and `skills/` Layout rows leave the table

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Plugin structure`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Plugin structure` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 3167
- **Transform:** rows2bullets
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Each of the three rows carries a do-not-restore-an-inventory instruction that a later editor needs where the editing happens, so the pointer keeps that clause rather than sending the reader away for it.

**Before:**

`````markdown
| `templates/` | Starter files `/fusion:setup` seeds into consuming projects; each has its own destination. **The set is deliberately not written here.** Run `ls templates/`, and read the setup skill for where each file goes — it names the destination beside the copy step, which an inventory in this row cannot. An inventory *did* stand here and went stale twice in one day on 2026-08-15, once per removal, invisibly to both lint gates: a bare filename is not a path, so `reference-resolution-lint` has nothing to resolve, and `derivable-enumerations-lint` carries no check for this row (defect `260815-0803_*_two-claude-md-inventory-rows-went-stale-and-neither-lint-gate-can-see-them.md`). Do not restore one. Two entries left that day: `plane.config.yaml` with the Plane mirror, and the investigator's capture-layout template with the agent that required it. |
| `docs/` | Conceptual docs that are useful both inside Claude Code (pointed at by skills) and as standalone reading. **The set is deliberately not written here.** Run `ls docs/`. An inventory stood in this row and named three files while the tree held four, invisibly: no lint gate resolves a bare filename here, which is the same hole the `templates/` row above records. Two things the command cannot tell you and this row therefore keeps. `philosophy.md` carries the load-bearing ideas and the domain parameter and is what `/fusion:help` points at. The `upgrading-to-vN.md` files are migration notes starting at v9, whose note was the first this repository ever shipped. Not every release has one and no filename is derivable from a version string, which is what `skills/help/SKILL.md`'s update topic tells a reader hunting an older one. A note is not conditional on the release removing something an installed base holds: `docs/upgrading-to-v10-3.md` opens by saying it rewrites nothing and `docs/upgrading-to-v10-4.md` that none of its three new gates can fire in your project. Each is pointed at from `README.md` `## Install`. `/fusion:help`'s update topic names only the last three releases and carries one standing line that sends a reader further behind to `docs/` (`skills/help/SKILL.md`, the update topic); that cap was the user's choice on 260822, over compressing the tail and over raising the `skills/` baseline. A Plane bridge install doc went with the mirror on 2026-08-15. |
| `skills/<name>/SKILL.md` | Skill bodies, one per directory. **How they divide — the ordinary session surface, the commands the end-of-session pipeline became, and the situational rest — is enumerated once, in the skill-bodies bullet under `## What this is`** — this row deliberately does not restate it. A copy stood here and drifted from that bullet twice: it said three and three and named no `post`, and no lint could see it because it named bare skill names rather than `/fusion:` tokens (`260908-1814_*_the-layout-rows-skill-split-still-reads-three-and-three-and-omits-post.md`) — the same hole the `templates/` and `docs/` rows record, and the same answer they took. `ls -1d skills/*/` is the set. Every directory is registered as `/fusion:<name>` whether or not the docs present it as one. |
`````

**After** (what the destination must carry):

`````markdown
- **`templates/`** — Starter files `/fusion:setup` seeds into consuming projects; each has its own destination. **The set is deliberately not written here.** Run `ls templates/`, and read the setup skill for where each file goes — it names the destination beside the copy step, which an inventory in this row cannot. An inventory *did* stand here and went stale twice in one day on 2026-08-15, once per removal, invisibly to both lint gates: a bare filename is not a path, so `reference-resolution-lint` has nothing to resolve, and `derivable-enumerations-lint` carries no check for this row (defect `260815-0803_*_two-claude-md-inventory-rows-went-stale-and-neither-lint-gate-can-see-them.md`). Do not restore one. Two entries left that day: `plane.config.yaml` with the Plane mirror, and the investigator's capture-layout template with the agent that required it.
- **`docs/`** — Conceptual docs that are useful both inside Claude Code (pointed at by skills) and as standalone reading. **The set is deliberately not written here.** Run `ls docs/`. An inventory stood in this row and named three files while the tree held four, invisibly: no lint gate resolves a bare filename here, which is the same hole the `templates/` row above records. Two things the command cannot tell you and this row therefore keeps. `philosophy.md` carries the load-bearing ideas and the domain parameter and is what `/fusion:help` points at. The `upgrading-to-vN.md` files are migration notes starting at v9, whose note was the first this repository ever shipped. Not every release has one and no filename is derivable from a version string, which is what `skills/help/SKILL.md`'s update topic tells a reader hunting an older one. A note is not conditional on the release removing something an installed base holds: `docs/upgrading-to-v10-3.md` opens by saying it rewrites nothing and `docs/upgrading-to-v10-4.md` that none of its three new gates can fire in your project. Each is pointed at from `README.md` `## Install`. `/fusion:help`'s update topic names only the last three releases and carries one standing line that sends a reader further behind to `docs/` (`skills/help/SKILL.md`, the update topic); that cap was the user's choice on 260822, over compressing the tail and over raising the `skills/` baseline. A Plane bridge install doc went with the mirror on 2026-08-15.
- **`skills/<name>/SKILL.md`** — Skill bodies, one per directory. **How they divide — the ordinary session surface, the commands the end-of-session pipeline became, and the situational rest — is enumerated once, in the skill-bodies bullet under `## What this is`** — this row deliberately does not restate it. A copy stood here and drifted from that bullet twice: it said three and three and named no `post`, and no lint could see it because it named bare skill names rather than `/fusion:` tokens (`260908-1814_*_the-layout-rows-skill-split-still-reads-three-and-three-and-omits-post.md`) — the same hole the `templates/` and `docs/` rows record, and the same answer they took. `ls -1d skills/*/` is the set. Every directory is registered as `/fusion:<name>` whether or not the docs present it as one.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
| `templates/` | Starter files setup seeds into a consuming project. The set is deliberately not written here — run `ls templates/`. Detail: `README-agents.md` `## Plugin structure`. |
| `docs/` | Conceptual docs, including the migration notes. The set is deliberately not written here — run `ls docs/`. Detail: `README-agents.md` `## Plugin structure`. |
| `skills/<name>/SKILL.md` | Skill bodies, one per directory; `ls -1d skills/*/` is the set. Detail: `README-agents.md`, the skills section. |
`````

### L10 — the `fusion-workbench/` Layout row leaves the table

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Where the work persists`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Where the work persists` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 1942
- **Transform:** rows2bullets
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The pointer keeps the authoring home the row names, because that is the pointer a session actually follows.

**Before:**

`````markdown
| `fusion-workbench/` | **Runtime artifact. In *this* repo it is git-tracked**; a consuming project decides for itself and this row is advice neither way. The one thing that ever turned on the tracked state was `260717-0030_*_git-stash-include-untracked-can-sweep-the-stash-directory.md`, closed on 260810 and moot since 260815 with the skill it was about. **A tracked-file count stood here until 260813 and was deleted rather than re-measured**, because a hand-written inventory of a directory every session writes to is wrong the day after it is written. Do not restore one. Created by `/fusion:setup`. Safe to delete. **A work item keeps its own container**: a directory under `circles/<stamp>-<slug>/` holding the item's record — named after the directory, carrying no marker — plus that item's own copy of the artifact stores. Everything belonging to no item lives in `shared/`. The store keeps the directory name `circles/` and that name outlived its concept: the six-state Circle and the portfolio layer over it went on 260910 and neither returns, while the container survived because its reason did. `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout` is the definition and this row does not restate it; the `## Origin Rule` there decides container against `shared/`, and `bin/fusion-paths` is where that decision is executed. One thing easy to lose: `reviews/` merges the code and onto senders, a third sender `conceptrev` was retired with its agent on 2026-08-15 and older review files still carry it. Filing a work item stays the user's act, by hand or through `/fusion:memo`, and no agent originates one; the orchestrator maintains the store at the user's word, one confirmation per operation. Session/hook state stays at the workbench root; which entries those are is enumerated once, in the tree under `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout`, and a copy here is what drifted. |
`````

**After** (what the destination must carry):

`````markdown
- **`fusion-workbench/`** — **Runtime artifact. In *this* repo it is git-tracked**; a consuming project decides for itself and this row is advice neither way. The one thing that ever turned on the tracked state was `260717-0030_*_git-stash-include-untracked-can-sweep-the-stash-directory.md`, closed on 260810 and moot since 260815 with the skill it was about. **A tracked-file count stood here until 260813 and was deleted rather than re-measured**, because a hand-written inventory of a directory every session writes to is wrong the day after it is written. Do not restore one. Created by `/fusion:setup`. Safe to delete. **A work item keeps its own container**: a directory under `circles/<stamp>-<slug>/` holding the item's record — named after the directory, carrying no marker — plus that item's own copy of the artifact stores. Everything belonging to no item lives in `shared/`. The store keeps the directory name `circles/` and that name outlived its concept: the six-state Circle and the portfolio layer over it went on 260910 and neither returns, while the container survived because its reason did. `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout` is the definition and this row does not restate it; the `## Origin Rule` there decides container against `shared/`, and `bin/fusion-paths` is where that decision is executed. One thing easy to lose: `reviews/` merges the code and onto senders, a third sender `conceptrev` was retired with its agent on 2026-08-15 and older review files still carry it. Filing a work item stays the user's act, by hand or through `/fusion:memo`, and no agent originates one; the orchestrator maintains the store at the user's word, one confirmation per operation. Session/hook state stays at the workbench root; which entries those are is enumerated once, in the tree under `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout`, and a copy here is what drifted.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
| `fusion-workbench/` | Runtime artifact, created by setup, safe to delete; the container-per-work-item layout is defined in `rules/fusion-workbench-conventions.md` `## fusion-workbench Layout`. Detail: `README-agents.md` `## Where the work persists`. |
`````

### L11 — the dispatch-parameters convention leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Dispatch parameters`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Dispatch parameters` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 1863
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The passage itself says the roster is authored once in the destination file, so the move puts the two facts it keeps beside the table they qualify.

**Before:**

`````markdown
- **Dispatch parameters (v1.11.0+)** — a number of agents read run-time parameters off the dispatch prompt as plain markdown `**<Keyword>:**` lines. **The roster is authored once, in `README-agents.md` `## Dispatch parameters`** — that table carries the agent, the line, its values, what happens when it is absent, and who passes it, each row cited to the prompt line it was read against. Do not restate it here; a second copy is how the planner came to be listed as domain-parameterised in four places while `agents/planner.md` never parsed the string. Two facts belong in this file because they shape dispatch decisions rather than describe one agent. First: `**Domain:** <code|data>` changes behaviour in `reconciler` only, defaulting to `code`; the two shaper modes that also read it went at v11 with the record they wrote; `planner` takes `**Executors:** coder, ontocoder, analyst` (default `[coder, ontocoder]`), which the orchestrator passes on **every** planner dispatch with no condition in front of it. The domain-conditional derivation was deleted on 2026-08-15: whether a step needs `analyst` is a question the plan answers, and no caller upstream of the plan holds the input to answer it. Second: `editor`'s `**Deliverable language:** <de|en>` is the one parameter with **no default** — the agent halts when the dispatch names no language rather than falling back to the chat or artifact declaration (decision `260807-2131_*_which-language-governs-a-customer-deliverable.md`, option 3; the customer-deliverable case in `rules/fusion-workbench-conventions.md` `## Project language`). The loudness is the point — a silent default hands the customer a finished document in the wrong language instead of stopping. Any caller dispatching these agents from outside the orchestrator prefixes the dispatch prompt with the appropriate parameter line.
`````

**After** (what the destination must carry):

`````markdown
- **Dispatch parameters (v1.11.0+)** — a number of agents read run-time parameters off the dispatch prompt as plain markdown `**<Keyword>:**` lines. **The roster is authored once, in `README-agents.md` `## Dispatch parameters`** — that table carries the agent, the line, its values, what happens when it is absent, and who passes it, each row cited to the prompt line it was read against. Do not restate it here; a second copy is how the planner came to be listed as domain-parameterised in four places while `agents/planner.md` never parsed the string. Two facts belong in this file because they shape dispatch decisions rather than describe one agent. First: `**Domain:** <code|data>` changes behaviour in `reconciler` only, defaulting to `code`; the two shaper modes that also read it went at v11 with the record they wrote; `planner` takes `**Executors:** coder, ontocoder, analyst` (default `[coder, ontocoder]`), which the orchestrator passes on **every** planner dispatch with no condition in front of it. The domain-conditional derivation was deleted on 2026-08-15: whether a step needs `analyst` is a question the plan answers, and no caller upstream of the plan holds the input to answer it. Second: `editor`'s `**Deliverable language:** <de|en>` is the one parameter with **no default** — the agent halts when the dispatch names no language rather than falling back to the chat or artifact declaration (decision `260807-2131_*_which-language-governs-a-customer-deliverable.md`, option 3; the customer-deliverable case in `rules/fusion-workbench-conventions.md` `## Project language`). The loudness is the point — a silent default hands the customer a finished document in the wrong language instead of stopping. Any caller dispatching these agents from outside the orchestrator prefixes the dispatch prompt with the appropriate parameter line.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **Dispatch parameters** — which agents read a `**<Keyword>:**` line off the dispatch prompt, and what each line does. Detail: `README-agents.md` `## Dispatch parameters`, which is that roster's single authoring home.
`````

### L12 — the rules-loading, output-style and critical-stance conventions leave `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `### Adding rules`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `### Adding rules` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 4879
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Three topics, so three pointer bullets; the criterion makes the topic word the thing a reader searches for.

**Before:**

`````markdown
- **Rules loading** — agents call `"$FUSION_PLUGIN_ROOT/bin/fusion-rules" <agent-name>` and read every emitted path. Plugin files are NOT auto-loaded into agent context; this helper is the only correct discovery path. The helper emits `rules/agent-setup.md` **first** (Circle D) — the factored Setup contract (read-every-emitted-path, the `bin/fusion-paths` `OUT_*`/`SCAN_*` semantics, the exit-3/exit-4 distinction) that every agent prompt points at through a shared Setup pointer — ahead of `fusion-workbench-conventions.md` and the other always-on rules, so an agent reads *how Setup works* before the detailed conventions. `$FUSION_PLUGIN_ROOT` is exported by `hooks/hooks.json` SessionStart. The helper searches three roots (in this order): the plugin's `rules/`, the project's `./rules/` (fusion-agent-specific rules — review priority overrides, anything no other Claude session would care about), and the project's `.claude/rules/` (project-wide rules every Claude session should respect — coding/ontology/normative guidelines). Filename patterns per agent are unchanged. Since v5.x (Circle B) the helper also accepts an optional topic (`fusion-rules <agent> [<topic>]`) and, if a consuming project ships `./rules/context-manifest.yaml`, additionally emits the manifest units whose agent AND topic match — `path` files loaded as today, `skill:<name>` pointers invoked on demand. Absent the manifest, the emitted set is byte-identical to before; the mechanism is authored in `rules/context-manifest.md` and the lean-`CLAUDE.md` convention in `rules/context-lean-claude-md.md`. **In this repo the helpers prefer the work tree** (v5.x, decision `260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen.md` option c): when `bin/fusion-plugin-cwd` says cwd is the plugin's own repo, `bin/fusion-rules` reads the checkout's `./rules/` instead of `$FUSION_PLUGIN_ROOT/rules/`, `bin/fusion-paths` reads the work-tree prompts instead of the install copy's, and `bin/fusion-source-root` hands a skill body the checkout as the root its citations of shipped files open against. That closed the stale-rules failure where a four-day session read the installed v5.8.0 rules while editing v5.9.1 sources ($FUSION_PLUGIN_ROOT pins to the installed copy for the whole session). **The bound is exact and unforgiving:** the check is at cwd with no upward walk, so a session whose cwd is a *subdirectory* of this repo gets the installed copy again, indistinguishable *to the helpers* from a legitimate consuming project — start sessions at the repo root. It is no longer indistinguishable to **you**: `hooks/session-start.ts` warns at SessionStart whenever the workbench root sits above the working directory, which is precisely this condition, and names both directories. The warning is a signal, not a fix — the helpers still resolve against cwd and still read the installed copy.
- **User-facing output style** — `rules/user-facing-output.md` is emitted to the agents the user reads directly (`orchestrator`, `consultant`, `shaper`, `editor`, `curator` — gate `260827-0910_*_does-every-dispatch-carry-the-full-user-facing-style-contract.md`), and reaches any other agent on a dispatch that names `**Audience:** user`; it mandates action-first ordering, plain-English option labels, no undefined jargon, and trailing details/references blocks for status reports, gate prompts, AskUserQuestion text and session summaries. Every other agent carries the chat profile's anti-patterns and the report-to-dispatcher shape via `rules/agent-setup.md` `## Voice profiles`. Agent prompts' "Output Style" sections point at the rule and add only domain-specific bits.
- **Critical stance (v3.21.2+)** — every agent also loads `rules/critical-stance.md` (auto-emitted by `bin/fusion-rules` alongside the always-on files above). Unlike `user-facing-output.md`, which governs *formatting*, this rule governs *reasoning substance*, in these norms: (1) own your errors instead of praising the user who caught them, (2) no premature/half-baked solutions — research the root cause and prefer the clean integrated fix over additive workarounds, (3) calibrated certainty — separate verified from inference from speculation, label the latter two, and never dress an unchecked claim as a checked one, (4) a case split is disjoint and complete or the question is cut wrong, which is what puts the mandatory `**Decidability:**` line in every plan head, (5) a cardinality is enumerated, derived, or commit-stamped — never asserted bare (decision `260826-1252_*_how-does-this-project-keep-a-cardinality-stated-in-prose-true-when-seven-passes-could-not.md`; this sentence stopped counting its own list the day the norm landed). It generalises to every agent (notably the orchestrator) the per-agent honesty lines that the consultation, analysis and review prompts already carried.
`````

**After** (what the destination must carry):

`````markdown
- **Rules loading** — agents call `"$FUSION_PLUGIN_ROOT/bin/fusion-rules" <agent-name>` and read every emitted path. Plugin files are NOT auto-loaded into agent context; this helper is the only correct discovery path. The helper emits `rules/agent-setup.md` **first** (Circle D) — the factored Setup contract (read-every-emitted-path, the `bin/fusion-paths` `OUT_*`/`SCAN_*` semantics, the exit-3/exit-4 distinction) that every agent prompt points at through a shared Setup pointer — ahead of `fusion-workbench-conventions.md` and the other always-on rules, so an agent reads *how Setup works* before the detailed conventions. `$FUSION_PLUGIN_ROOT` is exported by `hooks/hooks.json` SessionStart. The helper searches three roots (in this order): the plugin's `rules/`, the project's `./rules/` (fusion-agent-specific rules — review priority overrides, anything no other Claude session would care about), and the project's `.claude/rules/` (project-wide rules every Claude session should respect — coding/ontology/normative guidelines). Filename patterns per agent are unchanged. Since v5.x (Circle B) the helper also accepts an optional topic (`fusion-rules <agent> [<topic>]`) and, if a consuming project ships `./rules/context-manifest.yaml`, additionally emits the manifest units whose agent AND topic match — `path` files loaded as today, `skill:<name>` pointers invoked on demand. Absent the manifest, the emitted set is byte-identical to before; the mechanism is authored in `rules/context-manifest.md` and the lean-`CLAUDE.md` convention in `rules/context-lean-claude-md.md`. **In this repo the helpers prefer the work tree** (v5.x, decision `260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen.md` option c): when `bin/fusion-plugin-cwd` says cwd is the plugin's own repo, `bin/fusion-rules` reads the checkout's `./rules/` instead of `$FUSION_PLUGIN_ROOT/rules/`, `bin/fusion-paths` reads the work-tree prompts instead of the install copy's, and `bin/fusion-source-root` hands a skill body the checkout as the root its citations of shipped files open against. That closed the stale-rules failure where a four-day session read the installed v5.8.0 rules while editing v5.9.1 sources ($FUSION_PLUGIN_ROOT pins to the installed copy for the whole session). **The bound is exact and unforgiving:** the check is at cwd with no upward walk, so a session whose cwd is a *subdirectory* of this repo gets the installed copy again, indistinguishable *to the helpers* from a legitimate consuming project — start sessions at the repo root. It is no longer indistinguishable to **you**: `hooks/session-start.ts` warns at SessionStart whenever the workbench root sits above the working directory, which is precisely this condition, and names both directories. The warning is a signal, not a fix — the helpers still resolve against cwd and still read the installed copy.
- **User-facing output style** — `rules/user-facing-output.md` is emitted to the agents the user reads directly (`orchestrator`, `consultant`, `shaper`, `editor`, `curator` — gate `260827-0910_*_does-every-dispatch-carry-the-full-user-facing-style-contract.md`), and reaches any other agent on a dispatch that names `**Audience:** user`; it mandates action-first ordering, plain-English option labels, no undefined jargon, and trailing details/references blocks for status reports, gate prompts, AskUserQuestion text and session summaries. Every other agent carries the chat profile's anti-patterns and the report-to-dispatcher shape via `rules/agent-setup.md` `## Voice profiles`. Agent prompts' "Output Style" sections point at the rule and add only domain-specific bits.
- **Critical stance (v3.21.2+)** — every agent also loads `rules/critical-stance.md` (auto-emitted by `bin/fusion-rules` alongside the always-on files above). Unlike `user-facing-output.md`, which governs *formatting*, this rule governs *reasoning substance*, in these norms: (1) own your errors instead of praising the user who caught them, (2) no premature/half-baked solutions — research the root cause and prefer the clean integrated fix over additive workarounds, (3) calibrated certainty — separate verified from inference from speculation, label the latter two, and never dress an unchecked claim as a checked one, (4) a case split is disjoint and complete or the question is cut wrong, which is what puts the mandatory `**Decidability:**` line in every plan head, (5) a cardinality is enumerated, derived, or commit-stamped — never asserted bare (decision `260826-1252_*_how-does-this-project-keep-a-cardinality-stated-in-prose-true-when-seven-passes-could-not.md`; this sentence stopped counting its own list the day the norm landed). It generalises to every agent (notably the orchestrator) the per-agent honesty lines that the consultation, analysis and review prompts already carried.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **Rules loading** — how `bin/fusion-rules` discovers rules, the three roots it searches, and why this repository's helpers prefer the work tree. Detail: `README-agents.md` `### Adding rules`.
- **User-facing output style** — which agents receive the full style contract and what it mandates. Detail: `README-agents.md` `## Plugin structure`.
- **Critical stance** — the five reasoning norms every agent loads. Detail: `README-agents.md` `## Plugin structure`.
`````

### L13 — the retired-guard-rule convention leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-hooks.md` `## Concept`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-hooks.md` `## Concept` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 2016
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The passage is a removal history with one live instruction in it (measure the floor, never state it). The pointer keeps the instruction and sends the history away.

**Before:**

`````markdown
- **No guard rule is emitted any more.** The always-on `protected-path-discipline.md` (under `rules/` until 260812) was the agent-facing half of the protected-path measurement and went with it, as its sibling rule for the branch policy had gone on 260809. It was 10 541 bytes on every one of the sixteen agents' every dispatch. **Measured on 260812, the day it went:** the always-on floor stood at 98 443 bytes per dispatch before the removal, 91 090 of shipped rule text plus this project's 7 353-byte chat voice profile, and the step took 10 420 off that rather than the full 10 541, because it also had to correct a present-tense claim in `rules/critical-stance.md` that the removal made false, at +121 bytes. **The floor as it stands today is deliberately not stated here.** It moves with every rule edit, so any number written into this line is stale before it is committed: by 260813 the two that stood here had each drifted 5 796 bytes low. Measure it when you need it, with `wc -c` over the always-on set — the **unindented** `emit_if_exists` lines in `bin/fusion-rules`, plus the project's chat voice profile. The indented ones are conditional emissions and are not part of the floor; the same `wc -c` over those files gives what they add, which has been about a third again. In this repository `bin/fusion-rules coder | xargs wc -c` prints the same total in one command, because `coder` draws no conditional rule. **Consequence worth knowing, and since 260816 it has shrunk to nothing:** no rule tells a sub-agent what a halt is or how it is cleared, and there is no longer anything to tell. No halt is reachable, the counter that raised one and the script that cleared one are both gone, and a legacy flag a consuming project still carries blocks nothing — the two migration rows at the end of this document say what to do about it. **The always-on set is an explicit `emit_if_exists` list in `bin/fusion-rules`** (not a pattern match), so a new always-on rule needs a line there or it will not load.
`````

**After** (what the destination must carry):

`````markdown
- **No guard rule is emitted any more.** The always-on `protected-path-discipline.md` (under `rules/` until 260812) was the agent-facing half of the protected-path measurement and went with it, as its sibling rule for the branch policy had gone on 260809. It was 10 541 bytes on every one of the sixteen agents' every dispatch. **Measured on 260812, the day it went:** the always-on floor stood at 98 443 bytes per dispatch before the removal, 91 090 of shipped rule text plus this project's 7 353-byte chat voice profile, and the step took 10 420 off that rather than the full 10 541, because it also had to correct a present-tense claim in `rules/critical-stance.md` that the removal made false, at +121 bytes. **The floor as it stands today is deliberately not stated here.** It moves with every rule edit, so any number written into this line is stale before it is committed: by 260813 the two that stood here had each drifted 5 796 bytes low. Measure it when you need it, with `wc -c` over the always-on set — the **unindented** `emit_if_exists` lines in `bin/fusion-rules`, plus the project's chat voice profile. The indented ones are conditional emissions and are not part of the floor; the same `wc -c` over those files gives what they add, which has been about a third again. In this repository `bin/fusion-rules coder | xargs wc -c` prints the same total in one command, because `coder` draws no conditional rule. **Consequence worth knowing, and since 260816 it has shrunk to nothing:** no rule tells a sub-agent what a halt is or how it is cleared, and there is no longer anything to tell. No halt is reachable, the counter that raised one and the script that cleared one are both gone, and a legacy flag a consuming project still carries blocks nothing — the two migration rows at the end of this document say what to do about it. **The always-on set is an explicit `emit_if_exists` list in `bin/fusion-rules`** (not a pattern match), so a new always-on rule needs a line there or it will not load.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **No guard rule is emitted any more** — and how to measure the always-on floor rather than read a number out of this file. Detail: `README-hooks.md` `## Concept`.
`````

### L14 — the growth-bounds convention leaves `## Conventions`, keeping a one-line warning

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-hooks.md` `### Growth bounds on the shipped text`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-hooks.md` `### Growth bounds on the shipped text` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 1191
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** This is the one entry whose pointer is deliberately a warning rather than a bare pointer: it binds every edit to every shipped surface, so the fact that a bound exists is always-on and only the table is topic-bound. An open defect says the destination section does not carry the per-dispatch-path bound it promises (`260916-1320_*_readme-hooks-promises-the-fourth-bound-a-section-of-its-own-and-the-file-has-none.md`); it is the destination's defect, not this entry's, and it is named here so whoever writes the destination sees it.

**Before:**

`````markdown
- **Shipped text carries failing growth bounds (260815).** Editing `agents/*.md`, `skills/*/SKILL.md`, the hook tests or a rule file can turn `npm test` red — not for what the text says, but for how much it grew. **Which surfaces are bounded, at what head-room, at what each is measured from, and what no bound covers is one table in `README-hooks.md` `### Growth bounds on the shipped text`.** Do not copy a figure out of it into this file: every one of them has moved. **The budgets are independent:** shrinking `skills/` buys `agents/` nothing, by construction. A shrink never trips a bound — they measure the rate of addition, which is what the project's own history says the binding constraint is. **One further bound works differently and reaches this file**: `hooks/lib/__tests__/fixtures/dispatch-path.baseline` charges `CLAUDE.md` to all eleven dispatch paths at **zero head-room**, so N bytes added here puts every path N over at once and must be paid for inside the same paths. **The way out of a red bound is a cut, never an edit to a baseline or a head-room constant**; both move only at the events named in `hooks/lib/__tests__/helpers/growth-bound.ts` `## Re-baselining`.
`````

**After** (what the destination must carry):

`````markdown
- **Shipped text carries failing growth bounds (260815).** Editing `agents/*.md`, `skills/*/SKILL.md`, the hook tests or a rule file can turn `npm test` red — not for what the text says, but for how much it grew. **Which surfaces are bounded, at what head-room, at what each is measured from, and what no bound covers is one table in `README-hooks.md` `### Growth bounds on the shipped text`.** Do not copy a figure out of it into this file: every one of them has moved. **The budgets are independent:** shrinking `skills/` buys `agents/` nothing, by construction. A shrink never trips a bound — they measure the rate of addition, which is what the project's own history says the binding constraint is. **One further bound works differently and reaches this file**: `hooks/lib/__tests__/fixtures/dispatch-path.baseline` charges `CLAUDE.md` to all eleven dispatch paths at **zero head-room**, so N bytes added here puts every path N over at once and must be paid for inside the same paths. **The way out of a red bound is a cut, never an edit to a baseline or a head-room constant**; both move only at the events named in `hooks/lib/__tests__/helpers/growth-bound.ts` `## Re-baselining`.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **Growth bounds** — editing `agents/*.md`, `skills/*/SKILL.md`, a rule file or the hook tests can turn `npm test` red for how much the text grew, and this file is charged to all eleven dispatch paths at zero head-room. The way out of a red bound is a cut, never an edit to a baseline. Detail: `README-hooks.md` `### Growth bounds on the shipped text`.
`````

### L15 — the stylometric-profiles convention leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `### Adding rules`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `### Adding rules` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 1246
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The resolution rule itself is always-on elsewhere — `rules/fusion-workbench-conventions.md` `## Project language` — so nothing normative is carried only by the moving text.

**Before:**

`````markdown
- **Two stylometric profile families (v3.16.0+)** — both under `./fusion-workbench/stilwerk/`, each resolved from its own CLAUDE.md declaration line (`en`/`de`) by the rule that owns that resolution and every fallback in it, `rules/fusion-workbench-conventions.md` `## Project language`: `default-voice-<lang>.yaml` is the long-form **writing** profile (narrative outputs: session summaries, consultant reports, analysis reports, spec/plan prose), resolved from `**Artifact language:**` and emitted by `bin/fusion-rules` only for the prose agents. `chat-voice-<lang>.yaml` is the short-form **chat** profile (gate prompts, AskUserQuestion text, status reports, chat replies), resolved from `**Language:**` and emitted for **every** agent; it is a lean blacklist (em-dash/AI-tell anti-patterns) plus a minimal terse-and-direct whitelist, carrying no sentence-length bands so it never fights the length caps in `user-facing-output.md`. The chat profile's apply-to-every-human-readable-line semantics live in `rules/agent-setup.md` `## Voice profiles` (always-on); `user-facing-output.md` `## Style anti-patterns apply to everything` extends them for the user-facing agents that receive it. Setup copies all four files into the project workbench.
`````

**After** (what the destination must carry):

`````markdown
- **Two stylometric profile families (v3.16.0+)** — both under `./fusion-workbench/stilwerk/`, each resolved from its own CLAUDE.md declaration line (`en`/`de`) by the rule that owns that resolution and every fallback in it, `rules/fusion-workbench-conventions.md` `## Project language`: `default-voice-<lang>.yaml` is the long-form **writing** profile (narrative outputs: session summaries, consultant reports, analysis reports, spec/plan prose), resolved from `**Artifact language:**` and emitted by `bin/fusion-rules` only for the prose agents. `chat-voice-<lang>.yaml` is the short-form **chat** profile (gate prompts, AskUserQuestion text, status reports, chat replies), resolved from `**Language:**` and emitted for **every** agent; it is a lean blacklist (em-dash/AI-tell anti-patterns) plus a minimal terse-and-direct whitelist, carrying no sentence-length bands so it never fights the length caps in `user-facing-output.md`. The chat profile's apply-to-every-human-readable-line semantics live in `rules/agent-setup.md` `## Voice profiles` (always-on); `user-facing-output.md` `## Style anti-patterns apply to everything` extends them for the user-facing agents that receive it. Setup copies all four files into the project workbench.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **Voice profiles** — the two stylometric families under `fusion-workbench/stilwerk/`, and which declaration resolves each. Detail: `README-agents.md` `## Plugin structure`.
`````

### L16 — the single-orchestrator advisory leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Invariants`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Invariants` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 878
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Hidden coupling by the preserve list's third category, so it is relocated and never deleted; the pointer keeps the fact that the warning exists, which is the part a session acts on.

**Before:**

`````markdown
- **Single orchestrator per project** (advisory) — fusion has no concurrency lock. Two orchestrators running against the same project simultaneously double-dispatch the same work, interleave their commit history, and write two sessions' rows into one event log with no ordering between them. The most damaging mode is two parallel sessions each completing half of the same task with overlapping edits. Mitigation since v2.8.0: `/fusion:setup` Step 0c checks for an active-session marker (`fusion-workbench/.session-marker`, whose mtime the PostToolUse hook refreshes automatically while a session is in flight — v10.8.0; before that the orchestrator ran a per-Turn heartbeat) and **warns** the user when one is detected — `running` if heartbeat ≤ 10 min ago, `stale` if older. The warning is advisory; the user can still proceed but takes responsibility for sequencing.
`````

**After** (what the destination must carry):

`````markdown
- **Single orchestrator per project** (advisory) — fusion has no concurrency lock. Two orchestrators running against the same project simultaneously double-dispatch the same work, interleave their commit history, and write two sessions' rows into one event log with no ordering between them. The most damaging mode is two parallel sessions each completing half of the same task with overlapping edits. Mitigation since v2.8.0: `/fusion:setup` Step 0c checks for an active-session marker (`fusion-workbench/.session-marker`, whose mtime the PostToolUse hook refreshes automatically while a session is in flight — v10.8.0; before that the orchestrator ran a per-Turn heartbeat) and **warns** the user when one is detected — `running` if heartbeat ≤ 10 min ago, `stale` if older. The warning is advisory; the user can still proceed but takes responsibility for sequencing.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **One orchestrator per project** — advisory; setup warns when another session's marker is live. Detail: `README-agents.md` `## Invariants`.
`````

### L17 — the issues-versus-decisions convention leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Where the work persists`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Where the work persists` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 433
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** The pointer names the rule file rather than the README, because that file is the authoring home and is always-on anyway: the passage here was a second copy of a definition no session lacks.

**Before:**

`````markdown
- **Issues vs decisions (v2.0+)** — defects ("go fix it") go to `$OUT_ISSUE` with markers `_o_/_p_/_c_/_d_`; decisions / open questions ("decide and record") go to `$OUT_DECISION` with the richer `_o_/_a_/_i_/_d_/_s_` vocabulary. Both resolve (via `bin/fusion-paths`) into the item's container, or `shared/` when none is in scope. See `rules/fusion-workbench-conventions.md` for the decision rule and the decision-record template.
`````

**After** (what the destination must carry):

`````markdown
- **Issues vs decisions (v2.0+)** — defects ("go fix it") go to `$OUT_ISSUE` with markers `_o_/_p_/_c_/_d_`; decisions / open questions ("decide and record") go to `$OUT_DECISION` with the richer `_o_/_a_/_i_/_d_/_s_` vocabulary. Both resolve (via `bin/fusion-paths`) into the item's container, or `shared/` when none is in scope. See `rules/fusion-workbench-conventions.md` for the decision rule and the decision-record template.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **Issues vs decisions** — which store each goes to and which marker vocabulary each carries. Detail: `rules/fusion-workbench-conventions.md` `## Issues vs Decisions — when to use which`, which every agent already loads.
`````

### L18 — the SessionStart-output convention leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-hooks.md` `## Files`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-hooks.md` `## Files` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 138
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** **Net negative and said so.** The passage is 138 bytes and the pointer is longer, so this entry costs bytes rather than saving them. It is proposed because the criterion is topic-binding and not size, and it is the entry most worth declining at the gate.

**Before:**

`````markdown
- **SessionStart hook output** — `systemMessage` JSON for user-visible banners; plain stdout is `additionalContext` for the model only.
`````

**After** (what the destination must carry):

`````markdown
- **SessionStart hook output** — `systemMessage` JSON for user-visible banners; plain stdout is `additionalContext` for the model only.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **SessionStart output** — `systemMessage` JSON reaches the user, plain stdout reaches the model. Detail: `README-hooks.md` `## Files`.
`````

### L19 — the work-item convention leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Where the work persists`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Where the work persists` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 1425
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Same reading as L17: the grammar is authored always-on in the conventions and this passage says so itself, so the pointer goes to the authoring home and the Circle history goes to the README.

**Before:**

`````markdown
- **The work item is the unit of work (260910)** — one directory per item under `circles/`, the record inside it under the directory's own name, **no marker on either**, its state the `**Status:**` head field. The five statuses are `open`, `claimed`, `paused`, `done`, `dropped`, and `claimed` is the value none of the marker vocabularies has: it names **which checkout**, compared by equality on the eight hex characters `bin/fusion-identity` prints, which is what stops two people doing one job. A takeover overwrites the field. The grammar is authored once, in `rules/fusion-workbench-conventions.md` `## Backlog entries — work items`, and the maintenance mandate sits beside it. **What it replaced, and what that cost.** From v3 to v10 the unit of work was a *Circle*: a directory under `circles/<stamp>-<slug>/` holding a six-marker record plus its own copy of every store, ranked by a portfolio agent that went at v11, activated through a per-checkout `.active-circle` pointer that never travelled. One distinction went with it and stayed gone: closed-coherent against bounded closure, which rested on a Coherence verdict that no longer exists. A second came back: *deferred* as a live state had no status of its own and mapped to `dropped` until 260915, when it returned as the fifth value, `paused`. `/fusion:migrate` converts a workbench that still has Circles, and it is the one body that names the old layout.
`````

**After** (what the destination must carry):

`````markdown
- **The work item is the unit of work (260910)** — one directory per item under `circles/`, the record inside it under the directory's own name, **no marker on either**, its state the `**Status:**` head field. The five statuses are `open`, `claimed`, `paused`, `done`, `dropped`, and `claimed` is the value none of the marker vocabularies has: it names **which checkout**, compared by equality on the eight hex characters `bin/fusion-identity` prints, which is what stops two people doing one job. A takeover overwrites the field. The grammar is authored once, in `rules/fusion-workbench-conventions.md` `## Backlog entries — work items`, and the maintenance mandate sits beside it. **What it replaced, and what that cost.** From v3 to v10 the unit of work was a *Circle*: a directory under `circles/<stamp>-<slug>/` holding a six-marker record plus its own copy of every store, ranked by a portfolio agent that went at v11, activated through a per-checkout `.active-circle` pointer that never travelled. One distinction went with it and stayed gone: closed-coherent against bounded closure, which rested on a Coherence verdict that no longer exists. A second came back: *deferred* as a live state had no status of its own and mapped to `dropped` until 260915, when it returned as the fifth value, `paused`. `/fusion:migrate` converts a workbench that still has Circles, and it is the one body that names the old layout.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **The work item is the unit of work** — one directory per item, its state a head field, its holder a checkout. Detail: `rules/fusion-workbench-conventions.md` `## Backlog entries — work items`, which every agent already loads.
`````

### L20 — the bus-protocol removal note leaves `## Conventions`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Migration note`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Migration note` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 567
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Pure removal history, and the destination file already carries this repository's migration notes.

**Before:**

`````markdown
- **Bus protocol removed (v3.15.0)** — earlier versions of fusion shipped a workbench-mediated A2A messaging surface under `fusion-workbench/bus/`. It was removed in v3.15.0; concurrent agent sessions exchange work through the user (copy-paste between chat windows) again, the same way they did pre-v3.4. Consultant remains user-initiated; the reviewer files findings as issues, not as bus messages. The bus-era decision records were marked superseded by this removal and have since left the workbench (archived/cleaned); no live record documents the old protocol.
`````

**After** (what the destination must carry):

`````markdown
- **Bus protocol removed (v3.15.0)** — earlier versions of fusion shipped a workbench-mediated A2A messaging surface under `fusion-workbench/bus/`. It was removed in v3.15.0; concurrent agent sessions exchange work through the user (copy-paste between chat windows) again, the same way they did pre-v3.4. Consultant remains user-initiated; the reviewer files findings as issues, not as bus messages. The bus-era decision records were marked superseded by this removal and have since left the workbench (archived/cleaned); no live record documents the old protocol.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
- **The bus protocol is gone** (v3.15.0) — concurrent sessions exchange work through the user. Detail: `README-agents.md` `## Migration note`.
`````

### L21 — the release procedure leaves `CLAUDE.md`, its heading staying as the pointer's anchor

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-agents.md` `## Releasing (new)`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-agents.md` `## Releasing (new)` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 10351
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** This is the criterion's own worked *it moves* example, named in `rules/context-lean-claude-md.md` `### Two worked classifications`. The `## Release process` heading stays as the anchor a reader scans for; only the body moves, which is why the Before text starts at line 70 and not at 68.

**Before:**

`````markdown
Two repos involved: the **plugin** (this repo, `tenzoki/fusion`) and the **marketplace** (`tenzoki/claude-plugins`).

**The marketplace working clone is at `/Users/k1/Projects/productive/claude-plugins`.** That is the clone the release steps below mean. It is a development checkout, not the cache clone `/plugin install` reads: see the note after step 6, which is a separate copy and may not exist at all. At v6.0.0 it did not, so a release can complete correctly while `/plugin install` still serves the previous version locally.

Every release:

0. **Validate first:** `claude plugin validate .` must report **passed** (warnings are fine). This catches broken agent frontmatter before it ships — e.g. an unquoted `:` in a `description` makes the whole frontmatter fail to parse and the agent loads with empty metadata. Also run a smoke test that the default agent resolves under the install mechanism: `claude --plugin-dir . --agent fusion:orchestrator -p "reply SMOKE-OK"` (a wrong agent name aborts Claude immediately at startup). If the release touches the guard, also confirm before tagging that its behaviour was verified against a project root that is **not** this repository (the guard-harness integration tests spawn such roots; a scratch consuming project works too). **The reason for that step changed on 260816 and the step did not.** Until then the write guard's self-detect stand-down made local testing unrepresentative by construction; the stand-down is gone, so the hook is supposed to behave identically in both trees. The check is what establishes that identity against a real consuming root instead of asserting it. **Before tagging, also run `bin/fusion-review-coverage --since <previous tag>` and state its result in the release commit or the session log.** The checks above it ask whether the plugin *loads*; this one asks whether anyone looked at the change, and v7.2.0 went out while its own review pass was still running because nothing in this list asked. It is advisory and never a gate: a release may go out over an uncovered range, with the gap named, per `260815-2109_*_may-a-circle-close-over-an-uncovered-review-range-and-who-decides.md` (option 1). What it buys is that the `no` is visible before the tag is pushed rather than after. **Before tagging, also open the update topic in `skills/help/SKILL.md` `### 4. Update` and bring it to this release:** it carries the last three releases and no more, so a release adds its own paragraph, relabels the ones below it and drops the oldest. Each paragraph is labelled by the install a reader is coming from and describes the releases after it, so the labels move too. This is a shipped surface with its own byte ceiling, and it went unadvanced through two releases running because no step in this list reached it (`260906-2014_*_the-help-topic-on-updates-has-missed-two-releases-and-names-none-of-the-last-three.md`).
1. Bump `.claude-plugin/plugin.json` `version`
2. `git -C <marketplace> pull --rebase origin main` (it can drift if edited from elsewhere)
3. Bump fusion's `version` in `<marketplace>/.claude-plugin/marketplace.json`
4. Commit and push **both** repos
5. **Tag the fusion release** on the commit you just pushed: `git tag -a v<version> -m "fusion v<version>" && git push origin v<version>`. The tag is what `install.sh`'s `FUSION_REF=tags/v<version>` pinning resolves against — skip it and the documented pin is unusable for that version. (Tagging started at v5.5.0; `v5.5.0` was applied retroactively to its release commit.)
6. To pick up the new version locally:
   ```bash
   git -C ~/.claude/plugins/marketplaces/tenzoki-plugins pull origin main
   ```
   Then in Claude Code: `/plugin install fusion@tenzoki-plugins` and `/reload-plugins`.

**And between releases, before rule or guard work in this repo: run `fusion --update` and restart the session.** The work-tree preference (see Layout, `bin/fusion-plugin-cwd`) covers only `bin/fusion-rules`, `bin/fusion-paths` and `bin/fusion-source-root` — and the third of those roots which *documents* a skill body opens — plus, since 260821, the one asset comparison in `/fusion:setup` Step 0e — never which helper it runs, because whether the preference reaches helper resolution is part (c) of decision `260810-1544_*_should-prompt-called-bin-helpers-get-one-guarded-call-convention-and-does-the-work-tree-preference-extend-to-them.md` and is unanswered. The hooks themselves (guard, tracker) always run from the installed copy, pinned for the whole session, so a stale install still means stale guard behaviour while you edit guard sources. Behaviour rule (a) of decision `260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen.md`; it stays documented here even though option (c) landed, for exactly that residual. **The same pin makes any Circle that builds an agent and proves it by running it a two-session shape.** A session's agent and skill roster is read at start from the installed copy and never re-read, so the session that adds `agents/<name>.md` cannot dispatch `Agent(fusion:<name>)`; the proof run belongs to the next session, after `fusion --update` and a restart, or to a headless `claude --plugin-dir . --agent fusion:<name>` call that reaches the work tree outside the orchestrator's dispatch. The curator Circle met this at its own proof step (`260814-1200_*_the-proof-run-cannot-be-dispatched-from-the-session-that-built-the-agent.md`); plan for it rather than discovering it at C-last. The same shape holds one level down, for a `bin/` helper: one added in a session is absent from `$FUSION_PLUGIN_ROOT` until `fusion --update`, so every `[ -x ]` call site takes its miss branch for the rest of that session, and the helper is proven only in the next one (`260825-1329_*_every-session-runs-one-release-behind-on-a-bin-helper-the-same-repository-just-added.md`).

The marketplace **cache clone** at `~/.claude/plugins/marketplaces/<name>/` is the source-of-truth `/plugin install` reads — not the GitHub remote, and not the working clone named at the top of this section. Without the manual `git pull` on that cache clone, version bumps don't propagate locally even after uninstall/reinstall. If it does not exist (it did not at v6.0.0), `/plugin install` cannot see the release at all; `install.sh` is unaffected, because it reads the GitHub tarball directly.

### HTTPS installer (`install.sh`) — the recommended end-user path

`install.sh` at the repo root is a `curl | bash` installer that downloads the plugin tarball over plain HTTPS (`https://github.com/tenzoki/fusion/archive/refs/heads/main.tar.gz`), unpacks it into `~/.fusion`, and writes a `~/.local/bin/fusion` launcher that runs `claude --plugin-dir ~/.fusion --agent fusion:orchestrator`. It sidesteps the three marketplace failure modes for end users: git/SSH cloning, the unreliable plugin cache, and broken uninstall. Modelled on `tenzoki/flight`'s installer (v0.6.0).

fusion-specific invariants the installer relies on:
- **Compiled hooks must be committed.** `hooks/dist/*.js` are in git (`.gitignore` has the `!hooks/dist/` exception) and are self-contained (no external `require`s) — the tarball is runnable with no `npm`/`node_modules`. The installer defensively `rm -rf`s `hooks/node_modules` after copy.
- **`bin/` executables ship with the +x bit** (preserved through tar/`cp -R`).
- **`CLAUDE_PLUGIN_ROOT` resolves to `~/.fusion`** under `--plugin-dir`, so the SessionStart hook's `export FUSION_PLUGIN_ROOT=${CLAUDE_PLUGIN_ROOT}` and all `bin/` helpers work.
- The installer copies plugin assets — the set is the copy loop in `install.sh`, and that loop is the authority rather than a list here — and **no permission file is among them, because a plugin's own permission settings are not read under `--plugin-dir`.** Measured on Claude Code 2.1.226 by placing one identical `permissions.allow` entry in each of two locations: a project's `.claude/settings.local.json` honoured it, a plugin's own did not. fusion shipped such a file at its root for months, sixteen scoped auto-allows granting nothing on either install path; it was deleted on 2026-08-15 rather than annotated, so nothing in the tree looks like a grant while being inert. A fusion session's permissions come entirely from the consuming project's `.claude/` settings, which `/fusion:setup` Step 0g offers once to seed (defect `260810-0326_*_setup-must-seed-claude-settings-because-the-plugin-settings-json-is-not-a-permission-source.md`, decision `260815-0029_*_what-permission-grant-does-setup-seed-when-unlock-becomes-a-setup-step`). **Seed bare tool names only.** The same measurement found every directory-scoped form denied — `Write(fusion-workbench/**)` and each variant of it — even from a settings file that *is* read, so a scoped pattern grants nothing wherever it is written. The installer never copies `CLAUDE.md` or `.gitignore` (dev-only), and defensively drops `hooks/node_modules`.

So there are effectively **four version surfaces** to keep coherent: `plugin.json`, `marketplace.json`, the `install.sh` header comment's `FUSION_REF=tags/v<version>` example, and the same pin example in `README.md` (the surface a user reads first — it drifted for months because the old count stopped at three). The default ref is `heads/main`, so a stale example still doesn't break a plain install — but since step 5 tags every release, the example now names a tag that actually exists and a user who copies it gets exactly that version. Refresh it at each release rather than letting it drift back to a version nobody tagged. The marketplace entry stays as a documented alternative.

A fifth thing to keep coherent is not a version at all, which is why it is the one that slips: `plugin.json`'s `description` and the fusion entry's `description` in `marketplace.json` describe one product out of two repositories, and each install path shows only its own. They drift when a release rewrites one and leaves the other alone, which is what v9.0.0 did: the marketplace copy was brought up to date while the manifest went on describing the previous shape. Nothing catches that, because two version strings are either equal or not while two prose descriptions can both be well-formed and still disagree, so rewrite the pair together in the same release and read the two side by side before pushing.
`````

**After** (what the destination must carry):

`````markdown
Two repos involved: the **plugin** (this repo, `tenzoki/fusion`) and the **marketplace** (`tenzoki/claude-plugins`).

**The marketplace working clone is at `/Users/k1/Projects/productive/claude-plugins`.** That is the clone the release steps below mean. It is a development checkout, not the cache clone `/plugin install` reads: see the note after step 6, which is a separate copy and may not exist at all. At v6.0.0 it did not, so a release can complete correctly while `/plugin install` still serves the previous version locally.

Every release:

0. **Validate first:** `claude plugin validate .` must report **passed** (warnings are fine). This catches broken agent frontmatter before it ships — e.g. an unquoted `:` in a `description` makes the whole frontmatter fail to parse and the agent loads with empty metadata. Also run a smoke test that the default agent resolves under the install mechanism: `claude --plugin-dir . --agent fusion:orchestrator -p "reply SMOKE-OK"` (a wrong agent name aborts Claude immediately at startup). If the release touches the guard, also confirm before tagging that its behaviour was verified against a project root that is **not** this repository (the guard-harness integration tests spawn such roots; a scratch consuming project works too). **The reason for that step changed on 260816 and the step did not.** Until then the write guard's self-detect stand-down made local testing unrepresentative by construction; the stand-down is gone, so the hook is supposed to behave identically in both trees. The check is what establishes that identity against a real consuming root instead of asserting it. **Before tagging, also run `bin/fusion-review-coverage --since <previous tag>` and state its result in the release commit or the session log.** The checks above it ask whether the plugin *loads*; this one asks whether anyone looked at the change, and v7.2.0 went out while its own review pass was still running because nothing in this list asked. It is advisory and never a gate: a release may go out over an uncovered range, with the gap named, per `260815-2109_*_may-a-circle-close-over-an-uncovered-review-range-and-who-decides.md` (option 1). What it buys is that the `no` is visible before the tag is pushed rather than after. **Before tagging, also open the update topic in `skills/help/SKILL.md` `### 4. Update` and bring it to this release:** it carries the last three releases and no more, so a release adds its own paragraph, relabels the ones below it and drops the oldest. Each paragraph is labelled by the install a reader is coming from and describes the releases after it, so the labels move too. This is a shipped surface with its own byte ceiling, and it went unadvanced through two releases running because no step in this list reached it (`260906-2014_*_the-help-topic-on-updates-has-missed-two-releases-and-names-none-of-the-last-three.md`).
1. Bump `.claude-plugin/plugin.json` `version`
2. `git -C <marketplace> pull --rebase origin main` (it can drift if edited from elsewhere)
3. Bump fusion's `version` in `<marketplace>/.claude-plugin/marketplace.json`
4. Commit and push **both** repos
5. **Tag the fusion release** on the commit you just pushed: `git tag -a v<version> -m "fusion v<version>" && git push origin v<version>`. The tag is what `install.sh`'s `FUSION_REF=tags/v<version>` pinning resolves against — skip it and the documented pin is unusable for that version. (Tagging started at v5.5.0; `v5.5.0` was applied retroactively to its release commit.)
6. To pick up the new version locally:
   ```bash
   git -C ~/.claude/plugins/marketplaces/tenzoki-plugins pull origin main
   ```
   Then in Claude Code: `/plugin install fusion@tenzoki-plugins` and `/reload-plugins`.

**And between releases, before rule or guard work in this repo: run `fusion --update` and restart the session.** The work-tree preference (see Layout, `bin/fusion-plugin-cwd`) covers only `bin/fusion-rules`, `bin/fusion-paths` and `bin/fusion-source-root` — and the third of those roots which *documents* a skill body opens — plus, since 260821, the one asset comparison in `/fusion:setup` Step 0e — never which helper it runs, because whether the preference reaches helper resolution is part (c) of decision `260810-1544_*_should-prompt-called-bin-helpers-get-one-guarded-call-convention-and-does-the-work-tree-preference-extend-to-them.md` and is unanswered. The hooks themselves (guard, tracker) always run from the installed copy, pinned for the whole session, so a stale install still means stale guard behaviour while you edit guard sources. Behaviour rule (a) of decision `260806-0015_*_veraltete-regeln-im-eigenen-repo-melden-oder-umgehen.md`; it stays documented here even though option (c) landed, for exactly that residual. **The same pin makes any Circle that builds an agent and proves it by running it a two-session shape.** A session's agent and skill roster is read at start from the installed copy and never re-read, so the session that adds `agents/<name>.md` cannot dispatch `Agent(fusion:<name>)`; the proof run belongs to the next session, after `fusion --update` and a restart, or to a headless `claude --plugin-dir . --agent fusion:<name>` call that reaches the work tree outside the orchestrator's dispatch. The curator Circle met this at its own proof step (`260814-1200_*_the-proof-run-cannot-be-dispatched-from-the-session-that-built-the-agent.md`); plan for it rather than discovering it at C-last. The same shape holds one level down, for a `bin/` helper: one added in a session is absent from `$FUSION_PLUGIN_ROOT` until `fusion --update`, so every `[ -x ]` call site takes its miss branch for the rest of that session, and the helper is proven only in the next one (`260825-1329_*_every-session-runs-one-release-behind-on-a-bin-helper-the-same-repository-just-added.md`).

The marketplace **cache clone** at `~/.claude/plugins/marketplaces/<name>/` is the source-of-truth `/plugin install` reads — not the GitHub remote, and not the working clone named at the top of this section. Without the manual `git pull` on that cache clone, version bumps don't propagate locally even after uninstall/reinstall. If it does not exist (it did not at v6.0.0), `/plugin install` cannot see the release at all; `install.sh` is unaffected, because it reads the GitHub tarball directly.

### HTTPS installer (`install.sh`) — the recommended end-user path

`install.sh` at the repo root is a `curl | bash` installer that downloads the plugin tarball over plain HTTPS (`https://github.com/tenzoki/fusion/archive/refs/heads/main.tar.gz`), unpacks it into `~/.fusion`, and writes a `~/.local/bin/fusion` launcher that runs `claude --plugin-dir ~/.fusion --agent fusion:orchestrator`. It sidesteps the three marketplace failure modes for end users: git/SSH cloning, the unreliable plugin cache, and broken uninstall. Modelled on `tenzoki/flight`'s installer (v0.6.0).

fusion-specific invariants the installer relies on:
- **Compiled hooks must be committed.** `hooks/dist/*.js` are in git (`.gitignore` has the `!hooks/dist/` exception) and are self-contained (no external `require`s) — the tarball is runnable with no `npm`/`node_modules`. The installer defensively `rm -rf`s `hooks/node_modules` after copy.
- **`bin/` executables ship with the +x bit** (preserved through tar/`cp -R`).
- **`CLAUDE_PLUGIN_ROOT` resolves to `~/.fusion`** under `--plugin-dir`, so the SessionStart hook's `export FUSION_PLUGIN_ROOT=${CLAUDE_PLUGIN_ROOT}` and all `bin/` helpers work.
- The installer copies plugin assets — the set is the copy loop in `install.sh`, and that loop is the authority rather than a list here — and **no permission file is among them, because a plugin's own permission settings are not read under `--plugin-dir`.** Measured on Claude Code 2.1.226 by placing one identical `permissions.allow` entry in each of two locations: a project's `.claude/settings.local.json` honoured it, a plugin's own did not. fusion shipped such a file at its root for months, sixteen scoped auto-allows granting nothing on either install path; it was deleted on 2026-08-15 rather than annotated, so nothing in the tree looks like a grant while being inert. A fusion session's permissions come entirely from the consuming project's `.claude/` settings, which `/fusion:setup` Step 0g offers once to seed (defect `260810-0326_*_setup-must-seed-claude-settings-because-the-plugin-settings-json-is-not-a-permission-source.md`, decision `260815-0029_*_what-permission-grant-does-setup-seed-when-unlock-becomes-a-setup-step`). **Seed bare tool names only.** The same measurement found every directory-scoped form denied — `Write(fusion-workbench/**)` and each variant of it — even from a settings file that *is* read, so a scoped pattern grants nothing wherever it is written. The installer never copies `CLAUDE.md` or `.gitignore` (dev-only), and defensively drops `hooks/node_modules`.

So there are effectively **four version surfaces** to keep coherent: `plugin.json`, `marketplace.json`, the `install.sh` header comment's `FUSION_REF=tags/v<version>` example, and the same pin example in `README.md` (the surface a user reads first — it drifted for months because the old count stopped at three). The default ref is `heads/main`, so a stale example still doesn't break a plain install — but since step 5 tags every release, the example now names a tag that actually exists and a user who copies it gets exactly that version. Refresh it at each release rather than letting it drift back to a version nobody tagged. The marketplace entry stays as a documented alternative.

A fifth thing to keep coherent is not a version at all, which is why it is the one that slips: `plugin.json`'s `description` and the fusion entry's `description` in `marketplace.json` describe one product out of two repositories, and each install path shows only its own. They drift when a release rewrites one and leaves the other alone, which is what v9.0.0 did: the marketplace copy was brought up to date while the manifest went on describing the previous shape. Nothing catches that, because two version strings are either equal or not while two prose descriptions can both be well-formed and still disagree, so rewrite the pair together in the same release and read the two side by side before pushing.
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
The two repositories, the seven numbered steps, the tag, the marketplace cache clone, the HTTPS installer's invariants and the five surfaces a release keeps coherent are in `README-agents.md` `## Releasing`, beside `## Adding a new agent`.
`````

### L22 — the troubleshooting table leaves `CLAUDE.md`, its heading staying as the pointer's anchor

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** relocation
- **Citation:** placement criterion `rules/context-lean-claude-md.md` `### Step 2 — ask one question of each passage`, second question: the passage matters only where the work is about this topic. Destination: `README-hooks.md` `## Where to look when something breaks (new)`, assigned by `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md` `### Destination assignment (C4's fourth open question)`.
- **Consequence group:** relocation
- **Constraint removed:** none
- **Destination:** `README-hooks.md` `## Where to look when something breaks (new)` — **outside this agent's remit** (exclusion 5). Written by `coder` at step 8; see `## 9`.
- **Bytes moving:** 9608
- **Transform:** verbatim
- **Revert path:** `git checkout -- CLAUDE.md`
- **Note:** Every row is a preserve-list non-obvious failure mode, so the table is relocated whole and no row is dropped. The plan rules one home rather than a split across two files.

**Before:**

`````markdown
| Symptom | Likely cause |
|---|---|
| `Agent type 'X' not found` / `denied by permission rule 'Agent(...)' from settings` | Agent dispatch with bare name, or `tools:` allowlist missing namespace prefix. Every dispatch must use `<plugin>:<name>` form. |
| Agent reports it can't find a rule file | Rule discovery not run, or the expected file is project-specific — the plugin ships only its own `rules/` directory (the always-on set plus the conditional emissions in `bin/fusion-rules`); domain rules must live in `./rules/` or `.claude/rules/` of the consuming project. Problem 10. |
| Agent halts at Setup with "no fusion workbench found above pwd" | The project never ran `/fusion:setup`, or setup ran in a different directory. Run `/fusion:setup` at the project root once. The setup skill writes `fusion-workbench/.fusion-setup` which all subsequent agents and hooks look for. |
| Stray empty `fusion-workbench/.guard-state/` directories appearing in random folders | Pre-v2.5 behavior. Hooks bootstrapped the workbench from any `process.cwd()` they fired in. Fixed in v2.5: hooks no-op when no `.fusion-setup` marker is found upward. Safe to delete the stray directories. |
| All sub-agents fail to load after a fusion upgrade | Likely a frontmatter-syntax breakage. v2.8.1 added `disallowedTools: [Agent]` to 12 agents based on docs that turned out incorrect for our parser; agent loading broke entirely. Rolled back in v2.8.3. **Lesson:** changes to agent frontmatter beyond `name`/`description`/`tools` need empirical verification (run a sub-agent dispatch end-to-end) before release, not just docs-driven inference. |
| Orchestrator skipped Setup, dashboard never refreshed | "MUST run Setup" in agent prompt was overridden by user task urgency. The fix is `/fusion:setup`. Problem 11. |
| `/plugin install` keeps returning an old version | Marketplace cache clone at `~/.claude/plugins/marketplaces/` is stale; manual git pull required. Problem 12. |
| SessionStart banner not visible | Hook used plain stdout instead of `systemMessage` JSON. Problem 13. |
| SessionStart prints "fusion: restart this session at the project root" | Working as designed: a `fusion-workbench/.fusion-setup` marker was found in an ancestor of the working directory rather than at it. One fusion check still resolves against cwd — the plugin-repo detection in `bin/fusion-plugin-cwd`, which the three work-tree-preferring `bin/` helpers ask — so from a subdirectory it inspects the wrong directory and all three read the installed plugin copy instead of the work tree. Restart at the named root. The warning itself changes no behaviour. Two sharper instances stood here and are gone: the protected-path deny on 260812, and on 260816 the write-tool PreToolUse path computation together with the module that performed it. The issue that tracked the first, `260804-2100_*_from-a-subdirectory-cwd-the-protected-list-matches-nothing-while-fail-closed-still-denies.md`, is moot rather than fixed. |
| Setup prints no work-item hint although the backlog holds items | The backlog store is absent (a workbench set up before it existed), or `SCAN_BACKLOG` was not substituted into Setup's read, which reports itself as a fusion bug rather than as a zero |
| Orchestrator falls back to `Agent(general-purpose)` (denied) when trying to ask the user — at interrupted-session prompt, Coherence gate, Rebalance Grounding sub-flow, etc. | `AskUserQuestion` missing from `agents/orchestrator.md` `tools:` allowlist. Fixed in v3.0.1. The skill body (`/fusion:setup`) had `AskUserQuestion` in its `allowed-tools` so the skill path worked, but the orchestrator agent itself was denied — and sub-agents (shaper, planner) inheriting from the orchestrator would also have been denied if reached. **Cannot recur at HEAD:** the `tools:` line went on 260913 and no agent declares one, so every agent inherits the session's tools and nothing has to be listed anywhere (`260913-0909_*_may-the-orchestrator-reach-every-tool-and-may-an-agent-dispatch-another.md`). |
| Monitor times displayed off by user's TZ offset | `orchestrator-events.jsonl` timestamps are UTC but written by `date -u +%Y-%m-%dT%H:%M:%S` without the `Z` designator. JavaScript `new Date(ts)` parses such strings as local time per ECMA-262, so any rendering via `getHours()/getMinutes()` is wrong by the user's TZ offset. **Fix at consumer:** the `parseUTCTs()` helper in `bin/monitor` (or append `Z` before parsing) — `bin/monitor` now routes the ETA (v3.9.0, `6209645`) and the event-list/warning times through it. **Lesson:** any future tool consuming `orchestrator-events.jsonl` for time-based computation (JS, Python, Go) must defensively parse — the emit convention is intentional (clean strings in the file) but transfers the parsing burden to every consumer. |
| A protected path was changed and the guard wrote it back, halting the write tools | Cannot happen at HEAD. The whole protected-path half — the write-tool deny, the before/after fingerprint, the write-back, the `FUSION_ALLOW_RULES_WRITE` exemption and the `guard.protectedPaths` configuration leaf — was removed on 260812. If you meet this, you are running an installed copy older than that; `fusion --update` and restart, since the hooks always run from the installed copy and are pinned for the whole session. |
| A consuming project is halted and nothing at HEAD could have raised it | Expected, and it is the migration case. A halt raised by a removed mechanism is a value in that project's `escalation.json`, and it outlives the code that wrote it. At this version it blocks nothing: the halt check, the consecutive-block counter, the escalation module and the halt-clearing script all went on 260816, and no shipped code reads or rewrites the file. `/fusion:setup` offers to delete it. All three properties — nothing blocks, nothing rewrites the file, the offer exists — are pinned by `hooks/lib/__tests__/legacy-halt-clearing.test.ts` against both triggers no surviving code produces (`protected_path`, `protected_path_measured`). |
| A consuming project sees an advisory on every tool call naming `fusion-guard.json`, or one naming `guard`, `decisions`, `escalation`, `churn`, `orchestrator.maxTurns` or `orchestrator.dispatchMinutes` | Working as designed, and the two are the v10 migration. The first says that project's root still carries `fusion-guard.json`, which fusion no longer reads a byte of: **copy nothing across** — every setting that file could carry is retired too — and delete it. That instruction was the reverse until 260910, when the Turn budget it told you to rescue was itself retired. The second says the new file still carries a retired key: one of the four top-level ones (`RETIRED_TOP_LEVEL_KEYS` in `hooks/lib/config.ts`) or one of the two retired leaves inside `orchestrator` (`RETIRED_LEAF_KEYS`); delete the key. Nothing else in either file is affected while the advisory stands. `docs/upgrading-to-v10.md` states both at length. |
| `npm test` goes red on a citation, a plan or a compiled file I did not edit | Working as designed, and it is one of the three blocking gates documented in `README-hooks.md` `### Three gates that can fail the suite over text nobody compiled`. `hooks/lib/__tests__/workbench-citation-lint.test.ts` recomputes its corpus from the tree on every run and carries no approvable baseline, so an archive sweep or a newly filed record with a bad citation reddens the suite for somebody who touched nothing — correct the path, or restate the token as a statement rather than a pointer; that cost was accepted rather than mitigated by `260819-1645_*_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md`. `hooks/lib/__tests__/plan-stopping-section-lint.test.ts` fails on a live (`_o_`/`_p_`) plan whose `## Where this work stops` is missing, empty or still the shipped placeholder; it judges presence only. `hooks/lib/__tests__/committed-dist.test.ts` fails when the committed `hooks/dist/` is not the compilation of the committed source — run `npm run build` — or, separately, when the installed TypeScript is not the exact version `hooks/package.json` pins, which the failure names as a toolchain mismatch and not an artifact defect. |
| An agent writes a plan / issue / decision to the workbench root or into the wrong Circle (v4.0.0+) | The prompt named a store directory as a path literal instead of resolving it through `bin/fusion-paths`. Store paths are NOT written in prompts — the agent resolves `$OUT_*` / `$SCAN_*` at Setup step 2 (alongside `bin/fusion-rules`) and the resolver names the item's container or `shared/`. Two guards make a hard-coded literal impossible to ship: the resolver exits 4 (not a silent `KEY=`) when a prompt names a key it can't value, and the path-lint test (`hooks/lib/__tests__/path-literal-lint.test.ts`) fails `npm test` if any type-folder literal reappears in an `agents/*.md` or `skills/*/SKILL.md`. The files allowed to name store literals are enumerated as `DEFINITION_SITES` in that same test — `rules/fusion-workbench-conventions.md`, `bin/fusion-paths`, and the rule file that inherited a definition when the conventions file was partitioned (`workbench-path-resolution.md`). They sit outside the gate's file set by construction, so the list grants nothing; it exists so that a fifth site has to be added by hand rather than arriving because the gate never looked. If a write still lands wrong, check whether the consumer actually calls `fusion-paths <name>` under its own name (agents AND skills share one flat namespace). |
`````

**After** (what the destination must carry):

`````markdown
| Symptom | Likely cause |
|---|---|
| `Agent type 'X' not found` / `denied by permission rule 'Agent(...)' from settings` | Agent dispatch with bare name, or `tools:` allowlist missing namespace prefix. Every dispatch must use `<plugin>:<name>` form. |
| Agent reports it can't find a rule file | Rule discovery not run, or the expected file is project-specific — the plugin ships only its own `rules/` directory (the always-on set plus the conditional emissions in `bin/fusion-rules`); domain rules must live in `./rules/` or `.claude/rules/` of the consuming project. Problem 10. |
| Agent halts at Setup with "no fusion workbench found above pwd" | The project never ran `/fusion:setup`, or setup ran in a different directory. Run `/fusion:setup` at the project root once. The setup skill writes `fusion-workbench/.fusion-setup` which all subsequent agents and hooks look for. |
| Stray empty `fusion-workbench/.guard-state/` directories appearing in random folders | Pre-v2.5 behavior. Hooks bootstrapped the workbench from any `process.cwd()` they fired in. Fixed in v2.5: hooks no-op when no `.fusion-setup` marker is found upward. Safe to delete the stray directories. |
| All sub-agents fail to load after a fusion upgrade | Likely a frontmatter-syntax breakage. v2.8.1 added `disallowedTools: [Agent]` to 12 agents based on docs that turned out incorrect for our parser; agent loading broke entirely. Rolled back in v2.8.3. **Lesson:** changes to agent frontmatter beyond `name`/`description`/`tools` need empirical verification (run a sub-agent dispatch end-to-end) before release, not just docs-driven inference. |
| Orchestrator skipped Setup, dashboard never refreshed | "MUST run Setup" in agent prompt was overridden by user task urgency. The fix is `/fusion:setup`. Problem 11. |
| `/plugin install` keeps returning an old version | Marketplace cache clone at `~/.claude/plugins/marketplaces/` is stale; manual git pull required. Problem 12. |
| SessionStart banner not visible | Hook used plain stdout instead of `systemMessage` JSON. Problem 13. |
| SessionStart prints "fusion: restart this session at the project root" | Working as designed: a `fusion-workbench/.fusion-setup` marker was found in an ancestor of the working directory rather than at it. One fusion check still resolves against cwd — the plugin-repo detection in `bin/fusion-plugin-cwd`, which the three work-tree-preferring `bin/` helpers ask — so from a subdirectory it inspects the wrong directory and all three read the installed plugin copy instead of the work tree. Restart at the named root. The warning itself changes no behaviour. Two sharper instances stood here and are gone: the protected-path deny on 260812, and on 260816 the write-tool PreToolUse path computation together with the module that performed it. The issue that tracked the first, `260804-2100_*_from-a-subdirectory-cwd-the-protected-list-matches-nothing-while-fail-closed-still-denies.md`, is moot rather than fixed. |
| Setup prints no work-item hint although the backlog holds items | The backlog store is absent (a workbench set up before it existed), or `SCAN_BACKLOG` was not substituted into Setup's read, which reports itself as a fusion bug rather than as a zero |
| Orchestrator falls back to `Agent(general-purpose)` (denied) when trying to ask the user — at interrupted-session prompt, Coherence gate, Rebalance Grounding sub-flow, etc. | `AskUserQuestion` missing from `agents/orchestrator.md` `tools:` allowlist. Fixed in v3.0.1. The skill body (`/fusion:setup`) had `AskUserQuestion` in its `allowed-tools` so the skill path worked, but the orchestrator agent itself was denied — and sub-agents (shaper, planner) inheriting from the orchestrator would also have been denied if reached. **Cannot recur at HEAD:** the `tools:` line went on 260913 and no agent declares one, so every agent inherits the session's tools and nothing has to be listed anywhere (`260913-0909_*_may-the-orchestrator-reach-every-tool-and-may-an-agent-dispatch-another.md`). |
| Monitor times displayed off by user's TZ offset | `orchestrator-events.jsonl` timestamps are UTC but written by `date -u +%Y-%m-%dT%H:%M:%S` without the `Z` designator. JavaScript `new Date(ts)` parses such strings as local time per ECMA-262, so any rendering via `getHours()/getMinutes()` is wrong by the user's TZ offset. **Fix at consumer:** the `parseUTCTs()` helper in `bin/monitor` (or append `Z` before parsing) — `bin/monitor` now routes the ETA (v3.9.0, `6209645`) and the event-list/warning times through it. **Lesson:** any future tool consuming `orchestrator-events.jsonl` for time-based computation (JS, Python, Go) must defensively parse — the emit convention is intentional (clean strings in the file) but transfers the parsing burden to every consumer. |
| A protected path was changed and the guard wrote it back, halting the write tools | Cannot happen at HEAD. The whole protected-path half — the write-tool deny, the before/after fingerprint, the write-back, the `FUSION_ALLOW_RULES_WRITE` exemption and the `guard.protectedPaths` configuration leaf — was removed on 260812. If you meet this, you are running an installed copy older than that; `fusion --update` and restart, since the hooks always run from the installed copy and are pinned for the whole session. |
| A consuming project is halted and nothing at HEAD could have raised it | Expected, and it is the migration case. A halt raised by a removed mechanism is a value in that project's `escalation.json`, and it outlives the code that wrote it. At this version it blocks nothing: the halt check, the consecutive-block counter, the escalation module and the halt-clearing script all went on 260816, and no shipped code reads or rewrites the file. `/fusion:setup` offers to delete it. All three properties — nothing blocks, nothing rewrites the file, the offer exists — are pinned by `hooks/lib/__tests__/legacy-halt-clearing.test.ts` against both triggers no surviving code produces (`protected_path`, `protected_path_measured`). |
| A consuming project sees an advisory on every tool call naming `fusion-guard.json`, or one naming `guard`, `decisions`, `escalation`, `churn`, `orchestrator.maxTurns` or `orchestrator.dispatchMinutes` | Working as designed, and the two are the v10 migration. The first says that project's root still carries `fusion-guard.json`, which fusion no longer reads a byte of: **copy nothing across** — every setting that file could carry is retired too — and delete it. That instruction was the reverse until 260910, when the Turn budget it told you to rescue was itself retired. The second says the new file still carries a retired key: one of the four top-level ones (`RETIRED_TOP_LEVEL_KEYS` in `hooks/lib/config.ts`) or one of the two retired leaves inside `orchestrator` (`RETIRED_LEAF_KEYS`); delete the key. Nothing else in either file is affected while the advisory stands. `docs/upgrading-to-v10.md` states both at length. |
| `npm test` goes red on a citation, a plan or a compiled file I did not edit | Working as designed, and it is one of the three blocking gates documented in `README-hooks.md` `### Three gates that can fail the suite over text nobody compiled`. `hooks/lib/__tests__/workbench-citation-lint.test.ts` recomputes its corpus from the tree on every run and carries no approvable baseline, so an archive sweep or a newly filed record with a bad citation reddens the suite for somebody who touched nothing — correct the path, or restate the token as a statement rather than a pointer; that cost was accepted rather than mitigated by `260819-1645_*_what-defines-the-citation-gates-corpus-and-what-happens-when-a-marker-move-changes-it.md`. `hooks/lib/__tests__/plan-stopping-section-lint.test.ts` fails on a live (`_o_`/`_p_`) plan whose `## Where this work stops` is missing, empty or still the shipped placeholder; it judges presence only. `hooks/lib/__tests__/committed-dist.test.ts` fails when the committed `hooks/dist/` is not the compilation of the committed source — run `npm run build` — or, separately, when the installed TypeScript is not the exact version `hooks/package.json` pins, which the failure names as a toolchain mismatch and not an artifact defect. |
| An agent writes a plan / issue / decision to the workbench root or into the wrong Circle (v4.0.0+) | The prompt named a store directory as a path literal instead of resolving it through `bin/fusion-paths`. Store paths are NOT written in prompts — the agent resolves `$OUT_*` / `$SCAN_*` at Setup step 2 (alongside `bin/fusion-rules`) and the resolver names the item's container or `shared/`. Two guards make a hard-coded literal impossible to ship: the resolver exits 4 (not a silent `KEY=`) when a prompt names a key it can't value, and the path-lint test (`hooks/lib/__tests__/path-literal-lint.test.ts`) fails `npm test` if any type-folder literal reappears in an `agents/*.md` or `skills/*/SKILL.md`. The files allowed to name store literals are enumerated as `DEFINITION_SITES` in that same test — `rules/fusion-workbench-conventions.md`, `bin/fusion-paths`, and the rule file that inherited a definition when the conventions file was partitioned (`workbench-path-resolution.md`). They sit outside the gate's file set by construction, so the list grants nothing; it exists so that a fifth site has to be added by hand rather than arriving because the gate never looked. If a write still lands wrong, check whether the consumer actually calls `fusion-paths <name>` under its own name (agents AND skills share one flat namespace). |
`````

**Pointer left behind** (what replaces the passage in `CLAUDE.md`):

`````markdown
The nineteen symptom rows — what each failure looks like and what causes it — are in `README-hooks.md` `## Where to look when something breaks`.
`````

## 8. Candidates — not on offer, never applied

### C01 — `CLAUDE.md` line 21 says the enumeration lint asserts a direction no gate asserts

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, the last sentence of the skill-bodies bullet
- **Tier:** 1 (falsified claim)
- **Citation:** `da1c62ed` deleted `claudeMdDrift()` and its two `it` blocks from
  `hooks/lib/__tests__/derivable-enumerations-lint.test.ts`. The surviving closed enumeration asserts set
  equality against `README-agents.md`, not against `CLAUDE.md`. The open-set half over `CLAUDE.md` survives.
  The sentence claims the gate "asserts the match in the other direction too", and no gate does.
- **Consequence group:** candidate
- **Why it is a candidate:** an open defect already owns it —
  `260916-1315_*_the-retired-gates-redundancy-argument-covers-one-direction-and-claude-md-still-claims-the-other.md` —
  and its acceptance test binds the repair to a test change in the same commit ("either the closed direction
  over `CLAUDE.md` is asserted again, or `CLAUDE.md`'s sentence stops claiming it — and whichever lands, the
  two agree in the same commit"). The test file is exclusion 5. A text-only correction here would satisfy half
  of an acceptance test and leave the gate question unanswered, so it is reported rather than offered.
- **Interaction with L04:** L04 moves the whole bullet, this sentence included, as it reads. The relocation
  makes no claim about the sentence's truth and does not repair it. If the defect is closed by the second
  branch of its own test, the repair lands in `README-agents.md` after the move and is that defect's work.

### C02 — the pointer lines are left in place rather than collected into one table

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`
- **Tier:** consolidation
- **Citation:** `rules/context-lean-claude-md.md` `### Step 3 — what a passage that fails the test becomes`:
  "Collect the pointer lines into the one table described under *What stays always-on* above, rather than
  leaving each one stranded where its section used to be."
- **Consequence group:** candidate
- **Why it is a candidate:** its Before text does not exist yet. The collected table can only be written once
  the 22 relocations have landed and the pointers are on disk, so proposing it now would put a Before block in
  the ledger that no disk read could match, and the apply pass would mark it stale by its own rule. It is named
  here so the work is not lost: after step 8, the residue is a scatter of in-place pointers in three sections,
  and one table would read better than three scatters.

## 9. Work outside this agent's remit, reported rather than done

- **Every destination write.** `README-agents.md` and `README-hooks.md` are `README*.md`, exclusion 5. The
  executor is `coder`, at step 8 of `260916-1126_*_implementation-human-facing-docs-leave-claude-md.md`, which
  already sequences it: destinations first, byte for byte from this ledger, then the curator's apply pass.
  The source-side removal is held `stale` for as long as a destination does not carry its After text, so no
  passage ever exists nowhere. **A defect record is not filed for this**, and the reason is stated rather than
  assumed: the requirement is already carried by an approved plan step with a named executor, and a second
  record would be the duplication `rules/fusion-workbench-conventions.md` `## Record filing` refuses.
- **Two new section headings** — `README-agents.md` `## Releasing` and `README-hooks.md`
  `## Where to look when something breaks` — do not exist yet. Same executor, same step.
- **Gates that read `CLAUDE.md` and will move when it does.** Named here because the run report owes the
  owner, not the warning; the suite goes red loudly on the next run. `hooks/lib/__tests__/derivable-enumerations-lint.test.ts`
  (three agent-count claims, the skill-token surface list, the path-literal definition sites),
  `hooks/lib/__tests__/reference-resolution-lint.test.ts` (baseline re-approval),
  `hooks/lib/__tests__/workbench-citation-lint.test.ts` (every citation that moves must still resolve),
  and `hooks/lib/__tests__/fixtures/dispatch-path.baseline` (eleven rows re-measured). All are **coder work**,
  and step 6 of the plan already holds the wording for each. This run runs none of them.
- **`/fusion:curate` passes no `**Placement:** on` line**, so a run under that command proposes no relocation
  at all — which is what step 7 of the plan names as its route. Already filed as
  `260916-1326_*_step-7-runs-fusion-curate-which-passes-no-placement-line-so-it-can-propose-no-relocation.md`.
  This run reached placement because the orchestrator dispatched the agent directly with the line.

## 10. Net byte effect

| Measure | Bytes |
|---|---|
| `CLAUDE.md` today | 62505 |
| Moving, across 22 relocation entries | 59308 |
| Staying text (measured, before pointers) | 3197 |
| Pointer lines left behind | 4917 |
| `CLAUDE.md` if every entry is approved | 8114 |
| Saving on each of the eleven dispatch paths | 54391 |

The residue lands at 8114 bytes. **That is above the 5 000 to 7 000 the plan
inferred, by about 1114 bytes, and the overshoot is reported rather than
closed.** Nothing was sized against that range — the plan's C1 decision is that there is no target size and
says no step is measured against it — so the gap is a fact about the classification, not a miss. Where it
comes from is the 4917 bytes of pointer text: 22 entries leave 27 pointer lines, and a pointer that
names its topic, its destination file and its section is not short. The gate can shorten any of them, and
declining an entry returns its passage rather than its pointer.

## 11. Blast radius

**The stop fires, and by a wide margin.** 59308 bytes of 62505 is 94.9 percent of
`CLAUDE.md`, against a threshold of twenty. Relocations count toward it in full, because the passage leaves
the surface a session reads whether or not it survives at the destination. **No deletion is proposed at all:**
the deletion half of the measure is zero and the whole 94.9 percent is relocation, so
what the pause is being asked to confirm is the scale of a move and never the scale of a loss.

This is the scale the work item asked for, and the pause is still owed: the user confirms the scale in its
own prompt, before any ledger count is shown.

## 12. Constraints removed, one searchable line each

**None.** Every entry is a relocation and no constraint leaves the project. This section is written rather
than omitted, so a later grep for a rule that vanished finds the statement that none did.

## 13. Outcomes

Not yet run. This is a survey pass; outcomes are appended by an apply pass dispatched with this file's path
and an approved id set.
