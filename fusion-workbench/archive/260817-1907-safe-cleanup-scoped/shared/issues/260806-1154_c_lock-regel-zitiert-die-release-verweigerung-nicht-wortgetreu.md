Die Lock-Regel zitiert die Release-Verweigerung nicht wortgetreu — der zitierte String existiert im Code nicht
---
`rules/workbench-stash-and-lock.md:142` beschreibt die zwei Release-not-held-Meldungen als Zitate: „`not currently held by anyone`" und „a refusal (`lock directory exists but records no holder; refusing to guess`)". Die erste ist ein echter Substring der Code-Meldung (`bin/fusion-commit-lock:245`). Die zweite nicht: der Code sagt „lock directory **fusion-workbench/.commit-lock** exists but records no holder; refusing to guess…" (`bin/fusion-commit-lock:243`) — der Pfad steht mitten im zitierten Span. Wer die zitierte Zeile greppt (in Code oder Logs), findet nichts; ein backtick-gesetztes Zitat, das kein Substring der wirklichen Meldung ist, ist dieselbe Klasse „Text sagt nicht, was der Code tut" im Kleinsten. In dieser Review gemessen (Release auf holderloses Verzeichnis, Meldung wörtlich mitgeschnitten).

Fix-Richtung: entweder das Zitat mit Platzhalter schreiben („`lock directory <path> exists but records no holder; refusing to guess`") oder auf den grep-stabilen Teilstring kürzen („`records no holder; refusing to guess`").
---
Kontext: Low-Befund einer Implementation-vs-Intention-Review über `4a8fea0..cde5319`. Die Doku-Angleichung der Lock-Regel in `fbd8c4d` (geschlossener Befund `circles/260805-2005-textschicht-gegen-code-nachziehen/issues/260806-1030_*_lock-regel-mechanism-beschreibt-den-stale-detektor-ohne-die-neue-holderlose-alterung.md`) hat den Mechanism-Absatz nachgezogen, dieses Zitat im Failure-modes-Absatz aber nicht. Kein Lint prüft Meldungs-Strings gegen den Code — bewusst außerhalb des Referenz-Lints, daher hier als Einzelbefund.

---
Resolved: 2026-08-06, coder — Zitat in rules/workbench-stash-and-lock.md (Failure modes, Release-not-held) auf den grep-stabilen Teilstring gekürzt: „a refusal (`records no holder; refusing to guess`, the message naming the lock directory's path in full)". Beide zitierten Spans sind jetzt wörtliche Substrings der Code-Meldungen (bin/fusion-commit-lock:243/245); Skript unverändert. Emissions-Golden regeneriert per dokumentierter Prozedur (UPDATE_RULES_GOLDEN=1-Lauf, dann Lauf ohne Flag; Fixture-Diff: nur workbench-stash-and-lock.md 11180→11208, Orchestrator-Total 112605→112633). Suite 1611/1611 grün.
